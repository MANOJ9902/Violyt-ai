from __future__ import annotations




import logging
import base64
import json
import re
from copy import deepcopy
from enum import Enum
from io import BytesIO
from time import perf_counter
from typing import Any
from urllib.parse import parse_qs, urlparse
from uuid import uuid4

from PIL import Image, ImageDraw, ImageFilter, ImageStat
from pydantic import ValidationError

from app.ai.blueprint import BlueprintService
from app.ai.brand_intelligence import BrandIntelligenceService
from app.ai.context_resolution import ContextResolutionService
from app.ai.context_compiler import ContextCompilerService
from app.ai.contracts import (
    AIOrchestrationRequest,
    AIOrchestrationResponse,
    BlueprintPayload,
    CreativeDecisionPayload,
    GenerationSceneGraph,
    GeneratedImageAsset,
    GenerationTrace,
    MessageStrategyPayload,
    SceneGraphGeometry,
    SceneGraphValidationIssue,
    SceneGraphValidationReport,
    StructuredTextPayload,
)
from app.ai.guardrails import GuardrailService
from app.ai.prompt_intelligence import PromptIntelligenceService
from app.ai.providers.base import PromptEnvelope
from app.ai.providers.router import ProviderRouter
from app.ai.template_vision import TemplateVisionAnalyzer
from app.core.config import get_settings
from app.core.exceptions import GenerationFailureError
from app.ai.tone_intelligence import ToneIntelligenceService
from app.integrations.object_storage import get_object_storage
from app.services.content_planning import ContentPlanningService
from app.services.generation_trace import GenerationTraceService
from app.utils.input_access_tracking import InputAccessTracker
from app.utils.footer_safe_area import calculate_footer_safe_area
from app.services.research_editorial_planning import ResearchEditorialPlanningService
from app.utils.palette_roles import derive_palette_roles, hex_to_rgb, is_soft_neutral_color, normalize_hex

logger = logging.getLogger(__name__)

# Pipeline map: API/worker code builds AIOrchestrationRequest, this service compiles context, asks providers for plans/copy/images,
# validates or repairs the results, then returns AIOrchestrationResponse for persistence and rendering.


class GenerationStrategy(Enum):
    # Lists the coarse routes available before a request enters the full generation pipeline.
    # select_generation_engine returns one of these values so later branches know whether templates or references are authoritative.
    STANDARD_GENERATION = "standard_generation"
    STATIC_INFOGRAPHIC_REFERENCE = "static_infographic_reference"
    TEMPLATE_ADAPTANCE = "template_adaptance"
    CONTENT_INTELLIGENCE = "content_intelligence"
    WITHOUT_REFERENCE = "without_reference"


def select_generation_engine(
    format_type: Any,
    pin: Any,
    auto: Any,
    has_sample_creative: Any,
    has_template: Any,
) -> GenerationStrategy:
    # Chooses the first route from format, pin/auto flags, and whether a sample creative or template is available.
    # The enum result selects standard, template-adapted, content-intelligence, or no-reference handling.
    normalized_format = str(format_type or "").strip().casefold()
    is_pinned = bool(pin)
    is_auto = bool(auto)
    sample_available = bool(has_sample_creative)
    template_available = bool(has_template)

    if not sample_available and not template_available:
        return GenerationStrategy.WITHOUT_REFERENCE

    if normalized_format in {"static", "infographic"}:
        return GenerationStrategy.STATIC_INFOGRAPHIC_REFERENCE

    if normalized_format == "carousel":
        if is_pinned:
            return GenerationStrategy.TEMPLATE_ADAPTANCE
        if is_auto and not is_pinned:
            return GenerationStrategy.CONTENT_INTELLIGENCE

    return GenerationStrategy.STANDARD_GENERATION


class AIOrchestratorService:
    # Coordinates context, planning, copy, scene graphs, images, validation, rendering, tracing, and response assembly.
    # Workers call this service with AIOrchestrationRequest and persist the AIOrchestrationResponse it returns.
    # Main flow: resolve inputs -> compile context -> plan messaging/creative -> validate/repair -> render assets -> build response.
    IMAGE_PROMPT_MAX_LENGTH = 12000
    DEFAULT_CAROUSEL_IMAGE_PROMPT_MAX_LENGTH = 12000
    CAROUSEL_IMAGE_PROMPT_MAX_LENGTH = 21000
    CAROUSEL_STRICT_SAMPLE_IMAGE_PROMPT_MAX_LENGTH = 19000
    # These thresholds decide when the pipeline trusts generated visual output versus asking for another repair attempt.
    SAMPLE_SIMILARITY_ACCEPT_SCORE = 0.82
    SCENE_GRAPH_REPAIR_ATTEMPTS = 2
    CONTENT_SEMANTIC_REPAIR_ATTEMPTS = 1
    IMAGE_QUALITY_MIN_SCORE = 0.72
    # Format sets split responsibility between the image model, backend text overlay, and template/reference adaptation branches.
    AI_FINAL_RENDER_FORMATS = {"static", "story", "poster", "carousel", "infographic"}
    BACKEND_TEXT_OVERLAY_SCENE_GRAPH_FORMATS = {"carousel"}
    TEMPLATE_LOCKED_IMAGE_LED_FORMATS = {"static", "story", "poster"}
    DEFAULT_LAYERS = ["background", "decorative", "primary_visual", "content", "brand", "footer"]
    # These prompt words are ignored when checking whether the model merely echoed the user's instruction.
    PROMPT_ECHO_STOPWORDS = {
        "a",
        "an",
        "and",
        "book",
        "create",
        "design",
        "engaging",
        "for",
        "generate",
        "instagram",
        "lower",
        "make",
        "media",
        "post",
        "prompt",
        "shares",
        "social",
        "strategies",
        "that",
        "the",
        "tips",
        "to",
        "write",
    }
    TOPIC_ANCHOR_STOPWORDS = PROMPT_ECHO_STOPWORDS | {
        "audience",
        "brand",
        "carousel",
        "campaign",
        "confidence",
        "conversational",
        "copy",
        "friend",
        "headline",
        "indian",
        "intelligent",
        "journey",
        "length",
        "linkedin",
        "platform",
        "option",
        "options",
        "premium",
        "scannable",
        "slide",
        "slides",
        "smart",
        "smarter",
        "swipe",
        "tone",
        "tradeoffs",
        "visual",
        "wealth",
    }
    TOPIC_MATCH_NOISE_TOKENS = TOPIC_ANCHOR_STOPWORDS | {
        "accent",
        "adapted",
        "asset",
        "assets",
        "background",
        "body",
        "brief",
        "carousel",
        "color",
        "colors",
        "composition",
        "context",
        "creative",
        "creatives",
        "cta",
        "design",
        "editable",
        "export",
        "flat",
        "font",
        "format",
        "generated",
        "headline",
        "image",
        "infographic",
        "inter",
        "jiraaf",
        "label",
        "layout",
        "linkedin",
        "logo",
        "marketing",
        "metadata",
        "palette",
        "pdf",
        "platform",
        "primary",
        "reference",
        "social",
        "style",
        "surface",
        "template",
        "text",
        "trusted",
        "typography",
        "visual",
        "warning",
        "zone",
        "zones",
    }
    VISUAL_METADATA_COMPATIBILITY_STOPWORDS = TOPIC_ANCHOR_STOPWORDS | {
        "accent",
        "background",
        "brand-safe",
        "clean",
        "composition",
        "creative",
        "deep",
        "design",
        "direction",
        "editorial",
        "hero",
        "image",
        "layout",
        "look",
        "mood",
        "modern",
        "no",
        "palette",
        "polished",
        "poster",
        "premium",
        "prompt",
        "safe",
        "scene",
        "spacing",
        "style",
        "surface",
        "system",
        "text",
        "visuals",
        "warm",
        "with",
        "without",
    }
    CONTENT_ALLOCATION_STOPWORDS = TOPIC_ANCHOR_STOPWORDS | {
        "actually",
        "after",
        "around",
        "because",
        "beyond",
        "bigger",
        "cleanly",
        "detail",
        "details",
        "exactly",
        "explain",
        "explains",
        "generic",
        "hidden",
        "just",
        "keep",
        "matters",
        "missed",
        "more",
        "most",
        "next",
        "only",
        "reader",
        "really",
        "slide",
        "slides",
        "story",
        "than",
        "that",
        "their",
        "them",
        "there",
        "these",
        "they",
        "this",
        "those",
        "through",
        "under",
        "what",
        "when",
        "where",
        "which",
        "while",
        "why",
        "will",
        "with",
    }
    VISUAL_METADATA_FIELD_LIMITS = {
        "visual_direction": 180,
        "design_style": 80,
        "image_prompt": 220,
    }
    DATA_VISUAL_TERMS = (
        "table",
        "tabular",
        "chart",
        "graph",
        "bar chart",
        "bar graph",
        "line chart",
        "line graph",
        "pie chart",
        "donut chart",
        "area chart",
        "axis",
        "axes",
        "legend",
        "matrix",
        "comparison grid",
        "dashboard",
        "scorecard",
        "metric module",
        "metrics module",
        "timeline",
        "flowchart",
        "flow chart",
        "data visualization",
        "data visualisation",
        "data viz",
    )
    NUMERIC_DATA_VISUAL_TERMS = (
        "bar chart",
        "bar graph",
        "line chart",
        "line graph",
        "pie chart",
        "donut chart",
        "area chart",
        "axis",
        "axes",
        "legend",
        "scale",
        "percentage",
        "percent",
        "%",
        "currency",
        "ranked",
        "ranking",
        "time series",
        "trend line",
    )
    STRUCTURED_VISUAL_METADATA_SCHEMA_VERSION = "visual_metadata_v1"
    VISUAL_TREATMENT_PREFERENCE_VALUES = (
        "2d",
        "3d",
        "isometric",
        "flat_editorial",
        "product_dashboard",
        "icon_system",
        "photo_composite",
        "document_evidence",
        "data_module",
        "brand_led",
    )
    STRUCTURED_VISUAL_PRIMARY_ANCHOR_KEYS = (
        "stat_highlights",
        "claim_evidence_pairs",
        "proof_points",
        "body_points",
        "verified_facts",
        "evidence",
        "verified_evidence",
        "approved_evidence",
    )
    STRUCTURED_VISUAL_NESTED_ANCHOR_KEYS = (
        "stat_highlights",
        "claim_evidence_pairs",
        "proof_points",
        "verified_facts",
        "evidence",
        "verified_evidence",
        "approved_evidence",
    )
    STRUCTURED_VISUAL_APPROVED_ANCHOR_SOURCES = frozenset(
        {
            "user_prompt",
            "user-provided",
            "user_provided",
            "stat_highlight",
            "stat_highlights",
            "nested_stat_highlights",
            "claim_evidence",
            "claim_evidence_pairs",
            "nested_claim_evidence_pairs",
            "proof_point",
            "proof_points",
            "nested_proof_points",
            "body_point",
            "body_points",
            "verified",
            "verified_fact",
            "verified_facts",
            "nested_verified_facts",
            "evidence",
            "nested_evidence",
            "verified_evidence",
            "nested_verified_evidence",
            "approved_evidence",
            "nested_approved_evidence",
        }
    )
    DISALLOWED_GLYPH_PATTERN = re.compile(
        r"[\u2600-\u27BF\U0001F300-\U0001FAFF\ufe0e\ufe0f\u200d]"
    )
    MOJIBAKE_SYMBOL_PATTERN = re.compile(r"(?:âœ”ï¸\x8f|âœ”ï¸|âœ…|âž¡ï¸|ï¸|â€¢)")

    FALLBACK_COPY_INSTRUCTION_PREFIXES = (
        "address ",
        "anchor ",
        "back ",
        "build ",
        "connect ",
        "encourage ",
        "frame ",
        "ground ",
        "invite ",
        "keep ",
        "lead ",
        "prioritize ",
        "reinforce ",
        "show ",
        "start ",
        "translate ",
        "use ",
    )
    FALLBACK_COPY_DESCRIPTOR_MARKERS = (
        "-led",
        "brand-safe",
        "cta",
        "headline",
        "hook",
        "messaging",
        "outcome-first",
        "phrasing",
        "supporting copy",
        "supporting line",
    )
    OBJECTION_RESPONSE_MARKERS = (
        "backed",
        "guided",
        "no need",
        "plain english",
        "plain-english",
        "supported",
        "transparent",
        "without",
    )
    OBJECTION_RESPONSE_INSTRUCTION_PREFIXES = (
        "address ",
        "answer ",
        "handle ",
        "respond to ",
        "reassure ",
    )
    MISTAKE_CAROUSEL_SIGNAL_PATTERN = re.compile(
        r"\b(mistake|mistakes|pitfall|pitfalls|error|errors|wrong|misstep|missteps|avoid|avoiding|costly)\b",
        re.IGNORECASE,
    )
    MULTI_MISTAKE_SIGNAL_PATTERN = re.compile(
        r"\b(top|common|most|costly|biggest|these)\b[\w\s-]{0,40}\b(mistakes|pitfalls|errors|missteps)\b|\b(mistakes|pitfalls|errors|missteps)\b",
        re.IGNORECASE,
    )
    MISTAKE_GROUP_START_PATTERN = re.compile(
        r"^(?:mistake|mistakes|pitfall|pitfalls|error|errors|wrong move|misstep)\s*:\s*",
        re.IGNORECASE,
    )
    MISTAKE_WHY_PATTERN = re.compile(
        r"^(?:why|because|context)\s*:\s*",
        re.IGNORECASE,
    )
    IMPACT_LINE_PATTERN = re.compile(
        r"^(?:impact|why it matters|risk|result|consequence)\s*:\s*",
        re.IGNORECASE,
    )
    FIX_LINE_PATTERN = re.compile(
        r"^(?:fix|solution|what to do|instead|action|how to fix)\s*:\s*",
        re.IGNORECASE,
    )
    PROMOTIONAL_LINE_PATTERN = re.compile(
        r"\b(explore|discover|start|learn more|learn how|open doors?|today|platform|curated|trusted|transparent|regulated|guidance|opportunit(?:y|ies)|options?|simplif(?:y|ies)|offer(?:s|ing)?|empower(?:s|ing)?)\b",
        re.IGNORECASE,
    )
    NEGATIVE_MISTAKE_CANDIDATE_PATTERN = re.compile(
        r"\b(mistake|mistakes|pitfall|pitfalls|error|errors|risk|risky|fees?|default|loss|losses|duration|yield|credit|ignore|ignoring|underestimate|miss|missing|overlook|overlooking|concentration|volatility|interest rate)\b",
        re.IGNORECASE,
    )

    def __init__(self) -> None:
        # Wires providers, guardrails, prompt building, tone QA, blueprints, context compilation, storage, tracing, and vision analysis.
        # Each generation call reuses these collaborators while building one AIOrchestrationResponse.
        self.settings = get_settings()
        self.providers = ProviderRouter()
        self.guardrails = GuardrailService()
        self.prompts = PromptIntelligenceService()
        self.tone = ToneIntelligenceService()
        self.blueprints = BlueprintService()
        self.compiler = ContextCompilerService()
        self.brand_intelligence = BrandIntelligenceService()
        self.resolution = ContextResolutionService()
        self.trace = GenerationTraceService()
        self.storage = get_object_storage()
        self.template_vision = TemplateVisionAnalyzer()

    def _fallback_creative_decision(
        self,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any],
    ) -> dict[str, Any]:
        # Builds a conservative creative decision from backend layout hints when the planning LLM is unavailable or unusable.
        # The fallback keeps layout mode, selected template, rationale, and asset strategy present for downstream branches.
        planning_hints = dict(request.layout_decision or {})
        template_candidates = request.template_candidates or []
        hinted_mode = str(planning_hints.get("mode") or compiled_context.get("template_fit_brief", {}).get("mode") or "synthesized_layout")
        selected_template_id = str(planning_hints.get("template_id") or "").strip() or None
        reasoning = list(planning_hints.get("rationale") or planning_hints.get("reasoning") or [])
        if not reasoning:
            reasoning = ["Generated from backend planning hints while AI creative planning is unavailable."]
        return {
            "layout_mode": hinted_mode,
            "selected_template_id": selected_template_id,
            "confidence": float(planning_hints.get("confidence") or 0.58),
            "reasoning": reasoning,
            "adaptations": dict(planning_hints.get("adaptation_plan") or planning_hints.get("adaptations") or {}),
            "asset_strategy": dict(planning_hints.get("asset_strategy") or {}),
            "template_candidates": template_candidates[:6],
            "planning_hints": planning_hints,
        }

    @staticmethod
    def _normalize_template_lookup_key(value: object) -> str:
        # Normalizes template lookup key from input value for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        text = str(value or "").strip()
        if not text:
            return ""
        text = re.sub(r"^[a-z]+://", "", text.casefold())
        text = text.rsplit("/", 1)[-1]
        text = text.rsplit("\\", 1)[-1]
        text = text.split("?", 1)[0]
        text = text.rsplit(".", 1)[0]
        text = re.sub(r"-[0-9a-f]{8,}$", "", text)
        text = re.sub(r"[^a-z0-9]+", "-", text).strip("-")
        return text

    @classmethod
    def _sequence_reference_signature(cls, value: object) -> tuple[str, int] | None:
        # Centralizes sequence reference signature from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized = cls._normalize_template_lookup_key(value)
        if not normalized:
            return None
        match = re.match(r"^(?P<prefix>.+?)-(?P<index>\d{1,3})$", normalized)
        if not match:
            return None
        prefix = str(match.group("prefix") or "").strip("-")
        index = int(match.group("index") or 0)
        if not prefix or index <= 0:
            return None
        return prefix, index

    @classmethod
    def _storage_reference_candidates(cls, value: object) -> list[str]:
        # Centralizes storage reference candidates from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        raw = str(value or "").strip()
        if not raw:
            return []
        candidates: list[str] = [raw]
        try:
            parsed = urlparse(raw)
            query = parse_qs(parsed.query or "")
        except Exception:  # pragma: no cover - defensive parsing fallback
            query = {}
        for key in ("storage_path", "filename"):
            for item in query.get(key) or []:
                text = str(item or "").strip()
                if text:
                    candidates.append(text)
        for token in query.get("token") or []:
            token_payload = str(token or "").split(".", 1)[0].strip()
            if not token_payload:
                continue
            try:
                padding = "=" * (-len(token_payload) % 4)
                decoded = base64.urlsafe_b64decode(f"{token_payload}{padding}")
                payload = json.loads(decoded.decode("utf-8"))
            except Exception:  # pragma: no cover - corrupt or unexpected token
                continue
            if not isinstance(payload, dict):
                continue
            for key in ("storage_path", "filename"):
                text = str(payload.get(key) or "").strip()
                if text:
                    candidates.append(text)
        deduped: list[str] = []
        seen: set[str] = set()
        for candidate in candidates:
            key = candidate.casefold()
            if key in seen:
                continue
            seen.add(key)
            deduped.append(candidate)
        return deduped

    @classmethod
    def _effective_template_surface_policy(
        cls,
        request: AIOrchestrationRequest | None = None,
        *,
        creative_decision: CreativeDecisionPayload | None = None,
        metadata: dict[str, Any] | None = None,
    ) -> str:
        # Builds effective template surface policy from request state, creative decision, and metadata for AIOrchestratorService.
        # It calls _template_sequence_pack while assembling the payload or prompt text.
        candidate_values: list[Any] = []
        if creative_decision and isinstance(getattr(creative_decision, "asset_strategy", None), dict):
            candidate_values.append((creative_decision.asset_strategy or {}).get("template_surface_policy"))
        if isinstance(metadata, dict):
            candidate_values.append(metadata.get("template_surface_policy"))
            metadata_asset_strategy = (
                metadata.get("asset_strategy")
                if isinstance(metadata.get("asset_strategy"), dict)
                else {}
            )
            candidate_values.append(metadata_asset_strategy.get("template_surface_policy"))
            validation_hints = metadata.get("validation_hints") if isinstance(metadata.get("validation_hints"), dict) else {}
            candidate_values.append(validation_hints.get("template_surface_policy"))
        if request is not None:
            template_context = getattr(request, "template_context", {})
            template_context = template_context if isinstance(template_context, dict) else {}
            direct_sequence_pack = template_context.get("sequence_pack") if isinstance(template_context.get("sequence_pack"), dict) else {}
            if isinstance(direct_sequence_pack, dict):
                candidate_values.append(direct_sequence_pack.get("surface_policy"))
            sequence_pack = (
                cls._template_sequence_pack(request, creative_decision=creative_decision)
                if template_context or isinstance(request, AIOrchestrationRequest)
                else {}
            )
            if isinstance(sequence_pack, dict):
                candidate_values.append(sequence_pack.get("surface_policy"))
        for candidate in candidate_values:
            normalized = str(candidate or "").strip().lower()
            if normalized:
                return normalized
        return ""

    @classmethod
    def _resolve_template_candidate_identifier(
        cls,
        template_reference: str | None,
        *,
        template_name: str | None = None,
        template_candidates: list[dict[str, Any]] | None = None,
    ) -> str | None:
        # Resolves template candidate identifier from template reference, template name, and template candidates for AIOrchestratorService.
        # This hides source-specific naming and path quirks from the main flow.
        raw_reference = str(template_reference or "").strip()
        if raw_reference and re.fullmatch(r"[0-9a-fA-F-]{36}", raw_reference):
            return raw_reference
        normalized_targets = {
            cls._normalize_template_lookup_key(template_reference),
            cls._normalize_template_lookup_key(template_name),
        }
        if raw_reference:
            normalized_targets.add(cls._normalize_template_lookup_key(raw_reference))
        normalized_targets.discard("")
        for candidate in template_candidates or []:
            if not isinstance(candidate, dict):
                continue
            candidate_id = str(candidate.get("template_id") or "").strip()
            candidate_name = str(candidate.get("name") or "").strip()
            if not candidate_id:
                continue
            if cls._normalize_template_lookup_key(candidate_id) in normalized_targets:
                return candidate_id
            if cls._normalize_template_lookup_key(candidate_name) in normalized_targets:
                return candidate_id
        return raw_reference or None

    @classmethod
    def _template_sequence_pack(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> dict[str, Any] | None:
        # Centralizes template sequence pack from request state and creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        template_context = request.template_context if isinstance(request.template_context, dict) else {}
        pack = template_context.get("sequence_pack")
        if not isinstance(pack, dict):
            return None
        slides = pack.get("slides")
        if not isinstance(slides, list) or not slides:
            return None
        normalized_slides = [dict(item) for item in slides if isinstance(item, dict)]
        if not normalized_slides:
            return None
        normalized_slides.sort(key=lambda item: int(item.get("slide_index") or 0))
        normalized_pack = {
            **pack,
            "slide_count": max(int(pack.get("slide_count") or len(normalized_slides)), len(normalized_slides)),
            "slides": normalized_slides,
        }
        surface_policy = str(normalized_pack.get("surface_policy") or "").strip().lower()
        selected_template_authority = cls._sequence_pack_matches_active_template(
            request,
            normalized_pack,
            creative_decision=creative_decision,
        )
        if (
            surface_policy != "lock_template_surface"
            and not selected_template_authority
            and not cls._sequence_pack_is_semantically_relevant(
            request,
            normalized_pack,
            )
        ):
            return None
        return normalized_pack

    @classmethod
    def _sequence_pack_matches_active_template(
        cls,
        request: AIOrchestrationRequest | None,
        sequence_pack: dict[str, Any] | None,
        *,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> bool:
        # Centralizes sequence pack active template from request state, sequence pack, and creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(sequence_pack, dict):
            return False
        layout_decision = request.layout_decision if request is not None and isinstance(request.layout_decision, dict) else {}
        active_template_ids = {
            str(value or "").strip()
            for value in (
                getattr(creative_decision, "selected_template_id", None),
                layout_decision.get("template_id"),
                layout_decision.get("selected_template_id"),
                layout_decision.get("primary_adaptation_template_id"),
            )
            if str(value or "").strip()
        }
        pack_template_id = str(sequence_pack.get("selected_template_id") or "").strip()
        if pack_template_id and pack_template_id in active_template_ids:
            return True
        active_template_names = {
            cls._normalize_template_lookup_key(value)
            for value in (
                layout_decision.get("template_name"),
                layout_decision.get("selected_template_name"),
                layout_decision.get("primary_adaptation_template_name"),
                sequence_pack.get("selected_template_name"),
            )
            if cls._normalize_template_lookup_key(value)
        }
        pack_template_name = cls._normalize_template_lookup_key(sequence_pack.get("selected_template_name"))
        if pack_template_name and pack_template_name in active_template_names:
            return True
        return False

    @staticmethod
    def _explicit_requested_slide_count(request: AIOrchestrationRequest | None) -> int | None:
        # Counts explicit requested slide count from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None:
            return None
        prompt = str(getattr(request, "prompt", "") or "")
        match = re.search(r"\b(\d{1,2})\s*(?:-| )?slides?\b", prompt, re.IGNORECASE)
        if not match:
            match = re.search(r"\bslides?\s*:\s*(\d{1,2})\b", prompt, re.IGNORECASE)
        if not match:
            return None
        try:
            value = int(match.group(1))
        except (TypeError, ValueError):
            return None
        return value if 1 < value <= 20 else None

    @classmethod
    def _sequence_pack_semantic_tokens(cls, value: Any) -> set[str]:
        # Centralizes sequence pack semantic tokens from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        high_signal_short_tokens = {
            "aum",
            "cpi",
            "emi",
            "fta",
            "gdp",
            "gst",
            "inr",
            "ipo",
            "irr",
            "mom",
            "npa",
            "nps",
            "rbi",
            "roi",
            "sip",
            "usd",
            "xirr",
            "yoy",
            "ytd",
        }
        generic_tokens = {
            "adapted",
            "api",
            "blueprint",
            "brand",
            "campaign",
            "card",
            "carousel",
            "content",
            "cover",
            "creative",
            "detail",
            "download",
            "education",
            "editorial",
            "explainer",
            "family",
            "filename",
            "finance",
            "financial",
            "flow",
            "guide",
            "hook",
            "http",
            "https",
            "image",
            "instagram",
            "investing",
            "investment",
            "investor",
            "layout",
            "linkedin",
            "localhost",
            "mistake",
            "mistakes",
            "money",
            "page",
            "panel",
            "planning",
            "post",
            "reference",
            "retail",
            "sample",
            "section",
            "sequence",
            "series",
            "signed",
            "slide",
            "slides",
            "social",
            "storage",
            "static",
            "story",
            "structure",
            "style",
            "takeaway",
            "template",
            "tenant",
            "token",
            "top",
            "visual",
        }
        text = re.sub(r"[^a-z0-9]+", " ", str(value or "").casefold())
        tokens: set[str] = set()
        for raw_token in text.split():
            token = raw_token.strip()
            if token.endswith("s") and len(token) > 4:
                token = token[:-1]
            if (
                (len(token) >= 4 or token in high_signal_short_tokens)
                and token not in generic_tokens
                and not token.isdigit()
            ):
                tokens.add(token)
        return tokens

    @classmethod
    def _sequence_pack_is_semantically_relevant(
        cls,
        request: AIOrchestrationRequest | None,
        sequence_pack: dict[str, Any] | None,
    ) -> bool:
        # Centralizes sequence pack semantically relevant from request state and sequence pack for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None or not isinstance(sequence_pack, dict):
            return True
        prompt_tokens = cls._sequence_pack_semantic_tokens(getattr(request, "prompt", ""))
        if not prompt_tokens:
            return True
        semantic_tokens: set[str] = set()
        semantic_tokens.update(cls._sequence_pack_semantic_tokens(sequence_pack.get("family_name")))
        semantic_tokens.update(cls._sequence_pack_semantic_tokens(sequence_pack.get("selected_template_name")))
        reference_path_tokens: set[str] = set()
        for slide in [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]:
            semantic_tokens.update(cls._sequence_pack_semantic_tokens(slide.get("template_name")))
            semantic_tokens.update(cls._sequence_pack_semantic_tokens(slide.get("headline_hint")))
            reference_path_tokens.update(cls._sequence_pack_semantic_tokens(slide.get("reference_asset_path")))
        pack_tokens = semantic_tokens | reference_path_tokens
        if not semantic_tokens:
            return True
        if not pack_tokens:
            return True
        return bool(prompt_tokens.intersection(pack_tokens))

    @classmethod
    def _template_label_is_semantically_relevant(
        cls,
        request: AIOrchestrationRequest | None,
        template_label: Any,
    ) -> bool:
        # Extracts template label semantically relevant from request state and template label for AIOrchestratorService.
        # It calls _sequence_pack_semantic_tokens to turn raw evidence into the structured signal the caller needs.
        if request is None:
            return True
        prompt_tokens = cls._sequence_pack_semantic_tokens(getattr(request, "prompt", ""))
        if not prompt_tokens:
            return True
        label_tokens = cls._sequence_pack_semantic_tokens(template_label)
        if not label_tokens:
            return True
        return bool(prompt_tokens.intersection(label_tokens))

    @classmethod
    def _sequence_pack_style_reference_surface_paths(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> set[str]:
        # Derives sequence pack style reference surface paths from request state and creative decision for AIOrchestratorService.
        # It calls _template_sequence_pack to turn raw evidence into the structured signal the caller needs.
        pack = cls._template_sequence_pack(request, creative_decision=creative_decision)
        if not isinstance(pack, dict):
            return set()
        surface_policy = str(pack.get("surface_policy") or "").strip().lower()
        if surface_policy != "style_reference_only":
            return set()
        paths: set[str] = set()
        for slide in [dict(item) for item in pack.get("slides", []) if isinstance(item, dict)]:
            for key in ("reference_asset_path", "template_asset_path"):
                value = str(slide.get(key) or "").strip()
                if value:
                    paths.add(value)
        return paths

    @staticmethod
    def _structured_list_items(value: Any) -> list[str]:
        # Extracts structured items from input value for AIOrchestratorService.
        # It calls _coerce_text_value and _sanitize_text_for_canvas to turn raw evidence into the structured signal the caller needs.
        if isinstance(value, str):
            text = value
        else:
            text = AIOrchestratorService._coerce_text_value(value)
        if not text:
            return []
        normalized = text.replace("\r\n", "\n").replace("\r", "\n").strip()
        if "\n" not in normalized:
            return []
        pattern = re.compile(
            r"(?:^|\n)\s*(?:\d+[.)]|[-*])\s+(.*?)(?=(?:\n\s*(?:\d+[.)]|[-*])\s+)|$)",
            re.S,
        )
        items = [
            AIOrchestratorService._sanitize_text_for_canvas(" ".join(match.group(1).split()))
            for match in pattern.finditer(normalized)
        ]
        cleaned = [item for item in items if item]
        return cleaned if len(cleaned) >= 2 else []

    @classmethod
    def _should_force_sequence_pack(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> bool:
        # Checks force sequence pack from request state and creative decision for AIOrchestratorService.
        # The boolean result controls the nearby policy or validation branch.
        format_name = str(request.studio_panel.get("format") or "").strip().lower()
        if format_name != "carousel":
            return False
        template_context = request.template_context if isinstance(request.template_context, dict) else {}
        pack = template_context.get("sequence_pack")
        if not isinstance(pack, dict):
            return False
        surface_policy = str(pack.get("surface_policy") or "").strip().lower()
        if surface_policy != "lock_template_surface":
            return False
        slides = [dict(item) for item in pack.get("slides", []) if isinstance(item, dict)]
        if len(slides) < 3:
            return False
        explicit_template_id = str(
            (creative_decision.selected_template_id if creative_decision else None)
            or ((request.layout_decision or {}).get("template_id") if isinstance(request.layout_decision, dict) else "")
            or ""
        ).strip()
        if not explicit_template_id:
            explicit_template_id = str(pack.get("selected_template_id") or "").strip()
        if not explicit_template_id:
            return False
        if creative_decision and str(creative_decision.layout_mode or "").strip().lower() == "exact_template":
            return True
        return True

    @classmethod
    def _slide_reference_images(
        cls,
        slide: dict[str, Any],
        reference_images: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None = None,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> list[dict[str, Any]]:
        # Centralizes slide reference images from slide, reference images, and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        desired_path = str(slide_metadata.get("reference_asset_path") or "").strip()
        desired_template_name = str(slide_metadata.get("reference_template_name") or "").strip()
        desired_signature = cls._sequence_reference_signature(desired_template_name)
        slide_index = int(slide.get("slide_index") or slide_metadata.get("reference_slide_index") or 1)
        slide_count = int(slide.get("slide_count") or slide_metadata.get("reference_slide_count") or 1)
        is_carousel_request = request is not None and cls._requested_reference_format_family(request) == "carousel"
        preferred_paths = cls._preferred_sequence_reference_paths(request, creative_decision=creative_decision)
        blocked_sequence_surface_paths: set[str] = set()
        if request is not None and creative_decision is not None:
            blocked_sequence_surface_paths = cls._sequence_pack_style_reference_surface_paths(
                request,
                creative_decision=creative_decision,
            )
        usable_reference_images = [
            dict(asset)
            for asset in reference_images
            if isinstance(asset, dict)
            and (
                str(asset.get("storage_path") or "").strip() not in blocked_sequence_surface_paths
                or str(asset.get("storage_path") or "").strip() in preferred_paths
            )
        ]
        if desired_path and (desired_path not in blocked_sequence_surface_paths or desired_path in preferred_paths):
            matched = [
                dict(asset)
                for asset in usable_reference_images
                if str(asset.get("storage_path") or "").strip() == desired_path
            ]
            if matched:
                # When a carousel slide has its own exact carousel-native reference,
                # keep that slide locked to its matched sample instead of merging in
                # extra topical assets from other slides or families.
                if is_carousel_request and any(cls._reference_asset_is_carousel_native(asset) for asset in matched):
                    return cls._prepare_slide_reference_assets(
                        matched,
                        slide_index=slide_index,
                        slide_count=slide_count,
                    )
                topical_matches = cls._topic_relevant_reference_assets(
                    request,
                    creative_decision=creative_decision,
                    source_assets=usable_reference_images,
                    limit=2,
                )
                desired_score = max(
                    cls._reference_asset_topic_score(asset, request=request)
                    for asset in matched
                ) if request is not None else 0
                topical_score = max(
                    [cls._reference_asset_topic_score(asset, request=request) for asset in topical_matches]
                    or [0]
                ) if request is not None else 0
                if topical_matches and topical_score > max(desired_score, 0):
                    return cls._prepare_slide_reference_assets(
                        cls._merge_reference_asset_lists(matched, topical_matches),
                        slide_index=slide_index,
                        slide_count=slide_count,
                    )
                return cls._prepare_slide_reference_assets(
                    matched,
                    slide_index=slide_index,
                    slide_count=slide_count,
                )
            if request is not None:
                matched_asset = cls._reference_asset_by_storage_path(request, desired_path)
                if matched_asset is not None:
                    return cls._prepare_slide_reference_assets(
                        [matched_asset],
                        slide_index=slide_index,
                        slide_count=slide_count,
                    )
        if desired_signature:
            matched = []
            for asset in usable_reference_images:
                asset_signature = cls._sequence_reference_signature(
                    str((asset.get("metadata") or {}).get("label") or asset.get("storage_path") or "")
                )
                if asset_signature == desired_signature:
                    matched.append(dict(asset))
            if matched:
                return cls._prepare_slide_reference_assets(
                    matched,
                    slide_index=slide_index,
                    slide_count=slide_count,
                )
        if request is not None and cls._requested_reference_format_family(request) == "carousel":
            carousel_native = [
                dict(asset)
                for asset in usable_reference_images
                if cls._reference_asset_is_carousel_native(asset)
            ]
            if carousel_native:
                preferred_family_assets = cls._selected_carousel_reference_family_assets(
                    carousel_native,
                    request=request,
                    creative_decision=creative_decision,
                )
                family_assets = preferred_family_assets or cls._dominant_carousel_reference_family_assets(
                    carousel_native,
                    request=request,
                )
                png_slide_assets = [
                    dict(asset)
                    for asset in family_assets
                    if str(asset.get("mime_type") or "").strip().lower().startswith("image/")
                ]
                if png_slide_assets:
                    exact_slide_assets = [
                        dict(asset)
                        for asset in png_slide_assets
                        if cls._reference_asset_sequence_position(asset) == slide_index
                    ]
                    if exact_slide_assets:
                        return cls._prepare_slide_reference_assets(
                            exact_slide_assets,
                            slide_index=slide_index,
                            slide_count=slide_count,
                        )
                if family_assets:
                    return cls._prepare_slide_reference_assets(
                        family_assets,
                        slide_index=slide_index,
                        slide_count=slide_count,
                    )
        return cls._prepare_slide_reference_assets(
            usable_reference_images,
            slide_index=slide_index,
            slide_count=slide_count,
        )

    @classmethod
    def _style_reference_layout_anchor_asset(
        cls,
        slide: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> dict[str, Any] | None:
        # Derives style reference layout anchor asset from slide, request state, and creative decision for AIOrchestratorService.
        # It calls _effective_template_surface_policy and _reference_asset_by_storage_path to turn raw evidence into the structured signal the caller ne.
        if request is None:
            return None
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=slide_metadata,
        )
        if template_surface_policy != "style_reference_only":
            return None
        desired_reference_path = ""
        for key in ("reference_layout_asset_path", "reference_template_asset_path", "reference_asset_path"):
            candidate = str(slide_metadata.get(key) or "").strip()
            if candidate:
                desired_reference_path = candidate
                break
        if not desired_reference_path:
            return None
        matched = cls._reference_asset_by_storage_path(request, desired_reference_path)
        if matched is None:
            return None
        anchor_asset = dict(matched)
        metadata = dict(anchor_asset.get("metadata") or {})
        slide_index = int(slide.get("slide_index") or slide_metadata.get("reference_slide_index") or 1)
        slide_count = int(slide.get("slide_count") or slide_metadata.get("reference_slide_count") or 1)
        page_index = cls._reference_asset_slide_page_index(
            anchor_asset,
            slide_index=slide_index,
            slide_count=slide_count,
        )
        if page_index is not None:
            metadata["conditioning_page_index"] = page_index
        anchor_asset["metadata"] = metadata
        return anchor_asset

    @classmethod
    def _merge_slide_layout_anchor_reference(
        cls,
        slide: dict[str, Any],
        reference_assets: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None = None,
        for_conditioning: bool = False,
    ) -> list[dict[str, Any]]:
        # Merges slide layout anchor reference from slide, reference assets, and request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        merged = [dict(asset) for asset in reference_assets if isinstance(asset, dict)]
        layout_anchor = cls._style_reference_layout_anchor_asset(
            slide,
            request=request,
            creative_decision=creative_decision,
        )
        if layout_anchor is None:
            return merged
        if cls._reference_asset_is_mismatched_text_surface_conditioning(layout_anchor, request=request):
            return merged
        if for_conditioning and cls._reference_asset_is_layout_only_static_infographic_surface(layout_anchor, request=request):
            return merged
        merged_paths = {
            str(asset.get("storage_path") or "").strip()
            for asset in merged
            if str(asset.get("storage_path") or "").strip()
        }
        anchor_path = str(layout_anchor.get("storage_path") or "").strip()
        ordered_assets: list[dict[str, Any]] = [layout_anchor]
        ordered_assets.extend(
            dict(asset)
            for asset in merged
            if str(asset.get("storage_path") or "").strip() != anchor_path
        )
        if anchor_path and anchor_path not in merged_paths:
            return ordered_assets
        return ordered_assets

    @staticmethod
    def _proportional_sequence_reference_index(
        slide_index: int,
        *,
        output_slide_count: int,
        reference_slide_count: int,
    ) -> int:
        # Centralizes proportional sequence reference index from slide index, output slide count, and reference slide count for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_reference_count = max(int(reference_slide_count or 0), 0)
        if normalized_reference_count <= 0:
            return 0
        normalized_output_count = max(int(output_slide_count or 0), 1)
        normalized_slide_index = min(max(int(slide_index or 1), 1), normalized_output_count)
        if normalized_output_count == 1:
            return 1
        proportional = int(
            ((normalized_slide_index - 1) / max(normalized_output_count - 1, 1))
            * (normalized_reference_count - 1)
            + 0.5
        ) + 1
        return min(max(int(proportional), 1), normalized_reference_count)

    @classmethod
    def _prepare_slide_reference_assets(
        cls,
        assets: list[dict[str, Any]],
        *,
        slide_index: int,
        slide_count: int,
    ) -> list[dict[str, Any]]:
        # Extracts prepare slide reference assets from assets, slide index, and slide count for AIOrchestratorService.
        # It calls _reference_asset_slide_page_index to turn raw evidence into the structured signal the caller needs.
        prepared: list[dict[str, Any]] = []
        for asset in assets:
            if not isinstance(asset, dict):
                continue
            prepared_asset = dict(asset)
            page_index = cls._reference_asset_slide_page_index(
                prepared_asset,
                slide_index=slide_index,
                slide_count=slide_count,
            )
            if page_index is not None:
                metadata = dict(prepared_asset.get("metadata") or {})
                metadata["conditioning_page_index"] = page_index
                prepared_asset["metadata"] = metadata
            prepared.append(prepared_asset)
        return prepared

    @classmethod
    def _reference_asset_declared_page_count(cls, asset: dict[str, Any]) -> int:
        # Counts reference asset declared page count from asset record for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        for candidate in (
            asset.get("page_count"),
            ((asset.get("metadata") or {}) if isinstance(asset.get("metadata"), dict) else {}).get("page_count"),
            ((asset.get("analysis") or {}) if isinstance(asset.get("analysis"), dict) else {}).get("page_count"),
        ):
            try:
                value = int(candidate)
            except (TypeError, ValueError):
                continue
            if value > 0:
                return value
        return 0

    @classmethod
    def _reference_asset_slide_page_index(
        cls,
        asset: dict[str, Any],
        *,
        slide_index: int,
        slide_count: int,
    ) -> int | None:
        # Extracts reference asset slide page index from asset record, slide index, and slide count for AIOrchestratorService.
        # It calls _reference_asset_declared_page_count to turn raw evidence into the structured signal the caller needs.
        mime_type = str(asset.get("mime_type") or "").strip().lower()
        if mime_type != "application/pdf":
            return None
        metadata = asset.get("metadata") if isinstance(asset.get("metadata"), dict) else {}
        for candidate in (
            metadata.get("conditioning_page_index"),
            metadata.get("reference_page_index"),
            metadata.get("page_index"),
            metadata.get("page_number"),
        ):
            try:
                explicit = int(candidate)
            except (TypeError, ValueError):
                continue
            if explicit > 0:
                return explicit
        page_count = cls._reference_asset_declared_page_count(asset)
        if page_count <= 1:
            return 1
        normalized_slide_count = max(int(slide_count or 1), 1)
        normalized_slide_index = min(max(int(slide_index or 1), 1), normalized_slide_count)
        if normalized_slide_count == 1:
            return 1
        proportional = round(((normalized_slide_index - 1) / max(normalized_slide_count - 1, 1)) * (page_count - 1)) + 1
        return min(max(int(proportional), 1), page_count)

    @staticmethod
    def _merge_partial_sequence_specs(
        incoming: Any,
        fallback: Any,
        *,
        number_keys: tuple[str, ...],
    ) -> list[dict[str, Any]]:
        # Merges partial sequence specs from incoming, fallback payload, and number keys for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        fallback_items = [dict(item) for item in (fallback or []) if isinstance(item, dict)]
        incoming_items = [dict(item) for item in (incoming or []) if isinstance(item, dict)]
        if not incoming_items:
            return fallback_items
        if not fallback_items:
            return incoming_items
        if len(incoming_items) >= len(fallback_items):
            return incoming_items

        merged = [dict(item) for item in fallback_items]

        def _match_index(item: dict[str, Any], position: int) -> int | None:
            # Matches index from item and position for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            for key in number_keys:
                value = item.get(key)
                if isinstance(value, bool):
                    continue
                if isinstance(value, int) and 1 <= value <= len(merged):
                    return value - 1
                text = str(value or "").strip()
                if text.isdigit():
                    parsed = int(text)
                    if 1 <= parsed <= len(merged):
                        return parsed - 1
            if 0 <= position < len(merged):
                return position
            return None

        for position, item in enumerate(incoming_items):
            match_index = _match_index(item, position)
            if match_index is None:
                merged.append(item)
                continue
            existing = dict(merged[match_index] or {})
            merged[match_index] = {**existing, **item}
        return merged

    @classmethod
    def _preferred_sequence_reference_paths(
        cls,
        request: AIOrchestrationRequest | None,
        *,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> set[str]:
        # Centralizes preferred sequence reference paths from request state and creative decision for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if request is None:
            return set()
        pack = cls._template_sequence_pack(request, creative_decision=creative_decision)
        if not isinstance(pack, dict):
            return set()
        paths: set[str] = set()
        for slide in [item for item in (pack.get("slides") or []) if isinstance(item, dict)]:
            for key in ("reference_asset_path", "template_asset_path"):
                path = str(slide.get(key) or "").strip()
                if not path:
                    continue
                matched = cls._reference_asset_by_storage_path(request, path)
                if matched is not None:
                    storage_path = str(matched.get("storage_path") or "").strip()
                    if storage_path:
                        paths.add(storage_path)
                        continue
                paths.add(path)
        return paths

    @classmethod
    def _reference_asset_by_storage_path(
        cls,
        request: AIOrchestrationRequest,
        storage_path: str,
    ) -> dict[str, Any] | None:
        # Extracts reference asset storage path from request state and storage path for AIOrchestratorService.
        # It calls _storage_reference_candidates and _sequence_reference_signature to turn raw evidence into the structured signal the caller needs.
        desired = str(storage_path or "").strip()
        if not desired:
            return None
        desired_candidates = cls._storage_reference_candidates(desired)
        desired_path_texts = {candidate.casefold() for candidate in desired_candidates if candidate}
        desired_filenames = {
            candidate.rsplit("/", 1)[-1].strip().casefold()
            for candidate in desired_candidates
            if "/" in candidate or "." in candidate
        }
        desired_signatures = {
            signature
            for signature in (cls._sequence_reference_signature(candidate) for candidate in desired_candidates)
            if signature is not None
        }
        for asset in (request.asset_catalog or request.reference_assets or []):
            if not isinstance(asset, dict):
                continue
            if not cls._reference_asset_is_visual_source(asset):
                continue
            asset_storage_path = str(asset.get("storage_path") or "").strip()
            asset_storage_path_text = asset_storage_path.casefold()
            asset_filename = asset_storage_path.rsplit("/", 1)[-1].strip().casefold()
            asset_label_signature = cls._sequence_reference_signature(
                str(((asset.get("metadata") or {}) if isinstance(asset.get("metadata"), dict) else {}).get("label") or asset_storage_path)
            )
            if asset_storage_path in desired_candidates:
                return dict(asset)
            if asset_storage_path_text and any(asset_storage_path_text in text for text in desired_path_texts):
                return dict(asset)
            if asset_filename and asset_filename in desired_filenames:
                return dict(asset)
            if asset_label_signature and asset_label_signature in desired_signatures:
                return dict(asset)
        return None

    @classmethod
    def _catalog_asset_by_storage_path(
        cls,
        request: AIOrchestrationRequest,
        storage_path: str,
    ) -> dict[str, Any] | None:
        # Extracts catalog asset storage path from request state and storage path for AIOrchestratorService.
        # It calls _storage_reference_candidates to turn raw evidence into the structured signal the caller needs.
        desired = str(storage_path or "").strip()
        if not desired:
            return None
        desired_candidates = cls._storage_reference_candidates(desired)
        desired_path_texts = {candidate.casefold() for candidate in desired_candidates if candidate}
        desired_filenames = {
            candidate.rsplit("/", 1)[-1].strip().casefold()
            for candidate in desired_candidates
            if "/" in candidate or "." in candidate
        }
        for asset in (request.asset_catalog or request.reference_assets or []):
            if not isinstance(asset, dict):
                continue
            asset_storage_path = str(asset.get("storage_path") or "").strip()
            if not asset_storage_path:
                continue
            asset_storage_path_text = asset_storage_path.casefold()
            asset_filename = asset_storage_path.rsplit("/", 1)[-1].strip().casefold()
            if asset_storage_path in desired_candidates:
                return dict(asset)
            if asset_storage_path_text and any(asset_storage_path_text in text for text in desired_path_texts):
                return dict(asset)
            if asset_filename and asset_filename in desired_filenames:
                return dict(asset)
        return None

    @classmethod
    def _compact_slide_geometry_contract(
        cls,
        slide: dict[str, Any],
        scene_graph: GenerationSceneGraph,
        *,
        limit: int = 10,
    ) -> str:
        # Compacts slide geometry contract from slide, scene graph, and limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        zone_map = slide_metadata.get("reference_zone_map") if isinstance(slide_metadata.get("reference_zone_map"), dict) else {}
        raw_zones = zone_map.get("zones") if isinstance(zone_map.get("zones"), list) else []
        geometry_manifest: list[dict[str, Any]] = []
        for item in raw_zones[:limit]:
            if not isinstance(item, dict):
                continue
            role = cls._normalize_metadata_text(item.get("role"), limit=32).casefold()
            x = item.get("x")
            y = item.get("y")
            w = item.get("w") if item.get("w") is not None else item.get("width")
            h = item.get("h") if item.get("h") is not None else item.get("height")
            if not role or any(value is None for value in (x, y, w, h)):
                continue
            try:
                geometry_manifest.append(
                    {
                        "role": role,
                        "type": "reference_zone",
                        "x": round(float(x), 3),
                        "y": round(float(y), 3),
                        "w": round(float(w), 3),
                        "h": round(float(h), 3),
                    }
                )
            except (TypeError, ValueError):
                continue
        if geometry_manifest:
            return json.dumps(geometry_manifest, separators=(",", ":"), ensure_ascii=True)
        return cls._compact_scene_graph_geometry(scene_graph, limit=limit)

    @staticmethod
    def _zone_position_label(x: float, y: float, w: float, h: float) -> str:
        # Measures zone position label from x, y, and w for AIOrchestratorService.
        # Keeping the math here avoids subtle differences between renderer-facing branches.
        center_x = x + (w / 2.0)
        center_y = y + (h / 2.0)
        vertical = "top" if center_y < 0.34 else "bottom" if center_y > 0.66 else "middle"
        horizontal = "left" if center_x < 0.34 else "right" if center_x > 0.66 else "center"
        if vertical == "middle" and horizontal == "center":
            return "center"
        if vertical == "middle":
            return horizontal
        if horizontal == "center":
            return vertical
        return f"{vertical}-{horizontal}"

    @classmethod
    def _reference_zone_layout_guidance(
        cls,
        slide: dict[str, Any],
        *,
        limit: int = 6,
    ) -> str:
        # Measures reference zone layout guidance from slide and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text and _zone_position_label to turn raw evidence into the structured signal the caller needs.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        zone_map = slide_metadata.get("reference_zone_map") if isinstance(slide_metadata.get("reference_zone_map"), dict) else {}
        raw_zones = zone_map.get("zones") if isinstance(zone_map.get("zones"), list) else []
        normalized_zones: list[dict[str, Any]] = []
        for item in raw_zones:
            if not isinstance(item, dict):
                continue
            role = cls._normalize_metadata_text(item.get("role"), limit=32).casefold()
            x = item.get("x")
            y = item.get("y")
            w = item.get("w") if item.get("w") is not None else item.get("width")
            h = item.get("h") if item.get("h") is not None else item.get("height")
            if not role or any(value is None for value in (x, y, w, h)):
                continue
            try:
                zone = {
                    "role": role,
                    "x": float(x),
                    "y": float(y),
                    "w": float(w),
                    "h": float(h),
                }
            except (TypeError, ValueError):
                continue
            if zone["w"] <= 0.0 or zone["h"] <= 0.0:
                continue
            normalized_zones.append(zone)
        if not normalized_zones:
            return ""
        snippets: list[str] = []
        seen_roles: dict[str, int] = {}
        for zone in sorted(normalized_zones, key=lambda item: (item["y"], item["x"]))[:limit]:
            role = zone["role"]
            seen_roles[role] = seen_roles.get(role, 0) + 1
            role_label = role.replace("_", " ")
            if seen_roles[role] == 2:
                role_label = f"secondary {role_label}"
            elif seen_roles[role] > 2:
                role_label = f"{role_label} {seen_roles[role]}"
            position = cls._zone_position_label(zone["x"], zone["y"], zone["w"], zone["h"])
            width_label = "wide" if zone["w"] >= 0.55 else "compact" if zone["w"] <= 0.22 else ""
            height_label = "tall" if zone["h"] >= 0.22 else "shallow" if zone["h"] <= 0.08 else ""
            size_label = " ".join(part for part in [width_label, height_label] if part).strip()
            snippets.append(
                " ".join(
                    part
                    for part in [
                        role_label,
                        "block",
                        f"at the {position}",
                        f"({size_label})" if size_label else "",
                    ]
                    if part
                ).strip()
            )
        layout_type = cls._normalize_metadata_text(zone_map.get("layout_type"), limit=64)
        prefix = f"{layout_type} layout summary" if layout_type else "Reference slide layout summary"
        return f"{prefix}: {'; '.join(snippets)}."

    @classmethod
    def _slide_sample_metadata_semantically_contaminated(
        cls,
        slide: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
        compiled_context: dict[str, Any] | None = None,
    ) -> bool:
        # Centralizes slide sample metadata semantically contaminated from slide, request state, and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None or not isinstance(slide, dict):
            return False
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        evidence_text = " ".join(
            cls._coerce_text_value(part)
            for part in (
                getattr(request, "prompt", ""),
                slide.get("headline"),
                slide.get("supporting_line"),
                slide.get("body"),
                slide_metadata.get("reference_template_name"),
                slide_metadata.get("reference_layout_asset_path"),
                slide_metadata.get("reference_template_asset_path"),
                slide_metadata.get("reference_asset_path"),
                ((compiled_context or {}).get("research_summary") if isinstance(compiled_context, dict) else ""),
            )
            if cls._coerce_text_value(part)
        )
        evidence_tokens = cls._sequence_pack_semantic_tokens(evidence_text)
        if len(evidence_tokens) < 3:
            return False
        context_fragments: list[str] = []
        for key in (
            "reference_editorial_dna",
            "reference_subject_semantics",
            "reference_visual_craft",
            "reference_composition_logic",
        ):
            value = slide_metadata.get(key)
            if value:
                context_fragments.append(json.dumps(value, ensure_ascii=True, default=str))
        sequence_pack = cls._prompt_sequence_pack(compiled_context)
        slide_authority = cls._sequence_pack_slide_authority(sequence_pack, slide) if sequence_pack else {}
        for key in ("editorial_dna", "subject_semantics", "visual_craft", "composition_logic"):
            value = slide_authority.get(key) if isinstance(slide_authority, dict) else None
            if value:
                context_fragments.append(json.dumps(value, ensure_ascii=True, default=str))
        if not context_fragments:
            return False
        context_tokens = cls._sequence_pack_semantic_tokens(" ".join(context_fragments))
        if len(context_tokens) < 4:
            return False
        overlap = context_tokens.intersection(evidence_tokens)
        overlap_ratio = len(overlap) / max(len(context_tokens), 1)
        return overlap_ratio < 0.06

    @classmethod
    def _strict_sample_layout_contract(
        cls,
        *,
        slide: dict[str, Any],
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
        compiled_context: dict[str, Any] | None,
        scene_graph: GenerationSceneGraph | None = None,
        limit_zones: int = 12,
    ) -> str:
        # Builds strict sample layout contract from slide, request state, and creative decision for AIOrchestratorService.
        # It calls _effective_template_surface_policy and _slide_sample_metadata_semantically_contaminated while assembling the payload or prompt text.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=slide_metadata,
        )
        if template_surface_policy != "style_reference_only":
            return ""
        sample_page_blueprint = (
            slide_metadata.get("sample_page_blueprint")
            if isinstance(slide_metadata.get("sample_page_blueprint"), dict)
            else {}
        )
        metadata_contaminated = cls._slide_sample_metadata_semantically_contaminated(
            slide,
            request=request,
            compiled_context=compiled_context,
        )
        sequence_pack = cls._prompt_sequence_pack(compiled_context)
        slide_authority = cls._sequence_pack_slide_authority(sequence_pack, slide) if sequence_pack else {}
        zone_map = cls._coerce_mapping(
            {}
            if metadata_contaminated
            else slide_metadata.get("reference_zone_map")
            or slide_authority.get("zone_map")
        )
        # When metadata is stale, ignore extracted zone DNA and let the actual sample image remain authoritative.
        raw_zones = zone_map.get("zones") if isinstance(zone_map.get("zones"), list) else []
        zones: list[dict[str, Any]] = []
        role_counts: dict[str, int] = {}
        for item in raw_zones[:limit_zones]:
            if not isinstance(item, dict):
                continue
            role = cls._normalize_metadata_text(item.get("role"), limit=32).casefold()
            x = item.get("x")
            y = item.get("y")
            w = item.get("w") if item.get("w") is not None else item.get("width")
            h = item.get("h") if item.get("h") is not None else item.get("height")
            if not role or any(value is None for value in (x, y, w, h)):
                continue
            try:
                zone = {
                    "role": role,
                    "x": round(float(x), 3),
                    "y": round(float(y), 3),
                    "w": round(float(w), 3),
                    "h": round(float(h), 3),
                }
            except (TypeError, ValueError):
                continue
            zones.append(zone)
            role_counts[role] = role_counts.get(role, 0) + 1
        if not zones and isinstance(sample_page_blueprint.get("zones"), list):
            # sample_page_blueprint is the safe fallback because it comes from the selected rendered sample page.
            for item in sample_page_blueprint.get("zones", [])[:limit_zones]:
                if not isinstance(item, dict):
                    continue
                role = cls._normalize_metadata_text(item.get("role"), limit=32).casefold()
                x = item.get("x")
                y = item.get("y")
                w = item.get("w") if item.get("w") is not None else item.get("width")
                h = item.get("h") if item.get("h") is not None else item.get("height")
                if not role or any(value is None for value in (x, y, w, h)):
                    continue
                try:
                    zone = {
                        "role": role,
                        "x": round(float(x), 3),
                        "y": round(float(y), 3),
                        "w": round(float(w), 3),
                        "h": round(float(h), 3),
                        "source": "sample_page_blueprint",
                    }
                except (TypeError, ValueError):
                    continue
                zones.append(zone)
                role_counts[role] = role_counts.get(role, 0) + 1
        composition_logic = cls._coerce_mapping(
            {}
            if metadata_contaminated
            else slide_metadata.get("reference_composition_logic")
            or slide_authority.get("composition_logic")
        )
        visual_craft = cls._coerce_mapping(
            {}
            if metadata_contaminated
            else slide_metadata.get("reference_visual_craft")
            or slide_authority.get("visual_craft")
        )
        subject_semantics = cls._coerce_mapping(
            {}
            if metadata_contaminated
            else slide_metadata.get("reference_subject_semantics")
            or slide_authority.get("subject_semantics")
        )
        editorial_dna = cls._coerce_mapping(
            {}
            if metadata_contaminated
            else slide_metadata.get("reference_editorial_dna")
            or slide_authority.get("editorial_dna")
        )
        image_roles = {
            role: count
            for role, count in role_counts.items()
            if any(token in role for token in ("image", "visual", "photo", "illustration", "hero"))
        }
        text_roles = {
            role: count
            for role, count in role_counts.items()
            if any(token in role for token in ("headline", "body", "copy", "cta", "proof", "caption", "label"))
        }
        logo_roles = {
            role: count
            for role, count in role_counts.items()
            if "logo" in role
        }
        proof_module_count = cls._int_or_none(
            slide_metadata.get("proof_module_count")
            or editorial_dna.get("proof_module_count")
        )
        contract: dict[str, Any] = {
            "mode": "strict_sample_layout_contract",
            "surface_policy": template_surface_policy,
            "sample": {
                "family": cls._normalize_metadata_text(
                    (sequence_pack or {}).get("family_name") or slide_metadata.get("reference_template_name"),
                    limit=96,
                ),
                "reference_slide_index": cls._int_or_none(slide_metadata.get("reference_slide_index"))
                or cls._int_or_none(slide_authority.get("slide_index"))
                or cls._int_or_none(slide.get("slide_index")),
                "reference_slide_count": cls._int_or_none(slide_metadata.get("reference_slide_count"))
                or cls._int_or_none((sequence_pack or {}).get("slide_count"))
                or cls._int_or_none(slide.get("slide_count")),
                "layout_type": cls._normalize_metadata_text(zone_map.get("layout_type"), limit=64),
                "story_role": cls._normalize_metadata_text(
                    slide_metadata.get("story_role")
                    or slide_authority.get("story_role")
                    or slide.get("role"),
                    limit=48,
                ),
            },
            "zones": zones,
            "zone_counts": {
                "image_zone_count": sum(image_roles.values()),
                "text_zone_count": sum(text_roles.values()),
                "logo_zone_count": sum(logo_roles.values()),
                "by_role": role_counts,
            },
            "content_density": {
                "copy_density": cls._normalize_metadata_text(
                    slide_metadata.get("copy_density")
                    or editorial_dna.get("copy_density")
                    or slide_metadata.get("text_pressure"),
                    limit=32,
                ),
                "proof_module_count": proof_module_count,
                "headline_pattern": cls._normalize_metadata_list(
                    editorial_dna.get("headline_patterns"),
                    limit=2,
                ),
            },
            "spacing_geometry": {
                "spacing": cls._coerce_mapping(zone_map.get("spacing")),
                "balance": cls._normalize_metadata_text(composition_logic.get("balance"), limit=48),
                "framing": cls._normalize_metadata_text(composition_logic.get("framing"), limit=64),
                "layering": cls._normalize_metadata_text(composition_logic.get("layering"), limit=64),
                "focal_path": cls._normalize_metadata_list(composition_logic.get("focal_path"), limit=6),
            },
            "image_style": {
                "depth_style": cls._normalize_metadata_text(visual_craft.get("depth_style"), limit=48),
                "rendering_style": cls._normalize_metadata_text(visual_craft.get("rendering_style"), limit=48),
                "lighting": cls._normalize_metadata_text(visual_craft.get("lighting"), limit=48),
                "polish_level": cls._normalize_metadata_text(visual_craft.get("polish_level"), limit=48),
                "material_cues": cls._normalize_metadata_list(visual_craft.get("material_cues"), limit=5),
                "dimensionality_cues": cls._normalize_metadata_list(visual_craft.get("dimensionality_cues"), limit=5),
            },
            "subject_semantics": {
                "scene_type": cls._normalize_metadata_text(subject_semantics.get("scene_type"), limit=64),
                "primary_subjects": cls._normalize_metadata_list(subject_semantics.get("primary_subjects"), limit=5),
                "financial_objects": cls._normalize_metadata_list(subject_semantics.get("financial_objects"), limit=5),
                "abstraction_level": cls._normalize_metadata_text(subject_semantics.get("abstraction_level"), limit=48),
            },
            "closing_grammar": {
                "closing_style": cls._normalize_metadata_text(editorial_dna.get("closing_style"), limit=64),
                "only_final_slide_gets_cta": True,
            },
        }
        if sample_page_blueprint:
            prompt_sample_page_blueprint = cls._sample_blueprint_without_rendered_footer_text(sample_page_blueprint)
            # Preserve Feature 1/2/3 shared fields while trimming footer text that should not become content authority.
            contract["sample_page_blueprint"] = {
                "source": "rendered_selected_sample_page",
                "layout_category": cls._normalize_metadata_text(
                    prompt_sample_page_blueprint.get("layout_category"),
                    limit=64,
                ),
                "density": cls._normalize_metadata_text(prompt_sample_page_blueprint.get("density"), limit=32),
                "module_counts": prompt_sample_page_blueprint.get("module_counts")
                if isinstance(prompt_sample_page_blueprint.get("module_counts"), dict)
                else {},
                "visual_permissions": prompt_sample_page_blueprint.get("visual_permissions")
                if isinstance(prompt_sample_page_blueprint.get("visual_permissions"), dict)
                else {},
                "must_match": prompt_sample_page_blueprint.get("must_match")
                if isinstance(prompt_sample_page_blueprint.get("must_match"), list)
                else [],
            }
        if metadata_contaminated:
            contract["metadata_status"] = "ignored_stale_or_cross_sample_dna"
            contract["sample_page_authority"] = (
                "Use the actual selected sample page image as the layout authority. "
                "Do not use stale text-derived template DNA, subject lists, or zone maps from unrelated samples."
            )
        if (
            not zones
            and scene_graph is not None
            and not (
                template_surface_policy == "style_reference_only"
                and (metadata_contaminated or sample_page_blueprint)
            )
        ):
            # Scene graph geometry is only used when no sample/reference blueprint is already governing layout.
            geometry_contract = cls._compact_scene_graph_geometry(scene_graph, limit=limit_zones)
            if geometry_contract:
                try:
                    contract["zones"] = json.loads(geometry_contract)
                except (TypeError, ValueError, json.JSONDecodeError):
                    pass
        return json.dumps(contract, separators=(",", ":"), ensure_ascii=True)

    @classmethod
    def _style_reference_only_carousel_active(
        cls,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None = None,
        metadata: dict[str, Any] | None = None,
    ) -> bool:
        # Derives style reference only carousel active from request state, creative decision, and metadata for AIOrchestratorService.
        # It calls _effective_template_surface_policy to turn raw evidence into the structured signal the caller needs.
        if request is None:
            return False
        format_name = str((request.studio_panel or {}).get("format") or "").strip().lower()
        if format_name != "carousel":
            return False
        return cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata,
        ) == "style_reference_only"

    @classmethod
    def _reference_layout_adaptation_active(
        cls,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None = None,
        metadata: dict[str, Any] | None = None,
        compiled_context: dict[str, Any] | None = None,
        reference_images: list[dict[str, Any]] | None = None,
    ) -> bool:
        # Centralizes reference layout adaptation active from request state, creative decision, and metadata for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if cls._style_reference_only_carousel_active(
            request,
            creative_decision=creative_decision,
            metadata=metadata,
        ):
            return True
        context = compiled_context if isinstance(compiled_context, dict) else {}
        profile = context.get("reference_adaptation_profile") if isinstance(context.get("reference_adaptation_profile"), dict) else {}
        mode = cls._normalize_metadata_text(profile.get("adaptation_mode"), limit=64).casefold()
        source_family = cls._normalize_metadata_text(profile.get("source_format_family"), limit=32).casefold()
        output_family = cls._normalize_metadata_text(profile.get("output_format_family"), limit=32).casefold()
        if mode not in {
            "infographic_surface_adaptation",
            "static_creative_adaptation",
            "hybrid_reference_adaptation",
            "template_sequence_adaptation",
        }:
            return False
        if source_family not in {"carousel", "infographic", "static"}:
            return False
        if output_family not in {"carousel", "infographic", "static"}:
            return False
        try:
            confidence = float(profile.get("confidence") or 0.0)
        except (TypeError, ValueError):
            confidence = 0.0
        if confidence > 0.0:
            return True

        requested_family = cls._requested_reference_format_family(request)
        if requested_family not in {"static", "infographic"}:
            return False
        metadata_blueprint = (
            metadata.get("sample_page_blueprint")
            if isinstance(metadata, dict) and isinstance(metadata.get("sample_page_blueprint"), dict)
            else {}
        )
        if metadata_blueprint:
            return True

        evidence_assets = reference_images if reference_images is not None else []
        if not evidence_assets and request is not None:
            evidence_assets = list(getattr(request, "asset_catalog", []) or getattr(request, "reference_assets", []) or [])

        def normalize_family(value: Any) -> str:
            # Centralizes family from input value for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            normalized = cls._normalize_metadata_text(value, limit=48).casefold()
            normalized = normalized.replace("-", "_").replace(" ", "_").replace("/", "_").strip("_")
            if normalized in {"static", "story", "poster", "hero", "single_frame"}:
                return "static"
            if normalized in {"infographic", "diagram", "visual_explainer"}:
                return "infographic"
            return normalized if normalized in {"static", "infographic", "carousel"} else ""

        for asset in evidence_assets:
            if not isinstance(asset, dict) or not cls._reference_asset_is_visual_source(asset):
                continue
            if normalize_family(cls._reference_asset_format_family(asset)) != requested_family:
                continue
            asset_metadata = cls._asset_metadata(asset)
            asset_role = cls._normalize_metadata_text(asset.get("asset_role"), limit=32).casefold()
            trust_level = cls._normalize_metadata_text(asset.get("trust_level"), limit=32).casefold()
            has_blueprint = isinstance(asset_metadata.get("sample_page_blueprint"), dict) and bool(
                asset_metadata.get("sample_page_blueprint")
            )
            if trust_level in {"rejected", "blocked", "untrusted", "excluded"}:
                continue
            if asset_role in {"reference_creative", "template", "reference", "template_preview"} or has_blueprint:
                return True
        return False

    @classmethod
    def _thin_sample_conditioning_contract(
        cls,
        *,
        slide: dict[str, Any],
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
        scene_graph: GenerationSceneGraph | None = None,
    ) -> str:
        # Builds thin sample conditioning contract from slide, request state, and creative decision for AIOrchestratorService.
        # It calls _slide_sample_metadata_semantically_contaminated and _normalize_metadata_text while assembling the payload or prompt text.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        if not cls._style_reference_only_carousel_active(
            request,
            creative_decision=creative_decision,
            metadata=slide_metadata,
        ):
            return ""
        zone_map = slide_metadata.get("reference_zone_map") if isinstance(slide_metadata.get("reference_zone_map"), dict) else {}
        zones = zone_map.get("zones") if isinstance(zone_map.get("zones"), list) else []
        visual_craft = slide_metadata.get("reference_visual_craft") if isinstance(slide_metadata.get("reference_visual_craft"), dict) else {}
        composition_logic = slide_metadata.get("reference_composition_logic") if isinstance(slide_metadata.get("reference_composition_logic"), dict) else {}
        subject_semantics = slide_metadata.get("reference_subject_semantics") if isinstance(slide_metadata.get("reference_subject_semantics"), dict) else {}
        if cls._slide_sample_metadata_semantically_contaminated(
            slide,
            request=request,
        ):
            zones = []
            visual_craft = {}
            composition_logic = {}
            subject_semantics = {}
        sample_page_blueprint = (
            slide_metadata.get("sample_page_blueprint")
            if isinstance(slide_metadata.get("sample_page_blueprint"), dict)
            else {}
        )
        has_rich_sample_metadata = bool(zones) and any((visual_craft, composition_logic, subject_semantics))
        if has_rich_sample_metadata:
            return ""
        proof_count = max(
            len(cls._normalize_metadata_list(slide.get("proof_points"), limit=4)),
            len(cls._normalize_metadata_list(slide.get("body_points"), limit=4)),
            len(cls._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=4)),
        )
        geometry_contract = cls._compact_scene_graph_geometry(scene_graph, limit=10) if scene_graph is not None else ""
        story_role = cls._normalize_metadata_text(
            slide_metadata.get("story_role") or slide.get("role"),
            limit=48,
        )
        sample_contract = {
            "sample_page_editorial_role": slide_metadata.get("sample_page_editorial_role"),
            "sample_page_copy_behavior": slide_metadata.get("sample_page_copy_behavior"),
            "sample_page_copy_density": slide_metadata.get("sample_page_copy_density"),
            "sample_page_closing_grammar": slide_metadata.get("sample_page_closing_grammar"),
        }
        story_role_normalized = story_role.casefold().replace(" ", "_")
        sample_allows_product_cta = cls._sample_contract_allows_product_cta(sample_contract)
        visual_focus_rule = cls._normalize_metadata_text(slide.get("visual_focus"), limit=320)
        if (
            not sample_allows_product_cta
            and story_role_normalized in {"takeaway", "close", "closing", "cta", "final", "macro_takeaway", "strategic_meaning"}
            and (
                cls._is_product_cta_like_line(visual_focus_rule, request=request)
                or cls._style_reference_visual_focus_needs_sample_reset(
                    visual_focus_rule,
                    request=request,
                    sample_allows_data_or_product=False,
                )
            )
        ):
            visual_focus_rule = cls._sample_matched_visual_focus(slide, request=request)
        contract = {
            "reason": "sample_metadata_thin",
            "fallback_authority": "selected_sample_identity_plus_slide_story_contract",
            "story_role": story_role,
            "slide_index": cls._int_or_none(slide.get("slide_index")) or cls._int_or_none(slide_metadata.get("reference_slide_index")),
            "reference_slide_index": cls._int_or_none(slide_metadata.get("reference_slide_index")),
            "reference_slide_count": cls._int_or_none(slide_metadata.get("reference_slide_count")),
            "layout_rules": [
                "preserve the selected sample's page-specific layout cadence and whitespace behavior",
                "do not let supporting topic references become layout anchors",
                "keep headline, body, image, and CTA regions separated into distinct partitions",
                "match text density and module count to this slide's story role and proof count",
            ],
            "content_quantity": {
                "proof_or_body_module_count": proof_count,
                "has_cta": bool(cls._normalize_metadata_text(slide.get("cta"), limit=90)),
                "text_pressure": cls._carousel_slide_text_pressure(slide),
            },
            "visual_rules": [
                visual_focus_rule,
                cls._carousel_story_role_guidance(story_role),
                "copy the selected sample page's visible composition category first, then adapt the subject matter and approved copy",
                "avoid generic finance motifs, charts, documents, dashboards, or stock symbols unless they are visibly part of the selected sample page or explicitly requested",
            ],
            "scene_graph_geometry_fallback": "" if sample_page_blueprint else geometry_contract,
        }
        if sample_page_blueprint:
            prompt_sample_page_blueprint = cls._sample_blueprint_without_rendered_footer_text(sample_page_blueprint)
            contract["rendered_sample_page_blueprint"] = {
                "layout_category": cls._normalize_metadata_text(prompt_sample_page_blueprint.get("layout_category"), limit=64),
                "density": cls._normalize_metadata_text(prompt_sample_page_blueprint.get("density"), limit=32),
                "module_counts": prompt_sample_page_blueprint.get("module_counts")
                if isinstance(prompt_sample_page_blueprint.get("module_counts"), dict)
                else {},
                "visual_permissions": prompt_sample_page_blueprint.get("visual_permissions")
                if isinstance(prompt_sample_page_blueprint.get("visual_permissions"), dict)
                else {},
                "must_match": prompt_sample_page_blueprint.get("must_match")
                if isinstance(prompt_sample_page_blueprint.get("must_match"), list)
                else [],
            }
        return json.dumps(contract, separators=(",", ":"), ensure_ascii=True)

    @classmethod
    def _enforce_style_reference_only_final_prompt(
        cls,
        prompt: str,
        *,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
        metadata: dict[str, Any] | None = None,
    ) -> str:
        # Derives enforce style reference only final prompt from prompt text, request state, and creative decision for AIOrchestratorService.
        # It calls _style_reference_only_carousel_active to turn raw evidence into the structured signal the caller needs.
        if not cls._style_reference_only_carousel_active(
            request,
            creative_decision=creative_decision,
            metadata=metadata,
        ):
            return prompt
        cleaned = str(prompt or "")
        cleaned = cleaned.replace("Create the visual substrate", "Create the finished slide")
        cleaned = cleaned.replace(
            "TEXT OVERLAY CONTRACT:",
            "FINAL TEXT RENDER CONTRACT:",
        )
        cleaned = cleaned.replace(
            "not a finished text poster",
            "with integrated approved readable copy",
        )
        cleaned = cleaned.replace(
            "the backend will overlay",
            "the AI render must include",
        )
        if "FINAL TEXT RENDER CONTRACT" not in cleaned:
            cleaned = (
                cleaned.rstrip()
                + " FINAL TEXT RENDER CONTRACT: render the approved readable copy directly in this finished carousel slide; do not use backend text-overlay substrate behavior."
            )
        return cleaned

    @classmethod
    def _enforce_style_reference_only_final_asset_metadata(
        cls,
        metadata: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
        slide_metadata: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Derives enforce style reference only final asset metadata from metadata, request state, and creative decision for AIOrchestratorService.
        # It calls _style_reference_only_carousel_active to turn raw evidence into the structured signal the caller needs.
        cleaned = dict(metadata or {})
        if cls._style_reference_only_carousel_active(
            request,
            creative_decision=creative_decision,
            metadata=slide_metadata,
        ):
            cleaned["text_overlay_strategy"] = "ai_renders_finished_slide_with_exact_copy"
            cleaned.pop("render_overlay_scene_graph", None)
            cleaned.pop("render_overlay_text", None)
            cleaned["style_reference_only_overlay_guard"] = "backend_overlay_disabled"
        return cleaned

    @classmethod
    def _request_has_authoritative_reference_zone_maps(
        cls,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Measures request authoritative reference zone maps from request state for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        if request is None:
            return False
        template_context = request.template_context if isinstance(request.template_context, dict) else {}
        sequence_pack = template_context.get("sequence_pack") if isinstance(template_context.get("sequence_pack"), dict) else {}
        for slide in [item for item in (sequence_pack.get("slides") or []) if isinstance(item, dict)]:
            zone_map = slide.get("zone_map") if isinstance(slide.get("zone_map"), dict) else {}
            raw_zones = zone_map.get("zones")
            if isinstance(raw_zones, list) and len(raw_zones) >= 3:
                return True
        return False

    @classmethod
    def _should_block_low_quality_final_render(cls, assessment: dict[str, Any]) -> bool:
        # Checks block low quality final from assessment for AIOrchestratorService.
        # The boolean result controls the nearby policy or validation branch.
        score = float(assessment.get("score") or 0.0)
        issues = {str(item).strip() for item in (assessment.get("issues") or []) if str(item).strip()}
        hard_failure_issues = {
            "missing_headline",
            "missing_primary_visual",
            "weak_geometry_contract",
            "sample_structure_underused",
            "sparse_scene_graph",
            "craft_direction_weak",
            "reference_family_zone_drift",
            "reference_family_layout_drift",
            "reference_family_geometry_drift",
        }
        current_hard_failures = issues & hard_failure_issues
        if not current_hard_failures:
            return False
        if score < 0.62:
            return True
        return score < cls.IMAGE_QUALITY_MIN_SCORE

    def _fallback_message_strategy(
        self,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes fallback message strategy from request state and compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        brand_copy_brief = compiled_context.get("brand_copy_brief", {}) or {}
        objective_brief = {
            **self._coerce_mapping(request.objective_context),
            **self._coerce_mapping(compiled_context.get("objective_brief")),
        }
        audience_brief = self._coerce_mapping(compiled_context.get("audience_brief"))
        audience_insights = self._coerce_mapping(request.resolved_brand_context.get("audience_insights"))
        persona_context = self._coerce_mapping(request.persona_context)
        knowledge_brief = compiled_context.get("knowledge_brief", []) or []
        prompt_intelligence_brief = compiled_context.get("prompt_intelligence_brief", {}) or {}
        prompt_starter_texts = self._normalize_metadata_list(prompt_intelligence_brief.get("starter_texts"), limit=4)
        prompt_platform_rules = self._normalize_metadata_list(prompt_intelligence_brief.get("current_platform_rules"), limit=4)
        prompt_global_rules = self._normalize_metadata_list(prompt_intelligence_brief.get("global_rules"), limit=4)
        prompt_intelligence_summary = self._coerce_text_value(prompt_intelligence_brief.get("summary"), "")
        content_format_brief = self._coerce_mapping(compiled_context.get("content_format_brief"))
        content_format_name = str(
            content_format_brief.get("format") or request.studio_panel.get("format") or ""
        ).strip().casefold()
        content_format_slide_count = self._int_or_none(content_format_brief.get("preferred_slide_count"))
        content_format_headline_direction = ""
        content_format_supporting_direction = ""
        content_format_cta_intent = ""
        if content_format_brief and content_format_name == "carousel":
            slide_count_phrase = f"a {content_format_slide_count}-slide story" if content_format_slide_count else "the carousel story"
            content_format_headline_direction = f"Open with a hook strong enough to launch {slide_count_phrase}."
            content_format_supporting_direction = (
                "Build a paginated PDF story where each slide earns one idea, keeps the narrative moving, "
                "and avoids cramming multiple proof points onto the same page."
            )
            content_format_cta_intent = "Use a closing-slide CTA that completes the narrative without repeating every slide."
        audience_research_highlights = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("research_highlights"), limit=5),
                *self._normalize_metadata_list(audience_insights.get("research_highlights"), limit=5),
            ],
            limit=5,
        )
        audience_research_summary = self._coerce_text_value(
            audience_brief.get("research_summary") or audience_insights.get("research_summary"),
            "",
        )
        audience_research_note = self._coerce_text_value(
            " ".join(audience_research_highlights[:4]) or audience_research_summary,
            "",
        )
        audience_motivations = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(
                    audience_brief.get("audience_research_motivations") or audience_brief.get("motivations"),
                    limit=4,
                ),
                *self._normalize_metadata_list(audience_insights.get("motivations"), limit=4),
            ],
            limit=4,
        )
        audience_desired_outcomes = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("desired_outcomes"), limit=4),
                *self._normalize_metadata_list(audience_insights.get("desired_outcomes"), limit=4),
            ],
            limit=4,
        )
        audience_trust_signals = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("trust_signals"), limit=4),
                *self._normalize_metadata_list(audience_insights.get("trust_signals"), limit=4),
            ],
            limit=4,
        )
        audience_proof_cues = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("proof_cues"), limit=4),
                *self._normalize_metadata_list(audience_insights.get("proof_cues"), limit=4),
            ],
            limit=4,
        )
        audience_comparison_points = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("comparison_points"), limit=4),
                *self._normalize_metadata_list(audience_insights.get("comparison_points"), limit=4),
            ],
            limit=4,
        )
        audience_objections = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(
                    audience_brief.get("audience_research_objections") or audience_brief.get("objections"),
                    limit=4,
                ),
                *self._normalize_metadata_list(audience_insights.get("objections"), limit=4),
            ],
            limit=4,
        )
        persona_summary = self._coerce_text_value(
            brand_copy_brief.get("persona_messaging_summary")
            or audience_brief.get("persona_summary")
            or audience_insights.get("persona_summary")
            or persona_context.get("summary"),
            "",
        )
        persona_motivations = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("persona_motivations"), limit=4),
                *self._normalize_metadata_list(persona_context.get("motivations"), limit=4),
                *self._normalize_metadata_list(brand_copy_brief.get("persona_motivations"), limit=4),
            ],
            limit=4,
        )
        persona_pain_points = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("persona_pain_points"), limit=4),
                *self._normalize_metadata_list(persona_context.get("pain_points"), limit=4),
                *self._normalize_metadata_list(persona_context.get("fears_and_pain_points"), limit=4),
                *self._normalize_metadata_list(brand_copy_brief.get("persona_pain_points"), limit=4),
            ],
            limit=4,
        )
        persona_objections = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("persona_objections"), limit=4),
                *self._normalize_metadata_list(persona_context.get("objections"), limit=4),
                *self._normalize_metadata_list(brand_copy_brief.get("persona_objections"), limit=4),
            ],
            limit=4,
        )
        persona_language_preference = self._coerce_text_value(
            brand_copy_brief.get("persona_language_preference")
            or audience_brief.get("persona_language_preference")
            or audience_brief.get("language_preference")
            or audience_insights.get("language_preference")
            or persona_context.get("language_preference"),
            "",
        )
        audience_evidence_note = self._coerce_text_value(
            " ".join(
                [
                    *audience_desired_outcomes[:1],
                    *audience_proof_cues[:1],
                    *audience_comparison_points[:1],
                    *audience_trust_signals[:1],
                ]
            ),
            "",
        )
        primary_motivations = audience_motivations or persona_motivations
        primary_pain_points = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(
                    audience_brief.get("audience_research_pain_points") or audience_brief.get("pain_points"),
                    limit=4,
                ),
                *self._normalize_metadata_list(audience_insights.get("pain_points"), limit=4),
            ],
            limit=4,
        ) or persona_pain_points
        audience_research_pain_points = self._normalize_metadata_list(
            audience_brief.get("audience_research_pain_points"),
            limit=4,
        )
        primary_objections = audience_objections or persona_objections
        prompt_topic = self._fallback_topic_focus(request.prompt, compiled_context=compiled_context)
        primary_research_highlight = audience_research_highlights[0] if audience_research_highlights else ""
        semantic_support_phrase = self._topic_specific_detail_lens(prompt_topic)
        fallback_primary_theme = self._coerce_text_value(
            prompt_topic
            or objective_brief.get("name")
            or objective_brief.get("primary_goal")
            or objective_brief.get("description")
            or brand_copy_brief.get("objective_focus")
            or brand_copy_brief.get("brand_description")
            or "Brand-led social campaign"
        )
        objection_grounding = (audience_proof_cues or audience_trust_signals or audience_research_highlights)[:1]
        objection_reassurance = self._coerce_text_value(
            (
                f"Address the objection '{primary_objections[0]}' with specific reassurance, name the pain point '{audience_research_pain_points[0]}', and ground it in '{objection_grounding[0]}'."
                if primary_objections and audience_research_pain_points and objection_grounding
                else f"Address the objection '{primary_objections[0]}' with specific reassurance, name the pain point '{audience_research_pain_points[0]}', and use credible proof."
                if primary_objections and audience_research_pain_points
                else
                f"Address the objection '{primary_objections[0]}' with specific reassurance and ground it in '{objection_grounding[0]}'."
                if primary_objections and objection_grounding
                else f"Address the objection '{primary_objections[0]}' with specific reassurance and credible proof."
                if primary_objections
                else ""
            ),
            "",
        )
        pain_point_framing = self._coerce_text_value(
            (
                f"Lead from the pain point '{primary_pain_points[0]}' and resolve it with a clear benefit."
                if primary_pain_points
                else ""
            ),
            "",
        )
        prompt_text = self._coerce_text_value(request.prompt)
        keywords = [
            word
            for word in re.findall(r"[A-Za-z][A-Za-z0-9'-]{2,}", prompt_text)
            if word.casefold() not in self.PROMPT_ECHO_STOPWORDS
        ][:6]
        knowledge_line = ""
        if knowledge_brief:
            first_item = knowledge_brief[0] if isinstance(knowledge_brief[0], dict) else {}
            knowledge_line = self._coerce_text_value(first_item.get("content"), "")
        key_value_focus = self._coerce_text_value(
            (
                audience_desired_outcomes[0]
                if audience_desired_outcomes
                else audience_comparison_points[0]
                if audience_comparison_points
                else ""
            )
            or objective_brief.get("primary_goal")
            or objective_brief.get("description")
            or brand_copy_brief.get("brand_foundations")
            or "MISSING"
        )
        return {
            "primary_campaign_theme": fallback_primary_theme[:140] or "Brand-led social campaign",
            "core_audience_message": self._coerce_text_value(
                audience_research_note
                or audience_evidence_note
                or persona_summary
                or objective_brief.get("description")
                or prompt_intelligence_summary
                or knowledge_line
                or brand_copy_brief.get("brand_description")
                or "MISSING"
            ),
            "headline_direction": self._coerce_text_value(
                content_format_headline_direction
                or objective_brief.get("market_positioning")
                or (prompt_starter_texts[0] if prompt_starter_texts else "")
                or (f"Lead with the outcome '{audience_desired_outcomes[0]}'" if audience_desired_outcomes else "")
                or (f"Lead with the benefit '{primary_motivations[0]}'" if primary_motivations else "")
                or self._contextual_headline_fallback(
                    topic_focus=prompt_topic,
                    motivation=primary_motivations[0] if primary_motivations else "",
                    pain_point=primary_pain_points[0] if primary_pain_points else "",
                    research_highlight=audience_research_note or primary_research_highlight,
                    brand_foundations=brand_copy_brief.get("brand_foundations"),
                    knowledge_line=knowledge_line,
                    primary_goal=objective_brief.get("primary_goal"),
                    semantic_support_phrase=semantic_support_phrase,
                )
            ),
            "supporting_copy_direction": self._coerce_text_value(
                content_format_supporting_direction
                or prompt_intelligence_summary
                or (prompt_platform_rules[0] if prompt_platform_rules else "")
                or objection_reassurance
                or pain_point_framing
                or audience_research_note
                or audience_evidence_note
                or (
                    f"Keep the copy readable for {persona_language_preference} language preference."
                    if persona_language_preference
                    else ""
                )
                or persona_summary
                or knowledge_line
                or (prompt_global_rules[0] if prompt_global_rules else "")
                or self._contextual_supporting_fallback(
                    topic_focus=prompt_topic,
                    motivation=primary_motivations[0] if primary_motivations else "",
                    pain_point=primary_pain_points[0] if primary_pain_points else "",
                    objection=primary_objections[0] if primary_objections else "",
                    brand_foundations=brand_copy_brief.get("brand_foundations"),
                    research_highlight=primary_research_highlight,
                    knowledge_line=knowledge_line,
                    semantic_support_phrase=semantic_support_phrase,
                )
            ),
            "cta_intent": self._coerce_text_value(
                content_format_cta_intent
                or objective_brief.get("cta_bias")
                or (prompt_platform_rules[1] if len(prompt_platform_rules) > 1 else "")
                or (prompt_global_rules[1] if len(prompt_global_rules) > 1 else "")
                or (prompt_platform_rules[0] if len(prompt_platform_rules) == 1 else "")
                or self._contextual_cta_fallback(
                    topic_focus=prompt_topic or fallback_primary_theme,
                    primary_goal=objective_brief.get("primary_goal"),
                    pain_point=primary_pain_points[0] if primary_pain_points else "",
                    objection=primary_objections[0] if primary_objections else "",
                )
            ),
            "key_value_proposition": key_value_focus,
            "important_keywords": keywords or ["MISSING"],
            "emotional_messaging_direction": self._coerce_text_value(
                brand_copy_brief.get("primary_emotion") or "MISSING"
            ),
            "what_must_be_avoided_in_messaging": self._normalize_metadata_list(
                brand_copy_brief.get("donts") or [brand_copy_brief.get("avoided_emotion") or "MISSING"],
                limit=6,
            )
            or ["MISSING"],
        }

    @staticmethod
    def _infer_layout_type(prompt: str, studio_panel: dict[str, Any], text_payload: dict[str, Any]) -> str:
        # Centralizes infer layout type from prompt text, studio settings, and copy payload for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        lowered_prompt = prompt.lower()
        format_name = str(studio_panel.get("format") or "static")
        if format_name == "infographic":
            return "infographic_stack"
        metadata = text_payload.get("metadata") if isinstance(text_payload.get("metadata"), dict) else {}
        surface_contract = metadata.get("data_list_surface_contract") if isinstance(metadata.get("data_list_surface_contract"), dict) else {}
        requested_count = AIOrchestratorService._int_or_none(surface_contract.get("requested_count")) or AIOrchestratorService._explicit_top_n_item_count(prompt)
        if (
            requested_count
            and requested_count >= 7
            and re.search(r"\b(top\s+\d{1,2}|rank(?:ed|ing)?|table|comparison|compare|versus|vs\.?)\b", lowered_prompt)
        ):
            return "ranking_table"
        if any(token in lowered_prompt for token in ["tips", "tip", "strategies", "strategy", "steps", "checklist", "how to"]):
            return "checklist_card"
        if any(token in lowered_prompt for token in ["compare", "comparison", "versus", "vs "]):
            return "comparison_board"
        if len(str(text_payload.get("body") or "")) > 220:
            return "insight_split"
        return "editorial_hero"

    @staticmethod
    def _normalized_box(x: float, y: float, width: float, height: float) -> dict[str, Any]:
        # Normalizes box from x, y, and width for AIOrchestratorService.
        # Later prompt and metadata code can compare the cleaned value directly.
        return {"x": x, "y": y, "width": width, "height": height, "units": "normalized"}

    @staticmethod
    def _logo_safe_zone_geometry(scene_graph: GenerationSceneGraph | None) -> tuple[float, float, float, float] | None:
        # Measures logo safe zone geometry from scene graph for AIOrchestratorService.
        # Keeping the math here avoids subtle differences between renderer-facing branches.
        if not isinstance(scene_graph, GenerationSceneGraph):
            return None
        for element in scene_graph.elements or []:
            role = str(element.role or element.element_type or "").strip().lower()
            if role != "logo":
                continue
            geometry = element.geometry
            try:
                x = float(geometry.x if geometry.x is not None else 0.0)
                y = float(geometry.y if geometry.y is not None else 0.0)
                width = float(geometry.width if geometry.width is not None else 0.0)
                height = float(geometry.height if geometry.height is not None else 0.0)
            except (TypeError, ValueError):
                return None
            if width <= 0 or height <= 0:
                return None
            units = str(geometry.units or "normalized").strip().lower()
            canvas_width = max(int(scene_graph.canvas.width or 1080), 1)
            canvas_height = max(int(scene_graph.canvas.height or 1080), 1)
            if units == "normalized" or max(abs(x), abs(y), abs(width), abs(height)) <= 1.5:
                return (x, y, width, height)
            return (
                x / canvas_width,
                y / canvas_height,
                width / canvas_width,
                height / canvas_height,
            )
        return None

    @classmethod
    def _logo_anchor_from_hint(cls, hint: str | None) -> tuple[str, str] | None:
        # Centralizes logo anchor hint from hint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = str(hint or "").strip().lower()
        if not text:
            return None
        text = text.replace("buttom", "bottom")
        vertical = "top" if "top" in text else ("bottom" if "bottom" in text else "middle")
        horizontal = "right" if "right" in text else ("left" if "left" in text else "center")
        if vertical == "middle" and horizontal == "center" and "center" not in text and "middle" not in text:
            return None
        return (vertical, horizontal)

    @classmethod
    def _normalize_logo_position_option(cls, value: Any) -> str:
        # Normalizes logo position option from input value for AIOrchestratorService.
        # It delegates shared cleanup to _logo_anchor_from_hint and _normalize_metadata_text before returning the cleaned value.
        text = cls._normalize_metadata_text(value, limit=80).casefold()
        if not text:
            return ""
        text = text.replace("_", "-").replace("buttom", "bottom")
        anchor = cls._logo_anchor_from_hint(text)
        if anchor is None:
            if "center" in text or "middle" in text:
                return "center"
            return ""
        vertical, horizontal = anchor
        if vertical == "middle" and horizontal == "center":
            return "center"
        if vertical == "middle":
            return f"center-{horizontal}"
        if horizontal == "center":
            return f"{vertical}-center"
        return f"{vertical}-{horizontal}"

    @classmethod
    def _brand_logo_placement_policy(cls, brand_context: dict[str, Any] | None) -> dict[str, Any]:
        # Builds brand logo placement policy from brand context for AIOrchestratorService.
        # It calls _normalize_logo_position_option while assembling the payload or prompt text.
        visual_identity = (
            brand_context.get("visual_identity", {}) if isinstance(brand_context, dict) else {}
        ) or {}
        placement = (
            visual_identity.get("logo_placement")
            if isinstance(visual_identity.get("logo_placement"), dict)
            else {}
        )
        allowed_positions: list[str] = []
        for raw_value in placement.get("allowed_positions") or placement.get("positions") or []:
            normalized = cls._normalize_logo_position_option(raw_value)
            if normalized and normalized not in allowed_positions:
                allowed_positions.append(normalized)
        has_explicit_allowed_positions = bool(allowed_positions)
        default_position = cls._normalize_logo_position_option(
            placement.get("default_position")
            or placement.get("preferred_position")
            or placement.get("logo_position")
        )
        promoted_position = cls._normalize_logo_position_option(
            visual_identity.get("logo_position")
            or (
                (visual_identity.get("design_system") or {})
                if isinstance(visual_identity.get("design_system"), dict)
                else {}
            ).get("logo_anchor")
        )
        if has_explicit_allowed_positions:
            if default_position and default_position not in allowed_positions:
                default_position = ""
        else:
            default_position = default_position or promoted_position
        if not default_position and allowed_positions:
            default_position = allowed_positions[0]
        if allowed_positions and default_position and default_position not in allowed_positions:
            default_position = allowed_positions[0] if allowed_positions else ""
        return {
            "allowed_positions": allowed_positions,
            "default_position": (
                default_position
                if (not allowed_positions or default_position in allowed_positions)
                else (allowed_positions[0] if allowed_positions else "")
            ),
        }

    @classmethod
    def _logo_position_priority_for_request(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> list[str]:
        # Measures logo position priority request from request state and creative decision for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        format_name = str(request.studio_panel.get("format") or "").strip().lower()
        layout_mode = str((creative_decision.layout_mode if isinstance(creative_decision, CreativeDecisionPayload) else "") or "").strip().lower()
        if format_name == "infographic":
            priority = ["top-right", "top-left", "top-center", "bottom-right", "bottom-left", "bottom-center", "center"]
        elif format_name == "carousel":
            priority = ["top-right", "top-left", "bottom-right", "bottom-left", "top-center", "bottom-center", "center"]
        else:
            priority = ["top-right", "top-left", "top-center", "bottom-right", "bottom-left", "bottom-center", "center"]
        if "footer" in layout_mode or "bottom" in layout_mode:
            priority = ["top-right", "top-left", "top-center", "bottom-right", "bottom-left", "bottom-center", "center"]
        return priority

    @classmethod
    def _sample_blueprint_logo_position(cls, sample_page_blueprint: Any) -> str:
        # Measures sample blueprint logo position from sample page blueprint for AIOrchestratorService.
        # It calls _normalize_logo_position_option and _anchor_from_logo_geometry to turn raw evidence into the structured signal the caller needs.
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}
        if not blueprint:
            return ""
        for key in ("logo_position", "brand_header_position", "brand_logo_position"):
            normalized = cls._normalize_logo_position_option(blueprint.get(key))
            if normalized:
                return normalized
        zones: list[Any] = []
        for key in ("zones", "image_zones", "content_zones"):
            value = blueprint.get(key)
            if isinstance(value, list):
                zones.extend(value)
        for zone in zones:
            payload = zone if isinstance(zone, dict) else {}
            role_text = " ".join(
                cls._normalize_metadata_text(payload.get(key), limit=80)
                for key in ("role", "name", "label", "zone_role", "type")
            ).casefold()
            if "logo" not in role_text and "brand header" not in role_text:
                continue
            normalized = cls._normalize_logo_position_option(role_text)
            if normalized:
                return normalized
            try:
                x = float(payload.get("x"))
                y = float(payload.get("y"))
                width = float(payload.get("width") if payload.get("width") is not None else payload.get("w"))
                height = float(payload.get("height") if payload.get("height") is not None else payload.get("h"))
            except (TypeError, ValueError):
                continue
            if min(x, y, width, height) < 0:
                continue
            if max(x, y, width, height) > 1.5:
                continue
            return cls._normalize_logo_position_option(cls._anchor_from_logo_geometry((x, y, width, height)))
        return ""

    @classmethod
    def _effective_logo_position_hint(
        cls,
        *,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload,
        text_payload: dict[str, Any],
        scene_graph_payload: dict[str, Any] | None = None,
    ) -> str:
        # Measures effective logo position hint from request state, creative decision, and copy payload for AIOrchestratorService.
        # It calls _brand_logo_placement_policy and _sample_blueprint_logo_position to turn raw evidence into the structured signal the caller needs.
        policy = cls._brand_logo_placement_policy(request.resolved_brand_context)
        allowed_positions = policy.get("allowed_positions") or []
        default_position = str(policy.get("default_position") or "")
        metadata = text_payload.get("metadata") if isinstance(text_payload.get("metadata"), dict) else {}
        asset_strategy = creative_decision.asset_strategy or {}
        planning_hints = creative_decision.planning_hints or {}
        scene_graph_payload = scene_graph_payload or {}
        styles = scene_graph_payload.get("styles") if isinstance(scene_graph_payload.get("styles"), dict) else {}
        validation_hints = scene_graph_payload.get("validation_hints") if isinstance(scene_graph_payload.get("validation_hints"), dict) else {}
        sample_logo_position = cls._sample_blueprint_logo_position(metadata.get("sample_page_blueprint"))
        if default_position in {"top-right", "top-left", "top-center"}:
            return default_position
        for candidate in (
            sample_logo_position,
            metadata.get("logo_position"),
            planning_hints.get("logo_position"),
            asset_strategy.get("logo_position"),
            styles.get("logo_position"),
            validation_hints.get("logo_position"),
        ):
            normalized = cls._normalize_logo_position_option(candidate)
            if normalized and (not allowed_positions or normalized in allowed_positions):
                return normalized
        if default_position:
            return default_position
        if not allowed_positions:
            for candidate in cls._logo_position_priority_for_request(request, creative_decision):
                if candidate:
                    return candidate
        for candidate in cls._logo_position_priority_for_request(request, creative_decision):
            if candidate in allowed_positions:
                return candidate
        return ""

    @classmethod
    def _logo_reserved_area_label(cls, hint: str | None) -> str:
        # Extracts logo reserved area label from hint for AIOrchestratorService.
        # It calls _normalize_logo_position_option to turn raw evidence into the structured signal the caller needs.
        normalized = cls._normalize_logo_position_option(hint)
        return normalized or "reserved corner-safe"

    @staticmethod
    def _logo_size_scale() -> float:
        # Centralizes logo size scale for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        return 0.96

    @staticmethod
    def _scaled_logo_ratio(value: float) -> float:
        # Centralizes scaled logo ratio from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return max(float(value) * AIOrchestratorService._logo_size_scale(), 0.0)

    @staticmethod
    def _logo_horizontal_margin_ratio(canvas_width: int, width: float) -> float:
        # Centralizes logo horizontal margin ratio from canvas width and width for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return min(20 / max(float(canvas_width), 1.0), max(1.0 - width, 0.0))

    @staticmethod
    def _logo_top_margin_ratio(canvas_height: int, height: float) -> float:
        # Centralizes logo top margin ratio from canvas height and height for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        dynamic_margin = max(20 / max(float(canvas_height), 1.0), 0.035)
        return min(dynamic_margin, max(1.0 - height, 0.0))

    @staticmethod
    def _logo_bottom_margin_ratio(canvas_height: int, height: float) -> float:
        # Centralizes logo bottom margin ratio from canvas height and height for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        return min(20 / max(float(canvas_height), 1.0), max(1.0 - height, 0.0))

    @classmethod
    def _default_logo_safe_zone_geometry(
        cls,
        request: AIOrchestrationRequest,
        *,
        anchor: tuple[str, str] | None,
    ) -> tuple[float, float, float, float]:
        # Measures default logo safe zone geometry from request state and anchor for AIOrchestratorService.
        # It calls _logo_box_profile_for_panel and _reference_logo_safe_zone_geometry to turn raw evidence into the structured signal the caller needs.
        width, height = cls._logo_box_profile_for_panel(request.studio_panel)
        reference_geometry = cls._reference_logo_safe_zone_geometry(request=request, anchor=anchor)
        vertical, horizontal = anchor or ("top", "right")
        size = (request.studio_panel or {}).get("size")
        size = size if isinstance(size, dict) else {}
        canvas_width = max(int(size.get("width") or 1080), 1)
        canvas_height = max(int(size.get("height") or 1080), 1)
        if reference_geometry is not None:
            ref_x, ref_y, ref_width, ref_height = reference_geometry
            safe_padding_x = max(min(48 / canvas_width, 0.06), min(ref_width * 0.4, 0.06))
            safe_padding_y = max(min(32 / canvas_height, 0.04), min(ref_height * 0.25, 0.04))
            width = min(max(width, ref_width + safe_padding_x), 0.4)
            height = min(max(height, ref_height + safe_padding_y), 0.24)
            if horizontal == "left":
                x = min(max(ref_x, 0.0), max(1.0 - width, 0.0))
            elif horizontal == "center":
                x = max(min(ref_x + (ref_width / 2.0) - (width / 2.0), 1.0 - width), 0.0)
            else:
                x = max(min(ref_x + ref_width - width, 1.0 - width), 0.0)
            if vertical == "bottom":
                y = max(min(ref_y + ref_height - height, 1.0 - height), 0.0)
            elif vertical == "middle":
                y = max(min(ref_y + (ref_height / 2.0) - (height / 2.0), 1.0 - height), 0.0)
            else:
                y = min(max(ref_y, 0.0), max(1.0 - height, 0.0))
            return (x, y, width, height)
        margin_x = cls._logo_horizontal_margin_ratio(canvas_width, width)
        margin_top = cls._logo_top_margin_ratio(canvas_height, height)
        margin_bottom = cls._logo_bottom_margin_ratio(canvas_height, height)
        if horizontal == "left":
            x = margin_x
        elif horizontal == "center":
            x = max((1.0 - width) / 2.0, 0.0)
        else:
            x = max(1.0 - width - margin_x, 0.0)
        if vertical == "bottom":
            y = max(1.0 - height - margin_bottom, 0.0)
        elif vertical == "middle":
            y = max((1.0 - height) / 2.0, 0.0)
        else:
            y = margin_top
        return (x, y, width, height)

    @classmethod
    def _snap_logo_safe_zone_geometry_to_anchor_edge(
        cls,
        *,
        request: AIOrchestrationRequest,
        geometry: tuple[float, float, float, float],
        anchor: tuple[str, str],
    ) -> tuple[float, float, float, float]:
        # Snaps snap logo safe zone geometry anchor edge from request state, geometry, and anchor for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        x, y, width, height = geometry
        size = (request.studio_panel or {}).get("size")
        size = size if isinstance(size, dict) else {}
        canvas_width = max(int(size.get("width") or 1080), 1)
        canvas_height = max(int(size.get("height") or 1080), 1)
        margin_x = cls._logo_horizontal_margin_ratio(canvas_width, width)
        margin_top = cls._logo_top_margin_ratio(canvas_height, height)
        margin_bottom = cls._logo_bottom_margin_ratio(canvas_height, height)
        vertical, horizontal = anchor
        if horizontal == "left":
            x = margin_x
        elif horizontal == "right":
            x = max(1.0 - width - margin_x, 0.0)
        elif horizontal == "center":
            x = max((1.0 - width) / 2.0, 0.0)
        if vertical == "top":
            y = margin_top
        elif vertical == "bottom":
            y = max(1.0 - height - margin_bottom, 0.0)
        elif vertical == "middle":
            y = max((1.0 - height) / 2.0, 0.0)
        return (x, y, width, height)

    @classmethod
    def _cap_logo_safe_zone_geometry_to_profile(
        cls,
        *,
        request: AIOrchestrationRequest,
        geometry: tuple[float, float, float, float],
    ) -> tuple[float, float, float, float]:
        # Caps cap logo safe zone geometry profile from request state and geometry for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        x, y, width, height = geometry
        preferred_width, preferred_height = cls._logo_box_profile_for_panel(request.studio_panel)
        return (x, y, min(width, preferred_width), min(height, preferred_height))

    @staticmethod
    def _logo_box_profile_for_panel(studio_panel: dict[str, Any] | None) -> tuple[float, float]:
        # Measures logo box profile panel from studio settings for AIOrchestratorService.
        # It calls _scaled_logo_ratio to turn raw evidence into the structured signal the caller needs.
        panel = studio_panel or {}
        format_name = str(panel.get("format") or "").strip().lower()
        size = panel.get("size") if isinstance(panel.get("size"), dict) else {}
        width_px = max(int(size.get("width") or 1080), 1)
        height_px = max(int(size.get("height") or 1080), 1)
        aspect_ratio = width_px / max(height_px, 1)
        if format_name == "carousel":
            return (AIOrchestratorService._scaled_logo_ratio(0.2), AIOrchestratorService._scaled_logo_ratio(0.085))
        if format_name == "infographic":
            return (AIOrchestratorService._scaled_logo_ratio(0.19), AIOrchestratorService._scaled_logo_ratio(0.08))
        if aspect_ratio >= 1.3:
            return (AIOrchestratorService._scaled_logo_ratio(0.15), AIOrchestratorService._scaled_logo_ratio(0.075))
        return (AIOrchestratorService._scaled_logo_ratio(0.17), AIOrchestratorService._scaled_logo_ratio(0.075))

    @classmethod
    def _reference_logo_safe_zone_geometry(
        cls,
        *,
        request: AIOrchestrationRequest,
        anchor: tuple[str, str] | None,
    ) -> tuple[float, float, float, float] | None:
        # Measures reference logo safe zone geometry from request state and anchor for AIOrchestratorService.
        # It calls _coerce_list and _coerce_mapping to turn raw evidence into the structured signal the caller needs.
        if anchor is None:
            return None
        visual_identity = (
            request.resolved_brand_context.get("visual_identity", {})
            if isinstance(request.resolved_brand_context, dict)
            else {}
        ) or {}
        candidates: list[tuple[float, float, float, float]] = []

        def _zone_geometry(zone: Any) -> tuple[float, float, float, float] | None:
            # Measures zone geometry from zone for AIOrchestratorService.
            # It calls _coerce_mapping to turn raw evidence into the structured signal the caller needs.
            payload = cls._coerce_mapping(zone)
            if not payload:
                return None
            try:
                x = float(payload.get("x"))
                y = float(payload.get("y"))
                width = float(payload.get("width") if payload.get("width") is not None else payload.get("w"))
                height = float(payload.get("height") if payload.get("height") is not None else payload.get("h"))
            except (TypeError, ValueError):
                return None
            if min(x, y, width, height) < 0:
                return None
            if max(x, y, width, height) > 1.5:
                return None
            if width <= 0 or height <= 0:
                return None
            return (x, y, width, height)

        def _collect(zones: Any) -> None:
            # Collects collect from zones for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            for zone in cls._coerce_list(zones):
                payload = cls._coerce_mapping(zone)
                if str(payload.get("role") or "").strip().lower() != "logo":
                    continue
                geometry = _zone_geometry(payload)
                if geometry is None:
                    continue
                if cls._logo_anchor_from_hint(cls._anchor_from_logo_geometry(geometry)) == anchor:
                    candidates.append(geometry)

        for reference in cls._coerce_list(visual_identity.get("reference_creatives")):
            payload = cls._coerce_mapping(reference)
            _collect(payload.get("reusable_zones"))
            layout_structure = cls._coerce_mapping(payload.get("layout_structure"))
            _collect(layout_structure.get("zones"))
        for template_info in cls._coerce_list(visual_identity.get("template_intelligence")):
            payload = cls._coerce_mapping(template_info)
            analysis = cls._coerce_mapping(payload.get("analysis"))
            _collect(analysis.get("reusable_zones"))
            _collect(analysis.get("editable_zones"))
        if not candidates:
            return None

        def _median(values: list[float]) -> float:
            # Centralizes median from values for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            ordered = sorted(values)
            return ordered[len(ordered) // 2]

        return (
            _median([item[0] for item in candidates]),
            _median([item[1] for item in candidates]),
            _median([item[2] for item in candidates]),
            _median([item[3] for item in candidates]),
        )

    @staticmethod
    def _normalize_logo_background_tone(value: Any) -> str:
        # Normalizes logo background tone from input value for AIOrchestratorService.
        # Later prompt and metadata code can compare the cleaned value directly.
        text = str(value or "").strip().casefold()
        if not text:
            return ""
        normalized = text.replace("-", "_").replace(" ", "_")
        if any(token in normalized for token in ("light_on_dark", "dark_surface", "dark_background", "reverse", "inverse", "negative")):
            return "dark"
        if any(token in normalized for token in ("dark_on_light", "light_surface", "light_background", "full_color", "full_colour")):
            return "light"
        if any(token in normalized for token in ("neutral", "balanced", "mid_tone", "midtone")):
            return "neutral"
        return ""

    @classmethod
    def _resolve_logo_background_tone(
        cls,
        *,
        metadata: dict[str, Any] | None = None,
        creative_decision: CreativeDecisionPayload | dict[str, Any] | None = None,
        scene_graph: GenerationSceneGraph | None = None,
    ) -> str:
        # Resolves logo background tone from metadata, creative decision, and scene graph for AIOrchestratorService.
        # The caller receives the canonical identifier/path/config value expected by the next step.
        candidates: list[Any] = []
        metadata = metadata if isinstance(metadata, dict) else {}
        candidates.append(metadata.get("logo_background_tone"))
        if isinstance(creative_decision, CreativeDecisionPayload):
            asset_strategy = creative_decision.asset_strategy or {}
        elif isinstance(creative_decision, dict):
            asset_strategy = creative_decision.get("asset_strategy") if isinstance(creative_decision.get("asset_strategy"), dict) else {}
        else:
            asset_strategy = {}
        candidates.extend(
            [
                asset_strategy.get("logo_background_tone"),
                asset_strategy.get("background_variant"),
                asset_strategy.get("logo_variant"),
            ]
        )
        if isinstance(scene_graph, GenerationSceneGraph):
            for element in scene_graph.elements or []:
                if str(element.role or element.element_type or "").strip().lower() != "logo":
                    continue
                candidates.extend(
                    [
                        (element.validation_hints or {}).get("logo_background_tone"),
                        (element.style or {}).get("logo_background_tone"),
                        (element.validation_hints or {}).get("logo_variant"),
                        (element.style or {}).get("logo_variant"),
                        element.asset.variant if element.asset else None,
                    ]
                )
        for value in candidates:
            normalized = cls._normalize_logo_background_tone(value)
            if normalized:
                return normalized
        return ""

    @classmethod
    def _logo_surface_guidance(
        cls,
        *,
        background_tone: str | None,
    ) -> str:
        # Builds logo surface guidance from background tone for AIOrchestratorService.
        # It calls _normalize_logo_background_tone while assembling the payload or prompt text.
        tone = cls._normalize_logo_background_tone(background_tone)
        if tone == "light":
            return (
                "Keep the reserved safe zone on a calm light surface with low texture, soft gradients only, clean edge contrast, and transparent edges for the exact overlay asset."
            )
        if tone == "dark":
            return (
                "Keep the reserved safe zone on a calm dark surface with low texture, stable contrast, and transparent edges for the exact overlay asset."
            )
        return (
            "Keep the reserved safe zone on a smooth, visually quiet surface with stable contrast, transparent edges for the exact overlay asset, and no noisy texture, glass effects, or cutout-like detail."
        )

    @classmethod
    def _logo_non_generation_contract(cls, *, brand_name: str | None, reserved_logo_area: str) -> list[str]:
        # Builds logo non generation contract from brand name and reserved logo area for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        normalized_brand_name = cls._normalize_metadata_text(brand_name, limit=80)
        brand_label = normalized_brand_name or "the brand name"
        return [
            "STRICT BRAND-ASSET NON-GENERATION CONTRACT: Never create, imitate, stylize, reconstruct, approximate, trace, draw, typeset, watermark, or decorate with brand identity artwork. Leave every reserved brand-asset area blank and clean; the backend will place the exact stored brand asset afterward.",
            f"Brand context only: {brand_label}. Use this for palette, tone, and approved copy context only, never as a brand mark, masthead, signature, watermark, standalone brand mark, or top-corner wordmark.",
            "ZERO BRAND-ASSET RULE - no exceptions: do not generate any brand identity asset, fake identity mark, placeholder mark, mark-shaped decoration, or brand-name text anywhere in the AI base creative. The backend overlay is the only allowed source for brand asset placement.",
            "BRAND MARK RULE - no exceptions: the AI base creative must contain zero brand marks, wordmarks, brand-name signatures, monograms, watermarks, brand-mark-like shapes, symbol clusters, initials, mascot marks, or brand marks anywhere in the image.",
            "Do not render, invent, stylize, trace, emboss, blur, shadow, crop, duplicate, partially duplicate, fade, ghost, hint at, or reserve a visible placeholder for any brand mark or wordmark. The exact stored brand mark is composited afterward as a separate asset.",
            f"Never write or paint {brand_label} in the corner-safe area. If approved legal/footer copy contains the brand name, it may appear only inside the legal/footer text region, never as a brand mark, header signature, watermark, or decorative brand lockup.",
            f"The {reserved_logo_area} area must remain pure background reservation only: blank/quiet surface, no placeholder text, no ghosted wordmark, no orange/navy brand-mark-like marks, no icon cluster, no badge, no plate, no chip, and no decorative detail.",
            "For the reserved safe rectangle, no readable text may overlap that rectangle or its immediate padding; when large text shares that band, keep its right edge before about the reserved zone and use additional lines rather than stretching under the logo.",
        ]

    @classmethod
    def _normalize_logo_safe_zone_geometry(
        cls,
        *,
        request: AIOrchestrationRequest,
        geometry: tuple[float, float, float, float] | None,
        hint: str | None = None,
    ) -> tuple[float, float, float, float]:
        # Normalizes logo safe zone geometry from request state, geometry, and hint for AIOrchestratorService.
        # It delegates shared cleanup to _logo_anchor_from_hint and _reference_logo_safe_zone_geometry before returning the cleaned value.
        hint_anchor = cls._logo_anchor_from_hint(hint)
        reference_geometry = cls._reference_logo_safe_zone_geometry(request=request, anchor=hint_anchor)
        if geometry is None:
            return cls._default_logo_safe_zone_geometry(request, anchor=hint_anchor)
        x, y, width, height = geometry
        center_x = x + (width / 2.0)
        center_y = y + (height / 2.0)
        vertical = "top" if center_y <= 0.35 else ("bottom" if center_y >= 0.65 else "middle")
        horizontal = "left" if center_x <= 0.35 else ("right" if center_x >= 0.65 else "center")
        current_anchor = (vertical, horizontal)
        anchor_mismatch = bool(hint_anchor and hint_anchor != current_anchor)
        min_width, min_height = cls._logo_box_profile_for_panel(request.studio_panel)
        too_small = width < (min_width * 0.65) or height < (min_height * 0.65)
        if anchor_mismatch or too_small:
            return cls._default_logo_safe_zone_geometry(request, anchor=hint_anchor or current_anchor)
        if reference_geometry is not None:
            _ref_x, _ref_y, ref_width, ref_height = reference_geometry
            width_gap = abs(width - ref_width)
            height_gap = abs(height - ref_height)
            if width_gap >= 0.035 or height_gap >= 0.025:
                return cls._default_logo_safe_zone_geometry(request, anchor=hint_anchor or current_anchor)
        geometry = cls._cap_logo_safe_zone_geometry_to_profile(
            request=request,
            geometry=geometry,
        )
        return cls._snap_logo_safe_zone_geometry_to_anchor_edge(
            request=request,
            geometry=geometry,
            anchor=hint_anchor or current_anchor,
        )

    @classmethod
    def _logo_safe_zone_guidance(
        cls,
        request: AIOrchestrationRequest,
        scene_graph: GenerationSceneGraph | None = None,
        hint: str | None = None,
    ) -> str:
        # Measures logo safe zone guidance from request state, scene graph, and hint for AIOrchestratorService.
        # It calls _logo_safe_zone_geometry and _normalize_logo_safe_zone_geometry to turn raw evidence into the structured signal the caller needs.
        geometry = cls._logo_safe_zone_geometry(scene_graph)
        geometry = cls._normalize_logo_safe_zone_geometry(request=request, geometry=geometry, hint=hint)
        x, y, width, height = geometry
        center_x = x + (width / 2.0)
        center_y = y + (height / 2.0)
        if center_y <= 0.33:
            vertical_anchor = "top"
        elif center_y >= 0.67:
            vertical_anchor = "bottom"
        else:
            vertical_anchor = "middle"
        if center_x <= 0.33:
            horizontal_anchor = "left"
        elif center_x >= 0.67:
            horizontal_anchor = "right"
        else:
            horizontal_anchor = "center"
        if horizontal_anchor == "center" and vertical_anchor == "middle":
            anchor_phrase = "center area"
        elif horizontal_anchor == "center":
            anchor_phrase = f"{vertical_anchor} center"
        elif vertical_anchor == "middle":
            anchor_phrase = f"middle {horizontal_anchor}"
        else:
            anchor_phrase = f"{vertical_anchor}-{horizontal_anchor}"
        width_pct = max(int(round(width * 100)), 8)
        height_pct = max(int(round(height * 100)), 5)
        left_pct = max(int(round(x * 100)), 0)
        top_pct = max(int(round(y * 100)), 0)
        size = (request.studio_panel or {}).get("size")
        size = size if isinstance(size, dict) else {}
        canvas = getattr(scene_graph, "canvas", None)
        canvas_width = max(int(getattr(canvas, "width", None) or size.get("width") or 1080), 1)
        canvas_height = max(int(getattr(canvas, "height", None) or size.get("height") or 1080), 1)
        left_px = max(int(round(x * canvas_width)), 0)
        top_px = max(int(round(y * canvas_height)), 0)
        width_px = max(int(round(width * canvas_width)), 1)
        height_px = max(int(round(height * canvas_height)), 1)
        bottom_limit_pct = max(int(round(min(y + height + 0.04, 1.0) * 100)), 0)
        same_band_rule = ""
        if horizontal_anchor == "right":
            right_limit_pct = max(int(round(max(x - 0.04, 0.0) * 100)), 0)
            same_band_rule = (
                f"Any headline or large text that begins in the same top band as this safe zone must wrap before it reaches the zone: "
                f"keep its right edge before about {right_limit_pct}% of canvas width, and use additional lines rather than stretching under the zone."
            )
        elif horizontal_anchor == "left":
            left_limit_pct = min(int(round(min(x + width + 0.04, 1.0) * 100)), 100)
            same_band_rule = (
                f"Any headline or large text that begins in the same top band as this safe zone must start after about {left_limit_pct}% of canvas width, "
                "and use additional lines rather than stretching under the zone."
            )
        return (
            f"Reserve a clean empty safe zone in the {anchor_phrase} of the canvas, roughly {width_pct}% of the width and {height_pct}% of the height. "
            "This is the backend-calculated brand-asset reservation; keep this exact box blank because the backend places the stored brand asset there after image generation. "
            f"Initial backend brand-asset size analysis: before composing the image, reserve the final placement footprint as an empty box at about {left_px}px from the left and {top_px}px from the top, {width_px}px wide x {height_px}px tall on the target canvas. "
            f"Treat the safe rectangle as starting around {left_pct}% from the left and {top_pct}% from the top; no readable text may overlap that rectangle or its immediate padding. "
            "Leave that area visually calm and completely empty with no text, icons, faces, charts, decorative marks, or high-contrast detail. "
            "Do not mark the zone with a visible badge, panel, tile, chip, colored patch, box, plate, or placeholder shape. "
            f"No headline, body copy, supporting text, proof point, stat, CTA, or any content element may appear inside or immediately adjacent to the {anchor_phrase} safe zone — keep a clear margin around it. "
            f"The {anchor_phrase} area must have a clean, calm, low-contrast background without any content, text, or decorative element crowding it from any side. "
            f"Keep the vertical safe band clear through about {bottom_limit_pct}% of canvas height. "
            f"{same_band_rule}"
        )

    @staticmethod
    def _prior_layout_archetype(request: AIOrchestrationRequest) -> str:
        # Centralizes prior layout archetype from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        latest_content = (request.session_memory or {}).get("latest_content_version", {}) or {}
        scene_graph = latest_content.get("scene_graph", {}) if isinstance(latest_content, dict) else {}
        styles = scene_graph.get("styles", {}) if isinstance(scene_graph, dict) else {}
        generation_decision = latest_content.get("generation_decision", {}) if isinstance(latest_content, dict) else {}
        archetype = (
            styles.get("layout_archetype")
            or generation_decision.get("layout_archetype")
            or (generation_decision.get("planning_hints") or {}).get("layout_archetype")
        )
        return str(archetype or "").strip().casefold()

    @staticmethod
    def _follow_up_mode(request: AIOrchestrationRequest) -> str:
        # Centralizes follow up mode from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        follow_up_intent = (request.session_memory or {}).get("follow_up_intent", {}) or {}
        return str(follow_up_intent.get("mode") or "").strip().casefold()

    @classmethod
    def _choose_layout_archetype(
        cls,
        *,
        request: AIOrchestrationRequest,
        candidates: list[str],
        default: str,
    ) -> str:
        # Chooses layout archetype from request state, candidates, and default for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        follow_up_mode = cls._follow_up_mode(request)
        if follow_up_mode != "variant_of_previous":
            return default
        prior_archetype = cls._prior_layout_archetype(request)
        for candidate in candidates:
            if candidate.casefold() != prior_archetype:
                return candidate
        return default

    def _image_led_layout_profile(
        self,
        *,
        request: AIOrchestrationRequest,
        text_payload: dict[str, Any],
        compiled_context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Builds image led layout profile from request state, copy payload, and compiled context for AIOrchestratorService.
        # It calls _infer_layout_type and _extract_layout_dna_from_compiled_context while assembling the payload or prompt text.
        size = request.studio_panel.get("size") or BlueprintService.PRESET_DIMENSIONS.get(
            request.studio_panel.get("platform_preset", "instagram"),
            BlueprintService.PRESET_DIMENSIONS["instagram"],
        )
        width = max(int(size.get("width") or 1080), 1)
        height = max(int(size.get("height") or 1080), 1)
        aspect_ratio = width / height
        format_name = str(request.studio_panel.get("format") or "static")
        layout_type = self._infer_layout_type(request.prompt, request.studio_panel, text_payload)

        # Prefer exact template/sample layout DNA from compiled context, then fall back to brand-level reference DNA.
        layout_dna = self._extract_layout_dna_from_compiled_context(compiled_context or {}, format_name)
        if not layout_dna:
            layout_dna = self._extract_layout_dna_from_brand_context(request.resolved_brand_context, format_name)
        if layout_dna and layout_dna.get("zones"):
            # Use extracted layout DNA instead of hardcoded profiles
            return self._build_profile_from_layout_dna(layout_dna, layout_type, format_name)

        if format_name == "story" or aspect_ratio <= 0.78:
            archetype = self._choose_layout_archetype(
                request=request,
                candidates=["story_focus_stack"],
                default="story_focus_stack",
            )
            return {
                "layout_type": layout_type,
                "layout_archetype": archetype,
                "hero_image": self._normalized_box(0.08, 0.1, 0.84, 0.42),
                "overlay_panel": self._normalized_box(0.05, 0.56, 0.9, 0.34),
                "logo": self._normalized_box(0.08, 0.59, 0.22, 0.06),
                "headline": self._normalized_box(0.08, 0.68, 0.78, 0.09),
                "supporting_line": self._normalized_box(0.08, 0.78, 0.78, 0.07),
                "proof_points": self._normalized_box(0.08, 0.86, 0.78, 0.09),
                "cta": self._normalized_box(0.08, 0.93, 0.38, 0.05),
                "decorative_shapes": [
                    {
                        "element_id": "accent_badge",
                        "element_type": "decorative_shape",
                        "role": "decorative_shape",
                        "layer": "decorative",
                        "geometry": self._normalized_box(0.74, 0.58, 0.16, 0.08),
                        "style": {"shape": "rounded_rect", "fill_role": "accent", "border_radius": 20},
                    }
                ],
            }

        if format_name == "infographic" or layout_type == "infographic_stack":
            archetype = self._choose_layout_archetype(
                request=request,
                candidates=["infographic_stack", "insight_corner_split", "comparison_corner_card"],
                default="infographic_stack",
            )
            return {
                "layout_type": layout_type,
                "layout_archetype": archetype,
                "hero_image": self._normalized_box(0.6, 0.14, 0.28, 0.24),
                "overlay_panel": self._normalized_box(0.05, 0.08, 0.9, 0.84),
                "logo": self._normalized_box(0.74, 0.1, 0.17, 0.06),
                "headline": self._normalized_box(0.08, 0.13, 0.48, 0.14),
                "supporting_line": self._normalized_box(0.08, 0.29, 0.46, 0.1),
                "proof_points": self._normalized_box(0.08, 0.43, 0.84, 0.3),
                "cta": self._normalized_box(0.08, 0.8, 0.34, 0.07),
                "decorative_shapes": [
                    {
                        "element_id": "stat_highlight_badge",
                        "element_type": "decorative_shape",
                        "role": "decorative_shape",
                        "layer": "decorative",
                        "geometry": self._normalized_box(0.08, 0.37, 0.18, 0.05),
                        "style": {"shape": "rounded_rect", "fill_role": "accent", "border_radius": 18},
                    },
                    {
                        "element_id": "section_divider",
                        "element_type": "decorative_shape",
                        "role": "decorative_shape",
                        "layer": "decorative",
                        "geometry": self._normalized_box(0.08, 0.73, 0.78, 0.01),
                        "style": {"shape": "line", "fill_role": "primary", "opacity": 0.15},
                    },
                ],
            }

        if aspect_ratio >= 1.45:
            if layout_type == "checklist_card":
                candidate_archetypes = ["wide_checklist_split", "wide_editorial_split", "wide_insight_split"]
                archetype = "wide_checklist_split"
            elif layout_type == "comparison_board":
                candidate_archetypes = ["wide_comparison_split", "wide_editorial_split", "wide_insight_split"]
                archetype = "wide_comparison_split"
            elif layout_type == "insight_split":
                candidate_archetypes = ["wide_insight_split", "wide_editorial_split", "wide_checklist_split"]
                archetype = "wide_insight_split"
            else:
                candidate_archetypes = ["wide_editorial_split", "wide_insight_split", "wide_checklist_split"]
                archetype = "wide_editorial_split"
            archetype = self._choose_layout_archetype(
                request=request,
                candidates=candidate_archetypes,
                default=archetype,
            )
            return {
                "layout_type": layout_type,
                "layout_archetype": archetype,
                "hero_image": self._normalized_box(0.56, 0.12, 0.36, 0.64),
                "overlay_panel": self._normalized_box(0.05, 0.08, 0.46, 0.8),
                "logo": self._normalized_box(0.08, 0.11, 0.2, 0.08),
                "headline": self._normalized_box(0.08, 0.21, 0.4, 0.16),
                "supporting_line": self._normalized_box(0.08, 0.4, 0.37, 0.12),
                "proof_points": self._normalized_box(0.08, 0.54, 0.34, 0.18),
                "cta": self._normalized_box(0.08, 0.79, 0.3, 0.09),
                "decorative_shapes": [
                    {
                        "element_id": "hero_accent_arc",
                        "element_type": "decorative_shape",
                        "role": "decorative_shape",
                        "layer": "decorative",
                        "geometry": self._normalized_box(0.78, 0.04, 0.13, 0.07),
                        "style": {"shape": "ellipse", "fill_role": "accent"},
                    }
                ],
            }

        if layout_type == "checklist_card":
            candidate_archetypes = ["checklist_corner_card", "editorial_corner_split", "insight_corner_split"]
            archetype = "checklist_corner_card"
        elif layout_type == "comparison_board":
            candidate_archetypes = ["comparison_corner_card", "editorial_corner_split", "insight_corner_split"]
            archetype = "comparison_corner_card"
        elif layout_type == "insight_split":
            candidate_archetypes = ["insight_corner_split", "editorial_corner_split", "checklist_corner_card"]
            archetype = "insight_corner_split"
        else:
            candidate_archetypes = ["editorial_corner_split", "insight_corner_split", "checklist_corner_card"]
            archetype = "editorial_corner_split"
        archetype = self._choose_layout_archetype(
            request=request,
            candidates=candidate_archetypes,
            default=archetype,
        )

        return {
            "layout_type": layout_type,
            "layout_archetype": archetype,
            "hero_image": self._normalized_box(0.54, 0.14, 0.34, 0.42),
            "overlay_panel": self._normalized_box(0.05, 0.08, 0.46, 0.8),
            "logo": self._normalized_box(0.08, 0.11, 0.2, 0.07),
            "headline": self._normalized_box(0.08, 0.2, 0.38, 0.16),
            "supporting_line": self._normalized_box(0.08, 0.38, 0.37, 0.11),
            "proof_points": self._normalized_box(0.08, 0.52, 0.34, 0.18),
            "cta": self._normalized_box(0.08, 0.78, 0.3, 0.08),
            "decorative_shapes": [
                {
                    "element_id": "hero_glow",
                    "element_type": "decorative_shape",
                    "role": "decorative_shape",
                    "layer": "decorative",
                    "geometry": self._normalized_box(0.79, 0.05, 0.13, 0.09),
                    "style": {"shape": "ellipse", "fill_role": "accent"},
                }
            ],
        }

    def _fallback_scene_graph(
        self,
        *,
        request: AIOrchestrationRequest,
        text_payload: dict[str, Any],
        creative_decision: dict[str, Any],
        compiled_context: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes fallback scene graph from request state, copy payload, and creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        size = request.studio_panel.get("size") or BlueprintService.PRESET_DIMENSIONS.get(
            request.studio_panel.get("platform_preset", "instagram"),
            BlueprintService.PRESET_DIMENSIONS["instagram"],
        )
        layout_type = self._infer_layout_type(request.prompt, request.studio_panel, text_payload)
        palette_roles = dict((compiled_context.get("brand_visual_brief", {}) or {}).get("palette_roles", {}) or {})
        proof_points = list((text_payload.get("metadata") or {}).get("proof_points") or [])
        content_elements = [
            {
                "element_id": "background",
                "element_type": "background",
                "role": "background",
                "layer": "background",
                "geometry": {"x": 0, "y": 0, "width": 1, "height": 1, "units": "normalized"},
                "style": {
                    "fill_role": "background",
                    "primary_fill": palette_roles.get("background") or palette_roles.get("surface"),
                    "gradient_from": palette_roles.get("background") or palette_roles.get("surface"),
                    "gradient_to": palette_roles.get("primary"),
                },
            },
            {
                "element_id": "headline",
                "element_type": "text",
                "role": "headline",
                "layer": "content",
                "geometry": {"x": 0.08, "y": 0.09, "width": 0.68, "height": 0.16, "units": "normalized"},
                "text": text_payload.get("headline", ""),
                "style": {"font_size": 58, "font_role": "heading", "fill_role": "primary", "max_lines": 3},
            },
            {
                "element_id": "supporting_line",
                "element_type": "text",
                "role": "supporting_line",
                "layer": "content",
                "geometry": {"x": 0.08, "y": 0.24, "width": 0.68, "height": 0.1, "units": "normalized"},
                "text": (text_payload.get("metadata") or {}).get("supporting_line") or text_payload.get("body", ""),
                "style": {"font_size": 24, "font_role": "body", "fill_role": "secondary_text", "max_lines": 3},
            },
            {
                "element_id": "image",
                "element_type": "image",
                "role": "image",
                "layer": "primary_visual",
                "geometry": {"x": 0.56, "y": 0.22, "width": 0.34, "height": 0.46, "units": "normalized"},
                "style": {"fit": "cover", "border_radius": 28},
                "asset": {"asset_role": "ai_image"},
            },
            {
                "element_id": "cta",
                "element_type": "text",
                "role": "cta",
                "layer": "brand",
                "geometry": {"x": 0.08, "y": 0.8, "width": 0.34, "height": 0.09, "units": "normalized"},
                "text": text_payload.get("cta", ""),
                "style": {"font_size": 24, "font_role": "cta", "fill_role": "light_text", "background_fill_role": "primary", "max_lines": 2},
            },
            {
                "element_id": "logo",
                "element_type": "logo",
                "role": "logo",
                "layer": "brand",
                "geometry": {"x": 0.78, "y": 0.07, "width": 0.16, "height": 0.08, "units": "normalized"},
                "style": {"fit": "contain"},
                "asset": {"asset_role": "logo", "trust_level": "trusted"},
            },
            {
                "element_id": "decorative_glow",
                "element_type": "decorative_shape",
                "role": "decorative_shape",
                "layer": "decorative",
                "geometry": {"x": 0.72, "y": 0.12, "width": 0.2, "height": 0.14, "units": "normalized"},
                "style": {"shape": "ellipse", "fill_role": "accent"},
            },
        ]
        if proof_points and layout_type != "ranking_table":
            content_elements.insert(
                3,
                {
                    "element_id": "proof_points",
                    "element_type": "text",
                    "role": "proof_points",
                    "layer": "content",
                    "geometry": {"x": 0.08, "y": 0.36, "width": 0.4, "height": 0.28, "units": "normalized"},
                    "text": proof_points,
                    "style": {"font_size": 20, "font_role": "body", "fill_role": "secondary_text", "max_lines": 5},
                },
            )
        if layout_type == "ranking_table":
            metadata = text_payload.get("metadata") if isinstance(text_payload.get("metadata"), dict) else {}
            surface_contract = metadata.get("data_list_surface_contract") if isinstance(metadata.get("data_list_surface_contract"), dict) else {}
            requested_count = self._int_or_none(surface_contract.get("requested_count")) or self._explicit_top_n_item_count(request.prompt) or len(proof_points)
            module_count = max(1, min(self._int_or_none(surface_contract.get("module_count")) or requested_count or len(proof_points) or 1, 10))
            proof_rows = proof_points[:module_count] or self._sentences(text_payload.get("body", ""))[:module_count]
            if not any(element.get("role") == "proof_points" for element in content_elements):
                content_elements.insert(
                    3,
                    {
                        "element_id": "proof_points",
                        "element_type": "text",
                        "role": "proof_points",
                        "layer": "content",
                        "geometry": {"x": 0.08, "y": 0.30, "width": 0.84, "height": 0.58, "units": "normalized"},
                        "text": proof_rows,
                        "style": {"font_size": 16, "font_role": "body", "fill_role": "secondary_text", "max_lines": max(module_count, 8)},
                    },
                )
            for element in content_elements:
                role = element.get("role")
                if role == "headline":
                    element["geometry"] = {"x": 0.08, "y": 0.07, "width": 0.66, "height": 0.13, "units": "normalized"}
                    element["style"] = {**dict(element.get("style") or {}), "font_size": 48, "max_lines": 2}
                elif role == "supporting_line":
                    element["geometry"] = {"x": 0.08, "y": 0.205, "width": 0.78, "height": 0.06, "units": "normalized"}
                    element["style"] = {**dict(element.get("style") or {}), "font_size": 20, "max_lines": 2}
                elif role == "proof_points":
                    element["geometry"] = {"x": 0.08, "y": 0.30, "width": 0.84, "height": 0.58, "units": "normalized"}
                    element["text"] = proof_rows
                    element["style"] = {**dict(element.get("style") or {}), "font_size": 16, "max_lines": max(module_count, 8)}
                    element["validation_hints"] = {
                        **dict(element.get("validation_hints") or {}),
                        "badge_style": "numbered",
                        "surface_mode": "ranking_table",
                        "requested_count": requested_count,
                        "module_count": module_count,
                    }
                elif role == "cta":
                    element["geometry"] = {"x": 0.08, "y": 0.905, "width": 0.42, "height": 0.05, "units": "normalized"}
                    element["style"] = {**dict(element.get("style") or {}), "font_size": 18, "max_lines": 1}
                elif role == "logo":
                    element["geometry"] = {"x": 0.78, "y": 0.045, "width": 0.16, "height": 0.07, "units": "normalized"}
            content_elements = [
                element
                for element in content_elements
                if element.get("role") not in {"image"} and element.get("element_id") != "decorative_glow"
            ]
            content_elements.insert(
                3,
                {
                    "element_id": "ranking_table_panel",
                    "element_type": "decorative_shape",
                    "role": "decorative_shape",
                    "layer": "decorative",
                    "geometry": {"x": 0.06, "y": 0.28, "width": 0.88, "height": 0.62, "units": "normalized"},
                    "style": {"shape": "rounded_rect", "fill_role": "surface", "border_radius": 28, "opacity": 0.96},
                    "validation_hints": {"surface_mode": "ranking_table", "module_count": module_count},
                },
            )
            row_height = 0.58 / max(module_count, 1)
            for index in range(1, module_count):
                content_elements.append(
                    {
                        "element_id": f"ranking_row_divider_{index}",
                        "element_type": "decorative_shape",
                        "role": "decorative_shape",
                        "layer": "decorative",
                        "geometry": {"x": 0.09, "y": 0.30 + row_height * index, "width": 0.80, "height": 0.002, "units": "normalized"},
                        "style": {"shape": "line", "fill_role": "primary", "opacity": 0.12},
                    }
                )
        elif not proof_points:
            content_elements.insert(
                3,
                {
                    "element_id": "body",
                    "element_type": "text",
                    "role": "body",
                    "layer": "content",
                    "geometry": {"x": 0.08, "y": 0.36, "width": 0.44, "height": 0.28, "units": "normalized"},
                    "text": text_payload.get("body", ""),
                    "style": {"font_size": 20, "font_role": "body", "fill_role": "secondary_text", "max_lines": 5},
                },
            )
        if layout_type == "checklist_card":
            content_elements[2]["geometry"] = {"x": 0.08, "y": 0.24, "width": 0.38, "height": 0.1, "units": "normalized"}
            for element in content_elements:
                if element["role"] == "image":
                    element["geometry"] = {"x": 0.54, "y": 0.14, "width": 0.34, "height": 0.46, "units": "normalized"}
                    element["style"] = {
                        **dict(element.get("style") or {}),
                        "fit": "cover",
                        "border_radius": 34,
                    }
                if element["role"] == "cta":
                    element["geometry"] = {"x": 0.08, "y": 0.83, "width": 0.36, "height": 0.09, "units": "normalized"}
                    element["style"] = {
                        **dict(element.get("style") or {}),
                        "background_fill_role": "primary",
                        "fill_role": "light_text",
                    }
            content_elements = [
                element
                for element in content_elements
                if element["role"] not in {"proof_points", "body"}
            ]
            proof_rows = proof_points[:3] or self._sentences(text_payload.get("body", ""))[:3]
            row_y = 0.42
            for index, item in enumerate(proof_rows, start=1):
                y = row_y + ((index - 1) * 0.11)
                content_elements.extend(
                    [
                        {
                            "element_id": f"proof_card_{index}",
                            "element_type": "decorative_shape",
                            "role": "decorative_shape",
                            "layer": "decorative",
                            "geometry": {"x": 0.08, "y": y, "width": 0.38, "height": 0.085, "units": "normalized"},
                            "style": {"shape": "rounded_rect", "fill_role": "surface", "border_radius": 26},
                        },
                        {
                            "element_id": f"proof_accent_{index}",
                            "element_type": "decorative_shape",
                            "role": "decorative_shape",
                            "layer": "decorative",
                            "geometry": {"x": 0.095, "y": y + 0.018, "width": 0.024, "height": 0.048, "units": "normalized"},
                            "style": {"shape": "rounded_rect", "fill_role": "accent", "border_radius": 14},
                        },
                        {
                            "element_id": f"proof_text_{index}",
                            "element_type": "text",
                            "role": "body",
                            "layer": "content",
                            "geometry": {"x": 0.135, "y": y + 0.01, "width": 0.305, "height": 0.06, "units": "normalized"},
                            "text": item,
                            "style": {
                                "font_size": 22,
                                "font_role": "body_sans",
                                "fill_role": "secondary_text",
                                "max_lines": 2,
                            },
                        },
                    ]
                )
            content_elements.append(
                {
                    "element_id": "hero_glow",
                    "element_type": "decorative_shape",
                    "role": "decorative_shape",
                    "layer": "decorative",
                    "geometry": {"x": 0.67, "y": 0.08, "width": 0.22, "height": 0.14, "units": "normalized"},
                    "style": {"shape": "ellipse", "fill_role": "accent"},
                }
            )
        return {
            "version": "1.0",
            "canvas": {
                "width": int(size.get("width") or 1080),
                "height": int(size.get("height") or 1080),
                "platform": request.studio_panel.get("platform_preset", "instagram"),
                "file_type": request.studio_panel.get("file_type", "png"),
                "safe_margin": 48,
            },
            "layout_mode": creative_decision.get("layout_mode", "synthesized_layout"),
            "confidence": float(creative_decision.get("confidence") or 0.58),
            "layers": list(self.DEFAULT_LAYERS),
            "elements": content_elements,
            "styles": {
                "layout_type": layout_type,
                "layout_archetype": layout_type,
                "palette_roles": palette_roles,
            },
            "assets": [
                {
                    "asset_id": str(asset.get("asset_id") or ""),
                    "asset_role": str(asset.get("asset_role") or ""),
                    "storage_path": str(asset.get("storage_path") or ""),
                    "trust_level": str(asset.get("trust_level") or ""),
                }
                for asset in (request.asset_catalog or request.reference_assets)[:12]
                if isinstance(asset, dict)
            ],
            "template_adaptation": {
                **dict(creative_decision.get("adaptations") or {}),
                "selected_template_id": creative_decision.get("selected_template_id"),
            },
            "validation_hints": {"template_surface_policy": "reinterpret_if_flattened"},
        }

    @classmethod
    def _repair_common_mojibake(cls, value: str) -> str:
        # Repairs common mojibake from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = value or ""
        if not any(token in text for token in ("Ã", "Â", "â€", "â€™", "â€œ", "â€”", "â€“", "â€¢")):
            return text
        try:
            repaired = text.encode("latin-1").decode("utf-8")
        except (UnicodeEncodeError, UnicodeDecodeError):
            return text
        return repaired or text

    @classmethod
    def _sanitize_text_for_canvas(cls, value: str) -> str:
        # Sanitizes text canvas from input value for AIOrchestratorService.
        # It delegates shared cleanup to _repair_common_mojibake before returning the cleaned value.
        text = cls._repair_common_mojibake(value or "")
        text = cls.MOJIBAKE_SYMBOL_PATTERN.sub(" ", text)
        text = cls.DISALLOWED_GLYPH_PATTERN.sub(" ", text)
        return " ".join(text.split()).strip()

    @classmethod
    def _normalized_prompt_tokens(cls, value: str) -> list[str]:
        # Normalizes prompt tokens from input value for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        cleaned = re.sub(r"[^a-z0-9\s]+", " ", (value or "").lower())
        return [
            token
            for token in cleaned.split()
            if token and token not in cls.PROMPT_ECHO_STOPWORDS
        ]

    @classmethod
    def _looks_like_prompt_echo(cls, value: Any, prompt: str) -> bool:
        # Checks prompt echo from input value and prompt text for AIOrchestratorService.
        # It reuses _coerce_text_value and _normalized_prompt_tokens so related checks follow the same rule.
        candidate = cls._coerce_text_value(value)
        if not candidate:
            return False
        prompt_text = cls._coerce_text_value(prompt)
        if not prompt_text:
            return False
        lowered_candidate = candidate.lower().strip()
        if lowered_candidate.startswith(("create ", "make ", "design ", "generate ", "write ", "craft ")):
            return True
        candidate_tokens = cls._normalized_prompt_tokens(candidate)
        prompt_tokens = cls._normalized_prompt_tokens(prompt_text)
        if not candidate_tokens or not prompt_tokens:
            return False
        overlap_tokens = set(candidate_tokens) & set(prompt_tokens)
        overlap = len(overlap_tokens)
        prompt_instruction_markers = (
            "instagram post",
            "social media post",
            "create a",
            "make a",
            "design a",
            "generate a",
            "write a",
            "craft a",
            "cta for this post",
        )
        generic_fallback_starts = (
            "practical guidance for ",
            "smart strategies for ",
        )
        if lowered_candidate.startswith(generic_fallback_starts):
            return True
        audience_facing_starts = (
            "tips for ",
            "how to ",
            "ways to ",
            "why ",
            "want to ",
        )
        if lowered_candidate.startswith(audience_facing_starts) and not any(
            marker in lowered_candidate for marker in prompt_instruction_markers
        ):
            return False
        if not any(marker in lowered_candidate for marker in prompt_instruction_markers) and overlap <= 2:
            return False
        candidate_phrase = " ".join(candidate_tokens)
        prompt_phrase = " ".join(prompt_tokens)
        if candidate_phrase and (candidate_phrase in prompt_phrase or prompt_phrase in candidate_phrase):
            return True
        baseline = max(1, min(len(candidate_tokens), len(prompt_tokens)))
        return overlap >= 3 and (overlap / baseline) >= 0.8

    @classmethod
    def _has_mistake_carousel_signals(cls, *values: Any) -> bool:
        # Checks mistake carousel signals for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        combined = " ".join(
            cls._normalize_metadata_text(value, limit=240)
            for value in values
            if cls._normalize_metadata_text(value, limit=240)
        )
        return bool(cls.MISTAKE_CAROUSEL_SIGNAL_PATTERN.search(combined))

    @classmethod
    def _prefers_multiple_mistake_slides(cls, *values: Any) -> bool:
        # Centralizes prefers multiple mistake slides for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        combined = " ".join(
            cls._normalize_metadata_text(value, limit=240)
            for value in values
            if cls._normalize_metadata_text(value, limit=240)
        )
        return bool(cls.MULTI_MISTAKE_SIGNAL_PATTERN.search(combined))

    @classmethod
    def _is_generic_carousel_education_label(cls, value: Any) -> bool:
        # Checks generic carousel education label from input value for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        text = cls._normalize_metadata_text(value, limit=120).casefold()
        if not text:
            return False
        return (
            text in {
                "education",
                "investment education",
                "investor education",
                "financial education",
                "financial literacy",
                "tips",
                "key insight",
                "key point",
                "insight",
                "why this matters now",
                "what actually changed",
                "why it matters beyond the headline",
                "what to do with this insight",
                "the takeaway",
                "final takeaway",
            }
            or text.endswith(" education")
            or text.startswith("key insight")
            or text.startswith("key point")
        )

    @classmethod
    def _content_topic_tokens(cls, request: AIOrchestrationRequest | None) -> set[str]:
        # Centralizes content topic tokens from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        tokens = cls._request_topic_tokens(request)
        return {
            token
            for token in tokens
            if len(token) >= 4 and token not in cls.TOPIC_MATCH_NOISE_TOKENS
        }

    @classmethod
    def _headline_lacks_topic_specificity(
        cls,
        headline: Any,
        *,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Centralizes headline lacks topic specificity from headline and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._normalize_metadata_text(headline, limit=140)
        if not text:
            return True
        lowered = text.casefold()
        generic_patterns = (
            "why this matters",
            "what changed",
            "what actually changed",
            "why it matters",
            "beyond the headline",
            "what to do with this insight",
            "key insight",
            "key point",
            "the takeaway",
        )
        if not any(pattern in lowered for pattern in generic_patterns):
            return False
        topic_tokens = cls._content_topic_tokens(request)
        if not topic_tokens:
            return True
        headline_tokens = set(cls._normalized_prompt_tokens(text))
        return not bool(headline_tokens & topic_tokens)

    @staticmethod
    def _canonicalize_number_words_in_exact_claim_marker(value: str) -> str:
        # Counts canonicalize number words in exact claim marker from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        units = {
            "zero": 0,
            "one": 1,
            "two": 2,
            "three": 3,
            "four": 4,
            "five": 5,
            "six": 6,
            "seven": 7,
            "eight": 8,
            "nine": 9,
            "ten": 10,
            "eleven": 11,
            "twelve": 12,
            "thirteen": 13,
            "fourteen": 14,
            "fifteen": 15,
            "sixteen": 16,
            "seventeen": 17,
            "eighteen": 18,
            "nineteen": 19,
        }
        tens = {
            "twenty": 20,
            "thirty": 30,
            "forty": 40,
            "fifty": 50,
            "sixty": 60,
            "seventy": 70,
            "eighty": 80,
            "ninety": 90,
        }
        number_words = "|".join([*units.keys(), *tens.keys()])
        unit_words = "|".join(units.keys())

        def replace(match: re.Match[str]) -> str:
            # Centralizes replace from match for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            first = match.group("first")
            second = match.group("second")
            suffix = match.group("suffix")
            number = tens.get(first, units.get(first, 0))
            if second:
                number += units.get(second, 0)
            return f"{number} {suffix}"

        return re.sub(
            rf"\b(?P<first>{number_words})(?:[\s-]+(?P<second>{unit_words}))?\s+(?P<suffix>billion|million|crore|lakh|trillion|tariff lines|years|skilled professionals|occupations)\b",
            replace,
            value,
            flags=re.IGNORECASE,
        )

    @staticmethod
    def _canonical_exact_claim_marker(value: Any) -> str:
        # Centralizes canonical exact claim marker from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        marker = " ".join(str(value or "").casefold().replace("us$", "usd ").split())
        marker = marker.replace("nz$", "nzd ")
        marker = marker.replace("$", "usd ")
        marker = marker.replace("\u20b9", "inr ")
        marker = marker.replace("â‚¹", "inr ")
        marker = re.sub(r"\brs\.?\s*", "inr ", marker)
        marker = re.sub(
            r"\b(usd|nzd|inr)\s+(\d[\d,]*(?:\.\d+)?)\b",
            lambda match: f"{match.group(1)} {match.group(2).replace(',', '')}",
            marker,
        )
        marker = re.sub(r"\b(usd|nzd|inr)\s*(\d+(?:\.\d+)?)\s*b\b", r"\1 \2 billion", marker)
        marker = re.sub(r"\b(usd|nzd|inr)\s*(\d+(?:\.\d+)?)\s*m\b", r"\1 \2 million", marker)
        marker = re.sub(r"\b(\d+(?:\.\d+)?)\s*b\b", r"\1 billion", marker)
        marker = re.sub(r"\b(\d+(?:\.\d+)?)\s*m\b", r"\1 million", marker)
        marker = AIOrchestratorService._canonicalize_number_words_in_exact_claim_marker(marker)
        return re.sub(r"\s+", " ", marker).strip()

    @staticmethod
    def _exact_claim_marker_aliases(marker: str) -> set[str]:
        # Centralizes exact claim marker aliases from marker for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized = " ".join(str(marker or "").split())
        if not normalized:
            return set()
        aliases = {normalized}
        currencyless = re.sub(r"^(?:usd|nzd|inr)\s+", "", normalized).strip()
        if currencyless:
            aliases.add(currencyless)
        return aliases

    @classmethod
    def _exact_claim_markers(cls, value: Any) -> set[str]:
        # Centralizes exact claim markers from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        text = AIOrchestratorService._raw_text_for_exact_claims(value)
        if not text:
            return set()
        markers: set[str] = set()
        patterns = (
            r"\b\d{1,2}\s+(?:jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:tember)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\s+\d{4}\b",
            r"\b(?:19|20)\d{2}\b",
            r"\b\d+(?:\.\d+)?\s?%",
            r"\b(?:top|rank(?:ed)?|#)\s?\d+\b",
            r"(?<!\w)us\$\s?\d[\d,]*(?:\.\d+)?(?:\s?(?:billion|million|crore|lakh|trillion))?\b",
            r"(?<!\w)(?:usd|nzd|nz\$|inr|rs\.?|\u20b9|â‚¹|\$)\s?\d[\d,]*(?:\.\d+)?\s?[bm]\b",
            r"\b\d+(?:\.\d+)?\s?[bm]\b",
            r"(?<!\w)(?:usd|inr|rs\.?|\u20b9|â‚¹|\$)\s?\d[\d,]*(?:\.\d+)?(?:\s?(?:billion|million|crore|lakh|trillion))?\b",
            r"\b\d+(?:\.\d+)?\s?(?:billion|million|crore|lakh|trillion|tariff lines|years|skilled professionals|occupations)\b",
            r"\b(?:one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve|thirteen|fourteen|fifteen|sixteen|seventeen|eighteen|nineteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety)(?:[\s-]+(?:one|two|three|four|five|six|seven|eight|nine))?\s?(?:billion|million|crore|lakh|trillion|tariff lines|years|skilled professionals|occupations)\b",
        )
        for pattern in patterns:
            for match in re.findall(pattern, text, flags=re.IGNORECASE):
                marker = cls._canonical_exact_claim_marker(match)
                if marker:
                    markers.add(marker)
        return markers

    @staticmethod
    def _raw_text_for_exact_claims(value: Any) -> str:
        # Extracts text exact claims from input value for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        if value is None:
            return ""
        if isinstance(value, str):
            return value
        if isinstance(value, dict):
            return json.dumps(value, default=str, ensure_ascii=False)
        if isinstance(value, (list, tuple, set)):
            return " ".join(AIOrchestratorService._raw_text_for_exact_claims(item) for item in value)
        return str(value)

    @classmethod
    def _supported_exact_claim_markers(
        cls,
        *,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None,
    ) -> set[str]:
        # Centralizes supported exact claim markers from request state and compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        source_texts: list[Any] = [request.prompt]
        for source in (
            request.research_editorial_brief,
            request.live_research,
            (compiled_context or {}).get("research_editorial_brief"),
            (compiled_context or {}).get("live_research"),
        ):
            if not isinstance(source, dict):
                continue
            fact_model = source.get("fact_model") if isinstance(source.get("fact_model"), dict) else source
            for fact in fact_model.get("verified_facts") or []:
                if isinstance(fact, dict):
                    source_texts.extend([fact.get("label"), fact.get("value"), fact.get("detail")])
                else:
                    source_texts.append(fact)
        supported: set[str] = set()
        for text in source_texts:
            for marker in cls._exact_claim_markers(text):
                supported.update(cls._exact_claim_marker_aliases(marker))
        return supported

    @classmethod
    def _unsupported_exact_claim_markers(
        cls,
        text: Any,
        *,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None,
    ) -> set[str]:
        # Centralizes unsupported exact claim markers from text, request state, and compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        markers = cls._exact_claim_markers(text)
        if not markers:
            return set()
        supported = cls._supported_exact_claim_markers(
            request=request,
            compiled_context=compiled_context,
        )
        return {marker for marker in markers if not (cls._exact_claim_marker_aliases(marker) & supported)}

    @classmethod
    def _has_research_fact_context(
        cls,
        *,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None,
    ) -> bool:
        # Checks research fact context from request state and compiled context for AIOrchestratorService.
        # The boolean result controls the nearby policy or validation branch.
        for source in (
            request.research_editorial_brief,
            request.live_research,
            (compiled_context or {}).get("research_editorial_brief"),
            (compiled_context or {}).get("live_research"),
        ):
            if not isinstance(source, dict):
                continue
            fact_model = source.get("fact_model") if isinstance(source.get("fact_model"), dict) else source
            if fact_model.get("verified_facts"):
                return True
            if fact_model.get("research_summary") or fact_model.get("source_summaries"):
                return True
            if fact_model.get("evidence") or fact_model.get("sources"):
                return True
        return False

    @classmethod
    def _visual_focus_is_weak_for_carousel(cls, value: Any) -> bool:
        # Centralizes visual focus weak carousel from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._normalize_metadata_text(value, limit=420).casefold()
        if not text:
            return True
        if cls._visual_focus_is_raw_reference_payload(value):
            return True
        if "follow selected sample page" in text and "visual authority" in text:
            return False
        generic_markers = (
            "generic",
            "professional photo",
            "business person",
            "handshake",
            "flag-only",
            "signing ceremony",
            "conceptual visual",
            "clean graphic",
            "finance icon",
            "icons representing",
            "visual highlighting",
        )
        if any(marker in text for marker in generic_markers):
            return True
        specific_visual_markers = (
            "3d",
            "2.5d",
            "isometric",
            "dashboard",
            "chart",
            "graph",
            "module",
            "card",
            "document",
            "evidence",
            "data",
            "map",
            "network",
            "product",
            "screen",
            "yield",
            "bond",
            "tariff",
            "trade",
            "investment",
            "flat editorial",
            "illustration",
            "object",
            "system",
        )
        return not any(marker in text for marker in specific_visual_markers)

    @classmethod
    def _visual_focus_is_raw_reference_payload(cls, value: Any) -> bool:
        # Builds visual focus reference from input value for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        if isinstance(value, dict):
            raw_type = cls._normalize_metadata_text(value.get("type"), limit=80).casefold()
            if raw_type in {"reference_image", "uploaded_reference", "asset", "image_asset"}:
                return True
            if any(cls._normalize_metadata_text(value.get(key), limit=260) for key in ("storage_path", "asset_id", "path", "url")):
                return True
        text = cls._normalize_metadata_text(value, limit=520).casefold()
        if not text:
            return False
        reference_markers = (
            "storage_path",
            "reference_image",
            "reference_creatives/",
            "derived-assets/",
            "uploaded reference",
            ".pdf",
            ".png",
            ".jpg",
            ".jpeg",
        )
        return any(marker in text for marker in reference_markers)

    @classmethod
    def _content_line_is_evidence_anchor(
        cls,
        value: Any,
        *,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Extracts content line evidence anchor from input value and request state for AIOrchestratorService.
        # It calls _normalize_metadata_text and _content_topic_tokens to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=320)
        if len(text.split()) < 6:
            return False
        topic_tokens = cls._content_topic_tokens(request)
        if not topic_tokens:
            return True
        text_tokens = set(cls._normalized_prompt_tokens(text))
        if text_tokens & topic_tokens:
            return True
        domain_evidence_tokens = {
            "agreement",
            "tariff",
            "tariffs",
            "market",
            "access",
            "services",
            "mobility",
            "investment",
            "investor",
            "investors",
            "wealth",
            "sector",
            "sectors",
            "trade",
            "deal",
            "context",
        }
        return bool(text_tokens & domain_evidence_tokens)

    @classmethod
    def _is_promotional_line(cls, value: Any) -> bool:
        # Checks promotional line from input value for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        text = cls._normalize_metadata_text(value, limit=220)
        if not text:
            return False
        return bool(cls.PROMOTIONAL_LINE_PATTERN.search(text))

    @classmethod
    def _is_mistake_candidate_line(cls, value: Any) -> bool:
        # Checks mistake candidate line from input value for AIOrchestratorService.
        # It reuses _normalize_metadata_text and _is_promotional_line so related checks follow the same rule.
        text = cls._normalize_metadata_text(value, limit=220)
        if not text:
            return False
        if cls.MISTAKE_GROUP_START_PATTERN.match(text):
            return True
        if cls.IMPACT_LINE_PATTERN.match(text) or cls.FIX_LINE_PATTERN.match(text):
            return False
        return bool(cls.NEGATIVE_MISTAKE_CANDIDATE_PATTERN.search(text)) and not cls._is_promotional_line(text)

    @classmethod
    def _mistake_cover_headline(cls, *values: Any) -> str:
        # Centralizes mistake cover headline for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        combined = " ".join(
            cls._normalize_metadata_text(value, limit=240)
            for value in values
            if cls._normalize_metadata_text(value, limit=240)
        )
        focus = cls._prompt_topic_summary(combined)
        focus = re.sub(r"\b(?:top|common|most|costly)\b", " ", focus, flags=re.IGNORECASE)
        focus = " ".join(focus.split()).strip(" -:.;")
        if focus and "mistake" not in focus.casefold():
            return cls._normalize_metadata_text(f"Common {focus.title()} Mistakes To Avoid", limit=90)
        if focus:
            return cls._normalize_metadata_text(focus.title(), limit=90)
        return "Common Mistakes To Avoid"

    @classmethod
    def _strip_mistake_markers(cls, value: Any) -> str:
        # Strips strip mistake markers from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        cleaned = cls._normalize_metadata_text(value, limit=160)
        if not cleaned:
            return ""
        cleaned = cls.MISTAKE_GROUP_START_PATTERN.sub("", cleaned).strip(" -:.;")
        cleaned = cls.MISTAKE_WHY_PATTERN.sub("", cleaned).strip(" -:.;")
        cleaned = cls.IMPACT_LINE_PATTERN.sub("", cleaned).strip(" -:.;")
        cleaned = cls.FIX_LINE_PATTERN.sub("", cleaned).strip(" -:.;")
        return cleaned

    @classmethod
    def _infer_mistake_headline(cls, value: Any, *, fallback_focus: str = "", index: int = 1) -> str:
        # Centralizes infer mistake headline from input value, fallback focus, and index for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        cleaned = cls._strip_mistake_markers(value)
        lowered = cleaned.casefold()
        if "diversif" in lowered:
            focus_text = f"{cleaned} {fallback_focus}".casefold()
            if "bond" in focus_text:
                return "Mistake: Not Diversifying Your Bond Portfolio"
            return "Mistake: Not Diversifying Your Portfolio"
        if "yield" in lowered or "return" in lowered:
            return "Mistake: Chasing High Returns"
        if "credit quality" in lowered or "rating" in lowered or "risk" in lowered:
            return "Mistake: Ignoring Quality"
        if "duration" in lowered or "timing" in lowered:
            return "Mistake: Ignoring Timing"
        if cleaned:
            if cls._is_generic_carousel_education_label(cleaned):
                cleaned = ""
            elif str(value).strip().casefold().startswith("mistake:"):
                return cls._normalize_metadata_text(str(value).strip(), limit=110)
            else:
                return f"Mistake: {cleaned}"
        focus = cls._normalize_metadata_text(fallback_focus, limit=90).strip(" -:.;")
        if focus:
            return f"Mistake: {focus}"
        return f"Mistake: Key issue {index}"

    @classmethod
    def _mistake_supporting_line(cls, *values: Any) -> str:
        # Extracts mistake supporting line for AIOrchestratorService.
        # It calls _normalize_metadata_text and _is_promotional_line to turn raw evidence into the structured signal the caller needs.
        for candidate in values:
            cleaned = cls._normalize_metadata_text(candidate, limit=220)
            if not cleaned:
                continue
            if (
                cls.MISTAKE_GROUP_START_PATTERN.match(cleaned)
                or cls.IMPACT_LINE_PATTERN.match(cleaned)
                or cls.FIX_LINE_PATTERN.match(cleaned)
                or cls._is_promotional_line(cleaned)
            ):
                continue
            cleaned = cls.MISTAKE_WHY_PATTERN.sub("", cleaned).strip(" -:.;")
            if cleaned:
                return cleaned
        return ""

    @classmethod
    def _mistake_detail_groups(
        cls,
        *,
        headline: str,
        supporting_line: str,
        proof_points: list[str],
        stat_highlights: list[str],
        body_sentences: list[str],
    ) -> list[dict[str, Any]]:
        # Centralizes mistake detail groups from headline, supporting line, and proof points for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        groups: list[dict[str, Any]] = []
        current: dict[str, Any] | None = None
        labeled_stream = [*proof_points, *body_sentences]
        for item in labeled_stream:
            cleaned = cls._normalize_metadata_text(item, limit=220)
            if not cleaned:
                continue
            if cls.MISTAKE_GROUP_START_PATTERN.match(cleaned):
                if current and (current.get("headline") or current.get("supporting_line") or current.get("proof_points")):
                    groups.append(current)
                current = {
                    "headline": cls._infer_mistake_headline(cleaned, fallback_focus=headline, index=len(groups) + 1),
                    "supporting_line": "",
                    "proof_points": [],
                }
                continue
            if current is None:
                continue
            if not current.get("supporting_line") and not cls.IMPACT_LINE_PATTERN.match(cleaned) and not cls.FIX_LINE_PATTERN.match(cleaned):
                current["supporting_line"] = cls._mistake_supporting_line(cleaned)
            else:
                normalized_line = cls._normalize_metadata_text(cleaned, limit=180)
                if normalized_line and not cls._is_promotional_line(normalized_line):
                    current.setdefault("proof_points", []).append(normalized_line)
        if current and (current.get("headline") or current.get("supporting_line") or current.get("proof_points")):
            groups.append(current)
        if groups:
            normalized_groups: list[dict[str, Any]] = []
            seen_group_headlines: set[str] = set()
            for index, group in enumerate(groups, start=1):
                normalized_group = {
                    "headline": cls._infer_mistake_headline(group.get("headline"), fallback_focus=headline, index=index),
                    "supporting_line": cls._mistake_supporting_line(group.get("supporting_line"), supporting_line),
                    "proof_points": cls._normalize_metadata_list(group.get("proof_points"), limit=3),
                }
                group_key = cls._normalize_metadata_text(normalized_group.get("headline"), limit=120).casefold()
                if group_key and group_key in seen_group_headlines:
                    continue
                if group_key:
                    seen_group_headlines.add(group_key)
                normalized_groups.append(
                    {
                        **normalized_group,
                    }
                )
            return normalized_groups

        evidence_pool = [
            value
            for value in [*stat_highlights, *proof_points, *body_sentences]
            if cls._is_mistake_candidate_line(value)
        ]
        fallback_focus = next(
            (
                cls._strip_mistake_markers(value)
                for value in evidence_pool
                if cls._strip_mistake_markers(value) and not cls._is_generic_carousel_education_label(value)
            ),
            cls._strip_mistake_markers(headline) or cls._strip_mistake_markers(supporting_line),
        )
        wants_multiple = cls._prefers_multiple_mistake_slides(headline, supporting_line, *proof_points, *stat_highlights, *body_sentences)
        target_groups = max(1, len(evidence_pool), 3 if wants_multiple else 2)
        heuristics: list[dict[str, Any]] = []
        for index in range(target_groups):
            title_source = evidence_pool[index] if index < len(evidence_pool) else fallback_focus
            body_slice = body_sentences[index * 2 : index * 2 + 2]
            why_source = cls._mistake_supporting_line(*(body_slice or [supporting_line, title_source]))
            detail_points: list[str] = []
            for point in proof_points[index * 2 : index * 2 + 4]:
                normalized = cls._normalize_metadata_text(point, limit=180)
                if not normalized:
                    continue
                if normalized == cls._normalize_metadata_text(title_source, limit=180):
                    continue
                if cls._is_promotional_line(normalized):
                    continue
                detail_points.append(normalized)
            if not detail_points:
                for sentence in body_sentences[index * 2 + 1 : index * 2 + 4]:
                    normalized = cls._normalize_metadata_text(sentence, limit=180)
                    if normalized and normalized != why_source and not cls._is_promotional_line(normalized):
                        detail_points.append(normalized)
            heuristics.append(
                {
                    "headline": cls._infer_mistake_headline(title_source, fallback_focus=fallback_focus, index=index + 1),
                    "supporting_line": why_source,
                    "proof_points": detail_points[:3],
                }
            )
        return heuristics

    @classmethod
    def _repair_prompt_echo_text_payload(
        cls,
        payload: StructuredTextPayload,
        *,
        prompt: str,
    ) -> StructuredTextPayload:
        # Repairs prompt echo text from payload and prompt text for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_list and _prompt_topic_summary before returning the cleaned value.
        raw = payload.model_dump(mode="json")
        metadata = dict(raw.get("metadata") or {})
        proof_points = cls._normalize_metadata_list(metadata.get("proof_points"), limit=4)
        stat_highlights = cls._normalize_metadata_list(metadata.get("stat_highlights"), limit=3)
        prompt_topic = cls._prompt_topic_summary(prompt)
        mistake_style = cls._has_mistake_carousel_signals(
            prompt,
            raw.get("headline"),
            raw.get("body"),
            metadata.get("supporting_line"),
            *proof_points,
            *stat_highlights,
        )
        if cls._looks_like_prompt_echo(raw.get("body"), prompt):
            if proof_points:
                raw["body"] = ". ".join(point.rstrip(" .") for point in proof_points[:3]).strip() + "."
            elif stat_highlights:
                raw["body"] = ". ".join(item.rstrip(" .") for item in stat_highlights[:3]).strip() + "."
            elif prompt_topic:
                raw["body"] = cls._body_from_prompt_topic(prompt_topic)
        if cls._looks_like_prompt_echo(metadata.get("supporting_line"), prompt):
            if proof_points:
                metadata["supporting_line"] = proof_points[0]
            elif prompt_topic:
                metadata["supporting_line"] = cls._supporting_line_from_prompt_topic(prompt_topic)
            elif raw.get("body"):
                metadata["supporting_line"] = cls._sentences(raw.get("body"))[:1][0] if cls._sentences(raw.get("body")) else raw.get("body", "")
        if cls._looks_like_prompt_echo(raw.get("cta"), prompt):
            brand_name = cls._normalize_metadata_text(metadata.get("brand"), limit=48) or "the brand"
            metadata.setdefault("cta_repaired", True)
            raw["cta"] = f"Explore more with {brand_name}"[:72]
        if cls._looks_like_prompt_echo(raw.get("headline"), prompt):
            fallback_headline = (
                ""
                if mistake_style and cls._is_generic_carousel_education_label(metadata.get("section_label"))
                else cls._normalize_metadata_text(metadata.get("section_label"), limit=72)
            )
            if fallback_headline:
                raw["headline"] = fallback_headline
            elif prompt_topic:
                raw["headline"] = (
                    cls._infer_mistake_headline(prompt_topic, fallback_focus=prompt_topic)
                    if mistake_style
                    else cls._headline_from_prompt_topic(prompt_topic)
                )
        metadata["proof_points"] = proof_points or metadata.get("proof_points", [])
        raw["metadata"] = metadata
        return StructuredTextPayload.model_validate(raw)

    @staticmethod
    def _text_payload_prompt_dict(payload: Any) -> dict[str, Any]:
        # Builds text prompt from payload for AIOrchestratorService.
        # The returned payload is the compact contract consumed by the next branch.
        if hasattr(payload, "model_dump"):
            try:
                dumped = payload.model_dump(mode="json")
                if isinstance(dumped, dict):
                    return dumped
            except TypeError:
                pass
        metadata = getattr(payload, "metadata", {})
        return {
            "headline": getattr(payload, "headline", ""),
            "body": getattr(payload, "body", ""),
            "cta": getattr(payload, "cta", ""),
            "metadata": metadata if isinstance(metadata, dict) else {},
        }

    @classmethod
    def _prompt_topic_summary(cls, prompt: str) -> str:
        # Builds prompt topic summary from prompt text for AIOrchestratorService.
        # It calls _coerce_text_value while assembling the payload or prompt text.
        text = cls._coerce_text_value(prompt)
        if not text:
            return ""
        text = re.sub(
            r"^(?:please\s+)?(?:create|make|design|generate|write|craft)\s+(?:(?:an?|the)\s+)?",
            "",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"\b(?:an?|the)?\s*(?:engaging|premium|professional|clean|high[- ]quality)\s+",
            "",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"\b(?:instagram|linkedin|facebook|social media|social|post|image|graphic|creative|story|poster|static|carousel|thumbnail)\b",
            " ",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(r"\bthat\s+(?:shares?|highlights?|explains?)\b", " ", text, flags=re.IGNORECASE)
        text = re.sub(r"\babout\b", " ", text, flags=re.IGNORECASE)
        text = " ".join(text.replace("\n", " ").split())
        return text.strip(" ,.;:-")

    @classmethod
    def _headline_from_prompt_topic(cls, topic: str) -> str:
        # Builds headline prompt topic from topic for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        cleaned = cls._normalize_metadata_text(topic, limit=72)
        if not cleaned:
            return "Brand Update"
        headline = cleaned.title()
        headline = re.sub(r"\bAnd\b", "and", headline)
        headline = re.sub(r"\bFor\b", "for", headline)
        headline = re.sub(r"\bTo\b", "to", headline)
        return headline[:72].rstrip(" ,.;:")

    @classmethod
    def _body_from_prompt_topic(cls, topic: str) -> str:
        # Builds body prompt topic from topic for AIOrchestratorService.
        # It calls _normalize_metadata_text and _prompt_topic_focus while assembling the payload or prompt text.
        cleaned = cls._normalize_metadata_text(topic, limit=160)
        if not cleaned:
            return ""
        topic_kind, focus = cls._prompt_topic_focus(cleaned)
        lens = cls._topic_specific_detail_lens(focus or cleaned)
        if topic_kind == "action" and focus:
            return f"Want to {focus}? Start with {lens}."
        if topic_kind == "why" and focus:
            focus_text = focus[0].upper() + focus[1:]
            return f"{focus_text} matters more when people get {lens}."
        headline = cls._headline_from_prompt_topic(cleaned)
        return f"{headline} starts with {lens}."

    @classmethod
    def _supporting_line_from_prompt_topic(cls, topic: str) -> str:
        # Builds supporting line prompt topic from topic for AIOrchestratorService.
        # It calls _normalize_metadata_text and _prompt_topic_focus while assembling the payload or prompt text.
        cleaned = cls._normalize_metadata_text(topic, limit=120)
        if not cleaned:
            return ""
        topic_kind, focus = cls._prompt_topic_focus(cleaned)
        lens = cls._topic_specific_detail_lens(focus or cleaned)
        if topic_kind == "why" and focus:
            return f"{focus[0].upper() + focus[1:]} needs {lens}"
        if topic_kind == "action" and focus:
            return f"Start with {lens}"
        return f"Start with {lens}"

    @classmethod
    def _fallback_topic_focus(
        cls,
        prompt: str,
        *,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Centralizes fallback topic focus from prompt text and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        prompt_topic = cls._normalize_metadata_text(cls._prompt_topic_summary(prompt), limit=140)
        if prompt_topic:
            return prompt_topic
        compiled_context = compiled_context if isinstance(compiled_context, dict) else {}
        objective_brief = cls._coerce_mapping(compiled_context.get("objective_brief"))
        brand_copy_brief = cls._coerce_mapping(compiled_context.get("brand_copy_brief"))
        knowledge_brief = compiled_context.get("knowledge_brief", []) or []
        knowledge_line = ""
        if knowledge_brief and isinstance(knowledge_brief[0], dict):
            knowledge_line = cls._normalize_metadata_text(knowledge_brief[0].get("content"), limit=140)
        return cls._normalize_metadata_text(
            objective_brief.get("name")
            or objective_brief.get("primary_goal")
            or objective_brief.get("description")
            or brand_copy_brief.get("objective_focus")
            or brand_copy_brief.get("brand_foundations")
            or brand_copy_brief.get("brand_description")
            or knowledge_line,
            limit=140,
        )

    @classmethod
    def _prompt_topic_focus(cls, topic: str) -> tuple[str, str]:
        # Builds prompt topic focus from topic for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        cleaned = cls._normalize_metadata_text(topic, limit=160)
        lowered = cleaned.casefold()
        patterns: list[tuple[str, str]] = [
            (r"^(?:tips?(?:\s+and\s+strateg(?:y|ies))?|strateg(?:y|ies)|ways|steps|ideas)\s+to\s+(.+)$", "action"),
            (r"^(?:tips?(?:\s+and\s+strateg(?:y|ies))?|strateg(?:y|ies)|ways|steps|ideas|guide|checklist)\s+for\s+(.+)$", "topic"),
            (r"^how to\s+(.+)$", "action"),
            (r"^guide to\s+(.+)$", "action"),
            (r"^checklist for\s+(.+)$", "topic"),
            (r"^why\s+(.+)$", "why"),
        ]
        for pattern, topic_kind in patterns:
            match = re.match(pattern, lowered, flags=re.IGNORECASE)
            if not match:
                continue
            focus = cls._normalize_metadata_text(match.group(1), limit=140)
            if focus:
                return topic_kind, focus
        return "topic", cleaned

    @classmethod
    def _topic_specific_detail_lens(cls, topic: str) -> str:
        # Centralizes topic specific detail lens from topic for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        lowered = cls._normalize_metadata_text(topic, limit=160).casefold()
        if not lowered:
            return "specific details people can act on right away"
        if any(token in lowered for token in ("price", "pricing", "cost", "afford", "budget", "save", "savings", "cheaper", "lower")):
            return "the choices that change price the most"
        if any(token in lowered for token in ("risk", "trust", "confidence", "safe", "safety", "secure", "clarity", "opaque")):
            return "proof and plain-English clarity people can trust"
        if any(token in lowered for token in ("compare", "comparison", "versus", "vs ", "choose", "choice", "option")):
            return "the trade-offs that make the decision easier"
        if any(token in lowered for token in ("learn", "understand", "explain", "guide", "checklist", "how to", "tips", "strategy", "strategies")):
            return "plain-language takeaways people can use quickly"
        if any(token in lowered for token in ("grow", "improve", "scale", "increase", "launch", "build")):
            return "the moves that make progress feel real"
        return "specific details people can act on right away"

    @classmethod
    def _contextual_headline_fallback(
        cls,
        *,
        topic_focus: str,
        motivation: str,
        pain_point: str,
        research_highlight: str,
        brand_foundations: Any,
        knowledge_line: str,
        primary_goal: Any,
        semantic_support_phrase: str,
    ) -> str:
        # Centralizes contextual headline fallback from topic focus, motivation, and pain point for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized_topic = cls._normalize_metadata_text(topic_focus, limit=120)
        normalized_motivation = cls._normalize_metadata_text(motivation, limit=96)
        normalized_pain_point = cls._normalize_metadata_text(pain_point, limit=96)
        normalized_research = cls._normalize_metadata_text(research_highlight, limit=120)
        normalized_foundations = cls._normalize_metadata_text(brand_foundations, limit=120)
        normalized_knowledge = cls._normalize_metadata_text(knowledge_line, limit=120)
        normalized_goal = cls._normalize_metadata_text(primary_goal, limit=96)
        if normalized_topic:
            if normalized_motivation and normalized_research:
                return (
                    f"Frame '{normalized_topic}' around '{normalized_motivation}', using this audience truth as the proof: "
                    f"{normalized_research}"
                )
            if normalized_motivation and normalized_foundations:
                return (
                    f"Frame '{normalized_topic}' around '{normalized_motivation}' while reinforcing "
                    f"{normalized_foundations}."
                )
            if normalized_pain_point and normalized_research:
                return (
                    f"Lead '{normalized_topic}' by resolving '{normalized_pain_point}' with this audience truth: "
                    f"{normalized_research}"
                )
            if normalized_motivation:
                return (
                    f"Lead '{normalized_topic}' with the payoff of '{normalized_motivation}', not a generic benefit list."
                )
            if normalized_research:
                return f"Lead '{normalized_topic}' with this audience truth: {normalized_research}"
            if normalized_foundations:
                return f"Lead '{normalized_topic}' in a way that reinforces {normalized_foundations}."
            if normalized_knowledge:
                return f"Ground '{normalized_topic}' in this concrete fact: {normalized_knowledge}"
            if normalized_goal:
                return f"Lead '{normalized_topic}' in a way that moves people toward {normalized_goal}."
            return f"Lead '{normalized_topic}' with {semantic_support_phrase} so the value feels earned."
        if normalized_foundations and normalized_goal:
            return f"Anchor the message in {normalized_foundations} and connect it to {normalized_goal}."
        if normalized_research:
            return f"Anchor the message in this audience truth: {normalized_research}"
        if normalized_foundations:
            return f"Anchor the message in {normalized_foundations} so it stays distinctive."
        if normalized_knowledge:
            return f"Anchor the message in this concrete fact: {normalized_knowledge}"
        return "Anchor the message in a concrete audience payoff and believable proof."

    @classmethod
    def _contextual_supporting_fallback(
        cls,
        *,
        topic_focus: str,
        motivation: str,
        pain_point: str,
        objection: str,
        brand_foundations: Any,
        research_highlight: str,
        knowledge_line: str,
        semantic_support_phrase: str,
    ) -> str:
        # Centralizes contextual supporting fallback from topic focus, motivation, and pain point for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_topic = cls._normalize_metadata_text(topic_focus, limit=120)
        normalized_motivation = cls._normalize_metadata_text(motivation, limit=96)
        normalized_pain_point = cls._normalize_metadata_text(pain_point, limit=96)
        normalized_objection = cls._normalize_metadata_text(objection, limit=96)
        normalized_foundations = cls._normalize_metadata_text(brand_foundations, limit=120)
        normalized_research = cls._normalize_metadata_text(research_highlight, limit=120)
        normalized_knowledge = cls._normalize_metadata_text(knowledge_line, limit=120)
        if normalized_topic and normalized_objection and normalized_research:
            return (
                f"Back '{normalized_topic}' with concrete proof that answers '{normalized_objection}', "
                f"starting from this audience signal: {normalized_research}"
            )
        if normalized_topic and normalized_objection and normalized_knowledge:
            return (
                f"Back '{normalized_topic}' with concrete proof that answers '{normalized_objection}', "
                f"using this fact: {normalized_knowledge}"
            )
        if normalized_topic and normalized_pain_point and normalized_motivation:
            return (
                f"Show how '{normalized_topic}' moves people from '{normalized_pain_point}' to "
                f"'{normalized_motivation}' with concrete detail."
            )
        if normalized_topic and normalized_foundations and normalized_knowledge:
            return (
                f"Show how '{normalized_topic}' delivers on {normalized_foundations}, using this fact: "
                f"{normalized_knowledge}"
            )
        if normalized_topic and normalized_foundations:
            return f"Show how '{normalized_topic}' delivers on {normalized_foundations} with concrete detail."
        if normalized_topic and normalized_research:
            return f"Use this audience signal as the proof lens for '{normalized_topic}': {normalized_research}"
        if normalized_topic:
            return f"Back '{normalized_topic}' with {semantic_support_phrase}, so the message sounds useful rather than generic."
        if normalized_research:
            return f"Use this audience signal as the proof lens: {normalized_research}"
        if normalized_knowledge:
            return f"Build the support copy around this concrete fact: {normalized_knowledge}"
        if normalized_foundations:
            return f"Translate {normalized_foundations} into concrete proof the audience can picture."
        return f"Back the message with {semantic_support_phrase} so it feels earned."

    @classmethod
    def _contextual_cta_fallback(
        cls,
        *,
        topic_focus: str,
        primary_goal: Any,
        pain_point: str,
        objection: str,
    ) -> str:
        # Centralizes contextual cta fallback from topic focus, primary goal, and pain point for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized_goal = cls._normalize_metadata_text(primary_goal, limit=80)
        normalized_topic = cls._normalize_metadata_text(topic_focus, limit=120)
        normalized_pain_point = cls._normalize_metadata_text(pain_point, limit=96)
        normalized_objection = cls._normalize_metadata_text(objection, limit=96)
        if normalized_goal and normalized_topic:
            return (
                f"Invite a low-friction next step that helps the audience act on '{normalized_topic}' "
                f"and move toward {normalized_goal}."
            )
        if normalized_goal and normalized_objection:
            return (
                f"Invite a low-friction next step that helps skeptical readers validate the claim "
                f"and move toward {normalized_goal}."
            )
        if normalized_goal:
            return f"Invite a low-friction next step that moves the audience toward {normalized_goal}."
        if normalized_topic and normalized_objection:
            return (
                f"Invite a low-friction next step tied to '{normalized_topic}' so skeptical readers can validate it "
                f"for themselves."
            )
        if normalized_topic and normalized_pain_point:
            return (
                f"Invite a low-friction next step tied to '{normalized_topic}' that reduces the feeling of "
                f"'{normalized_pain_point}'."
            )
        if normalized_topic:
            return f"Invite a low-friction next step tied to '{normalized_topic}' so the CTA feels useful, not generic."
        return "Invite a low-friction next step that helps the audience act with more clarity."

    @classmethod
    def _looks_like_fallback_copy_scaffold(cls, value: Any) -> bool:
        # Checks fallback copy scaffold from input value for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        text = cls._normalize_metadata_text(value, limit=220).casefold()
        if not text:
            return False
        if any(text.startswith(prefix) for prefix in cls.FALLBACK_COPY_INSTRUCTION_PREFIXES):
            return True
        return any(marker in text for marker in cls.FALLBACK_COPY_DESCRIPTOR_MARKERS)

    @classmethod
    def _usable_fallback_copy_text(
        cls,
        value: Any,
        *,
        limit: int,
        prompt: str,
    ) -> str:
        # Extracts usable fallback copy text from input value, limit, and prompt text for AIOrchestratorService.
        # It calls _normalize_metadata_text and _looks_like_fallback_copy_scaffold to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=limit)
        if not text or text.casefold() == "missing":
            return ""
        if cls._looks_like_fallback_copy_scaffold(text):
            return ""
        if prompt and cls._looks_like_prompt_echo(text, prompt):
            return ""
        return text

    @classmethod
    def _fallback_copy_fragments(
        cls,
        candidates: list[Any],
        *,
        prompt: str,
        limit: int,
        max_items: int,
        exclude: list[str] | None = None,
    ) -> list[str]:
        # Centralizes fallback copy fragments from candidates, prompt text, and limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        fragments: list[str] = []
        seen = {
            cls._normalize_metadata_text(item, limit=limit).casefold()
            for item in (exclude or [])
            if cls._normalize_metadata_text(item, limit=limit)
        }
        for candidate in candidates:
            text = cls._usable_fallback_copy_text(candidate, limit=limit, prompt=prompt)
            if not text:
                continue
            key = text.casefold()
            if key in seen:
                continue
            fragments.append(text.rstrip())
            seen.add(key)
            if len(fragments) >= max_items:
                break
        return fragments

    @classmethod
    def _fallback_headline_copy(
        cls,
        *,
        prompt: str,
        claim_evidence_pairs: list[dict[str, str]],
        desired_outcomes: list[str],
        key_value_proposition: Any,
        headline_direction: Any,
        primary_campaign_theme: Any,
        core_audience_message: Any,
    ) -> str:
        # Centralizes fallback headline copy from prompt text, claim evidence pairs, and desired outcomes for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        candidates: list[Any] = [
            *desired_outcomes[:2],
            key_value_proposition,
            *[pair.get("claim") for pair in claim_evidence_pairs if isinstance(pair, dict)],
            primary_campaign_theme,
            headline_direction,
            core_audience_message,
        ]
        fragments = cls._fallback_copy_fragments(
            candidates,
            prompt=prompt,
            limit=80,
            max_items=1,
        )
        if fragments:
            return fragments[0].rstrip(" .!?")
        prompt_topic = cls._prompt_topic_summary(prompt)
        if prompt_topic:
            return cls._headline_from_prompt_topic(prompt_topic)
        return "Brand Update"

    @classmethod
    def _fallback_body_copy(
        cls,
        *,
        prompt: str,
        headline: str,
        core_audience_message: Any,
        claim_evidence_pairs: list[dict[str, str]],
        trust_builders: list[str],
        proof_points: list[str],
        research_highlights: list[str],
        supporting_copy_direction: Any,
        key_value_proposition: Any,
    ) -> str:
        # Centralizes fallback body copy from prompt text, headline, and core audience message for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        claim_lines = cls._claim_evidence_pair_lines(claim_evidence_pairs, limit=2)
        fragments = cls._fallback_copy_fragments(
            [
                core_audience_message,
                *claim_lines,
                *trust_builders[:2],
                *proof_points[:2],
                *research_highlights[:2],
                supporting_copy_direction,
                key_value_proposition,
            ],
            prompt=prompt,
            limit=180,
            max_items=2,
            exclude=[headline],
        )
        if fragments:
            return cls._normalize_metadata_text(" ".join(fragments), limit=220)
        prompt_topic = cls._prompt_topic_summary(prompt)
        if prompt_topic:
            return cls._body_from_prompt_topic(prompt_topic)
        return "See the details in a concrete, audience-relevant way."

    @classmethod
    def _fallback_cta_copy(
        cls,
        *,
        prompt: str,
        cta_intent: Any,
        primary_goal: Any,
        topic_focus: str,
        comparison_points: list[str],
        proof_cues: list[str],
        trust_signals: list[str],
    ) -> str:
        # Centralizes fallback cta copy from prompt text, cta intent, and primary goal for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        direct_cta = cls._usable_fallback_copy_text(cta_intent, limit=44, prompt=prompt)
        if direct_cta and len(direct_cta.split()) <= 6:
            return direct_cta.rstrip(" .!?")

        normalized_goal = cls._normalize_metadata_text(primary_goal, limit=80).casefold()
        normalized_intent = cls._normalize_metadata_text(cta_intent, limit=120).casefold()
        normalized_topic = cls._normalize_metadata_text(topic_focus, limit=120).casefold()

        if any(token in normalized_goal or token in normalized_intent for token in ("demo", "consult", "call")):
            return "Book a demo"
        if any(token in normalized_goal or token in normalized_intent for token in ("download", "guide", "report", "ebook")):
            return "Get the guide"
        if any(token in normalized_goal or token in normalized_intent for token in ("sign up", "signup", "register", "join", "subscribe")):
            return "Sign up"
        if comparison_points or any(
            token in normalized_intent or token in normalized_topic
            for token in ("compare", "comparison", "versus", "vs", "option", "options", "trade-off", "tradeoff", "difference")
        ):
            return "Compare the options"
        if proof_cues or trust_signals or any(
            token in normalized_intent or token in normalized_topic
            for token in ("proof", "trust", "clarity", "confidence", "details", "risk", "validate")
        ):
            return "See the details"
        if any(
            token in normalized_intent or token in normalized_topic
            for token in ("learn", "understand", "explain", "guide", "checklist", "tips", "strategy", "strategies", "how to")
        ):
            return "Get the details"
        return "See how it works"

    @classmethod
    def _looks_like_objection_response(cls, value: Any) -> bool:
        # Checks objection response from input value for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        text = cls._normalize_metadata_text(value, limit=180)
        if not text:
            return False
        lowered = text.casefold()
        if any(lowered.startswith(prefix) for prefix in cls.OBJECTION_RESPONSE_INSTRUCTION_PREFIXES):
            return False
        return any(marker in lowered for marker in cls.OBJECTION_RESPONSE_MARKERS)

    @classmethod
    def _fallback_objection_handling_copy(
        cls,
        *,
        objections: list[str],
        pain_points: list[str],
        proof_cues: list[str],
        trust_signals: list[str],
        comparison_points: list[str],
        research_highlights: list[str],
    ) -> str:
        # Centralizes fallback objection handling copy from objections, pain points, and proof cues for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        objection = cls._normalize_metadata_text(objections[0] if objections else "", limit=160)
        pain_point = cls._normalize_metadata_text(pain_points[0] if pain_points else "", limit=140)
        proof_cue = cls._normalize_metadata_text(proof_cues[0] if proof_cues else "", limit=140)
        trust_signal = cls._normalize_metadata_text(trust_signals[0] if trust_signals else "", limit=140)
        comparison_point = cls._normalize_metadata_text(comparison_points[0] if comparison_points else "", limit=140)
        research_highlight = cls._normalize_metadata_text(research_highlights[0] if research_highlights else "", limit=140)
        objection_context = f"{objection} {pain_point}".casefold()

        if any(
            token in objection_context
            for token in ("switch", "switching", "migrate", "migration", "change", "onboarding", "implementation", "rollout")
        ):
            anchor = trust_signal or proof_cue or "Guided onboarding"
            return cls._normalize_metadata_text(
                f"{anchor.rstrip('.')} so the next step feels lower-risk without forcing an overnight change.",
                limit=180,
            )
        if any(
            token in objection_context
            for token in ("risk", "return", "returns", "trust", "confidence", "opaque", "clarity", "clear", "proof")
        ):
            anchor = proof_cue or trust_signal or research_highlight or comparison_point or "Transparent downside framing"
            return cls._normalize_metadata_text(
                f"{anchor.rstrip('.')} with transparent downside framing so the trade-off feels clearer and easier to trust.",
                limit=180,
            )
        if any(
            token in objection_context
            for token in ("price", "pricing", "cost", "budget", "expensive", "afford", "value")
        ):
            anchor = comparison_point or proof_cue or trust_signal or "Transparent price framing"
            return cls._normalize_metadata_text(
                f"{anchor.rstrip('.')} so the value is easier to compare without guesswork.",
                limit=180,
            )
        anchor = proof_cue or trust_signal or comparison_point or research_highlight
        if anchor:
            return cls._normalize_metadata_text(
                f"{anchor.rstrip('.')} so the next step feels supported and lower risk.",
                limit=180,
            )
        if pain_point:
            return cls._normalize_metadata_text(
                "Without extra complexity, the next step gives the audience a clearer path forward.",
                limit=180,
            )
        return ""

    @classmethod
    def _sync_scene_graph_copy_from_text_payload(
        cls,
        *,
        elements: list[dict[str, Any]],
        text_payload: dict[str, Any],
    ) -> list[dict[str, Any]]:
        # Builds sync scene graph copy text from elements and copy payload for AIOrchestratorService.
        # It calls _coerce_mapping and _normalize_metadata_text while assembling the payload or prompt text.
        metadata = cls._coerce_mapping(text_payload.get("metadata"))
        supporting_line = cls._normalize_metadata_text(
            metadata.get("supporting_line") or text_payload.get("body"),
            limit=220,
        )
        proof_points = cls._normalize_metadata_list(metadata.get("proof_points"), limit=4)
        stat_highlights = cls._normalize_metadata_list(metadata.get("stat_highlights"), limit=3)
        synced: list[dict[str, Any]] = []
        for element in elements:
            normalized = dict(element)
            role = str(normalized.get("role") or "").strip().lower()
            if role == "headline":
                normalized["text"] = text_payload.get("headline", "")
            elif role == "body":
                normalized["text"] = text_payload.get("body", "")
            elif role == "supporting_line":
                normalized["text"] = supporting_line
            elif role == "cta":
                normalized["text"] = text_payload.get("cta", "")
            elif role == "proof_points":
                normalized["text"] = proof_points or stat_highlights or cls._sentences(text_payload.get("body"))[:3]
            elif role == "stat_highlights":
                normalized["text"] = stat_highlights
            elif role == "section_label":
                normalized["text"] = cls._normalize_metadata_text(metadata.get("section_label"), limit=64)
            synced.append(normalized)
        return synced

    @classmethod
    def _contains_disallowed_glyphs(cls, value: str) -> bool:
        # Centralizes contains disallowed glyphs from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        return bool(cls.DISALLOWED_GLYPH_PATTERN.search(value or "") or cls.MOJIBAKE_SYMBOL_PATTERN.search(value or ""))

    @staticmethod
    def _scene_graph_visible_elements(scene_graph: GenerationSceneGraph) -> list[Any]:
        # Centralizes scene graph visible elements from scene graph for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return [element for element in scene_graph.elements if element.visible]

    def _should_apply_support_fallback(
        self,
        *,
        scene_graph: GenerationSceneGraph,
        validation_report: SceneGraphValidationReport,
    ) -> bool:
        # Checks apply support fallback from scene graph and validation report for AIOrchestratorService.
        # It reuses _scene_graph_visible_elements so related checks follow the same rule.
        visible_elements = self._scene_graph_visible_elements(scene_graph)
        content_elements = [element for element in visible_elements if element.role != "background"]
        severe_rules = {
            "missing_headline",
            "logo_required",
            "insufficient_scene_graph_structure",
            "missing_visual_emphasis",
            "asset_strategy_unfulfilled",
            "asset_strategy_overloaded",
            "icon_stamp_column",
            "icon_overuse_with_hero_image",
            "prompt_echo_copy",
            "topic_anchor_missing",
        }
        if len(content_elements) < 4:
            return True
        if any(issue.rule_id in severe_rules for issue in validation_report.issues):
            return True
        return False

    @staticmethod
    def _should_request_fresh_replan(validation_report: SceneGraphValidationReport) -> bool:
        # Checks request fresh replan from validation report for AIOrchestratorService.
        # This keeps the allowed/blocked rule in one place.
        severe_rules = {
            "missing_headline",
            "insufficient_scene_graph_structure",
            "missing_visual_emphasis",
            "topic_anchor_missing",
            "prompt_echo_copy",
        }
        return any(issue.rule_id in severe_rules for issue in validation_report.issues)

    @staticmethod
    def _fresh_replan_note(validation_report: SceneGraphValidationReport) -> str:
        # Centralizes fresh replan note from validation report for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        issues = [str(issue.rule_id or "").strip() for issue in validation_report.issues if str(issue.rule_id or "").strip()]
        if not issues:
            return (
                "Discard the prior weak scene graph and create a fresh plan from the user prompt, "
                "brand knowledge, objective, and guardrails."
            )
        return (
            "Discard the prior weak scene graph instead of repairing around it. "
            "Create a fresh plan from the user prompt, brand knowledge, objective, and guardrails. "
            f"The prior plan failed on: {', '.join(issues)}."
        )

    @classmethod
    def _scene_graph_has_authoritative_geometry(cls, scene_graph: GenerationSceneGraph | None) -> bool:
        # Measures scene graph authoritative geometry from scene graph for AIOrchestratorService.
        # It calls _scene_graph_element_is_visual_image_like to turn raw evidence into the structured signal the caller needs.
        if not isinstance(scene_graph, GenerationSceneGraph):
            return False
        geometry_count = 0
        anchored_roles: set[str] = set()
        for element in scene_graph.elements:
            if not element.visible or element.role == "background":
                continue
            geometry = element.geometry or SceneGraphGeometry()
            if all(getattr(geometry, attr, None) is not None for attr in ("x", "y", "width", "height")):
                geometry_count += 1
                normalized_role = str(element.role or "").strip().lower()
                anchored_roles.add(normalized_role)
                if cls._scene_graph_element_is_visual_image_like(element):
                    anchored_roles.add("image")
        return geometry_count >= 3 or {"headline", "image", "cta"} <= anchored_roles

    @classmethod
    def _compiled_context_has_authoritative_layout(cls, compiled_context: dict[str, Any] | None) -> bool:
        # Centralizes compiled context authoritative layout from compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        context = dict(compiled_context or {})
        template_fit = cls._coerce_mapping(context.get("template_fit_brief"))
        brand_visual = cls._coerce_mapping(context.get("brand_visual_brief"))
        design_system = cls._coerce_mapping(brand_visual.get("design_system"))
        layout_dna = (
            template_fit.get("template_layout_dna")
            or design_system.get("template_layout_dna")
            or design_system.get("layout_dna")
        )
        sequence_pack = template_fit.get("sequence_pack") or design_system.get("template_sequence_pack")
        if isinstance(layout_dna, dict) and (
            layout_dna.get("zone_map")
            or layout_dna.get("zones")
            or layout_dna.get("layout_type")
        ):
            return True
        if isinstance(sequence_pack, dict) and (
            sequence_pack.get("pages")
            or sequence_pack.get("sequence")
            or sequence_pack.get("story_arc_roles")
            or sequence_pack.get("slides")
            or sequence_pack.get("sequence_cues")
        ):
            return True
        return False

    @classmethod
    def _scene_graph_element_is_visual_image_like(cls, element: SceneGraphElement | dict[str, Any] | None) -> bool:
        # Centralizes scene graph element visual image from element for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if element is None:
            return False
        if isinstance(element, dict):
            visible = bool(element.get("visible", True))
            role = str(element.get("role") or "").strip().casefold()
            element_type = str(element.get("element_type") or "").strip().casefold()
            asset_payload = element.get("asset") if isinstance(element.get("asset"), dict) else {}
        else:
            visible = bool(element.visible)
            role = str(element.role or "").strip().casefold()
            element_type = str(element.element_type or "").strip().casefold()
            asset_payload = (
                element.asset.model_dump(mode="json")
                if getattr(element, "asset", None) is not None
                else {}
            )
        if not visible or role == "background":
            return False
        if role == "image" or element_type == "image":
            return True
        image_like_roles = {
            "hero_visual",
            "hero_image",
            "primary_visual",
            "supporting_visual",
            "supporting_visuals",
            "illustration",
            "illustration_group",
            "infographic_cluster",
            "visual_cluster",
        }
        if role in image_like_roles:
            return True
        asset_role = str((asset_payload or {}).get("asset_role") or "").strip().casefold()
        return any(
            token in asset_role
            for token in ("image", "illustration", "photo", "render", "visual", "infographic")
        )

    @staticmethod
    def _compact_scene_graph_geometry(scene_graph: GenerationSceneGraph | None, limit: int = 8) -> str:
        # Compacts scene graph geometry from scene graph and limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(scene_graph, GenerationSceneGraph):
            return ""
        geometry_manifest: list[dict[str, Any]] = []
        for element in scene_graph.elements:
            if (
                not element.visible
                or element.role == "background"
                or str(element.role or "").strip().casefold() == "logo"
                or str(element.element_type or "").strip().casefold() == "logo"
                or str(element.element_type or "").strip().casefold() == "multi_slide_sequence"
            ):
                continue
            geometry = element.geometry or SceneGraphGeometry()
            if not all(getattr(geometry, attr, None) is not None for attr in ("x", "y", "width", "height")):
                continue
            entry = {
                "role": str(element.role or "").strip().lower(),
                "type": str(element.element_type or "").strip().lower(),
                "x": round(float(geometry.x), 3),
                "y": round(float(geometry.y), 3),
                "w": round(float(geometry.width), 3),
                "h": round(float(geometry.height), 3),
            }
            if geometry.anchor:
                entry["anchor"] = str(geometry.anchor)
            if geometry.z_index is not None:
                entry["z"] = int(geometry.z_index)
            if element.validation_hints:
                text_role = str((element.validation_hints or {}).get("text_role") or "").strip()
                if text_role:
                    entry["text_role"] = text_role
            geometry_manifest.append(entry)
            if len(geometry_manifest) >= limit:
                break
        if not geometry_manifest:
            return ""
        return json.dumps(geometry_manifest, separators=(",", ":"), ensure_ascii=True)

    @classmethod
    def _compact_layout_dna_contract(cls, compiled_context: dict[str, Any] | None, limit: int = 8) -> str:
        # Compacts layout dna contract from compiled context and limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        context = dict(compiled_context or {})
        template_fit = cls._coerce_mapping(context.get("template_fit_brief"))
        brand_visual = cls._coerce_mapping(context.get("brand_visual_brief"))
        design_system = cls._coerce_mapping(brand_visual.get("design_system"))
        layout_dna = (
            template_fit.get("template_layout_dna")
            or design_system.get("template_layout_dna")
            or design_system.get("layout_dna")
        )
        if not isinstance(layout_dna, dict):
            return ""
        payload: dict[str, Any] = {}
        layout_type = cls._normalize_metadata_text(layout_dna.get("layout_type"), limit=64)
        canvas_ratio = cls._normalize_metadata_text(layout_dna.get("canvas_ratio"), limit=32)
        if layout_type:
            payload["layout_type"] = layout_type
        if canvas_ratio:
            payload["ratio"] = canvas_ratio
        zones: list[dict[str, Any]] = []
        raw_zones = layout_dna.get("zone_map") or layout_dna.get("zones") or []
        if isinstance(raw_zones, dict):
            raw_zones = [
                {"role": key, **(value if isinstance(value, dict) else {})}
                for key, value in raw_zones.items()
            ]
        for item in raw_zones[:limit]:
            if not isinstance(item, dict):
                continue
            zone = {
                "role": cls._normalize_metadata_text(item.get("role") or item.get("zone_role"), limit=32),
                "x": item.get("x"),
                "y": item.get("y"),
                "w": item.get("width") if item.get("width") is not None else item.get("w"),
                "h": item.get("height") if item.get("height") is not None else item.get("h"),
            }
            if item.get("anchor"):
                zone["anchor"] = cls._normalize_metadata_text(item.get("anchor"), limit=24)
            if item.get("priority"):
                zone["priority"] = cls._normalize_metadata_text(item.get("priority"), limit=16)
            if all(zone.get(key) is not None for key in ("x", "y", "w", "h")) and zone.get("role"):
                for key in ("x", "y", "w", "h"):
                    zone[key] = round(float(zone[key]), 3)
                zones.append(zone)
        if zones:
            payload["zones"] = zones
        if not payload:
            return ""
        return json.dumps(payload, separators=(",", ":"), ensure_ascii=True)

    @classmethod
    def _final_render_grounding_sections(
        cls,
        *,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload,
        text_payload: StructuredTextPayload,
        compiled_context: dict[str, Any] | None,
        reference_assets: list[dict[str, Any]] | None,
        allow_template_surface_imitation: bool = False,
    ) -> list[str]:
        # Centralizes final grounding sections from request state, creative decision, and copy payload for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        compiled_context = dict(compiled_context or {})
        metadata = text_payload.metadata or {}
        visual_identity = request.resolved_brand_context.get("visual_identity", {}) or {}
        asset_strategy = (creative_decision.asset_strategy if creative_decision else {}) or {}
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata if isinstance(metadata, dict) else {},
        )
        design_style = cls._normalize_metadata_text(metadata.get("design_style"), limit=80)
        visual_direction = cls._normalize_metadata_text(metadata.get("visual_direction"), limit=180)
        preferred_scene = cls._normalize_metadata_text(metadata.get("image_prompt"), limit=220)
        layout_decision = cls._compact_layout_decision(request.layout_decision)
        visual_knowledge_brief = ContextCompilerService.coerce_visual_knowledge_brief(
            compiled_context.get("visual_knowledge_brief"),
        )
        direct_brand_grounding = cls._brand_knowledge_visual_grounding(visual_knowledge_brief)
        grounding_mode = cls._normalize_metadata_text(visual_knowledge_brief.get("grounding_mode"), limit=32)
        grounding_strength = cls._normalize_metadata_text(visual_knowledge_brief.get("grounding_strength"), limit=32)
        grounding_abstention_reason = cls._normalize_metadata_text(
            visual_knowledge_brief.get("abstention_reason"),
            limit=64,
        )
        template_suppressed = bool(visual_knowledge_brief.get("template_suppressed"))
        include_reference_system = bool(reference_assets) or bool(visual_identity.get("reusable_design_assets"))
        reusable_assets = cls._compact_named_items(visual_identity.get("reusable_design_assets"), limit=4)
        reference_summary = cls._compact_reference_assets(reference_assets or [])
        dominant_visual_system = cls._normalized_dominant_visual_system(asset_strategy) or "generated_image"
        supporting_visual_system = cls._normalized_supporting_visual_system(asset_strategy)
        include_reusable_asset_names = include_reference_system and template_surface_policy != "style_reference_only"
        sections = [
            (
                f"Brand knowledge grounding mode: {grounding_mode or 'brand_knowledge'} (strength: {grounding_strength or 'supported'})."
                if direct_brand_grounding
                else (
                    f"Brand knowledge grounding mode: llm_fallback (reason: {grounding_abstention_reason})."
                    if grounding_abstention_reason
                    else "Brand knowledge grounding mode: llm_fallback."
                )
            ),
            (
                f"Brand knowledge grounding: {direct_brand_grounding}. Treat these retrieved brand-knowledge cues as the primary visual grounding contract."
                if direct_brand_grounding
                else "Brand knowledge grounding: no retrieved brand knowledge is available, so infer the visual through the approved copy, message strategy, and brand system."
            ),
            (
                "If retrieved brand knowledge is present, do not override it with a generic invented scene. Use model reasoning only to combine the retrieved cues coherently."
                if direct_brand_grounding
                else "Because retrieved brand knowledge is absent, derive the visual direction from the approved copy, message strategy, and brand system."
            ),
            (
                "Template-derived cues were suppressed because stronger visual identity or mood-board evidence exists."
                if direct_brand_grounding and template_suppressed
                else ""
            ),
            (
                "If the active brand grounding is fallback-only, use template and metadata cues only for structure, palette, spacing, and composition. Do not literalize template copy."
                if direct_brand_grounding and grounding_strength == "fallback_only"
                else ""
            ),
            f"Layout approach: {layout_decision}.",
            f"Dominant visual system: {dominant_visual_system}.",
            f"Supporting visual system: {supporting_visual_system or 'none'}.",
            (
                f"Reusable brand design assets available as stylistic cues: {reusable_assets}."
                if include_reusable_asset_names and reusable_assets and reusable_assets != "None"
                else ""
            ),
            (
                f"Reference asset system available: {reference_summary}."
                if include_reference_system and reference_summary and template_surface_policy != "style_reference_only"
                else ""
            ),
            (
                "When the active template policy is style-reference-only, preserve the uploaded sample's composition skeleton, region proportions, negative space, sequencing, and craft direction as closely as possible while reinterpreting all artwork and text from scratch. Do not trace or copy the literal sample surface."
                if template_surface_policy == "style_reference_only" and not allow_template_surface_imitation
                else ""
            ),
            (f"Fallback design style hint: {design_style}." if design_style else ""),
            (f"Fallback visual direction hint: {visual_direction}." if visual_direction else ""),
            (f"Fallback preferred scene hint: {preferred_scene}." if preferred_scene else ""),
        ]
        return [section for section in sections if section]

    @classmethod
    def _research_editorial_prompt_section(
        cls,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Builds research editorial prompt section from request state and compiled context for AIOrchestratorService.
        # It calls _claim_evidence_pairs_from_research_brief and _normalize_metadata_text while assembling the payload or prompt text.
        context = dict(compiled_context or {})
        request_brief = getattr(request, "research_editorial_brief", None)
        brief = (
            context.get("research_editorial_brief")
            if isinstance(context.get("research_editorial_brief"), dict)
            else request_brief
            if isinstance(request_brief, dict)
            else {}
        )
        if not isinstance(brief, dict):
            return (
                "Research discipline: prefer source-backed specificity when available, and never invent unsupported "
                "numbers, rankings, timelines, or market claims."
            )
        claim_pairs = cls._claim_evidence_pairs_from_research_brief(brief, limit=2)
        verified_facts = (
            (brief.get("fact_model") or {}).get("verified_facts")
            if isinstance(brief.get("fact_model"), dict)
            else []
        )
        if claim_pairs or verified_facts:
            return (
                "Research discipline: anchor specific claims to verified facts and approved claim-evidence pairs only. "
                "Keep the synthesis concise, and do not add extra statistics, rankings, or market assertions that are not supported."
            )
        needs_live_research = bool(brief.get("needs_live_research"))
        research_status = cls._normalize_metadata_text(brief.get("research_status"), limit=32).casefold()
        if needs_live_research or research_status in {"unavailable", "required", "missing"}:
            return (
                "Research discipline: when verified research is limited, stay descriptive and principle-led. "
                "Do not invent exact figures, dates, performance claims, or factual certainty."
            )
        return (
            "Research discipline: prefer source-backed specificity when available, and never invent unsupported "
            "numbers, rankings, timelines, or market claims."
        )

    @classmethod
    def _image_generation_anti_hallucination_prompt_section(
        cls,
        *,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Builds image generation anti hallucination prompt section from request state and compiled context for AIOrchestratorService.
        # It calls _exact_claim_markers and _exact_claim_marker_aliases while assembling the payload or prompt text.
        context = dict(compiled_context or {})
        source_texts: list[Any] = [getattr(request, "prompt", "")]
        for source in (
            getattr(request, "research_editorial_brief", None),
            getattr(request, "live_research", None),
            context.get("research_editorial_brief"),
            context.get("live_research"),
        ):
            if not isinstance(source, dict):
                continue
            fact_model = source.get("fact_model") if isinstance(source.get("fact_model"), dict) else source
            for fact in fact_model.get("verified_facts") or []:
                if isinstance(fact, dict):
                    source_texts.extend([fact.get("label"), fact.get("value"), fact.get("detail")])
                else:
                    source_texts.append(fact)
        supported_markers: set[str] = set()
        for text in source_texts:
            for marker in cls._exact_claim_markers(text):
                supported_markers.update(cls._exact_claim_marker_aliases(marker))
        supported_marker_line = (
            "Source-backed exact values allowed in visible facts/data: "
            + ", ".join(sorted(supported_markers)[:8])
            + ". Add no other numbers, dates, ranks, currency, percentages, or chart values."
            if supported_markers
            else (
                "No source-backed exact values are available for visible facts/data; keep data-like visuals qualitative "
                "and do not render numeric values, dates, rankings, percentages, currency amounts, axes, or chart labels."
            )
        )
        return (
            "Anti-hallucination guard: visible facts, data, objects, documents, UI, products, official marks, source labels, badges, maps, "
            "and charts must be directly supported by the user prompt, verified research, retrieved brand knowledge, references, or approved layout metadata. "
            "Approved copy guides theme and wording only; do not invent fake stats, dashboards, sources, certifications, brand marks, official symbols, "
            "product screens, unrelated industries, pseudo text, or adjacent claims. "
            "Unsupported specifics must become visual absence, neutral placeholders, or generic category-level shapes, not invented detail; do not create named organizations, countries, documents, app screens, people, awards, certificates, source mastheads, maps, UI controls, product labels, or identity marks unless explicitly supported. "
            + supported_marker_line
        )

    @staticmethod
    def _consultant_quality_contract(
        *,
        for_visual_only: bool,
        for_carousel: bool,
        for_static_infographic_ad: bool = False,
    ) -> list[str]:
        # Builds consultant quality contract from for visual only, for carousel, and for static infographic ad for AIOrchestratorService.
        # The returned payload is the compact contract consumed by the next branch.
        layout_target = (
            "Use an advanced explanatory slide composition with deliberate grouping, alignment, rhythm, and negative space; "
            "do not collapse into a generic top-heading, middle-image, bottom-footer layout."
            if for_carousel
            else "Use a simple, scroll-stopping social ad composition with one dominant focal idea, clear headline hierarchy, generous breathing room, and a visible CTA path; do not make it feel like a document page, dense report, or plain text poster."
            if for_static_infographic_ad
            else "Use an advanced explanatory composition with deliberate grouping, alignment, rhythm, and negative space; "
            "do not collapse into a generic top-heading, centered-visual, bottom-footer poster."
        )
        copy_target = (
            "Copy discipline: keep the message concise, high-signal, and editorially tight. Prefer one sharp headline, one supporting line, and only essential proof points. Remove filler, repetition, generic hype, and broad claims."
            if not for_visual_only
            else "Message discipline: make the visual support a concise, high-signal story rather than a vague or generic mood piece."
        )
        return [
            copy_target,
            f"Layout discipline: {layout_target}",
            "Brand and asset discipline: every image, icon, chart cue, or decorative asset must earn its place by explaining the message and matching the brand system. Prefer fewer stronger elements over decorative clutter or generic stock filler.",
        ]

    @classmethod
    def _social_media_ad_composition_contract(
        cls,
        *,
        format_name: Any,
        platform: Any,
    ) -> str:
        # Builds social media ad composition contract from format name and platform for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold()
        normalized_platform = cls._normalize_metadata_text(platform, limit=48) or "social media"
        base = (
            f"Social media ad composition contract for {normalized_platform}: make the image feel like a native feed ad, not a document, slide, report, or template preview. "
            "Use a clean background, one immediate visual hook, strong readable type hierarchy, brand-color restraint, and a CTA treatment that is visible without overpowering the message. "
            "Keep the layout simple, modern, and creative: fewer elements, stronger focal path, no crowded paragraphs, no random icon clutter, and no generic stock-business fallback."
        )
        if normalized_format == "infographic":
            return (
                base
                + " For infographic posts, use a compact social infographic: 3-5 clear modules, icons/charts/diagrams only when they explain the prompt, short labels, clear section rhythm, and enough spacing for mobile readability."
            )
        if normalized_format == "static":
            return (
                base
                + " For static posts, use one bold central message, one supporting line at most, one topic-matched hero visual or metaphor, and one clean CTA button or CTA chip."
            )
        return base

    @classmethod
    def _image_hallucination_control_contract(
        cls,
        *,
        format_name: Any,
        use_backend_text_overlay: bool = False,
    ) -> str:
        # Builds image hallucination control contract from format name and use backend text overlay for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold() or "image"
        text_authority = (
            "Backend will place exact text later, so the image model must not create visible wording, numbers, logos, badges, source labels, or pseudo-text."
            if use_backend_text_overlay
            else "For static/infographic creatives, render only content that is present in the current user prompt, approved text payload, retrieved verified facts, live-research facts, or brand context."
        )
        return (
            f"Image hallucination control for {normalized_format}: treat every visible word, number, ranking, chart label, table row, country/company/person/product name, source label, badge, logo-like mark, legal claim, metric, screenshot, and UI detail as a factual claim. "
            "Do not invent, borrow, or carry over any such detail from model memory, previous requests, sample/template content, placeholder examples, or generic social media patterns. "
            "If a detail is unsupported, omit it or replace it with qualitative, non-specific visual language rather than fake specificity. "
            f"{text_authority} "
            "Every visual subject, icon, chart cue, object, and scene must stay tied to the current prompt and approved brand context; never introduce unrelated people, industries, countries, policy themes, template topics, or stale visual metaphors."
        )

    @classmethod
    def _compact_static_infographic_hallucination_guard(
        cls,
        *,
        request: AIOrchestrationRequest | None,
        format_name: Any,
    ) -> str:
        # Compacts static infographic hallucination guard from request state and format name for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold()
        if normalized_format not in {"static", "infographic"}:
            return ""
        topic_anchors = sorted(cls._final_render_request_topic_tokens(request))[:8] if request is not None else []
        anchors_summary = ", ".join(topic_anchors) if topic_anchors else "the current prompt topic"
        return (
            "Compact hallucination guard / Image hallucination control: treat every visible word, number, ranking, chart label, logo, source, name, metric, and UI detail as factual; each must come only from the prompt, approved copy, verified facts, live research, or brand context. "
            f"Do not invent, borrow, or carry over facts from samples, templates, placeholders, unrelated industries, or model memory; if unsupported, omit it or use qualitative visuals tied to {anchors_summary}. Never invent fake values, stale template topics, placeholders, unrelated industries, or model-memory facts."
        )

    @classmethod
    def _content_visual_explanation_contract(
        cls,
        *,
        request: AIOrchestrationRequest | None,
        format_name: Any,
        platform: Any,
    ) -> str:
        # Builds content visual explanation contract from request state, format name, and platform for AIOrchestratorService.
        # It calls _normalize_metadata_text and _final_render_request_topic_tokens while assembling the payload or prompt text.
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold()
        normalized_platform = cls._normalize_metadata_text(platform, limit=48) or "social media"
        topic_anchors = sorted(cls._final_render_request_topic_tokens(request))[:8]
        anchors_summary = ", ".join(topic_anchors) if topic_anchors else "the current prompt topic"
        base = (
            f"Content + visual explanation contract for {normalized_platform}: the post must explain the topic through both concise readable content and topic-matched imagery. "
            "Use a two-channel composition: text states the idea; visuals show, compare, demonstrate, or symbolize the same idea. "
            "Do not produce a mostly text-only poster, a blank card layout, or a beautiful image with weak token copy. "
            f"Derive the visual metaphor, icons, charts, objects, or diagram logic from these prompt anchors: {anchors_summary}."
        )
        if normalized_format == "infographic":
            return (
                base
                + " For infographic output, pair each major text module with an explanatory visual cue such as an icon, mini chart, flow connector, comparison bar, map-like cue, or diagram shape when it helps the topic; maintain visual hierarchy so the reader can learn by scanning."
            )
        if normalized_format == "static":
            return (
                base
                + " For static output, use one strong hero visual or metaphor, one sharp headline, one short supporting line, and one visible CTA path; the hero visual must carry meaning, not decoration."
            )
        return base

    @classmethod
    def _creative_topic_3d_visual_contract(
        cls,
        *,
        request: AIOrchestrationRequest | None,
        format_name: Any,
        compiled_context: dict[str, Any] | None,
        reference_assets: list[dict[str, Any]] | None = None,
    ) -> str:
        # Builds creative topic 3 d visual contract from request state, format name, and compiled context for AIOrchestratorService.
        # It calls _compiled_visual_style_policy and _select_reference_visual_profile while assembling the payload or prompt text.
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold()
        if normalized_format not in {"static", "infographic"}:
            return ""
        policy = cls._compiled_visual_style_policy(compiled_context, format_name=normalized_format)
        selected_reference_profile = cls._select_reference_visual_profile(
            compiled_context,
            reference_assets=reference_assets,
        )
        policy_three_d_usage = cls._normalize_metadata_text(policy.get("three_d_usage"), limit=24).casefold()
        dominant_image_mode = cls._normalize_metadata_text(policy.get("dominant_image_mode"), limit=24).casefold()
        dominant_depth_mode = cls._normalize_metadata_text(policy.get("dominant_depth_mode"), limit=24).casefold()
        dominant_rendering_mode = cls._normalize_metadata_text(policy.get("dominant_rendering_mode"), limit=24).casefold()
        selected_values = {
            cls._normalize_metadata_text(selected_reference_profile.get("image_mode"), limit=24).casefold(),
            cls._normalize_metadata_text(selected_reference_profile.get("depth_mode"), limit=24).casefold(),
            cls._normalize_metadata_text(selected_reference_profile.get("rendering_mode"), limit=24).casefold(),
        }
        selected_values.discard("")
        selected_reference_is_3d = bool(selected_values & {"3d", "true_3d", "3d_render", "isometric", "2.5d"})
        selected_reference_is_flat_or_photo = bool(
            selected_values
            and not selected_reference_is_3d
            and selected_values & {"flat", "photo", "photo_led", "vector", "2d", "illustration"}
        )
        policy_blocks_3d = (
            policy_three_d_usage == "none"
            or selected_reference_is_flat_or_photo
            or (
                dominant_depth_mode == "flat"
                and dominant_rendering_mode in {"photo", "vector", "illustration"}
                and dominant_image_mode not in {"3d", "isometric"}
            )
        )
        if policy_blocks_3d:
            return ""

        topic_anchors = sorted(cls._final_render_request_topic_tokens(request))[:8] if request is not None else []
        anchors_summary = ", ".join(topic_anchors) if topic_anchors else "the current prompt topic"
        return (
            "Creative topic-specific 3D visual direction: prefer premium 3D/2.5D/isometric topic visuals built from "
            f"{anchors_summary}. Use custom rendered objects, material depth, lighting, and spatial metaphors; "
            "avoid generic coins, arrows, stock dashboards, business portraits, random icon scatter, or unrelated 3D decor."
        )

    @classmethod
    def _final_render_static_infographic_quality_floor_contract(
        cls,
        *,
        request: AIOrchestrationRequest | None,
        format_name: Any,
    ) -> str:
        # Builds final static infographic quality floor contract from request state and format name for AIOrchestratorService.
        # It calls _normalize_metadata_text and _final_render_request_topic_tokens while assembling the payload or prompt text.
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold()
        if normalized_format not in {"static", "infographic"}:
            return ""
        topic_anchors = sorted(cls._final_render_request_topic_tokens(request))[:8]
        anchors_summary = ", ".join(topic_anchors) if topic_anchors else "the current prompt topic"
        base = (
            "Minimum output quality floor for final static/infographic image: finished premium social creative at thumbnail size and full size; "
            "clear focal path, crisp contrast, deliberate spacing, clean module edges, coherent visual treatment, and at least one topic-specific visual unit tied to "
            f"{anchors_summary}; never blank, text-only, generic-stock, or random-icon output. "
            "Layout discipline: use a simple, scroll-stopping social ad composition."
        )
        if normalized_format == "infographic":
            return (
                base
                + " Infographic floor: show a scannable section rhythm with visible row/card/module structure, paired visual cues, and mobile-readable labels."
            )
        return (
            base
            + " Static floor: show one dominant topic-specific hero concept with concise readable copy support, clear CTA path, and no clutter competing with the main message."
        )

    @classmethod
    def _multimodal_balance_contract(
        cls,
        *,
        format_name: str,
        supporting_line: str = "",
        proof_points: list[str] | None = None,
        claim_evidence_pairs: list[dict[str, str]] | None = None,
        for_visual_only: bool,
    ) -> list[str]:
        # Builds multimodal balance contract from format name, supporting line, and proof points for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        normalized_format = str(format_name or "").strip().casefold()
        proof_count = len([item for item in (proof_points or []) if cls._normalize_metadata_text(item, limit=140)])
        claim_count = len(
            [
                pair
                for pair in (claim_evidence_pairs or [])
                if isinstance(pair, dict)
                and (
                    cls._normalize_metadata_text(pair.get("claim"), limit=140)
                    or cls._normalize_metadata_text(pair.get("evidence"), limit=180)
                )
            ]
        )
        has_supporting_line = bool(cls._normalize_metadata_text(supporting_line, limit=220))
        evidence_density = proof_count + claim_count + (1 if has_supporting_line else 0)

        if for_visual_only:
            base = (
                "Premium multimodal balance: build one dominant explanatory visual system with one controlled supporting layer. "
                "Keep enough calm negative space for the approved text regions, and do not let decorative visuals overpower the message."
            )
        elif normalized_format == "carousel":
            base = (
                "Premium multimodal balance: each slide should feel visually led but editorially grounded. "
                "Let the main image or explainer structure carry the concept, while headline, supporting line, and proof modules add only the necessary meaning."
            )
        elif normalized_format == "infographic":
            base = (
                "Premium multimodal balance: make the canvas a true visual explainer, not a text slab and not a decorative poster. "
                "Distribute evidence across modular visual sections with one clear focal path."
            )
        else:
            base = (
                "Premium multimodal balance: keep the creative visually led with concise text support. "
                "Do not turn the layout into either a text-heavy block or an empty hero image with token copy."
            )

        sections = [base]
        if evidence_density >= 4:
            sections.append(
                "Evidence-density rule: because the content carries multiple proof or claim modules, use deliberate evidence containers or callout regions instead of stacking all copy into one dense paragraph block."
            )
        else:
            sections.append(
                "Restraint rule: keep supporting evidence selective and high-signal so the composition stays premium rather than crowded."
            )
        sections.append(
            "Do not duplicate the same message in every modality. The visual should explain something the text does not fully restate, and the text should clarify what the visual implies."
        )
        return sections

    @staticmethod
    def _element_normalized_area(element: Any) -> float:
        # Centralizes element area from element for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        geometry = getattr(element, "geometry", None)
        if geometry is None:
            return 0.0
        try:
            width = float(geometry.width if geometry.width is not None else 0.0)
            height = float(geometry.height if geometry.height is not None else 0.0)
        except (TypeError, ValueError):
            return 0.0
        if width <= 0 or height <= 0:
            return 0.0
        units = str(getattr(geometry, "units", "") or "normalized").strip().lower()
        if units == "normalized" or max(abs(width), abs(height)) <= 1.5:
            return max(min(width * height, 1.0), 0.0)
        return 0.0

    @classmethod
    def _scene_graph_text_like_element(cls, element: Any) -> bool:
        # Extracts scene graph text element from element for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        role = str(getattr(element, "role", "") or getattr(element, "element_type", "")).strip().lower()
        element_type = str(getattr(element, "element_type", "")).strip().lower()
        return role in {
            "headline",
            "supporting_line",
            "body",
            "proof_points",
            "stat_highlights",
            "cta",
            "section_label",
            "footer",
            "legal",
        } or element_type in {"text", "text_block", "text_group", "headline"}

    @classmethod
    def _multimodal_balance_issue(
        cls,
        scene_graph: GenerationSceneGraph,
        *,
        format_name: str,
    ) -> str:
        # Centralizes multimodal balance issue from scene graph and format name for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_format = str(format_name or "").strip().casefold()
        if normalized_format not in {"static", "carousel", "infographic", "story", "poster"}:
            return ""
        visible_elements = [element for element in scene_graph.elements if element.visible and element.role != "background"]
        if not visible_elements:
            return ""
        text_elements = [element for element in visible_elements if cls._scene_graph_text_like_element(element)]
        image_elements = [element for element in visible_elements if cls._scene_graph_element_is_visual_image_like(element)]
        if not text_elements or not image_elements:
            return ""
        text_area = sum(cls._element_normalized_area(element) for element in text_elements)
        image_area = sum(cls._element_normalized_area(element) for element in image_elements)
        if text_area >= 0.58 and image_area <= 0.18:
            return "text_heavy"
        if len(text_elements) >= 4 and text_area >= 0.32 and image_area <= 0.05:
            return "text_heavy"
        if image_area >= 0.78 and text_area <= 0.1 and len(text_elements) <= 2:
            return "image_heavy"
        return ""

    @classmethod
    def _reference_family_zone_boxes(cls, value: Any, *, limit: int = 10) -> list[dict[str, Any]]:
        # Measures reference family zone boxes from input value and limit for AIOrchestratorService.
        # It calls _coerce_list and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        zone_boxes: list[dict[str, Any]] = []
        for item in cls._coerce_list(value)[:limit]:
            if not isinstance(item, dict):
                continue
            role = cls._normalize_metadata_text(item.get("role"), limit=32).casefold()
            if not role:
                continue
            try:
                x = float(item.get("x"))
                y = float(item.get("y"))
                w = float(item.get("w") if item.get("w") is not None else item.get("width"))
                h = float(item.get("h") if item.get("h") is not None else item.get("height"))
            except (TypeError, ValueError):
                continue
            if w <= 0 or h <= 0:
                continue
            zone_boxes.append(
                {
                    "role": role,
                    "x": round(x, 3),
                    "y": round(y, 3),
                    "w": round(w, 3),
                    "h": round(h, 3),
                }
            )
        return zone_boxes

    @classmethod
    def _reference_family_zone_role_candidates(cls, role: str) -> list[str]:
        # Measures reference family zone role candidates from role for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        normalized = str(role or "").strip().casefold()
        mapping = {
            "headline": ["headline"],
            "supporting_line": ["supporting_line", "body"],
            "body": ["body", "supporting_line", "proof_points"],
            "proof_points": ["proof_points", "stat_highlights", "body"],
            "stat_highlights": ["stat_highlights", "proof_points"],
            "cta": ["cta", "footer"],
            "footer": ["footer", "cta", "legal"],
            "legal": ["legal", "footer"],
            "logo": ["logo"],
            "image": ["image", "hero_visual", "hero_image", "primary_visual", "illustration"],
            "hero_visual": ["hero_visual", "image", "hero_image", "primary_visual"],
            "hero_image": ["hero_image", "hero_visual", "image", "primary_visual"],
            "primary_visual": ["primary_visual", "hero_visual", "image", "hero_image"],
            "illustration": ["illustration", "primary_visual", "hero_visual", "image"],
        }
        return mapping.get(normalized, [normalized])

    @classmethod
    def _reference_family_role_box(
        cls,
        zone_boxes: list[dict[str, Any]],
        role: str,
        *,
        image_like: bool = False,
    ) -> dict[str, Any]:
        # Measures reference family role box from zone boxes, role, and image like for AIOrchestratorService.
        # It calls _reference_family_zone_role_candidates to turn raw evidence into the structured signal the caller needs.
        candidates = cls._reference_family_zone_role_candidates(role)
        if image_like:
            candidates = [
                *candidates,
                "hero_visual",
                "hero_image",
                "primary_visual",
                "illustration",
                "image",
            ]
        desired = {candidate for candidate in candidates if candidate}
        for zone in zone_boxes:
            zone_role = str(zone.get("role") or "").strip().casefold()
            if zone_role in desired:
                return zone
        return {}

    @staticmethod
    def _normalized_geometry_iou(current: dict[str, Any], target: dict[str, Any]) -> float:
        # Normalizes geometry iou from current and target for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        try:
            current_x = float(current.get("x"))
            current_y = float(current.get("y"))
            current_w = float(current.get("width") if current.get("width") is not None else current.get("w"))
            current_h = float(current.get("height") if current.get("height") is not None else current.get("h"))
            target_x = float(target.get("x"))
            target_y = float(target.get("y"))
            target_w = float(target.get("width") if target.get("width") is not None else target.get("w"))
            target_h = float(target.get("height") if target.get("height") is not None else target.get("h"))
        except (TypeError, ValueError):
            return 0.0
        if min(current_w, current_h, target_w, target_h) <= 0:
            return 0.0
        intersection_x1 = max(current_x, target_x)
        intersection_y1 = max(current_y, target_y)
        intersection_x2 = min(current_x + current_w, target_x + target_w)
        intersection_y2 = min(current_y + current_h, target_y + target_h)
        intersection_w = max(0.0, intersection_x2 - intersection_x1)
        intersection_h = max(0.0, intersection_y2 - intersection_y1)
        intersection = intersection_w * intersection_h
        if intersection <= 0:
            return 0.0
        current_area = current_w * current_h
        target_area = target_w * target_h
        union = current_area + target_area - intersection
        if union <= 0:
            return 0.0
        return intersection / union

    @classmethod
    def _reference_family_active_slide_profile(
        cls,
        compiled_context: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Builds reference family active slide profile from compiled context for AIOrchestratorService.
        # It calls _reference_family_profile while assembling the payload or prompt text.
        profile = cls._reference_family_profile(compiled_context)
        slide_profiles = [dict(item) for item in profile.get("slide_profiles", []) if isinstance(item, dict)]
        return slide_profiles[0] if slide_profiles else {}

    @classmethod
    def _apply_reference_family_scene_defaults(
        cls,
        scene_graph_payload: dict[str, Any],
        *,
        compiled_context: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Applies apply reference family scene defaults from scene graph payload and compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        payload = dict(scene_graph_payload or {})
        profile = cls._reference_family_profile(compiled_context)
        slide_profile = cls._reference_family_active_slide_profile(compiled_context) if profile else {}
        zone_boxes = cls._reference_family_zone_boxes(slide_profile.get("zone_boxes") or profile.get("zone_boxes"))
        reference_template_lock = False
        reference_template_layout_type = ""
        if not zone_boxes:
            context = cls._coerce_mapping(compiled_context)
            adaptation_profile = cls._coerce_mapping(context.get("reference_adaptation_profile"))
            source_family = cls._normalize_metadata_text(adaptation_profile.get("source_format_family"), limit=32).casefold()
            output_family = cls._normalize_metadata_text(adaptation_profile.get("output_format_family"), limit=32).casefold()
            if source_family == output_family and output_family in {"static", "infographic"}:
                template_structure = cls._coerce_mapping(adaptation_profile.get("template_structure"))
                sample_page_blueprint = cls._coerce_mapping(template_structure.get("sample_page_blueprint"))
                layout_dna = cls._coerce_mapping(template_structure.get("layout_dna"))
                reference_template_layout_type = (
                    cls._normalize_metadata_text(layout_dna.get("layout_type"), limit=64)
                    or cls._normalize_metadata_text(sample_page_blueprint.get("layout_category"), limit=64)
                )
                zone_boxes = cls._reference_family_zone_boxes(
                    layout_dna.get("zones")
                    or sample_page_blueprint.get("zones")
                    or sample_page_blueprint.get("image_zones")
                )
                reference_template_lock = bool(zone_boxes)
        if not zone_boxes:
            return payload
        lock_strength = cls._normalize_metadata_text(profile.get("layout_lock_strength"), limit=24).casefold()
        if reference_template_lock and lock_strength in {"", "light"}:
            lock_strength = "strong"
        minimum_iou = {"strict": 0.62, "strong": 0.45, "guided": 0.3}.get(lock_strength, 0.0)
        normalized_elements: list[dict[str, Any]] = []
        for element in cls._coerce_list(payload.get("elements")):
            if not isinstance(element, dict):
                normalized_elements.append(element)
                continue
            element_copy = dict(element)
            if not bool(element_copy.get("visible", True)):
                normalized_elements.append(element_copy)
                continue
            role = str(element_copy.get("role") or "").strip().casefold()
            if role == "background":
                normalized_elements.append(element_copy)
                continue
            target_box = cls._reference_family_role_box(
                zone_boxes,
                role,
                image_like=cls._scene_graph_element_is_visual_image_like(element_copy),
            )
            if not target_box:
                normalized_elements.append(element_copy)
                continue
            geometry = cls._coerce_mapping(element_copy.get("geometry"))
            current_iou = cls._normalized_geometry_iou(geometry, target_box) if geometry else 0.0
            if not geometry or current_iou < minimum_iou:
                element_copy["geometry"] = {
                    "x": target_box["x"],
                    "y": target_box["y"],
                    "width": target_box["w"],
                    "height": target_box["h"],
                    "units": "normalized",
                }
            validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
            validation_hints.setdefault("reference_zone_role", str(target_box.get("role") or ""))
            validation_hints["reference_family_lock_strength"] = lock_strength or "guided"
            if reference_template_lock:
                validation_hints["reference_template_layout_locked"] = True
            if validation_hints:
                element_copy["validation_hints"] = validation_hints
            normalized_elements.append(element_copy)
        styles = cls._coerce_mapping(payload.get("styles"))
        if not styles.get("layout_archetype"):
            styles["layout_archetype"] = (
                cls._normalize_metadata_text(slide_profile.get("layout_type"), limit=64)
                or next(iter(cls._normalize_metadata_list(profile.get("layout_archetypes"), limit=1)), "")
                or reference_template_layout_type
            )
        styles["reference_family_lock_strength"] = lock_strength or "guided"
        if reference_template_lock:
            styles["reference_template_layout_locked"] = True
        payload["styles"] = styles
        payload["elements"] = normalized_elements
        payload = cls._apply_context_visual_craft_hints(
            payload,
            compiled_context=compiled_context,
        )
        return payload

    @classmethod
    def _apply_sequence_pack_scene_defaults(
        cls,
        scene_graph_payload: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
    ) -> dict[str, Any]:
        # Applies apply sequence pack scene defaults from scene graph payload, request state, and creative decision for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        payload = dict(scene_graph_payload or {})
        if request is None:
            return payload
        if str(request.studio_panel.get("format") or "").strip().lower() != "carousel":
            return payload
        template_context = request.template_context if isinstance(request.template_context, dict) else {}
        direct_sequence_pack = template_context.get("sequence_pack") if isinstance(template_context.get("sequence_pack"), dict) else {}
        sequence_pack = (
            direct_sequence_pack
            if direct_sequence_pack.get("slides")
            else cls._template_sequence_pack(request, creative_decision=creative_decision)
        )
        if not isinstance(sequence_pack, dict):
            return payload
        surface_policy = cls._normalize_metadata_text(sequence_pack.get("surface_policy"), limit=32).casefold()
        if surface_policy not in {"style_reference_only", "lock_template_surface", "sequence_pack_locked"} and not cls._should_force_sequence_pack(
            request,
            creative_decision=creative_decision,
        ):
            return payload
        sequence_slides = [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]
        if not sequence_slides:
            return payload
        active_slide = sequence_slides[0]
        zone_map = cls._coerce_mapping(active_slide.get("zone_map"))
        zone_boxes = cls._reference_family_zone_boxes(zone_map.get("zones"))
        if not zone_boxes:
            return payload
        minimum_iou = {
            "lock_template_surface": 0.7,
            "sequence_pack_locked": 0.7,
            "style_reference_only": 0.5,
        }.get(surface_policy, 0.55)
        normalized_elements: list[dict[str, Any]] = []
        for element in cls._coerce_list(payload.get("elements")):
            if not isinstance(element, dict):
                normalized_elements.append(element)
                continue
            element_copy = dict(element)
            if not bool(element_copy.get("visible", True)):
                normalized_elements.append(element_copy)
                continue
            role = str(element_copy.get("role") or "").strip().casefold()
            if role == "background":
                normalized_elements.append(element_copy)
                continue
            target_box = cls._reference_family_role_box(
                zone_boxes,
                role,
                image_like=cls._scene_graph_element_is_visual_image_like(element_copy),
            )
            if not target_box:
                normalized_elements.append(element_copy)
                continue
            geometry = cls._coerce_mapping(element_copy.get("geometry"))
            current_iou = cls._normalized_geometry_iou(geometry, target_box) if geometry else 0.0
            if not geometry or current_iou < minimum_iou:
                element_copy["geometry"] = {
                    "x": target_box["x"],
                    "y": target_box["y"],
                    "width": target_box["w"],
                    "height": target_box["h"],
                    "units": "normalized",
                }
            validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
            validation_hints["reference_zone_role"] = str(target_box.get("role") or "")
            validation_hints["sequence_pack_layout_locked"] = True
            validation_hints["reference_family_lock_strength"] = validation_hints.get("reference_family_lock_strength") or "strong"
            element_copy["validation_hints"] = validation_hints
            normalized_elements.append(element_copy)
        styles = cls._coerce_mapping(payload.get("styles"))
        layout_type = cls._normalize_metadata_text(zone_map.get("layout_type"), limit=64)
        if layout_type:
            styles["layout_archetype"] = layout_type
            styles["layout_type"] = layout_type
        styles["sequence_pack_layout_locked"] = True
        payload["styles"] = styles
        payload["elements"] = normalized_elements
        validation_hints = cls._coerce_mapping(payload.get("validation_hints"))
        validation_hints["sequence_pack_layout_locked"] = True
        validation_hints["template_surface_policy"] = surface_policy or validation_hints.get("template_surface_policy") or ""
        payload["validation_hints"] = validation_hints
        return payload

    @classmethod
    def _context_visual_craft_hints(
        cls,
        compiled_context: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Centralizes context visual craft hints from compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        context = cls._coerce_mapping(compiled_context)
        brand_visual = cls._coerce_mapping(context.get("brand_visual_brief"))
        design_system = cls._coerce_mapping(brand_visual.get("design_system"))
        profile = cls._reference_family_profile(compiled_context)

        visual_craft = cls._coerce_mapping(
            design_system.get("visual_craft")
            or brand_visual.get("visual_craft")
            or profile.get("visual_craft")
        )
        composition_logic = cls._coerce_mapping(
            design_system.get("composition_logic")
            or brand_visual.get("composition_logic")
            or profile.get("composition_logic")
        )
        subject_semantics = cls._coerce_mapping(
            design_system.get("subject_semantics")
            or brand_visual.get("subject_semantics")
            or profile.get("subject_semantics")
        )

        def first_value(mapping: dict[str, Any], *keys: str) -> str:
            # Centralizes first from mapping for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            for key in keys:
                value = mapping.get(key)
                if isinstance(value, list):
                    value = next((item for item in value if str(item or "").strip()), "")
                text = cls._normalize_metadata_text(value, limit=80).casefold()
                if text:
                    return text
            return ""

        hints: dict[str, Any] = {}
        for target_key, source_keys in {
            "visual_depth_style": ("depth_styles", "depth_style"),
            "visual_rendering_style": ("rendering_styles", "rendering_style"),
            "visual_lighting_mode": ("lighting_modes", "lighting"),
            "visual_polish_level": ("polish_levels", "polish_level"),
            "composition_balance": ("balances", "balance"),
            "composition_framing": ("framings", "framing"),
            "composition_layering": ("layerings", "layering"),
            "subject_scene_type": ("scene_types", "scene_type"),
            "subject_abstraction_level": ("abstraction_levels", "abstraction_level"),
        }.items():
            source_mapping = (
                visual_craft
                if target_key.startswith("visual_")
                else composition_logic
                if target_key.startswith("composition_")
                else subject_semantics
            )
            value = first_value(source_mapping, *source_keys)
            if value:
                hints[target_key] = value

        primary_subjects = cls._normalize_metadata_list(subject_semantics.get("primary_subjects"), limit=4)
        financial_objects = cls._normalize_metadata_list(subject_semantics.get("financial_objects"), limit=4)
        material_cues = cls._normalize_metadata_list(visual_craft.get("material_cues"), limit=4)
        dimensionality_cues = cls._normalize_metadata_list(visual_craft.get("dimensionality_cues"), limit=4)
        if primary_subjects:
            hints["primary_subjects"] = primary_subjects
        if financial_objects:
            hints["financial_objects"] = financial_objects
        if material_cues:
            hints["material_cues"] = material_cues
        if dimensionality_cues:
            hints["dimensionality_cues"] = dimensionality_cues
        return hints

    @classmethod
    def _apply_context_visual_craft_hints(
        cls,
        scene_graph_payload: dict[str, Any],
        *,
        compiled_context: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Applies apply context visual craft hints from scene graph payload and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        hints = cls._context_visual_craft_hints(compiled_context)
        if not hints:
            return scene_graph_payload
        payload = dict(scene_graph_payload or {})
        normalized_elements: list[dict[str, Any]] = []
        for element in cls._coerce_list(payload.get("elements")):
            if not isinstance(element, dict):
                normalized_elements.append(element)
                continue
            element_copy = dict(element)
            if cls._scene_graph_element_is_visual_image_like(element_copy):
                validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
                for key, value in hints.items():
                    validation_hints.setdefault(key, value)
                element_copy["validation_hints"] = validation_hints
            normalized_elements.append(element_copy)
        payload["elements"] = normalized_elements
        return payload

    @classmethod
    def _reference_family_closeness(
        cls,
        scene_graph: GenerationSceneGraph,
        *,
        compiled_context: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Centralizes reference family closeness from scene graph and compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        profile = cls._reference_family_profile(compiled_context)
        if not profile:
            return {"score": 1.0, "issues": []}
        slide_profile = cls._reference_family_active_slide_profile(compiled_context)
        zone_boxes = cls._reference_family_zone_boxes(slide_profile.get("zone_boxes") or profile.get("zone_boxes"))
        visible_roles: set[str] = set()
        for element in scene_graph.elements:
            if not element.visible:
                continue
            role = str(element.role or "").strip().casefold()
            if role:
                visible_roles.add(role)
            hint_role = cls._normalize_metadata_text((element.validation_hints or {}).get("reference_zone_role"), limit=32).casefold()
            if hint_role:
                visible_roles.add(hint_role)
            if cls._scene_graph_element_is_visual_image_like(element):
                visible_roles.update({"image", "hero_visual"})
        active_profile = slide_profile or profile
        expected_roles = {
            str(role or "").strip().casefold()
            for role in cls._normalize_metadata_list(
                active_profile.get("zone_roles")
                or active_profile.get("preferred_zone_roles")
                or profile.get("preferred_zone_roles"),
                limit=12,
            )
            if str(role or "").strip()
        }
        expected_image_roles = {
            str(role or "").strip().casefold()
            for role in cls._normalize_metadata_list(
                active_profile.get("approved_image_zone_roles")
                or profile.get("approved_image_zone_roles"),
                limit=6,
            )
            if str(role or "").strip()
        }
        module_patterns = {
            str(role or "").strip().casefold()
            for role in cls._normalize_metadata_list(
                active_profile.get("module_patterns")
                or profile.get("module_patterns"),
                limit=8,
            )
            if str(role or "").strip()
        }
        issues: list[str] = []
        score = 1.0
        if expected_roles:
            overlap = len(expected_roles & visible_roles)
            coverage = overlap / max(len(expected_roles), 1)
            if coverage < 0.55:
                score -= 0.18
                issues.append("reference_family_zone_drift")
            elif coverage < 0.8:
                score -= 0.08
        if expected_image_roles and not (expected_image_roles & visible_roles):
            score -= 0.14
            issues.append("reference_family_image_zone_drift")
        current_archetype = str((scene_graph.styles or {}).get("layout_archetype") or (scene_graph.styles or {}).get("layout_type") or "").strip().casefold()
        target_archetypes = {
            str(item or "").strip().casefold()
            for item in cls._normalize_metadata_list(profile.get("layout_archetypes"), limit=6)
            if str(item or "").strip()
        }
        if current_archetype and target_archetypes and current_archetype not in target_archetypes:
            score -= 0.08
            issues.append("reference_family_layout_drift")
        module_role_map = {
            "cover_hero_split": {"headline", "image", "hero_visual"},
            "proof_grid": {"proof_points", "proof_point", "proof_module"},
            "stat_callout": {"stat_highlights", "stat_highlight"},
            "closing_cta_strip": {"cta", "footer"},
            "comparison_module": {"comparison", "body", "proof_points"},
            "sequence_explainer": {"body", "proof_points", "supporting_line"},
            "icon_label_stack": {"icon", "image"},
        }
        missing_modules = 0
        for pattern in module_patterns:
            expected = module_role_map.get(pattern, set())
            if expected and not (expected & visible_roles):
                missing_modules += 1
        if module_patterns and missing_modules >= max(1, len(module_patterns) // 2):
            score -= 0.12
            issues.append("reference_family_module_drift")
        if zone_boxes:
            geometry_matches = 0
            geometry_scores: list[float] = []
            for zone in zone_boxes[:6]:
                role = str(zone.get("role") or "").strip().casefold()
                if not role:
                    continue
                best_iou = 0.0
                for element in scene_graph.elements:
                    if not element.visible or element.role == "background":
                        continue
                    candidates = {
                        str(element.role or "").strip().casefold(),
                        cls._normalize_metadata_text((element.validation_hints or {}).get("reference_zone_role"), limit=32).casefold(),
                    }
                    if cls._scene_graph_element_is_visual_image_like(element):
                        candidates.update({"image", "hero_visual", "hero_image", "primary_visual", "illustration"})
                    if role not in candidates:
                        continue
                    geometry = element.geometry or SceneGraphGeometry()
                    best_iou = max(
                        best_iou,
                        cls._normalized_geometry_iou(
                            {
                                "x": geometry.x,
                                "y": geometry.y,
                                "width": geometry.width,
                                "height": geometry.height,
                            },
                            zone,
                        ),
                    )
                if best_iou >= 0.45:
                    geometry_matches += 1
                geometry_scores.append(best_iou)
            if geometry_scores:
                average_iou = sum(geometry_scores) / len(geometry_scores)
                if geometry_matches < max(1, min(len(geometry_scores), 2)) or average_iou < 0.34:
                    score -= 0.14
                    issues.append("reference_family_geometry_drift")
        return {"score": max(round(score, 2), 0.0), "issues": issues}

    @classmethod
    def _prompt_sequence_pack(cls, compiled_context: dict[str, Any] | None) -> dict[str, Any]:
        # Builds prompt sequence pack from compiled context for AIOrchestratorService.
        # It calls _coerce_mapping while assembling the payload or prompt text.
        context = dict(compiled_context or {})
        template_fit = cls._coerce_mapping(context.get("template_fit_brief"))
        for candidate in (
            template_fit.get("sequence_pack"),
            template_fit.get("template_sequence_pack"),
            context.get("sequence_pack"),
        ):
            mapping = cls._coerce_mapping(candidate)
            if mapping and (
                mapping.get("slides")
                or mapping.get("family_name")
                or mapping.get("sequence_kind")
                or mapping.get("sequence_cues")
            ):
                return mapping
        return {}

    @classmethod
    def _reference_family_profile(cls, compiled_context: dict[str, Any] | None) -> dict[str, Any]:
        # Builds reference family profile from compiled context for AIOrchestratorService.
        # It calls _coerce_mapping while assembling the payload or prompt text.
        context = dict(compiled_context or {})
        profile = cls._coerce_mapping(context.get("reference_family_profile"))
        return profile if profile else {}

    @classmethod
    def _reference_family_slide_profile(
        cls,
        profile: dict[str, Any],
        slide: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Builds reference family slide profile from profile and slide for AIOrchestratorService.
        # It calls _int_or_none while assembling the payload or prompt text.
        if not isinstance(profile, dict) or not isinstance(slide, dict):
            return {}
        slide_profiles = [dict(item) for item in profile.get("slide_profiles", []) if isinstance(item, dict)]
        if not slide_profiles:
            return {}
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        target_index = cls._int_or_none(slide_metadata.get("reference_slide_index")) or cls._int_or_none(slide.get("slide_index"))
        if target_index is not None:
            for candidate in slide_profiles:
                if cls._int_or_none(candidate.get("slide_index")) == target_index:
                    return candidate
        return slide_profiles[0]

    @classmethod
    def _reference_family_contract_sections(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        slide: dict[str, Any] | None = None,
        for_visual_only: bool,
    ) -> list[str]:
        # Builds reference family contract sections from compiled context, slide, and for visual only for AIOrchestratorService.
        # It calls _reference_family_profile and _normalize_metadata_text while assembling the payload or prompt text.
        profile = cls._reference_family_profile(compiled_context)
        if not profile:
            return []
        family_name = cls._normalize_metadata_text(profile.get("family_name"), limit=96)
        format_family = cls._normalize_metadata_text(profile.get("format_family"), limit=24)
        sequence_kind = cls._normalize_metadata_text(profile.get("sequence_kind"), limit=64)
        lock_strength = cls._normalize_metadata_text(profile.get("layout_lock_strength"), limit=24)
        preferred_zone_roles = cls._normalize_metadata_list(profile.get("preferred_zone_roles"), limit=8)
        image_zone_roles = cls._normalize_metadata_list(profile.get("approved_image_zone_roles"), limit=4)
        module_patterns = cls._normalize_metadata_list(profile.get("module_patterns"), limit=6)
        density_target = cls._normalize_metadata_text(profile.get("density_target"), limit=32)
        balance_target = cls._normalize_metadata_text(profile.get("image_text_balance_target"), limit=48)
        spacing_rhythm = cls._normalize_metadata_text(profile.get("spacing_rhythm"), limit=180)
        composition_summary = cls._normalize_metadata_text(profile.get("composition_summary"), limit=180)
        visual_craft_summary = cls._normalize_metadata_text(profile.get("visual_craft_summary"), limit=180)
        subject_summary = cls._normalize_metadata_text(profile.get("subject_semantics_summary"), limit=180)
        sections: list[str] = []
        descriptor = " / ".join(part for part in [family_name, sequence_kind] if part)
        if descriptor:
            sections.append(
                f"Reference family contract: {descriptor}. Treat this as a first-class target, not optional inspiration."
            )
        if lock_strength:
            sections.append(
                f"Reference family layout lock: {lock_strength}. Stay inside the approved family system rather than inventing a fresh layout grammar."
            )
        if preferred_zone_roles:
            sanitized_roles = [r if r != "logo" else "corner_safe_zone" for r in preferred_zone_roles]
            sections.append(f"Reference family zone grammar: {', '.join(sanitized_roles)}.")
        if image_zone_roles:
            sections.append(
                f"Approved image zones only: generated imagery should live inside these family roles only: {', '.join(image_zone_roles)}."
            )
        if module_patterns:
            sections.append(
                f"Reference family module grammar: {', '.join(module_patterns)}. Compose from these approved module types instead of ad-hoc blocks."
            )
        if density_target or balance_target:
            sections.append(
                f"Reference family density target: {density_target or 'balanced'}. Image/text balance target: {balance_target or 'editorial_balanced'}."
            )
        if spacing_rhythm:
            sections.append(f"Reference family spacing rhythm: {spacing_rhythm}.")
        if composition_summary:
            sections.append(f"Reference family composition target: {composition_summary}.")
        if visual_craft_summary:
            sections.append(f"Reference family craft target: {visual_craft_summary}.")
        if subject_summary and not for_visual_only:
            sections.append(f"Reference family subject target: {subject_summary}.")

        slide_profile = cls._reference_family_slide_profile(profile, slide)
        if slide_profile:
            slide_roles = cls._normalize_metadata_list(slide_profile.get("zone_roles"), limit=8)
            slide_image_roles = cls._normalize_metadata_list(slide_profile.get("approved_image_zone_roles"), limit=4)
            slide_patterns = cls._normalize_metadata_list(slide_profile.get("module_patterns"), limit=6)
            slide_headline_hint = cls._normalize_metadata_text(slide_profile.get("headline_hint"), limit=96)
            slide_story_role = cls._normalize_metadata_text(slide_profile.get("story_role"), limit=40)
            if slide_story_role:
                sections.append(f"Reference family slide role target: {slide_story_role}.")
            if slide_headline_hint and not for_visual_only:
                sections.append(f"Reference family slide headline pattern: {slide_headline_hint}.")
            if slide_roles:
                sections.append(f"Reference family slide zone roles: {', '.join(slide_roles)}.")
            if slide_image_roles:
                sections.append(f"Reference family slide image zones: {', '.join(slide_image_roles)}.")
            if slide_patterns:
                sections.append(f"Reference family slide module patterns: {', '.join(slide_patterns)}.")
        elif format_family:
            sections.append(
                f"Reference family format target: {format_family}. Match its native layout behavior rather than a generic social post fallback."
            )
        return sections

    @classmethod
    def _sequence_pack_slide_authority(
        cls,
        sequence_pack: dict[str, Any],
        slide: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Centralizes sequence pack slide authority from sequence pack and slide for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(sequence_pack, dict) or not isinstance(slide, dict):
            return {}
        slides = [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]
        if not slides:
            return {}
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        target_index = cls._int_or_none(slide_metadata.get("reference_slide_index")) or cls._int_or_none(slide.get("slide_index"))
        target_name = cls._normalize_metadata_text(
            slide_metadata.get("reference_template_name") or slide.get("template_name"),
            limit=96,
        ).casefold()
        if target_index is not None:
            for candidate in slides:
                if cls._int_or_none(candidate.get("slide_index")) == target_index:
                    return candidate
        if target_name:
            for candidate in slides:
                if cls._normalize_metadata_text(candidate.get("template_name"), limit=96).casefold() == target_name:
                    return candidate
        return slides[0]

    @classmethod
    def _sequence_pack_mapping_summary(
        cls,
        value: Any,
        *,
        scalar_keys: tuple[str, ...],
        list_keys: tuple[str, ...],
        limit: int = 6,
        text_limit: int = 200,
    ) -> str:
        # Builds sequence pack mapping summary from input value, scalar keys, and list keys for AIOrchestratorService.
        # It calls _normalize_metadata_text and _normalize_metadata_list while assembling the payload or prompt text.
        mapping = value if isinstance(value, dict) else {}
        parts: list[str] = []
        seen: set[str] = set()
        for key in scalar_keys:
            text = cls._normalize_metadata_text(mapping.get(key), limit=80)
            if text:
                normalized = text.casefold()
                if normalized not in seen:
                    seen.add(normalized)
                    parts.append(text)
            if len(parts) >= limit:
                break
        if len(parts) < limit:
            for key in list_keys:
                for item in cls._normalize_metadata_list(mapping.get(key), limit=limit):
                    normalized = item.casefold()
                    if normalized in seen:
                        continue
                    seen.add(normalized)
                    parts.append(item)
                    if len(parts) >= limit:
                        break
                if len(parts) >= limit:
                    break
        return cls._normalize_metadata_text(", ".join(parts), limit=text_limit)

    @classmethod
    def _sequence_blueprint_alignment_sections(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        slide: dict[str, Any] | None = None,
        for_carousel: bool,
        for_visual_only: bool,
    ) -> list[str]:
        # Centralizes sequence blueprint alignment sections from compiled context, slide, and for carousel for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        sequence_pack = cls._prompt_sequence_pack(compiled_context)
        if not sequence_pack:
            return []
        family_name = cls._normalize_metadata_text(sequence_pack.get("family_name"), limit=96)
        sequence_kind = cls._normalize_metadata_text(sequence_pack.get("sequence_kind"), limit=64)
        surface_policy = cls._normalize_metadata_text(sequence_pack.get("surface_policy"), limit=32).casefold()
        slide_count = cls._int_or_none(sequence_pack.get("slide_count")) or 0
        story_roles = cls._normalize_metadata_list(sequence_pack.get("story_roles"), limit=8)
        sequence_cues = cls._normalize_metadata_list(sequence_pack.get("sequence_cues"), limit=8)
        headline_hints = cls._normalize_metadata_list(sequence_pack.get("headline_hints"), limit=4)

        sections: list[str] = []
        authority_label = " / ".join(part for part in [family_name, sequence_kind] if part)
        if authority_label or slide_count:
            sections.append(
                f"Sequence blueprint authority: {authority_label or 'approved sample sequence'}"
                f"{f' ({slide_count} slides)' if slide_count else ''}. "
                "This output must feel sample-driven and brand-driven, not like a generic social template."
            )
        if story_roles:
            sections.append(
                f"Sequence narrative rhythm: {' -> '.join(story_roles)}. Preserve this editorial progression in hierarchy, pacing, and information density."
            )
        if sequence_cues:
            sections.append(
                f"Sequence structural cues from the approved sample: {', '.join(sequence_cues)}."
            )
        if headline_hints and not for_visual_only:
            sections.append(
                f"Approved sample headline intents: {', '.join(headline_hints)}. Keep the visual explanation aligned to this level of specificity."
            )
        if surface_policy == "style_reference_only":
            sections.append(
                (
                    "Sample-driven enforcement: use the uploaded sample intelligence as the governing contract for composition, spacing rhythm, negative space, image treatment, and editorial pacing. "
                    "Rebuild the artwork from scratch, but do not downgrade into a generic carousel, infographic, or static social post."
                )
            )

        slide_authority = cls._sequence_pack_slide_authority(sequence_pack, slide)
        if not slide_authority:
            return sections

        reference_index = cls._int_or_none(slide_authority.get("slide_index"))
        story_role = cls._normalize_metadata_text(slide_authority.get("story_role"), limit=48)
        headline_hint = cls._normalize_metadata_text(slide_authority.get("headline_hint"), limit=96)
        structural_cues = cls._normalize_metadata_list(slide_authority.get("structural_cues"), limit=4)
        sequence_summary = cls._normalize_metadata_text(slide_authority.get("sequence_summary"), limit=160)
        zone_map = cls._coerce_mapping(slide_authority.get("zone_map"))
        composition_logic = cls._coerce_mapping(slide_authority.get("composition_logic"))
        visual_craft = cls._coerce_mapping(slide_authority.get("visual_craft"))
        subject_semantics = cls._coerce_mapping(slide_authority.get("subject_semantics"))
        editorial_dna = cls._coerce_mapping(slide_authority.get("editorial_dna"))

        slide_descriptor = []
        if reference_index is not None:
            slide_descriptor.append(f"slide {reference_index}")
        if slide_count:
            slide_descriptor.append(f"of {slide_count}")
        if story_role:
            slide_descriptor.append(f"role {story_role}")
        if slide_descriptor:
            sections.append(
                f"Current sample slide authority: {' '.join(slide_descriptor)}."
            )
        if headline_hint:
            sections.append(
                f"Current sample slide headline intent: {headline_hint}."
            )
        if structural_cues:
            sections.append(
                f"Current sample slide structural cues: {', '.join(structural_cues)}."
            )
        if sequence_summary and not for_visual_only:
            sections.append(
                f"Current sample slide explanation summary: {sequence_summary}."
            )
        layout_type = cls._normalize_metadata_text(zone_map.get("layout_type"), limit=64)
        if layout_type:
            sections.append(
                f"Current sample slide layout type: {layout_type}. Keep the image region, text-safe spacing, and composition balance consistent with this reference logic."
            )
        composition_summary = cls._sequence_pack_mapping_summary(
            composition_logic,
            scalar_keys=("balance", "framing", "layering"),
            list_keys=("depth_cues", "focal_path"),
            limit=6,
            text_limit=180,
        )
        if composition_summary:
            sections.append(f"Current sample slide composition cues: {composition_summary}.")
        visual_craft_summary = cls._sequence_pack_mapping_summary(
            visual_craft,
            scalar_keys=("depth_style", "rendering_style", "lighting", "polish_level"),
            list_keys=("material_cues", "dimensionality_cues"),
            limit=6,
            text_limit=180,
        )
        if visual_craft_summary:
            sections.append(f"Current sample slide craft cues: {visual_craft_summary}.")
        subject_summary = cls._sequence_pack_mapping_summary(
            subject_semantics,
            scalar_keys=("scene_type", "human_presence", "environment", "abstraction_level"),
            list_keys=("primary_subjects", "domain_cues", "financial_objects"),
            limit=7,
            text_limit=200,
        )
        if subject_summary:
            sections.append(f"Current sample slide subject cues: {subject_summary}.")
        editorial_summary = cls._sequence_pack_mapping_summary(
            editorial_dna,
            scalar_keys=("storytelling_mode", "copy_density", "closing_style"),
            list_keys=("story_arc_roles", "headline_patterns", "supporting_patterns", "editorial_signals", "explanation_styles"),
            limit=7,
            text_limit=200,
        )
        if editorial_summary:
            sections.append(f"Current sample slide editorial cues: {editorial_summary}.")
        return sections

    @classmethod
    def _sample_visual_alignment_sections(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        for_carousel: bool,
    ) -> list[str]:
        # Centralizes sample visual alignment sections from compiled context and for carousel for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        context = dict(compiled_context or {})
        template_fit = cls._coerce_mapping(context.get("template_fit_brief"))
        brand_visual = cls._coerce_mapping(context.get("brand_visual_brief"))
        design_system = cls._coerce_mapping(brand_visual.get("design_system"))
        sequence_pack = cls._prompt_sequence_pack(compiled_context)
        layout_dna = cls._coerce_mapping(
            template_fit.get("template_layout_dna")
            or design_system.get("template_layout_dna")
            or design_system.get("layout_dna")
        )
        composition_logic = cls._coerce_mapping(
            template_fit.get("template_composition_logic")
            or design_system.get("template_composition_logic")
            or design_system.get("composition_logic")
        )
        visual_craft = cls._coerce_mapping(
            template_fit.get("template_visual_craft")
            or design_system.get("template_visual_craft")
            or design_system.get("visual_craft")
        )
        subject_semantics = cls._coerce_mapping(
            template_fit.get("template_subject_semantics")
            or design_system.get("template_subject_semantics")
            or design_system.get("subject_semantics")
        )
        editorial_dna = cls._coerce_mapping(
            template_fit.get("template_editorial_dna")
            or design_system.get("template_editorial_dna")
        )

        def _joined_terms(*values: Any, limit: int = 6, text_limit: int = 180) -> str:
            # Joins joined terms from limit and text limit for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            collected: list[str] = []
            seen: set[str] = set()
            for value in values:
                for item in cls._normalize_metadata_list(value, limit=limit):
                    key = item.casefold()
                    if key in seen:
                        continue
                    seen.add(key)
                    collected.append(item)
                    if len(collected) >= limit:
                        break
                if len(collected) >= limit:
                    break
            return cls._normalize_metadata_text(", ".join(collected), limit=text_limit)

        active_authority = cls._normalize_metadata_text(
            template_fit.get("template_name")
            or sequence_pack.get("family_name")
            or design_system.get("dominant_layout_family"),
            limit=96,
        )
        zone_roles = cls._normalize_metadata_list(
            template_fit.get("template_zone_roles")
            or design_system.get("preferred_zone_roles"),
            limit=6,
        )
        hierarchy_summary = cls._normalize_metadata_text(
            design_system.get("hierarchy_summary"),
            limit=180,
        )
        content_structure_summary = cls._normalize_metadata_text(
            design_system.get("content_structure_summary"),
            limit=180,
        )
        visual_craft_summary = cls._normalize_metadata_text(
            design_system.get("visual_craft_summary")
            or _joined_terms(
                visual_craft.get("depth_styles") or visual_craft.get("depth_style"),
                visual_craft.get("rendering_styles") or visual_craft.get("rendering_style"),
                visual_craft.get("lighting_modes") or visual_craft.get("lighting"),
                visual_craft.get("polish_levels") or visual_craft.get("polish_level"),
                visual_craft.get("material_cues"),
                visual_craft.get("dimensionality_cues"),
                limit=7,
            ),
            limit=200,
        )
        composition_summary = cls._normalize_metadata_text(
            design_system.get("composition_logic_summary")
            or _joined_terms(
                composition_logic.get("balances") or composition_logic.get("balance"),
                composition_logic.get("framings") or composition_logic.get("framing"),
                composition_logic.get("layerings") or composition_logic.get("layering"),
                composition_logic.get("depth_cues"),
                composition_logic.get("focal_path"),
            ),
            limit=200,
        )
        subject_summary = cls._normalize_metadata_text(
            design_system.get("subject_semantics_summary")
            or _joined_terms(
                subject_semantics.get("scene_types") or subject_semantics.get("scene_type"),
                subject_semantics.get("primary_subjects"),
                subject_semantics.get("financial_objects"),
                subject_semantics.get("domain_cues"),
                subject_semantics.get("abstraction_levels") or subject_semantics.get("abstraction_level"),
                subject_semantics.get("environment"),
                limit=7,
            ),
            limit=200,
        )
        editorial_summary = cls._normalize_metadata_text(
            design_system.get("editorial_story_arc_summary")
            or design_system.get("editorial_style_summary")
            or _joined_terms(
                editorial_dna.get("story_arc_roles"),
                editorial_dna.get("headline_patterns"),
                editorial_dna.get("supporting_patterns"),
                editorial_dna.get("explanation_styles"),
                editorial_dna.get("storytelling_mode"),
                editorial_dna.get("editorial_signals"),
                editorial_dna.get("closing_style"),
                limit=6,
            ),
            limit=200,
        )
        layout_type = cls._normalize_metadata_text(layout_dna.get("layout_type"), limit=64)
        canvas_ratio = cls._normalize_metadata_text(layout_dna.get("canvas_ratio"), limit=32)

        sections: list[str] = []
        sections.append(
            (
                "Anti-generic rule: do not reduce this slide to a basic hero-left/text-right finance post, a flat 2D vector explainer, a smiling office-worker poster, or an arrow-and-bar-chart composition unless the approved sample explicitly uses that language."
                if for_carousel
                else "Anti-generic rule: do not default to flat 2D vector finance explainers, smiling office workers, isolated coin or plant metaphors, dashboard-tile collages, or rising-arrow hero scenes unless the approved sample explicitly uses that language."
            )
        )
        if active_authority:
            sections.append(
                f"Active sample/template authority: {active_authority}. Match its editorial hierarchy, spacing rhythm, and image-to-text balance instead of inventing a new generic finance layout."
            )
        if layout_type or canvas_ratio:
            descriptor = " ".join(part for part in [layout_type, canvas_ratio] if part).strip()
            sections.append(
                f"Sample layout DNA: {descriptor}. Preserve this composition archetype rather than reverting to a flat generic explainer layout."
            )
        if zone_roles:
            sanitized_roles = [r if r != "logo" else "corner_safe_zone" for r in zone_roles]
            sections.append(
                f"Preferred zone-role rhythm from the selected sample: {', '.join(sanitized_roles)}. Keep these role neighborhoods distinct and do not collapse the slide into one hero-plus-text poster block."
            )
        if hierarchy_summary:
            sections.append(
                f"Sample hierarchy cues: {hierarchy_summary}. Keep the same focal emphasis and whitespace discipline."
            )
        if content_structure_summary:
            sections.append(
                f"Sample content-density cues: {content_structure_summary}. Preserve the same editorial pacing instead of overfilling the canvas."
            )
        if visual_craft_summary:
            sections.append(
                f"Sample visual-craft cues: {visual_craft_summary}. Favor dimensional, premium, editorially polished imagery with controlled depth and material detail when the sample allows it; do not fall back to flat low-detail vector treatment unless the sample is clearly minimal by design."
            )
        if composition_summary:
            sections.append(
                f"Sample composition cues: {composition_summary}. Preserve framing, layering, and negative-space behavior instead of centering one generic finance icon or illustration."
            )
        if subject_summary:
            sections.append(
                f"Sample subject cues: {subject_summary}. Choose topic-specific scenes and objects from this vocabulary instead of generic office workers, random coins, abstract arrows, or dashboard stickers."
            )
        if editorial_summary:
            sections.append(
                f"Sample editorial rhythm: {editorial_summary}. Avoid repeating the same slide composition or generic social-poster formula."
            )
        return [section for section in sections if section]

    @classmethod
    def _compiled_visual_style_policy(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        format_name: str,
    ) -> dict[str, Any]:
        # Derives compiled visual style policy from compiled context and format name for AIOrchestratorService.
        # It calls _coerce_mapping to turn raw evidence into the structured signal the caller needs.
        context = dict(compiled_context or {})
        brand_visual = cls._coerce_mapping(context.get("brand_visual_brief"))
        design_system = cls._coerce_mapping(brand_visual.get("design_system"))
        format_profiles = brand_visual.get("format_visual_style_profiles")
        if not isinstance(format_profiles, dict):
            format_profiles = (
                design_system.get("format_visual_style_profiles")
                if isinstance(design_system.get("format_visual_style_profiles"), dict)
                else {}
            )
        if isinstance(format_profiles, dict):
            format_policy = cls._coerce_mapping(format_profiles.get(str(format_name or "").strip().lower()))
            if format_policy:
                return format_policy
        policy = cls._coerce_mapping(brand_visual.get("visual_style_policy"))
        if policy:
            return policy
        return cls._coerce_mapping(design_system.get("visual_style_policy"))

    @classmethod
    def _compiled_reference_visual_profiles(
        cls,
        compiled_context: dict[str, Any] | None,
    ) -> list[dict[str, Any]]:
        # Centralizes compiled reference visual profiles from compiled context for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        context = dict(compiled_context or {})
        brand_visual = cls._coerce_mapping(context.get("brand_visual_brief"))
        raw_profiles = brand_visual.get("reference_visual_profiles")
        if not isinstance(raw_profiles, list):
            raw_profiles = []
        return [cls._coerce_mapping(item) for item in raw_profiles if isinstance(item, dict)]

    @classmethod
    def _select_reference_visual_profile(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        reference_assets: list[dict[str, Any]] | None = None,
        slide: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Selects reference visual profile from compiled context, reference assets, and slide for AIOrchestratorService.
        # It reuses _compiled_reference_visual_profiles and _normalize_metadata_text so related checks follow the same rule.
        profiles = cls._compiled_reference_visual_profiles(compiled_context)
        if not profiles:
            return {}
        reference_ids = {
            cls._normalize_metadata_text(item.get("asset_id"), limit=96).casefold()
            for item in (reference_assets or [])
            if isinstance(item, dict) and cls._normalize_metadata_text(item.get("asset_id"), limit=96)
        }
        if reference_ids:
            for profile in profiles:
                asset_id = cls._normalize_metadata_text(profile.get("asset_id"), limit=96).casefold()
                if asset_id and asset_id in reference_ids:
                    return profile
        slide_metadata = slide.get("metadata") if isinstance(slide, dict) and isinstance(slide.get("metadata"), dict) else {}
        story_role = cls._normalize_metadata_text(
            (slide_metadata.get("story_role") if isinstance(slide_metadata, dict) else None)
            or (slide.get("role") if isinstance(slide, dict) else None),
            limit=48,
        ).casefold()
        if story_role:
            for profile in profiles:
                profile_role = cls._normalize_metadata_text(profile.get("story_visual_role"), limit=48).casefold()
                if profile_role and profile_role == story_role:
                    return profile
        if len(profiles) == 1:
            return profiles[0]
        return {}

    @classmethod
    def _visual_style_evidence_summary(
        cls,
        mapping: dict[str, Any] | None,
        *,
        policy_mode: bool,
    ) -> str:
        # Derives visual style evidence summary from mapping and policy mode for AIOrchestratorService.
        # It calls _coerce_mapping and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        source = cls._coerce_mapping(mapping)
        if not source:
            return ""
        keys = {
            "image mode": "dominant_image_mode" if policy_mode else "image_mode",
            "depth mode": "dominant_depth_mode" if policy_mode else "depth_mode",
            "rendering mode": "dominant_rendering_mode" if policy_mode else "rendering_mode",
            "subject mode": "dominant_subject_mode" if policy_mode else "subject_mode",
            "support mode": "dominant_support_mode" if policy_mode else "support_mode",
        }
        parts: list[str] = []
        for label, key in keys.items():
            value = cls._normalize_metadata_text(source.get(key), limit=32)
            if value:
                parts.append(f"{label} {value}")
        if policy_mode:
            style_consistency = cls._normalize_metadata_text(source.get("style_consistency"), limit=24)
            three_d_usage = cls._normalize_metadata_text(source.get("three_d_usage"), limit=24)
            if style_consistency:
                parts.append(f"style consistency {style_consistency}")
            if three_d_usage:
                parts.append(f"3D usage {three_d_usage}")
        else:
            story_role = cls._normalize_metadata_text(source.get("story_visual_role"), limit=32)
            if story_role:
                parts.append(f"story role {story_role}")
        return "; ".join(parts)

    @classmethod
    def _visual_style_policy_prompt_sections(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        format_name: str,
        reference_assets: list[dict[str, Any]] | None = None,
        slide: dict[str, Any] | None = None,
    ) -> list[str]:
        # Derives visual style policy prompt sections from compiled context, format name, and reference assets for AIOrchestratorService.
        # It calls _compiled_visual_style_policy and _select_reference_visual_profile to turn raw evidence into the structured signal the caller needs.
        policy = cls._compiled_visual_style_policy(compiled_context, format_name=format_name)
        if not policy:
            return []
        selected_reference_profile = cls._select_reference_visual_profile(
            compiled_context,
            reference_assets=reference_assets,
            slide=slide,
        )
        reference_priority = cls._normalize_metadata_text(
            policy.get("reference_pattern_priority"),
            limit=32,
        ).casefold()
        dominant_image_mode = cls._normalize_metadata_text(policy.get("dominant_image_mode"), limit=24).casefold()
        dominant_depth_mode = cls._normalize_metadata_text(policy.get("dominant_depth_mode"), limit=24).casefold()
        dominant_rendering_mode = cls._normalize_metadata_text(policy.get("dominant_rendering_mode"), limit=24).casefold()
        style_consistency = cls._normalize_metadata_text(policy.get("style_consistency"), limit=24).casefold()
        three_d_usage = cls._normalize_metadata_text(policy.get("three_d_usage"), limit=24).casefold()
        selected_image_mode = cls._normalize_metadata_text(selected_reference_profile.get("image_mode"), limit=24).casefold()
        selected_depth_mode = cls._normalize_metadata_text(selected_reference_profile.get("depth_mode"), limit=24).casefold()
        selected_rendering_mode = cls._normalize_metadata_text(selected_reference_profile.get("rendering_mode"), limit=24).casefold()
        selected_is_3d = any(
            value in {"3d", "true_3d", "3d_render"}
            for value in [selected_image_mode, selected_depth_mode, selected_rendering_mode]
        )
        sections = [
            "Dynamic visual-style policy: follow the observed brand image language from analyzed carousel/reference samples instead of defaulting to generic 3D, generic flat vector, or unrelated stock styles.",
        ]
        policy_summary = cls._visual_style_evidence_summary(policy, policy_mode=True)
        if policy_summary:
            sections.append(f"Observed brand style evidence: {policy_summary}.")
        if reference_priority == "brand_dominant" and style_consistency in {"strong", "moderate"}:
            sections.append(
                "Brand-dominant execution: stay inside this dominant image family across the generated visual unless the selected reference evidence for this run clearly points elsewhere."
            )
        elif reference_priority == "reference_specific":
            if selected_reference_profile:
                sections.append(
                    "Reference-specific override: this brand uses multiple visual families, so follow the selected reference image pattern for image language, depth, and rendering instead of averaging across all samples."
                )
            else:
                sections.append(
                    "Mixed-style brand rule: this brand uses multiple visual families, so do not hardcode one image treatment; resolve the final image language from the selected reference evidence when available."
                )
        selected_summary = cls._visual_style_evidence_summary(selected_reference_profile, policy_mode=False)
        if selected_summary:
            sections.append(f"Selected reference visual profile: {selected_summary}.")
        if selected_reference_profile:
            if selected_is_3d:
                sections.append(
                    "Selected-reference execution: keep this run inside the chosen dimensional family and do not flatten it into a generic photo-led or flat vector scene."
                )
            else:
                sections.append(
                    "Selected-reference execution: keep this run inside the chosen photo-led or flatter family and do not force 3D or isometric rendering."
                )
        elif three_d_usage == "none":
            sections.append(
                "3D restraint: do not force 3D, isometric, or rendered object scenes when the analyzed brand/reference samples are primarily photo-led or flat."
            )
        elif three_d_usage == "often" or any(
            value in {"3d", "true_3d", "3d_render"}
            for value in [dominant_image_mode, dominant_depth_mode, dominant_rendering_mode]
        ):
            sections.append(
                "3D execution: the analyzed brand/reference samples regularly use true 3D or dimensional rendering, so preserve that depth language instead of flattening the scene into generic 2D artwork."
            )
        elif three_d_usage == "sometimes":
            sections.append(
                "Conditional 3D rule: use 3D or dimensional rendering only when the selected reference or slide-specific evidence points there; do not force it onto every slide or brand."
            )
        if not selected_reference_profile and dominant_depth_mode == "flat" and dominant_rendering_mode in {"photo", "vector"}:
            sections.append(
                "Flat-style guardrail: preserve the brand's flatter or photo-led image treatment and do not inject artificial pseudo-3D depth unless the selected reference evidence clearly supports it."
            )
        return [section for section in sections if section]

    @classmethod
    def _should_ignore_scene_graph_for_final_render(
        cls,
        *,
        generation_path: str,
        fresh_replan_attempted: bool,
        validation_report: SceneGraphValidationReport,
        scene_graph: GenerationSceneGraph,
        compiled_context: dict[str, Any] | None = None,
        style_reference_final_render_active: bool = False,
    ) -> bool:
        # Checks ignore scene graph final from generation path, fresh replan attempted, and validation report for AIOrchestratorService.
        # It reuses _should_request_fresh_replan and _scene_graph_has_authoritative_geometry so related checks follow the same rule.
        if generation_path == "image_led_social" and style_reference_final_render_active:
            return True
        return (
            generation_path == "image_led_social"
            and fresh_replan_attempted
            and cls._should_request_fresh_replan(validation_report)
            and not cls._scene_graph_has_authoritative_geometry(scene_graph)
            and not cls._compiled_context_has_authoritative_layout(compiled_context)
        )

    @staticmethod
    def _final_render_ignore_note(validation_report: SceneGraphValidationReport) -> str:
        # Centralizes final ignore note from validation report for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        issues = [str(issue.rule_id or "").strip() for issue in validation_report.issues if str(issue.rule_id or "").strip()]
        if not issues:
            return (
                "Ignore the prior weak scene graph because it lacked enough usable geometry. Rebuild the composition around the extracted sample layout skeleton, "
                "brand system, and scene-safe geometry contract instead of improvising a new composition."
            )
        return (
            "Ignore the prior weak scene graph because it lacked enough usable geometry. Rebuild the composition around the extracted sample layout skeleton, "
            "brand system, and scene-safe geometry contract instead of improvising a new composition. "
            f"Do not repeat these failed planning traits: {', '.join(issues)}."
        )

    @staticmethod
    def _coerce_mapping(value: Any) -> dict[str, Any]:
        # Coerces mapping from input value for AIOrchestratorService.
        # This keeps provider output from leaking inconsistent types into later steps.
        if isinstance(value, dict):
            return dict(value)
        if value is None:
            return {}
        if isinstance(value, (list, tuple, set)):
            merged: dict[str, Any] = {}
            text_items: list[str] = []
            for item in value:
                if isinstance(item, dict):
                    merged.update(item)
                else:
                    text = str(item).strip()
                    if text:
                        text_items.append(text)
            if merged:
                if text_items:
                    merged.setdefault("notes", "; ".join(text_items[:8]))
                return merged
            if text_items:
                return {"notes": "; ".join(text_items[:8])}
            return {}
        text = str(value).strip()
        return {"notes": text} if text else {}

    @staticmethod
    def _coerce_list(value: Any) -> list[Any]:
        # Coerces coerce list from input value for AIOrchestratorService.
        # This keeps provider output from leaking inconsistent types into later steps.
        if value is None:
            return []
        if isinstance(value, list):
            return value
        if isinstance(value, tuple):
            return list(value)
        if isinstance(value, set):
            return list(value)
        return [value]

    @staticmethod
    def _normalize_trace_payload(payload: Any) -> Any:
        # Normalizes trace from payload for AIOrchestratorService.
        # It delegates shared cleanup to _repair_common_mojibake before returning the cleaned value.
        if isinstance(payload, str):
            return AIOrchestratorService._repair_common_mojibake(payload)
        if isinstance(payload, dict):
            return {
                key: AIOrchestratorService._normalize_trace_payload(value)
                for key, value in payload.items()
            }
        if isinstance(payload, list):
            return [AIOrchestratorService._normalize_trace_payload(value) for value in payload]
        if isinstance(payload, tuple):
            return [AIOrchestratorService._normalize_trace_payload(value) for value in payload]
        return payload

    @staticmethod
    def _trace_payload(trace_id: str | None, tracer: GenerationTraceService, filename: str, payload: Any) -> None:
        # Builds trace from trace id, tracer, and filename for AIOrchestratorService.
        # It calls _normalize_trace_payload while assembling the payload or prompt text.
        tracer.write_payload(
            trace_id,
            filename,
            AIOrchestratorService._normalize_trace_payload(payload),
        )

    @staticmethod
    def _trace_event(trace_id: str | None, tracer: GenerationTraceService, event: str, payload: Any | None = None) -> None:
        # Centralizes trace event from trace id, tracer, and event for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        tracer.append_event(trace_id, event, payload)

    @classmethod
    def _compiled_font_families(cls, compiled_context: dict[str, Any]) -> list[str]:
        # Derives compiled font families from compiled context for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        families: list[str] = []
        for item in (compiled_context.get("brand_visual_brief", {}) or {}).get("font_families", []) or []:
            normalized = cls._normalize_metadata_text(item, limit=64)
            if normalized and normalized not in families:
                families.append(normalized)
        return families

    @classmethod
    def _extract_font_sizes_from_brand_context(cls, brand_context: dict[str, Any]) -> dict[str, int]:
        # Extracts font sizes brand context from brand context for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        """Extract font sizes from analyzed reference samples (typography_dna)"""
        # Default font sizes (fallback if not extracted)
        defaults = {
            "headline": 58,
            "subheading": 36,
            "body": 24,
            "caption": 16,
            "footer": 8
        }

        # Try to extract from reference creatives' typography_dna
        visual_identity = brand_context.get("visual_identity", {})
        reference_creatives = visual_identity.get("reference_creatives", [])

        for reference in reference_creatives:
            style_chars = reference.get("style_characteristics", {})
            typography_dna = style_chars.get("typography_dna", {})
            font_size_palette = typography_dna.get("font_size_palette", {})

            if font_size_palette:
                # Found font size palette from vision analysis!
                return {
                    "headline": int(font_size_palette.get("headline_pt", defaults["headline"])),
                    "subheading": int(font_size_palette.get("subheading_pt", defaults["subheading"])),
                    "body": int(font_size_palette.get("body_pt", defaults["body"])),
                    "caption": int(font_size_palette.get("caption_pt", defaults["caption"])),
                    "footer": int(font_size_palette.get("footer_pt", defaults["footer"]))
                }

        # Fallback to defaults if not found in references
        return defaults

    @classmethod
    def _extract_layout_dna_from_brand_context(cls, brand_context: dict[str, Any], format_hint: str) -> dict[str, Any] | None:
        # Extracts layout dna brand context from brand context and format hint for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        """Extract layout DNA from reference creatives"""
        visual_identity = brand_context.get("visual_identity", {})
        reference_creatives = visual_identity.get("reference_creatives", [])

        for reference in reference_creatives:
            layout_structure = reference.get("layout_structure", {})
            dna = layout_structure.get("layout_dna")
            if dna and dna.get("zones"):
                return dna

        return None

    @classmethod
    def _extract_layout_dna_from_compiled_context(cls, compiled_context: dict[str, Any], format_hint: str) -> dict[str, Any] | None:
        # Extracts layout dna compiled context from compiled context and format hint for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        brand_visual_brief = compiled_context.get("brand_visual_brief", {}) if isinstance(compiled_context, dict) else {}
        template_fit_brief = compiled_context.get("template_fit_brief", {}) if isinstance(compiled_context, dict) else {}
        candidates = [
            (template_fit_brief.get("template_layout_dna") if isinstance(template_fit_brief, dict) else {}),
            (brand_visual_brief.get("template_layout_dna") if isinstance(brand_visual_brief, dict) else {}),
            (
                ((brand_visual_brief.get("design_system") or {}).get("template_layout_dna"))
                if isinstance((brand_visual_brief.get("design_system") if isinstance(brand_visual_brief, dict) else {}), dict)
                else {}
            ),
        ]
        for candidate in candidates:
            if isinstance(candidate, dict) and candidate.get("zones"):
                return candidate
        return None

    @classmethod
    def _build_profile_from_layout_dna(cls, layout_dna: dict[str, Any], layout_type: str, format_name: str) -> dict[str, Any]:
        # Builds profile layout dna from layout dna, layout type, and format name for AIOrchestratorService.
        # It calls _normalized_box and _sanitize_scene_element_name while assembling the payload or prompt text.
        """Build layout profile from extracted layout DNA"""
        raw_zones = layout_dna.get("zones", {})
        spacing = layout_dna.get("spacing", {})
        zones: dict[str, dict[str, Any]] = {}

        if isinstance(raw_zones, dict):
            for role_name, zone in raw_zones.items():
                if not isinstance(zone, dict):
                    continue
                normalized = zone.get("normalized") if isinstance(zone.get("normalized"), dict) else {}
                zone_copy = dict(zone)
                zone_copy.setdefault("role", str(role_name))
                if normalized:
                    zone_copy["normalized"] = normalized
                elif all(zone.get(key) is not None for key in ("x", "y", "w", "h")):
                    zone_copy["normalized"] = {
                        "x": zone.get("x"),
                        "y": zone.get("y"),
                        "w": zone.get("w"),
                        "h": zone.get("h"),
                    }
                zones[str(role_name)] = zone_copy
        elif isinstance(raw_zones, list):
            for zone in raw_zones:
                if not isinstance(zone, dict):
                    continue
                role_name = str(zone.get("role") or zone.get("zone_role") or "").strip()
                if not role_name:
                    continue
                normalized = zone.get("normalized") if isinstance(zone.get("normalized"), dict) else {}
                zone_copy = dict(zone)
                zone_copy.setdefault("role", role_name)
                if normalized:
                    zone_copy["normalized"] = normalized
                elif all(zone.get(key) is not None for key in ("x", "y", "w", "h")):
                    zone_copy["normalized"] = {
                        "x": zone.get("x"),
                        "y": zone.get("y"),
                        "w": zone.get("w"),
                        "h": zone.get("h"),
                    }
                elif all(zone.get(key) is not None for key in ("x", "y", "width", "height")):
                    zone_copy["normalized"] = {
                        "x": zone.get("x"),
                        "y": zone.get("y"),
                        "w": zone.get("width"),
                        "h": zone.get("height"),
                    }
                zones[role_name] = zone_copy

        profile = {
            "layout_type": layout_type,
            "layout_archetype": cls._sanitize_scene_element_name(
                str(layout_dna.get("layout_type") or "extracted_from_sample")
            ) or "extracted_from_sample",
        }

        # Map zone roles to profile keys
        role_mapping = {
            "headline": "headline",
            "supporting_line": "supporting_line",
            "body": "supporting_line",
            "proof_points": "proof_points",
            "stat_highlights": "proof_points",
            "image": "hero_image",
            "hero_visual": "hero_image",
            "hero_image": "hero_image",
            "primary_visual": "hero_image",
            "logo": "logo",
            "cta": "cta",
        }

        for zone_role, profile_key in role_mapping.items():
            zone = zones.get(zone_role)
            if zone:
                normalized = zone.get("normalized", {})
                profile[profile_key] = cls._normalized_box(
                    normalized.get("x", 0),
                    normalized.get("y", 0),
                    normalized.get("w", 1),
                    normalized.get("h", 0.1)
                )

        content_boxes: list[dict[str, float]] = []
        for role_name, zone in zones.items():
            if not isinstance(zone, dict):
                continue
            normalized = zone.get("normalized", {}) if isinstance(zone.get("normalized"), dict) else {}
            if role_name in {"image", "logo"}:
                continue
            if all(isinstance(normalized.get(key), (int, float)) for key in ("x", "y", "w", "h")):
                content_boxes.append(normalized)
        if content_boxes:
            min_x = min(float(box.get("x") or 0.0) for box in content_boxes)
            min_y = min(float(box.get("y") or 0.0) for box in content_boxes)
            max_x = max(float(box.get("x") or 0.0) + float(box.get("w") or 0.0) for box in content_boxes)
            max_y = max(float(box.get("y") or 0.0) + float(box.get("h") or 0.0) for box in content_boxes)
            pad_x = float(spacing.get("x_padding") or spacing.get("padding_x") or 0.03)
            pad_y = float(spacing.get("y_padding") or spacing.get("padding_y") or 0.03)
            profile["overlay_panel"] = cls._normalized_box(
                max(min_x - pad_x, 0.0),
                max(min_y - pad_y, 0.0),
                min(max_x - min_x + pad_x * 2, 0.96),
                min(max_y - min_y + pad_y * 2, 0.96),
            )

        # Add proof_points fallback if body zone exists (typically below supporting line)
        if "proof_points" not in profile and "body" in zones:
            body_zone = zones["body"]
            normalized = body_zone.get("normalized", {})
            y_start = normalized.get("y", 0) + normalized.get("h", 0) + 0.05
            profile["proof_points"] = cls._normalized_box(0.08, y_start, 0.84, 0.3)

        overlay_panel = profile.get("overlay_panel") if isinstance(profile.get("overlay_panel"), dict) else {}
        if overlay_panel:
            panel_x = float(overlay_panel.get("x") or 0.05)
            panel_y = float(overlay_panel.get("y") or 0.08)
            panel_w = float(overlay_panel.get("width") or 0.84)
            panel_h = float(overlay_panel.get("height") or 0.76)
        else:
            panel_x, panel_y, panel_w, panel_h = 0.05, 0.08, 0.84, 0.76

        if "headline" not in profile:
            profile["headline"] = cls._normalized_box(panel_x, panel_y, min(panel_w, 0.62), 0.12)
        headline_box = profile.get("headline") if isinstance(profile.get("headline"), dict) else cls._normalized_box(panel_x, panel_y, min(panel_w, 0.62), 0.12)
        headline_y_end = float(headline_box.get("y") or panel_y) + float(headline_box.get("height") or 0.12)

        if "supporting_line" not in profile:
            profile["supporting_line"] = cls._normalized_box(
                panel_x,
                min(headline_y_end + 0.04, 0.86),
                min(panel_w, 0.72),
                0.1,
            )
        supporting_box = profile.get("supporting_line") if isinstance(profile.get("supporting_line"), dict) else {}
        supporting_y_end = float(supporting_box.get("y") or headline_y_end) + float(supporting_box.get("height") or 0.1)

        if "proof_points" not in profile:
            profile["proof_points"] = cls._normalized_box(
                panel_x,
                min(supporting_y_end + 0.04, 0.88),
                min(panel_w, 0.78),
                min(max(panel_h - (supporting_y_end - panel_y) - 0.18, 0.12), 0.24),
            )
        if "cta" not in profile:
            proof_box = profile.get("proof_points") if isinstance(profile.get("proof_points"), dict) else {}
            proof_y_end = float(proof_box.get("y") or supporting_y_end) + float(proof_box.get("height") or 0.16)
            profile["cta"] = cls._normalized_box(
                panel_x,
                min(proof_y_end + 0.04, 0.9),
                min(panel_w * 0.45, 0.34),
                0.08,
            )
        if "logo" not in profile:
            profile["logo"] = cls._normalized_box(min(panel_x + panel_w - 0.18, 0.78), max(panel_y - 0.04, 0.04), 0.16, 0.06)
        if "hero_image" not in profile:
            profile["hero_image"] = cls._normalized_box(min(panel_x + panel_w + 0.03, 0.56), 0.12, 0.32, 0.34)
        if "overlay_panel" not in profile:
            profile["overlay_panel"] = cls._normalized_box(panel_x, panel_y, panel_w, panel_h)

        return profile

    @staticmethod
    def _default_font_role_for_role(role: str) -> str:
        # Derives default font role role from role for AIOrchestratorService.
        # The derived signal gives the LLM explicit evidence instead of asking it to infer the same thing later.
        normalized = str(role or "").strip().lower()
        if normalized == "headline":
            return "heading_sans"
        if normalized == "cta":
            return "cta_sans"
        if normalized in {"footer", "legal"}:
            return "caption_sans"
        return "body_sans"

    @classmethod
    def _normalize_text_style_for_brand(
        cls,
        style: dict[str, Any],
        *,
        role: str,
        allowed_font_families: list[str],
        validation_hints: dict[str, Any],
    ) -> dict[str, Any]:
        # Normalizes text style brand from style, role, and allowed font families for AIOrchestratorService.
        # It delegates shared cleanup to _default_font_role_for_role before returning the cleaned value.
        normalized = dict(style or {})
        requested_family = str(
            normalized.get("font_family")
            or normalized.get("family")
            or normalized.get("fontFamily")
            or ""
        ).strip()
        normalized["font_role"] = str(normalized.get("font_role") or cls._default_font_role_for_role(role)).strip()
        if not requested_family:
            normalized.pop("font_family", None)
            return normalized
        validation_hints.setdefault("font_family_requested", requested_family)
        matched = next(
            (family for family in allowed_font_families if family.casefold() == requested_family.casefold()),
            None,
        )
        if matched:
            normalized["font_family"] = matched
            validation_hints["font_family_resolved"] = matched
            return normalized
        if allowed_font_families:
            normalized["font_family"] = allowed_font_families[0]
            validation_hints["font_family_resolved"] = allowed_font_families[0]
            validation_hints["font_family_normalized_from"] = requested_family
            return normalized
        normalized.pop("font_family", None)
        validation_hints["font_family_strategy"] = "renderer_fallback"
        return normalized

    @staticmethod
    def _major_asset_strategy_flags(asset_strategy: dict[str, Any]) -> dict[str, bool]:
        # Extracts major asset strategy flags from asset strategy for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        strategy = asset_strategy or {}
        return {
            "generated_image": bool(strategy.get("use_generated_image")),
            "template_background": bool(strategy.get("use_template_background")),
            "reference_assets": bool(strategy.get("use_brand_reference_assets")),
            "icon_sequence": bool(strategy.get("icon_sequence")),
        }

    @staticmethod
    def _normalized_dominant_visual_system(asset_strategy: dict[str, Any]) -> str:
        # Normalizes dominant visual system from asset strategy for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        value = str((asset_strategy or {}).get("dominant_visual_system") or "").strip().lower()
        aliases = {
            "image_led": "generated_image",
            "generated_image": "generated_image",
            "hero_image": "generated_image",
            "template_led": "template_background",
            "exact_template": "template_background",
            "adapted_template": "template_background",
            "template_background": "template_background",
            "asset_led": "reference_assets",
            "reference_led": "reference_assets",
            "reference_assets": "reference_assets",
            "icon_led": "icon_sequence",
            "icon_sequence": "icon_sequence",
            "type_led": "type_led",
        }
        return aliases.get(value, value or "")

    @staticmethod
    def _normalized_supporting_visual_system(asset_strategy: dict[str, Any]) -> str:
        # Normalizes supporting visual system from asset strategy for AIOrchestratorService.
        # Later prompt and metadata code can compare the cleaned value directly.
        value = str(
            (asset_strategy or {}).get("supporting_visual_system")
            or (asset_strategy or {}).get("type_led_supporting_system")
            or ""
        ).strip().lower()
        aliases = {
            "iconography": "icon_sequence",
            "icons": "icon_sequence",
            "icon_led": "icon_sequence",
            "icon_sequence": "icon_sequence",
            "reference_assets": "reference_assets",
            "reference_led": "reference_assets",
            "asset_led": "reference_assets",
        }
        return aliases.get(value, value or "")

    @staticmethod
    def _visual_explanation_plan(
        request: AIOrchestrationRequest,
        text_payload: Any,
        creative_decision: CreativeDecisionPayload | None = None,
        reference_images: list[dict[str, Any]] | None = None,
        message_strategy: MessageStrategyPayload | None = None,
    ) -> dict[str, str]:
        # Builds visual explanation plan from request state, copy payload, and creative decision for AIOrchestratorService.
        # It calls _normalize_metadata_list and _normalized_supporting_visual_system while assembling the payload or prompt text.
        metadata = getattr(text_payload, "metadata", None)
        metadata = metadata if isinstance(metadata, dict) else {}
        strategy_payload = message_strategy.model_dump(mode="json") if isinstance(message_strategy, MessageStrategyPayload) else {}
        asset_strategy = (creative_decision.asset_strategy if isinstance(creative_decision, CreativeDecisionPayload) else {}) or {}
        format_name = str((getattr(request, "studio_panel", {}) or {}).get("format") or "static").strip().lower()
        text = " ".join(
            AIOrchestratorService._coerce_text_value(part)
            for part in [
                getattr(request, "prompt", ""),
                getattr(text_payload, "headline", ""),
                getattr(text_payload, "body", ""),
                getattr(text_payload, "cta", ""),
                metadata.get("supporting_line"),
                metadata.get("visual_direction"),
                metadata.get("image_prompt"),
                metadata.get("proof_points"),
                metadata.get("stat_highlights"),
                strategy_payload.get("primary_campaign_theme"),
                strategy_payload.get("core_audience_message"),
                strategy_payload.get("important_keywords"),
            ]
            if AIOrchestratorService._coerce_text_value(part)
        ).casefold()
        proof_points = AIOrchestratorService._normalize_metadata_list(metadata.get("proof_points"), limit=6)
        supporting_visual_system = AIOrchestratorService._normalized_supporting_visual_system(asset_strategy)
        has_reference_images = bool(reference_images)
        user_explicit_data_visual_request = AIOrchestratorService._has_explicit_data_visual_request(
            getattr(request, "prompt", ""),
            text_payload=None,
        )
        explicit_data_visual_request = AIOrchestratorService._has_explicit_data_visual_request(
            getattr(request, "prompt", ""),
            text_payload=text_payload,
        )
        template_surface_policy = AIOrchestratorService._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata,
        )

        beginner_terms = (
            "first-time",
            "first time",
            "beginner",
            "beginners",
            "new investor",
            "new investors",
            "approachable",
            "easy start",
            "getting started",
            "start investing",
            "guided",
            "onboarding",
            "entry point",
            "entry-level",
            "simple access",
            "market is approachable",
            "free market is approachable",
        )
        process_terms = (
            "tips",
            "checklist",
            "steps",
            "step-by-step",
            "how to",
            "strategy",
            "strategies",
            "playbook",
            "roadmap",
            "journey",
            "process",
            "guide",
        )
        comparison_terms = (
            "compare",
            "comparison",
            "versus",
            " vs ",
            "before and after",
            "before/after",
            "confusion to clarity",
            "risk return",
            "risk-return",
            "risk and return",
            "old vs new",
            "trade-off",
            "tradeoff",
        )
        data_terms = (
            "market trend",
            "market trends",
            "trend",
            "trends",
            "graph",
            "chart",
            "data",
            "metric",
            "metrics",
            "percentage",
            "%",
        )

        def has_term(term: str) -> bool:
            # Checks term from term for AIOrchestratorService.
            # The boolean result controls the nearby policy or validation branch.
            normalized = term.strip().casefold()
            if not normalized:
                return False
            if normalized == "%":
                return "%" in text
            return bool(re.search(rf"(?<![a-z0-9]){re.escape(normalized)}(?![a-z0-9])", text))

        def has_any(terms: tuple[str, ...]) -> bool:
            # Checks any from terms for AIOrchestratorService.
            # This keeps the allowed/blocked rule in one place.
            return any(has_term(term) for term in terms)

        if (
            template_surface_policy == "style_reference_only"
            and has_reference_images
            and not user_explicit_data_visual_request
        ):
            return {
                "mode": "sample_page_adaptation",
                "need": "high",
                "density": "match_sample",
                "rationale": "Selected sample page controls visual structure; do not infer charts or generic finance modules from numeric facts alone.",
            }
        if has_any(beginner_terms):
            return {
                "mode": "beginner_path",
                "need": "high",
                "density": "medium",
                "rationale": "Prompt asks for first-time, approachable, or guided-entry understanding.",
            }
        if has_any(comparison_terms):
            return {
                "mode": "comparison",
                "need": "medium",
                "density": "low" if format_name == "static" else "medium",
                "rationale": "Message benefits from a clear contrast or before-after transformation.",
            }
        if explicit_data_visual_request or has_any(data_terms):
            return {
                "mode": "data_cue",
                "need": "high" if format_name == "infographic" else "medium",
                "density": "medium" if format_name != "infographic" else "high",
                "rationale": "Message explicitly asks for chart-worthy information, numeric visualization, or diagrammatic evidence.",
            }
        if has_any(process_terms):
            return {
                "mode": "process_steps",
                "need": "high",
                "density": "medium",
                "rationale": "Message is instructional and should read visually as a simple sequence.",
            }
        if format_name in {"carousel", "infographic"} or supporting_visual_system == "icon_sequence":
            return {
                "mode": "icon_support",
                "need": "medium",
                "density": "low" if format_name == "static" else "medium",
                "rationale": "Format or proof points can benefit from restrained supporting symbols without a full diagram.",
            }
        if has_reference_images:
            return {
                "mode": "minimal_brand_scene",
                "need": "low",
                "density": "low",
                "rationale": "Reference assets can provide style while the message does not require extra explanation.",
            }
        return {
            "mode": "minimal_brand_scene",
            "need": "low",
            "density": "low",
            "rationale": "Message can be served by a clean brand scene without forced explanatory devices.",
        }

    @staticmethod
    def _has_explicit_data_visual_request(
        prompt: str,
        *,
        text_payload: StructuredTextPayload | None = None,
    ) -> bool:
        # Checks explicit visual request from prompt text and copy payload for AIOrchestratorService.
        # It reuses _coerce_text_value so related checks follow the same rule.
        metadata = (text_payload.metadata or {}) if isinstance(text_payload, StructuredTextPayload) else {}
        combined = " ".join(
            part
            for part in [
                AIOrchestratorService._coerce_text_value(prompt),
                AIOrchestratorService._coerce_text_value(getattr(text_payload, "headline", "") if text_payload else ""),
                AIOrchestratorService._coerce_text_value(getattr(text_payload, "body", "") if text_payload else ""),
                AIOrchestratorService._coerce_text_value(metadata.get("supporting_line")),
                AIOrchestratorService._coerce_text_value(metadata.get("proof_points")),
                AIOrchestratorService._coerce_text_value(metadata.get("stat_highlights")),
            ]
            if AIOrchestratorService._coerce_text_value(part)
        ).casefold()
        if not combined:
            return False
        explicit_visual_terms = (
            "bar chart",
            "bar graph",
            "line chart",
            "line graph",
            "pie chart",
            "donut chart",
            "area chart",
            "graph",
            "chart",
            "timeline",
            "diagram",
            "visualization",
            "data viz",
            "infographic",
            "table",
            "matrix",
            "checklist",
            "step-by-step",
            "steps",
            "process",
            "flow",
        )
        def contains_explicit_visual_term(term: str) -> bool:
            # Centralizes contains explicit visual term from term for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            normalized = term.strip().casefold()
            if not normalized:
                return False
            return bool(re.search(rf"(?<![a-z0-9]){re.escape(normalized)}(?![a-z0-9])", combined))

        if any(contains_explicit_visual_term(term) for term in explicit_visual_terms):
            return True
        if re.search(r"\b\d{4}\b", combined) and re.search(r"\b(chart|graph|trend|timeline|growth|decline|increase|decrease)\b", combined):
            return True
        if "%" in combined and re.search(r"\b(chart|graph|compare|comparison|trend|data|metric)\b", combined):
            return True
        return False

    @classmethod
    def _infographic_surface_intent(
        cls,
        *,
        prompt: Any,
        format_name: str,
        visual_plan: dict[str, Any] | None = None,
    ) -> bool:
        # Centralizes infographic surface intent from prompt text, format name, and visual plan for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if str(format_name or "").strip().casefold() != "infographic":
            return False
        text = cls._normalize_metadata_text(prompt, limit=720).casefold()
        plan = visual_plan if isinstance(visual_plan, dict) else {}
        mode = cls._normalize_metadata_text(plan.get("mode"), limit=40).casefold()
        if mode in {"comparison", "data_cue", "process_steps"}:
            return True
        if cls._has_explicit_data_visual_request(text, text_payload=None):
            return True
        return bool(
            re.search(
                r"\b(top\s+\d{1,2}|rank(?:ed|ing)?|compar(?:e|ing|ison)|versus|vs\.?|by\s+[a-z0-9 -]{2,80})\b",
                text,
                flags=re.IGNORECASE,
            )
        )

    @classmethod
    def _explicit_top_n_item_count(cls, *values: Any) -> int | None:
        # Counts explicit top n item count for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._normalize_metadata_text(" ".join(cls._coerce_text_value(value) for value in values), limit=900)
        if not text:
            return None
        match = re.search(r"\btop\s+(\d{1,2})\b", text, flags=re.IGNORECASE)
        if not match:
            return None
        try:
            value = int(match.group(1))
        except (TypeError, ValueError):
            return None
        return value if 1 <= value <= 20 else None

    @classmethod
    def _data_list_surface_available_count(cls, metadata: Any) -> int:
        # Counts surface available count from metadata for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not isinstance(metadata, dict):
            return 0
        counts = [
            cls._rough_collection_length(metadata.get("proof_points")),
            cls._rough_collection_length(metadata.get("stat_highlights")),
            cls._rough_collection_length(metadata.get("claim_evidence_pairs")),
        ]
        static_panel_spec = metadata.get("static_panel_spec")
        if isinstance(static_panel_spec, dict):
            counts.extend(
                [
                    cls._rough_collection_length(static_panel_spec.get("proof_points")),
                    cls._rough_collection_length(static_panel_spec.get("stat_highlights")),
                    cls._rough_collection_length(static_panel_spec.get("claim_evidence_pairs")),
                ]
            )
        infographic_sections = metadata.get("infographic_section_specs")
        if isinstance(infographic_sections, list):
            valid_sections = [section for section in infographic_sections if isinstance(section, dict)]
            counts.append(len(valid_sections))
            for section in valid_sections:
                counts.extend(
                    [
                        cls._rough_collection_length(section.get("body_points")),
                        cls._rough_collection_length(section.get("proof_points")),
                        cls._rough_collection_length(section.get("stat_highlights")),
                        cls._rough_collection_length(section.get("claim_evidence_pairs")),
                    ]
                )
        return max(counts or [0])

    @classmethod
    def _data_list_surface_intent(
        cls,
        *,
        prompt: Any,
        format_name: str,
        metadata: dict[str, Any] | None = None,
        text_payload: StructuredTextPayload | None = None,
        visual_plan: dict[str, Any] | None = None,
    ) -> bool:
        # Centralizes surface intent from prompt text, format name, and metadata for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized_format = str(format_name or "").strip().casefold()
        if normalized_format not in {"static", "infographic"}:
            return False
        metadata = metadata if isinstance(metadata, dict) else {}
        if cls._explicit_top_n_item_count(prompt, metadata):
            return True
        plan = visual_plan if isinstance(visual_plan, dict) else {}
        mode = cls._normalize_metadata_text(plan.get("mode"), limit=40).casefold()
        if mode in {"comparison", "data_cue", "process_steps"}:
            return True
        if cls._has_explicit_data_visual_request(str(prompt or ""), text_payload=text_payload):
            return True
        combined = " ".join(
            cls._coerce_text_value(value)
            for value in (
                prompt,
                metadata.get("panel_goal"),
                metadata.get("visual_focus"),
                metadata.get("proof_points"),
                metadata.get("stat_highlights"),
                metadata.get("claim_evidence_pairs"),
                metadata.get("static_panel_spec"),
                metadata.get("infographic_section_specs"),
            )
            if cls._coerce_text_value(value)
        ).casefold()
        if not combined:
            return False
        return bool(
            re.search(
                r"\b("
                r"rank(?:ed|ing)?|compar(?:e|ing|ison)|versus|vs\.?|"
                r"list(?:ing)?|checklist|scorecard|matrix|table|"
                r"rules?|requirements?|criteria|fees?|charges?|rates?|penalt(?:y|ies)|"
                r"key\s+(?:rules|points|factors|takeaways)|"
                r"options?|items?|categories|providers"
                r")\b",
                combined,
                flags=re.IGNORECASE,
            )
        )

    @classmethod
    def _data_list_surface_contract(
        cls,
        *,
        prompt: Any,
        format_name: str,
        metadata: dict[str, Any] | None = None,
        text_payload: StructuredTextPayload | None = None,
        visual_plan: dict[str, Any] | None = None,
    ) -> dict[str, Any] | None:
        # Builds surface contract from prompt text, format name, and metadata for AIOrchestratorService.
        # It calls _explicit_top_n_item_count and _data_list_surface_available_count while assembling the payload or prompt text.
        normalized_format = str(format_name or "").strip().casefold()
        if not cls._data_list_surface_intent(
            prompt=prompt,
            format_name=normalized_format,
            metadata=metadata,
            text_payload=text_payload,
            visual_plan=visual_plan,
        ):
            return None
        metadata = metadata if isinstance(metadata, dict) else {}
        top_n = cls._explicit_top_n_item_count(prompt, metadata)
        available_count = cls._data_list_surface_available_count(metadata)
        requested_count = top_n or max(available_count, 3)
        module_cap = 10 if normalized_format == "static" and top_n else 6 if normalized_format == "static" else 10
        module_count = max(1, min(module_cap, requested_count))
        intent = "top_n_list" if top_n else "comparison_or_list"
        ranking_surface = bool(
            top_n
            or re.search(
                r"\b(rank(?:ed|ing)?|table|scorecard|matrix|compar(?:e|ing|ison)|versus|vs\.?)\b",
                cls._coerce_text_value(prompt).casefold(),
            )
        )
        return {
            "intent": intent,
            "format": normalized_format,
            "surface_mode": "ranking_table" if ranking_surface and module_count >= 7 else "comparison_cards",
            "requested_count": requested_count,
            "available_count": available_count,
            "module_count": module_count,
        }

    @classmethod
    def _sanitize_infographic_surface_visual_metadata_fields(
        cls,
        metadata: dict[str, Any],
        *,
        request_prompt: Any,
        format_name: str,
    ) -> tuple[dict[str, Any], list[str]]:
        # Sanitizes infographic surface visual metadata fields from metadata, request prompt, and format name for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_text and _topic_anchor_keywords before returning the cleaned value.
        cleaned = dict(metadata or {})
        if not cleaned or not cls._infographic_surface_intent(
            prompt=request_prompt,
            format_name=format_name,
        ):
            return cleaned, []

        prompt_text = cls._normalize_metadata_text(request_prompt, limit=720)
        prompt_keywords = cls._topic_anchor_keywords(prompt_text, limit=14)
        prompt_allows_photo_scene = bool(
            re.search(
                r"\b(photo|photographic|realistic|portrait|lifestyle|image[- ]led|environment)\b",
                prompt_text,
                flags=re.IGNORECASE,
            )
        )
        removed_fields: list[str] = []
        for field, limit in cls.VISUAL_METADATA_FIELD_LIMITS.items():
            text = cls._normalize_metadata_text(cleaned.get(field), limit=limit)
            cleaned[field] = text
            if not text:
                continue
            treatment, _source = cls._visual_treatment_from_text(text)
            field_keywords = cls._visual_metadata_keywords(text, limit=12)
            shared_topic = bool(field_keywords & prompt_keywords)
            scene_led = treatment == "photo_composite" or bool(
                re.search(
                    r"\b(full[- ]bleed|single[- ]subject|lifestyle|portrait|backdrop|environment)\b",
                    text,
                    flags=re.IGNORECASE,
                )
            )
            if scene_led and not prompt_allows_photo_scene and not shared_topic:
                cleaned[field] = ""
                removed_fields.append(field)
        return cleaned, removed_fields

    @classmethod
    def _infographic_surface_enforcement_section(
        cls,
        *,
        request: AIOrchestrationRequest,
        format_name: str,
        structured_visual_metadata: Any = None,
        visual_plan: dict[str, Any] | None = None,
    ) -> str:
        # Builds infographic surface enforcement section from request state, format name, and structured visual metadata for AIOrchestratorService.
        # It calls _explicit_top_n_item_count and _infographic_surface_intent while assembling the payload or prompt text.
        if not cls._infographic_surface_intent(
            prompt=getattr(request, "prompt", ""),
            format_name=format_name,
            visual_plan=visual_plan,
        ):
            return ""
        structured = structured_visual_metadata if isinstance(structured_visual_metadata, dict) else {}
        strict_anchors = structured.get("data_anchors") if isinstance(structured.get("data_anchors"), list) else []
        top_n = cls._explicit_top_n_item_count(getattr(request, "prompt", ""))
        top_n_rule = (
            f"Top-N comparison contract: the prompt asks for {top_n} item slot(s); represent that count with {top_n} visible comparison modules, cards, rows, or grouped list slots when the layout budget allows."
            if top_n
            else "Comparison contract: represent the requested compared categories with visible modules, cards, rows, or grouped list slots when the layout budget allows."
        )
        data_rule = (
            "Strict anchors are present: any table, bar, metric, ranking, label, value, axis, or legend must use only those approved anchors."
            if strict_anchors
            else "No strict approved data_anchors are present: keep the comparison qualitative and non-numeric; do not invent item labels, values, axes, bars, rankings, legends, dashboard tiles, or map-data values."
        )
        return (
            "Infographic surface enforcement: selected format is infographic, so the visual must be a module-first infographic surface with clear comparison structure, not an unrelated single-scene backdrop or generic image-led poster. "
            f"{top_n_rule} "
            "Show the relationship between compared items through ranked/list/card/module structure, connectors, dividers, grouped panels, or a non-numeric matrix that matches the user prompt intent. "
            f"{data_rule} "
            "Decorative imagery must stay secondary to the data/comparison surface. Keep title, modules, icons, and any comparison structure outside backend header/logo and footer/disclaimer safe areas."
        )

    @classmethod
    def _data_visualization_requested(cls, *values: Any) -> bool:
        # Centralizes visualization requested for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        combined = " ".join(
            cls._normalize_metadata_text(value, limit=520)
            for value in values
            if cls._normalize_metadata_text(value, limit=520)
        ).casefold()
        if not combined:
            return False
        return any(term in combined for term in cls.DATA_VISUAL_TERMS)

    @classmethod
    def _numeric_data_visualization_requested(cls, *values: Any) -> bool:
        # Centralizes numeric visualization requested for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        combined = " ".join(
            cls._normalize_metadata_text(value, limit=520)
            for value in values
            if cls._normalize_metadata_text(value, limit=520)
        ).casefold()
        if not combined:
            return False
        return any(term in combined for term in cls.NUMERIC_DATA_VISUAL_TERMS)

    @classmethod
    def _data_visualization_anchor_lines(
        cls,
        *values: Any,
        limit: int = 10,
    ) -> list[str]:
        # Extracts visualization anchor lines from limit for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        anchors: list[str] = []
        for value in values:
            if isinstance(value, dict):
                for key in (
                    "claim",
                    "evidence",
                    "headline",
                    "body",
                    "supporting_line",
                    "dominant_message",
                    "visual_focus",
                ):
                    text = cls._normalize_metadata_text(value.get(key), limit=260)
                    if text:
                        anchors.append(text)
                for key in (
                    "body_points",
                    "proof_points",
                    "stat_highlights",
                    "claim_evidence_pairs",
                    "static_panel_spec",
                    "infographic_section_specs",
                ):
                    nested = value.get(key)
                    if nested:
                        anchors.extend(cls._data_visualization_anchor_lines(nested, limit=limit))
                continue
            if isinstance(value, list):
                for item in value:
                    anchors.extend(cls._data_visualization_anchor_lines(item, limit=limit))
                continue
            text = cls._normalize_metadata_text(value, limit=260)
            if text:
                anchors.append(text)
        deduped: list[str] = []
        seen: set[str] = set()
        for anchor in anchors:
            key = anchor.casefold()
            if key in seen:
                continue
            seen.add(key)
            deduped.append(anchor)
            if len(deduped) >= limit:
                break
        return deduped

    @classmethod
    def _data_visualization_has_content_anchor(
        cls,
        *,
        request: AIOrchestrationRequest,
        anchors: list[str],
    ) -> bool:
        # Centralizes visualization content anchor from request state and anchors for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not anchors:
            return False
        topic_tokens = cls._content_topic_tokens(request)
        for anchor in anchors:
            if cls._exact_claim_markers(anchor):
                return True
            if cls._content_line_is_evidence_anchor(anchor, request=request):
                return True
            if topic_tokens and (set(cls._normalized_prompt_tokens(anchor)) & topic_tokens):
                return True
        return False

    @classmethod
    def _data_visualization_contract(
        cls,
        *,
        request: AIOrchestrationRequest,
        visual_intent: Any,
        anchors: list[str],
        format_name: str,
    ) -> str:
        # Builds visualization contract from request state, visual intent, and anchors for AIOrchestratorService.
        # It calls _numeric_data_visualization_requested and _data_visualization_has_content_anchor while assembling the payload or prompt text.
        if not cls._data_visualization_requested(request.prompt, visual_intent, *anchors):
            return ""
        numeric_requested = cls._numeric_data_visualization_requested(request.prompt, visual_intent, *anchors)
        exact_markers: set[str] = set()
        for anchor in anchors:
            exact_markers.update(cls._exact_claim_markers(anchor))
        anchor_summary = "; ".join(f'"{item}"' for item in anchors[:5]) or "no approved anchors"
        has_content_anchor = cls._data_visualization_has_content_anchor(
            request=request,
            anchors=anchors,
        )
        if not has_content_anchor:
            return (
                f"Data visualization execution contract for {format_name}: no approved data/content anchors are available. "
                "Strict prohibition: do not render a table, tabular layout, chart, graph, dashboard, scorecard, timeline, metric module, fake UI analytics panel, matrix, generic bars, fake axes, random percentages, placeholder numbers, or unlabeled data shapes. "
                "Use a non-data visual treatment tied to the approved headline/body instead."
            )
        value_rule = (
            "Numeric charting is allowed only for the exact values present in these anchors; do not add any extra values, percentages, axes, or rankings."
            if exact_markers
            else "No exact numeric series is approved here, so do not render numeric axes, bars, lines, percentages, currency amounts, rankings, or trend graphs. Use only qualitative modules, labeled comparison cards, process blocks, or a non-numeric matrix tied to the approved anchors."
        )
        type_rule = (
            "Choose table/matrix for comparisons, bar chart for ranked categories with real values, line chart for dated trends, flowchart for sequences, dashboard/scorecard modules for qualitative evidence, and callout cards for single proof points."
            if numeric_requested
            else "If a data-like structure helps, keep it as readable cards/modules tied to the content instead of decorative chart furniture."
        )
        return (
            f"Data visualization execution contract for {format_name}: approved data/content anchors are {anchor_summary}. "
            f"{value_rule} {type_rule} Every row, column, axis, legend, label, metric, or module heading must map to one approved anchor; never render generic bars, fake axes, random dashboard tiles, placeholder numbers, or unlabeled chart shapes."
        )

    @classmethod
    def _broad_visual_context_prompt_section(cls, lines: Any, *, limit: int = 5) -> str:
        # Builds broad visual context prompt section from lines and limit for AIOrchestratorService.
        # It calls _normalize_metadata_list while assembling the payload or prompt text.
        context_lines = cls._normalize_metadata_list(lines, limit=limit)
        if not context_lines:
            return ""
        return (
            "Broad visual/layout context hints, not strict data anchors: "
            + "; ".join(context_lines)
            + ". Use these only for composition, hierarchy, and subject context; they cannot unlock charts, dashboards, numeric callouts, or data visuals."
        )

    @classmethod
    def _sample_visual_permissions(cls, sample_page_blueprint: Any) -> dict[str, Any]:
        # Centralizes sample visual permissions from sample page blueprint for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}
        permissions = blueprint.get("visual_permissions") if isinstance(blueprint.get("visual_permissions"), dict) else {}
        return dict(permissions)

    @classmethod
    def _sample_permission_allows_visual_type(
        cls,
        *,
        sample_page_blueprint: Any,
        visual_type: str,
    ) -> bool:
        # Centralizes sample permission allows visual type from sample page blueprint and visual type for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}
        permissions = cls._sample_visual_permissions(blueprint)
        if not blueprint or not permissions:
            return True
        normalized_type = visual_type.strip().casefold()
        if normalized_type == "table":
            return bool(permissions.get("table_allowed", True))
        if normalized_type == "dashboard":
            return bool(permissions.get("dashboard_allowed", True))
        if normalized_type == "chart":
            observed_keys = [
                key
                for key in ("chart_or_graph_allowed", "metric_tiles_allowed", "dashboard_allowed")
                if key in permissions
            ]
            if not observed_keys:
                return True
            return any(bool(permissions.get(key)) for key in observed_keys)
        if normalized_type in {"3d", "isometric"}:
            observed_keys = [
                key
                for key in (
                    "three_d_allowed",
                    "3d_allowed",
                    "dimensional_allowed",
                    "depth_allowed",
                    "isometric_allowed",
                )
                if key in permissions
            ]
            if not observed_keys:
                return True
            return any(bool(permissions.get(key)) for key in observed_keys)
        if normalized_type == "icon_system":
            observed_keys = [
                key
                for key in ("icon_system_allowed", "icons_allowed", "iconography_allowed")
                if key in permissions
            ]
            if not observed_keys:
                return True
            return any(bool(permissions.get(key)) for key in observed_keys)
        if normalized_type == "photo_composite":
            observed_keys = [
                key
                for key in ("photo_allowed", "photography_allowed", "image_composite_allowed")
                if key in permissions
            ]
            if not observed_keys:
                return True
            return any(bool(permissions.get(key)) for key in observed_keys)
        return True

    @classmethod
    def _prompt_has_explicit_data_anchor(cls, prompt: Any) -> bool:
        # Builds prompt explicit anchor from prompt text for AIOrchestratorService.
        # It calls _normalize_metadata_text and _exact_claim_markers while assembling the payload or prompt text.
        text = cls._normalize_metadata_text(prompt, limit=520)
        if not text:
            return False
        if cls._exact_claim_markers(text):
            return True
        return bool(
            re.search(
                r"\b(?:table|matrix|comparison|compare|versus|vs\.?)\b.{0,120}\b(?:by|across|between|for)\b",
                text,
                flags=re.IGNORECASE,
            )
        )

    @classmethod
    def _structured_visual_anchor_source_allowed(cls, source: Any) -> bool:
        # Checks structured visual anchor source allowed from source for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        source_label = cls._normalize_metadata_text(source, limit=64).casefold()
        return source_label in cls.STRUCTURED_VISUAL_APPROVED_ANCHOR_SOURCES

    @classmethod
    def _structured_visual_anchor_lines(
        cls,
        value: Any,
        *,
        source_label: str,
        limit: int = 8,
    ) -> list[str]:
        # Extracts structured visual anchor lines from input value, source label, and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        lines: list[str] = []
        seen: set[str] = set()
        normalized_source = cls._normalize_metadata_text(source_label, limit=64).casefold()

        def append_line(candidate: Any) -> None:
            # Extracts append line from candidate for AIOrchestratorService.
            # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
            if len(lines) >= limit:
                return
            text = cls._normalize_metadata_text(candidate, limit=260)
            if not text:
                return
            key = text.casefold()
            if key in seen:
                return
            seen.add(key)
            lines.append(text)

        def collect(item: Any) -> None:
            # Centralizes collect from item for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if len(lines) >= limit:
                return
            if isinstance(item, dict):
                if normalized_source in {"claim_evidence", "claim_evidence_pairs", "nested_claim_evidence_pairs"}:
                    for key in ("claim", "evidence"):
                        append_line(item.get(key))
                    return
                if "verified" in normalized_source or normalized_source.endswith("evidence"):
                    for key in ("fact", "statement", "text", "claim", "evidence", "value"):
                        append_line(item.get(key))
                    return
                if normalized_source in {"stat_highlight", "stat_highlights", "nested_stat_highlights"}:
                    for key in ("text", "value", "stat", "fact", "evidence"):
                        append_line(item.get(key))
                    return
                for key in ("text", "value", "fact", "statement"):
                    append_line(item.get(key))
                return
            if isinstance(item, (list, tuple, set)):
                for nested in item:
                    collect(nested)
                    if len(lines) >= limit:
                        break
                return
            append_line(item)

        collect(value)
        return lines[:limit]

    @classmethod
    def _structured_visual_nested_anchor_sources(cls, *containers: Any) -> list[tuple[str, Any]]:
        # Centralizes structured visual nested anchor sources for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        buckets: dict[str, list[Any]] = {}

        def add(source: str, value: Any) -> None:
            # Centralizes add from source and input value for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if value is None:
                return
            buckets.setdefault(source, []).append(value)

        def collect(value: Any) -> None:
            # Centralizes collect from input value for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if isinstance(value, dict):
                for key in cls.STRUCTURED_VISUAL_NESTED_ANCHOR_KEYS:
                    if key in value:
                        add(f"nested_{key}", value.get(key))
                fact_model = value.get("fact_model") if isinstance(value.get("fact_model"), dict) else {}
                if fact_model:
                    for key in ("verified_facts", "evidence", "verified_evidence", "approved_evidence"):
                        if key in fact_model:
                            add(f"nested_{key}", fact_model.get(key))
                return
            if isinstance(value, (list, tuple, set)):
                for item in value:
                    collect(item)

        for container in containers:
            collect(container)
        return [(source, values) for source, values in buckets.items() if values]

    @classmethod
    def _structured_visual_data_anchor_sources(
        cls,
        metadata: Any,
        *,
        prompt: Any = "",
        request: AIOrchestrationRequest | None = None,
    ) -> list[tuple[str, Any]]:
        # Centralizes structured visual anchor sources from metadata, prompt text, and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        sources: list[tuple[str, Any]] = []
        prompt_text = prompt
        if not prompt_text and request is not None:
            prompt_text = request.prompt
        if prompt_text and cls._prompt_has_explicit_data_anchor(prompt_text):
            sources.append(("user_prompt", prompt_text))
        if not isinstance(metadata, dict):
            return sources

        for key in cls.STRUCTURED_VISUAL_PRIMARY_ANCHOR_KEYS:
            sources.append((key, metadata.get(key)))

        fact_model = metadata.get("fact_model") if isinstance(metadata.get("fact_model"), dict) else {}
        if fact_model:
            for key in ("verified_facts", "evidence", "verified_evidence", "approved_evidence"):
                if key in fact_model:
                    sources.append((key, fact_model.get(key)))

        sources.extend(
            cls._structured_visual_nested_anchor_sources(
                metadata.get("infographic_section_specs"),
                metadata.get("static_panel_spec"),
            )
        )
        return sources

    @classmethod
    def _structured_visual_anchor_entries(
        cls,
        sources: list[tuple[str, Any]],
        *,
        request: AIOrchestrationRequest | None = None,
        limit: int = 8,
    ) -> list[dict[str, Any]]:
        # Centralizes structured visual anchor entries from sources, request state, and limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        entries: list[dict[str, Any]] = []
        seen: set[str] = set()
        for source, value in sources:
            source_label = cls._normalize_metadata_text(source, limit=48) or "metadata"
            if not cls._structured_visual_anchor_source_allowed(source_label):
                continue
            for line in cls._structured_visual_anchor_lines(value, source_label=source_label, limit=limit):
                normalized = cls._normalize_metadata_text(line, limit=260)
                if not normalized:
                    continue
                key = normalized.casefold()
                if key in seen:
                    continue
                seen.add(key)
                entries.append(
                    {
                        "source": source_label,
                        "text": normalized,
                        "has_exact_value": bool(cls._exact_claim_markers(normalized)),
                    }
                )
                if len(entries) >= limit:
                    break
            if len(entries) >= limit:
                break
        if not entries:
            return []
        if request is not None and not cls._data_visualization_has_content_anchor(
            request=request,
            anchors=[entry["text"] for entry in entries],
        ):
            return []
        return entries[:limit]

    @classmethod
    def _brand_visual_grounding_evidence(
        cls,
        *,
        visual_identity: Any,
        reference_assets: Any = None,
        asset_catalog: Any = None,
        compiled_context: dict[str, Any] | None = None,
        limit: int = 5,
        include_reusable_design_assets: bool = True,
    ) -> list[str]:
        # Centralizes brand visual grounding evidence from visual identity, reference assets, and asset catalog for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        evidence: list[str] = []

        def add(value: Any, *, item_limit: int = 120) -> None:
            # Centralizes add from input value and item limit for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            normalized = cls._normalize_metadata_text(value, limit=item_limit)
            if not normalized:
                return
            if any(normalized.casefold() == item.casefold() for item in evidence):
                return
            evidence.append(cls._scrub_image_prompt_brand_mark_triggers(normalized, replacement="reserved space"))

        if isinstance(visual_identity, dict):
            for key in ("visual_style", "brand_mood", "illustration_style", "design_style", "composition_style"):
                add(visual_identity.get(key), item_limit=90)
            reusable_assets = visual_identity.get("reusable_design_assets")
            if include_reusable_design_assets and isinstance(reusable_assets, list):
                for asset in reusable_assets[:6]:
                    if not isinstance(asset, dict):
                        add(asset, item_limit=80)
                        continue
                    asset_parts = [
                        asset.get("name"),
                        asset.get("label"),
                        asset.get("asset_kind"),
                        asset.get("style"),
                        ", ".join(str(tag) for tag in asset.get("tags", [])[:4]) if isinstance(asset.get("tags"), list) else "",
                    ]
                    add(" ".join(str(part).strip() for part in asset_parts if str(part or "").strip()), item_limit=120)
                    if len(evidence) >= limit:
                        break

        reference_asset_items = reference_assets if isinstance(reference_assets, list) else []
        asset_catalog_items = asset_catalog if isinstance(asset_catalog, list) else []
        for asset in [*reference_asset_items[:4], *asset_catalog_items[:4]]:
            if not isinstance(asset, dict):
                continue
            label = cls._reference_asset_display_label(asset)
            role = cls._normalize_metadata_text(asset.get("asset_role"), limit=48)
            add(" ".join(part for part in [role, label] if part), item_limit=120)
            if len(evidence) >= limit:
                break

        visual_knowledge_brief = ContextCompilerService.coerce_visual_knowledge_brief(
            (compiled_context or {}).get("visual_knowledge_brief"),
        )
        for item in (visual_knowledge_brief.get("items") or [])[:4]:
            if not isinstance(item, dict):
                continue
            add(item.get("content"), item_limit=140)
            if len(evidence) >= limit:
                break

        return evidence[:limit]

    @classmethod
    def _visual_treatment_from_text(cls, value: Any) -> tuple[str, str]:
        # Extracts visual treatment text from input value for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=900).casefold()
        if not text:
            return "brand_led", "default"
        if re.search(r"\b(isometric|2\.5d|2-5d|axonometric)\b", text):
            return "isometric", "text"
        if re.search(r"\b(3d|three[- ]dimensional|volumetric|depth|dimensional|rendered object)\b", text):
            return "3d", "text"
        if re.search(r"\b(product dashboard|dashboard|analytics panel|platform surface|app surface|laptop|screen ui|ui panel)\b", text):
            return "product_dashboard", "text"
        if re.search(r"\b(icon system|icon set|icons|pictogram|glyph)\b", text):
            return "icon_system", "text"
        if re.search(r"\b(table|chart|graph|scorecard|metric module|data module|matrix)\b", text):
            return "data_module", "text"
        if re.search(r"\b(photo|photographic|image-led|lifestyle|portrait|environment)\b", text):
            return "photo_composite", "text"
        if re.search(r"\b(document|evidence|paper|report|contract|file|certificate)\b", text):
            return "document_evidence", "text"
        if re.search(r"\b(flat|editorial|magazine|poster|minimal|minimalist|illustration)\b", text):
            return "flat_editorial", "text"
        if re.search(r"\b(2d|two[- ]dimensional)\b", text):
            return "2d", "text"
        return "brand_led", "default"

    @classmethod
    def _resolve_visual_treatment_preference(
        cls,
        *,
        prompt_text: Any = "",
        combined_intent: Any = "",
        visual_plan: dict[str, Any] | None = None,
        raw_preference: Any = None,
        sample_page_blueprint: Any = None,
        brand_grounding: list[str] | None = None,
        table_requested: bool = False,
        chart_requested: bool = False,
        table_allowed: bool = False,
        chart_allowed: bool = False,
        anchor_count: int = 0,
        format_name: str = "",
    ) -> dict[str, Any]:
        # Resolves visual treatment preference from prompt text, combined intent, and visual plan for AIOrchestratorService.
        # The caller receives the canonical identifier/path/config value expected by the next step.
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}
        plan = visual_plan if isinstance(visual_plan, dict) else {}
        grounding = list(brand_grounding or [])
        scores = {value: 0.0 for value in cls.VISUAL_TREATMENT_PREFERENCE_VALUES}
        evidence: dict[str, list[str]] = {value: [] for value in cls.VISUAL_TREATMENT_PREFERENCE_VALUES}
        evidence_sources: dict[str, list[str]] = {value: [] for value in cls.VISUAL_TREATMENT_PREFERENCE_VALUES}

        def add_score(treatment: str, amount: float, source: str, reason: str) -> None:
            # Centralizes add from treatment, amount, and source for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            if treatment not in scores or amount <= 0:
                return
            scores[treatment] += amount
            compact_reason = cls._normalize_metadata_text(reason, limit=80)
            if compact_reason and compact_reason not in evidence[treatment]:
                evidence[treatment].append(compact_reason)
            if source and source not in evidence_sources[treatment]:
                evidence_sources[treatment].append(source)

        treatment_patterns: tuple[tuple[str, str, str], ...] = (
            ("isometric", r"\b(isometric|2\.5d|2-5d|axonometric)\b", "isometric/dimensional system cue"),
            ("3d", r"\b(3d|three[- ]dimensional|volumetric|true[- ]3d|rendered object|soft surfaces|depth)\b", "3d/depth cue"),
            ("product_dashboard", r"\b(product dashboard|dashboard|analytics panel|platform surface|app surface|laptop|screen ui|ui panel|product surface)\b", "product/ui surface cue"),
            ("icon_system", r"\b(icon system|icon set|icons|pictogram|glyph|iconography|symbol system)\b", "icon-system cue"),
            ("data_module", r"\b(table|chart|graph|scorecard|metric module|data module|matrix|dashboard tile|axis|axes)\b", "data/module cue"),
            ("photo_composite", r"\b(photo|photographic|image-led|lifestyle|portrait|environment|realistic scene)\b", "photo/composite cue"),
            ("document_evidence", r"\b(document|evidence|paper|report|contract|file|certificate|record)\b", "document/evidence cue"),
            ("flat_editorial", r"\b(flat|editorial|magazine|poster|minimal|minimalist|illustration|campaign)\b", "flat/editorial cue"),
            ("2d", r"\b(2d|two[- ]dimensional|vector|line art)\b", "2d cue"),
        )

        def score_text(source: str, value: Any, weight: float) -> None:
            # Extracts text from source, input value, and weight for AIOrchestratorService.
            # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
            text = cls._normalize_metadata_text(value, limit=900)
            if not text:
                return
            lowered = text.casefold()
            matched = False
            for treatment, pattern, reason in treatment_patterns:
                if re.search(pattern, lowered):
                    add_score(treatment, weight, source, reason)
                    matched = True
            if not matched and source in {"sample_layout", "fallback_context"}:
                add_score("brand_led", max(weight * 0.4, 1.0), source, "general context fit")

        score_text("user_prompt", prompt_text, 90.0)
        score_text("metadata", combined_intent, 42.0)
        score_text(
            "visual_plan",
            " ".join(
                cls._coerce_text_value(plan.get(key))
                for key in ("mode", "need", "rationale", "density")
                if cls._coerce_text_value(plan.get(key)).strip()
            ),
            28.0,
        )

        existing = raw_preference if isinstance(raw_preference, dict) else {}
        existing_preferred = cls._normalize_metadata_text(existing.get("preferred"), limit=40).casefold()
        if existing_preferred in cls.VISUAL_TREATMENT_PREFERENCE_VALUES:
            add_score(existing_preferred, 36.0, "structured_metadata", "existing structured preference")

        for item in grounding[:6]:
            score_text("brand_context", item, 62.0)

        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        layout_parts: list[str] = [
            cls._normalize_metadata_text(blueprint.get("layout_category"), limit=80),
            cls._normalize_metadata_text(blueprint.get("layout_mode"), limit=80),
            cls._normalize_metadata_text(blueprint.get("density"), limit=32),
        ]
        for zone_key in ("image_zones", "zones", "content_zones"):
            zones = blueprint.get(zone_key)
            if isinstance(zones, list):
                for zone in zones[:6]:
                    if isinstance(zone, dict):
                        layout_parts.extend(
                            cls._normalize_metadata_text(zone.get(key), limit=40)
                            for key in ("role", "name", "label", "zone_role")
                            if cls._normalize_metadata_text(zone.get(key), limit=40)
                        )
        score_text("sample_layout", " ".join(part for part in layout_parts if part), 30.0)
        try:
            small_icon_count = int(counts.get("small_icon_like_count") or 0)
            card_count = int(counts.get("card_like_count") or 0)
            visual_count = int(counts.get("large_visual_count") or 0)
        except (TypeError, ValueError):
            small_icon_count = card_count = visual_count = 0
        if small_icon_count > 0:
            add_score("icon_system", 22.0, "sample_layout", "sample has icon-like modules")
        if card_count > 0:
            add_score("flat_editorial", 12.0, "sample_layout", "sample has card/callout modules")
        if visual_count > 0 and not any(scores[value] for value in ("3d", "isometric", "photo_composite")):
            add_score("brand_led", 8.0, "sample_layout", "sample has visual slot")

        intent_text = cls._normalize_metadata_text(" ".join([cls._coerce_text_value(prompt_text), cls._coerce_text_value(combined_intent)]), limit=900).casefold()
        if re.search(r"\b(process|workflow|steps|sequence|journey|system|flow)\b", intent_text):
            add_score("icon_system", 20.0, "fallback_context", "process/workflow clarity cue")
            add_score("isometric", 12.0, "fallback_context", "process/workflow dimensional cue")
        if re.search(r"\b(compare|comparison|versus|vs\.?|before[- ]after|trade[- ]off)\b", intent_text):
            add_score("flat_editorial", 20.0, "fallback_context", "comparison clarity cue")
        if re.search(r"\b(service|explain|explanation|educat|guide|clarity)\b", intent_text):
            add_score("flat_editorial", 16.0, "fallback_context", "clarity-first explanation cue")
        if str(format_name or "").strip().casefold() in {"carousel", "infographic", "poster"}:
            add_score("flat_editorial", 8.0, "fallback_context", "format needs clear composition")

        if (table_requested or chart_requested) and anchor_count and (table_allowed or chart_allowed):
            add_score("data_module", 110.0, "approved_data_anchors", "strict data anchors and permissions")

        def product_context_present() -> bool:
            # Centralizes product context present for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            return bool(
                re.search(
                    r"\b(product|platform|app|screen|dashboard|ui|interface|analytics panel|laptop)\b",
                    intent_text + " " + " ".join(item.casefold() for item in grounding[:4]),
                )
            )

        blocked_reasons: list[str] = []

        def permission_allowed(treatment: str) -> bool:
            # Checks permission allowed from treatment for AIOrchestratorService.
            # It reuses _sample_permission_allows_visual_type so related checks follow the same rule.
            if treatment == "product_dashboard":
                if not product_context_present():
                    blocked_reasons.append("product_dashboard blocked: no product/ui context evidence")
                    return False
                if not cls._sample_permission_allows_visual_type(sample_page_blueprint=blueprint, visual_type="dashboard"):
                    blocked_reasons.append("product_dashboard blocked by sample visual_permissions")
                    return False
                return True
            if treatment == "data_module":
                if not anchor_count:
                    blocked_reasons.append("data_module blocked: no strict data anchors")
                    return False
                if not ((table_requested and table_allowed) or (chart_requested and chart_allowed)):
                    blocked_reasons.append("data_module blocked by data intent or sample permissions")
                    return False
                return True
            if treatment in {"3d", "isometric", "icon_system", "photo_composite"}:
                if not cls._sample_permission_allows_visual_type(sample_page_blueprint=blueprint, visual_type=treatment):
                    blocked_reasons.append(f"{treatment} blocked by sample visual_permissions")
                    return False
            return True

        ranked = sorted(
            ((treatment, score) for treatment, score in scores.items() if score > 0),
            key=lambda item: item[1],
            reverse=True,
        )
        preferred = ""
        preferred_score = 0.0
        for treatment, score in ranked:
            if permission_allowed(treatment):
                preferred = treatment
                preferred_score = score
                break

        fallback_reason = ""
        if not preferred:
            if blueprint:
                preferred = "flat_editorial"
                fallback_reason = "sample layout present without stronger allowed treatment evidence"
            else:
                preferred = "brand_led"
                fallback_reason = "no stronger treatment evidence"
            preferred_score = scores.get(preferred, 0.0)
            if preferred_score <= 0:
                add_score(preferred, 12.0, "fallback_context", fallback_reason)
                preferred_score = scores.get(preferred, 12.0)

        source_priority = ("user_prompt", "brand_context", "approved_data_anchors", "metadata", "visual_plan", "structured_metadata", "sample_layout", "fallback_context")
        preferred_sources = evidence_sources.get(preferred) or ["fallback_context"]
        if preferred == "data_module" and anchor_count:
            source = "data_anchors"
        else:
            source = next((candidate for candidate in source_priority if candidate in preferred_sources), preferred_sources[0])
            if source == "approved_data_anchors":
                source = "data_anchors"
        confidence = round(min(max(preferred_score / 120.0, 0.05), 0.99), 2)
        decision_evidence = evidence.get(preferred)[:5]
        if not fallback_reason and "fallback_context" in preferred_sources and source == "fallback_context":
            fallback_reason = "context-derived fallback"

        return {
            "preferred": preferred,
            "allowed": True,
            "source": source,
            "brand_asset_grounding": grounding,
            "layout_obedience": "sample_layout_first" if blueprint else "approved_layout_slots_first",
            "decision_evidence": decision_evidence,
            "blocked_reasons": list(dict.fromkeys(blocked_reasons))[:5],
            "fallback_reason": fallback_reason,
            "confidence": confidence,
        }

    @classmethod
    def _creativity_signal_lines(cls, value: Any, *, brand_grounding: list[str], limit: int = 5) -> list[str]:
        # Extracts creativity signal lines from input value, brand grounding, and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=900)
        lowered = text.casefold()
        signals: list[str] = []
        for label, pattern in (
            ("creative_prompt", r"\b(creative|attractive|premium|high-end|polished|dynamic|engaging|attention-grabbing)\b"),
            ("dimensional_visual", r"\b(3d|three[- ]dimensional|isometric|2\.5d|depth|volumetric)\b"),
            ("editorial_visual", r"\b(editorial|magazine|campaign|poster|flat|minimal)\b"),
            ("structured_visual", r"\b(table|chart|graph|dashboard|matrix|scorecard|comparison)\b"),
            ("asset_grounded", r"\b(reference|asset|visual identity|brand system|icon|illustration)\b"),
        ):
            if re.search(pattern, lowered):
                signals.append(label)
        if brand_grounding:
            signals.append("brand_asset_context")
        deduped: list[str] = []
        for signal in signals:
            if signal not in deduped:
                deduped.append(signal)
            if len(deduped) >= limit:
                break
        return deduped

    @classmethod
    def _normalize_structured_visual_metadata(
        cls,
        *,
        metadata: dict[str, Any],
        request: AIOrchestrationRequest | None = None,
        visual_intent: Any = "",
        anchor_entries: list[dict[str, Any]] | None = None,
        format_name: str = "",
        sample_page_blueprint: Any = None,
        visual_plan: dict[str, Any] | None = None,
        visual_identity: Any = None,
        reference_assets: Any = None,
        asset_catalog: Any = None,
        compiled_context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Normalizes structured visual metadata from metadata, request state, and visual intent for AIOrchestratorService.
        # It delegates shared cleanup to _numeric_data_visualization_requested and _sample_permission_allows_visual_type before returning the cleaned va.
        raw = metadata.get("structured_visual_metadata") if isinstance(metadata, dict) else {}
        raw = raw if isinstance(raw, dict) else {}
        blueprint = (
            sample_page_blueprint
            if isinstance(sample_page_blueprint, dict)
            else metadata.get("sample_page_blueprint")
            if isinstance(metadata.get("sample_page_blueprint"), dict)
            else {}
        )
        prompt_text = request.prompt if request is not None else ""
        combined_intent = " ".join(
            part
            for part in [
                cls._coerce_text_value(prompt_text),
                cls._coerce_text_value(visual_intent),
                cls._coerce_text_value(metadata.get("visual_focus")),
                cls._coerce_text_value(metadata.get("visual_direction")),
                cls._coerce_text_value(metadata.get("design_style")),
                cls._coerce_text_value(metadata.get("image_prompt")),
            ]
            if cls._coerce_text_value(part).strip()
        )
        lowered_intent = combined_intent.casefold()
        table_requested = bool(re.search(r"\b(table|tabular|matrix|comparison grid)\b", lowered_intent))
        chart_requested = bool(
            re.search(
                r"\b(chart|graph|dashboard|scorecard|metric module|metrics module|timeline|axis|axes|data visuali[sz]ation|data viz)\b",
                lowered_intent,
            )
        )
        numeric_requested = cls._numeric_data_visualization_requested(combined_intent)
        data_anchors = list(anchor_entries or [])
        if not data_anchors and isinstance(raw.get("data_anchors"), list):
            for item in raw.get("data_anchors", [])[:8]:
                if isinstance(item, dict):
                    text = cls._normalize_metadata_text(item.get("text"), limit=260)
                    source_label = cls._normalize_metadata_text(item.get("source"), limit=48) or "structured_visual_metadata"
                else:
                    text = cls._normalize_metadata_text(item, limit=260)
                    source_label = "structured_visual_metadata"
                if text and cls._structured_visual_anchor_source_allowed(source_label):
                    data_anchors.append(
                        {
                            "source": source_label,
                            "text": text,
                            "has_exact_value": bool(cls._exact_claim_markers(text)),
                        }
                    )
        anchor_count = len(data_anchors)
        prompt_explicit_visual = bool(request and cls._has_explicit_data_visual_request(prompt_text, text_payload=None))
        explicit_anchor_override = bool(prompt_explicit_visual and anchor_count)
        sample_table_allowed = cls._sample_permission_allows_visual_type(
            sample_page_blueprint=blueprint,
            visual_type="table",
        )
        sample_chart_allowed = cls._sample_permission_allows_visual_type(
            sample_page_blueprint=blueprint,
            visual_type="chart",
        )
        table_allowed = bool(table_requested and anchor_count and (sample_table_allowed or explicit_anchor_override))
        chart_allowed = bool(chart_requested and anchor_count and (sample_chart_allowed or explicit_anchor_override))
        brand_grounding = cls._brand_visual_grounding_evidence(
            visual_identity=visual_identity,
            reference_assets=reference_assets,
            asset_catalog=asset_catalog,
            compiled_context=compiled_context,
            include_reusable_design_assets=not bool(blueprint),
        )
        existing_preference = raw.get("visual_treatment_preference") if isinstance(raw.get("visual_treatment_preference"), dict) else {}
        treatment_preference = cls._resolve_visual_treatment_preference(
            prompt_text=prompt_text,
            combined_intent=combined_intent,
            visual_plan=visual_plan,
            raw_preference=existing_preference,
            sample_page_blueprint=blueprint,
            brand_grounding=brand_grounding,
            table_requested=table_requested,
            chart_requested=chart_requested,
            table_allowed=table_allowed,
            chart_allowed=chart_allowed,
            anchor_count=anchor_count,
            format_name=format_name,
        )
        preferred_treatment = cls._normalize_metadata_text(
            treatment_preference.get("preferred"),
            limit=40,
        ).casefold()
        creativity_signals = cls._creativity_signal_lines(
            combined_intent,
            brand_grounding=brand_grounding,
        )
        needs_attractive_visual = bool(
            creativity_signals
            or preferred_treatment in {"3d", "isometric", "flat_editorial", "photo_composite", "document_evidence"}
            or str(format_name or "").strip().casefold() in {"carousel", "infographic", "poster"}
        )
        source = "user_prompt" if prompt_explicit_visual else "metadata" if (table_requested or chart_requested) else "none"
        return {
            "schema_version": cls.STRUCTURED_VISUAL_METADATA_SCHEMA_VERSION,
            "table_intent": {
                "requested": table_requested,
                "allowed": table_allowed,
                "requires_data_anchors": True,
                "anchor_count": anchor_count if table_requested else 0,
                "source": source if table_requested else "none",
            },
            "chart_intent": {
                "requested": chart_requested,
                "allowed": chart_allowed,
                "numeric": numeric_requested,
                "requires_data_anchors": True,
                "anchor_count": anchor_count if chart_requested else 0,
                "source": source if chart_requested else "none",
            },
            "data_anchors": data_anchors[:8],
            "creativity_signals": {
                "needs_attractive_visual": needs_attractive_visual,
                "signals": creativity_signals,
                "source": "user_prompt" if creativity_signals and prompt_text else "brand_context" if brand_grounding else "metadata",
            },
            "visual_treatment_preference": {
                **treatment_preference,
            },
        }

    @classmethod
    def _structured_visual_metadata_prompt_section(cls, structured_visual_metadata: Any) -> str:
        # Builds structured visual metadata prompt section from structured visual metadata for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        metadata = structured_visual_metadata if isinstance(structured_visual_metadata, dict) else {}
        if not metadata:
            return ""
        compact = json.dumps(metadata, ensure_ascii=True, separators=(",", ":"))
        preference = metadata.get("visual_treatment_preference") if isinstance(metadata.get("visual_treatment_preference"), dict) else {}
        table_intent = metadata.get("table_intent") if isinstance(metadata.get("table_intent"), dict) else {}
        chart_intent = metadata.get("chart_intent") if isinstance(metadata.get("chart_intent"), dict) else {}
        grounding = preference.get("brand_asset_grounding") if isinstance(preference.get("brand_asset_grounding"), list) else []
        grounding_text = "; ".join(cls._normalize_metadata_text(item, limit=120) for item in grounding[:4] if cls._normalize_metadata_text(item, limit=120))
        restrictions: list[str] = [
            f"Structured visual metadata contract JSON: {compact}.",
            "Layout obedience is stronger than creativity: apply the preferred visual treatment only inside approved layout, image, module, or visual slots; never invent new regions.",
            "Do not bypass sample visual_permissions; when a table/chart/dashboard intent is not allowed or has anchor_count=0, render a non-data visual treatment instead.",
        ]
        if grounding_text:
            restrictions.append(
                f"Brand-grounded visual treatment preference: {preference.get('preferred')} from {preference.get('source')}; grounding cues: {grounding_text}."
            )
        if (table_intent.get("requested") and not table_intent.get("allowed")) or (chart_intent.get("requested") and not chart_intent.get("allowed")):
            restrictions.append(
                "Table/chart request is blocked for this surface unless approved anchors and sample permissions both support it; do not draw tables, charts, axes, dashboards, or metric furniture."
            )
        return " ".join(part for part in restrictions if part)

    @classmethod
    def _dynamic_visual_art_direction_section(
        cls,
        *,
        request: AIOrchestrationRequest | None = None,
        format_name: str = "",
        semantic_visual_brief: Any = "",
        visual_intent: Any = "",
        structured_visual_metadata: Any = None,
        visual_plan: dict[str, Any] | None = None,
        visual_identity: Any = None,
        data_visualization_anchor_lines: list[str] | None = None,
        sample_page_blueprint: Any = None,
        slide_purpose: Any = "",
        reference_assets: Any = None,
        asset_catalog: Any = None,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Builds dynamic visual art direction section from request state, format name, and semantic visual brief for AIOrchestratorService.
        # It calls _normalize_metadata_text and _visual_treatment_from_text while assembling the payload or prompt text.
        metadata = structured_visual_metadata if isinstance(structured_visual_metadata, dict) else {}
        plan = visual_plan if isinstance(visual_plan, dict) else {}
        preference = metadata.get("visual_treatment_preference") if isinstance(metadata.get("visual_treatment_preference"), dict) else {}
        creativity = metadata.get("creativity_signals") if isinstance(metadata.get("creativity_signals"), dict) else {}
        table_intent = metadata.get("table_intent") if isinstance(metadata.get("table_intent"), dict) else {}
        chart_intent = metadata.get("chart_intent") if isinstance(metadata.get("chart_intent"), dict) else {}
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}

        prompt_text = getattr(request, "prompt", "") if request is not None else ""
        communicate = cls._normalize_metadata_text(
            semantic_visual_brief
            or visual_intent
            or prompt_text,
            limit=260,
        )
        if not communicate:
            return ""

        plan_parts = [
            cls._normalize_metadata_text(format_name, limit=40),
            cls._normalize_metadata_text(slide_purpose, limit=120),
            cls._normalize_metadata_text(plan.get("mode"), limit=40),
            cls._normalize_metadata_text(plan.get("rationale"), limit=140),
        ]
        purpose = cls._normalize_metadata_text(
            " | ".join(part for part in plan_parts if part),
            limit=240,
        )
        preferred = cls._normalize_metadata_text(preference.get("preferred"), limit=40).casefold()
        if preferred not in cls.VISUAL_TREATMENT_PREFERENCE_VALUES:
            preferred, _ = cls._visual_treatment_from_text(
                " ".join(
                    part
                    for part in [
                        cls._coerce_text_value(visual_intent),
                        communicate,
                        purpose,
                    ]
                    if cls._coerce_text_value(part).strip()
                )
            )
        treatment_allowed = bool(preference.get("allowed", True))
        treatment = preferred if treatment_allowed else f"{preferred} blocked by layout permissions"

        grounding = preference.get("brand_asset_grounding") if isinstance(preference.get("brand_asset_grounding"), list) else []
        brand_cues: list[str] = []
        for item in grounding[:4]:
            cue = cls._normalize_metadata_text(item, limit=120)
            if cue and cue not in brand_cues:
                brand_cues.append(cue)
        if not brand_cues:
            brand_cues = cls._brand_visual_grounding_evidence(
                visual_identity=visual_identity,
                reference_assets=reference_assets,
                asset_catalog=asset_catalog,
                compiled_context=compiled_context,
                limit=4,
            )
        brand_cue_text = "; ".join(brand_cues[:4]) or "derive from approved copy, palette, typography, and current visual identity"

        data_lines = data_visualization_anchor_lines if isinstance(data_visualization_anchor_lines, list) else []
        def anchor_count_value(value: Any) -> int:
            # Counts anchor count from input value for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            try:
                return int(value or 0)
            except (TypeError, ValueError):
                return 0

        anchor_count = max(
            anchor_count_value(table_intent.get("anchor_count")),
            anchor_count_value(chart_intent.get("anchor_count")),
            len(metadata.get("data_anchors") if isinstance(metadata.get("data_anchors"), list) else []),
        )
        table_requested = bool(table_intent.get("requested"))
        chart_requested = bool(chart_intent.get("requested"))
        table_allowed = bool(table_intent.get("allowed"))
        chart_allowed = bool(chart_intent.get("allowed"))
        if table_requested or chart_requested:
            if (table_requested and table_allowed) or (chart_requested and chart_allowed):
                data_posture = f"use only approved anchors for any visible data structure; anchor_count={anchor_count}"
            elif anchor_count:
                data_posture = "keep approved facts as callouts or non-data modules unless layout permissions allow data structures"
            else:
                data_posture = "avoid data-visual structures because no approved anchors are available"
        elif data_lines:
            data_posture = "use available factual context for hierarchy or callouts only; do not invent numeric visuals"
        else:
            data_posture = "do not introduce table, chart, dashboard, or axis grammar unless the prompt and anchors require it"

        explicit_data_visual_request = bool(
            request is not None
            and cls._has_explicit_data_visual_request(prompt_text, text_payload=None)
        )
        avoid_parts: list[str] = []
        if not explicit_data_visual_request:
            avoid_parts.append("unrequested data-visual hero treatment")
        if not treatment_allowed:
            avoid_parts.append(f"blocked {preferred} treatment")
        if blueprint:
            avoid_parts.append("new regions outside sample_page_blueprint visual permissions")
        avoid_line = "; ".join(dict.fromkeys(part for part in avoid_parts if part)) or "motifs unsupported by prompt, brand evidence, or approved layout slots"

        craft_posture = (
            "raise craft through the selected treatment and brand-grounded cues"
            if creativity.get("needs_attractive_visual")
            else "keep craft restrained around message clarity and brand fit"
        )
        layout_line = (
            "follow sample_page_blueprint visual_permissions, image_zones, and module slots first"
            if blueprint
            else "stay inside approved visual, image, and module slots from the active layout"
        )
        return (
            "Dynamic visual art direction: "
            f"communicate={communicate}; "
            f"slide purpose={purpose or 'current surface'}; "
            f"treatment={treatment}; "
            f"brand cues={brand_cue_text}; "
            f"data posture={data_posture}; "
            f"craft posture={craft_posture}; "
            f"avoid={avoid_line}; "
            f"layout={layout_line}."
        )

    @classmethod
    def _resolve_reference_creative_profile(
        cls,
        *,
        request: AIOrchestrationRequest | None = None,
        format_name: str = "",
        sample_page_blueprint: Any = None,
        visual_identity: Any = None,
        visual_plan: dict[str, Any] | None = None,
        structured_visual_metadata: Any = None,
        layout_budget: dict[str, Any] | None = None,
        slide: dict[str, Any] | None = None,
        metadata: dict[str, Any] | None = None,
        slide_purpose: Any = "",
        reference_assets: Any = None,
        asset_catalog: Any = None,
        compiled_context: dict[str, Any] | None = None,
        logo_position_hint: str = "",
    ) -> dict[str, Any]:
        # Resolves reference creative profile from request state, format name, and sample page blueprint for AIOrchestratorService.
        # The caller receives the canonical identifier/path/config value expected by the next step.
        blueprint = (
            cls._sample_blueprint_without_rendered_footer_text(sample_page_blueprint)
            if isinstance(sample_page_blueprint, dict)
            else {}
        )
        identity = visual_identity if isinstance(visual_identity, dict) else {}
        plan = visual_plan if isinstance(visual_plan, dict) else {}
        structured = structured_visual_metadata if isinstance(structured_visual_metadata, dict) else {}
        context = compiled_context if isinstance(compiled_context, dict) else {}
        adaptation_profile = (
            context.get("reference_adaptation_profile")
            if isinstance(context.get("reference_adaptation_profile"), dict)
            else {}
        )
        preference = structured.get("visual_treatment_preference") if isinstance(structured.get("visual_treatment_preference"), dict) else {}
        budget = layout_budget if isinstance(layout_budget, dict) else {}
        slide_payload = slide if isinstance(slide, dict) else {}
        meta = metadata if isinstance(metadata, dict) else {}
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        permissions = cls._sample_visual_permissions(blueprint)

        layout_mode = cls._sample_blueprint_layout_mode(blueprint)
        layout_instruction = cls._sample_blueprint_layout_instruction(blueprint)
        layout_system = cls._normalize_metadata_text(
            " | ".join(
                part
                for part in [
                    cls._normalize_metadata_text(blueprint.get("layout_category"), limit=80),
                    cls._normalize_metadata_text(layout_mode, limit=80),
                    cls._normalize_metadata_text(blueprint.get("density"), limit=40),
                    cls._normalize_metadata_text(layout_instruction, limit=180),
                ]
                if part
            ),
            limit=320,
        )

        palette_roles = derive_palette_roles(identity) if identity else {}
        if not palette_roles and isinstance(identity.get("palette_roles"), dict):
            palette_roles = identity.get("palette_roles") or {}
        color_roles = {
            role: value
            for role in ("background", "surface", "primary", "secondary", "accent")
            if (value := cls._normalize_metadata_text(palette_roles.get(role), limit=32))
        }
        if not color_roles:
            color_roles = {
                "policy": "derive background, primary, secondary, accent, and legal/footer roles from approved brand or reference evidence"
            }

        typography = identity.get("typography") or identity.get("typography_guide") or {}
        typography_roles: dict[str, Any] = {}
        if isinstance(typography, dict):
            for role in ("headline", "subheadline", "callout", "body", "label", "source", "footer"):
                value = (
                    typography.get(role)
                    or typography.get(f"{role}_style")
                    or typography.get(f"{role}_role")
                )
                normalized = cls._normalize_metadata_text(value, limit=80)
                if normalized:
                    typography_roles[role] = normalized
        typography_roles.setdefault(
            "hierarchy_policy",
            "separate headline, subheadline, callout, body, label, source, and footer roles; do not make every text block bold or large",
        )

        brand_grounding = cls._brand_visual_grounding_evidence(
            visual_identity=identity,
            reference_assets=reference_assets,
            asset_catalog=asset_catalog,
            compiled_context=compiled_context,
            include_reusable_design_assets=not bool(blueprint),
            limit=4,
        )
        image_style_parts = [
            f"treatment={cls._normalize_metadata_text(preference.get('preferred'), limit=40)}"
            if preference.get("preferred")
            else "",
            cls._normalize_metadata_text(plan.get("mode"), limit=60),
            cls._normalize_metadata_text(plan.get("rationale"), limit=120),
            cls._normalize_metadata_text(meta.get("reference_visual_craft"), limit=140),
            "; ".join(brand_grounding[:3]),
        ]
        image_style = cls._normalize_metadata_text(
            " | ".join(part for part in image_style_parts if part),
            limit=320,
        ) or "derive image treatment from approved visual identity, reference assets, and current slide role"

        role_text = cls._normalize_metadata_text(
            " ".join(
                part
                for part in [
                    slide_payload.get("role"),
                    meta.get("story_role"),
                    meta.get("carousel_archetype"),
                    slide_purpose,
                    layout_mode,
                    cls._normalize_metadata_text(blueprint.get("layout_category"), limit=80),
                ]
                if cls._coerce_text_value(part).strip()
            ),
            limit=260,
        ).casefold()
        if re.search(r"\b(hook|cover|opening|opener|title)\b", role_text):
            slide_archetype = "hook/opener"
        elif re.search(r"\b(persona|example|story|relatable)\b", role_text):
            slide_archetype = "relatable persona setup"
        elif re.search(r"\b(problem|mistake|risk|pain|warning)\b", role_text):
            slide_archetype = "problem/mistake"
        elif re.search(r"\b(process|workflow|step|journey|behavior|behaviour|change)\b", role_text):
            slide_archetype = "behavior change/process"
        elif re.search(r"\b(proof|calculation|comparison|compare|table|chart|metric|data)\b", role_text):
            slide_archetype = "proof/calculation/comparison"
        elif re.search(r"\b(cta|close|closing|summary|takeaway|final|insight)\b", role_text):
            slide_archetype = "insight/CTA/summary"
        else:
            slide_archetype = "current slide role"

        persona_context = getattr(request, "persona_context", {}) if request is not None else {}
        persona_context = persona_context if isinstance(persona_context, dict) else {}
        persona_source_values: list[Any] = []
        for key in (
            "persona_summary",
            "audience_summary",
            "motivations",
            "pain_points",
            "objections",
            "language_preference",
        ):
            value = persona_context.get(key)
            if isinstance(value, (list, tuple, set)):
                persona_source_values.extend(value)
            else:
                persona_source_values.append(value)
        persona_clues = cls._normalize_metadata_list(persona_source_values, limit=4)
        persona_policy = (
            "reuse one non-sensitive audience/persona brief across the carousel: " + "; ".join(persona_clues)
            if persona_clues
            else "do not invent a recurring persona unless prompt, slide role, or reference evidence requires one"
        )

        logo_policy = cls._normalize_metadata_text(budget.get("brand_header_policy"), limit=180)
        if logo_policy and "brand header slot" in logo_policy.casefold() and "brand-header" not in logo_policy.casefold():
            logo_policy = f"{logo_policy}; brand-header slot alias for the reserved logo/header zone"
        if not logo_policy:
            logo_position = (
                cls._normalize_metadata_text(logo_position_hint, limit=64)
                or cls._sample_blueprint_logo_position(blueprint)
            )
            logo_policy = (
                f"reserve the approved brand-header slot at {logo_position}; use exact asset overlay when available and do not redraw the mark"
                if logo_position
                else "reserve any approved brand-header/logo zone from the active layout; use exact asset overlay when available and do not redraw the mark"
            )

        footer_policy = cls._normalize_metadata_text(budget.get("footer_policy"), limit=220)
        if not footer_policy:
            footer_count = cls._int_or_none(counts.get("footer_band_count")) or 0
            footer_policy = (
                "reserve observed footer/disclaimer band before visual placement; keep characters, icons, captions, CTA, and objects outside it"
                if footer_count
                else "if backend footer/disclaimer metadata exists, exclude that safe area from content and visual-object placement"
            )

        object_density_parts: list[str] = []
        for key in (
            "large_visual_count",
            "small_icon_like_count",
            "card_like_count",
            "horizontal_band_count",
            "text_block_count",
        ):
            value = cls._int_or_none(counts.get(key))
            if value is not None:
                object_density_parts.append(f"{key}={value}")
        visible_capacity = cls._int_or_none(budget.get("visible_capacity"))
        if visible_capacity is not None:
            object_density_parts.append(f"visible_content_capacity={visible_capacity}")
        object_density = (
            "; ".join(object_density_parts)
            + "; do not add extra hero objects, icons, callouts, or decorative metaphors beyond observed capacity"
            if object_density_parts
            else "use the minimum hero objects, icons, callouts, and decorative metaphors needed for the approved layout"
        )

        treatment_policy = cls._normalize_metadata_text(preference.get("preferred"), limit=40) or "brand_led"
        if permissions:
            treatment_policy += "; obey sample visual_permissions"

        return {
            "layout_system": layout_system or "derive from active layout slots",
            "color_roles": color_roles,
            "typography_roles": typography_roles,
            "image_style": image_style,
            "logo_header_policy": logo_policy,
            "footer_safe_policy": footer_policy,
            "slide_archetype": slide_archetype,
            "persona_policy": persona_policy,
            "object_density": object_density,
            "visual_treatment_consistency": treatment_policy,
            "reference_obedience": (
                "sample_first"
                if blueprint
                else (
                    cls._normalize_metadata_text(adaptation_profile.get("adaptation_mode"), limit=64)
                    if adaptation_profile
                    else "brand_context_first"
                )
            ),
            "reference_adaptation_mode": cls._normalize_metadata_text(adaptation_profile.get("adaptation_mode"), limit=64) if adaptation_profile else "",
        }

    @classmethod
    def _reference_creative_profile_prompt_section(cls, profile: Any) -> str:
        # Builds reference creative profile prompt section from profile for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        if not isinstance(profile, dict) or not profile:
            return ""
        profile = dict(profile)
        if "logo_header_policy" not in profile and profile.get("brand_header_policy"):
            profile["logo_header_policy"] = profile.get("brand_header_policy")
        if "footer_safe_policy" not in profile and profile.get("footer_policy"):
            profile["footer_safe_policy"] = profile.get("footer_policy")
        compact = json.dumps(profile, ensure_ascii=True, separators=(",", ":"))
        return (
            f"Reference creative profile JSON: {compact}. "
            "Use this as source-aware creative DNA for layout, color roles, typography hierarchy, image composition, persona continuity, brand-header handling, footer-safe placement, and object density. "
            "It must enhance only approved slots; do not copy literal sample text, artwork, brand mark, or unrelated subject matter, and do not modify structured_visual_metadata.data_anchors."
        )

    @classmethod
    def _compact_prompt_json_value(
        cls,
        value: Any,
        *,
        depth: int = 0,
        max_depth: int = 3,
        list_limit: int = 6,
        text_limit: int = 180,
    ) -> Any:
        # Compacts prompt json from input value, depth, and max depth for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if value in ("", None, [], {}):
            return None
        if isinstance(value, dict):
            compact: dict[str, Any] = {}
            for key, item in value.items():
                normalized_key = cls._normalize_metadata_text(key, limit=64)
                if not normalized_key:
                    continue
                if depth >= max_depth and isinstance(item, (dict, list, tuple, set)):
                    if isinstance(item, dict):
                        scalar_items = {
                            cls._normalize_metadata_text(nested_key, limit=64): cls._compact_prompt_json_value(
                                nested_value,
                                depth=depth + 1,
                                max_depth=max_depth,
                                list_limit=list_limit,
                                text_limit=text_limit,
                            )
                            for nested_key, nested_value in list(item.items())[:list_limit]
                            if not isinstance(nested_value, (dict, list, tuple, set))
                        }
                        compact_value = {k: v for k, v in scalar_items.items() if k and v not in ("", None, [], {})}
                    else:
                        compact_value = [
                            cls._compact_prompt_json_value(
                                nested,
                                depth=depth + 1,
                                max_depth=max_depth,
                                list_limit=list_limit,
                                text_limit=text_limit,
                            )
                            for nested in list(item)[:list_limit]
                        ]
                        compact_value = [nested for nested in compact_value if nested not in ("", None, [], {})]
                else:
                    compact_value = cls._compact_prompt_json_value(
                        item,
                        depth=depth + 1,
                        max_depth=max_depth,
                        list_limit=list_limit,
                        text_limit=text_limit,
                    )
                if compact_value not in ("", None, [], {}):
                    compact[normalized_key] = compact_value
            return compact or None
        if isinstance(value, (list, tuple, set)):
            compact_list = [
                cls._compact_prompt_json_value(
                    item,
                    depth=depth + 1,
                    max_depth=max_depth,
                    list_limit=list_limit,
                    text_limit=text_limit,
                )
                for item in list(value)[:list_limit]
            ]
            compact_list = [item for item in compact_list if item not in ("", None, [], {})]
            return compact_list or None
        if isinstance(value, str):
            return cls._normalize_metadata_text(value, limit=text_limit)
        return value

    @classmethod
    def _compact_reference_adaptation_profile_for_prompt(cls, profile: dict[str, Any]) -> dict[str, Any]:
        # Compacts reference adaptation profile prompt from profile for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        compact: dict[str, Any] = {}
        for key in (
            "source_format_family",
            "output_format_family",
            "selected_output_behavior",
            "adaptation_mode",
            "confidence",
            "adapt",
            "do_not_adapt",
            "balance",
        ):
            if key not in profile:
                continue
            compact_value = cls._compact_prompt_json_value(
                profile.get(key),
                max_depth=3,
                list_limit=6,
                text_limit=160,
            )
            if compact_value not in ("", None, [], {}):
                compact[key] = compact_value
        if isinstance(profile.get("template_structure"), dict) and profile.get("template_structure"):
            compact["template_structure_available"] = True
        return compact or dict(profile)

    @classmethod
    def _reference_adaptation_profile_prompt_section(cls, profile: Any) -> str:
        # Builds reference adaptation profile prompt section from profile for AIOrchestratorService.
        # It calls _compact_reference_adaptation_profile_for_prompt while assembling the payload or prompt text.
        if not isinstance(profile, dict) or not profile:
            return ""
        compact_profile = cls._compact_reference_adaptation_profile_for_prompt(profile)
        compact = json.dumps(compact_profile, ensure_ascii=True, separators=(",", ":"))
        return (
            f"Reference adaptation profile JSON: {compact}. "
            "Use adaptation_mode to decide whether the active source is a template sequence, infographic surface, static creative, hybrid visual guide, or brand-only fallback. "
            "Apply only the visual-system traits marked in adapt; do not copy literal source text, source subject matter, unsupported claims, brand marks without permission, or unsupported data visuals. "
            "Conflict rule: current prompt content and approved evidence come first, reference visual system second, and footer/logo/data-anchor safety overrides both."
        )

    @classmethod
    def _generation_surface_contract_prompt_section(cls, contract: Any) -> str:
        # Builds generation surface contract prompt section from contract for AIOrchestratorService.
        # The returned payload is the compact contract consumed by the next branch.
        if not isinstance(contract, dict) or not contract:
            return ""
        compact = json.dumps(contract, ensure_ascii=True, separators=(",", ":"))
        return (
            f"Generation surface contract JSON: {compact}. "
            "Selected format, platform, size, file type, and campaign goal define the output surface. "
            "Reference format can guide only compatible visual grammar; it must not convert static output into carousel, carousel output into a poster, or infographic output into generic static layout. "
            "File type controls export/render path only, not content meaning."
        )

    @classmethod
    def _reference_template_structure_payload(
        cls,
        compiled_context: dict[str, Any] | None,
        profile: dict[str, Any],
        *,
        source_family: str,
        output_family: str,
        layout_content_budget: dict[str, Any] | None = None,
        sample_page_blueprint: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Builds reference template structure from compiled context, profile, and source family for AIOrchestratorService.
        # It calls _sample_blueprint_without_rendered_footer_text and _sample_blueprint_layout_mode while assembling the payload or prompt text.
        context = compiled_context if isinstance(compiled_context, dict) else {}
        structure = profile.get("template_structure") if isinstance(profile.get("template_structure"), dict) else {}
        runtime_blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}

        def normalize_family(value: Any) -> str:
            # Centralizes family from input value for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            text = cls._normalize_metadata_text(value, limit=48).casefold()
            normalized = text.replace("-", "_").replace(" ", "_").replace("/", "_").strip("_")
            if normalized in {"static", "story", "poster", "hero", "single_frame"}:
                return "static"
            if normalized in {"carousel", "carousel_series", "slide_deck", "slide_series"}:
                return "carousel"
            if normalized in {"infographic", "diagram", "visual_explainer"}:
                return "infographic"
            return normalized if normalized in {"static", "carousel", "infographic"} else ""

        def trusted_reference(item: Any) -> bool:
            # Centralizes trusted reference from item for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if not isinstance(item, dict):
                return False
            role = cls._normalize_metadata_text(item.get("role") or item.get("asset_role"), limit=32).casefold()
            trust = cls._normalize_metadata_text(item.get("trust_level"), limit=32).casefold()
            return (
                role in {"reference_creative", "template", "reference", "template_sequence"}
                and trust not in {"rejected", "blocked", "untrusted"}
            )

        selected_asset: dict[str, Any] = {}
        for item in context.get("reference_asset_brief") or []:
            if not isinstance(item, dict) or not trusted_reference(item):
                continue
            if normalize_family(item.get("format_family") or item.get("format")) == source_family:
                selected_asset = item
                break

        blueprint = (
            structure.get("sample_page_blueprint")
            if isinstance(structure.get("sample_page_blueprint"), dict)
            else profile.get("sample_page_blueprint")
            if isinstance(profile.get("sample_page_blueprint"), dict)
            else selected_asset.get("sample_page_blueprint")
            if isinstance(selected_asset.get("sample_page_blueprint"), dict)
            else runtime_blueprint
            if runtime_blueprint
            else {}
        )
        blueprint = cls._sample_blueprint_without_rendered_footer_text(blueprint) if blueprint else {}
        module_counts = (
            blueprint.get("module_counts")
            if isinstance(blueprint.get("module_counts"), dict)
            else structure.get("module_counts")
            if isinstance(structure.get("module_counts"), dict)
            else profile.get("module_counts")
            if isinstance(profile.get("module_counts"), dict)
            else {}
        )
        visual_permissions = (
            blueprint.get("visual_permissions")
            if isinstance(blueprint.get("visual_permissions"), dict)
            else structure.get("visual_permissions")
            if isinstance(structure.get("visual_permissions"), dict)
            else profile.get("visual_permissions")
            if isinstance(profile.get("visual_permissions"), dict)
            else {}
        )
        layout_dna = (
            structure.get("layout_dna")
            if isinstance(structure.get("layout_dna"), dict)
            else selected_asset.get("layout_dna")
            if isinstance(selected_asset.get("layout_dna"), dict)
            else {}
        )
        composition_logic = (
            structure.get("composition_logic")
            if isinstance(structure.get("composition_logic"), dict)
            else selected_asset.get("composition_logic")
            if isinstance(selected_asset.get("composition_logic"), dict)
            else {}
        )
        structural_cues = (
            structure.get("structural_cues")
            if isinstance(structure.get("structural_cues"), list)
            else selected_asset.get("structural_cues")
            if isinstance(selected_asset.get("structural_cues"), list)
            else []
        )
        template_layout_grammar = (
            structure.get("template_layout_grammar")
            if isinstance(structure.get("template_layout_grammar"), dict)
            else profile.get("template_layout_grammar")
            if isinstance(profile.get("template_layout_grammar"), dict)
            else selected_asset.get("template_layout_grammar")
            if isinstance(selected_asset.get("template_layout_grammar"), dict)
            else {}
        )
        budget = layout_content_budget if isinstance(layout_content_budget, dict) else {}
        surface_contract = (
            context.get("generation_surface_contract")
            if isinstance(context.get("generation_surface_contract"), dict)
            else {}
        )
        surface_budget = surface_contract.get("content_budget") if isinstance(surface_contract.get("content_budget"), dict) else {}

        payload: dict[str, Any] = {
            "selected_output_format": output_family,
            "reference_source_family": source_family,
        }
        if blueprint:
            sample_payload: dict[str, Any] = {}
            for key, limit in (("layout_category", 80), ("density", 40)):
                value = cls._normalize_metadata_text(blueprint.get(key), limit=limit)
                if value:
                    sample_payload[key] = value
            layout_mode = cls._sample_blueprint_layout_mode(blueprint)
            if layout_mode:
                sample_payload["layout_mode"] = layout_mode
            if module_counts:
                sample_payload["module_counts"] = module_counts
            if visual_permissions:
                sample_payload["visual_permissions"] = visual_permissions
            image_zones = blueprint.get("image_zones") if isinstance(blueprint.get("image_zones"), list) else []
            if image_zones:
                sample_payload["image_zones"] = image_zones[:4]
            layout_instruction = cls._sample_blueprint_layout_instruction(blueprint)
            if layout_instruction:
                sample_payload["layout_instruction"] = layout_instruction
            if sample_payload:
                payload["sample_page_blueprint"] = sample_payload
        elif module_counts or visual_permissions:
            payload["sample_page_blueprint"] = {
                **({"module_counts": module_counts} if module_counts else {}),
                **({"visual_permissions": visual_permissions} if visual_permissions else {}),
            }
        if layout_dna:
            compact_layout_dna = cls._compact_prompt_json_value(
                layout_dna,
                max_depth=2,
                list_limit=6,
                text_limit=120,
            )
            if compact_layout_dna:
                payload["layout_dna"] = compact_layout_dna
        if composition_logic:
            compact_composition_logic = cls._compact_prompt_json_value(
                composition_logic,
                max_depth=2,
                list_limit=6,
                text_limit=120,
            )
            if compact_composition_logic:
                payload["composition_logic"] = compact_composition_logic
        if structural_cues:
            payload["structural_cues"] = [
                cls._normalize_metadata_text(item, limit=80)
                for item in structural_cues[:8]
                if cls._normalize_metadata_text(item, limit=80)
            ]
        if template_layout_grammar:
            compact_template_layout_grammar = cls._compact_prompt_json_value(
                template_layout_grammar,
                max_depth=2,
                list_limit=8,
                text_limit=100,
            )
            if compact_template_layout_grammar:
                payload["template_layout_grammar"] = compact_template_layout_grammar
        layout_budget_payload: dict[str, Any] = {}
        for key in (
            "canvas_summary",
            "brand_header_policy",
            "footer_policy",
            "cta_policy",
            "visible_capacity",
            "overflow_count",
            "capacity_source",
            "density_label",
            "density_policy",
        ):
            value = budget.get(key)
            if value not in ("", {}, [], None):
                layout_budget_payload[key] = value
        if layout_budget_payload:
            payload["layout_content_budget"] = layout_budget_payload
        elif surface_budget:
            payload["surface_content_budget"] = surface_budget
        return {key: value for key, value in payload.items() if value not in ("", None, {}, [])}

    @classmethod
    def _reference_template_sample_page_blueprint(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        format_name: str,
    ) -> dict[str, Any]:
        # Centralizes reference template sample page blueprint from compiled context and format name for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        context = compiled_context if isinstance(compiled_context, dict) else {}
        profile = context.get("reference_adaptation_profile") if isinstance(context.get("reference_adaptation_profile"), dict) else {}
        if not profile:
            return {}
        source_family = cls._normalize_metadata_text(profile.get("source_format_family"), limit=32).casefold()
        output_family = (
            cls._normalize_metadata_text(profile.get("output_format_family"), limit=32).casefold()
            or cls._normalize_metadata_text(format_name, limit=32).casefold()
        )
        if source_family != output_family or output_family not in {"static", "infographic"}:
            return {}
        try:
            confidence = float(profile.get("confidence") or 0.0)
        except (TypeError, ValueError):
            confidence = 0.0
        if confidence <= 0.0:
            return {}
        payload = cls._reference_template_structure_payload(
            context,
            profile,
            source_family=source_family,
            output_family=output_family,
        )
        sample = payload.get("sample_page_blueprint") if isinstance(payload.get("sample_page_blueprint"), dict) else {}
        blueprint = (
            profile.get("template_structure", {}).get("sample_page_blueprint")
            if isinstance(profile.get("template_structure"), dict)
            and isinstance(profile.get("template_structure", {}).get("sample_page_blueprint"), dict)
            else profile.get("sample_page_blueprint")
            if isinstance(profile.get("sample_page_blueprint"), dict)
            else {}
        )
        if not blueprint:
            for item in context.get("reference_asset_brief") or []:
                if not isinstance(item, dict):
                    continue
                family = cls._normalize_metadata_text(item.get("format_family") or item.get("format"), limit=32).casefold()
                if family == source_family and isinstance(item.get("sample_page_blueprint"), dict):
                    blueprint = item.get("sample_page_blueprint")
                    break
        if blueprint:
            return cls._sample_blueprint_without_rendered_footer_text(blueprint)
        return sample

    @classmethod
    def _reference_template_adaptation_prompt_sections(
        cls,
        compiled_context: dict[str, Any] | None,
        *,
        format_name: str,
        layout_content_budget: dict[str, Any] | None = None,
        sample_page_blueprint: dict[str, Any] | None = None,
    ) -> list[str]:
        # Builds reference template adaptation prompt sections from compiled context, format name, and layout content budget for AIOrchestratorService.
        # It calls _normalize_metadata_text and _reference_template_structure_payload while assembling the payload or prompt text.
        context = compiled_context if isinstance(compiled_context, dict) else {}
        profile = context.get("reference_adaptation_profile") if isinstance(context.get("reference_adaptation_profile"), dict) else {}
        runtime_blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else {}
        if not profile:
            output_family = cls._normalize_metadata_text(format_name, limit=32).casefold()
            if output_family not in {"static", "infographic"} or not runtime_blueprint:
                return []
            profile = {
                "source_format_family": output_family,
                "output_format_family": output_family,
                "selected_output_behavior": (
                    "infographic_surface_output"
                    if output_family == "infographic"
                    else "static_creative_output"
                ),
                "adaptation_mode": (
                    "infographic_surface_adaptation"
                    if output_family == "infographic"
                    else "static_creative_adaptation"
                ),
                "confidence": 1.0,
                "adapt": {
                    "layout_rhythm": True,
                    "module_density": True,
                    "logo_footer_policy": True,
                    "slide_archetype": True,
                },
                "template_structure": {
                    "sample_page_blueprint": cls._sample_blueprint_without_rendered_footer_text(runtime_blueprint),
                },
            }
        mode = cls._normalize_metadata_text(profile.get("adaptation_mode"), limit=64).casefold()
        if mode in {"", "brand_only", "video_output_disabled_or_unsupported"}:
            return []
        source_family = cls._normalize_metadata_text(profile.get("source_format_family"), limit=32).casefold()
        output_family = (
            cls._normalize_metadata_text(profile.get("output_format_family"), limit=32).casefold()
            or cls._normalize_metadata_text(format_name, limit=32).casefold()
        )
        if source_family not in {"carousel", "infographic", "static"}:
            return []
        if output_family not in {"carousel", "infographic", "static"}:
            return []
        if output_family == "carousel" and source_family == "carousel":
            return []
        try:
            confidence = float(profile.get("confidence") or 0.0)
        except (TypeError, ValueError):
            confidence = 0.0
        if confidence <= 0.0:
            return []

        adapt = profile.get("adapt") if isinstance(profile.get("adapt"), dict) else {}
        active_traits = [
            label
            for key, label in (
                ("layout_rhythm", "layout rhythm"),
                ("module_density", "module density"),
                ("color_roles", "color roles"),
                ("typography_hierarchy", "typography hierarchy"),
                ("image_treatment", "image treatment"),
                ("logo_footer_policy", "logo/header and footer policy"),
                ("slide_archetype", "surface/slide archetype"),
            )
            if adapt.get(key)
        ]
        selected_behavior = cls._normalize_metadata_text(profile.get("selected_output_behavior"), limit=64)
        trait_summary = ", ".join(active_traits) if active_traits else "compatible visual-system traits"
        sections = [
            (
                "Reference template adaptation contract: "
                f"selected output format is {output_family}; reference source family is {source_family}; "
                f"adaptation mode is {mode}; selected output behavior is {selected_behavior or output_family}. "
                f"Apply only these approved visual-system traits: {trait_summary}. "
                "Prompt content and approved evidence remain the subject authority; reference text, claims, topic, and brand marks are not source material."
            ),
            (
                "Reference adaptation safety: preserve layout rhythm, module positions, hierarchy, typography scale roles, color roles, image treatment, object density, logo/header policy, footer/legal policy, spacing rhythm, and composition grammar only where compatible. "
                "This contract does not authorize charts, tables, dashboards, metrics, or data visuals; structured_visual_metadata.data_anchors and sample visual_permissions remain the only data-visual gate."
            ),
        ]
        structure_payload = cls._reference_template_structure_payload(
            context,
            profile,
            source_family=source_family,
            output_family=output_family,
            layout_content_budget=layout_content_budget,
            sample_page_blueprint=runtime_blueprint,
        )
        if structure_payload and source_family == output_family and output_family in {"static", "infographic"}:
            compact_structure = json.dumps(structure_payload, ensure_ascii=True, separators=(",", ":"))
            infographic_structure_guidance = (
                "For infographic outputs, preserve repeated card/row/panel rhythm, section cadence, connector/callout spacing, and vertical explanatory flow before adding new decorative ideas. "
                if output_family == "infographic"
                else ""
            )
            sections.append(
                f"{output_family.title()} template structure lock JSON: {compact_structure}. "
                "Treat sample_page_blueprint, template_layout_grammar, module_counts, visual_permissions, image_zones, and layout_content_budget as the prompt-visible layout payload. "
                "Map the current prompt content into the observed template modules; preserve macro flow, section ordering, card/panel/box shells, row/column rhythm, connector/callout/takeaway behavior, visual density, spacing roles, typography roles, color roles, and safe-area posture before adding new decorative ideas. "
                f"{infographic_structure_guidance}"
                "Replace the reference topic, copy, claims, numbers, image objects, icons, and assets with prompt-relevant content and prompt-relevant replacement visuals. "
                "If content exceeds visible capacity, compress or summarize within the observed layout instead of changing module counts, inventing unsupported regions, or copying reference source text."
            )
            sections.append(
                f"{output_family.title()} fallback takeover guard: any fallback photo, illustration, object, or human-subject visual can only fill an approved image zone or large-visual slot from the template payload. "
                "It must not become the whole composition, turn into an unrelated scenic, cartographic, dashboard, or hero redesign, erase headline/support/proof/CTA surfaces, remove card/panel/row/callout shells, or override header/logo and footer safe areas."
            )
        elif structure_payload and source_family != output_family:
            compact_structure = json.dumps(structure_payload, ensure_ascii=True, separators=(",", ":"))
            sections.append(
                f"Compatible reference template structure JSON: {compact_structure}. "
                "Use this only as compatible layout DNA; selected output format remains authoritative and current prompt content remains the subject authority."
            )
        if output_family == "static":
            if source_family == "static":
                sections.append(
                    "Static template adaptation: strongly adapt the trusted static reference as a single finished poster/hero system. "
                    "Preserve header/body/CTA/footer hierarchy, dominant-message path, image placement and focal object structure, whitespace, object density, color-role logic, typography-role hierarchy, logo/header policy, and footer/legal safe zones."
                )
            else:
                sections.append(
                    "Static cross-format adaptation: selected Static output remains a single static creative. "
                    "Use the reference only as compatible visual-system guidance; do not force carousel pagination, multi-slide sequence logic, infographic row structure, or a diagram-first surface unless the selected static layout and prompt explicitly support it."
                )
        elif output_family == "infographic":
            if source_family == "infographic":
                sections.append(
                    "Infographic template adaptation: strongly adapt the trusted infographic surface grammar. "
                    "Preserve section hierarchy, module grouping, row/column rhythm, connector/arrow relationship logic, callout/card/panel hierarchy, compact typography, diagram-first composition, color-role logic, and header/footer safe zones."
                )
            else:
                sections.append(
                    "Infographic cross-format adaptation: selected Infographic output remains an infographic surface. "
                    "Use the reference only for compatible visual rhythm, hierarchy, and craft; do not collapse into a generic static poster, a plain hero image, or carousel pagination."
                )
        elif output_family == "carousel" and source_family != "carousel":
            sections.append(
                "Carousel cross-format adaptation: selected Carousel output remains a slide sequence. "
                "Translate compatible reference rhythm into slide-safe modules and recurring visual grammar; do not let a static reference cause single-frame takeover or an infographic reference force one dense all-in-one surface."
            )
        return sections

    @classmethod
    def _visual_quality_bar_for_treatment(cls, structured_visual_metadata: Any) -> str:
        # Checks visual quality bar treatment from structured visual metadata for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        metadata = structured_visual_metadata if isinstance(structured_visual_metadata, dict) else {}
        preference = metadata.get("visual_treatment_preference") if isinstance(metadata.get("visual_treatment_preference"), dict) else {}
        treatment = cls._normalize_metadata_text(preference.get("preferred"), limit=40).casefold()
        allowed = bool(preference.get("allowed", True))
        if not allowed:
            return (
                "Visual quality bar: clean, modern, brand-safe composition that avoids the blocked visual treatment and keeps the message clear."
            )
        if treatment in {"3d", "isometric"}:
            return (
                "Visual quality bar: polished dimensional treatment with controlled lighting, coherent materials, clean negative space, and brand-safe accents."
            )
        if treatment == "photo_composite":
            return (
                "Visual quality bar: polished photographic or composite-led scene with credible subject matter, natural lighting, and clean copy-safe space."
            )
        if treatment == "data_module":
            return (
                "Visual quality bar: sparse data/module composition tied only to approved anchors, with clean hierarchy and no invented metrics."
            )
        if treatment == "icon_system":
            return (
                "Visual quality bar: crisp icon-system or pictorial explainer with intentional spacing, consistent stroke/detail language, and restrained brand accents."
            )
        if treatment in {"2d", "flat_editorial"}:
            return (
                "Visual quality bar: polished flat/editorial composition with strong hierarchy, refined surfaces, clean negative space, and brand-safe accents."
            )
        if treatment == "document_evidence":
            return (
                "Visual quality bar: credible document/evidence-led composition with clean hierarchy, realistic context cues, and no fabricated values."
            )
        return (
            "Visual quality bar: premium, high-end, richly detailed brand-led imagery with elegant negative space, clear hierarchy, and brand-safe color accents."
        )

    @classmethod
    def _dynamic_layout_content_budget(
        cls,
        *,
        request: AIOrchestrationRequest | None = None,
        format_name: str = "",
        text_payload: StructuredTextPayload | None = None,
        slide: dict[str, Any] | None = None,
        metadata: dict[str, Any] | None = None,
        sample_page_blueprint: Any = None,
        scene_graph: GenerationSceneGraph | None = None,
        legal_footer_text: Any = "",
        disclaimer_guidance: Any = "",
        logo_position_hint: str = "",
    ) -> dict[str, Any]:
        # Centralizes dynamic layout content budget from request state, format name, and copy payload for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        payload = text_payload
        slide_payload = slide if isinstance(slide, dict) else {}
        meta = metadata if isinstance(metadata, dict) else {}
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else (
            meta.get("sample_page_blueprint") if isinstance(meta.get("sample_page_blueprint"), dict) else {}
        )
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        permissions = cls._sample_visual_permissions(blueprint)

        def count_int(key: str) -> int:
            # Counts count int from key for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            try:
                return int(counts.get(key) or 0)
            except (TypeError, ValueError):
                return 0

        studio_panel = getattr(request, "studio_panel", {}) if request is not None else {}
        studio_panel = studio_panel if isinstance(studio_panel, dict) else {}
        size = studio_panel.get("size") if isinstance(studio_panel.get("size"), dict) else {}
        canvas = getattr(scene_graph, "canvas", None)
        width = int(getattr(canvas, "width", None) or size.get("width") or 0)
        height = int(getattr(canvas, "height", None) or size.get("height") or 0)
        aspect_ratio = round(width / height, 3) if width and height else 0
        aspect_label = (
            "landscape"
            if aspect_ratio > 1.15
            else "portrait"
            if aspect_ratio and aspect_ratio < 0.95
            else "square"
            if aspect_ratio
            else "unknown"
        )
        canvas_summary = f"{width}x{height} {aspect_label}" if width and height else f"{aspect_label} canvas"

        prompt_text = getattr(request, "prompt", "") if request is not None else ""
        legal_footer = cls._normalize_metadata_text(legal_footer_text, limit=420)
        disclaimer = cls._normalize_metadata_text(disclaimer_guidance, limit=180)
        brief = getattr(request, "research_editorial_brief", {}) if request is not None else {}
        brief = brief if isinstance(brief, dict) else {}
        footer_band_count = count_int("footer_band_count")
        footer_requested = bool(
            legal_footer
            or disclaimer
            or brief.get("disclaimer_requested")
            or ("disclaimer" in str(prompt_text or "").casefold())
            or footer_band_count > 0
        )
        footer_source = (
            "sample_page_blueprint footer_band_count"
            if footer_band_count > 0
            else "legal/disclaimer metadata"
            if footer_requested
            else "none"
        )
        footer_policy = (
            f"reserve footer/disclaimer safe area before content and CTA using {footer_source}; "
            "leave exact bottom strip completely blank: no text/icons/objects/charts/cards/shadows/patterns/details"
            if footer_requested
            else "no footer/disclaimer safe area requested"
        )
        footer_safe_area = (
            calculate_footer_safe_area(
                canvas_width=width,
                canvas_height=height,
                footer_text=legal_footer or disclaimer,
            )
            if footer_requested and width and height
            else calculate_footer_safe_area(
                canvas_width=width or 1,
                canvas_height=height or 1,
                footer_text="",
            )
        )
        footer_reserved_height = int(footer_safe_area.get("reserved_height") or 0)
        content_canvas_height = int(footer_safe_area.get("content_canvas_height") or height or 0)
        content_safe_ratio = (
            max(0.0, min(content_canvas_height / max(float(height), 1.0), 1.0))
            if height
            else 1.0
        )
        if footer_requested and footer_reserved_height:
            footer_policy = (
                f"{footer_policy}; actual footer_safe_area reserves {footer_reserved_height}px "
                f"({footer_safe_area.get('reserved_ratio')}) and leaves {content_canvas_height}px content-safe height"
            )
        format_key = str(format_name or "").strip().casefold()
        if footer_requested and format_key == "infographic":
            footer_policy = " ".join(
                [
                    footer_policy,
                    cls._infographic_footer_safe_layout_contract(
                        format_name=format_key,
                        footer_requested=True,
                        footer_safe_area=footer_safe_area,
                    ),
                ]
            ).strip()

        cta_text = cls._normalize_metadata_text(
            getattr(payload, "cta", "") if payload is not None else slide_payload.get("cta") or meta.get("cta"),
            limit=90,
        )
        zones: list[Any] = []
        for key in ("zones", "image_zones", "content_zones"):
            value = blueprint.get(key)
            if isinstance(value, list):
                zones.extend(value)
        has_cta_slot = count_int("cta_count") > 0 or any(
            isinstance(zone, dict)
            and "cta" in " ".join(
                cls._normalize_metadata_text(zone.get(key), limit=60)
                for key in ("role", "name", "label", "zone_role")
            ).casefold()
            for zone in zones
        )
        cta_allowed = permissions.get("cta_allowed", True) is not False
        cta_render_allowed = bool(cta_text and cta_allowed and (has_cta_slot or (not footer_requested and not blueprint)))
        if cta_text and not cta_allowed:
            cta_policy = "no approved CTA slot: omit CTA shell or downgrade to a quiet approved content module outside footer/disclaimer"
        elif cta_text and footer_requested and not has_cta_slot:
            cta_policy = "no safe CTA slot above reserved footer/disclaimer: omit CTA shell or downgrade to text-only intent outside the image"
        elif cta_text and footer_requested:
            cta_policy = "CTA may render only in a CTA-safe slot above the reserved footer/disclaimer safe area; omit if no such slot exists"
        elif cta_text and blueprint and not has_cta_slot:
            cta_policy = "CTA has no explicit blueprint slot; render only if the active layout already reserves a CTA-safe zone"
        elif cta_text:
            cta_policy = "CTA must stay inside an approved CTA-safe slot"
        else:
            cta_policy = "no CTA requested; do not invent CTA button"

        raw_items: list[tuple[str, str]] = []

        def add_line(source: str, value: Any) -> None:
            # Extracts add line from source and input value for AIOrchestratorService.
            # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
            text = cls._normalize_metadata_text(value, limit=180)
            if text:
                raw_items.append((source, text))

        for source, value in (
            ("proof_points", slide_payload.get("proof_points", meta.get("proof_points"))),
            ("body_points", slide_payload.get("body_points", meta.get("body_points"))),
            ("stat_highlights", slide_payload.get("stat_highlights", meta.get("stat_highlights"))),
        ):
            for item in cls._normalize_metadata_list(value, limit=10):
                add_line(source, item)
        for item in cls._claim_evidence_pair_lines(
            cls._normalize_claim_evidence_pairs(
                slide_payload.get("claim_evidence_pairs", meta.get("claim_evidence_pairs")),
                limit=4,
            ),
            limit=4,
        ):
            add_line("claim_evidence_pairs", item)

        blocked_keys = {
            cls._normalize_metadata_text(item, limit=180).casefold()
            for item in [
                getattr(payload, "headline", "") if payload is not None else slide_payload.get("headline"),
                slide_payload.get("supporting_line") or meta.get("supporting_line"),
                getattr(payload, "body", "") if payload is not None else slide_payload.get("body"),
                cta_text,
            ]
            if cls._normalize_metadata_text(item, limit=180)
        }
        deduped_items: list[tuple[str, str]] = []
        seen: set[str] = set()
        duplicate_count = 0
        for source, text in raw_items:
            key = text.casefold()
            if key in blocked_keys:
                continue
            if key in seen:
                duplicate_count += 1
                continue
            seen.add(key)
            deduped_items.append((source, text))

        capacity_candidates: list[tuple[str, int]] = []
        for key in ("horizontal_band_count", "card_like_count", "small_icon_like_count", "text_block_count"):
            count_value = count_int(key)
            if count_value > 0:
                capacity_candidates.append((key, count_value))
        if capacity_candidates:
            capacity_key, capacity_value = capacity_candidates[0]
            content_safe_capacity = int((capacity_value * content_safe_ratio) + 0.5) if footer_requested else capacity_value
            visible_capacity = max(1, min(content_safe_capacity, capacity_value, 8))
            capacity_source = (
                f"sample_page_blueprint module_counts.{capacity_key} after reserving brand header and footer/disclaimer safe areas"
            )
        else:
            reserved_segments = (1 if footer_requested else 0) + (1 if cta_render_allowed else 0) + (1 if request is not None else 0)
            raw_capacity = max(len(deduped_items) - reserved_segments, 1) if deduped_items else 1
            content_safe_capacity = int((raw_capacity * content_safe_ratio) + 0.5) if footer_requested else raw_capacity
            visible_capacity = max(1, min(content_safe_capacity, raw_capacity, 8))
            capacity_source = "deduped approved item count after reserving brand header, CTA, and footer/disclaimer safe areas"
        if footer_requested and format_key == "infographic" and visible_capacity > 3:
            visible_capacity = 3
            capacity_source = f"{capacity_source} with infographic footer-safe cap"
        visible_item_pairs = deduped_items[:visible_capacity]
        visible_items = [text for _, text in visible_item_pairs]
        overflow_items = [text for _, text in deduped_items[visible_capacity:]]

        headline = cls._normalize_metadata_text(
            getattr(payload, "headline", "") if payload is not None else slide_payload.get("headline"),
            limit=220,
        )
        body = cls._normalize_metadata_text(
            getattr(payload, "body", "") if payload is not None else slide_payload.get("body"),
            limit=360,
        )
        supporting = cls._normalize_metadata_text(slide_payload.get("supporting_line") or meta.get("supporting_line"), limit=220)
        text_pressure = len(headline.split()) + len(body.split()) + len(supporting.split()) + (len(deduped_items) * 8)
        if cta_text:
            text_pressure += 6
        if footer_requested:
            text_pressure += 10
        if request is not None:
            text_pressure += 6
        density_label = (
            "high"
            if text_pressure > 70 or overflow_items
            else "medium"
            if text_pressure > 42 or len(deduped_items) > visible_capacity
            else "low"
        )
        density_policy = (
            f"text density={density_label}; if density is medium/high, compress copy surfaces, prioritize strongest approved points, and reduce visual complexity before rendering"
        )

        logo_policy = "brand header/logo slot=not resolved"
        if request is not None:
            resolved_logo_hint = (
                cls._sample_blueprint_logo_position(blueprint)
                or cls._normalize_logo_position_option(logo_position_hint)
                or cls._effective_logo_position_hint(
                    request=request,
                    creative_decision=CreativeDecisionPayload(),
                    text_payload={"metadata": meta},
                )
            )
            logo_geometry = cls._normalize_logo_safe_zone_geometry(
                request=request,
                geometry=cls._logo_safe_zone_geometry(scene_graph),
                hint=resolved_logo_hint,
            )
            logo_x, logo_y, logo_width, logo_height = logo_geometry
            logo_left_pct = max(int(round(logo_x * 100)), 0)
            logo_top_pct = max(int(round(logo_y * 100)), 0)
            logo_width_pct = max(int(round(logo_width * 100)), 1)
            logo_height_pct = max(int(round(logo_height * 100)), 1)
            logo_left_px = max(int(round(logo_x * max(width, 1))), 0) if width else 0
            logo_top_px = max(int(round(logo_y * max(height, 1))), 0) if height else 0
            logo_width_px = max(int(round(logo_width * max(width, 1))), 1) if width else 0
            logo_height_px = max(int(round(logo_height * max(height, 1))), 1) if height else 0
            footprint_detail = (
                f"initial footprint starts around {logo_left_pct}% left/{logo_top_pct}% top "
                f"({logo_left_px}px/{logo_top_px}px) and measures {logo_width_pct}% x {logo_height_pct}% "
                f"({logo_width_px}px wide x {logo_height_px}px tall); "
                if width and height
                else f"initial footprint starts around {logo_left_pct}% left/{logo_top_pct}% top and measures {logo_width_pct}% x {logo_height_pct}%; "
            )
            logo_policy = (
                f"brand header slot=required readable reserved slot at {cls._logo_reserved_area_label(resolved_logo_hint)} "
                f"using pre-generation backend brand-asset size analysis; {footprint_detail}"
                "reserve this exact blank space before composing the AI image so the exact stored brand asset can be composited, and keep placement consistent across carousel slides unless the sample blueprint overrides it"
            )

        prompt_metadata = {
            "proof_points": visible_items,
            "body_points": [],
            "stat_highlights": [],
            "claim_evidence_pairs": [],
            "layout_content_budget": {
                "visible_capacity": visible_capacity,
                "visible_count": len(visible_items),
                "overflow_count": len(overflow_items),
                "capacity_source": capacity_source,
                "footer_reserved": footer_requested,
                "footer_safe_area": {
                    key: value
                    for key, value in footer_safe_area.items()
                    if not str(key).startswith("_")
                },
                "cta_render_allowed": cta_render_allowed,
                "density": density_label,
            },
        }
        return {
            "canvas_summary": canvas_summary,
            "brand_header_policy": logo_policy,
            "footer_policy": footer_policy,
            "cta_policy": cta_policy,
            "cta_render_allowed": cta_render_allowed,
            "visible_capacity": visible_capacity,
            "visible_items": visible_items,
            "overflow_items": overflow_items,
            "overflow_count": len(overflow_items),
            "capacity_source": capacity_source,
            "duplicate_count": duplicate_count,
            "density_label": density_label,
            "density_policy": density_policy,
            "prompt_metadata": prompt_metadata,
            "prompt_cta": cta_text if cta_render_allowed else "",
        }

    @classmethod
    def _prompt_visible_slide_from_layout_budget(
        cls,
        slide: dict[str, Any],
        layout_budget: dict[str, Any] | None,
    ) -> dict[str, Any]:
        # Builds prompt visible slide layout budget from slide and layout budget for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        if not isinstance(layout_budget, dict):
            return slide
        prompt_slide = dict(slide)
        prompt_slide["proof_points"] = list(layout_budget.get("visible_items") or [])
        prompt_slide["body_points"] = []
        prompt_slide["stat_highlights"] = []
        prompt_slide["claim_evidence_pairs"] = []
        prompt_slide["cta"] = layout_budget.get("prompt_cta", prompt_slide.get("cta"))
        metadata = dict(prompt_slide.get("metadata") if isinstance(prompt_slide.get("metadata"), dict) else {})
        metadata["layout_content_budget"] = (layout_budget.get("prompt_metadata") or {}).get("layout_content_budget", {})
        prompt_slide["metadata"] = metadata
        return prompt_slide

    @classmethod
    def _prompt_visible_text_payload_from_layout_budget(
        cls,
        text_payload: StructuredTextPayload,
        layout_budget: dict[str, Any] | None,
    ) -> StructuredTextPayload:
        # Builds prompt visible text layout budget from copy payload and layout budget for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        if not isinstance(layout_budget, dict):
            return text_payload
        metadata = dict(text_payload.metadata or {})
        metadata.update(layout_budget.get("prompt_metadata") or {})
        return StructuredTextPayload(
            headline=text_payload.headline,
            body=text_payload.body,
            cta=layout_budget.get("prompt_cta", text_payload.cta),
            hashtags=list(text_payload.hashtags or []),
            metadata=metadata,
        )

    @classmethod
    def _dynamic_layout_quality_guard_section(
        cls,
        *,
        request: AIOrchestrationRequest | None = None,
        format_name: str = "",
        text_payload: StructuredTextPayload | None = None,
        metadata: dict[str, Any] | None = None,
        sample_page_blueprint: Any = None,
        structured_visual_metadata: Any = None,
        data_visualization_anchor_lines: list[str] | None = None,
        visual_intent: Any = "",
        visual_plan: dict[str, Any] | None = None,
        visual_identity: Any = None,
        reference_assets: Any = None,
        asset_catalog: Any = None,
        scene_graph: GenerationSceneGraph | None = None,
        legal_footer_text: Any = "",
        disclaimer_guidance: Any = "",
        layout_budget: dict[str, Any] | None = None,
    ) -> str:
        # Builds dynamic layout quality guard section from request state, format name, and copy payload for AIOrchestratorService.
        # It calls _sample_visual_permissions and _normalize_metadata_text while assembling the payload or prompt text.
        payload = text_payload
        meta = metadata if isinstance(metadata, dict) else {}
        blueprint = sample_page_blueprint if isinstance(sample_page_blueprint, dict) else (
            meta.get("sample_page_blueprint") if isinstance(meta.get("sample_page_blueprint"), dict) else {}
        )
        structured = structured_visual_metadata if isinstance(structured_visual_metadata, dict) else {}
        visual_plan = visual_plan if isinstance(visual_plan, dict) else {}
        budget = layout_budget if isinstance(layout_budget, dict) else {}
        permissions = cls._sample_visual_permissions(blueprint)
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}

        studio_panel = getattr(request, "studio_panel", {}) if request is not None else {}
        studio_panel = studio_panel if isinstance(studio_panel, dict) else {}
        size = studio_panel.get("size") if isinstance(studio_panel.get("size"), dict) else {}
        canvas = getattr(scene_graph, "canvas", None)
        width = int(
            getattr(canvas, "width", None)
            or size.get("width")
            or 0
        )
        height = int(
            getattr(canvas, "height", None)
            or size.get("height")
            or 0
        )
        aspect_ratio = round(width / height, 3) if width and height else 0
        aspect_label = (
            "landscape"
            if aspect_ratio > 1.15
            else "portrait"
            if aspect_ratio and aspect_ratio < 0.95
            else "square"
            if aspect_ratio
            else "unknown"
        )
        canvas_summary = f"{width}x{height} {aspect_label}" if width and height else f"{aspect_label} canvas"

        prompt_text = getattr(request, "prompt", "") if request is not None else ""
        legal_footer = cls._normalize_metadata_text(legal_footer_text, limit=420)
        disclaimer = cls._normalize_metadata_text(disclaimer_guidance, limit=180)
        brief = getattr(request, "research_editorial_brief", {}) if request is not None else {}
        brief = brief if isinstance(brief, dict) else {}
        footer_requested = bool(
            legal_footer
            or disclaimer
            or brief.get("disclaimer_requested")
            or ("disclaimer" in str(prompt_text or "").casefold())
        )
        def count_int(key: str) -> int:
            # Counts count int from key for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            try:
                return int(counts.get(key) or 0)
            except (TypeError, ValueError):
                return 0

        footer_band_count = count_int("footer_band_count")
        footer_requested = bool(footer_requested or footer_band_count > 0)
        footer_source = (
            "sample_page_blueprint footer_band_count"
            if footer_band_count > 0
            else "legal/disclaimer metadata"
            if footer_requested
            else "none"
        )
        footer_policy = (
            f"reserve footer/disclaimer safe area before content and CTA using {footer_source}; "
            "leave exact bottom strip completely blank: no text/icons/objects/charts/cards/shadows/patterns/details"
            if footer_requested
            else "no footer/disclaimer safe area requested"
        )

        cta_text = cls._normalize_metadata_text(
            getattr(payload, "cta", "") if payload is not None else meta.get("cta"),
            limit=90,
        )
        zones: list[Any] = []
        for key in ("zones", "image_zones", "content_zones"):
            value = blueprint.get(key)
            if isinstance(value, list):
                zones.extend(value)
        has_cta_slot = count_int("cta_count") > 0 or any(
            isinstance(zone, dict)
            and "cta" in " ".join(
                cls._normalize_metadata_text(zone.get(key), limit=60)
                for key in ("role", "name", "label", "zone_role")
            ).casefold()
            for zone in zones
        )
        cta_allowed = permissions.get("cta_allowed", True) is not False
        if cta_text and not cta_allowed:
            cta_policy = "no approved CTA slot: omit CTA shell or downgrade to a quiet approved content module outside footer/disclaimer"
        elif cta_text and footer_requested:
            cta_policy = "CTA may render only in a CTA-safe slot above the reserved footer/disclaimer safe area; omit if no such slot exists"
        elif cta_text and blueprint and not has_cta_slot:
            cta_policy = "CTA has no explicit blueprint slot; render only if the active layout already reserves a CTA-safe zone"
        elif cta_text:
            cta_policy = "CTA must stay inside an approved CTA-safe slot"
        else:
            cta_policy = "no CTA requested; do not invent CTA button"

        raw_item_lines: list[str] = []
        for key, item_limit in (
            ("proof_points", 10),
            ("body_points", 10),
            ("stat_highlights", 10),
        ):
            raw_item_lines.extend(cls._normalize_metadata_list(meta.get(key), limit=item_limit))
        raw_item_lines.extend(
            cls._claim_evidence_pair_lines(
                cls._normalize_claim_evidence_pairs(meta.get("claim_evidence_pairs"), limit=4),
                limit=4,
            )
        )
        normalized_raw_items = [
            cls._normalize_metadata_text(item, limit=180)
            for item in raw_item_lines
            if cls._normalize_metadata_text(item, limit=180)
        ]
        deduped_items = cls._dedupe_metadata_collection(
            normalized_raw_items,
            blocked_texts=[
                getattr(payload, "headline", "") if payload is not None else "",
                meta.get("supporting_line"),
                getattr(payload, "body", "") if payload is not None else "",
                cta_text,
            ],
            limit=12,
        )
        duplicate_count = max(0, len(normalized_raw_items) - len(deduped_items))

        capacity_candidates: list[tuple[str, int]] = []
        for key in ("horizontal_band_count", "card_like_count", "small_icon_like_count", "text_block_count"):
            try:
                count_value = int(counts.get(key) or 0)
            except (TypeError, ValueError):
                count_value = 0
            if count_value > 0:
                capacity_candidates.append((key, count_value))
        if capacity_candidates:
            capacity_key, capacity_value = capacity_candidates[0]
            visible_capacity = max(1, min(capacity_value, 8))
            capacity_source = f"sample_page_blueprint module_counts.{capacity_key}"
        else:
            visible_capacity = max(1, min(len(deduped_items) or 1, 8))
            capacity_source = "deduped approved item count and active layout density"
        visible_items = deduped_items[:visible_capacity]
        overflow_count = max(0, len(deduped_items) - len(visible_items))
        item_policy = (
            f"visible item capacity={visible_capacity} derived from {capacity_source}; "
            f"render only {len(visible_items)} deduped approved item(s); overflow={overflow_count}; "
            "never render hidden, clipped, or partially visible rows; summarize overflow or carry it to the next slide when supported"
        )
        dedupe_policy = (
            f"dedupe removed {duplicate_count} repeated content item(s); preserve first occurrence order"
            if duplicate_count
            else "dedupe checked approved list/card items; no repeated visible rows"
        )

        headline = cls._normalize_metadata_text(getattr(payload, "headline", "") if payload is not None else "", limit=220)
        body = cls._normalize_metadata_text(getattr(payload, "body", "") if payload is not None else "", limit=360)
        supporting = cls._normalize_metadata_text(meta.get("supporting_line"), limit=220)
        text_pressure = len(headline.split()) + len(body.split()) + len(supporting.split()) + (len(deduped_items) * 8)
        if cta_text:
            text_pressure += 6
        if footer_requested:
            text_pressure += 10
        density_label = (
            "high"
            if text_pressure > 70 or overflow_count > 0
            else "medium"
            if text_pressure > 42 or len(deduped_items) > visible_capacity
            else "low"
        )
        density_policy = (
            f"text density={density_label}; if density is medium/high, compress copy surfaces, prioritize strongest approved points, and reduce visual complexity before rendering"
        )
        brand_header_policy = ""
        if budget:
            canvas_summary = cls._normalize_metadata_text(budget.get("canvas_summary"), limit=80) or canvas_summary
            footer_policy = cls._normalize_metadata_text(budget.get("footer_policy"), limit=720) or footer_policy
            cta_policy = cls._normalize_metadata_text(budget.get("cta_policy"), limit=220) or cta_policy
            visible_capacity = int(budget.get("visible_capacity") or visible_capacity)
            visible_items = list(budget.get("visible_items") or [])
            overflow_count = int(budget.get("overflow_count") or 0)
            capacity_source = cls._normalize_metadata_text(budget.get("capacity_source"), limit=180) or capacity_source
            duplicate_count = int(budget.get("duplicate_count") or 0)
            density_policy = cls._normalize_metadata_text(budget.get("density_policy"), limit=220) or density_policy
            brand_header_policy = cls._normalize_metadata_text(budget.get("brand_header_policy"), limit=520)
            item_policy = (
                f"visible item capacity={visible_capacity} derived from {capacity_source}; "
                f"render only {len(visible_items)} deduped approved item(s); overflow={overflow_count}; "
                "never render hidden, clipped, or partially visible rows; summarize overflow or carry it to the next slide when supported"
            )
            dedupe_policy = (
                f"dedupe removed {duplicate_count} repeated content item(s); preserve first occurrence order"
                if duplicate_count
                else "dedupe checked approved list/card items; no repeated visible rows"
            )

        table_intent = structured.get("table_intent") if isinstance(structured.get("table_intent"), dict) else {}
        chart_intent = structured.get("chart_intent") if isinstance(structured.get("chart_intent"), dict) else {}
        strict_anchors = structured.get("data_anchors") if isinstance(structured.get("data_anchors"), list) else []
        data_requested = bool(table_intent.get("requested") or chart_intent.get("requested"))
        data_allowed = bool(table_intent.get("allowed") or chart_intent.get("allowed"))
        data_lines = data_visualization_anchor_lines if isinstance(data_visualization_anchor_lines, list) else []
        if data_requested and data_allowed and strict_anchors:
            data_policy = f"data visual gate=allowed only from {len(strict_anchors)} strict approved anchor(s); no extra axes, values, percentages, or dashboard furniture"
        elif data_requested or data_lines:
            data_policy = "data visual gate=blocked for chart-like visuals unless strict approved data_anchors allow them; broad layout/body context cannot unlock charts"
        else:
            data_policy = "data visual gate=no chart/table/dashboard grammar requested"

        combined_symbol_text = " ".join(
            part
            for part in [
                prompt_text,
                cls._coerce_text_value(visual_intent),
                headline,
                body,
                supporting,
            ]
            if cls._coerce_text_value(part).strip()
        ).casefold()
        official_symbol_requested = bool(
            re.search(
                r"\b(official|regulated|government|national|flag|seal|emblem|crest|wordmark|brand mark|logo)\b",
                combined_symbol_text,
            )
        )

        def asset_mentions_symbol(asset: Any) -> bool:
            # Extracts asset mentions symbol from asset record for AIOrchestratorService.
            # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
            if not isinstance(asset, dict):
                return False
            fields = [
                asset.get("asset_role"),
                asset.get("name"),
                asset.get("label"),
                asset.get("asset_kind"),
                asset.get("type"),
                asset.get("description"),
                " ".join(str(tag) for tag in asset.get("tags", [])[:6]) if isinstance(asset.get("tags"), list) else "",
            ]
            text = " ".join(cls._normalize_metadata_text(item, limit=90) for item in fields).casefold()
            return bool(re.search(r"\b(flag|seal|emblem|crest|logo|wordmark|brand mark|official)\b", text))

        reference_items = reference_assets if isinstance(reference_assets, list) else []
        catalog_items = asset_catalog if isinstance(asset_catalog, list) else []
        visual_assets = visual_identity.get("reusable_design_assets") if isinstance(visual_identity, dict) else []
        visual_assets = visual_assets if isinstance(visual_assets, list) else []
        approved_symbol_asset = any(asset_mentions_symbol(asset) for asset in [*reference_items, *catalog_items, *visual_assets])
        if official_symbol_requested and approved_symbol_asset:
            official_symbol_policy = "official symbol policy=use only approved uploaded/reference/vector asset evidence; do not freehand exact marks"
        elif official_symbol_requested:
            official_symbol_policy = "official symbol policy=no approved official-symbol asset available; use abstract geographic, institutional, or partnership cues instead of recreating exact flags/logos/marks"
        else:
            official_symbol_policy = "official symbol policy=no exact official mark required"

        plan_mode = cls._normalize_metadata_text(visual_plan.get("mode"), limit=40)
        layout_priority = (
            "layout priority=sample_page_blueprint, visual_permissions, footer/disclaimer safe area, CTA slot, and data-anchor correctness outrank dynamic visual art direction"
            if blueprint
            else "layout priority=active layout slots, footer/disclaimer safe area, CTA slot, and data-anchor correctness outrank dynamic visual art direction"
        )
        return (
            "Layout quality guard: "
            f"canvas={canvas_summary}; format={cls._normalize_metadata_text(format_name, limit=40) or 'unknown'}; "
            f"purpose={plan_mode or 'current slide/page'}; "
            f"{brand_header_policy + '; ' if brand_header_policy else ''}"
            f"safe areas={footer_policy}; "
            f"CTA policy={cta_policy}; "
            f"content budget={item_policy}; "
            f"text density guard={density_policy}; "
            f"duplicate guard={dedupe_policy}; "
            f"{data_policy}; "
            f"{official_symbol_policy}; "
            f"{layout_priority}."
        )

    @classmethod
    def _content_format_guide_prompt_section(
        cls,
        request: AIOrchestrationRequest,
    ) -> str:
        # Builds content guide prompt section from request state for AIOrchestratorService.
        # It calls _normalize_metadata_text and _normalize_metadata_list while assembling the payload or prompt text.
        guide = request.content_format_guide if isinstance(request.content_format_guide, dict) else {}
        if not guide:
            return ""
        summary = cls._normalize_metadata_text(guide.get("summary"), limit=220)
        rules = guide.get("rules") if isinstance(guide.get("rules"), dict) else {}
        format_name = str(request.studio_panel.get("format") or "").strip().lower()
        platform = str(request.studio_panel.get("platform_preset") or "").strip().lower()
        global_rules = guide.get("global_rules") if isinstance(guide.get("global_rules"), list) else []
        selected_rules = cls._normalize_metadata_list(
            [
                *(rules.get(format_name) or []),
                *(rules.get(platform) or []),
                *global_rules,
            ],
            limit=5,
        )
        if not summary and not selected_rules:
            return ""
        return " ".join(
            part
            for part in (
                f"Content format guide summary: {summary}." if summary else "",
                f"Content format guide rules: {'; '.join(selected_rules)}." if selected_rules else "",
            )
            if part
        )

    @classmethod
    def _live_research_verified_facts_prompt_section(
        cls,
        request: AIOrchestrationRequest,
    ) -> str:
        # Builds live research verified facts prompt section from request state for AIOrchestratorService.
        # It calls _data_list_surface_contract and _int_or_none while assembling the payload or prompt text.
        research = request.live_research if isinstance(request.live_research, dict) else {}
        fact_model = research.get("fact_model") if isinstance(research.get("fact_model"), dict) else research
        raw_fact_limit = cls._int_or_none(
            research.get("verified_fact_limit")
            or research.get("fact_limit")
            or fact_model.get("verified_fact_limit")
            or fact_model.get("fact_limit")
        ) or 6
        data_surface_contract = cls._data_list_surface_contract(
            prompt=request.prompt,
            format_name=str(request.studio_panel.get("format") or ""),
            metadata={},
        )
        if isinstance(data_surface_contract, dict):
            raw_fact_limit = max(raw_fact_limit, int(data_surface_contract.get("module_count") or 0))
        fact_limit = max(1, min(raw_fact_limit, 10))
        facts: list[str] = []
        for fact in (fact_model.get("verified_facts") or [])[:fact_limit]:
            if not isinstance(fact, dict):
                continue
            label = cls._normalize_metadata_text(fact.get("label") or fact.get("title"), limit=90)
            value = cls._normalize_metadata_text(fact.get("value") or fact.get("detail"), limit=120)
            source = cls._normalize_metadata_text(fact.get("source_title") or fact.get("source"), limit=80)
            parts = [part for part in (label, value, source) if part]
            if parts:
                facts.append(" - ".join(parts))
        if not facts:
            return ""
        return "Use these externally verified facts exactly; do not invent adjacent values, rankings, dates, or claims: " + "; ".join(facts) + "."

    @staticmethod
    def _strip_blocked_compliance_phrases(value: str) -> str:
        # Strips strip blocked compliance phrases from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        text = str(value or "")
        text = re.sub(r"\bSEBI[-\s]*(?:regulated|licensed|registered|aware)?\b", "", text, flags=re.IGNORECASE)
        text = re.sub(r"\bregulated\b", "", text, flags=re.IGNORECASE)
        text = re.sub(r"\b(?:guaranteed|assured|risk[-\s]*free)\b", "", text, flags=re.IGNORECASE)
        text = re.sub(r"\bwith confidence\b", "", text, flags=re.IGNORECASE)
        text = re.sub(r"\btransparent\s+platform\b", "", text, flags=re.IGNORECASE)
        text = re.sub(r"#SEBI\w*", "", text, flags=re.IGNORECASE)
        text = re.sub(r"\s{2,}", " ", text)
        text = re.sub(r"\s+([,.;:])", r"\1", text)
        return text.strip()

    @staticmethod
    def _scrub_image_prompt_brand_mark_triggers(value: str, replacement: str = "empty space") -> str:
        # Builds scrub image prompt brand mark triggers from input value and replacement for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        text = str(value or "")
        safe_replacement = replacement.strip() or "empty space"
        substitutions = (
            (r"\bcorner[_-]safe[_-]zone[a-z0-9_]*\b", safe_replacement),
            (r"\bcorner[_\s-]*safe[_\s-]*zone(?:s)?\b", safe_replacement),
            (r"\blogo[_\s-]*safe\b", safe_replacement),
            (r"\blogo[_\s-]*(?:like|style|shaped)\b", safe_replacement),
            (r"(?<![a-zA-Z])logos?(?![a-zA-Z])", safe_replacement),
            (r"\bwatermarks?\b", "stray text"),
            (r"\bwatermarked\b", "marked"),
            (r"\bwordmarks?\b", "stray text"),
            (r"\bbrand[_\s-]*marks?\b", "standalone graphics"),
            (r"\bbrandmarks?\b", "standalone graphics"),
            (r"\bbrand[-\s]*name signatures?\b", "brand-name text"),
            (r"\bmonograms?\b", "letter symbols"),
            (r"\bemblems?\b", "symbols"),
            (r"\blockups?\b", "symbol groups"),
        )
        for pattern, substitute in substitutions:
            text = re.sub(pattern, substitute, text, flags=re.IGNORECASE)
        text = re.sub(
            r"legal copy, and empty space after image generation",
            "legal copy, and logo after image generation",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"must contain zero standalone graphics, stray text, brand-name text",
            "must contain zero logos, wordmarks, brand-name signatures",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"The exact stored standalone graphics is composited afterward as a separate asset",
            "The exact stored brand logo is composited afterward as a separate asset",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"Never write or paint ([^.]+?) in the corner-safe area",
            r"Never write or paint \1 in the logo-safe corner",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"never as a standalone graphics, masthead, signature, stray text, standalone standalone graphics, or top-corner stray text",
            "never as a logo, masthead, signature, watermark, standalone brand mark, or top-corner wordmark",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"no ghosted stray text",
            "no ghosted wordmark",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(r"SAMPLE empty space EXCEPTION", "SAMPLE LOGO EXCEPTION", text, flags=re.IGNORECASE)
        text = re.sub(
            r"copy only the empty empty space zone geometry",
            "copy only the empty logo-safe zone geometry",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"Do not copy the sample page's empty space artwork",
            "Do not copy the sample page's logo artwork",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"literal text, empty space, or exact original artwork",
            "literal text, logo, or exact original artwork",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"derived from the (?:empty|reserved) space",
            "derived from the logo",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r"stretching under the (?:empty|reserved) space",
            "stretching under the logo",
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(
            r'"(?:empty|reserved) space_header_policy"',
            '"logo_header_policy"',
            text,
            flags=re.IGNORECASE,
        )
        text = re.sub(r"\s{2,}", " ", text)
        text = re.sub(r"\s+([,.;:])", r"\1", text)
        return text.strip()

    @staticmethod
    def _visual_explanation_guidance(visual_plan: dict[str, Any] | None) -> str:
        # Builds visual explanation guidance from visual plan for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        plan = visual_plan if isinstance(visual_plan, dict) else {}
        mode = str(plan.get("mode") or "minimal_brand_scene").strip().lower()
        need = str(plan.get("need") or "low").strip().lower()
        density = str(plan.get("density") or "low").strip().lower()
        rationale = AIOrchestratorService._normalize_metadata_text(plan.get("rationale"), limit=180)
        guidance_by_mode = {
            "beginner_path": (
                "Use a beginner-friendly path, guided entry, simplified market steps, bond ladder, or confusion-to-clarity progression. "
                "Do not rely on a person holding a coin as the only visual idea."
            ),
            "process_steps": (
                "Use a simple sequence, pathway, checklist flow, or step markers that make the recommended actions understandable at a glance."
            ),
            "comparison": (
                "Use clear contrast zones, side-by-side comparison, or a visual transformation tied to the actual topic. "
                "Do not default to rising bar charts, stock arrows, generic finance dashboards, or a chart/graph-style hero image unless the copy explicitly contains quantitative chart data."
            ),
            "data_cue": (
                "Prefer tables, labeled figures, comparison blocks, ladders, or process markers before defaulting to charts. "
                "Use chart or graph motifs only when the copy includes explicit numeric series, time-based trends, or metric comparisons that truly need charting. "
                "Otherwise do not let a chart, bars, or arrow become the main hero image."
            ),
            "sample_page_adaptation": (
                "Use the selected sample page as the primary visual explanation system. "
                "Match its visible layout category, object count, image/copy balance, module rhythm, and whitespace before adding any chart, dashboard, icon set, or finance metaphor. "
                "Numeric facts may become callouts only when the sample page uses callouts; do not convert them into charts unless the user explicitly asked for charts."
            ),
            "icon_support": (
                "Use a few premium integrated icons, diagram cues, or proof-point symbols only where they clarify the message; keep them secondary. "
                "Do not fall back to stock upward arrows, rising bars, generic finance app tiles, repeated chart stickers, or a graph-like hero image."
            ),
            "minimal_brand_scene": (
                "Keep visual explanation low. Do not force charts, dashboards, random icons, diagrams, or faux UI when the message works as a clean brand scene."
            ),
        }
        mode_guidance = guidance_by_mode.get(mode, guidance_by_mode["minimal_brand_scene"])
        return (
            f"Visual explanation plan: mode={mode}, need={need}, density={density}. "
            f"Rationale: {rationale}. {mode_guidance} "
            "Reference/template fit: if the reference is image-led, place the explanatory visual in the same image-emphasis region; "
            "if it is type-led or minimal, keep explanation subtle and lower density. "
            "Never let reference creatives force generic people imagery when this mode calls for an explainer."
        )

    def normalize_creative_decision_payload(
        self,
        raw: Any,
        fallback: dict[str, Any],
        *,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any],
    ) -> CreativeDecisionPayload:
        # Builds creative decision from raw, fallback payload, and request state for AIOrchestratorService.
        # It calls _resolve_template_candidate_identifier and _coerce_mapping while assembling the payload or prompt text.
        payload = dict(fallback or {})
        if isinstance(raw, dict):
            payload.update(raw)
        payload["layout_mode"] = str(
            payload.get("layout_mode")
            or payload.get("mode")
            or fallback.get("layout_mode")
            or "synthesized_layout"
        )
        if payload["layout_mode"] not in {"exact_template", "adapted_template", "synthesized_layout"}:
            payload["layout_mode"] = "synthesized_layout"
        payload["selected_template_id"] = self._resolve_template_candidate_identifier(
            str(payload.get("selected_template_id") or payload.get("template_id") or "").strip() or None,
            template_name=(
                str(payload.get("template_name") or "").strip()
                or str((payload.get("planning_hints") or {}).get("template_name") or "").strip()
                or str((fallback.get("planning_hints") or {}).get("template_name") or "").strip()
                or None
            ),
            template_candidates=[
                item
                for item in self._coerce_list(payload.get("template_candidates") or request.template_candidates or [])
                if isinstance(item, dict)
            ],
        )
        try:
            payload["confidence"] = max(0.0, min(float(payload.get("confidence", 0.0)), 1.0))
        except (TypeError, ValueError):
            payload["confidence"] = float(fallback.get("confidence") or 0.0)
        payload["reasoning"] = [
            str(item).strip()
            for item in self._coerce_list(payload.get("reasoning") or payload.get("rationale") or fallback.get("reasoning") or [])
            if str(item).strip()
        ]
        payload["adaptations"] = self._coerce_mapping(payload.get("adaptations") or payload.get("adaptation_plan"))
        asset_strategy = self._coerce_mapping(payload.get("asset_strategy") or fallback.get("asset_strategy"))
        for key in ("use_generated_image", "use_template_background", "use_brand_reference_assets", "logo_injection_required"):
            if key in asset_strategy:
                asset_strategy[key] = bool(asset_strategy.get(key))
        if asset_strategy.get("logo"):
            asset_strategy.setdefault("logo_injection_required", True)
        inferred_surface_policy = self._effective_template_surface_policy(
            request,
            creative_decision=None,
            metadata={"asset_strategy": asset_strategy},
        )
        if inferred_surface_policy and not asset_strategy.get("template_surface_policy"):
            asset_strategy["template_surface_policy"] = inferred_surface_policy
        if asset_strategy.get("template_surface_policy") == "style_reference_only":
            asset_strategy["use_template_background"] = False
        major_flags = self._major_asset_strategy_flags(asset_strategy)
        dominant = self._normalized_dominant_visual_system(asset_strategy)
        supporting = self._normalized_supporting_visual_system(asset_strategy)
        if not dominant:
            dominant = next((name for name, enabled in major_flags.items() if enabled), "generated_image")
        asset_strategy["dominant_visual_system"] = dominant
        if supporting:
            asset_strategy["supporting_visual_system"] = supporting
        if dominant == "generated_image":
            asset_strategy["use_generated_image"] = True
            if payload["layout_mode"] == "synthesized_layout" or asset_strategy.get("template_surface_policy") == "style_reference_only":
                asset_strategy["use_template_background"] = False
            if supporting != "reference_assets":
                asset_strategy["use_brand_reference_assets"] = False
            if supporting == "icon_sequence":
                asset_strategy["icon_sequence"] = True
                asset_strategy["use_brand_reference_assets"] = True
        elif dominant == "template_background":
            asset_strategy["use_template_background"] = payload["layout_mode"] in {"exact_template", "adapted_template"}
            asset_strategy["use_generated_image"] = False
            if asset_strategy.get("template_surface_policy") == "style_reference_only":
                asset_strategy["use_template_background"] = False
        elif dominant == "reference_assets":
            asset_strategy["use_brand_reference_assets"] = True
            asset_strategy["use_template_background"] = False
            asset_strategy["use_generated_image"] = False
        elif dominant == "icon_sequence":
            asset_strategy["icon_sequence"] = True
        if supporting == "icon_sequence":
            asset_strategy["icon_sequence"] = True
            if dominant in {"generated_image", "type_led"}:
                asset_strategy["use_brand_reference_assets"] = True
        payload["asset_strategy"] = asset_strategy
        payload["template_candidates"] = [
            item
            for item in self._coerce_list(payload.get("template_candidates") or request.template_candidates or [])
            if isinstance(item, dict)
        ]
        payload["planning_hints"] = {
            **self._coerce_mapping(fallback.get("planning_hints")),
            **self._coerce_mapping(payload.get("planning_hints")),
            "backend_compiled_template_fit": compiled_context.get("template_fit_brief", {}),
        }
        normalized = CreativeDecisionPayload.model_validate(payload)
        sequence_pack = self._template_sequence_pack(request, creative_decision=normalized)
        if isinstance(sequence_pack, dict):
            sequence_surface_policy = str(sequence_pack.get("surface_policy") or "").strip().lower()
            if sequence_surface_policy == "style_reference_only":
                normalized_payload = normalized.model_dump(mode="json")
                normalized_asset_strategy = self._coerce_mapping(normalized_payload.get("asset_strategy"))
                normalized_asset_strategy["template_surface_policy"] = "style_reference_only"
                normalized_asset_strategy["use_template_background"] = False
                normalized_asset_strategy["use_brand_reference_assets"] = True
                dominant_visual_system = self._normalized_dominant_visual_system(normalized_asset_strategy)
                if dominant_visual_system in {"", "template_background"}:
                    normalized_asset_strategy["dominant_visual_system"] = "generated_image"
                    normalized_asset_strategy["use_generated_image"] = True
                if normalized_payload.get("layout_mode") == "exact_template":
                    normalized_payload["layout_mode"] = "adapted_template"
                normalized_payload["asset_strategy"] = normalized_asset_strategy
                normalized_payload["reasoning"] = [
                    *[
                        str(item).strip()
                        for item in self._coerce_list(normalized_payload.get("reasoning") or [])
                        if str(item).strip()
                    ],
                    "Use the ordered reference sequence as story and slide guidance only; do not reuse the sample surface as the literal background.",
                ]
                normalized = CreativeDecisionPayload.model_validate(normalized_payload)
        if self._should_force_sequence_pack(request, creative_decision=normalized):
            normalized_payload = normalized.model_dump(mode="json")
            if normalized.layout_mode == "synthesized_layout":
                normalized_payload["layout_mode"] = "adapted_template"
            normalized_asset_strategy = self._coerce_mapping(normalized_payload.get("asset_strategy"))
            normalized_asset_strategy["use_template_background"] = True
            normalized_asset_strategy["use_brand_reference_assets"] = True
            normalized_asset_strategy["use_generated_image"] = False
            normalized_asset_strategy["template_surface_policy"] = "sequence_pack_locked"
            normalized_asset_strategy.setdefault("dominant_visual_system", "template_background")
            normalized_payload["asset_strategy"] = normalized_asset_strategy
            normalized_payload["reasoning"] = [
                *[
                    str(item).strip()
                    for item in self._coerce_list(normalized_payload.get("reasoning") or [])
                    if str(item).strip()
                ],
                "Forced to ordered reference sequence pack because a locked multi-slide template pack is available for this carousel.",
            ]
            normalized = CreativeDecisionPayload.model_validate(normalized_payload)
        return normalized

    def normalize_message_strategy_payload(
        self,
        raw: Any,
        fallback: dict[str, Any],
    ) -> MessageStrategyPayload:
        # Builds message strategy from raw and fallback payload for AIOrchestratorService.
        # It calls _normalize_metadata_list and _coerce_text_value while assembling the payload or prompt text.
        payload = dict(fallback or {})
        if isinstance(raw, dict):
            payload.update(raw)
        aliases = {
            "primary_campaign_theme": ["primary_campaign_theme", "Primary Campaign Theme"],
            "core_audience_message": ["core_audience_message", "Core Audience Message"],
            "headline_direction": ["headline_direction", "Headline Direction"],
            "supporting_copy_direction": ["supporting_copy_direction", "Supporting Copy Direction"],
            "cta_intent": ["cta_intent", "CTA Intent"],
            "key_value_proposition": ["key_value_proposition", "Key Value Proposition"],
            "important_keywords": ["important_keywords", "Important Keywords/Phrases", "keywords"],
            "emotional_messaging_direction": ["emotional_messaging_direction", "Emotional Messaging Direction"],
            "what_must_be_avoided_in_messaging": [
                "what_must_be_avoided_in_messaging",
                "What Must Be Avoided In Messaging",
                "avoid_messaging",
            ],
        }
        normalized: dict[str, Any] = {}
        for target, keys in aliases.items():
            value = None
            fallback_candidate = None
            for key in keys:
                candidate = payload.get(key) if key in payload else None
                if candidate is None or candidate == "":
                    continue
                if target in {"important_keywords", "what_must_be_avoided_in_messaging"}:
                    candidate_items = self._normalize_metadata_list(candidate, limit=8)
                    if candidate_items and any(item.strip().upper() != "MISSING" for item in candidate_items):
                        value = candidate_items
                        break
                    fallback_candidate = candidate_items
                    continue
                if str(candidate).strip() and str(candidate).strip().upper() != "MISSING":
                    value = candidate
                    break
                fallback_candidate = candidate
            if value is None:
                value = fallback_candidate
            if target in {"important_keywords", "what_must_be_avoided_in_messaging"}:
                normalized[target] = self._normalize_metadata_list(value, limit=8)
            else:
                text = self._coerce_text_value(value).strip()
                normalized[target] = text or self._coerce_text_value(fallback.get(target) or "MISSING")
        if not normalized["important_keywords"]:
            normalized["important_keywords"] = self._normalize_metadata_list(fallback.get("important_keywords"), limit=8) or ["MISSING"]
        if not normalized["what_must_be_avoided_in_messaging"]:
            normalized["what_must_be_avoided_in_messaging"] = (
                self._normalize_metadata_list(fallback.get("what_must_be_avoided_in_messaging"), limit=8) or ["MISSING"]
            )
        return MessageStrategyPayload.model_validate(normalized)

    @classmethod
    def _should_use_image_led_social(
        cls,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any],
    ) -> bool:
        # Checks use image led social from request state and compiled context for AIOrchestratorService.
        # It reuses _request_requires_ai_final_render and _effective_template_surface_policy so related checks follow the same rule.
        ai_final_render_required = cls._request_requires_ai_final_render(request)
        if not cls._image_generation_requested(request) and not ai_final_render_required:
            return False
        platform = str(request.studio_panel.get("platform_preset") or "").strip().lower()
        format_name = str(request.studio_panel.get("format") or "").strip().lower()
        if platform not in {"instagram", "linkedin", "x", "youtube_thumbnail"}:
            return False
        if format_name not in cls.AI_FINAL_RENDER_FORMATS:
            return False
        if cls._should_force_sequence_pack(request) and not ai_final_render_required:
            return False
        planning_hints = request.layout_decision or {}
        mode = str(planning_hints.get("mode") or "").strip().lower()
        review_flags = {
            str(flag).strip().lower()
            for flag in cls._coerce_list(planning_hints.get("review_flags") or [])
            if str(flag).strip()
        }
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            metadata=planning_hints if isinstance(planning_hints, dict) else {},
        )
        template_fit_brief = compiled_context.get("template_fit_brief", {}) or {}
        template_confidence = 0.0
        try:
            template_confidence = float(template_fit_brief.get("confidence") or 0.0)
        except (TypeError, ValueError):
            template_confidence = 0.0
        if format_name in cls.TEMPLATE_LOCKED_IMAGE_LED_FORMATS and not ai_final_render_required:
            if mode == "exact_template" and not review_flags and template_surface_policy != "style_reference_only":
                return False
            if template_confidence >= 0.9 and mode == "exact_template" and template_surface_policy != "style_reference_only":
                return False
        return True

    @staticmethod
    def _image_generation_size(studio_panel: dict[str, Any]) -> str:
        # Builds image generation size from studio settings for AIOrchestratorService.
        # The returned payload is the compact contract consumed by the next branch.
        size = studio_panel.get("size") or {}
        width = int(size.get("width") or 1080)
        height = int(size.get("height") or 1080)
        if width >= height * 1.2:
            return "1536x1024"
        if height >= width * 1.2:
            return "1024x1536"
        return "1024x1024"

    @staticmethod
    def _canvas_fit_guidance(studio_panel: dict[str, Any]) -> str:
        # Builds canvas fit guidance from studio settings for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        size = studio_panel.get("size") or {}
        width = int(size.get("width") or 1080)
        height = int(size.get("height") or 1080)
        if width <= 0 or height <= 0:
            width, height = 1080, 1080
        orientation = "square"
        if width >= height * 1.15:
            orientation = "landscape"
        elif height >= width * 1.15:
            orientation = "portrait"
        return (
            f"Canvas fit: design for the requested {width}x{height} {orientation} output ratio. "
            "Keep every headline, body line, proof point, CTA, corner-safe area, and visual focal subject fully inside a centered target-aspect safe frame with at least 8% inner margin. "
            "Never place CTA buttons, bullets, faces, hands, or key objects on the outer edge; if text feels tight, reduce copy density and scale down the layout instead of letting anything run below or outside the canvas."
        )

    @classmethod
    def _generation_to_export_fit_guidance(cls, studio_panel: dict[str, Any]) -> str:
        # Builds generation export fit guidance from studio settings for AIOrchestratorService.
        # It calls _requested_canvas_dimensions and _image_generation_size while assembling the payload or prompt text.
        target_width, target_height = cls._requested_canvas_dimensions(studio_panel)
        generation_size = cls._image_generation_size(studio_panel)
        try:
            generation_width, generation_height = [int(part) for part in generation_size.lower().split("x", 1)]
        except (TypeError, ValueError):
            generation_width, generation_height = target_width, target_height
        target_ratio = target_width / max(target_height, 1)
        generation_ratio = generation_width / max(generation_height, 1)
        aspect_delta = abs(generation_ratio - target_ratio) / max(target_ratio, 0.01)
        if aspect_delta < 0.06:
            return (
                f"Generation-to-export fit: model canvas {generation_size} closely matches the final {target_width}x{target_height} export. "
                "Keep all modules, visual anchors, and overlay-safe surfaces inside the requested canvas."
            )
        return (
            f"Generation-to-export fit: model canvas {generation_size} will be center-fitted into the final {target_width}x{target_height} export. "
            "Keep every important module, face, object, divider, card shell, and overlay-safe surface inside the central target-aspect safe frame; avoid critical detail near the model-canvas edges."
        )

    @classmethod
    def _should_use_ai_final_render(
        cls,
        request: AIOrchestrationRequest,
        generation_path: str,
        creative_decision: CreativeDecisionPayload,
    ) -> bool:
        # Checks use ai final from request state, generation path, and creative decision for AIOrchestratorService.
        # It reuses _effective_template_surface_policy and _request_requires_ai_final_render so related checks follow the same rule.
        if generation_path != "image_led_social":
            return False
        if cls._request_requires_ai_final_render(request) and cls._image_generation_requested(request):
            return True
        layout_mode = str(getattr(creative_decision, "layout_mode", "") or "").strip().lower()
        request_layout = request.layout_decision if isinstance(request.layout_decision, dict) else {}
        request_mode = str(request_layout.get("mode") or "").strip().lower()
        request_template_context = request.template_context if isinstance(request.template_context, dict) else {}
        request_sequence_pack = (
            request_template_context.get("sequence_pack")
            if isinstance(request_template_context.get("sequence_pack"), dict)
            else {}
        )
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=request_layout,
        )
        if (
            cls._request_requires_ai_final_render(request)
            and (
                request_mode in {"adapted_template", "exact_template"}
                or str(request_sequence_pack.get("surface_policy") or "").strip().lower() == "style_reference_only"
            )
        ):
            return True
        if not cls._image_generation_requested(request):
            return False
        return cls._request_requires_ai_final_render(request)

    @classmethod
    def _image_generation_requested(cls, request: AIOrchestrationRequest) -> bool:
        # Builds image generation requested from request state for AIOrchestratorService.
        # It calls _request_requires_ai_final_render while assembling the payload or prompt text.
        if not bool(request.generate_image):
            format_name = str(request.studio_panel.get("format") or "").strip().lower()
            template_context = request.template_context if isinstance(request.template_context, dict) else {}
            sequence_pack = template_context.get("sequence_pack") if isinstance(template_context.get("sequence_pack"), dict) else {}
            surface_policy = str(sequence_pack.get("surface_policy") or "").strip().lower()
            if format_name == "carousel" and surface_policy == "style_reference_only":
                return True
            return False
        if cls._request_requires_ai_final_render(request):
            return True
        return bool(request.generate_image)

    @classmethod
    def _request_requires_ai_final_render(cls, request: AIOrchestrationRequest) -> bool:
        # Centralizes request requires ai final from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        format_name = str(request.studio_panel.get("format") or "").strip().lower()
        file_type = str(request.studio_panel.get("file_type") or "").strip().lower()
        return format_name in cls.AI_FINAL_RENDER_FORMATS and file_type in {"png", "jpg", "pdf", "doc"}

    @staticmethod
    def _ai_final_render_failure_message() -> str:
        # Centralizes ai final failure message for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return "AI final render failed and backend fallback rendering is disabled for this format."

    @staticmethod
    def _resolve_logo_box(scene_graph: GenerationSceneGraph) -> tuple[float, float, float, float] | None:
        # Resolves logo box from scene graph for AIOrchestratorService.
        # The caller receives the canonical identifier/path/config value expected by the next step.
        for element in scene_graph.elements:
            if not element.visible or element.role != "logo":
                continue
            geometry = element.geometry
            if (
                geometry.x is None
                or geometry.y is None
                or geometry.width is None
                or geometry.height is None
            ):
                continue
            return (
                float(geometry.x),
                float(geometry.y),
                float(geometry.width),
                float(geometry.height),
            )
        return None

    @classmethod
    def _build_logo_mask_bytes(
        cls,
        *,
        scene_graph: GenerationSceneGraph,
        width: int,
        height: int,
    ) -> bytes | None:
        # Builds logo mask bytes from scene graph, width, and height for AIOrchestratorService.
        # It calls _resolve_logo_box while assembling the payload or prompt text.
        box = cls._resolve_logo_box(scene_graph)
        if box is None:
            left, top, box_width, box_height = 0.76, 0.06, 0.18, 0.1
        else:
            left, top, box_width, box_height = box
        x0 = max(int(left * width), 0)
        y0 = max(int(top * height), 0)
        x1 = min(int((left + box_width) * width), width)
        y1 = min(int((top + box_height) * height), height)
        if x1 <= x0 or y1 <= y0:
            return None

        mask = Image.new("RGBA", (width, height), (0, 0, 0, 255))
        draw = ImageDraw.Draw(mask)
        draw.rounded_rectangle((x0, y0, x1, y1), radius=max(min(x1 - x0, y1 - y0) // 5, 8), fill=(0, 0, 0, 0))
        buffer = BytesIO()
        mask.save(buffer, format="PNG")
        return buffer.getvalue()

    def _apply_ai_logo_edit_pass(
        self,
        *,
        image_provider,
        tenant_id,
        brand_space_id,
        prompt: str,
        base_asset_payload: dict[str, Any],
        logo_storage_path: str | None,
        scene_graph: GenerationSceneGraph,
        size: str | None,
        trace_id: str | None,
        trace_label: str,
    ) -> tuple[dict[str, Any], bool]:
        # Applies apply ai logo edit pass from image provider, tenant id, and brand space id for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not logo_storage_path or not self.storage.exists(logo_storage_path):
            return base_asset_payload, False
        base_storage_path = str(base_asset_payload.get("storage_path") or "").strip()
        if not base_storage_path or not self.storage.exists(base_storage_path):
            return base_asset_payload, False
        width = int(base_asset_payload.get("width") or 0)
        height = int(base_asset_payload.get("height") or 0)
        if width <= 0 or height <= 0:
            return base_asset_payload, False
        mask_png_bytes = self._build_logo_mask_bytes(
            scene_graph=scene_graph,
            width=width,
            height=height,
        )
        if not mask_png_bytes:
            return base_asset_payload, False
        try:
            edited_asset = self._edit_image_with_retries(
                image_provider=image_provider,
                tenant_id=tenant_id,
                brand_space_id=brand_space_id,
                prompt=(
                    f"{prompt}\n\n"
                    "Use the exact provided brand logo in the reserved logo area only. "
                    "Preserve the composition, copy layout, and overall design. "
                    "Do not add extra text, icons, or substitute marks."
                ),
                image_paths=[
                    self.storage.absolute_path(base_storage_path),
                    self.storage.absolute_path(logo_storage_path),
                ],
                size=size,
                mask_png_bytes=mask_png_bytes,
                trace_id=trace_id,
                trace_label=trace_label,
            )
            return edited_asset, True
        except Exception as exc:  # pragma: no cover - resilience path
            self._trace_payload(
                trace_id,
                self.trace,
                f"{trace_label}_error",
                {
                    "error": str(exc),
                    "logo_storage_path": logo_storage_path,
                    "base_storage_path": base_storage_path,
                },
            )
            return base_asset_payload, False

    def _fallback_image_led_scene_graph(
        self,
        *,
        request: AIOrchestrationRequest,
        text_payload: dict[str, Any],
        creative_decision: dict[str, Any],
        compiled_context: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes fallback image led scene graph from request state, copy payload, and creative decision for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        size = request.studio_panel.get("size") or BlueprintService.PRESET_DIMENSIONS.get(
            request.studio_panel.get("platform_preset", "instagram"),
            BlueprintService.PRESET_DIMENSIONS["instagram"],
        )
        palette_roles = dict((compiled_context.get("brand_visual_brief", {}) or {}).get("palette_roles", {}) or {})
        proof_points = list((text_payload.get("metadata") or {}).get("proof_points") or [])

        # Extract font sizes from brand context (from analyzed reference samples)
        font_sizes = self._extract_font_sizes_from_brand_context(request.resolved_brand_context)

        layout_profile = self._image_led_layout_profile(
            request=request,
            text_payload=text_payload,
            compiled_context=compiled_context,
        )
        elements: list[dict[str, Any]] = [
            {
                "element_id": "background",
                "element_type": "background",
                "role": "background",
                "layer": "background",
                "geometry": self._normalized_box(0, 0, 1, 1),
                "style": {
                    "fill_role": "background",
                    "primary_fill": palette_roles.get("background") or palette_roles.get("surface"),
                    "gradient_from": palette_roles.get("background") or palette_roles.get("surface"),
                    "gradient_to": palette_roles.get("primary"),
                },
            },
            {
                "element_id": "hero_image",
                "element_type": "image",
                "role": "image",
                "layer": "primary_visual",
                "geometry": layout_profile["hero_image"],
                "style": {"fit": "cover", "border_radius": 28},
                "asset": {"asset_role": "ai_image"},
            },
            {
                "element_id": "overlay_panel",
                "element_type": "decorative_shape",
                "role": "decorative_shape",
                "layer": "decorative",
                "geometry": layout_profile["overlay_panel"],
                "style": {
                    "shape": "rounded_rect",
                    "fill_role": "surface",
                    "fill": "#FFFFFF",
                    "border_radius": 32,
                },
            },
            *layout_profile.get("decorative_shapes", []),
            {
                "element_id": "logo",
                "element_type": "logo",
                "role": "logo",
                "layer": "brand",
                "geometry": layout_profile["logo"],
                "style": {"fit": "contain"},
                "asset": {"asset_role": "logo", "trust_level": "trusted"},
            },
            {
                "element_id": "headline",
                "element_type": "text",
                "role": "headline",
                "layer": "content",
                "geometry": layout_profile["headline"],
                "text": text_payload.get("headline", ""),
                "style": {"font_size": font_sizes["headline"], "font_role": "heading", "fill_role": "primary", "max_lines": 3},
            },
            {
                "element_id": "supporting_line",
                "element_type": "text",
                "role": "supporting_line",
                "layer": "content",
                "geometry": layout_profile["supporting_line"],
                "text": (text_payload.get("metadata") or {}).get("supporting_line") or text_payload.get("body", ""),
                "style": {"font_size": font_sizes["body"], "font_role": "body", "fill_role": "secondary_text", "max_lines": 4},
            },
            {
                "element_id": "proof_points",
                "element_type": "text",
                "role": "proof_points",
                "layer": "content",
                "geometry": layout_profile["proof_points"],
                "text": proof_points[:4] or self._sentences(text_payload.get("body", ""))[:4],
                "style": {"font_size": 20, "font_role": "body", "fill_role": "secondary_text", "max_lines": 5},
            },
            {
                "element_id": "cta",
                "element_type": "text",
                "role": "cta",
                "layer": "brand",
                "geometry": layout_profile["cta"],
                "text": text_payload.get("cta", ""),
                "style": {
                    "font_size": 22,
                    "font_role": "cta",
                    "fill_role": "light_text",
                    "background_fill_role": "primary",
                    "max_lines": 2,
                },
            },
        ]
        return {
            "layout_mode": creative_decision.get("layout_mode") or "synthesized_layout",
            "confidence": creative_decision.get("confidence") or 0.72,
            "canvas": {
                "width": int(size.get("width") or 1080),
                "height": int(size.get("height") or 1080),
                "platform": request.studio_panel.get("platform_preset", "instagram"),
                "file_type": request.studio_panel.get("file_type", "png"),
                "safe_margin": 52,
            },
            "layers": ["background", "primary_visual", "decorative", "content", "brand", "footer"],
            "styles": {
                "layout_type": "image_led_social",
                "layout_archetype": layout_profile["layout_archetype"],
            },
            "assets": [],
            "template_adaptation": {
                **self._coerce_mapping(creative_decision.get("adaptations")),
                "selected_template_id": creative_decision.get("selected_template_id"),
            },
            "validation_hints": {
                "overlay_style": "image_led_social",
                "template_surface_policy": "style_reference_only",
                "layout_type": layout_profile["layout_type"],
            },
            "elements": elements,
        }

    @classmethod
    def _repair_element_slide_prefix(cls, element: dict[str, Any]) -> int | None:
        # Repairs element slide prefix from element for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        raw_id = str(element.get("element_id") or "").strip().lower()
        if not raw_id:
            return None
        match = re.match(r"slide[_-]?(\d{1,2})(?:[_-]|$)", raw_id)
        if not match:
            return None
        try:
            value = int(match.group(1))
        except (TypeError, ValueError):
            return None
        return value if value > 0 else None

    @classmethod
    def _filter_multislide_repair_elements(
        cls,
        repair_elements: list[dict[str, Any]],
        existing_elements: list[dict[str, Any]],
        repair_attempt: int,
    ) -> list[dict[str, Any]]:
        # Centralizes filter multislide elements from repair elements, existing elements, and repair attempt for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        slide_prefixes = {
            prefix
            for element in repair_elements
            for prefix in [cls._repair_element_slide_prefix(element)]
            if prefix is not None
        }
        if len(slide_prefixes) <= 1:
            return repair_elements

        existing_prefixes = [
            prefix
            for element in existing_elements
            for prefix in [cls._repair_element_slide_prefix(element)]
            if prefix is not None
        ]
        active_prefix = existing_prefixes[0] if existing_prefixes else min(slide_prefixes)
        filtered: list[dict[str, Any]] = []
        dropped = 0
        for element in repair_elements:
            prefix = cls._repair_element_slide_prefix(element)
            if prefix is not None and prefix != active_prefix:
                dropped += 1
                continue
            filtered.append(element)
        if dropped:
            logger.warning(
                "Repair %s: Dropped %s multi-slide repair elements outside active slide %s",
                repair_attempt,
                dropped,
                active_prefix,
            )
        return filtered

    @classmethod
    def _merge_repair_into_scene_graph(
        cls,
        existing_scene_graph: dict[str, Any],
        repair_scene_graph: dict[str, Any],
        repair_attempt: int,
    ) -> dict[str, Any]:
        # Merges into scene graph from existing scene graph, repair scene graph, and repair attempt for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        """Merge repair response into existing scene graph instead of replacing it.

        LLM may return only the repaired elements, not the complete scene graph.
        This method merges repaired elements by element_id into the existing scene graph.

        Args:
            existing_scene_graph: Current complete scene graph
            repair_scene_graph: Repair response scene graph (may be partial)
            repair_attempt: Current repair attempt number for logging

        Returns:
            Merged scene graph with repairs applied
        """
        if not repair_scene_graph:
            logger.warning(f"Repair {repair_attempt}: Empty repair scene graph, keeping existing")
            return existing_scene_graph

        # Start with existing scene graph
        merged = existing_scene_graph.copy()

        # Check if repair has elements array
        repair_elements = repair_scene_graph.get("elements", [])
        existing_elements = merged.get("elements", [])

        if not repair_elements:
            logger.warning(f"Repair {repair_attempt}: No elements in repair response, keeping existing {len(existing_elements)} elements")
            return merged
        repair_elements = cls._filter_multislide_repair_elements(
            repair_elements,
            existing_elements,
            repair_attempt,
        )

        # Build element_id index of repaired elements
        repaired_by_id = {}
        for elem in repair_elements:
            elem_id = elem.get("element_id")
            if elem_id:
                repaired_by_id[elem_id] = elem

        if not repaired_by_id:
            logger.warning(f"Repair {repair_attempt}: No element_ids in repair response, treating as complete scene graph replacement")
            # If no element_ids, repair is likely a complete scene graph, not a partial update
            merged["elements"] = repair_elements
            logger.info(f"Repair {repair_attempt}: Replaced scene graph with {len(repair_elements)} elements")
        else:
            primary_text_roles = {
                "headline",
                "supporting_line",
                "body",
                "proof_points",
                "cta",
                "stat_highlights",
                "section_label",
                "footer",
                "legal",
            }
            existing_role_counts: dict[str, int] = {}
            existing_role_indexes: dict[str, list[int]] = {}
            for index, elem in enumerate(existing_elements):
                role = str(elem.get("role") or "").strip().lower()
                if not role:
                    continue
                existing_role_counts[role] = existing_role_counts.get(role, 0) + 1
                existing_role_indexes.setdefault(role, []).append(index)

            repair_role_counts: dict[str, int] = {}
            for elem in repair_elements:
                role = str(elem.get("role") or "").strip().lower()
                if role:
                    repair_role_counts[role] = repair_role_counts.get(role, 0) + 1

            # Merge: update existing elements that have matching IDs
            merged_count = 0
            for i, elem in enumerate(existing_elements):
                elem_id = elem.get("element_id")
                if elem_id and elem_id in repaired_by_id:
                    # Replace with repaired version
                    existing_elements[i] = repaired_by_id[elem_id]
                    merged_count += 1
                    logger.info(f"Repair {repair_attempt}: Merged repair for element '{elem_id}'")
                    del repaired_by_id[elem_id]

            # Add any new elements from repair that weren't in existing
            added_count = 0
            for new_elem in repaired_by_id.values():
                role = str(new_elem.get("role") or "").strip().lower()
                if role in primary_text_roles:
                    if repair_role_counts.get(role, 0) > 1 and existing_role_counts.get(role, 0) >= 1:
                        logger.warning(
                            "Repair %s: Skipping suspicious duplicate repaired text role '%s' to avoid multi-section scene drift",
                            repair_attempt,
                            role,
                        )
                        continue
                    existing_indexes = existing_role_indexes.get(role, [])
                    if len(existing_indexes) == 1 and repair_role_counts.get(role, 0) == 1:
                        replacement = dict(new_elem)
                        replacement["element_id"] = existing_elements[existing_indexes[0]].get("element_id") or replacement.get("element_id")
                        existing_elements[existing_indexes[0]] = replacement
                        merged_count += 1
                        logger.info(
                            "Repair %s: Merged repaired '%s' by role into existing element '%s'",
                            repair_attempt,
                            role,
                            replacement.get("element_id"),
                        )
                        continue
                existing_elements.append(new_elem)
                new_elem_id = new_elem.get("element_id", "unknown")
                logger.info(f"Repair {repair_attempt}: Added new element '{new_elem_id}' from repair")
                added_count += 1

            merged["elements"] = existing_elements
            logger.info(
                f"Repair {repair_attempt}: Merged {merged_count} elements, added {added_count} new elements, "
                f"final count: {len(existing_elements)} elements"
            )

        # Merge other top-level properties (styles, canvas, confidence, etc.)
        for key in ("styles", "canvas", "layout_mode", "confidence", "layers"):
            if key in repair_scene_graph:
                merged[key] = repair_scene_graph[key]
                logger.debug(f"Repair {repair_attempt}: Updated scene_graph.{key}")

        return merged

    def normalize_scene_graph_payload(
        self,
        raw: Any,
        *,
        fallback: dict[str, Any],
        creative_decision: CreativeDecisionPayload,
        text_payload: dict[str, Any],
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any],
        allow_recovery: bool = True,
    ) -> GenerationSceneGraph:
        # Builds scene graph from raw, fallback payload, and creative decision for AIOrchestratorService.
        # It calls _coerce_mapping and _normalize_scene_graph_layers while assembling the payload or prompt text.
        payload = dict(fallback or {})
        if isinstance(raw, dict):
            payload.update(raw)
        payload["layout_mode"] = creative_decision.layout_mode
        raw_layers = payload.get("layers")
        raw_assets = payload.get("assets") or fallback.get("assets") or []
        canvas = self._coerce_mapping(payload.get("canvas"))
        size = request.studio_panel.get("size") or {}
        canvas.setdefault("width", int(size.get("width") or 1080))
        canvas.setdefault("height", int(size.get("height") or 1080))
        canvas.setdefault("platform", request.studio_panel.get("platform_preset", "instagram"))
        canvas.setdefault("file_type", request.studio_panel.get("file_type", "png"))
        canvas.setdefault("safe_margin", 48)
        payload["canvas"] = canvas
        try:
            payload["confidence"] = float(payload.get("confidence") or creative_decision.confidence or 0.0)
        except (TypeError, ValueError):
            payload["confidence"] = float(creative_decision.confidence or 0.0)
        payload["layers"] = self._normalize_scene_graph_layers(raw_layers or self.DEFAULT_LAYERS)
        if not payload["layers"]:
            payload["layers"] = list(self.DEFAULT_LAYERS)
        asset_catalog = self._normalize_scene_graph_assets(raw_assets)
        payload["assets"] = asset_catalog
        payload["template_adaptation"] = {
            **self._coerce_mapping(payload.get("template_adaptation")),
            **self._coerce_mapping(creative_decision.adaptations),
            "selected_template_id": creative_decision.selected_template_id,
        }
        payload["styles"] = {
            **self._coerce_mapping(fallback.get("styles")),
            **self._coerce_mapping(payload.get("styles")),
            "copy_snapshot": {
                "headline": text_payload.get("headline", ""),
                "body": text_payload.get("body", ""),
                "cta": text_payload.get("cta", ""),
            },
            "brand_visual_brief": compiled_context.get("brand_visual_brief", {}),
        }
        payload["validation_hints"] = {
            **self._coerce_mapping(fallback.get("validation_hints")),
            **self._coerce_mapping(payload.get("validation_hints")),
        }
        allowed_font_families = self._compiled_font_families(compiled_context)
        elements = payload.get("elements")
        source_elements = self._extract_scene_graph_source_elements(
            elements_source=elements,
            layers_source=raw_layers,
            fallback_source=(fallback or {}).get("elements") or [],
        )
        normalized_elements: list[dict[str, Any]] = []
        role_counts: dict[str, int] = {}
        seen_element_ids: set[str] = set()
        for item in self._coerce_list(source_elements):
            if not isinstance(item, dict):
                continue
            normalized = dict(item)
            source_key = str(normalized.pop("_source_key", "") or "").strip()
            if normalized.get("element_id") is None and normalized.get("id") is not None:
                normalized["element_id"] = normalized.get("id")
            if normalized.get("element_type") is None and normalized.get("type") is not None:
                normalized["element_type"] = normalized.get("type")
            if normalized.get("text") is None and source_key in {"headline", "supporting_line", "body", "proof_points", "cta", "footer", "legal", "section_label", "stat_highlights"}:
                source_value = item.get("text")
                if source_value is None:
                    source_value = item.get("value")
                if source_value is None:
                    source_value = item.get("content")
                normalized["text"] = source_value
            element_name_hint = self._sanitize_scene_element_name(
                normalized.get("element_id") or normalized.get("id") or normalized.get("role") or source_key
            )
            role_name = self._infer_scene_element_role(normalized, source_key=source_key)
            role_key = re.sub(r"[^a-z0-9]+", "_", role_name.casefold()).strip("_") or "element"
            role_counts[role_key] = role_counts.get(role_key, 0) + 1
            normalized.setdefault("role", role_key)
            normalized.setdefault("element_type", self._default_scene_element_type(normalized))
            normalized["element_type"] = self._normalize_scene_element_type(normalized.get("element_type"))
            normalized.setdefault(
                "element_id",
                element_name_hint or (role_key if role_counts[role_key] == 1 else f"{role_key}_{role_counts[role_key]}"),
            )
            element_id = str(normalized.get("element_id") or "").strip()
            if not element_id:
                element_id = role_key if role_counts[role_key] == 1 else f"{role_key}_{role_counts[role_key]}"
                normalized["element_id"] = element_id
            if element_id in seen_element_ids:
                continue
            seen_element_ids.add(element_id)
            raw_layer_value = normalized.get("layer")
            numeric_layer_index: int | None = None
            if isinstance(raw_layer_value, (int, float)) and not isinstance(raw_layer_value, bool):
                numeric_layer_index = int(raw_layer_value)
                if 0 <= numeric_layer_index < len(payload["layers"]):
                    normalized["layer"] = payload["layers"][numeric_layer_index]
                else:
                    normalized["layer"] = str(numeric_layer_index)
            elif raw_layer_value is not None:
                normalized["layer"] = str(raw_layer_value)
            normalized["geometry"] = self._normalize_scene_element_geometry(normalized)
            if numeric_layer_index is not None and normalized["geometry"].get("z_index") is None:
                normalized["geometry"]["z_index"] = numeric_layer_index
            normalized["text"] = self._normalize_scene_element_text(normalized)
            normalized["validation_hints"] = self._coerce_mapping(normalized.get("validation_hints"))
            normalized["style"] = self._normalize_text_style_for_brand(
                self._normalize_scene_element_style(normalized),
                role=str(normalized.get("role") or normalized.get("element_type") or ""),
                allowed_font_families=allowed_font_families,
                validation_hints=normalized["validation_hints"],
            )
            asset_payload = self._coerce_mapping(normalized.get("asset"))
            if not asset_payload:
                for candidate_key in filter(None, [source_key, role_key, str(normalized.get("element_id") or "").strip()]):
                    candidate_asset = self._coerce_mapping(raw_assets.get(candidate_key) if isinstance(raw_assets, dict) else {})
                    if candidate_asset:
                        asset_payload = candidate_asset
                        break
            for key in ("asset_id", "asset_role", "storage_path", "trust_level", "variant", "notes"):
                if key not in asset_payload and normalized.get(key) is not None:
                    asset_payload[key] = normalized.get(key)
            normalized["asset"] = self._sanitize_scene_element_asset_payload(
                asset_payload,
                element=normalized,
                request=request,
            )
            normalized_elements.append(normalized)
        payload["elements"] = self._sync_scene_graph_copy_from_text_payload(
            elements=normalized_elements,
            text_payload=text_payload,
        )
        payload["elements"] = self._finalize_logo_scene_policy(
            scene_graph_payload=payload,
            elements=payload["elements"],
            request=request,
            text_payload=text_payload,
            creative_decision=creative_decision,
        )
        payload = self._apply_design_system_scene_defaults(
            scene_graph_payload=payload,
            request=request,
        )
        payload = self._apply_reference_family_scene_defaults(
            payload,
            compiled_context=compiled_context,
        )
        payload = self._apply_sequence_pack_scene_defaults(
            payload,
            request=request,
            creative_decision=creative_decision,
        )
        # Fix 2: Inject legal disclaimers from brand assets
        payload["elements"] = self._inject_legal_disclaimers(
            elements=payload["elements"],
            request=request,
            canvas=canvas,
        )
        # Fix 3: Apply CTA template styling to CTA elements
        payload["elements"] = self._apply_cta_template_styling(
            elements=payload["elements"],
            request=request,
        )
        # Fix 4: Apply component motif patterns (numbered badges, background boxes)
        payload["elements"] = self._apply_component_motif_patterns(
            elements=payload["elements"],
            request=request,
        )
        # Fix 5: Remap template colors to brand palette
        payload = self._remap_template_colors_to_brand_palette(
            scene_graph=payload,
            compiled_context=compiled_context,
            planning_hints=request.layout_decision if isinstance(request.layout_decision, dict) else None,
        )
        payload = self._collapse_stacked_carousel_scene_graph(
            payload,
            request=request,
        )
        payload["elements"] = self._clamp_normalized_scene_graph_bounds(payload.get("elements"))
        try:
            return GenerationSceneGraph.model_validate(payload)
        except ValidationError:
            if not allow_recovery:
                raise
            logger.warning(
                "orchestrator.scene_graph_normalization_recovered brand_space_id=%s format=%s",
                request.brand_space_id,
                request.studio_panel.get("format"),
                exc_info=True,
            )
            recovery_payload = self._fallback_image_led_scene_graph(
                request=request,
                text_payload=text_payload,
                creative_decision=creative_decision.model_dump(mode="json"),
                compiled_context=compiled_context,
            )
            return self.normalize_scene_graph_payload(
                recovery_payload,
                fallback=fallback,
                creative_decision=creative_decision,
                text_payload=text_payload,
                request=request,
                compiled_context=compiled_context,
                allow_recovery=False,
            )

    @classmethod
    def _clamp_normalized_scene_graph_bounds(cls, elements: Any) -> list[dict[str, Any]]:
        # Centralizes clamp scene graph bounds from elements for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        clamped_elements: list[dict[str, Any]] = []
        for item in cls._coerce_list(elements):
            if not isinstance(item, dict):
                continue
            element = dict(item)
            geometry = dict(element.get("geometry") if isinstance(element.get("geometry"), dict) else {})
            if str(geometry.get("units") or "normalized").strip().casefold() == "normalized":
                try:
                    x = float(geometry.get("x") if geometry.get("x") is not None else 0.0)
                    y = float(geometry.get("y") if geometry.get("y") is not None else 0.0)
                    width = float(geometry.get("width") if geometry.get("width") is not None else 0.01)
                    height = float(geometry.get("height") if geometry.get("height") is not None else 0.01)
                except (TypeError, ValueError):
                    x, y, width, height = 0.0, 0.0, 0.01, 0.01
                x = max(0.0, min(x, 0.99))
                y = max(0.0, min(y, 0.99))
                width = max(0.01, min(width, 1.0 - x))
                height = max(0.01, min(height, 1.0 - y))
                geometry.update(
                    {
                        "x": round(x, 4),
                        "y": round(y, 4),
                        "width": round(width, 4),
                        "height": round(height, 4),
                    }
                )
                element["geometry"] = geometry
            if isinstance(element.get("elements"), list):
                element["elements"] = cls._clamp_normalized_scene_graph_bounds(element.get("elements"))
            clamped_elements.append(element)
        return clamped_elements

    def _extract_scene_graph_source_elements(
        self,
        *,
        elements_source: Any,
        layers_source: Any,
        fallback_source: Any,
    ) -> list[dict[str, Any]]:
        # Extracts scene graph source elements from elements source, layers source, and fallback source for AIOrchestratorService.
        # It calls _sanitize_scene_element_name and _coerce_list to turn raw evidence into the structured signal the caller needs.
        if isinstance(elements_source, list) and elements_source:
            return [dict(item) for item in elements_source if isinstance(item, dict)]

        layer_items = [dict(item) for item in self._coerce_list(layers_source) if isinstance(item, dict)]
        layer_keys = {
            self._sanitize_scene_element_name(
                str(item.get("role") or item.get("id") or item.get("element_id") or item.get("type") or "")
            )
            for item in layer_items
        }
        layer_keys.discard("")

        extracted: list[dict[str, Any]] = []
        if isinstance(elements_source, dict):
            for key, value in elements_source.items():
                source_key = self._sanitize_scene_element_name(key)
                if isinstance(value, dict):
                    candidate = dict(value)
                    candidate.setdefault("_source_key", key)
                    extracted.append(candidate)
                    continue
                if source_key in {"headline", "supporting_line", "body", "proof_points", "cta", "footer", "legal", "section_label", "stat_highlights"}:
                    if source_key in layer_keys:
                        continue
                    text_value = self._coerce_text_value(value).strip()
                    if not text_value:
                        continue
                    extracted.append({"_source_key": key, "role": source_key, "text": value})
        for item in layer_items:
            extracted.append(item)
        if extracted:
            return extracted
        if isinstance(fallback_source, list) and fallback_source:
            return [dict(item) for item in fallback_source if isinstance(item, dict)]
        if isinstance(fallback_source, dict):
            return self._extract_scene_graph_source_elements(
                elements_source=fallback_source,
                layers_source=[],
                fallback_source=[],
            )
        return []

    @classmethod
    def _carousel_slide_index_from_element_id(cls, value: Any) -> int | None:
        # Centralizes carousel slide index element id from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        token = cls._sanitize_scene_element_name(value)
        if not token:
            return None
        match = re.match(r"slide_(\d+)_", token)
        if not match:
            return None
        try:
            return int(match.group(1))
        except (TypeError, ValueError):
            return None

    @classmethod
    def _geometry_frame_from_elements(cls, elements: list[dict[str, Any]]) -> dict[str, float] | None:
        # Measures geometry frame elements from elements for AIOrchestratorService.
        # It calls _coerce_mapping to turn raw evidence into the structured signal the caller needs.
        geometry_candidates: list[dict[str, float]] = []
        preferred_roles = {"content_card", "panel", "overlay_panel", "card"}
        for element in elements:
            geometry = cls._coerce_mapping(element.get("geometry"))
            try:
                x = float(geometry.get("x"))
                y = float(geometry.get("y"))
                width = float(geometry.get("width"))
                height = float(geometry.get("height"))
            except (TypeError, ValueError):
                continue
            if width <= 0 or height <= 0:
                continue
            candidate = {"x": x, "y": y, "width": width, "height": height}
            if str(element.get("role") or "").strip().casefold() in preferred_roles:
                return candidate
            geometry_candidates.append(candidate)
        if not geometry_candidates:
            return None
        min_x = min(item["x"] for item in geometry_candidates)
        min_y = min(item["y"] for item in geometry_candidates)
        max_x = max(item["x"] + item["width"] for item in geometry_candidates)
        max_y = max(item["y"] + item["height"] for item in geometry_candidates)
        width = max_x - min_x
        height = max_y - min_y
        if width <= 0 or height <= 0:
            return None
        return {"x": min_x, "y": min_y, "width": width, "height": height}

    @classmethod
    def _localize_geometry_to_frame(
        cls,
        geometry: dict[str, Any],
        frame: dict[str, float],
    ) -> dict[str, Any]:
        # Measures localize geometry frame from geometry and frame for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        localized = dict(geometry or {})
        try:
            x = float(localized.get("x"))
            y = float(localized.get("y"))
            width = float(localized.get("width"))
            height = float(localized.get("height"))
            frame_x = float(frame.get("x"))
            frame_y = float(frame.get("y"))
            frame_width = float(frame.get("width"))
            frame_height = float(frame.get("height"))
        except (TypeError, ValueError):
            return localized
        if frame_width <= 0 or frame_height <= 0:
            return localized
        localized["x"] = round((x - frame_x) / frame_width, 4)
        localized["y"] = round((y - frame_y) / frame_height, 4)
        localized["width"] = round(width / frame_width, 4)
        localized["height"] = round(height / frame_height, 4)
        localized["units"] = "normalized"
        return localized

    @classmethod
    def _collapse_stacked_carousel_scene_graph(
        cls,
        payload: dict[str, Any],
        *,
        request: AIOrchestrationRequest,
    ) -> dict[str, Any]:
        # Centralizes collapse stacked carousel scene graph from payload and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if str(request.studio_panel.get("format") or "").strip().lower() != "carousel":
            return payload
        elements = [dict(item) for item in cls._coerce_list(payload.get("elements")) if isinstance(item, dict)]
        if not elements:
            return payload
        slide_groups: dict[int, list[dict[str, Any]]] = {}
        shared_elements: list[dict[str, Any]] = []
        for element in elements:
            slide_index = cls._carousel_slide_index_from_element_id(element.get("element_id"))
            if slide_index is None:
                shared_elements.append(dict(element))
                continue
            slide_groups.setdefault(slide_index, []).append(dict(element))
        if len(slide_groups) < 2:
            return payload
        first_slide_index = min(slide_groups)
        first_slide_elements = [dict(item) for item in slide_groups.get(first_slide_index, [])]
        frame = cls._geometry_frame_from_elements(first_slide_elements)
        if frame is None:
            return payload

        collapsed_elements: list[dict[str, Any]] = []
        for element in shared_elements:
            role = str(element.get("role") or "").strip().casefold()
            element_id = str(element.get("element_id") or "").strip().casefold()
            if role not in {"background", "logo", "legal", "footer", "decorative_shape"}:
                continue
            if element_id.startswith("final_"):
                continue
            collapsed_elements.append(dict(element))
        for element in first_slide_elements:
            localized = dict(element)
            geometry = cls._coerce_mapping(localized.get("geometry"))
            if geometry:
                localized["geometry"] = cls._localize_geometry_to_frame(geometry, frame)
            collapsed_elements.append(localized)
        if not collapsed_elements:
            return payload

        styles = cls._coerce_mapping(payload.get("styles"))
        validation_hints = cls._coerce_mapping(payload.get("validation_hints"))
        styles.setdefault("carousel_scene_scope", "slide_1_localized")
        validation_hints["carousel_scene_scope"] = "slide_1_localized"
        payload["elements"] = collapsed_elements
        payload["styles"] = styles
        payload["validation_hints"] = validation_hints
        return payload

    @classmethod
    def _sanitize_scene_element_name(cls, value: Any) -> str:
        # Sanitizes scene element name from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return re.sub(r"[^a-z0-9]+", "_", str(value or "").strip().casefold()).strip("_")

    @classmethod
    def _infer_scene_element_role(cls, element: dict[str, Any], *, source_key: str = "") -> str:
        # Centralizes infer scene element role from element and source key for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        candidate_values = [
            element.get("role"),
            source_key,
            element.get("element_id"),
            element.get("id"),
            element.get("text_role"),
            element.get("asset_role"),
            element.get("type"),
            element.get("element_type"),
        ]
        for candidate in candidate_values:
            token = cls._sanitize_scene_element_name(candidate)
            if not token:
                continue
            if token in {"headline", "heading", "heading_sans", "title"}:
                return "headline"
            if token in {"supporting_line", "subheadline", "supporting_copy", "subhead"}:
                return "supporting_line"
            if token in {"body", "body_text", "body_sans", "copy"}:
                return "body"
            if token in {"proof_points", "bullet_list", "checklist", "stat_highlights"}:
                return "proof_points" if token != "stat_highlights" else "stat_highlights"
            if token in {"cta", "cta_button", "text_button", "button"}:
                return "cta"
            if token in {"logo", "logo_placeholder", "brand_logo", "corner_safe_zone", "brand_mark", "corner_safe", "safe_zone", "empty_corner", "asset_zone", "brand_space", "reserved_zone"}:
                return "logo"
            if token in {"background", "color_fill"}:
                return "background"
            if token in {"image", "hero_image", "primary_image", "hero_visual", "photo_hero", "generated_image"}:
                return "image"
            if token in {"icon", "icons"}:
                return "icon"
            if token in {"decorative_shape", "background_shape", "micro_design_element", "footer_decor", "enhancement_component", "vector_shape", "shape", "minimal_accent", "circle_accent", "overlay_panel"}:
                return "decorative_shape"
            if "headline" in token:
                return "headline"
            if token.endswith("_image") or "image" in token or "hero" in token:
                return "image"
            if "logo" in token:
                return "logo"
        return "element"

    @classmethod
    def _normalize_scene_element_type(cls, value: Any) -> str:
        # Normalizes scene element type from input value for AIOrchestratorService.
        # It delegates shared cleanup to _sanitize_scene_element_name before returning the cleaned value.
        token = cls._sanitize_scene_element_name(value)
        if token in {"color_fill"}:
            return "background"
        if token in {"text_button"}:
            return "text"
        if token in {"vector_shape", "shape", "minimal_accent", "circle_accent"}:
            return "decorative_shape"
        if token in {"brand_logo", "logo_placeholder"}:
            return "logo"
        if token in {"photo_hero", "generated_image"}:
            return "image"
        return token or "decorative_shape"

    @classmethod
    def _normalize_scene_graph_layers(cls, value: Any) -> list[str]:
        # Normalizes scene graph layers from input value for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_list before returning the cleaned value.
        normalized_layers: list[str] = []
        for item in cls._coerce_list(value):
            if isinstance(item, dict):
                label = item.get("layer") or item.get("id") or item.get("role") or item.get("type")
            else:
                label = item
            layer_name = str(label or "").strip()
            if layer_name:
                normalized_layers.append(layer_name)
        return normalized_layers

    @classmethod
    def _normalize_scene_graph_assets(cls, value: Any) -> list[dict[str, Any]]:
        # Normalizes scene graph assets from input value for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_mapping and _coerce_list before returning the cleaned value.
        if isinstance(value, dict):
            normalized_assets: list[dict[str, Any]] = []
            for key, item in value.items():
                asset_payload = cls._coerce_mapping(item)
                if not asset_payload:
                    continue
                asset_payload.setdefault("notes", str(key))
                normalized_assets.append(asset_payload)
            return normalized_assets
        return [
            item
            for item in cls._coerce_list(value)
            if isinstance(item, dict)
        ]

    @staticmethod
    def _parse_geometry_value(value: Any) -> float | int | None:
        # Measures geometry from input value for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        """Convert geometry values including percentage strings to numeric values"""
        if value is None:
            return None
        if isinstance(value, (int, float)):
            return value
        if isinstance(value, str):
            value = value.strip()
            # Handle percentage strings like '80%'
            if value.endswith('%'):
                try:
                    return float(value.rstrip('%')) / 100.0
                except (ValueError, TypeError):
                    return None
            # Try to parse as number
            try:
                if '.' in value:
                    return float(value)
                return int(value)
            except (ValueError, TypeError):
                return None
        return None

    @classmethod
    def _default_geometry_for_role(cls, role: str, anchor: str | None) -> dict[str, float]:
        # Measures default geometry role from role and anchor for AIOrchestratorService.
        # Keeping the math here avoids subtle differences between renderer-facing branches.
        """Provide safe default normalized geometry based on element role."""
        # Role-based defaults for common social media layouts
        defaults = {
            "background": {"x": 0.0, "y": 0.0, "width": 1.0, "height": 1.0},
            "headline": {"x": 0.08, "y": 0.12, "width": 0.84, "height": 0.15},
            "supporting_line": {"x": 0.08, "y": 0.30, "width": 0.84, "height": 0.08},
            "body": {"x": 0.08, "y": 0.42, "width": 0.84, "height": 0.25},
            "cta": {"x": 0.08, "y": 0.78, "width": 0.40, "height": 0.08},
            "logo": {"x": 0.85, "y": 0.85, "width": 0.10, "height": 0.10},
            "image": {"x": 0.15, "y": 0.20, "width": 0.70, "height": 0.50},
            "icon": {"x": 0.08, "y": 0.50, "width": 0.12, "height": 0.12},
            "decorative_shape": {"x": 0.70, "y": 0.10, "width": 0.25, "height": 0.25},
            "legal_footer": {"x": 0.05, "y": 0.92, "width": 0.90, "height": 0.05},
            "footer": {"x": 0.05, "y": 0.92, "width": 0.90, "height": 0.05},
        }

        default = defaults.get(role, {"x": 0.1, "y": 0.1, "width": 0.8, "height": 0.2})

        # Adjust based on anchor if provided
        if anchor:
            anchor_adjustments = {
                "top-left": {},
                "top-center": {"x": 0.5 - default["width"]/2},
                "top-right": {"x": 1.0 - default["width"]},
                "center": {"x": 0.5 - default["width"]/2, "y": 0.5 - default["height"]/2},
                "bottom-center": {"x": 0.5 - default["width"]/2, "y": 1.0 - default["height"]},
                "bottom-left": {"y": 1.0 - default["height"]},
                "bottom-right": {"x": 1.0 - default["width"], "y": 1.0 - default["height"]},
            }
            default.update(anchor_adjustments.get(anchor, {}))

        return default

    @classmethod
    def _normalize_scene_element_geometry(cls, element: dict[str, Any]) -> dict[str, Any]:
        # Normalizes scene element geometry from element for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_mapping and _default_geometry_for_role before returning the cleaned value.
        geometry = element.get("geometry")
        if isinstance(geometry, str):
            role = str(element.get("role") or "").strip().lower()
            default_geometry = cls._default_geometry_for_role(role, geometry)
            return {**default_geometry, "anchor": geometry, "units": "normalized"}
        normalized_geometry = cls._coerce_mapping(geometry)
        # Parse percentage strings in existing geometry
        if normalized_geometry:
            for key in ("x", "y", "width", "height"):
                if key in normalized_geometry:
                    parsed = cls._parse_geometry_value(normalized_geometry[key])
                    if parsed is not None:
                        normalized_geometry[key] = parsed
            if "z_index" in normalized_geometry:
                parsed_z = cls._parse_geometry_value(normalized_geometry.get("z_index"))
                if parsed_z is None:
                    normalized_geometry.pop("z_index", None)
                else:
                    normalized_geometry["z_index"] = int(round(float(parsed_z)))
            if isinstance(normalized_geometry.get("padding"), dict):
                padding_payload: dict[str, float | int] = {}
                for key, value in list(normalized_geometry.get("padding", {}).items()):
                    parsed = cls._parse_geometry_value(value)
                    if parsed is not None:
                        padding_payload[str(key)] = parsed
                normalized_geometry["padding"] = padding_payload
            units = str(normalized_geometry.get("units") or "").strip().lower()
            is_normalized = units != "px"
            if not units:
                is_normalized = not any(
                    isinstance(normalized_geometry.get(metric), (int, float)) and float(normalized_geometry.get(metric)) > 1.0
                    for metric in ("x", "y", "width", "height")
                )
                if is_normalized:
                    normalized_geometry["units"] = "normalized"
            if is_normalized:
                x = normalized_geometry.get("x")
                y = normalized_geometry.get("y")
                width = normalized_geometry.get("width")
                height = normalized_geometry.get("height")
                if isinstance(width, (int, float)):
                    normalized_geometry["width"] = max(0.0, min(float(width), 1.0))
                if isinstance(height, (int, float)):
                    normalized_geometry["height"] = max(0.0, min(float(height), 1.0))
                width = normalized_geometry.get("width")
                height = normalized_geometry.get("height")
                if isinstance(x, (int, float)):
                    clamped_x = max(0.0, min(float(x), 1.0))
                    if isinstance(width, (int, float)) and clamped_x + float(width) > 1.0:
                        clamped_x = max(0.0, 1.0 - float(width))
                    normalized_geometry["x"] = clamped_x
                if isinstance(y, (int, float)):
                    clamped_y = max(0.0, min(float(y), 1.0))
                    if isinstance(height, (int, float)) and clamped_y + float(height) > 1.0:
                        clamped_y = max(0.0, 1.0 - float(height))
                    normalized_geometry["y"] = clamped_y
            if not all(normalized_geometry.get(metric) is not None for metric in ("x", "y", "width", "height")):
                role = str(element.get("role") or "").strip().lower()
                anchor = str(normalized_geometry.get("anchor") or element.get("anchor") or "").strip() or None
                default_geometry = cls._default_geometry_for_role(role, anchor)
                for metric, default_value in default_geometry.items():
                    normalized_geometry.setdefault(metric, default_value)
                normalized_geometry.setdefault("units", "normalized")
                logger.info(
                    "Applied default geometry for incomplete role '%s': x=%s, y=%s, width=%s, height=%s",
                    role,
                    normalized_geometry.get("x"),
                    normalized_geometry.get("y"),
                    normalized_geometry.get("width"),
                    normalized_geometry.get("height"),
                )
            return normalized_geometry
        position = cls._coerce_mapping(element.get("position"))
        size = element.get("size")
        area = cls._coerce_mapping(element.get("area"))
        max_size = cls._coerce_mapping(element.get("max_size"))
        geometry_payload: dict[str, Any] = {}
        if position.get("x") is not None:
            geometry_payload["x"] = cls._parse_geometry_value(position.get("x"))
        if position.get("y") is not None:
            geometry_payload["y"] = cls._parse_geometry_value(position.get("y"))
        width = None
        height = None
        if isinstance(size, dict):
            width = size.get("width")
            height = size.get("height")
        elif isinstance(size, (int, float)):
            width = size
            height = size
        width = area.get("width") if area.get("width") is not None else width
        height = area.get("height") if area.get("height") is not None else height
        width = max_size.get("width") if max_size.get("width") is not None else width
        height = max_size.get("height") if max_size.get("height") is not None else height
        width = element.get("max_width") if element.get("max_width") is not None else width
        height = element.get("max_height") if element.get("max_height") is not None else height
        if width is not None:
            geometry_payload["width"] = cls._parse_geometry_value(width)
        if height is not None:
            geometry_payload["height"] = cls._parse_geometry_value(height)
        anchor = element.get("anchor")
        if anchor:
            geometry_payload["anchor"] = anchor
        z_index = element.get("z_index")
        if z_index is not None:
            parsed_z = cls._parse_geometry_value(z_index)
            if parsed_z is not None:
                geometry_payload["z_index"] = int(round(float(parsed_z)))
        if geometry_payload:
            units = "normalized"
            for metric in ("x", "y", "width", "height"):
                value = geometry_payload.get(metric)
                if isinstance(value, (int, float)) and value > 1:
                    units = "px"
                    break
            geometry_payload.setdefault("units", units)

        # Apply default geometry if coordinates are missing
        if geometry_payload and not all(k in geometry_payload for k in ("x", "y", "width", "height")):
            role = element.get("role", "")
            anchor = geometry_payload.get("anchor") or element.get("anchor")
            default_geom = cls._default_geometry_for_role(role, anchor)
            geometry_payload.setdefault("x", default_geom["x"])
            geometry_payload.setdefault("y", default_geom["y"])
            geometry_payload.setdefault("width", default_geom["width"])
            geometry_payload.setdefault("height", default_geom["height"])
            logger.info(f"Applied default geometry for role '{role}': x={geometry_payload['x']}, y={geometry_payload['y']}, width={geometry_payload['width']}, height={geometry_payload['height']}")

        if not geometry_payload:
            role = str(element.get("role") or "").strip().lower()
            anchor = str(element.get("anchor") or "").strip() or None
            default_geom = cls._default_geometry_for_role(role, anchor)
            geometry_payload = {
                **default_geom,
                "units": "normalized",
            }
            if anchor:
                geometry_payload["anchor"] = anchor
            logger.info(
                "Applied default geometry for missing role '%s': x=%s, y=%s, width=%s, height=%s",
                role,
                geometry_payload["x"],
                geometry_payload["y"],
                geometry_payload["width"],
                geometry_payload["height"],
            )

        return geometry_payload

    @classmethod
    def _normalize_scene_element_style(cls, element: dict[str, Any]) -> dict[str, Any]:
        # Normalizes scene element style from element for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_mapping before returning the cleaned value.
        style = cls._coerce_mapping(element.get("style"))
        top_level_mappings = {
            "font_family": "font_family",
            "font": "font_family",
            "font_size": "font_size",
            "font_weight": "font_weight",
            "color": "fill",
            "background_color": "background_fill",
            "opacity": "opacity",
            "shape": "shape",
            "alignment": "text_align",
            "text_align": "text_align",
            "text_role": "font_role",
            "border_radius": "border_radius",
            "line_height": "line_height",
            "max_lines": "max_lines",
        }
        for source_key, target_key in top_level_mappings.items():
            if style.get(target_key) is None and element.get(source_key) is not None:
                style[target_key] = element.get(source_key)
        return style

    @classmethod
    def _normalize_scene_element_text(cls, element: dict[str, Any]) -> str | list[str] | None:
        # Normalizes scene element text from element for AIOrchestratorService.
        # It delegates shared cleanup to _repair_common_mojibake and _coerce_text_value before returning the cleaned value.
        raw_text = element.get("text")
        if raw_text is None:
            return None
        if isinstance(raw_text, list):
            normalized_list = [
                cls._repair_common_mojibake(cls._coerce_text_value(item)).strip()
                for item in raw_text
                if cls._repair_common_mojibake(cls._coerce_text_value(item)).strip()
            ]
            return normalized_list or None
        if isinstance(raw_text, dict):
            preferred_parts = [
                raw_text.get("headline"),
                raw_text.get("supporting_line"),
                raw_text.get("body"),
                raw_text.get("cta"),
            ]
            normalized_parts = [
                cls._repair_common_mojibake(cls._coerce_text_value(item)).strip()
                for item in preferred_parts
                if cls._repair_common_mojibake(cls._coerce_text_value(item)).strip()
            ]
            if normalized_parts:
                return "\n".join(normalized_parts[:3])
            fallback_text = cls._repair_common_mojibake(cls._coerce_text_value(raw_text)).strip()
            return fallback_text or None
        normalized_text = cls._repair_common_mojibake(cls._coerce_text_value(raw_text)).strip()
        return normalized_text or None

    @classmethod
    def _sanitize_scene_element_asset_payload(
        cls,
        asset_payload: dict[str, Any],
        *,
        element: dict[str, Any],
        request: AIOrchestrationRequest,
    ) -> dict[str, Any] | None:
        # Sanitizes scene element asset from asset payload, element, and request state for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_mapping and _catalog_asset_by_storage_path before returning the cleaned value.
        payload = cls._coerce_mapping(asset_payload)
        if not payload:
            return None
        role = str(element.get("role") or "").strip().lower()
        element_type = str(element.get("element_type") or "").strip().lower()
        storage_path = str(payload.get("storage_path") or "").strip()
        asset_role = str(payload.get("asset_role") or "").strip().lower()
        if storage_path.casefold() in {"unknown", "none", "null"}:
            storage_path = ""
        matched_asset = None
        if storage_path:
            matched_asset = cls._catalog_asset_by_storage_path(request, storage_path)
        if matched_asset is not None:
            payload = {
                "asset_id": matched_asset.get("asset_id"),
                "asset_role": matched_asset.get("asset_role"),
                "storage_path": matched_asset.get("storage_path"),
                "trust_level": matched_asset.get("trust_level"),
                "variant": payload.get("variant") or matched_asset.get("variant"),
                "notes": payload.get("notes") or matched_asset.get("notes"),
            }
            storage_path = str(payload.get("storage_path") or "").strip()
            asset_role = str(payload.get("asset_role") or "").strip().lower()
        if role == "logo" or asset_role in {"logo", "logo_variant"} or element_type == "logo":
            fallback_logo_path = str(request.logo_asset_path or "").strip()
            if fallback_logo_path and (not storage_path or matched_asset is None):
                payload["storage_path"] = fallback_logo_path
                payload.setdefault("asset_role", "logo")
                payload.setdefault("trust_level", "trusted")
                storage_path = fallback_logo_path
            if not storage_path:
                return None
        is_visual_image_like = (
            role in {"image", "hero_visual", "hero_image", "primary_visual", "supporting_visual", "illustration"}
            or element_type == "image"
        )
        if is_visual_image_like:
            if storage_path and str(storage_path).lower().endswith(".pdf"):
                return None
            if storage_path and matched_asset is None:
                return None
        return payload or None

    @staticmethod
    def _default_scene_element_type(element: dict[str, Any]) -> str:
        # Centralizes default scene element type from element for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        role = str(element.get("role") or "").strip().lower()
        asset_role = str(element.get("asset_role") or ((element.get("asset") or {}) if isinstance(element.get("asset"), dict) else {}).get("asset_role") or "").strip().lower()
        if role in {"headline", "supporting_line", "body", "proof_points", "cta", "footer", "legal", "section_label", "stat_highlights"}:
            return "text"
        if role == "logo" or asset_role == "logo":
            return "logo"
        if role in {"image", "hero_image"} or asset_role in {"ai_image", "image", "photo"}:
            return "image"
        if role == "icon" or asset_role == "icon":
            return "icon"
        if role == "background":
            return "background"
        if role in {"decorative_shape", "background_shape", "micro_design_element", "footer_decor"}:
            return "decorative_shape"
        return "text" if element.get("text") is not None else "decorative_shape"

    @classmethod
    def _logo_position_hint_from_payload(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: dict[str, Any],
        creative_decision: CreativeDecisionPayload,
        scene_graph_payload: dict[str, Any],
    ) -> str:
        # Measures logo position hint from request state, copy payload, and creative decision for AIOrchestratorService.
        # It calls _effective_logo_position_hint to turn raw evidence into the structured signal the caller needs.
        return cls._effective_logo_position_hint(
            request=request,
            creative_decision=creative_decision,
            text_payload=text_payload,
            scene_graph_payload=scene_graph_payload,
        )

    @classmethod
    def _logo_variant_hint_from_decision(cls, creative_decision: CreativeDecisionPayload) -> str:
        # Centralizes logo variant hint decision from creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        asset_strategy = creative_decision.asset_strategy or {}
        for candidate in (
            asset_strategy.get("logo_variant"),
            asset_strategy.get("logo_variant_type"),
            asset_strategy.get("logo_variant_hint"),
        ):
            normalized = cls._normalize_logo_variant_hint(candidate)
            if normalized:
                return normalized
        return ""

    @classmethod
    def _coerce_logo_geometry_tuple(cls, geometry: Any) -> tuple[float, float, float, float] | None:
        # Coerces logo geometry tuple from geometry for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_mapping before returning the cleaned value.
        candidate = cls._coerce_mapping(geometry)
        if not candidate:
            return None
        try:
            x = float(candidate.get("x"))
            y = float(candidate.get("y"))
            width = float(candidate.get("width"))
            height = float(candidate.get("height"))
        except (TypeError, ValueError):
            return None
        units = str(candidate.get("units") or "normalized").strip().lower()
        if units != "normalized" and max(abs(x), abs(y), abs(width), abs(height)) > 1.5:
            return None
        if width <= 0 or height <= 0:
            return None
        return (x, y, width, height)

    @classmethod
    def _coerce_canvas_geometry_tuple(
        cls,
        geometry: Any,
        *,
        request: AIOrchestrationRequest,
    ) -> tuple[float, float, float, float] | None:
        # Coerces canvas geometry tuple from geometry and request state for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_mapping before returning the cleaned value.
        candidate = cls._coerce_mapping(geometry)
        if not candidate:
            return None
        try:
            x = float(candidate.get("x"))
            y = float(candidate.get("y"))
            width = float(candidate.get("width"))
            height = float(candidate.get("height"))
        except (TypeError, ValueError):
            return None
        if width <= 0 or height <= 0:
            return None
        units = str(candidate.get("units") or "normalized").strip().lower()
        if units == "normalized" or max(abs(x), abs(y), abs(width), abs(height)) <= 1.5:
            return (x, y, width, height)
        size = request.studio_panel.get("size") if isinstance(request.studio_panel.get("size"), dict) else {}
        canvas_width = max(float(size.get("width") or 1080), 1.0)
        canvas_height = max(float(size.get("height") or 1080), 1.0)
        return (
            x / canvas_width,
            y / canvas_height,
            width / canvas_width,
            height / canvas_height,
        )

    @staticmethod
    def _extract_visual_hierarchy_for_element(
        element: dict[str, Any], visual_context: dict[str, Any]
    ) -> dict[str, Any]:
        # Extracts visual hierarchy element from element and visual context for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        """🔥 PHASE 5: Extract visual hierarchy metadata for scene graph element"""
        role = element.get("role", "body")

        # Infer hierarchy level from visual reading order
        visual_hierarchies = visual_context.get("visual_hierarchies", [])
        hierarchy_level = "secondary"
        reading_order_position = None

        if visual_hierarchies:
            hierarchy = visual_hierarchies[0]
            reading_order = hierarchy.get("reading_order", [])
            if isinstance(reading_order, list) and role in reading_order:
                reading_order_position = reading_order.index(role)
                if reading_order_position == 0:
                    hierarchy_level = "primary"
                elif reading_order_position == 1:
                    hierarchy_level = "secondary"
                else:
                    hierarchy_level = "tertiary"

            if hierarchy.get("focal_role") == role:
                hierarchy_level = "primary"

        # Find associated icons
        associated_icons = []
        for pattern in visual_context.get("component_patterns", []):
            patterns_list = pattern.get("patterns", [])
            for p in patterns_list:
                if "icon_text_pairs" in p and role in ["headline", "subheading", "supporting"]:
                    associated_icons.append("icon_suggested")
                    break

        # Determine spatial group
        spatial_group = None
        layout_structures = visual_context.get("layout_structures", [])
        if layout_structures:
            struct = layout_structures[0]
            if struct.get("spatial_groups_count"):
                spatial_group = f"group_{reading_order_position if reading_order_position is not None else 0}"

        return {
            "hierarchy_level": hierarchy_level,
            "reading_order_position": reading_order_position,
            "associated_icons": associated_icons,
            "spatial_group": spatial_group,
        }

    @classmethod
    def _anchor_from_logo_geometry(cls, geometry: tuple[float, float, float, float]) -> str:
        # Measures anchor logo geometry from geometry for AIOrchestratorService.
        # Keeping the math here avoids subtle differences between renderer-facing branches.
        x, y, width, height = geometry
        center_x = x + (width / 2.0)
        center_y = y + (height / 2.0)
        vertical = "top" if center_y <= 0.35 else ("bottom" if center_y >= 0.65 else "middle")
        horizontal = "left" if center_x <= 0.35 else ("right" if center_x >= 0.65 else "center")
        return f"{vertical}-{horizontal}"

    @staticmethod
    def _normalized_rects_overlap(
        first: tuple[float, float, float, float],
        second: tuple[float, float, float, float],
        *,
        gutter: float = 0.0,
    ) -> bool:
        # Normalizes rects overlap from first, second, and gutter for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        first_left, first_top, first_width, first_height = first
        second_left, second_top, second_width, second_height = second
        first_right = first_left + first_width
        first_bottom = first_top + first_height
        second_right = second_left + second_width
        second_bottom = second_top + second_height
        return not (
            first_right <= (second_left - gutter)
            or second_right <= (first_left - gutter)
            or first_bottom <= (second_top - gutter)
            or second_bottom <= (first_top - gutter)
        )

    @classmethod
    def _repair_text_geometry_for_logo_safe_zone(
        cls,
        *,
        element: dict[str, Any],
        logo_geometry: tuple[float, float, float, float],
        request: AIOrchestrationRequest,
    ) -> dict[str, Any]:
        # Repairs text geometry logo safe zone from element, logo geometry, and request state for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_canvas_geometry_tuple and _coerce_mapping before returning the cleaned value.
        role = str(element.get("role") or "").strip().lower()
        if role not in {
            "headline",
            "supporting_line",
            "body",
            "proof_points",
            "stat_highlights",
            "section_label",
            "footer",
            "legal",
        }:
            return element
        geometry = cls._coerce_canvas_geometry_tuple(element.get("geometry"), request=request)
        if geometry is None:
            return element

        gutter = 0.02
        if not cls._normalized_rects_overlap(geometry, logo_geometry, gutter=gutter):
            return element

        x, y, width, height = geometry
        logo_x, logo_y, logo_width, logo_height = logo_geometry
        logo_left = max(logo_x - gutter, 0.0)
        logo_top = max(logo_y - gutter, 0.0)
        logo_right = min(logo_x + logo_width + gutter, 1.0)
        logo_bottom = min(logo_y + logo_height + gutter, 1.0)
        min_width = 0.26 if role == "headline" else 0.18
        page_margin = 0.04

        repaired_geometry = {"x": x, "y": y, "width": width, "height": height, "units": "normalized"}

        if x < logo_left:
            width_left = max(logo_left - x, 0.0)
            if width_left >= min_width:
                repaired_geometry["width"] = round(width_left, 4)
                element = dict(element)
                element["geometry"] = repaired_geometry
                validation_hints = cls._coerce_mapping(element.get("validation_hints"))
                validation_hints["avoids_logo_safe_zone"] = True
                element["validation_hints"] = validation_hints
                return element

        space_above = max(logo_top - page_margin, 0.0)
        space_below = max(1.0 - logo_bottom - page_margin, 0.0)
        if space_below >= height and space_below >= space_above:
            repaired_geometry["y"] = round(min(max(logo_bottom, y), 1.0 - page_margin - height), 4)
        elif space_above >= height:
            repaired_geometry["y"] = round(max(page_margin, logo_top - height), 4)
        elif x < logo_left:
            repaired_geometry["width"] = round(max(min_width, min(width, logo_left - x)), 4)
        else:
            repaired_geometry["x"] = round(min(max(page_margin, logo_right), 1.0 - page_margin - min(width, 0.4)), 4)
            repaired_geometry["width"] = round(min(width, 1.0 - repaired_geometry["x"] - page_margin), 4)

        element = dict(element)
        element["geometry"] = repaired_geometry
        validation_hints = cls._coerce_mapping(element.get("validation_hints"))
        validation_hints["avoids_logo_safe_zone"] = True
        element["validation_hints"] = validation_hints
        return element

    @classmethod
    def _finalize_logo_scene_policy(
        cls,
        *,
        scene_graph_payload: dict[str, Any],
        elements: list[dict[str, Any]],
        request: AIOrchestrationRequest,
        text_payload: dict[str, Any],
        creative_decision: CreativeDecisionPayload,
    ) -> list[dict[str, Any]]:
        # Builds finalize logo scene policy from scene graph payload, elements, and request state for AIOrchestratorService.
        # It calls _logo_position_hint_from_payload and _resolve_logo_background_tone while assembling the payload or prompt text.
        logo_position_hint = cls._logo_position_hint_from_payload(
            request=request,
            text_payload=text_payload,
            creative_decision=creative_decision,
            scene_graph_payload=scene_graph_payload,
        )
        metadata = text_payload.get("metadata") if isinstance(text_payload.get("metadata"), dict) else {}
        logo_background_tone = cls._resolve_logo_background_tone(
            metadata=metadata,
            creative_decision=creative_decision,
        )
        logo_variant = cls._logo_variant_hint_from_decision(creative_decision)

        scene_graph_payload.setdefault("styles", {})

        # Fix 1: Apply brand background color dynamically (not hardcoded white)
        visual_identity = request.resolved_brand_context.get("visual_identity", {})
        brand_color_palette = visual_identity.get("brand_color_palette", {})
        if isinstance(brand_color_palette, dict) and "background" in brand_color_palette:
            background_color = brand_color_palette["background"]
            if background_color and background_color != "#FFFFFF":  # Only set if not default white
                scene_graph_payload["styles"]["background_fill"] = background_color

        if logo_position_hint:
            scene_graph_payload["styles"]["logo_position"] = logo_position_hint
        scene_graph_payload.setdefault("validation_hints", {})
        if logo_background_tone:
            scene_graph_payload["validation_hints"]["logo_background_tone"] = logo_background_tone
        if logo_position_hint:
            scene_graph_payload["validation_hints"]["logo_position"] = logo_position_hint
        scene_graph_payload["validation_hints"]["logo_overlay_only"] = True

        resolved_logo_geometry = cls._normalize_logo_safe_zone_geometry(
            request=request,
            geometry=None,
            hint=logo_position_hint or None,
        )
        finalized: list[dict[str, Any]] = []
        kept_logo = False
        for element in elements:
            normalized = dict(element)
            role = str(normalized.get("role") or normalized.get("element_type") or "").strip().lower()
            if role != "logo":
                finalized.append(
                    cls._repair_text_geometry_for_logo_safe_zone(
                        element=normalized,
                        logo_geometry=resolved_logo_geometry,
                        request=request,
                    )
                )
                continue
            geometry_tuple = cls._coerce_canvas_geometry_tuple(normalized.get("geometry"), request=request)
            resolved_geometry = cls._normalize_logo_safe_zone_geometry(
                request=request,
                geometry=geometry_tuple,
                hint=logo_position_hint or None,
            )
            resolved_logo_geometry = resolved_geometry
            anchor = cls._anchor_from_logo_geometry(resolved_geometry)
            normalized["geometry"] = {
                "x": round(resolved_geometry[0], 4),
                "y": round(resolved_geometry[1], 4),
                "width": round(resolved_geometry[2], 4),
                "height": round(resolved_geometry[3], 4),
                "units": "normalized",
                "anchor": anchor,
            }
            style = cls._coerce_mapping(normalized.get("style"))
            style.setdefault("fit", "contain")
            if logo_variant:
                style.setdefault("logo_variant", logo_variant)
            if logo_background_tone:
                style.setdefault("logo_background_tone", logo_background_tone)
            normalized["style"] = style
            validation_hints = cls._coerce_mapping(normalized.get("validation_hints"))
            validation_hints["logo_overlay_only"] = True
            validation_hints["logo_safe_zone_required"] = True
            validation_hints["logo_position"] = logo_position_hint or anchor
            if logo_background_tone:
                validation_hints["logo_background_tone"] = logo_background_tone
            if logo_variant:
                validation_hints["logo_variant"] = logo_variant
            normalized["validation_hints"] = validation_hints
            asset_payload = cls._coerce_mapping(normalized.get("asset"))
            asset_payload.setdefault("asset_role", "logo")
            asset_payload.setdefault("trust_level", "trusted")
            if logo_variant:
                asset_payload.setdefault("variant", logo_variant)
            normalized["asset"] = asset_payload
            if not kept_logo:
                finalized.append(normalized)
                kept_logo = True
        if kept_logo:
            finalized = [
                element
                if str(element.get("role") or "").strip().lower() == "logo"
                else cls._repair_text_geometry_for_logo_safe_zone(
                    element=element,
                    logo_geometry=resolved_logo_geometry,
                    request=request,
                )
                for element in finalized
            ]
        return finalized

    @classmethod
    def _inject_legal_disclaimers(
        cls,
        *,
        elements: list[dict[str, Any]],
        request: AIOrchestrationRequest,
        canvas: dict[str, Any],
    ) -> list[dict[str, Any]]:
        # Centralizes inject legal disclaimers from elements, request state, and canvas for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        """Inject legal disclaimers from brand assets as footer elements"""
        # Get brand legal assets from resolved context
        brand_assets = request.resolved_brand_context.get("brand_assets", {})
        legal_disclaimers = brand_assets.get("legal_disclaimers", [])

        if not legal_disclaimers or not isinstance(legal_disclaimers, list):
            return elements

        # Get current format from studio panel
        current_format = request.studio_panel.get("format", "static")
        disclaimer_requested = bool(
            isinstance(request.research_editorial_brief, dict)
            and request.research_editorial_brief.get("disclaimer_requested")
        ) or ("disclaimer" in str(getattr(request, "prompt", "") or "").casefold())

        # Find applicable disclaimers for this format
        applicable_disclaimers = [
            disclaimer
            for disclaimer in legal_disclaimers
            if isinstance(disclaimer, dict)
            and current_format in disclaimer.get("applies_to_formats", [])
        ]

        if not applicable_disclaimers and disclaimer_requested:
            applicable_disclaimers = [
                disclaimer
                for disclaimer in legal_disclaimers
                if isinstance(disclaimer, dict) and str(disclaimer.get("text_template") or "").strip()
            ][:1]

        if not applicable_disclaimers:
            return elements

        # Use the first applicable disclaimer (could be enhanced to handle multiple)
        disclaimer = applicable_disclaimers[0]

        # Check if a legal footer already exists
        has_legal_footer = any(
            str(element.get("role") or "").strip().lower() in {"legal", "footer", "disclaimer"}
            for element in elements
        )

        if has_legal_footer:
            # Don't add duplicate legal footer
            return elements

        # Create legal footer element
        footer_text = disclaimer.get("text_template", "")
        if not footer_text:
            return elements

        # Get canvas dimensions
        try:
            canvas_width = max(int(canvas.get("width") or 1080), 1)
        except (TypeError, ValueError):
            canvas_width = 1080
        try:
            canvas_height = max(int(canvas.get("height") or 1080), 1)
        except (TypeError, ValueError):
            canvas_height = 1080

        footer_safe_area = calculate_footer_safe_area(
            canvas_width=canvas_width,
            canvas_height=canvas_height,
            footer_text=footer_text,
        )
        footer_text_box = (
            footer_safe_area.get("footer_text_box")
            if isinstance(footer_safe_area.get("footer_text_box"), dict)
            else {}
        )

        def normalized_box_value(value: Any, scale: int) -> float:
            # Measures box from input value and scale for AIOrchestratorService.
            # Keeping the math here avoids subtle differences between renderer-facing branches.
            try:
                return round(max(float(value or 0), 0.0) / max(float(scale), 1.0), 4)
            except (TypeError, ValueError):
                return 0.0

        footer_geometry = {
            "x": normalized_box_value(footer_text_box.get("x"), canvas_width),
            "y": normalized_box_value(footer_text_box.get("y"), canvas_height),
            "width": normalized_box_value(footer_text_box.get("width"), canvas_width),
            "height": normalized_box_value(footer_text_box.get("height"), canvas_height),
            "units": "normalized",
            "anchor": "top_left",
        }

        # Get styling from disclaimer
        font_size = int(disclaimer.get("font_size", 8))
        text_color = disclaimer.get("text_color", "#666666")

        # Create footer element
        footer_element = {
            "element_id": "legal_footer",
            "element_type": "text",
            "role": "legal",
            "text": footer_text,
            "geometry": footer_geometry,
            "style": {
                "font_size": font_size,
                "fill": text_color,
                "align": "left",
                "vertical_align": "bottom",
                "font_weight": "normal",
            },
            "visible": True,
            "validation_hints": {
                "legal_compliance": True,
                "required": True,
                "footer_layout_source": "dynamic_footer_safe_area",
                "footer_safe_area": {
                    key: value
                    for key, value in footer_safe_area.items()
                    if not str(key).startswith("_")
                },
            },
        }

        # Add footer to elements list
        return [*elements, footer_element]

    @classmethod
    def _apply_cta_template_styling(
        cls,
        *,
        elements: list[dict[str, Any]],
        request: AIOrchestrationRequest,
    ) -> list[dict[str, Any]]:
        # Applies apply cta template styling from elements and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        """Apply CTA template styling to CTA button elements"""
        # Get brand CTA templates from resolved context
        brand_assets = request.resolved_brand_context.get("brand_assets", {})
        cta_templates = brand_assets.get("cta_templates", [])

        if not cta_templates or not isinstance(cta_templates, list):
            return elements

        # Find default template or use first one
        default_template = next(
            (t for t in cta_templates if isinstance(t, dict) and t.get("is_default")),
            cta_templates[0] if cta_templates else None,
        )

        if not default_template:
            return elements

        # Apply styling to CTA elements
        styled_elements = []
        for element in elements:
            element_copy = dict(element)
            role = str(element_copy.get("role") or "").strip().lower()

            if role == "cta":
                # Apply CTA template styling
                style = cls._coerce_mapping(element_copy.get("style"))

                # Apply button colors
                button_color = default_template.get("button_color")
                button_text_color = default_template.get("button_text_color")
                if button_color:
                    style["background_fill"] = button_color
                if button_text_color:
                    style["fill"] = button_text_color

                # Apply button style (rounded, sharp, pill)
                button_style = default_template.get("button_style", "rounded")
                if button_style == "rounded":
                    style["border_radius"] = 8
                elif button_style == "pill":
                    style["border_radius"] = 999  # Very large radius for pill shape
                elif button_style == "sharp":
                    style["border_radius"] = 0

                # Store icon hint in validation_hints for renderer
                icon_hint = default_template.get("icon_hint")
                if icon_hint:
                    validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
                    validation_hints["cta_icon_hint"] = icon_hint
                    element_copy["validation_hints"] = validation_hints

                element_copy["style"] = style

            styled_elements.append(element_copy)

        return styled_elements

    @classmethod
    def _apply_component_motif_patterns(
        cls,
        *,
        elements: list[dict[str, Any]],
        request: AIOrchestrationRequest,
    ) -> list[dict[str, Any]]:
        # Applies apply component motif patterns from elements and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        """Apply component motif patterns from brand visual references (numbered badges, background boxes)"""
        # Get visual references from resolved context
        visual_identity = request.resolved_brand_context.get("visual_identity", {})
        component_motifs = (
            visual_identity.get("component_motifs")
            if isinstance(visual_identity.get("component_motifs"), dict)
            else {}
        )

        if not component_motifs:
            reference_creatives = visual_identity.get("reference_creatives", [])
            if not reference_creatives or not isinstance(reference_creatives, list):
                return elements

            # Fallback: extract component motifs from visual references
            for reference in reference_creatives:
                if not isinstance(reference, dict):
                    continue
                style_chars = reference.get("style_characteristics", {})
                if isinstance(style_chars, dict):
                    motifs = style_chars.get("component_motifs", {})
                    if isinstance(motifs, dict) and motifs:
                        component_motifs.update(motifs)

        if not component_motifs:
            return elements

        # Check for numbered badge pattern
        has_numbered_badges = False
        badge_style = {}

        # Look for numbered badge indicators in component motifs
        # This could be detected by template vision or manually added
        if "numbered_badges" in component_motifs:
            numbered_badge_config = component_motifs["numbered_badges"]
            if isinstance(numbered_badge_config, dict) and numbered_badge_config.get("detected"):
                has_numbered_badges = True
                badge_style = numbered_badge_config

        # Check for background box pattern for subheadings
        has_background_boxes = False
        background_box_style = {}

        if "text_background_boxes" in component_motifs:
            bg_box_config = component_motifs["text_background_boxes"]
            if isinstance(bg_box_config, dict) and bg_box_config.get("detected"):
                has_background_boxes = True
                background_box_style = bg_box_config

        # Apply patterns to elements
        enhanced_elements = []
        for element in elements:
            element_copy = dict(element)
            role = str(element_copy.get("role") or "").strip().lower()

            # Fix 4: Apply numbered badges to list elements
            if has_numbered_badges and role in {"proof_points", "stat_highlights", "list"}:
                validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
                validation_hints["uses_numbered_badges"] = True
                # TODO: Load default_badge_color from brand_visual_brief
                default_badge_color = "#F7941D"  # Fallback orange
                validation_hints["badge_style"] = {
                    "shape": badge_style.get("shape", "rounded_rect"),
                    "badge_color": badge_style.get("badge_color", default_badge_color),
                    "text_color": badge_style.get("text_color", "#FFFFFF"),
                    "radius_px": int(badge_style.get("radius_px", 12)),
                    "padding_px": int(badge_style.get("padding_px", 8)),
                    "number_format": badge_style.get("number_format", "01"),
                }
                element_copy["validation_hints"] = validation_hints

            # Fix 5: Apply background boxes to subheadings/supporting lines
            if has_background_boxes and role in {"supporting_line", "subheading", "section_label"}:
                applies_to_roles = background_box_style.get("applies_to_roles", [])
                if not applies_to_roles or role in applies_to_roles:
                    style = cls._coerce_mapping(element_copy.get("style"))

                    # Apply background box color
                    box_color = background_box_style.get("box_color")
                    if box_color:
                        style["background_fill"] = box_color

                    # Apply border radius
                    border_radius = background_box_style.get("border_radius_px")
                    if border_radius:
                        style["background_radius"] = int(border_radius)

                    # Store padding hints
                    padding_x = background_box_style.get("padding_x", 12)
                    padding_y = background_box_style.get("padding_y", 8)
                    validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
                    validation_hints["background_box_padding"] = {
                        "x": int(padding_x),
                        "y": int(padding_y),
                    }

                    element_copy["style"] = style
                    element_copy["validation_hints"] = validation_hints

            enhanced_elements.append(element_copy)

        return enhanced_elements

    @classmethod
    def _remap_template_colors_to_brand_palette(
        cls,
        scene_graph: dict[str, Any],
        compiled_context: dict[str, Any],
        planning_hints: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Derives remap template colors brand palette from scene graph, compiled context, and planning hints for AIOrchestratorService.
        # It calls _extract_brand_color_system and _extract_template_colors to turn raw evidence into the structured signal the caller needs.
        """Remap template colors to brand palette using brand data (NO HARDCODED COLORS).

        Uses luminance-based semantic role analysis to map template colors to brand colors
        by understanding their purpose (text, background, accent) rather than hardcoded matches.
        """
        brand_color_system = cls._extract_brand_color_system(
            compiled_context,
            planning_hints=planning_hints,
        )
        if not brand_color_system:
            logger.warning("No brand color system found, skipping color remapping")
            return scene_graph

        # Build color mapping using semantic role analysis
        template_colors = cls._extract_template_colors(scene_graph)
        color_mapping = {}

        for template_color in template_colors:
            semantic_role = cls._analyze_color_semantic_role(template_color)
            brand_color = cls._get_brand_color_for_role(semantic_role, brand_color_system)

            if brand_color and template_color.lower() != brand_color.lower():
                color_mapping[template_color.lower()] = brand_color
                logger.info(f"Mapped {template_color} ({semantic_role}) → {brand_color}")

        # Apply mapping to all element styles
        if color_mapping:
            for element in scene_graph.get("elements", []):
                style = element.get("style", {})
                if not isinstance(style, dict):
                    continue
                for key in ("background_color", "fill_color", "text_color", "border_color", "background_fill", "fill"):
                    if key in style and style[key]:
                        color = str(style[key]).lower()
                        if color in color_mapping:
                            old_color = style[key]
                            style[key] = color_mapping[color]
                            logger.info(f"Remapped element '{element.get('role', 'unknown')}' {key} from {old_color} to {color_mapping[color]}")

        return scene_graph

    @classmethod
    def _normalize_palette_roles_color_system(
        cls,
        palette_roles: dict[str, Any],
    ) -> dict[str, Any]:
        # Normalizes palette roles color system from palette roles for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        if not isinstance(palette_roles, dict):
            return {}

        normalized_palette = {
            str(key).strip().lower(): str(value).strip()
            for key, value in palette_roles.items()
            if str(key).strip() and str(value).strip()
        }
        if not normalized_palette:
            return {}

        primary = normalized_palette.get("primary") or normalized_palette.get("brand_primary")
        secondary = normalized_palette.get("secondary") or normalized_palette.get("brand_secondary")
        accent = (
            normalized_palette.get("accent")
            or normalized_palette.get("highlight")
            or normalized_palette.get("cta")
            or secondary
        )
        background = (
            normalized_palette.get("background")
            or normalized_palette.get("canvas")
            or normalized_palette.get("surface")
        )
        surface = normalized_palette.get("surface") or background
        text = (
            normalized_palette.get("text")
            or normalized_palette.get("foreground")
            or normalized_palette.get("text_primary")
            or primary
        )
        validated_palette: list[str] = []
        for value in (primary, secondary, accent, background, surface, text):
            if value and value not in validated_palette:
                validated_palette.append(value)
        return {
            "primary": primary,
            "secondary": secondary,
            "accent": accent,
            "validated_palette": validated_palette,
            "text_primary": text,
            "text_secondary": normalized_palette.get("text_secondary") or secondary or text,
            "background_light": background,
            "background_dark": normalized_palette.get("background_dark") or primary,
            "neutral_light": normalized_palette.get("neutral_light") or surface or background,
            "neutral_dark": normalized_palette.get("neutral_dark") or normalized_palette.get("text_secondary") or primary,
        }

    @classmethod
    def _extract_brand_color_system(
        cls,
        compiled_context: dict[str, Any],
        *,
        planning_hints: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Extracts brand color system from compiled context and planning hints for AIOrchestratorService.
        # It calls _coerce_mapping and _normalize_palette_roles_color_system to turn raw evidence into the structured signal the caller needs.
        """Extract brand color system with semantic roles from brand data."""
        brand_profile = compiled_context.get("brand_profile", {})
        colors = brand_profile.get("colors", {}) if isinstance(brand_profile, dict) else {}
        if not isinstance(colors, dict):
            colors = {}
        if not colors:
            brand_visual_brief = cls._coerce_mapping(compiled_context.get("brand_visual_brief"))
            colors = cls._normalize_palette_roles_color_system(
                cls._coerce_mapping(brand_visual_brief.get("palette_roles"))
            )
        if not colors and isinstance(planning_hints, dict):
            colors = cls._normalize_palette_roles_color_system(
                cls._coerce_mapping(
                    cls._coerce_mapping(planning_hints.get("brand_rule_hints")).get("palette_roles")
                )
            )
        if not colors:
            return {}

        # Extract semantic color roles from brand analysis
        color_system = {
            "primary": colors.get("primary"),
            "secondary": colors.get("secondary"),
            "accent": colors.get("accent"),
            "validated_palette": colors.get("validated_palette", []),
            # Look for semantic roles in brand analysis
            "text_primary": colors.get("text_primary") or colors.get("primary"),
            "text_secondary": colors.get("text_secondary") or colors.get("secondary"),
            "background_light": colors.get("background_light") or colors.get("background_primary"),
            "background_dark": colors.get("background_dark") or colors.get("primary"),
            "neutral_light": colors.get("neutral_light"),
            "neutral_dark": colors.get("neutral_dark"),
        }

        # Filter out None values
        color_system = {k: v for k, v in color_system.items() if v}

        # Calculate luminance for each color for intelligent matching
        for key, color_value in list(color_system.items()):
            if key != "validated_palette" and isinstance(color_value, str):
                try:
                    color_system[f"{key}_luminance"] = cls._calculate_color_luminance(color_value)
                except Exception:
                    pass

        return color_system

    @classmethod
    def _analyze_color_semantic_role(cls, color_hex: str) -> str:
        # Derives analyze color semantic role from color hex for AIOrchestratorService.
        # It calls _calculate_color_luminance to turn raw evidence into the structured signal the caller needs.
        """Analyze what semantic role a template color likely serves based on luminance."""
        try:
            luminance = cls._calculate_color_luminance(color_hex)
        except Exception:
            return "accent"  # Default fallback

        # Classify based on luminance and common patterns
        if luminance > 0.9:
            return "background_light"  # Very light colors → light backgrounds
        elif luminance > 0.7:
            return "neutral_light"  # Light neutrals → subtle backgrounds/borders
        elif luminance < 0.1:
            return "text_primary"  # Very dark colors → primary text
        elif luminance < 0.3:
            return "text_secondary"  # Dark colors → secondary text or dark backgrounds
        elif luminance < 0.5:
            return "neutral_dark"  # Medium-dark → neutral elements
        else:
            return "accent"  # Mid-range colors → accents/highlights

    @classmethod
    def _calculate_color_luminance(cls, color_hex: str) -> float:
        # Derives calculate color luminance from color hex for AIOrchestratorService.
        # The derived signal gives the LLM explicit evidence instead of asking it to infer the same thing later.
        """Calculate relative luminance of a color (0.0 = black, 1.0 = white)."""
        # Remove # prefix
        hex_color = str(color_hex).lstrip("#").strip()

        # Convert to RGB
        try:
            r = int(hex_color[0:2], 16) / 255.0
            g = int(hex_color[2:4], 16) / 255.0
            b = int(hex_color[4:6], 16) / 255.0
        except (ValueError, IndexError):
            return 0.5  # Default to mid-range if parsing fails

        # Calculate relative luminance using standard formula
        def linearize(channel):
            # Centralizes linearize from channel for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if channel <= 0.03928:
                return channel / 12.92
            return ((channel + 0.055) / 1.055) ** 2.4

        r_lin = linearize(r)
        g_lin = linearize(g)
        b_lin = linearize(b)

        return 0.2126 * r_lin + 0.7152 * g_lin + 0.0722 * b_lin

    @classmethod
    def _get_brand_color_for_role(cls, semantic_role: str, brand_color_system: dict[str, Any]) -> str | None:
        # Derives brand color role from semantic role and brand color system for AIOrchestratorService.
        # The derived signal gives the LLM explicit evidence instead of asking it to infer the same thing later.
        """Get brand color for a semantic role, with fallbacks from brand data."""
        # Direct match
        if semantic_role in brand_color_system:
            return brand_color_system[semantic_role]

        # Fallback logic based on role (using brand data only)
        fallback_chain = {
            "text_primary": ["text_primary", "primary", "validated_palette[0]"],
            "text_secondary": ["text_secondary", "secondary", "primary"],
            "background_light": ["background_light", "validated_palette[-1]", "accent"],
            "background_dark": ["background_dark", "primary"],
            "neutral_light": ["neutral_light", "background_light", "accent"],
            "neutral_dark": ["neutral_dark", "text_secondary", "primary"],
            "accent": ["accent", "secondary"],
        }

        for fallback_key in fallback_chain.get(semantic_role, []):
            if fallback_key.startswith("validated_palette["):
                # Extract from palette array
                palette = brand_color_system.get("validated_palette", [])
                if not isinstance(palette, list):
                    continue
                try:
                    if "[0]" in fallback_key and palette:
                        return palette[0]
                    elif "[-1]" in fallback_key and palette:
                        return palette[-1]
                except IndexError:
                    continue
            elif fallback_key in brand_color_system:
                return brand_color_system[fallback_key]

        # Last resort: first color in validated palette
        validated = brand_color_system.get("validated_palette", [])
        if isinstance(validated, list) and validated:
            return validated[0]

        return None

    @classmethod
    def _extract_brand_palette(cls, compiled_context: dict[str, Any]) -> list[str]:
        # Extracts brand palette from compiled context for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        """Extract validated brand colors from context."""
        brand_profile = compiled_context.get("brand_profile", {})
        if not isinstance(brand_profile, dict):
            return []

        colors = brand_profile.get("colors", {})
        if not isinstance(colors, dict):
            return []

        validated_colors = colors.get("validated_palette", [])

        if not validated_colors or not isinstance(validated_colors, list):
            # Fallback to primary/secondary colors
            validated_colors = [
                colors.get("primary"),
                colors.get("secondary"),
                colors.get("accent"),
            ]
            validated_colors = [str(c).strip().upper() for c in validated_colors if c]

        return [str(c).strip().upper() for c in validated_colors if c]

    @classmethod
    def _extract_template_colors(cls, scene_graph: dict[str, Any]) -> set[str]:
        # Extracts template colors from scene graph for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        """Extract all colors used in scene graph."""
        colors = set()
        for element in scene_graph.get("elements", []):
            if not isinstance(element, dict):
                continue
            style = element.get("style", {})
            if not isinstance(style, dict):
                continue
            for key in ("background_color", "fill_color", "text_color", "border_color", "background_fill", "fill"):
                if key in style and style[key]:
                    colors.add(str(style[key]).strip())
        return colors

    @classmethod
    def _apply_design_system_scene_defaults(
        cls,
        *,
        scene_graph_payload: dict[str, Any],
        request: AIOrchestrationRequest,
    ) -> dict[str, Any]:
        # Applies apply design system scene defaults from scene graph payload and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        payload = dict(scene_graph_payload)
        visual_identity = request.resolved_brand_context.get("visual_identity", {})
        if not isinstance(visual_identity, dict):
            return payload

        design_system = visual_identity.get("design_system", {})
        if not isinstance(design_system, dict):
            design_system = {}

        styles = cls._coerce_mapping(payload.get("styles"))
        background_style = (
            visual_identity.get("background_style")
            if isinstance(visual_identity.get("background_style"), dict)
            else (design_system.get("background_style", {}) if isinstance(design_system.get("background_style"), dict) else {})
        )
        if background_style:
            primary_hex = str(background_style.get("primary_hex") or "").strip()
            if primary_hex and not styles.get("background_fill"):
                styles["background_fill"] = primary_hex
            background_type = str(background_style.get("type") or background_style.get("dominant_mode") or "").strip()
            if background_type and not styles.get("background_style"):
                styles["background_style"] = background_type

        gradient_preferences = (
            visual_identity.get("gradient_preferences")
            if isinstance(visual_identity.get("gradient_preferences"), list)
            else (design_system.get("gradient_preferences", []) if isinstance(design_system.get("gradient_preferences"), list) else [])
        )
        first_gradient = next((item for item in gradient_preferences if isinstance(item, dict)), None)
        if first_gradient:
            if first_gradient.get("start_color") and not styles.get("gradient_from"):
                styles["gradient_from"] = first_gradient.get("start_color")
            if first_gradient.get("end_color") and not styles.get("gradient_to"):
                styles["gradient_to"] = first_gradient.get("end_color")

        promoted_logo_position = cls._normalize_logo_position_option(
            visual_identity.get("logo_position") or design_system.get("logo_anchor")
        )
        if promoted_logo_position and not styles.get("logo_position"):
            styles["logo_position"] = promoted_logo_position

        payload["styles"] = styles

        hierarchy = design_system.get("visual_hierarchy", {}) if isinstance(design_system.get("visual_hierarchy"), dict) else {}
        content_structure = design_system.get("content_structure", {}) if isinstance(design_system.get("content_structure"), dict) else {}
        image_treatment = design_system.get("image_treatment", {}) if isinstance(design_system.get("image_treatment"), dict) else {}
        visual_craft = design_system.get("visual_craft", {}) if isinstance(design_system.get("visual_craft"), dict) else {}
        composition_logic = design_system.get("composition_logic", {}) if isinstance(design_system.get("composition_logic"), dict) else {}
        subject_semantics = design_system.get("subject_semantics", {}) if isinstance(design_system.get("subject_semantics"), dict) else {}
        layout_preferences = design_system.get("layout_preferences", {}) if isinstance(design_system.get("layout_preferences"), dict) else {}

        focal_roles = {
            str(item).strip().lower()
            for item in (hierarchy.get("focal_roles") or [])
            if str(item).strip()
        }
        density_preference = str(((hierarchy.get("density_preferences") or [None])[0]) or "").strip().lower()
        whitespace_preference = str(((hierarchy.get("whitespace_preferences") or [None])[0]) or "").strip().lower()
        storytelling_mode = str(((content_structure.get("storytelling_modes") or [None])[0]) or "").strip().lower()
        cta_prominence = str(content_structure.get("cta_prominence") or "").strip().lower()
        preferred_zone_roles = {
            str(item).strip().lower()
            for item in (layout_preferences.get("preferred_zone_roles") or [])
            if str(item).strip()
        }
        image_styles = {
            str(item).strip().lower()
            for item in (image_treatment.get("styles") or [])
            if str(item).strip()
        }
        craft_depth = str(((visual_craft.get("depth_styles") or [None])[0]) or "").strip().lower()
        craft_rendering = str(((visual_craft.get("rendering_styles") or [None])[0]) or "").strip().lower()
        craft_lighting = str(((visual_craft.get("lighting_modes") or [None])[0]) or "").strip().lower()
        craft_polish = str(((visual_craft.get("polish_levels") or [None])[0]) or "").strip().lower()
        composition_balance = str(((composition_logic.get("balances") or [None])[0]) or "").strip().lower()
        composition_framing = str(((composition_logic.get("framings") or [None])[0]) or "").strip().lower()
        composition_layering = str(((composition_logic.get("layerings") or [None])[0]) or "").strip().lower()
        subject_scene_type = str(((subject_semantics.get("scene_types") or [None])[0]) or "").strip().lower()
        subject_abstraction = str(((subject_semantics.get("abstraction_levels") or [None])[0]) or "").strip().lower()
        subject_primary = [str(item).strip().lower() for item in (subject_semantics.get("primary_subjects") or []) if str(item).strip()][:4]
        subject_financial = [str(item).strip().lower() for item in (subject_semantics.get("financial_objects") or []) if str(item).strip()][:4]

        normalized_elements: list[dict[str, Any]] = []
        for element in cls._coerce_list(payload.get("elements")):
            if not isinstance(element, dict):
                normalized_elements.append(element)
                continue
            element_copy = dict(element)
            role = str(element_copy.get("role") or "").strip().lower()
            validation_hints = cls._coerce_mapping(element_copy.get("validation_hints"))
            style = cls._coerce_mapping(element_copy.get("style"))

            if role and role in focal_roles:
                validation_hints["brand_focal_role"] = True
            if density_preference and role in {"headline", "supporting_line", "body", "proof_points", "stat_highlights", "image"}:
                validation_hints.setdefault("design_density", density_preference)
            if whitespace_preference and role in {"headline", "supporting_line", "body", "proof_points", "image"}:
                validation_hints.setdefault("design_whitespace", whitespace_preference)
            if storytelling_mode and role in {"body", "proof_points", "stat_highlights", "section_label"}:
                validation_hints.setdefault("storytelling_mode", storytelling_mode)
            if cta_prominence and role == "cta":
                validation_hints.setdefault("cta_prominence", cta_prominence)
                if cta_prominence == "low":
                    style.setdefault("font_size", 22)
            if role in preferred_zone_roles:
                validation_hints.setdefault("preferred_zone_role", True)
            if image_styles and role == "image":
                validation_hints.setdefault("image_treatment_styles", sorted(image_styles))
                if craft_depth:
                    validation_hints.setdefault("visual_depth_style", craft_depth)
                if craft_rendering:
                    validation_hints.setdefault("visual_rendering_style", craft_rendering)
                if craft_lighting:
                    validation_hints.setdefault("visual_lighting_mode", craft_lighting)
                if craft_polish:
                    validation_hints.setdefault("visual_polish_level", craft_polish)
                if composition_balance:
                    validation_hints.setdefault("composition_balance", composition_balance)
                if composition_framing:
                    validation_hints.setdefault("composition_framing", composition_framing)
                if composition_layering:
                    validation_hints.setdefault("composition_layering", composition_layering)
                if subject_scene_type:
                    validation_hints.setdefault("subject_scene_type", subject_scene_type)
                if subject_abstraction:
                    validation_hints.setdefault("subject_abstraction_level", subject_abstraction)
                if subject_primary:
                    validation_hints.setdefault("primary_subjects", subject_primary)
                if subject_financial:
                    validation_hints.setdefault("financial_objects", subject_financial)

            if role == "background" and background_style:
                if background_style.get("primary_hex") and not style.get("background_fill"):
                    style["background_fill"] = background_style.get("primary_hex")
                if first_gradient and first_gradient.get("start_color") and first_gradient.get("end_color"):
                    style.setdefault("gradient_from", first_gradient.get("start_color"))
                    style.setdefault("gradient_to", first_gradient.get("end_color"))

            if validation_hints:
                element_copy["validation_hints"] = validation_hints
            if style:
                element_copy["style"] = style
            normalized_elements.append(element_copy)

        payload["elements"] = normalized_elements
        return payload

    @staticmethod
    def _design_system_prompt_guidance(visual_identity: dict[str, Any]) -> dict[str, str]:
        # Builds design system prompt guidance from visual identity for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        design_system = visual_identity.get("design_system", {}) if isinstance(visual_identity.get("design_system"), dict) else {}
        background_style = visual_identity.get("background_style") if isinstance(visual_identity.get("background_style"), dict) else (
            design_system.get("background_style", {}) if isinstance(design_system.get("background_style"), dict) else {}
        )
        component_motifs = visual_identity.get("component_motifs") if isinstance(visual_identity.get("component_motifs"), dict) else (
            design_system.get("component_motifs", {}) if isinstance(design_system.get("component_motifs"), dict) else {}
        )
        layout_preferences = design_system.get("layout_preferences", {}) if isinstance(design_system.get("layout_preferences"), dict) else {}
        hierarchy = design_system.get("visual_hierarchy", {}) if isinstance(design_system.get("visual_hierarchy"), dict) else {}
        content_structure = design_system.get("content_structure", {}) if isinstance(design_system.get("content_structure"), dict) else {}
        image_treatment = design_system.get("image_treatment", {}) if isinstance(design_system.get("image_treatment"), dict) else {}
        visual_craft = design_system.get("visual_craft", {}) if isinstance(design_system.get("visual_craft"), dict) else {}
        composition_logic = design_system.get("composition_logic", {}) if isinstance(design_system.get("composition_logic"), dict) else {}
        subject_semantics = design_system.get("subject_semantics", {}) if isinstance(design_system.get("subject_semantics"), dict) else {}
        editorial_patterns = design_system.get("editorial_patterns", {}) if isinstance(design_system.get("editorial_patterns"), dict) else {}
        brand_cues = design_system.get("brand_cues", {}) if isinstance(design_system.get("brand_cues"), dict) else {}

        motif_names = [str(key).replace("_", " ") for key, value in component_motifs.items() if value]
        return {
            "layout": AIOrchestratorService._normalize_metadata_text(
                layout_preferences.get("dominant") or ", ".join(str(item) for item in (layout_preferences.get("common") or [])[:3]),
                limit=120,
            ),
            "zones": AIOrchestratorService._normalize_metadata_text(
                ", ".join(str(item) for item in (layout_preferences.get("preferred_zone_roles") or [])[:6]),
                limit=140,
            ),
            "background": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    part for part in [
                        str(background_style.get("type") or background_style.get("dominant_mode") or "").strip(),
                        str(background_style.get("primary_hex") or "").strip(),
                    ] if part
                ),
                limit=120,
            ),
            "motifs": AIOrchestratorService._normalize_metadata_text(", ".join(motif_names[:5]), limit=140),
            "hierarchy": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *((hierarchy.get("focal_roles") or [])[:2]),
                        *((hierarchy.get("density_preferences") or [])[:1]),
                        *((hierarchy.get("whitespace_preferences") or [])[:1]),
                    ]
                    if str(item).strip()
                ),
                limit=140,
            ),
            "content_structure": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *((content_structure.get("storytelling_modes") or [])[:2]),
                        content_structure.get("cta_prominence"),
                    ]
                    if str(item).strip()
                ),
                limit=140,
            ),
            "image_treatment": AIOrchestratorService._normalize_metadata_text(
                ", ".join(str(item) for item in (image_treatment.get("styles") or [])[:3]),
                limit=120,
            ),
            "visual_craft": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *((visual_craft.get("depth_styles") or [])[:2]),
                        *((visual_craft.get("rendering_styles") or [])[:2]),
                        *((visual_craft.get("lighting_modes") or [])[:1]),
                        *((visual_craft.get("polish_levels") or [])[:1]),
                    ]
                    if str(item).strip()
                ),
                limit=160,
            ),
            "composition": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *((composition_logic.get("balances") or [])[:2]),
                        *((composition_logic.get("framings") or [])[:2]),
                        *((composition_logic.get("layerings") or [])[:1]),
                    ]
                    if str(item).strip()
                ),
                limit=160,
            ),
            "subjects": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *((subject_semantics.get("scene_types") or [])[:2]),
                        *((subject_semantics.get("primary_subjects") or [])[:3]),
                        *((subject_semantics.get("financial_objects") or [])[:3]),
                        *((subject_semantics.get("abstraction_levels") or [])[:1]),
                    ]
                    if str(item).strip()
                ),
                limit=180,
            ),
            "editorial": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *(((editorial_patterns.get("carousel") or {}).get("dominant_story_arc") or [])[:3]),
                        *(((editorial_patterns.get("infographic") or {}).get("dominant_story_arc") or [])[:2]),
                        *(((editorial_patterns.get("static") or {}).get("dominant_story_arc") or [])[:2]),
                    ]
                    if str(item).strip()
                ),
                limit=180,
            ),
            "brand_cues": AIOrchestratorService._normalize_metadata_text(
                ", ".join(
                    str(item)
                    for item in [
                        *((brand_cues.get("tone_keywords") or [])[:3]),
                        *((brand_cues.get("trust_markers") or [])[:3]),
                    ]
                    if str(item).strip()
                ),
                limit=160,
            ),
            "logo_position": AIOrchestratorService._normalize_metadata_text(
                visual_identity.get("logo_position") or design_system.get("logo_anchor"),
                limit=40,
            ),
        }

    @staticmethod
    def _normalize_logo_variant_hint(variant_hint: Any) -> str:
        # Normalizes logo variant hint from variant hint for AIOrchestratorService.
        # This shields downstream matching from casing, whitespace, and empty-value noise.
        text = str(variant_hint or "").strip().casefold()
        if not text:
            return ""
        replacements = {
            "dark on light": "dark_on_light",
            "light on dark": "light_on_dark",
            "full color": "dark_on_light",
            "full-colour": "dark_on_light",
            "full colour": "dark_on_light",
            "reverse": "light_on_dark",
            "inverse": "light_on_dark",
            "negative": "light_on_dark",
            "vertical": "stacked",
            "lockup": "horizontal",
            "brandmark": "icon_only",
            "icon only": "icon_only",
            "compact": "icon_only",
        }
        for source, target in replacements.items():
            text = text.replace(source, target)
        return re.sub(r"[^a-z0-9_]+", "_", text).strip("_")

    @classmethod
    def _requested_logo_variant(
        cls,
        creative_decision: CreativeDecisionPayload,
        scene_graph: GenerationSceneGraph,
    ) -> str | None:
        # Centralizes requested logo variant from creative decision and scene graph for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        asset_strategy = creative_decision.asset_strategy or {}
        candidate_values: list[Any] = [
            asset_strategy.get("logo_variant"),
            asset_strategy.get("logo_variant_type"),
            asset_strategy.get("logo_variant_hint"),
        ]
        logo_preferences = asset_strategy.get("logo_preferences")
        if isinstance(logo_preferences, dict):
            candidate_values.extend(
                [
                    logo_preferences.get("variant"),
                    logo_preferences.get("type"),
                    logo_preferences.get("background_variant"),
                ]
            )
        for element in scene_graph.elements:
            if element.role != "logo":
                continue
            if element.asset is not None:
                candidate_values.extend(
                    [
                        element.asset.variant,
                        element.asset.notes,
                    ]
                )
            candidate_values.extend(
                [
                    (element.style or {}).get("logo_variant"),
                    (element.validation_hints or {}).get("logo_variant"),
                ]
            )
        for value in candidate_values:
            normalized = cls._normalize_logo_variant_hint(value)
            if normalized:
                return normalized
        return None

    @classmethod
    def _score_logo_candidate_for_hint(cls, candidate: dict[str, Any], variant_hint: str) -> int:
        # Scores logo candidate hint from candidate and variant hint for AIOrchestratorService.
        # It reuses _normalize_logo_variant_hint so related checks follow the same rule.
        normalized_hint = cls._normalize_logo_variant_hint(variant_hint)
        score = int(candidate.get("source_priority") or 0)
        traits = candidate.get("traits", {}) or {}
        orientation = str(traits.get("orientation") or "flex")
        background_variant = str(traits.get("background_variant") or "")
        trust_level = str(candidate.get("trust_level") or "").strip().lower()
        if trust_level == "trusted":
            score += 4
        elif trust_level in {"usable_with_warning", "usable-with-warning"}:
            score += 1
        elif trust_level == "excluded":
            return -10_000

        if "dark_on_light" in normalized_hint:
            if background_variant == "light":
                score += 18
            elif background_variant == "dark":
                score -= 10
        if "light_on_dark" in normalized_hint:
            if background_variant == "dark":
                score += 18
            elif background_variant == "light":
                score -= 10
        if "horizontal" in normalized_hint or "wordmark" in normalized_hint:
            score += 16 if orientation == "horizontal" else -8
        if "stacked" in normalized_hint:
            score += 16 if orientation == "stacked" else -8
        if "icon_only" in normalized_hint:
            score += 14 if orientation == "icon" else -6
        return score

    @classmethod
    def _select_logo_candidate_for_render(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload,
        scene_graph: GenerationSceneGraph,
    ) -> tuple[str | None, str | None]:
        # Selects logo candidate from request state, creative decision, and scene graph for AIOrchestratorService.
        # It reuses _requested_logo_variant and _score_logo_candidate_for_hint so related checks follow the same rule.
        candidates = list(request.logo_asset_candidates or [])
        if not candidates:
            fallback_path = str(request.logo_asset_path or "").strip() or None
            return fallback_path, None

        requested_variant = cls._requested_logo_variant(creative_decision, scene_graph)
        if not requested_variant:
            fallback_path = str(request.logo_asset_path or "").strip() or None
            return fallback_path, None

        scored_candidates = sorted(
            candidates,
            key=lambda item: (
                cls._score_logo_candidate_for_hint(item, requested_variant),
                int(item.get("source_priority") or 0),
                str(item.get("storage_path") or ""),
            ),
            reverse=True,
        )
        selected_path = str(scored_candidates[0].get("storage_path") or "").strip() or None
        fallback_path = str(request.logo_asset_path or "").strip() or None
        if not selected_path:
            return fallback_path, requested_variant
        return selected_path, requested_variant

    @staticmethod
    def _reference_image_limit(studio_panel: dict[str, Any]) -> int:
        # Centralizes reference image limit from studio settings for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        format_name = str(studio_panel.get("format") or "static").strip().lower()
        if format_name == "carousel":
            return 4
        if format_name in {"pdf", "doc"}:
            return 4
        if format_name == "infographic":
            return 2
        return 1

    @staticmethod
    def _asset_label_text(asset: dict[str, Any]) -> str:
        # Extracts asset label text from asset record for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        metadata = asset.get("metadata") if isinstance(asset, dict) else {}
        metadata = metadata if isinstance(metadata, dict) else {}
        parts = [
            asset.get("asset_role"),
            asset.get("mime_type"),
            metadata.get("label"),
            metadata.get("name"),
            metadata.get("review_class"),
            metadata.get("review_reason"),
            asset.get("storage_path"),
        ]
        return " ".join(str(part).strip() for part in parts if str(part or "").strip()).casefold()

    @classmethod
    def _reference_asset_display_label(cls, asset: dict[str, Any]) -> str:
        # Extracts reference asset display label from asset record for AIOrchestratorService.
        # It calls _asset_metadata and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        storage_path = str(asset.get("storage_path") or "").strip()
        filename = storage_path.replace("\\", "/").rsplit("/", 1)[-1]
        stem = filename.rsplit(".", 1)[0] if "." in filename else filename
        return cls._normalize_metadata_text(
            asset.get("label")
            or asset.get("name")
            or metadata.get("label")
            or metadata.get("name")
            or metadata.get("title")
            or stem
            or asset.get("asset_role")
            or asset.get("mime_type"),
            limit=72,
        )

    @classmethod
    def _reference_asset_is_visual_source(cls, asset: dict[str, Any]) -> bool:
        # Extracts reference asset visual source from asset record for AIOrchestratorService.
        # It calls _asset_label_text to turn raw evidence into the structured signal the caller needs.
        if not isinstance(asset, dict):
            return False
        mime_type = str(asset.get("mime_type") or "").strip().lower()
        if not (mime_type.startswith("image/") or mime_type == "application/pdf"):
            return False
        trust_level = str(asset.get("trust_level") or "").strip().lower()
        if trust_level in {"excluded", "reference_only"}:
            return False
        asset_role = str(asset.get("asset_role") or "").strip().lower()
        if asset_role in {"logo", "logo_variant"}:
            return False
        label_text = cls._asset_label_text(asset)
        if any(token in label_text for token in ("logo", "wordmark", "brandmark", "lockup", "emblem")):
            return False
        return True

    @classmethod
    def _reference_asset_topic_tokens(cls, asset: dict[str, Any]) -> set[str]:
        # Extracts reference asset topic tokens from asset record for AIOrchestratorService.
        # It calls _asset_metadata and _asset_label_text to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        editorial_dna = metadata.get("editorial_dna") if isinstance(metadata.get("editorial_dna"), dict) else {}
        subject_semantics = metadata.get("subject_semantics") if isinstance(metadata.get("subject_semantics"), dict) else {}
        parts: list[str] = [
            cls._asset_label_text(asset),
            str(metadata.get("summary") or ""),
            str(metadata.get("description") or ""),
            str(metadata.get("tags") or ""),
            str(metadata.get("sequence_family") or ""),
            str(metadata.get("family_name") or ""),
            str(asset.get("storage_path") or ""),
            str(subject_semantics.get("domain_cues") or ""),
            str(subject_semantics.get("financial_objects") or ""),
            str(editorial_dna.get("headline_patterns") or ""),
        ]
        tokens = set(cls._normalized_prompt_tokens(" ".join(parts)))
        return {
            token
            for token in tokens
            if len(token) >= 3
            and any(ch.isalpha() for ch in token)
            and token not in cls.TOPIC_MATCH_NOISE_TOKENS
            and not re.fullmatch(r"[a-f0-9]{8,}", token)
        }

    @classmethod
    def _topic_alias_tokens(cls, values: list[str]) -> set[str]:
        # Centralizes topic alias tokens from values for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        aliases: set[str] = set()
        for value in values:
            ordered_tokens = [
                token
                for token in cls._normalized_prompt_tokens(value)
                if len(token) >= 3
                and any(ch.isalpha() for ch in token)
                and token not in cls.TOPIC_MATCH_NOISE_TOKENS
            ]
            if len(ordered_tokens) < 2:
                continue
            for window_size in range(2, min(len(ordered_tokens), 5) + 1):
                for index in range(0, len(ordered_tokens) - window_size + 1):
                    window = ordered_tokens[index : index + window_size]
                    acronym = "".join(token[0] for token in window if token and token[0].isalpha())
                    if 2 <= len(acronym) <= 6:
                        aliases.add(acronym)
        return aliases

    @classmethod
    def _request_topic_tokens(cls, request: AIOrchestrationRequest | None) -> set[str]:
        # Centralizes request topic tokens from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None:
            return set()
        parts = [
            str(getattr(request, "prompt", "") or ""),
            json.dumps(getattr(request, "research_editorial_brief", {}) or {}, default=str),
            json.dumps(getattr(request, "objective_context", {}) or {}, default=str),
        ]
        tokens = set(cls._normalized_prompt_tokens(" ".join(parts)))
        generic = {
            "linkedin",
            "carousel",
            "slide",
            "slides",
            "tone",
            "short",
            "scannable",
            "conversational",
            "analytical",
            "intelligent",
            "brand",
            "jiraaf",
            "platform",
            "finance",
            "investment",
            "investments",
            "alternative",
            "indian",
        } | cls.TOPIC_MATCH_NOISE_TOKENS
        topic_tokens = {
            token
            for token in tokens
            if len(token) >= 3
            and any(ch.isalpha() for ch in token)
            and not any(ch.isdigit() for ch in token)
            and token not in generic
            and not re.fullmatch(r"[a-f0-9]{8,}", token)
        }
        topic_tokens.update(cls._topic_alias_tokens(parts))
        return topic_tokens

    @staticmethod
    def _brand_subject_focus_terms(
        request: AIOrchestrationRequest | None,
        *,
        limit: int = 6,
    ) -> list[str]:
        # Extracts brand subject focus terms from request state and limit for AIOrchestratorService.
        # It calls _normalized_prompt_tokens to turn raw evidence into the structured signal the caller needs.
        if request is None:
            return []
        visual_identity = (
            request.resolved_brand_context.get("visual_identity", {})
            if isinstance(request.resolved_brand_context, dict)
            else {}
        )
        if not isinstance(visual_identity, dict):
            visual_identity = {}
        design_system = visual_identity.get("design_system") if isinstance(visual_identity.get("design_system"), dict) else {}
        subject_semantics = design_system.get("subject_semantics") if isinstance(design_system.get("subject_semantics"), dict) else {}
        values: list[Any] = [
            visual_identity.get("subject_semantics_summary"),
            visual_identity.get("image_treatment_summary"),
            visual_identity.get("content_structure_summary"),
            subject_semantics.get("scene_types"),
            subject_semantics.get("primary_subjects"),
            subject_semantics.get("financial_objects"),
            subject_semantics.get("domain_cues"),
        ]
        terms: list[str] = []
        seen: set[str] = set()
        for value in values:
            normalized_value = value
            if isinstance(value, (list, tuple, set, dict)):
                normalized_value = json.dumps(value, default=str)
            for token in AIOrchestratorService._normalized_prompt_tokens(str(normalized_value or "")):
                if (
                    len(token) < 4
                    or token in AIOrchestratorService.TOPIC_MATCH_NOISE_TOKENS
                    or token in seen
                    or any(ch.isdigit() for ch in token)
                ):
                    continue
                seen.add(token)
                terms.append(token)
                if len(terms) >= limit:
                    return terms
        return terms

    @classmethod
    def _reference_asset_topic_score(
        cls,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
    ) -> int:
        # Extracts reference asset topic from asset record and request state for AIOrchestratorService.
        # It calls _request_topic_tokens and _reference_asset_topic_tokens to turn raw evidence into the structured signal the caller needs.
        request_tokens = cls._request_topic_tokens(request)
        if not request_tokens:
            return 0
        asset_tokens = cls._reference_asset_topic_tokens(asset)
        overlap = request_tokens & asset_tokens
        if not overlap:
            return 0
        high_signal = {
            "fta",
            "trade",
            "tariff",
            "tariffs",
            "exports",
            "export",
            "zealand",
            "agreement",
            "services",
            "visa",
            "bilateral",
        }
        high_signal_overlap = overlap & high_signal
        score = (len(high_signal_overlap) * 30) + ((len(overlap) - len(high_signal_overlap)) * 4)
        if str(asset.get("mime_type") or "").strip().lower() == "application/pdf":
            score += 4
        return score

    @classmethod
    def _topic_relevant_reference_assets(
        cls,
        request: AIOrchestrationRequest | None,
        *,
        creative_decision: CreativeDecisionPayload | None,
        source_assets: list[dict[str, Any]] | None = None,
        limit: int = 2,
    ) -> list[dict[str, Any]]:
        # Extracts topic relevant reference assets from request state, creative decision, and source assets for AIOrchestratorService.
        # It calls _reference_asset_topic_score and _reference_asset_is_visual_source to turn raw evidence into the structured signal the caller needs.
        if request is None:
            return []
        raw_assets = source_assets or request.asset_catalog or request.reference_assets or []
        scored: list[tuple[int, dict[str, Any]]] = []
        for asset in raw_assets:
            if not isinstance(asset, dict) or not cls._reference_asset_is_visual_source(asset):
                continue
            score = cls._reference_asset_topic_score(asset, request=request)
            if score <= 0:
                continue
            scored.append((score, dict(asset)))
        if not scored:
            return []
        scored.sort(
            key=lambda item: (
                item[0],
                str(item[1].get("trust_level") or "").strip().lower() == "trusted",
                str(item[1].get("storage_path") or ""),
            ),
            reverse=True,
        )
        top_score = scored[0][0]
        if top_score >= 30:
            topic_floor = max(30, int(top_score * 0.6))
            scored = [item for item in scored if item[0] >= topic_floor]
        selected: list[dict[str, Any]] = []
        seen: set[str] = set()
        for _score, asset in scored:
            storage_path = str(asset.get("storage_path") or "").strip()
            if not storage_path or storage_path in seen:
                continue
            seen.add(storage_path)
            selected.append(asset)
            if len(selected) >= limit:
                break
        return selected

    @classmethod
    def _reference_topic_alignment_issue(
        cls,
        request: AIOrchestrationRequest,
        selected_reference_images: list[dict[str, Any]],
    ) -> str:
        # Centralizes reference topic alignment issue from request state and selected reference images for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        selected_scores = [
            cls._reference_asset_topic_score(asset, request=request)
            for asset in selected_reference_images
            if isinstance(asset, dict)
        ]
        best_selected_score = max(selected_scores or [0])
        candidate_scores = [
            cls._reference_asset_topic_score(asset, request=request)
            for asset in (request.asset_catalog or request.reference_assets or [])
            if isinstance(asset, dict) and cls._reference_asset_is_visual_source(asset)
        ]
        best_available_score = max(candidate_scores or [0])
        if best_available_score < 30:
            return ""
        if best_selected_score <= 0:
            return "reference_topic_mismatch"
        if best_selected_score < int(best_available_score * 0.75):
            return "reference_topic_mismatch"
        return ""

    @staticmethod
    def _merge_reference_asset_lists(*groups: list[dict[str, Any]] | None) -> list[dict[str, Any]]:
        # Merges reference asset lists for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        merged: list[dict[str, Any]] = []
        seen: set[str] = set()
        for group in groups:
            for asset in group or []:
                if not isinstance(asset, dict):
                    continue
                storage_path = str(asset.get("storage_path") or "").strip()
                if not storage_path or storage_path in seen:
                    continue
                seen.add(storage_path)
                merged.append(dict(asset))
        return merged

    @classmethod
    def _asset_metadata(cls, asset: dict[str, Any]) -> dict[str, Any]:
        # Extracts asset metadata from asset record for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        metadata = asset.get("metadata") if isinstance(asset, dict) else {}
        return metadata if isinstance(metadata, dict) else {}

    @classmethod
    def _reference_asset_format_family(cls, asset: dict[str, Any]) -> str:
        # Extracts reference asset family from asset record for AIOrchestratorService.
        # It calls _asset_metadata and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        editorial_dna = metadata.get("editorial_dna") if isinstance(metadata.get("editorial_dna"), dict) else {}
        analysis = asset.get("analysis") if isinstance(asset.get("analysis"), dict) else {}
        normalized_analysis = (
            metadata.get("normalized_metadata")
            if isinstance(metadata.get("normalized_metadata"), dict)
            else {}
        )
        for candidate in (
            asset.get("format_family"),
            metadata.get("format_family"),
            metadata.get("surface_format"),
            metadata.get("content_format"),
            editorial_dna.get("format_family"),
            analysis.get("format_family"),
            normalized_analysis.get("format_family"),
        ):
            value = cls._normalize_metadata_text(candidate, limit=32).casefold()
            if value:
                return value
        return ""

    @classmethod
    def _requested_reference_format_family(cls, request: AIOrchestrationRequest | None) -> str:
        # Centralizes requested reference family from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if request is None:
            return ""
        format_name = cls._normalize_metadata_text(
            ((request.studio_panel or {}) if isinstance(request.studio_panel, dict) else {}).get("format"),
            limit=24,
        ).casefold()
        return format_name if format_name in {"static", "infographic", "carousel"} else ""

    @classmethod
    def _reference_asset_sequence_family(cls, asset: dict[str, Any]) -> str:
        # Extracts reference asset sequence family from asset record for AIOrchestratorService.
        # It calls _asset_metadata and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        editorial_dna = metadata.get("editorial_dna") if isinstance(metadata.get("editorial_dna"), dict) else {}
        for candidate in (
            asset.get("family_name"),
            metadata.get("family_name"),
            metadata.get("sequence_family"),
            metadata.get("sequence_pack_family"),
            metadata.get("reference_family"),
            metadata.get("sample_family"),
            editorial_dna.get("family_name"),
        ):
            value = cls._normalize_metadata_text(candidate, limit=80).casefold()
            if value:
                return value
        storage_path = str(asset.get("storage_path") or "").replace("\\", "/").strip()
        filename = storage_path.rsplit("/", 1)[-1]
        stem = filename.rsplit(".", 1)[0] if "." in filename else filename
        normalized = re.sub(r"[_\s]+", "-", stem.casefold()).strip("-")
        normalized = re.sub(r"(?:-|_)?(?:slide|page)[-_ ]*\d+$", "", normalized)
        normalized = re.sub(r"(?:-|_)\d+$", "", normalized)
        return normalized.strip("-")

    @classmethod
    def _reference_asset_sequence_position(cls, asset: dict[str, Any]) -> int | None:
        # Measures reference asset sequence position from asset record for AIOrchestratorService.
        # It calls _asset_metadata to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        for candidate in (
            asset.get("reference_slide_index"),
            asset.get("slide_index"),
            metadata.get("reference_slide_index"),
            metadata.get("slide_index"),
            metadata.get("page_index"),
            metadata.get("page_number"),
            metadata.get("reference_page_index"),
        ):
            try:
                value = int(candidate)
            except (TypeError, ValueError):
                continue
            if value > 0:
                return value
        storage_path = str(asset.get("storage_path") or "").replace("\\", "/").strip()
        filename = storage_path.rsplit("/", 1)[-1]
        stem = filename.rsplit(".", 1)[0] if "." in filename else filename
        match = re.search(r"(?:^|[-_ ])(?:slide|page)?[-_ ]*(\d{1,2})$", stem.casefold())
        if match:
            try:
                value = int(match.group(1))
            except (TypeError, ValueError):
                return None
            if value > 0:
                return value
        return None

    @classmethod
    def _reference_asset_is_carousel_native(cls, asset: dict[str, Any]) -> bool:
        # Extracts reference asset carousel native from asset record for AIOrchestratorService.
        # It calls _reference_asset_format_family and _asset_metadata to turn raw evidence into the structured signal the caller needs.
        if not isinstance(asset, dict):
            return False
        format_family = cls._reference_asset_format_family(asset)
        if format_family in {"carousel", "carousel_series", "slide_deck", "slide_series"}:
            return True
        metadata = cls._asset_metadata(asset)
        mime_type = str(asset.get("mime_type") or "").strip().lower()
        page_count = cls._reference_asset_declared_page_count(asset)
        if mime_type == "application/pdf" and page_count > 1:
            return True
        for candidate in (
            metadata.get("slide_count"),
            metadata.get("sequence_slide_count"),
            metadata.get("reference_slide_count"),
        ):
            try:
                if int(candidate) > 1:
                    return True
            except (TypeError, ValueError):
                continue
        label_text = cls._asset_label_text(asset)
        if any(
            token in label_text
            for token in (
                "carousel",
                "slide ",
                "slides",
                "slide-",
                "slide_",
                "page ",
                "page-",
                "page_",
                "series",
                "deck",
                "set",
                "swipe",
            )
        ):
            return True
        return False

    @classmethod
    def _reference_asset_is_explicit_single_surface(cls, asset: dict[str, Any]) -> bool:
        # Extracts reference asset explicit single surface from asset record for AIOrchestratorService.
        # It calls _reference_asset_format_family and _asset_label_text to turn raw evidence into the structured signal the caller needs.
        format_family = cls._reference_asset_format_family(asset)
        if format_family in {"static", "infographic", "story", "poster"}:
            return True
        label_text = cls._asset_label_text(asset)
        return any(token in label_text for token in ("infographic", "poster", "single post", "single-page"))

    @classmethod
    def _filter_reference_assets_for_requested_format(
        cls,
        assets: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
    ) -> list[dict[str, Any]]:
        # Extracts filter reference assets requested from assets and request state for AIOrchestratorService.
        # It calls _requested_reference_format_family and _reference_asset_format_family to turn raw evidence into the structured signal the caller needs.
        requested_family = cls._requested_reference_format_family(request)
        if not requested_family:
            return [dict(asset) for asset in assets if isinstance(asset, dict)]
        exact_matches = [
            dict(asset)
            for asset in assets
            if isinstance(asset, dict) and cls._reference_asset_format_family(asset) == requested_family
        ]
        if exact_matches:
            return exact_matches
        if requested_family == "carousel":
            carousel_native = [
                dict(asset)
                for asset in assets
                if isinstance(asset, dict) and cls._reference_asset_is_carousel_native(asset)
            ]
            if carousel_native:
                retained_paths = {
                    str(asset.get("storage_path") or "").strip()
                    for asset in carousel_native
                    if str(asset.get("storage_path") or "").strip()
                }
                neutral_supporting_assets = [
                    dict(asset)
                    for asset in assets
                    if isinstance(asset, dict)
                    and str(asset.get("storage_path") or "").strip() not in retained_paths
                    and not cls._reference_asset_is_explicit_single_surface(asset)
                ]
                return [*carousel_native, *neutral_supporting_assets]
        return [dict(asset) for asset in assets if isinstance(asset, dict)]

    @classmethod
    def _carousel_reference_family_size(
        cls,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
    ) -> int:
        # Centralizes carousel reference family size from asset record and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        family = cls._reference_asset_sequence_family(asset)
        if not family or request is None:
            return 0
        raw_assets = request.asset_catalog or request.reference_assets or []
        count = 0
        for candidate in raw_assets:
            if not isinstance(candidate, dict):
                continue
            if not cls._reference_asset_is_visual_source(candidate):
                continue
            if not cls._reference_asset_is_carousel_native(candidate):
                continue
            if cls._reference_asset_sequence_family(candidate) == family:
                count += 1
        return count

    @classmethod
    def _dominant_carousel_reference_family_assets(
        cls,
        assets: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
    ) -> list[dict[str, Any]]:
        # Extracts dominant carousel reference family assets from assets and request state for AIOrchestratorService.
        # It calls _reference_asset_sequence_family and _reference_asset_topic_score to turn raw evidence into the structured signal the caller needs.
        family_groups: dict[str, list[dict[str, Any]]] = {}
        for asset in assets:
            if not isinstance(asset, dict):
                continue
            family = cls._reference_asset_sequence_family(asset)
            if not family:
                continue
            family_groups.setdefault(family, []).append(dict(asset))
        if not family_groups:
            return [dict(asset) for asset in assets if isinstance(asset, dict)]

        def family_score(items: list[dict[str, Any]]) -> tuple[int, int, int]:
            # Centralizes family from items for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            return (
                len(items),
                sum(cls._reference_asset_topic_score(item, request=request) for item in items) if request is not None else 0,
                sum(1 for item in items if str(item.get("trust_level") or "").strip().lower() == "trusted"),
            )

        best_family = max(
            family_groups.items(),
            key=lambda entry: (family_score(entry[1]), entry[0]),
        )[0]
        return family_groups.get(best_family, [dict(asset) for asset in assets if isinstance(asset, dict)])

    @classmethod
    def _selected_carousel_reference_family_assets(
        cls,
        assets: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
    ) -> list[dict[str, Any]]:
        # Selects selected carousel reference family assets from assets, request state, and creative decision for AIOrchestratorService.
        # It reuses _normalize_template_lookup_key and _reference_asset_sequence_family so related checks follow the same rule.
        if request is None:
            return []
        target_candidates = [
            cls._normalize_template_lookup_key(
                getattr(creative_decision, "selected_template_name", "") if creative_decision is not None else ""
            ),
            cls._normalize_template_lookup_key(
                ((request.layout_decision or {}) if isinstance(request.layout_decision, dict) else {}).get("template_name")
            ),
            cls._normalize_template_lookup_key(
                ((request.layout_decision or {}) if isinstance(request.layout_decision, dict) else {}).get("selected_template_name")
            ),
            cls._normalize_template_lookup_key(
                ((request.template_context or {}) if isinstance(request.template_context, dict) else {}).get("template_name")
            ),
        ]
        targets = [target for target in target_candidates if target]
        if not targets:
            return []

        matched: list[dict[str, Any]] = []
        for asset in assets:
            if not isinstance(asset, dict):
                continue
            family_name = cls._normalize_template_lookup_key(cls._reference_asset_sequence_family(asset))
            label_name = cls._normalize_template_lookup_key(
                str(((asset.get("metadata") or {}) if isinstance(asset.get("metadata"), dict) else {}).get("label") or asset.get("storage_path") or "")
            )
            if any(
                target
                and (
                    (family_name and (family_name.startswith(target) or target.startswith(family_name)))
                    or (label_name and (label_name.startswith(target) or target in label_name))
                )
                for target in targets
            ):
                matched.append(dict(asset))
        return matched

    @classmethod
    def _asset_logo_cue_text(cls, asset: dict[str, Any]) -> str:
        # Extracts asset logo cue text from asset record for AIOrchestratorService.
        # It calls _asset_metadata and _asset_label_text to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        parts: list[str] = [cls._asset_label_text(asset)]
        skip_value_keys = {
            "brand",
            "brand_name",
            "brandname",
            "tenant",
            "tenant_name",
            "owner",
            "summary",
            "style_summary",
            "structure_summary",
            "content",
            "layout_structure",
            "reusable_zones",
            "editable_zones",
            "zones",
            "design_system",
        }

        def collect(value: Any, *, key: str = "", depth: int = 0) -> None:
            # Centralizes collect from input value, key, and depth for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if depth > 4:
                return
            if isinstance(value, dict):
                for child_key, child_value in value.items():
                    child_key_text = str(child_key or "").strip().casefold()
                    if child_key_text:
                        parts.append(child_key_text)
                    if child_key_text in skip_value_keys:
                        continue
                    collect(child_value, key=child_key_text, depth=depth + 1)
                return
            if isinstance(value, (list, tuple, set)):
                for item in value:
                    collect(item, key=key, depth=depth + 1)
                return
            text = str(value or "").strip()
            if text:
                parts.append(text.casefold())

        collect(metadata)
        analysis = asset.get("analysis") if isinstance(asset, dict) else None
        collect(analysis, key="analysis")
        return " ".join(parts)

    @classmethod
    def _reference_image_has_logo_cue(cls, asset: dict[str, Any]) -> bool:
        # Centralizes reference image logo cue from asset record for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        asset_role = str(asset.get("asset_role") or "").strip().casefold()
        if asset_role in {"logo", "logo_variant"}:
            return True
        metadata = cls._asset_metadata(asset)
        normalized_metadata = metadata.get("normalized_metadata")
        normalized_metadata = normalized_metadata if isinstance(normalized_metadata, dict) else {}
        source_metadata = metadata.get("source_metadata")
        source_metadata = source_metadata if isinstance(source_metadata, dict) else {}
        analysis = asset.get("analysis") if isinstance(asset.get("analysis"), dict) else {}
        explicit_logo_classes = {
            str(asset.get("review_class") or "").strip().casefold(),
            str(asset.get("asset_kind") or "").strip().casefold(),
            str(metadata.get("review_class") or "").strip().casefold(),
            str(metadata.get("asset_kind") or "").strip().casefold(),
            str(normalized_metadata.get("review_class") or "").strip().casefold(),
            str(source_metadata.get("review_class") or "").strip().casefold(),
            str(source_metadata.get("origin_category") or "").strip().casefold(),
        }
        if explicit_logo_classes & {"logo", "logo_variant", "wordmark", "brandmark"}:
            return True
        for candidate in (
            metadata.get("contains_logo"),
            metadata.get("logo_detected"),
            metadata.get("watermark_detected"),
            normalized_metadata.get("contains_logo"),
            normalized_metadata.get("logo_detected"),
            source_metadata.get("contains_logo"),
            source_metadata.get("logo_detected"),
            analysis.get("contains_logo"),
            analysis.get("logo_detected"),
            analysis.get("watermark_detected"),
        ):
            if candidate is True:
                return True
        cue_text = " ".join(
            str(part or "").strip()
            for part in (
                cls._asset_label_text(asset),
                str(metadata.get("ocr_text") or ""),
                str(metadata.get("detected_text") or ""),
                str(metadata.get("alt_text") or ""),
                json.dumps(analysis, ensure_ascii=False, sort_keys=True) if analysis else "",
                json.dumps(normalized_metadata, ensure_ascii=False, sort_keys=True) if normalized_metadata else "",
                json.dumps(source_metadata, ensure_ascii=False, sort_keys=True) if source_metadata else "",
            )
            if str(part or "").strip()
        ).casefold()
        return any(
            token in cue_text
            for token in (
                "logo",
                "wordmark",
                "brandmark",
                "brand mark",
                "brand signature",
                "lockup",
                "monogram",
                "watermark",
            )
        )

    @classmethod
    def _reference_conditioning_surface_kind(cls, asset: dict[str, Any]) -> str:
        # Centralizes reference conditioning surface kind from asset record for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        metadata = cls._asset_metadata(asset)
        normalized_metadata = metadata.get("normalized_metadata")
        normalized_metadata = normalized_metadata if isinstance(normalized_metadata, dict) else {}
        source_metadata = metadata.get("source_metadata")
        source_metadata = source_metadata if isinstance(source_metadata, dict) else {}
        candidates = (
            metadata.get("surface_kind"),
            normalized_metadata.get("surface_kind"),
            source_metadata.get("surface_kind"),
        )
        return next((str(value).strip().lower() for value in candidates if str(value or "").strip()), "")

    @classmethod
    def _reference_conditioning_overlay_safe(cls, asset: dict[str, Any]) -> bool | None:
        # Centralizes reference conditioning overlay safe from asset record for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        metadata = cls._asset_metadata(asset)
        normalized_metadata = metadata.get("normalized_metadata")
        normalized_metadata = normalized_metadata if isinstance(normalized_metadata, dict) else {}
        source_metadata = metadata.get("source_metadata")
        source_metadata = source_metadata if isinstance(source_metadata, dict) else {}
        for value in (
            metadata.get("overlay_safe"),
            normalized_metadata.get("overlay_safe"),
            source_metadata.get("overlay_safe"),
        ):
            if isinstance(value, bool):
                return value
        return None

    @classmethod
    def _reference_text_surface_metadata_sources(cls, asset: dict[str, Any]) -> list[dict[str, Any]]:
        # Extracts reference text surface metadata sources from asset record for AIOrchestratorService.
        # It calls _asset_metadata to turn raw evidence into the structured signal the caller needs.
        metadata = cls._asset_metadata(asset)
        sources: list[dict[str, Any]] = []
        for candidate in (
            metadata,
            metadata.get("normalized_metadata") if isinstance(metadata.get("normalized_metadata"), dict) else {},
            metadata.get("source_metadata") if isinstance(metadata.get("source_metadata"), dict) else {},
            asset.get("analysis") if isinstance(asset.get("analysis"), dict) else {},
        ):
            if isinstance(candidate, dict) and candidate not in sources:
                sources.append(candidate)
        return sources

    @classmethod
    def _reference_asset_sample_blueprints(cls, asset: dict[str, Any]) -> list[dict[str, Any]]:
        # Extracts reference asset sample blueprints from asset record for AIOrchestratorService.
        # It calls _reference_text_surface_metadata_sources to turn raw evidence into the structured signal the caller needs.
        blueprints: list[dict[str, Any]] = []
        for source in cls._reference_text_surface_metadata_sources(asset):
            candidates: list[Any] = [
                source.get("sample_page_blueprint"),
                source.get("blueprint_payload"),
                source.get("rendered_blueprint"),
            ]
            template_structure = source.get("template_structure")
            if isinstance(template_structure, dict):
                candidates.append(template_structure.get("sample_page_blueprint"))
            for candidate in candidates:
                if not isinstance(candidate, dict):
                    continue
                nested = candidate.get("sample_page_blueprint")
                if isinstance(nested, dict):
                    candidate = nested
                if isinstance(candidate, dict) and candidate and candidate not in blueprints:
                    blueprints.append(candidate)
        return blueprints

    @classmethod
    def _reference_asset_text_surface_evidence(cls, asset: dict[str, Any]) -> str:
        # Extracts reference asset text surface evidence from asset record for AIOrchestratorService.
        # It calls _reference_text_surface_metadata_sources and _reference_asset_sample_blueprints to turn raw evidence into the structured signal the c.
        parts: list[str] = []
        ocr_sources: list[dict[str, Any]] = []
        for source in cls._reference_text_surface_metadata_sources(asset):
            for key in (
                "ocr_text",
                "detected_text",
                "recognized_text",
                "extracted_text",
                "extracted_text_preview",
                "surface_text",
                "rendered_text",
                "visible_text",
                "text_preview",
            ):
                value = cls._normalize_metadata_text(source.get(key), limit=360)
                if value:
                    parts.append(value)
            for ocr_candidate in (
                source.get("ocr_structure"),
                source.get("ocr"),
                source.get("vision_ocr"),
            ):
                if isinstance(ocr_candidate, dict) and ocr_candidate not in ocr_sources:
                    ocr_sources.append(ocr_candidate)
        for blueprint in cls._reference_asset_sample_blueprints(asset):
            ocr_candidate = blueprint.get("ocr_structure")
            if isinstance(ocr_candidate, dict) and ocr_candidate not in ocr_sources:
                ocr_sources.append(ocr_candidate)
        for ocr in ocr_sources:
            for key in ("full_text", "ocr_text", "detected_text", "recognized_text", "visible_text", "text_preview"):
                value = cls._normalize_metadata_text(ocr.get(key), limit=360)
                if value:
                    parts.append(value)
            block_summary = ocr.get("block_summary")
            if isinstance(block_summary, list):
                for block in block_summary[:8]:
                    if not isinstance(block, dict):
                        continue
                    value = cls._normalize_metadata_text(
                        block.get("text_excerpt") or block.get("text") or block.get("label") or block.get("value"),
                        limit=160,
                    )
                    if value:
                        parts.append(value)
        return cls._normalize_metadata_text(" ".join(parts), limit=1200)

    @classmethod
    def _reference_asset_readable_text_block_count(cls, asset: dict[str, Any]) -> int:
        # Counts reference asset readable text block count from asset record for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        counts: list[int] = []
        for source in cls._reference_text_surface_metadata_sources(asset):
            ocr_candidates = (
                source.get("ocr_structure"),
                source.get("ocr"),
                source.get("vision_ocr"),
            )
            for ocr in ocr_candidates:
                if not isinstance(ocr, dict):
                    continue
                for key in ("readable_text_blocks", "text_block_count", "visible_text_blocks"):
                    try:
                        count = int(ocr.get(key))
                    except (TypeError, ValueError):
                        continue
                    if count > 0:
                        counts.append(count)
                block_summary = ocr.get("block_summary")
                if isinstance(block_summary, list):
                    text_blocks = [
                        block
                        for block in block_summary
                        if isinstance(block, dict)
                        and cls._normalize_metadata_text(
                            block.get("text_excerpt") or block.get("text") or block.get("label") or block.get("value"),
                            limit=80,
                        )
                    ]
                    if text_blocks:
                        counts.append(len(text_blocks))
        for blueprint in cls._reference_asset_sample_blueprints(asset):
            ocr = blueprint.get("ocr_structure")
            if not isinstance(ocr, dict):
                continue
            for key in ("readable_text_blocks", "text_block_count", "visible_text_blocks"):
                try:
                    count = int(ocr.get(key))
                except (TypeError, ValueError):
                    continue
                if count > 0:
                    counts.append(count)
        return max(counts or [0])

    @classmethod
    def _reference_asset_layout_text_module_count(cls, asset: dict[str, Any]) -> int:
        # Counts reference asset layout text module count from asset record for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        counts: list[int] = []
        for blueprint in cls._reference_asset_sample_blueprints(asset):
            module_counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
            total = 0
            for key in (
                "top_text_band_count",
                "text_block_count",
                "horizontal_band_count",
                "card_like_count",
                "row_module_count",
                "list_item_count",
            ):
                try:
                    total += max(int(module_counts.get(key) or 0), 0)
                except (TypeError, ValueError):
                    continue
            if total > 0:
                counts.append(total)
        return max(counts or [0])

    @classmethod
    def _reference_asset_is_text_bearing_surface(cls, asset: dict[str, Any]) -> bool:
        # Extracts reference asset text bearing surface from asset record for AIOrchestratorService.
        # It calls _reference_asset_format_family and _reference_conditioning_surface_kind to turn raw evidence into the structured signal the caller ne.
        asset_role = str(asset.get("asset_role") or "").strip().lower()
        surface_role = asset_role in {"reference_creative", "template_preview", "template", "hero_image", "image", "photo"}
        format_family = cls._reference_asset_format_family(asset)
        surface_kind = cls._reference_conditioning_surface_kind(asset)
        surface_kind_normalized = surface_kind.replace("-", "_").replace(" ", "_")
        if not surface_role and format_family not in {"static", "infographic", "poster", "story"}:
            return False
        if "flattened" in surface_kind_normalized and "text" in surface_kind_normalized:
            return True
        if any(token in surface_kind_normalized for token in ("text_heavy", "text_surface", "rendered_text")):
            return True
        for source in cls._reference_text_surface_metadata_sources(asset):
            for key in (
                "text_heavy",
                "contains_readable_text",
                "has_readable_text",
                "visible_text_present",
                "generated_text_present",
                "rendered_text_present",
            ):
                if source.get(key) is True:
                    return True
        if cls._reference_asset_readable_text_block_count(asset) >= 2:
            return True
        if cls._reference_asset_layout_text_module_count(asset) >= 2:
            return True
        evidence_tokens = cls._normalized_prompt_tokens(cls._reference_asset_text_surface_evidence(asset))
        return len(evidence_tokens) >= 6

    @classmethod
    def _reference_tokens_align_with_request_topic(
        cls,
        reference_tokens: set[str],
        *,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Centralizes reference tokens align request topic from reference tokens and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        request_tokens = cls._request_topic_tokens(request)
        if not request_tokens or not reference_tokens:
            return False
        overlap = request_tokens & reference_tokens
        if not overlap:
            return False
        weak_overlap_tokens = {
            "compare",
            "compared",
            "compares",
            "comparing",
            "comparison",
            "country",
            "countries",
            "global",
            "india",
            "indian",
            "rank",
            "ranking",
            "rankings",
            "rate",
            "rates",
            "static",
            "top",
            "world",
            "worldwide",
        }
        return any(token not in weak_overlap_tokens for token in overlap)

    @classmethod
    def _reference_text_surface_topic_mismatch(
        cls,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Extracts reference text surface topic mismatch from asset record and request state for AIOrchestratorService.
        # It calls _reference_asset_text_surface_evidence and _reference_asset_topic_tokens to turn raw evidence into the structured signal the caller n.
        if request is None or not cls._request_topic_tokens(request):
            return False
        evidence = cls._reference_asset_text_surface_evidence(asset)
        evidence_tokens = {
            token
            for token in cls._normalized_prompt_tokens(evidence)
            if len(token) >= 3
            and any(ch.isalpha() for ch in token)
            and token not in cls.TOPIC_MATCH_NOISE_TOKENS
            and not re.fullmatch(r"[a-f0-9]{8,}", token)
        }
        if evidence_tokens:
            return not cls._reference_tokens_align_with_request_topic(evidence_tokens, request=request)
        asset_tokens = cls._reference_asset_topic_tokens(asset)
        if asset_tokens:
            return not cls._reference_tokens_align_with_request_topic(asset_tokens, request=request)
        return cls._reference_asset_topic_score(asset, request=request) <= 0

    @classmethod
    def _reference_asset_is_mismatched_text_surface_conditioning(
        cls,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Extracts reference asset mismatched text surface conditioning from asset record and request state for AIOrchestratorService.
        # It calls _requested_reference_format_family and _reference_asset_format_family to turn raw evidence into the structured signal the caller needs.
        requested_family = cls._requested_reference_format_family(request)
        asset_family = cls._reference_asset_format_family(asset)
        return (
            (requested_family in {"static", "infographic"} or asset_family in {"static", "infographic", "poster", "story"})
            and cls._reference_asset_is_text_bearing_surface(asset)
            and cls._reference_text_surface_topic_mismatch(asset, request=request)
        )

    @classmethod
    def _reference_asset_has_layout_surface_metadata(cls, asset: dict[str, Any]) -> bool:
        # Extracts reference asset layout surface metadata from asset record for AIOrchestratorService.
        # It calls _reference_text_surface_metadata_sources and _reference_asset_sample_blueprints to turn raw evidence into the structured signal the c.
        layout_keys = {
            "sample_page_blueprint",
            "blueprint_payload",
            "rendered_blueprint",
            "template_structure",
            "zone_map",
            "reference_zone_map",
            "module_counts",
            "visual_permissions",
            "image_zones",
            "layout_dna",
            "layout_blueprint",
        }
        for source in cls._reference_text_surface_metadata_sources(asset):
            if any(isinstance(source.get(key), (dict, list)) and bool(source.get(key)) for key in layout_keys):
                return True
        return bool(cls._reference_asset_sample_blueprints(asset))

    @classmethod
    def _reference_asset_is_layout_only_static_infographic_surface(
        cls,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Extracts reference asset layout only static infographic surface from asset record and request state for AIOrchestratorService.
        # It calls _requested_reference_format_family and _reference_asset_format_family to turn raw evidence into the structured signal the caller needs.
        requested_family = cls._requested_reference_format_family(request)
        if requested_family not in {"static", "infographic"}:
            return False
        asset_role = str(asset.get("asset_role") or "").strip().lower()
        asset_family = cls._reference_asset_format_family(asset)
        if asset_role in {"template", "template_preview"}:
            return True
        if asset_role not in {"reference_creative", "reference"}:
            return False
        if asset_family in {"static", "infographic", "poster", "story"}:
            return True
        if cls._reference_asset_is_text_bearing_surface(asset):
            return True
        if cls._reference_asset_has_layout_surface_metadata(asset):
            return True
        label_text = cls._asset_label_text(asset)
        return any(
            token in label_text
            for token in (
                "layout reference",
                "static layout",
                "infographic layout",
                "template surface",
                "single post",
                "single-page",
            )
        )

    @classmethod
    def _is_reference_image_asset(cls, asset: dict[str, Any]) -> bool:
        # Checks reference image asset from asset record for AIOrchestratorService.
        # It reuses _asset_label_text so related checks follow the same rule.
        if not isinstance(asset, dict):
            return False
        mime_type = str(asset.get("mime_type") or "").strip().lower()
        if not mime_type.startswith("image/"):
            return False
        trust_level = str(asset.get("trust_level") or "").strip().lower()
        if trust_level in {"excluded", "reference_only"}:
            return False
        label_text = cls._asset_label_text(asset)
        if any(token in label_text for token in ("logo", "wordmark", "brandmark", "lockup", "emblem")):
            return False
        asset_role = str(asset.get("asset_role") or "").strip().lower()
        if asset_role in {"logo", "logo_variant"}:
            return False
        return True

    @classmethod
    def _is_conditioning_safe_reference_image_asset(
        cls,
        asset: dict[str, Any],
        *,
        creative_decision: CreativeDecisionPayload,
        request: AIOrchestrationRequest | None = None,
    ) -> bool:
        # Checks conditioning safe reference image asset from asset record, creative decision, and request state for AIOrchestratorService.
        # It reuses _asset_metadata and _reference_conditioning_surface_kind so related checks follow the same rule.
        if not cls._reference_asset_is_visual_source(asset):
            return False
        metadata = cls._asset_metadata(asset)
        asset_role = str(asset.get("asset_role") or "").strip().lower()
        review_status = str(metadata.get("review_status") or "").strip().lower()
        surface_kind = cls._reference_conditioning_surface_kind(asset)
        overlay_safe = cls._reference_conditioning_overlay_safe(asset)
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata,
        )
        if review_status in {"reference_only", "excluded"}:
            return False
        if surface_kind == "reference_only_flattened_text":
            return False
        if overlay_safe is False:
            return False
        if cls._reference_asset_is_mismatched_text_surface_conditioning(asset, request=request):
            return False
        if cls._reference_asset_is_layout_only_static_infographic_surface(asset, request=request):
            return False
        if asset_role == "template_preview":
            allow_conditioning = bool(
                metadata.get("allow_conditioning")
                or ((metadata.get("normalized_metadata") or {}) if isinstance(metadata.get("normalized_metadata"), dict) else {}).get(
                    "allow_conditioning"
                )
            )
            if not allow_conditioning:
                return False
        if template_surface_policy == "style_reference_only":
            preferred_paths = cls._preferred_sequence_reference_paths(request, creative_decision=creative_decision)
            asset_storage_path = str(asset.get("storage_path") or "").strip()
            if request is None and overlay_safe is True:
                return True
            if asset_role in {"template_preview", "reference_creative", "template"} and asset_storage_path not in preferred_paths:
                topic_score = cls._reference_asset_topic_score(asset, request=request) if request is not None else 0
                best_topic_score = 0
                if request is not None:
                    best_topic_score = max(
                        [
                            cls._reference_asset_topic_score(candidate, request=request)
                            for candidate in (request.asset_catalog or request.reference_assets or [])
                            if isinstance(candidate, dict) and cls._reference_asset_is_visual_source(candidate)
                        ]
                        or [0]
                    )
                if topic_score >= 30 and topic_score >= int(best_topic_score * 0.75):
                    return True
                return False
        return True

    @classmethod
    def _score_reference_image_candidate(
        cls,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload,
    ) -> int:
        # Scores reference image candidate from asset record, request state, and creative decision for AIOrchestratorService.
        # It reuses _asset_label_text and _preferred_sequence_reference_paths so related checks follow the same rule.
        asset_role = str(asset.get("asset_role") or "").strip().lower()
        trust_level = str(asset.get("trust_level") or "").strip().lower()
        label_text = cls._asset_label_text(asset)
        prompt_text = str(request.prompt or "").casefold()
        score = 0
        preferred_paths = cls._preferred_sequence_reference_paths(request, creative_decision=creative_decision)
        asset_storage_path = str(asset.get("storage_path") or "").strip()
        if trust_level == "trusted":
            score += 12
        elif trust_level in {"usable_with_warning", "usable-with-warning"}:
            score += 6
        if asset_storage_path and asset_storage_path in preferred_paths:
            score += 36
        if asset_role in {"reference_creative", "hero_image", "image", "photo"}:
            score += 10
        if asset_role in {"icon", "micro_design_element", "enhancement_component", "mood_board"}:
            score += 10 if str(request.studio_panel.get("format") or "").strip().lower() in {"carousel", "infographic"} else 6
        if "reference_creative" in label_text:
            score += 6
        score += cls._reference_asset_topic_score(asset, request=request)
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=cls._asset_metadata(asset),
        )
        if template_surface_policy == "style_reference_only" and asset_role in {"reference_creative", "template_preview", "template"}:
            if preferred_paths and asset_storage_path not in preferred_paths:
                score -= 28
            elif not preferred_paths:
                score -= 12
        keyword_map = {
            "travel": ["travel", "flight", "airport", "booking", "trip", "journey"],
            "finance": ["invest", "financial", "money", "growth", "portfolio", "business"],
            "lifestyle": ["person", "professional", "lifestyle", "team", "modern"],
        }
        for keywords in keyword_map.values():
            if any(token in prompt_text for token in keywords) and any(token in label_text for token in keywords):
                score += 8
                break
        format_name = str(request.studio_panel.get("format") or "static").strip().lower()
        if format_name == "carousel":
            if cls._reference_asset_is_carousel_native(asset):
                score += 28
                family_size = cls._carousel_reference_family_size(asset, request=request)
                if family_size >= 2:
                    score += min(18, family_size * 6)
            elif cls._reference_asset_format_family(asset) in {"static", "infographic"}:
                score -= 18
            elif any(token in label_text for token in ("series", "set", "slide")):
                score += 4
        dominant_visual_system = cls._normalized_dominant_visual_system(creative_decision.asset_strategy or {})
        if dominant_visual_system in {"reference_assets", "asset_led"}:
            score += 4
        return score

    @classmethod
    def _select_reference_image_assets(
        cls,
        *,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload,
    ) -> list[dict[str, Any]]:
        # Selects reference image assets from request state and creative decision for AIOrchestratorService.
        # It reuses _preferred_sequence_reference_paths and _sequence_pack_style_reference_surface_paths so related checks follow the same rule.
        format_name = str(request.studio_panel.get("format") or "static").strip().lower()
        preferred_paths = cls._preferred_sequence_reference_paths(request, creative_decision=creative_decision)
        blocked_sequence_surface_paths = cls._sequence_pack_style_reference_surface_paths(
            request,
            creative_decision=creative_decision,
        )
        candidates = [
            dict(asset)
            for asset in (request.asset_catalog or request.reference_assets or [])
            if isinstance(asset, dict)
            and cls._reference_asset_is_visual_source(asset)
            and (
                str(asset.get("storage_path") or "").strip() not in blocked_sequence_surface_paths
                or str(asset.get("storage_path") or "").strip() in preferred_paths
            )
        ]
        if not candidates:
            return []
        candidates = cls._filter_reference_assets_for_requested_format(
            candidates,
            request=request,
        )
        if not candidates:
            return []
        topic_candidate_pool = list(candidates)
        if format_name == "carousel":
            carousel_anchor_roles = {"reference_creative", "template_preview", "template", "hero_image", "image", "photo"}
            anchor_candidates = [
                dict(asset)
                for asset in candidates
                if str(asset.get("asset_role") or "").strip().lower() in carousel_anchor_roles
            ]
            if len(anchor_candidates) >= 2:
                candidates = anchor_candidates
                topic_candidate_pool = list(candidates)
            carousel_candidates = [
                dict(asset)
                for asset in candidates
                if cls._reference_asset_is_carousel_native(asset)
            ]
            if carousel_candidates:
                topic_candidate_pool = list(carousel_candidates)
                preferred_family_assets = cls._selected_carousel_reference_family_assets(
                    carousel_candidates,
                    request=request,
                    creative_decision=creative_decision,
                )
                if preferred_family_assets:
                    candidates = list(preferred_family_assets)
                else:
                    carousel_candidates = cls._dominant_carousel_reference_family_assets(
                        carousel_candidates,
                        request=request,
                    )
                    carousel_candidate_paths = {
                        str(item.get("storage_path") or "").strip() for item in carousel_candidates
                    }
                    candidates = list(carousel_candidates)
                    candidates.extend(
                        dict(asset)
                        for asset in anchor_candidates
                        if str(asset.get("storage_path") or "").strip()
                        and str(asset.get("storage_path") or "").strip() not in carousel_candidate_paths
                        and not cls._reference_asset_is_explicit_single_surface(asset)
                    )
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
        )
        if template_surface_policy == "style_reference_only" and preferred_paths:
            preferred_candidates = [
                dict(asset)
                for asset in candidates
                if str(asset.get("storage_path") or "").strip() in preferred_paths
            ]
            if preferred_candidates:
                candidates = preferred_candidates
        strong_topic_candidates = cls._topic_relevant_reference_assets(
            request,
            creative_decision=creative_decision,
            source_assets=topic_candidate_pool,
            limit=max(cls._reference_image_limit(request.studio_panel), 2),
        )
        if strong_topic_candidates:
            strongest_topic_score = max(
                cls._reference_asset_topic_score(asset, request=request)
                for asset in strong_topic_candidates
            )
            if strongest_topic_score >= 30:
                locked_topic_score = max(
                    [
                        cls._reference_asset_topic_score(asset, request=request)
                        for asset in candidates
                        if isinstance(asset, dict)
                    ]
                    or [0]
                )
                if locked_topic_score <= 0 or locked_topic_score < int(strongest_topic_score * 0.75):
                    candidates = [dict(asset) for asset in strong_topic_candidates]
                else:
                    strong_topic_paths = {
                        str(asset.get("storage_path") or "").strip()
                        for asset in strong_topic_candidates
                        if str(asset.get("storage_path") or "").strip()
                    }
                    narrowed_candidates = [
                        dict(asset)
                        for asset in candidates
                        if str(asset.get("storage_path") or "").strip() in strong_topic_paths
                    ]
                    if narrowed_candidates:
                        candidates = narrowed_candidates
        scored = sorted(
            candidates,
            key=lambda asset: (
                str(asset.get("storage_path") or "").strip() in preferred_paths,
                cls._is_conditioning_safe_reference_image_asset(asset, creative_decision=creative_decision, request=request),
                cls._score_reference_image_candidate(asset, request=request, creative_decision=creative_decision),
                str(asset.get("storage_path") or ""),
            ),
            reverse=True,
        )
        limit = cls._reference_image_limit(request.studio_panel)
        selected: list[dict[str, Any]] = []
        seen_paths: set[str] = set()
        for asset in scored:
            storage_path = str(asset.get("storage_path") or "").strip()
            if not storage_path or storage_path in seen_paths:
                continue
            seen_paths.add(storage_path)
            selected.append(asset)
            if len(selected) >= limit:
                break
        return selected

    @classmethod
    def _conditioning_reference_image_assets(
        cls,
        reference_assets: list[dict[str, Any]],
        *,
        creative_decision: CreativeDecisionPayload,
        request: AIOrchestrationRequest | None = None,
    ) -> list[dict[str, Any]]:
        # Extracts conditioning reference image assets from reference assets, creative decision, and request state for AIOrchestratorService.
        # It calls _is_conditioning_safe_reference_image_asset to turn raw evidence into the structured signal the caller needs.
        return [
            dict(asset)
            for asset in reference_assets
            if isinstance(asset, dict)
            and cls._is_conditioning_safe_reference_image_asset(asset, creative_decision=creative_decision, request=request)
        ]

    @classmethod
    def _layout_analysis_reference_image_assets(
        cls,
        reference_assets: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
    ) -> list[dict[str, Any]]:
        # Extracts layout analysis reference image assets from reference assets and request state for AIOrchestratorService.
        # It calls _requested_reference_format_family and _reference_asset_is_layout_only_static_infographic_surface to turn raw evidence into the struc.
        assets = [dict(asset) for asset in reference_assets if isinstance(asset, dict)]
        if cls._requested_reference_format_family(request) not in {"static", "infographic"}:
            return assets
        layout_assets = [
            dict(asset)
            for asset in assets
            if cls._reference_asset_is_layout_only_static_infographic_surface(asset, request=request)
            or cls._reference_asset_format_family(asset) in {"static", "infographic", "poster", "story"}
            or cls._reference_asset_has_layout_surface_metadata(asset)
        ]
        return layout_assets or assets

    @classmethod
    def _carousel_sequence_uses_multiple_reference_images(
        cls,
        request: AIOrchestrationRequest | None,
    ) -> bool:
        # Centralizes carousel sequence uses multiple reference images from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None:
            return False
        if str(request.studio_panel.get("format") or "").strip().lower() != "carousel":
            return False
        sequence_pack = cls._template_sequence_pack(request, creative_decision=None)
        if not isinstance(sequence_pack, dict):
            return False
        reference_paths: set[str] = set()
        for slide in [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]:
            for key in ("reference_asset_path", "template_asset_path"):
                storage_path = str(slide.get(key) or "").strip()
                if storage_path:
                    reference_paths.add(storage_path)
        return len(reference_paths) >= 2

    @classmethod
    def _filter_logo_bearing_conditioning_reference_images(
        cls,
        reference_assets: list[dict[str, Any]],
        *,
        exact_logo_overlay_required: bool,
    ) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
        # Centralizes filter logo bearing conditioning reference images from reference assets and exact logo overlay required for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not exact_logo_overlay_required:
            return [dict(asset) for asset in reference_assets if isinstance(asset, dict)], []
        allowed: list[dict[str, Any]] = []
        skipped: list[dict[str, Any]] = []
        for asset in reference_assets:
            if not isinstance(asset, dict):
                continue
            if cls._reference_image_has_logo_cue(asset):
                skipped.append(dict(asset))
                continue
            allowed.append(dict(asset))
        return allowed, skipped

    def _filter_available_reference_image_assets(
        self,
        reference_assets: list[dict[str, Any]],
    ) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
        # Extracts filter available reference image assets from reference assets for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        available: list[dict[str, Any]] = []
        missing: list[dict[str, Any]] = []
        for asset in reference_assets:
            if not isinstance(asset, dict):
                continue
            storage_path = str(asset.get("storage_path") or "").strip()
            if not storage_path:
                missing.append(asset)
                continue
            if self.storage.exists(storage_path):
                available.append(asset)
            else:
                missing.append(asset)
        return available, missing

    def _reference_asset_conditioning_storage_path(
        self,
        asset: dict[str, Any],
        *,
        request: AIOrchestrationRequest,
        cache: dict[str, str],
    ) -> str:
        # Extracts reference asset conditioning storage path from asset record, request state, and cache for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        storage_path = str(asset.get("storage_path") or "").strip()
        if not storage_path or not self.storage.exists(storage_path):
            return ""
        mime_type = str(asset.get("mime_type") or "").strip().lower()
        if mime_type.startswith("image/"):
            return storage_path
        if mime_type != "application/pdf":
            return ""
        metadata = asset.get("metadata") if isinstance(asset.get("metadata"), dict) else {}
        requested_page_index = 1
        try:
            requested_page_index = int(metadata.get("conditioning_page_index") or 1)
        except (TypeError, ValueError):
            requested_page_index = 1
        cache_key = f"{storage_path}::page::{requested_page_index}"
        if cache_key in cache:
            return cache[cache_key]
        try:
            import fitz  # type: ignore[import-untyped]

            document = fitz.open(self.storage.absolute_path(storage_path))
            if len(document) <= 0:
                return ""
            page_offset = min(max(requested_page_index - 1, 0), max(len(document) - 1, 0))
            page = document[page_offset]
            pixmap = page.get_pixmap(matrix=fitz.Matrix(1.5, 1.5), alpha=False)
            image = Image.frombytes("RGB", [pixmap.width, pixmap.height], pixmap.samples)
            buffer = BytesIO()
            image.save(buffer, format="PNG")
            filename = storage_path.replace("\\", "/").rsplit("/", 1)[-1].rsplit(".", 1)[0]
            stored = self.storage.save_bytes(
                tenant_id=request.tenant_id,
                brand_space_id=request.brand_space_id,
                category="generated/reference-conditioning",
                filename=f"{filename or 'reference'}-page-{page_offset + 1}.png",
                content=buffer.getvalue(),
            )
            cache[cache_key] = stored.storage_path
            return stored.storage_path
        except Exception as exc:  # pragma: no cover - defensive conditioning fallback
            logger.warning(
                "orchestrator.reference_pdf_conditioning_failed brand_space_id=%s storage_path=%s error=%s",
                request.brand_space_id,
                storage_path,
                exc,
            )
            return ""

    def _conditioning_reference_image_paths(
        self,
        reference_assets: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest,
        cache: dict[str, str],
    ) -> list[str]:
        # Centralizes conditioning reference image paths from reference assets, request state, and cache for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        paths: list[str] = []
        seen: set[str] = set()
        for asset in reference_assets:
            if not isinstance(asset, dict):
                continue
            storage_path = self._reference_asset_conditioning_storage_path(
                asset,
                request=request,
                cache=cache,
            )
            if not storage_path or storage_path in seen or not self.storage.exists(storage_path):
                continue
            seen.add(storage_path)
            paths.append(self.storage.absolute_path(storage_path))
        return paths

    @staticmethod
    def _sample_page_blueprint_from_image_path(
        image_path: str,
        *,
        slide_index: int | None = None,
        slide_count: int | None = None,
    ) -> dict[str, Any]:
        # Resolves sample page blueprint image path from image path, slide index, and slide count for AIOrchestratorService.
        # This hides source-specific naming and path quirks from the main flow.
        path = str(image_path or "").strip()
        if not path:
            return {}
        try:
            image = Image.open(path).convert("RGB")
        except Exception:  # noqa: BLE001
            return {}
        width, height = image.size
        if width <= 0 or height <= 0:
            return {}

        max_width = 260
        scale = min(1.0, max_width / max(width, 1))
        small = image.resize((max(1, int(width * scale)), max(1, int(height * scale))))
        sw, sh = small.size
        pixels = small.load()
        corner_samples: list[tuple[int, int, int]] = []
        corner_points = [
            (0, 0),
            (sw - 1, 0),
            (0, sh - 1),
            (sw - 1, sh - 1),
            (sw // 2, 0),
            (0, sh // 2),
            (sw - 1, sh // 2),
        ]
        for x, y in corner_points:
            r, g, b = pixels[max(0, min(sw - 1, x)), max(0, min(sh - 1, y))]
            corner_samples.append((r, g, b))
        background = tuple(
            int(sorted(sample[channel] for sample in corner_samples)[len(corner_samples) // 2])
            for channel in range(3)
        )

        def distance(pixel: tuple[int, int, int]) -> int:
            # Centralizes distance from pixel for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            return sum(abs(int(pixel[index]) - int(background[index])) for index in range(3))

        mask = [[False for _ in range(sw)] for _ in range(sh)]
        for y in range(sh):
            for x in range(sw):
                pixel = pixels[x, y]
                r, g, b = pixel
                saturation = max(pixel) - min(pixel)
                if distance(pixel) > 44 or (saturation > 30 and distance(pixel) > 22):
                    mask[y][x] = True

        row_density = [sum(1 for value in row if value) / max(sw, 1) for row in mask]
        bands: list[dict[str, Any]] = []
        start: int | None = None
        threshold = 0.035
        for y, density in enumerate(row_density + [0.0]):
            if density >= threshold and start is None:
                start = y
            elif (density < threshold or y == sh) and start is not None:
                end = y - 1
                if end - start + 1 >= max(2, int(sh * 0.006)):
                    bands.append(
                        {
                            "y": round(start / sh, 3),
                            "h": round((end - start + 1) / sh, 3),
                            "coverage": round(max(row_density[start : end + 1] or [0.0]), 3),
                        }
                    )
                start = None

        visited = [[False for _ in range(sw)] for _ in range(sh)]
        components: list[dict[str, Any]] = []
        min_area = max(10, int(sw * sh * 0.00035))
        for sy in range(sh):
            for sx in range(sw):
                if visited[sy][sx] or not mask[sy][sx]:
                    continue
                stack = [(sx, sy)]
                visited[sy][sx] = True
                min_x = max_x = sx
                min_y = max_y = sy
                area = 0
                while stack:
                    x, y = stack.pop()
                    area += 1
                    min_x = min(min_x, x)
                    max_x = max(max_x, x)
                    min_y = min(min_y, y)
                    max_y = max(max_y, y)
                    for nx, ny in ((x + 1, y), (x - 1, y), (x, y + 1), (x, y - 1)):
                        if nx < 0 or ny < 0 or nx >= sw or ny >= sh:
                            continue
                        if visited[ny][nx] or not mask[ny][nx]:
                            continue
                        visited[ny][nx] = True
                        stack.append((nx, ny))
                if area < min_area:
                    continue
                box_w = max_x - min_x + 1
                box_h = max_y - min_y + 1
                if box_w <= 1 or box_h <= 1:
                    continue
                components.append(
                    {
                        "x": round(min_x / sw, 3),
                        "y": round(min_y / sh, 3),
                        "w": round(box_w / sw, 3),
                        "h": round(box_h / sh, 3),
                        "area": round(area / max(sw * sh, 1), 4),
                        "aspect": round(box_w / max(box_h, 1), 2),
                    }
                )
        components = sorted(components, key=lambda item: float(item.get("area") or 0.0), reverse=True)[:16]

        text_bands = [band for band in bands if float(band.get("y") or 0.0) < 0.36]
        footer_bands = [band for band in bands if float(band.get("y") or 0.0) > 0.88]
        row_like_bands = [
            band
            for band in bands
            if 0.18 <= float(band.get("y") or 0.0) <= 0.88
            and 0.018 <= float(band.get("h") or 0.0) <= 0.095
            and float(band.get("coverage") or 0.0) >= 0.18
        ]
        large_visuals = [
            comp
            for comp in components
            if float(comp.get("area") or 0.0) >= 0.035
            and float(comp.get("y") or 0.0) >= 0.18
            and float(comp.get("h") or 0.0) >= 0.12
        ]
        lower_points = [
            (x, y)
            for y in range(int(sh * 0.2), int(sh * 0.9))
            for x in range(sw)
            if mask[y][x]
        ]
        aggregate_visual: dict[str, Any] | None = None
        if lower_points:
            min_x = min(point[0] for point in lower_points)
            max_x = max(point[0] for point in lower_points)
            min_y = min(point[1] for point in lower_points)
            max_y = max(point[1] for point in lower_points)
            area_ratio = len(lower_points) / max(sw * sh, 1)
            box_w = max_x - min_x + 1
            box_h = max_y - min_y + 1
            if area_ratio >= 0.055 and box_h / max(sh, 1) >= 0.18:
                aggregate_visual = {
                    "x": round(min_x / sw, 3),
                    "y": round(min_y / sh, 3),
                    "w": round(box_w / sw, 3),
                    "h": round(box_h / sh, 3),
                    "area": round(area_ratio, 4),
                    "aspect": round(box_w / max(box_h, 1), 2),
                    "source": "aggregate_non_background_mass",
                }
                large_visuals.insert(0, aggregate_visual)
        card_like = [
            comp
            for comp in components
            if 0.18 <= float(comp.get("y") or 0.0) <= 0.86
            and 0.12 <= float(comp.get("w") or 0.0) <= 0.58
            and 0.07 <= float(comp.get("h") or 0.0) <= 0.28
            and float(comp.get("aspect") or 0.0) >= 1.1
        ]
        icon_like = [
            comp
            for comp in components
            if float(comp.get("area") or 0.0) < 0.025
            and 0.18 <= float(comp.get("y") or 0.0) <= 0.88
        ]
        bottom_cta_like = [
            comp
            for comp in components
            if float(comp.get("y") or 0.0) > 0.72
            and float(comp.get("w") or 0.0) > 0.45
            and 0.045 <= float(comp.get("h") or 0.0) <= 0.16
        ]
        center_column_coverage = sum(
            1
            for y in range(int(sh * 0.2), int(sh * 0.86))
            if any(mask[y][x] for x in range(int(sw * 0.38), int(sw * 0.62)))
        ) / max(int(sh * 0.66), 1)

        if aggregate_visual and float(aggregate_visual.get("area") or 0.0) >= 0.14 and len(text_bands) <= 3:
            layout_category = "cover_or_hero_visual"
        elif len(row_like_bands) >= 4 and len(icon_like) >= 8:
            layout_category = "numbered_or_icon_row_list"
        elif len(card_like) >= 3 or (len(row_like_bands) >= 3 and len(icon_like) <= 6):
            layout_category = "card_callout_grid"
        elif center_column_coverage > 0.38 and len(large_visuals) <= 4:
            layout_category = "timeline_or_winding_process_path"
        elif bottom_cta_like:
            layout_category = "closing_cta_or_product_surface"
        elif large_visuals and float(large_visuals[0].get("area") or 0.0) >= 0.12:
            layout_category = "cover_or_hero_visual"
        else:
            layout_category = "editorial_explainer"

        density_score = len(text_bands) + len(row_like_bands) + len(card_like)
        density = "dense" if density_score >= 8 else ("balanced" if density_score >= 4 else "airy")
        visual_permissions = {
            "chart_or_graph_allowed": False,
            "table_allowed": layout_category == "numbered_or_icon_row_list",
            "dashboard_allowed": layout_category == "closing_cta_or_product_surface",
            "metric_tiles_allowed": len(card_like) >= 2 or len(row_like_bands) >= 2,
            "cta_allowed": layout_category == "closing_cta_or_product_surface",
        }
        zones: list[dict[str, Any]] = []
        if text_bands:
            first = text_bands[0]
            zones.append({"role": "headline_cluster", "x": 0.05, "y": first["y"], "w": 0.9, "h": min(0.24, sum(float(b.get("h") or 0.0) for b in text_bands[:3]) + 0.04)})
        for index, band in enumerate(row_like_bands[:8], start=1):
            zones.append({"role": f"row_module_{index}", "x": 0.04, "y": band["y"], "w": 0.92, "h": band["h"]})
        for index, comp in enumerate(card_like[:4], start=1):
            zones.append({"role": f"card_module_{index}", "x": comp["x"], "y": comp["y"], "w": comp["w"], "h": comp["h"]})
        for index, comp in enumerate(large_visuals[:3], start=1):
            zones.append({"role": f"hero_visual_{index}", "x": comp["x"], "y": comp["y"], "w": comp["w"], "h": comp["h"]})
        if footer_bands:
            zones.append({"role": "empty_bottom_safe_strip", "x": 0.02, "y": footer_bands[0]["y"], "w": 0.96, "h": min(0.06, footer_bands[0]["h"])})

        return {
            "canvas": {"width": width, "height": height},
            "slide_index": slide_index,
            "slide_count": slide_count,
            "layout_category": layout_category,
            "density": density,
            "background_rgb": list(background),
            "zones": zones[:12],
            "module_counts": {
                "horizontal_band_count": len(row_like_bands),
                "card_like_count": len(card_like),
                "large_visual_count": len(large_visuals),
                "small_icon_like_count": len(icon_like),
                "top_text_band_count": len(text_bands),
                "footer_band_count": len(footer_bands),
            },
            "visual_permissions": visual_permissions,
            "prominent_regions": components[:8],
            "must_match": [
                f"layout_category:{layout_category}",
                f"density:{density}",
                f"horizontal_band_count:{len(row_like_bands)}",
                f"card_like_count:{len(card_like)}",
                f"large_visual_count:{len(large_visuals)}",
                f"top_text_band_count:{len(text_bands)}",
            ],
        }

    @classmethod
    def _annotate_slide_with_sample_page_blueprint(
        cls,
        slide: dict[str, Any],
        *,
        reference_image_paths: list[str],
    ) -> dict[str, Any]:
        # Centralizes annotate slide sample page blueprint from slide and reference image paths for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not reference_image_paths:
            return slide
        annotated = dict(slide)
        metadata = dict(annotated.get("metadata") or {})
        if isinstance(metadata.get("sample_page_blueprint"), dict):
            return annotated
        blueprint = cls._sample_page_blueprint_from_image_path(
            reference_image_paths[0],
            slide_index=cls._int_or_none(annotated.get("slide_index")),
            slide_count=cls._int_or_none(annotated.get("slide_count")),
        )
        if not blueprint:
            return annotated
        metadata["sample_page_blueprint"] = blueprint
        metadata["sample_page_blueprint_source"] = "rendered_conditioning_reference"
        annotated["metadata"] = metadata
        return annotated

    @staticmethod
    def _count_similarity(expected: int, actual: int, *, tolerance: int = 1) -> float:
        # Counts count similarity from expected, actual, and tolerance for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        expected = max(int(expected or 0), 0)
        actual = max(int(actual or 0), 0)
        if expected == actual:
            return 1.0
        if expected == 0:
            return 0.35 if actual <= tolerance else 0.0
        drift = max(abs(expected - actual) - tolerance, 0)
        return max(0.0, 1.0 - (drift / max(expected, actual, 1)))

    @classmethod
    def _layout_category_compatible(cls, expected: str, actual: str) -> bool:
        # Checks layout category compatible from expected and actual for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        expected = cls._normalize_metadata_text(expected, limit=80).casefold()
        actual = cls._normalize_metadata_text(actual, limit=80).casefold()
        if not expected or not actual:
            return True
        if expected == actual:
            return True
        compatible_groups = [
            {"card_callout_grid", "numbered_or_icon_row_list"},
            {"cover_or_hero_visual", "editorial_explainer"},
        ]
        return any(expected in group and actual in group for group in compatible_groups)

    @classmethod
    def _blueprint_ocr_blocks(cls, blueprint: dict[str, Any]) -> list[dict[str, Any]]:
        # Centralizes blueprint OCR blocks from blueprint for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        ocr = blueprint.get("ocr_structure") if isinstance(blueprint.get("ocr_structure"), dict) else {}
        blocks = ocr.get("block_summary")
        return [block for block in blocks if isinstance(block, dict)] if isinstance(blocks, list) else []

    @staticmethod
    def _similarity_float(value: Any, *, default: float = 0.0) -> float:
        # Centralizes similarity float from input value and default for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        try:
            return float(value)
        except (TypeError, ValueError):
            return default

    @classmethod
    def _text_looks_like_non_cta_label(cls, text: str) -> bool:
        # Extracts text non cta label from text for AIOrchestratorService.
        # It calls _text_has_cta_action and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        normalized = cls._normalize_metadata_text(text, limit=140).casefold()
        if not normalized:
            return True
        if cls._text_has_cta_action(normalized):
            return False
        date_or_doc_terms = {
            "agreement",
            "as of",
            "certificate",
            "dated",
            "document",
            "effective",
            "fta",
            "free trade",
            "memorandum",
            "report",
            "signed",
        }
        has_year_or_date = bool(re.search(r"\b(?:19|20)\d{2}\b|\b\d{1,2}\s+[a-z]{3,9}\s+(?:19|20)\d{2}\b", normalized))
        if has_year_or_date and any(term in normalized for term in date_or_doc_terms):
            return True
        words = re.findall(r"[a-z0-9]+", normalized)
        if len(words) <= 6 and any(term in normalized for term in date_or_doc_terms):
            return True
        return False

    @classmethod
    def _text_has_cta_action(cls, text: str) -> bool:
        # Extracts text cta action from text for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        normalized = cls._normalize_metadata_text(text, limit=140).casefold()
        action_terms = {
            "apply",
            "book",
            "buy",
            "call",
            "check",
            "click",
            "compare",
            "contact",
            "discover",
            "download",
            "explore",
            "get started",
            "invest",
            "join",
            "learn",
            "open",
            "read",
            "register",
            "schedule",
            "sign up",
            "start",
            "subscribe",
            "swipe",
            "try",
            "unlock",
            "visit",
        }
        for term in action_terms:
            pattern = r"\b" + r"\s+".join(re.escape(part) for part in term.split()) + r"\b"
            if re.search(pattern, normalized):
                return True
        return False

    @classmethod
    def _blueprint_effective_cta_count(cls, blueprint: dict[str, Any]) -> int:
        # Counts blueprint effective cta count from blueprint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        blocks = cls._blueprint_ocr_blocks(blueprint)
        raw_layout_category = cls._normalize_metadata_text(blueprint.get("layout_category"), limit=80).casefold()
        product_or_closing_layouts = {
            "closing_cta_or_product_surface",
            "dashboard_or_product_surface",
            "data_dashboard",
            "product_surface",
        }
        layout_category = (
            raw_layout_category
            if raw_layout_category in product_or_closing_layouts
            else (cls._sample_blueprint_layout_mode(blueprint) or raw_layout_category).casefold()
        )
        cta_blocks = []
        for block in blocks:
            role = cls._normalize_metadata_text(block.get("role"), limit=48).casefold()
            text = cls._normalize_metadata_text(block.get("text_excerpt"), limit=160)
            y = cls._similarity_float(block.get("y"), default=0.0)
            w = cls._similarity_float(block.get("w"), default=0.0)
            role_marks_cta = "cta" in role or "button" in role or "call_to_action" in role
            bottom_marketing_region = y >= 0.70 and w >= 0.45 and role not in {"footer", "footer_legal", "legal", "logo"}
            action_like_text = cls._text_has_cta_action(text)
            if layout_category == "card_callout_grid":
                cta_like = role_marks_cta and action_like_text
            else:
                button_like_role = role_marks_cta and (bottom_marketing_region or action_like_text)
                cta_like = button_like_role or bottom_marketing_region
            if cta_like and not cls._text_looks_like_non_cta_label(text):
                cta_blocks.append(block)
        if blocks:
            return len(cta_blocks)
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        fallback_cta_count = max(int(counts.get("cta_count") or 0), 0)
        card_like_count = max(int(counts.get("card_like_count") or 0), 0)
        if layout_category == "card_callout_grid" and card_like_count >= max(fallback_cta_count, 1):
            return 0
        return fallback_cta_count

    @classmethod
    def _sample_output_similarity_report(
        cls,
        *,
        sample_blueprint: dict[str, Any],
        output_blueprint: dict[str, Any],
        exact_logo_overlay_deferred: bool = False,
        legal_footer_overlay_deferred: bool = False,
    ) -> dict[str, Any]:
        # Centralizes sample output similarity report from sample blueprint, output blueprint, and exact logo overlay deferred for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not sample_blueprint or not output_blueprint:
            return {"score": 0.0, "issues": ["missing_blueprint"], "retry_recommended": False}
        sample_counts = sample_blueprint.get("module_counts") if isinstance(sample_blueprint.get("module_counts"), dict) else {}
        output_counts = output_blueprint.get("module_counts") if isinstance(output_blueprint.get("module_counts"), dict) else {}
        sample_permissions = sample_blueprint.get("visual_permissions") if isinstance(sample_blueprint.get("visual_permissions"), dict) else {}
        output_permissions = output_blueprint.get("visual_permissions") if isinstance(output_blueprint.get("visual_permissions"), dict) else {}
        sample_category = cls._normalize_metadata_text(sample_blueprint.get("layout_category"), limit=80)
        output_category = cls._normalize_metadata_text(output_blueprint.get("layout_category"), limit=80)

        def effective_layout_category(blueprint: dict[str, Any], raw_category: str) -> str:
            # Centralizes effective layout category from blueprint and raw category for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            raw = cls._normalize_metadata_text(raw_category, limit=80).casefold()
            if raw in {"closing_cta_or_product_surface", "dashboard_or_product_surface", "data_dashboard", "product_surface"}:
                return raw_category
            return cls._sample_blueprint_layout_mode(blueprint) or raw_category

        effective_sample_category = effective_layout_category(sample_blueprint, sample_category)
        effective_output_category = effective_layout_category(output_blueprint, output_category)
        issues: list[str] = []
        corrections: list[str] = []
        hard_retry_issues: set[str] = set()
        score_parts: list[float] = []

        if cls._layout_category_compatible(effective_sample_category, effective_output_category):
            score_parts.append(1.0)
        else:
            score_parts.append(0.15)
            issues.append("layout_category_drift")
            hard_retry_issues.add("layout_category_drift")
            corrections.append(
                f"Sample page layout is {effective_sample_category}; output reads as {effective_output_category}. Rebuild the slide using the sample page's composition category first."
            )

        count_specs = [
            ("horizontal_band_count", "row/list bands", 1),
            ("card_like_count", "card/callout modules", 1),
            ("large_visual_count", "large hero visual regions", 1),
            ("small_icon_like_count", "small icon-like visual marks", 3),
            ("top_text_band_count", "top text bands", 1),
            ("footer_band_count", "empty bottom safe strips", 1),
        ]
        count_scores: list[float] = []
        for key, label, tolerance in count_specs:
            expected = int(sample_counts.get(key) or 0)
            actual = int(output_counts.get(key) or 0)
            if (
                key == "card_like_count"
                and effective_sample_category == "card_callout_grid"
                and expected == 0
                and actual > 0
                and int(sample_counts.get("text_block_count") or 0) >= 3
            ):
                expected = max(1, min(int(sample_counts.get("text_block_count") or 0) - 1, 6))
            if key == "footer_band_count" and legal_footer_overlay_deferred and actual < expected:
                count_scores.append(1.0)
                continue
            if (
                key == "horizontal_band_count"
                and effective_sample_category == "card_callout_grid"
                and cls._layout_category_compatible(effective_sample_category, effective_output_category)
                and int(sample_counts.get("card_like_count") or 0) > 0
                and cls._count_similarity(
                    int(sample_counts.get("card_like_count") or 0),
                    int(output_counts.get("card_like_count") or 0),
                    tolerance=1,
                )
                >= 0.58
            ):
                count_scores.append(1.0)
                continue
            score = cls._count_similarity(expected, actual, tolerance=tolerance)
            count_scores.append(score)
            if score < 0.58:
                issues.append(f"{key}_drift")
                if key in {"horizontal_band_count", "card_like_count", "large_visual_count", "top_text_band_count"}:
                    hard_retry_issues.add(f"{key}_drift")
                corrections.append(f"Sample has about {expected} {label}; output has about {actual}. Match the sample count and rhythm more closely.")
        score_parts.append(sum(count_scores) / max(len(count_scores), 1))

        output_logo_count = max(int(output_counts.get("logo_count") or 0), 0)
        if exact_logo_overlay_deferred and output_logo_count > 0:
            issues.append("ai_generated_logo_or_wordmark_detected")
            hard_retry_issues.add("ai_generated_logo_or_wordmark_detected")
            corrections.append(
                "The exact asset is deferred to post-generation overlay, so the AI base image must contain zero logos, wordmarks, brand signatures, ghost marks, or graphic shapes. Leave only an empty safe background area."
            )
            score_parts.append(0.15)

        sample_density = cls._normalize_metadata_text(sample_blueprint.get("density"), limit=32)
        output_density = cls._normalize_metadata_text(output_blueprint.get("density"), limit=32)
        if sample_density and output_density and sample_density != output_density:
            score_parts.append(0.55)
            issues.append("content_density_drift")
            corrections.append(f"Sample density is {sample_density}; output density is {output_density}. Adjust copy and visual density to the sample page.")
        else:
            score_parts.append(1.0)

        if not bool(sample_permissions.get("dashboard_allowed")) and bool(output_permissions.get("dashboard_allowed")):
            issues.append("dashboard_or_product_surface_invented")
            hard_retry_issues.add("dashboard_or_product_surface_invented")
            corrections.append("Output introduced a dashboard/product-surface grammar, but the sample page does not permit that structure.")
            score_parts.append(0.25)
        sample_chart_count = int(sample_counts.get("chart_count") or 0)
        output_chart_count = int(output_counts.get("chart_count") or 0)
        if not bool(sample_permissions.get("chart_or_graph_allowed")) and output_chart_count > max(sample_chart_count, 0):
            issues.append("chart_or_graph_invented")
            hard_retry_issues.add("chart_or_graph_invented")
            corrections.append("Output introduced chart/graph modules, but the sample page does not permit chart or graph grammar.")
            score_parts.append(0.25)
        sample_table_count = int(sample_counts.get("table_count") or 0)
        output_table_count = int(output_counts.get("table_count") or 0)
        if not bool(sample_permissions.get("table_allowed")) and output_table_count > max(sample_table_count, 0):
            issues.append("table_invented")
            hard_retry_issues.add("table_invented")
            corrections.append("Output introduced table modules, but the sample page does not permit table grammar.")
            score_parts.append(0.3)
        sample_metric_tile_count = max(int(sample_counts.get("card_like_count") or 0), 0)
        output_metric_tile_count = max(int(output_counts.get("card_like_count") or 0), 0)
        if not bool(sample_permissions.get("metric_tiles_allowed")) and bool(output_permissions.get("metric_tiles_allowed")) and output_metric_tile_count > max(sample_metric_tile_count + 1, 1):
            issues.append("metric_tiles_invented")
            hard_retry_issues.add("metric_tiles_invented")
            corrections.append("Output introduced metric/card tile grammar, but the sample page does not permit metric-tile structure.")
            score_parts.append(0.35)
        sample_cta_count = cls._blueprint_effective_cta_count(sample_blueprint)
        output_cta_count = cls._blueprint_effective_cta_count(output_blueprint)
        output_cta_allowed = bool(output_permissions.get("cta_allowed")) and output_cta_count > 0
        if not bool(sample_permissions.get("cta_allowed")) and output_cta_allowed:
            issues.append("cta_grammar_invented")
            corrections.append("Output introduced a CTA/button-like grammar, but the sample page does not use a CTA region.")
            if output_cta_count > max(sample_cta_count, 0):
                hard_retry_issues.add("cta_grammar_invented")
            score_parts.append(0.35)
        if (
            effective_sample_category != "numbered_or_icon_row_list"
            and not bool(sample_permissions.get("table_allowed"))
            and effective_output_category == "numbered_or_icon_row_list"
        ):
            issues.append("list_or_table_grammar_invented")
            hard_retry_issues.add("list_or_table_grammar_invented")
            corrections.append("Output introduced row/list/table grammar, but the selected sample page does not use that structure.")
            score_parts.append(0.45)

        sample_ocr = sample_blueprint.get("ocr_structure") if isinstance(sample_blueprint.get("ocr_structure"), dict) else {}
        output_ocr = output_blueprint.get("ocr_structure") if isinstance(output_blueprint.get("ocr_structure"), dict) else {}
        sample_text_blocks = sample_ocr.get("readable_text_blocks")
        output_text_blocks = output_ocr.get("readable_text_blocks")
        module_text_layouts = {"numbered_or_icon_row_list", "card_callout_grid", "vertical_icon_explainer"}
        sample_module_text_blocks = int(sample_counts.get("text_block_count") or 0)
        output_module_text_blocks = int(output_counts.get("text_block_count") or 0)
        text_block_count_source = "ocr"
        if (
            effective_sample_category in module_text_layouts
            and cls._layout_category_compatible(effective_sample_category, effective_output_category)
            and sample_module_text_blocks > 0
            and output_module_text_blocks > 0
        ):
            sample_text_blocks = sample_module_text_blocks
            output_text_blocks = output_module_text_blocks
            text_block_count_source = "module_counts"
        if isinstance(sample_text_blocks, (int, float)) and isinstance(output_text_blocks, (int, float)):
            text_score = cls._count_similarity(int(sample_text_blocks), int(output_text_blocks), tolerance=1)
            score_parts.append(text_score)
            if text_score < 0.62:
                issues.append("ocr_text_block_count_drift")
                hard_retry_issues.add("ocr_text_block_count_drift")
                if text_block_count_source == "module_counts":
                    corrections.append(f"Sample has about {int(sample_text_blocks)} readable module text blocks; output has about {int(output_text_blocks)}. Match the sample module text count and placement.")
                else:
                    corrections.append(f"Vision/OCR sees about {int(sample_text_blocks)} readable text blocks in the sample and {int(output_text_blocks)} in the output. Match the sample text-block count and placement.")
        output_collision = cls._normalize_metadata_text(output_ocr.get("text_overlap_or_collision_risk"), limit=24).casefold()
        if output_collision in {"medium", "high"}:
            issues.append("ocr_text_collision_risk")
            corrections.append("Vision/OCR detected medium or high text overlap/collision risk in the output. Increase separation around headline, logo, footer, and body zones.")
            if output_collision == "high":
                hard_retry_issues.add("ocr_text_collision_risk")
            score_parts.append(0.35 if output_collision == "high" else 0.55)

        sample_quality = sample_blueprint.get("premium_quality") if isinstance(sample_blueprint.get("premium_quality"), dict) else {}
        output_quality = output_blueprint.get("premium_quality") if isinstance(output_blueprint.get("premium_quality"), dict) else {}
        for key, label in (
            ("overall_score", "overall design quality"),
            ("typography_score", "typography"),
            ("spacing_score", "spacing"),
            ("craft_score", "visual craft"),
            ("brand_finish_score", "brand finish"),
        ):
            expected_quality = sample_quality.get(key)
            actual_quality = output_quality.get(key)
            if not isinstance(expected_quality, (int, float)) or not isinstance(actual_quality, (int, float)):
                continue
            if key == "brand_finish_score" and exact_logo_overlay_deferred:
                continue
            if float(expected_quality) >= 0.75 and float(actual_quality) < max(0.68, float(expected_quality) - 0.18):
                issues.append(f"premium_{key}_drift")
                corrections.append(f"Vision quality review rates sample {label} at {float(expected_quality):.2f} and output at {float(actual_quality):.2f}. Raise polish, typography, spacing, and finish to match the sample.")
                score_parts.append(max(0.25, float(actual_quality) / max(float(expected_quality), 0.01)))

        sample_craft = sample_blueprint.get("visual_craft_dna") if isinstance(sample_blueprint.get("visual_craft_dna"), dict) else {}
        output_craft = output_blueprint.get("visual_craft_dna") if isinstance(output_blueprint.get("visual_craft_dna"), dict) else {}
        for key, label in (("depth_style", "depth style"), ("rendering_style", "rendering style"), ("lighting", "lighting")):
            expected_craft = cls._normalize_metadata_text(sample_craft.get(key), limit=48).casefold()
            actual_craft = cls._normalize_metadata_text(output_craft.get(key), limit=48).casefold()
            if expected_craft and actual_craft and expected_craft != actual_craft:
                issues.append(f"craft_{key}_drift")
                corrections.append(f"Sample {label} is {expected_craft}; output {label} is {actual_craft}. Match the sample's visual craft language.")
                score_parts.append(0.72)

        sample_footer = int(sample_counts.get("footer_band_count") or 0)
        output_footer = int(output_counts.get("footer_band_count") or 0)
        if output_footer > sample_footer + 2:
            issues.append("footer_collision_risk")
            hard_retry_issues.add("footer_collision_risk")
            corrections.append("Output has more bottom/footer visual bands than the sample, which can create legal/footer collision risk.")
            score_parts.append(0.55)

        score = round(max(0.0, min(1.0, sum(score_parts) / max(len(score_parts), 1))), 2)
        high_score_tolerable_hard_issues = {
            "footer_band_count_drift",
            "large_visual_count_drift",
            "top_text_band_count_drift",
        }
        blocking_hard_retry_issues = set(hard_retry_issues)
        if score >= 0.88 and blocking_hard_retry_issues <= high_score_tolerable_hard_issues:
            blocking_hard_retry_issues = set()
        retry_recommended = score < cls.SAMPLE_SIMILARITY_ACCEPT_SCORE or bool(blocking_hard_retry_issues)
        return {
            "score": score,
            "issues": issues,
            "hard_retry_issues": sorted(hard_retry_issues),
            "blocking_hard_retry_issues": sorted(blocking_hard_retry_issues),
            "corrections": corrections[:8],
            "retry_recommended": retry_recommended,
            "sample_layout_category": sample_category,
            "output_layout_category": output_category,
            "effective_sample_layout_category": effective_sample_category,
            "effective_output_layout_category": effective_output_category,
            "sample_density": sample_density,
            "output_density": output_density,
            "sample_module_counts": sample_counts,
            "output_module_counts": output_counts,
            "text_block_count_source": text_block_count_source,
            "sample_visual_permissions": sample_permissions,
            "output_visual_permissions": output_permissions,
            "effective_sample_cta_count": sample_cta_count,
            "effective_output_cta_count": output_cta_count,
            "post_generation_overlays_expected": {
                "exact_logo": bool(exact_logo_overlay_deferred),
                "legal_footer": bool(legal_footer_overlay_deferred),
            },
        }

    @classmethod
    def _sample_output_similarity_from_paths(
        cls,
        *,
        sample_image_path: str,
        output_image_path: str,
        sample_blueprint: dict[str, Any] | None = None,
        slide_index: int | None = None,
        slide_count: int | None = None,
        exact_logo_overlay_deferred: bool = False,
        legal_footer_overlay_deferred: bool = False,
    ) -> dict[str, Any]:
        # Centralizes sample output similarity paths from sample image path, output image path, and sample blueprint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        sample = sample_blueprint if isinstance(sample_blueprint, dict) and sample_blueprint else cls._sample_page_blueprint_from_image_path(
            sample_image_path,
            slide_index=slide_index,
            slide_count=slide_count,
        )
        output = cls._sample_page_blueprint_from_image_path(
            output_image_path,
            slide_index=slide_index,
            slide_count=slide_count,
        )
        report = cls._sample_output_similarity_report(
            sample_blueprint=sample,
            output_blueprint=output,
            exact_logo_overlay_deferred=exact_logo_overlay_deferred,
            legal_footer_overlay_deferred=legal_footer_overlay_deferred,
        )
        report["sample_blueprint"] = sample
        report["output_blueprint"] = output
        return report

    @classmethod
    def _append_sample_similarity_repair_prompt(
        cls,
        prompt: str,
        *,
        similarity_report: dict[str, Any],
    ) -> str:
        # Builds append sample similarity prompt from prompt text and similarity report for AIOrchestratorService.
        # It calls _strip_leading_sample_similarity_repair_prompt and _scrub_image_prompt_brand_mark_triggers while assembling the payload or prompt text.
        prompt = cls._strip_leading_sample_similarity_repair_prompt(prompt)
        corrections = [
            cls._normalize_metadata_text(item, limit=220)
            for item in (similarity_report.get("corrections") or [])
            if cls._normalize_metadata_text(item, limit=220)
        ]
        issues = [
            cls._normalize_metadata_text(item, limit=80)
            for item in (similarity_report.get("issues") or [])
            if cls._normalize_metadata_text(item, limit=80)
        ]
        if not corrections and not issues:
            return prompt
        hard_issues = [
            cls._normalize_metadata_text(item, limit=80)
            for item in (similarity_report.get("hard_retry_issues") or [])
            if cls._normalize_metadata_text(item, limit=80)
        ]
        try:
            score = float(similarity_report.get("score") or 0.0)
        except (TypeError, ValueError):
            score = 0.0
        instruction = (
            "Keep the current composition and make only targeted corrections to the listed sample-fidelity drift. Preserve the existing slide geometry unless a listed hard issue requires a local structural fix."
            if score >= cls.SAMPLE_SIMILARITY_ACCEPT_SCORE and not hard_issues
            else "Regenerate this slide to match the selected sample page's layout geometry, module count, text density, visual-slot rhythm, footer safety, and closing/CTA grammar. Replace only the topic content and literal artwork."
        )
        repair_contract = json.dumps(
            {
                "post_generation_sample_similarity_repair": {
                    "score": similarity_report.get("score"),
                    "issues": issues[:8],
                    "hard_retry_issues": hard_issues[:8],
                    "required_corrections": corrections[:8],
                    "instruction": instruction,
                }
            },
            separators=(",", ":"),
            ensure_ascii=True,
        )
        repair_contract = cls._scrub_image_prompt_brand_mark_triggers(repair_contract)
        remaining = max(cls.CAROUSEL_IMAGE_PROMPT_MAX_LENGTH - len(repair_contract) - 1, 1200)
        return cls._scrub_image_prompt_brand_mark_triggers(cls._trim_prompt(
            f"{repair_contract} {cls._trim_prompt(prompt, remaining)}",
            cls.CAROUSEL_IMAGE_PROMPT_MAX_LENGTH,
        ))

    @classmethod
    def _strip_leading_sample_similarity_repair_prompt(cls, prompt: str) -> str:
        # Strips strip leading sample similarity prompt from prompt text for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = str(prompt or "")
        stripped = text.lstrip()
        leading_whitespace = len(text) - len(stripped)
        if not stripped.startswith("{"):
            return text
        try:
            parsed, end_index = json.JSONDecoder().raw_decode(stripped)
        except json.JSONDecodeError:
            return text
        if not isinstance(parsed, dict) or "post_generation_sample_similarity_repair" not in parsed:
            return text
        return text[leading_whitespace + end_index :].lstrip()

    @classmethod
    def _sample_blueprint_layout_mode(cls, blueprint: dict[str, Any]) -> str:
        # Centralizes sample blueprint layout mode from blueprint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(blueprint, dict) or not blueprint:
            return ""
        layout_category = cls._normalize_metadata_text(blueprint.get("layout_category"), limit=80).casefold()
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        must_match_text = " ".join(
            cls._normalize_metadata_text(item, limit=120).casefold()
            for item in (blueprint.get("must_match") if isinstance(blueprint.get("must_match"), list) else [])
            if cls._normalize_metadata_text(item, limit=120)
        )
        horizontal_count = int(counts.get("horizontal_band_count") or 0)
        icon_count = int(counts.get("small_icon_like_count") or 0)
        card_count = int(counts.get("card_like_count") or 0)
        large_visual_count = int(counts.get("large_visual_count") or 0)
        zones = blueprint.get("zones") if isinstance(blueprint.get("zones"), list) else []
        image_body_zone_count = 0
        for zone in zones:
            if not isinstance(zone, dict):
                continue
            zone_text = " ".join(
                cls._normalize_metadata_text(zone.get(key), limit=90).casefold()
                for key in ("role", "description")
                if cls._normalize_metadata_text(zone.get(key), limit=90)
            )
            if ("image+body" in zone_text or "icon" in zone_text) and (
                "text" in zone_text or "body" in zone_text or "section" in zone_text
            ):
                image_body_zone_count += 1
        if (
            layout_category == "numbered_or_icon_row_list"
            or "numbered" in must_match_text
            or "number badge" in must_match_text
            or (horizontal_count >= 5 and icon_count >= 5)
        ):
            return "numbered_or_icon_row_list"
        if layout_category == "card_callout_grid" or card_count >= 2:
            return "card_callout_grid"
        if (
            layout_category in {"editorial_explainer", "timeline_or_winding_process_path"}
            and horizontal_count >= 3
            and icon_count >= 2
        ):
            return "vertical_icon_explainer"
        if (
            layout_category in {"cover_or_hero_visual", "photo_led_cover", "editorial_explainer"}
            and large_visual_count == 0
            and horizontal_count >= 3
            and icon_count >= 2
            and (image_body_zone_count >= 2 or "icon" in must_match_text or "section" in must_match_text)
        ):
            return "vertical_icon_explainer"
        if (
            layout_category in {"cover_or_hero_visual", "photo_led_cover", "editorial_explainer"}
            and large_visual_count == 0
            and icon_count >= 3
            and (
                "vertical icon" in must_match_text
                or "icons aligned" in must_match_text
                or "icon linkage" in must_match_text
                or "body block" in must_match_text
                or "text right with icons" in must_match_text
                or "icons aligned with body" in must_match_text
            )
        ):
            return "vertical_icon_explainer"
        return layout_category

    @classmethod
    def _sample_blueprint_layout_instruction(cls, blueprint: dict[str, Any]) -> str:
        # Centralizes sample blueprint layout instruction from blueprint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(blueprint, dict) or not blueprint:
            return ""
        blueprint = cls._sample_blueprint_without_rendered_footer_text(blueprint)
        mode = cls._sample_blueprint_layout_mode(blueprint)
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        zones = blueprint.get("zones") if isinstance(blueprint.get("zones"), list) else []
        must_match = [
            cls._normalize_metadata_text(item, limit=90)
            for item in (blueprint.get("must_match") if isinstance(blueprint.get("must_match"), list) else [])
            if cls._normalize_metadata_text(item, limit=90)
        ][:6]

        role_counts: dict[str, int] = {}
        zone_summaries: list[str] = []
        for zone in zones[:12]:
            if not isinstance(zone, dict):
                continue
            role = cls._normalize_metadata_text(zone.get("role"), limit=40).casefold()
            if not role:
                continue
            role_counts[role] = role_counts.get(role, 0) + 1
            normalized_role = role
            if "card" in normalized_role:
                normalized_role = "card"
            elif "row" in normalized_role:
                normalized_role = "row"
            elif "number" in normalized_role or "badge" in normalized_role:
                normalized_role = "number_badge"
            elif "icon" in normalized_role:
                normalized_role = "icon"
            elif "footer" in normalized_role or "legal" in normalized_role or "disclaimer" in normalized_role:
                normalized_role = "empty_bottom_strip"
            if normalized_role not in {"headline", "body", "card", "row", "number_badge", "image", "icon", "cta", "empty_bottom_strip", "subheading"}:
                continue
            x = cls._similarity_float(zone.get("x"), default=0.0)
            y = cls._similarity_float(zone.get("y"), default=0.0)
            w = cls._similarity_float(zone.get("w"), default=0.0)
            h = cls._similarity_float(zone.get("h"), default=0.0)
            position = cls._zone_position_label(x, y, w, h)
            zone_summaries.append(f"{normalized_role} at {position} ({x:.2f},{y:.2f},{w:.2f},{h:.2f})")

        module_parts = []
        for key, label in (
            ("horizontal_band_count", "horizontal/list bands"),
            ("card_like_count", "card/callout modules"),
            ("large_visual_count", "large visual regions"),
            ("small_icon_like_count", "small icon/image marks"),
            ("top_text_band_count", "top text bands"),
            ("footer_band_count", "empty bottom safe strips"),
        ):
            value = int(counts.get(key) or 0)
            if value:
                module_parts.append(f"{value} {label}")

        if mode == "numbered_or_icon_row_list":
            base = "Use the selected sample page's observed row/list module structure."
            avoid = "Do not switch to any unobserved visual system, module family, or generic finance motif unless the selected sample page visibly uses it."
        elif mode == "card_callout_grid":
            base = "Use the selected sample page's observed card/callout module structure."
            avoid = "Do not switch to any unobserved hero scene, screen-based visual, two-column text layout, or generic finance motif unless the selected sample page visibly uses that structure."
        elif mode == "vertical_icon_explainer":
            base = "Use the selected sample page's observed icon/text explainer structure, including its vertical flow and any visible connector/path treatment."
            avoid = "Do not switch to any unobserved person/product scene, CTA module, screen-based visual, single hero visual, or generic closing motif unless the selected sample page visibly uses that structure."
        else:
            base = "Use the selected sample page's observed layout structure."
            avoid = "Do not replace the selected sample page's structure with a generic carousel layout."

        details = []
        if module_parts:
            details.append("Preserve approximately " + ", ".join(module_parts) + ".")
        if zone_summaries:
            details.append("Follow observed zones: " + "; ".join(zone_summaries[:8]) + ".")
        if must_match:
            details.append("Honor sample must-match cues: " + "; ".join(must_match) + ".")
        return " ".join([base, *details, avoid]).strip()

    @classmethod
    def _sample_blueprint_without_rendered_footer_text(cls, blueprint: dict[str, Any]) -> dict[str, Any]:
        # Extracts sample blueprint without rendered footer text from blueprint for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        if not isinstance(blueprint, dict) or not blueprint:
            return {}
        cleaned = dict(blueprint)

        def footer_like(value: Any) -> bool:
            # Centralizes footer from input value for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            text = cls._normalize_metadata_text(value, limit=180).casefold()
            return any(token in text for token in ("footer", "legal", "disclaimer", "fine print"))

        zones: list[dict[str, Any]] = []
        source_zones = blueprint.get("zones") if isinstance(blueprint.get("zones"), list) else []
        for zone in source_zones:
            if not isinstance(zone, dict):
                continue
            copied = dict(zone)
            if footer_like(copied.get("role")):
                copied["role"] = "empty_bottom_safe_strip"
            zones.append(copied)
        if zones:
            cleaned["zones"] = zones

        module_counts = dict(blueprint.get("module_counts") or {}) if isinstance(blueprint.get("module_counts"), dict) else {}
        if module_counts.get("footer_band_count"):
            module_counts["empty_bottom_safe_strip_count"] = int(module_counts.get("footer_band_count") or 0)
            module_counts["footer_band_count"] = 0
        if module_counts:
            cleaned["module_counts"] = module_counts

        must_match: list[str] = []
        added_empty_strip = False
        source_must_match = blueprint.get("must_match") if isinstance(blueprint.get("must_match"), list) else []
        for item in source_must_match:
            text = cls._normalize_metadata_text(item, limit=160)
            if not text:
                continue
            if footer_like(text):
                if not added_empty_strip:
                    must_match.append("bottom safe strip kept blank for exact post-processing text")
                    added_empty_strip = True
                continue
            must_match.append(text)
        if must_match:
            cleaned["must_match"] = must_match
        return cleaned

    @classmethod
    def _sample_adaptation_fact_lines(
        cls,
        slide: dict[str, Any],
        *,
        content_metadata: dict[str, Any],
        limit: int,
    ) -> list[str]:
        # Extracts sample adaptation fact lines from slide, content metadata, and limit for AIOrchestratorService.
        # It calls _claim_evidence_pair_lines and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        row_lines: list[str] = []
        seen_lines: set[str] = set()

        def add_line(value: Any, *, text_limit: int = 150) -> None:
            # Extracts add line from input value and text limit for AIOrchestratorService.
            # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
            text = cls._normalize_metadata_text(value, limit=text_limit)
            key = re.sub(r"[^a-z0-9]+", " ", text.casefold()).strip()
            if not text or not key or key in seen_lines:
                return
            seen_lines.add(key)
            row_lines.append(text)

        for key in ("body_points", "proof_points", "stat_highlights"):
            for item in cls._normalize_metadata_list(slide.get(key), limit=limit):
                add_line(item)
        for key in ("proof_points", "body_points", "stat_highlights", "trust_builders"):
            for item in cls._normalize_metadata_list(content_metadata.get(key), limit=limit):
                add_line(item)
        for pair_line in cls._claim_evidence_pair_lines(
            cls._normalize_claim_evidence_pairs(content_metadata.get("claim_evidence_pairs"), limit=limit),
            limit=limit,
        ):
            add_line(pair_line)
        for sentence in cls._sentences(slide.get("body"))[:limit]:
            add_line(sentence)
        return row_lines[:limit]

    @classmethod
    def _adapt_slide_copy_to_sample_blueprint(
        cls,
        slide: dict[str, Any],
        *,
        content_metadata: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Centralizes adapt slide copy sample blueprint from slide and content metadata for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        content_metadata = content_metadata if isinstance(content_metadata, dict) else {}
        blueprint = metadata.get("sample_page_blueprint") if isinstance(metadata.get("sample_page_blueprint"), dict) else {}
        if not blueprint:
            return slide
        layout_category = cls._normalize_metadata_text(blueprint.get("layout_category"), limit=80).casefold()
        sample_layout_mode = cls._sample_blueprint_layout_mode(blueprint)
        density = cls._normalize_metadata_text(blueprint.get("density"), limit=32).casefold()
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        permissions = blueprint.get("visual_permissions") if isinstance(blueprint.get("visual_permissions"), dict) else {}
        adapted = dict(slide)
        adapted_metadata = dict(metadata)
        original_copy_shape = {
            "proof_points": len(adapted.get("proof_points") or []),
            "body_points": len(adapted.get("body_points") or []),
            "stat_highlights": len(adapted.get("stat_highlights") or []),
            "has_cta": bool(cls._normalize_metadata_text(adapted.get("cta"), limit=120)),
        }
        max_modules = max(
            int(counts.get("card_like_count") or 0),
            min(int(counts.get("horizontal_band_count") or 0), 8),
            1,
        )
        if sample_layout_mode == "numbered_or_icon_row_list":
            max_items = max(3, min(int(counts.get("horizontal_band_count") or max_modules), 8))
            adapted["supporting_line"] = cls._normalize_metadata_text(adapted.get("supporting_line"), limit=120)
            row_lines = cls._sample_adaptation_fact_lines(adapted, content_metadata=content_metadata, limit=max_items)
            if row_lines:
                adapted["proof_points"] = row_lines[:max_items]
                adapted["body_points"] = []
                adapted["stat_highlights"] = []
            adapted["visual_focus"] = (
                cls._sample_blueprint_layout_instruction(blueprint)
                + " Use one concise approved factual line per observed row/list module."
            )
            adapted_metadata["sample_row_list_target_count"] = max_items
            adapted_metadata["sample_row_list_source_count"] = len(row_lines)
        elif sample_layout_mode == "card_callout_grid":
            max_items = max(2, min(int(counts.get("card_like_count") or max_modules), 4))
            adapted["supporting_line"] = cls._normalize_metadata_text(adapted.get("supporting_line"), limit=120)
            card_lines = cls._sample_adaptation_fact_lines(adapted, content_metadata=content_metadata, limit=max_items)
            if card_lines:
                adapted["proof_points"] = card_lines[:max_items]
                adapted["body_points"] = []
                adapted["stat_highlights"] = []
            adapted["visual_focus"] = (
                cls._sample_blueprint_layout_instruction(blueprint)
                + " Use one concise approved factual line per observed card/callout module."
            )
            adapted_metadata["sample_card_grid_target_count"] = max_items
            adapted_metadata["sample_card_grid_source_count"] = len(card_lines)
        elif sample_layout_mode == "vertical_icon_explainer":
            max_items = max(3, min(int(counts.get("horizontal_band_count") or 3), 4))
            adapted["supporting_line"] = cls._normalize_metadata_text(adapted.get("supporting_line"), limit=120)
            node_lines = cls._sample_adaptation_fact_lines(adapted, content_metadata=content_metadata, limit=max_items)
            if node_lines:
                adapted["proof_points"] = node_lines[:max_items]
                adapted["body_points"] = []
                adapted["stat_highlights"] = []
            adapted["cta"] = ""
            adapted["visual_focus"] = (
                cls._sample_blueprint_layout_instruction(blueprint)
                + " Use one concise approved factual line per observed icon/text module."
            )
            adapted_metadata["sample_vertical_icon_target_count"] = max_items
            adapted_metadata["sample_vertical_icon_source_count"] = len(node_lines)
        elif layout_category == "cover_or_hero_visual":
            max_items = 0 if density == "airy" else 1
            adapted["supporting_line"] = cls._normalize_metadata_text(adapted.get("supporting_line"), limit=150)
        elif layout_category in {"timeline_or_winding_process_path"}:
            max_items = max(2, min(max_modules, 4))
            adapted["supporting_line"] = cls._normalize_metadata_text(adapted.get("supporting_line"), limit=140)
        elif density == "airy":
            max_items = 1
            adapted["supporting_line"] = cls._normalize_metadata_text(adapted.get("supporting_line"), limit=150)
        elif density == "balanced":
            max_items = 3
        else:
            max_items = max(3, min(max_modules, 6))

        for key in ("proof_points", "body_points", "stat_highlights"):
            values = adapted.get(key)
            if isinstance(values, list):
                adapted[key] = values[:max_items]
        if not bool(permissions.get("cta_allowed")):
            adapted["cta"] = ""
        adapted_metadata["sample_copy_density_enforced"] = {
            "layout_category": layout_category,
            "density": density,
            "max_items": max_items,
            "cta_allowed": bool(permissions.get("cta_allowed")),
            "original_copy_shape": original_copy_shape,
        }
        adapted["metadata"] = adapted_metadata
        return adapted

    @classmethod
    def _merge_vision_page_blueprint(
        cls,
        *,
        base_blueprint: dict[str, Any],
        vision_analysis: dict[str, Any],
    ) -> dict[str, Any]:
        # Merges vision page blueprint from base blueprint and vision analysis for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        merged = dict(base_blueprint or {})
        page_blueprint = (
            vision_analysis.get("page_blueprint")
            if isinstance(vision_analysis.get("page_blueprint"), dict)
            else {}
        )
        if page_blueprint:
            for key in ("layout_category", "density", "closing_grammar"):
                value = cls._normalize_metadata_text(page_blueprint.get(key), limit=90)
                if value:
                    merged[key] = value
            if isinstance(page_blueprint.get("zones"), list) and page_blueprint.get("zones"):
                merged["zones"] = page_blueprint.get("zones")[:16]
            if isinstance(page_blueprint.get("must_match"), list) and page_blueprint.get("must_match"):
                merged["must_match"] = [
                    cls._normalize_metadata_text(item, limit=160)
                    for item in page_blueprint.get("must_match")[:10]
                    if cls._normalize_metadata_text(item, limit=160)
                ]
            base_counts = merged.get("module_counts") if isinstance(merged.get("module_counts"), dict) else {}
            vision_counts = page_blueprint.get("module_counts") if isinstance(page_blueprint.get("module_counts"), dict) else {}
            if vision_counts:
                translated_counts = dict(base_counts)
                key_map = {
                    "row_module_count": "horizontal_band_count",
                    "card_module_count": "card_like_count",
                    "icon_count": "small_icon_like_count",
                    "large_visual_count": "large_visual_count",
                    "footer_block_count": "footer_band_count",
                    "headline_block_count": "top_text_band_count",
                    "text_block_count": "text_block_count",
                    "cta_count": "cta_count",
                    "chart_count": "chart_count",
                    "table_count": "table_count",
                    "metric_tile_count": "metric_tile_count",
                    "metric_tiles_count": "metric_tiles_count",
                    "logo_count": "logo_count",
                }
                for source_key, target_key in key_map.items():
                    value = vision_counts.get(source_key)
                    if isinstance(value, (int, float)):
                        translated_counts[target_key] = int(value)
                merged["module_counts"] = translated_counts
            if isinstance(page_blueprint.get("visual_permissions"), dict):
                merged["visual_permissions"] = {
                    **(merged.get("visual_permissions") if isinstance(merged.get("visual_permissions"), dict) else {}),
                    **page_blueprint.get("visual_permissions"),
                }
        for key in ("ocr_structure", "premium_quality", "visual_craft_dna", "image_treatment", "typography_dna", "composition_logic", "content_structure", "subject_semantics"):
            value = vision_analysis.get(key)
            if isinstance(value, dict) and value:
                merged[key] = value
        provider_usage = vision_analysis.get("provider_usage")
        if isinstance(provider_usage, dict) and provider_usage:
            merged["provider_usage"] = provider_usage
        vision_cache = vision_analysis.get("vision_cache")
        if isinstance(vision_cache, dict) and vision_cache:
            merged["vision_cache"] = vision_cache
        if vision_analysis:
            merged["vision_analysis_status"] = "openai_vision_enhanced"
        return merged

    def _vision_enhance_page_blueprint(
        self,
        *,
        image_path: str,
        base_blueprint: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes vision enhance page blueprint from image path and base blueprint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not image_path or not isinstance(base_blueprint, dict):
            return base_blueprint or {}
        if not getattr(self.template_vision, "client", None):
            return base_blueprint
        fallback = {
            "page_blueprint": base_blueprint,
            "layout_type": base_blueprint.get("layout_category", "template"),
            "visual_hierarchy": {"density": base_blueprint.get("density")},
            "content_structure": {},
            "visual_craft_dna": {},
            "ocr_structure": {},
            "premium_quality": {},
        }
        try:
            analysis = self.template_vision.analyze(image_path, fallback)
        except Exception:  # noqa: BLE001
            return base_blueprint
        if not isinstance(analysis, dict):
            return base_blueprint
        return self._merge_vision_page_blueprint(
            base_blueprint=base_blueprint,
            vision_analysis=analysis,
        )

    def _sample_output_similarity_from_paths_with_vision(
        self,
        *,
        sample_image_path: str,
        output_image_path: str,
        sample_blueprint: dict[str, Any] | None = None,
        slide_index: int | None = None,
        slide_count: int | None = None,
        exact_logo_overlay_deferred: bool = False,
        legal_footer_overlay_deferred: bool = False,
    ) -> dict[str, Any]:
        # Centralizes sample output similarity paths vision from sample image path, output image path, and sample blueprint for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        sample = sample_blueprint if isinstance(sample_blueprint, dict) and sample_blueprint else self._sample_page_blueprint_from_image_path(
            sample_image_path,
            slide_index=slide_index,
            slide_count=slide_count,
        )
        if sample.get("vision_analysis_status") != "openai_vision_enhanced":
            sample = self._vision_enhance_page_blueprint(
                image_path=sample_image_path,
                base_blueprint=sample,
            )
        output = self._sample_page_blueprint_from_image_path(
            output_image_path,
            slide_index=slide_index,
            slide_count=slide_count,
        )
        output = self._vision_enhance_page_blueprint(
            image_path=output_image_path,
            base_blueprint=output,
        )
        report = self._sample_output_similarity_report(
            sample_blueprint=sample,
            output_blueprint=output,
            exact_logo_overlay_deferred=exact_logo_overlay_deferred,
            legal_footer_overlay_deferred=legal_footer_overlay_deferred,
        )
        report["sample_blueprint"] = sample
        report["output_blueprint"] = output
        report["vision_enhanced"] = bool(
            sample.get("vision_analysis_status") == "openai_vision_enhanced"
            or output.get("vision_analysis_status") == "openai_vision_enhanced"
        )
        return report

    @classmethod
    def _scene_graph_explicit_reference_assets(
        cls,
        scene_graph: GenerationSceneGraph,
        *,
        request: AIOrchestrationRequest,
    ) -> list[dict[str, Any]]:
        # Extracts scene graph explicit reference assets from scene graph and request state for AIOrchestratorService.
        # It calls _reference_asset_by_storage_path and _scene_graph_element_is_visual_image_like to turn raw evidence into the structured signal the ca.
        assets: list[dict[str, Any]] = []
        seen_paths: set[str] = set()
        for element in scene_graph.elements:
            if not cls._scene_graph_element_is_visual_image_like(element):
                continue
            asset = element.asset
            if asset is None:
                continue
            storage_path = str(asset.storage_path or "").strip()
            if not storage_path or storage_path in seen_paths:
                continue
            matched = cls._reference_asset_by_storage_path(request, storage_path)
            if matched is None:
                continue
            seen_paths.add(storage_path)
            assets.append(matched)
        return assets

    @staticmethod
    def _is_literal_reference_surface_role(asset_role: str | None) -> bool:
        # Checks literal reference surface role from asset role for AIOrchestratorService.
        # The boolean result controls the nearby policy or validation branch.
        normalized = str(asset_role or "").strip().casefold()
        return normalized in {
            "reference_creative",
            "template_preview",
            "template",
        }

    @classmethod
    def _asset_allows_literal_scene_binding(cls, asset: dict[str, Any]) -> bool:
        # Extracts asset allows literal scene binding from asset record for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        metadata = asset.get("metadata")
        if not isinstance(metadata, dict):
            metadata = {}
        return bool(
            metadata.get("literal_render_allowed")
            or metadata.get("renderer_safe_reference")
            or metadata.get("use_as_content_image")
        )

    @classmethod
    def _should_skip_literal_reference_binding(
        cls,
        scene_graph: GenerationSceneGraph,
        asset: dict[str, Any],
    ) -> bool:
        # Checks skip literal reference binding from scene graph and asset record for AIOrchestratorService.
        # It reuses _is_literal_reference_surface_role and _asset_allows_literal_scene_binding so related checks follow the same rule.
        validation_hints = scene_graph.validation_hints if isinstance(scene_graph.validation_hints, dict) else {}
        template_surface_policy = str(validation_hints.get("template_surface_policy") or "").strip().lower()
        return (
            cls._is_literal_reference_surface_role(asset.get("asset_role"))
            and not cls._asset_allows_literal_scene_binding(asset)
            and (
                scene_graph.layout_mode == "synthesized_layout"
                or template_surface_policy == "style_reference_only"
            )
        )

    @staticmethod
    def bind_reference_assets(
        scene_graph: GenerationSceneGraph,
        reference_assets: list[dict[str, Any]],
    ) -> GenerationSceneGraph:
        # Extracts bind reference assets from scene graph and reference assets for AIOrchestratorService.
        # It calls _should_skip_literal_reference_binding and _scene_graph_element_is_visual_image_like to turn raw evidence into the structured signal.
        if not reference_assets:
            return scene_graph
        scene_graph_data = scene_graph.model_dump(mode="json")
        existing_assets = list(scene_graph_data.get("assets") or [])
        existing_paths = {
            str(asset.get("storage_path") or "").strip()
            for asset in existing_assets
            if isinstance(asset, dict) and str(asset.get("storage_path") or "").strip()
        }
        for asset in reference_assets:
            if AIOrchestratorService._should_skip_literal_reference_binding(scene_graph, asset):
                continue
            storage_path = str(asset.get("storage_path") or "").strip()
            if not storage_path or storage_path in existing_paths:
                continue
            existing_paths.add(storage_path)
            existing_assets.append(
                {
                    "asset_id": str(asset.get("asset_id") or ""),
                    "asset_role": str(asset.get("asset_role") or "reference_creative"),
                    "storage_path": storage_path,
                    "trust_level": str(asset.get("trust_level") or "trusted"),
                    "notes": str(((asset.get("metadata") or {}) if isinstance(asset.get("metadata"), dict) else {}).get("label") or ""),
                }
            )
        scene_graph_data["assets"] = existing_assets
        image_elements = [
            element
            for element in scene_graph_data.get("elements", [])
            if isinstance(element, dict) and AIOrchestratorService._scene_graph_element_is_visual_image_like(element)
        ]
        asset_index = 0
        for element in image_elements:
            if asset_index >= len(reference_assets):
                break
            asset = reference_assets[asset_index]
            if AIOrchestratorService._should_skip_literal_reference_binding(scene_graph, asset):
                asset_index += 1
                continue
            asset_payload = dict(element.get("asset") or {})
            current_storage_path = str(asset_payload.get("storage_path") or "").strip()
            if current_storage_path:
                continue
            asset_payload["asset_id"] = str(asset.get("asset_id") or "") or asset_payload.get("asset_id")
            asset_payload["asset_role"] = str(asset.get("asset_role") or "reference_creative")
            asset_payload["storage_path"] = str(asset.get("storage_path") or "")
            asset_payload["trust_level"] = str(asset.get("trust_level") or "trusted")
            element["asset"] = asset_payload
            asset_index += 1
        return GenerationSceneGraph.model_validate(scene_graph_data)

    @classmethod
    def assess_creative_quality(
        cls,
        *,
        scene_graph: GenerationSceneGraph,
        creative_decision: CreativeDecisionPayload,
        validation_report: SceneGraphValidationReport,
        request: AIOrchestrationRequest,
        selected_reference_images: list[dict[str, Any]],
        used_support_fallback: bool,
        compiled_context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Checks assess creative quality from scene graph, creative decision, and validation report for AIOrchestratorService.
        # It reuses _follow_up_mode and _prior_layout_archetype so related checks follow the same rule.
        score = 0.92
        issues: list[str] = []
        issue_lookup = {issue.rule_id for issue in validation_report.issues}
        if validation_report.status != "clean":
            score -= min(len(validation_report.issues) * 0.06, 0.3)
            issues.extend(sorted(issue_lookup))
        if used_support_fallback:
            score -= 0.12
            issues.append("support_fallback_used")
        visible_elements = [element for element in scene_graph.elements if element.visible]
        content_elements = [element for element in visible_elements if element.role != "background"]
        if not cls._scene_graph_has_authoritative_geometry(scene_graph):
            score -= 0.1
            issues.append("weak_geometry_contract")
        elif cls._compiled_context_has_authoritative_layout(compiled_context):
            score += 0.03
        if len(content_elements) < 4:
            score -= 0.14
            issues.append("sparse_scene_graph")
        image_elements = [element for element in content_elements if cls._scene_graph_element_is_visual_image_like(element)]
        if not image_elements:
            score -= 0.08
            issues.append("missing_primary_visual")
        if not any(element.role == "logo" for element in content_elements):
            score -= 0.06
            issues.append("missing_logo")
        if "asset_strategy_overloaded" in issue_lookup:
            score -= 0.08
        follow_up_mode = cls._follow_up_mode(request)
        prior_archetype = cls._prior_layout_archetype(request)
        current_archetype = str((scene_graph.styles or {}).get("layout_archetype") or "").strip().casefold()
        if follow_up_mode == "variant_of_previous" and prior_archetype and current_archetype == prior_archetype:
            score -= 0.18
            issues.append("repeated_layout_archetype")
        if len(selected_reference_images) >= 2 and str(request.studio_panel.get("format") or "").strip().lower() == "carousel":
            bound_paths = {
                str((element.asset.storage_path if element.asset else "") or "").strip()
                for element in scene_graph.elements
                if cls._scene_graph_element_is_visual_image_like(element)
            }
            matched = sum(1 for asset in selected_reference_images if str(asset.get("storage_path") or "").strip() in bound_paths)
            if (
                matched < min(len(selected_reference_images), 2)
                and not cls._carousel_sequence_uses_multiple_reference_images(request)
            ):
                score -= 0.08
                issues.append("multi_image_underused")
        reference_topic_issue = cls._reference_topic_alignment_issue(request, selected_reference_images)
        if reference_topic_issue:
            score -= 0.24
            issues.append(reference_topic_issue)
        if image_elements:
            craft_supported = any(
                bool((element.validation_hints or {}).get(key))
                for element in image_elements
                for key in (
                    "visual_depth_style",
                    "visual_rendering_style",
                    "visual_polish_level",
                    "composition_balance",
                    "composition_framing",
                    "subject_scene_type",
                )
            )
            if not craft_supported:
                score -= 0.08
                issues.append("craft_direction_weak")
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=compiled_context if isinstance(compiled_context, dict) else {},
        )
        has_reference_zone_maps = cls._request_has_authoritative_reference_zone_maps(request)
        reference_family_match = (
            {"score": 1.0, "issues": [], "deferred_to_sample_page_blueprint": True}
            if template_surface_policy == "style_reference_only" and not has_reference_zone_maps
            else cls._reference_family_closeness(
                scene_graph,
                compiled_context=compiled_context,
            )
        )
        if reference_family_match.get("issues"):
            score -= min((1.0 - float(reference_family_match.get("score") or 0.0)) * 0.2, 0.18)
            issues.extend(
                issue
                for issue in reference_family_match.get("issues", [])
                if issue not in issues
            )
        multimodal_issue = cls._multimodal_balance_issue(
            scene_graph,
            format_name=str(request.studio_panel.get("format") or ""),
        )
        if multimodal_issue:
            score -= 0.06
            issues.append(f"multimodal_balance_{multimodal_issue}")
        if template_surface_policy == "style_reference_only" and selected_reference_images:
            if has_reference_zone_maps and not cls._scene_graph_has_authoritative_geometry(scene_graph):
                score -= 0.1
                issues.append("sample_structure_underused")
            elif not cls._compiled_context_has_authoritative_layout(compiled_context):
                score -= 0.08
                issues.append("sample_structure_underused")
        if template_surface_policy == "style_reference_only" and has_reference_zone_maps and not selected_reference_images:
            score -= 0.22
            issues.append("reference_conditioning_missing")
        if scene_graph.confidence < 0.55:
            score -= 0.06
            issues.append("low_scene_confidence")
        score = max(0.0, round(score, 2))
        return {
            "score": score,
            "issues": issues,
            "retry_recommended": score < cls.IMAGE_QUALITY_MIN_SCORE,
            "used_support_fallback": used_support_fallback,
            "selected_reference_image_count": len(selected_reference_images),
            "layout_archetype": current_archetype or None,
            "reference_family_match": reference_family_match,
        }

    @staticmethod
    def _quality_report_from_assessment(assessment: dict[str, Any]) -> SceneGraphValidationReport:
        # Checks quality report assessment from assessment for AIOrchestratorService.
        # The boolean result controls the nearby policy or validation branch.
        issue_map = {
            "support_fallback_used": (
                "support_fallback_used",
                "Fallback composition was used and still needs a more premium, original layout.",
                "Produce a more premium scene graph without relying on the fallback archetype.",
            ),
            "sparse_scene_graph": (
                "quality_sparse_scene_graph",
                "Scene graph still reads as too sparse for a premium social creative.",
                "Increase hierarchy, supporting structure, and visual emphasis while keeping the layout clean.",
            ),
            "missing_primary_visual": (
                "quality_missing_primary_visual",
                "Creative lacks a convincing primary visual emphasis.",
                "Add or strengthen the hero visual treatment with cleaner negative space.",
            ),
            "missing_logo": (
                "quality_missing_logo",
                "Brand identity is still too weak because the logo treatment is missing.",
                "Include a clear logo placement in a brand-safe area.",
            ),
            "repeated_layout_archetype": (
                "quality_repeated_layout_archetype",
                "Variant/regenerate request reused the same layout archetype as the prior creative.",
                "Choose a distinctly different layout archetype and composition from the previous output.",
            ),
            "multi_image_underused": (
                "quality_multi_image_underused",
                "Multiple reference images were available but the composition did not make deliberate use of them.",
                "Use the strongest subset of the uploaded images in a purposeful multi-image composition.",
            ),
            "reference_topic_mismatch": (
                "quality_reference_topic_mismatch",
                "Selected reference imagery does not match the current request topic strongly enough.",
                "Use topic-relevant uploaded samples or factual source assets instead of generic brand/template assets that can leak unrelated objects.",
            ),
            "weak_geometry_contract": (
                "quality_weak_geometry_contract",
                "The current plan does not preserve a strong enough geometry contract for premium rendering.",
                "Return a scene graph with explicit normalized coordinates and stronger region discipline for headline, image, CTA, and corner-safe zones.",
            ),
            "craft_direction_weak": (
                "quality_craft_direction_weak",
                "The current plan lacks enough premium craft direction to match the sample's visual finish.",
                "Add stronger craft, rendering, composition, and subject guidance to the scene graph instead of generic visual placeholders.",
            ),
            "reference_family_zone_drift": (
                "quality_reference_family_zone_drift",
                "The current layout is drifting away from the approved reference family zone grammar.",
                "Bring the scene graph back toward the expected family zone roles, hierarchy, and region structure instead of using a generic layout.",
            ),
            "reference_family_image_zone_drift": (
                "quality_reference_family_image_zone_drift",
                "The current plan is not keeping imagery inside the approved reference family image zones.",
                "Place generated imagery only inside the expected image roles from the reference family profile.",
            ),
            "reference_family_layout_drift": (
                "quality_reference_family_layout_drift",
                "The current layout archetype is drifting away from the approved reference family.",
                "Use the reference family's intended layout archetype instead of a generic or unrelated composition.",
            ),
            "reference_family_geometry_drift": (
                "quality_reference_family_geometry_drift",
                "The current plan is drifting away from the approved reference-family region geometry.",
                "Bring the scene graph boxes back toward the sample-driven zone map so spacing, hierarchy, and negative space stay faithful to the approved family.",
            ),
            "reference_family_module_drift": (
                "quality_reference_family_module_drift",
                "The current plan is missing too much of the approved reference family module grammar.",
                "Recompose the creative using the expected module patterns such as proof grids, comparison modules, hero splits, or CTA strips.",
            ),
            "multimodal_balance_text_heavy": (
                "quality_multimodal_balance_text_heavy",
                "The current plan is too text-heavy relative to the supporting visual system.",
                "Give the composition a stronger image-led or explainer-led focal system and distribute evidence into cleaner modular regions instead of one dense text block.",
            ),
            "multimodal_balance_image_heavy": (
                "quality_multimodal_balance_image_heavy",
                "The current plan is visually dominant but under-supports the message with enough editorial structure.",
                "Add clearer headline, support, or evidence regions so the message feels deliberate rather than like a generic hero image.",
            ),
            "sample_structure_underused": (
                "quality_sample_structure_underused",
                "The current plan is underusing the uploaded sample's composition skeleton.",
                "Preserve the sample's region proportions, sequencing, and negative-space structure more strictly while still reinterpreting the artwork from scratch.",
            ),
            "reference_conditioning_missing": (
                "quality_reference_conditioning_missing",
                "The current plan lost the intended sample-reference visual conditioning and is likely to drift into generic imagery.",
                "Use the approved sample-family reference images for conditioning so palette, image treatment, and visual motifs stay closer to the selected brand/sample family.",
            ),
            "low_scene_confidence": (
                "quality_low_scene_confidence",
                "The current plan is not confident enough for final rendering.",
                "Return a clearer, more deliberate premium layout plan with stronger visual hierarchy.",
            ),
        }
        issues: list[SceneGraphValidationIssue] = []
        for item in assessment.get("issues", []) or []:
            mapped = issue_map.get(str(item))
            if not mapped:
                continue
            rule_id, message, correction = mapped
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id=rule_id,
                    message=message,
                    expected_correction=correction,
                    repairable=True,
                )
            )
        return SceneGraphValidationReport(
            status="needs_repair" if issues else "clean",
            issues=issues,
            summary=[issue.message for issue in issues[:6]],
            repairable=True,
        )

    @staticmethod
    def _allowed_palette_values(compiled_context: dict[str, Any]) -> set[str]:
        # Derives allowed palette from compiled context for AIOrchestratorService.
        # This turns source evidence into a stable planning hint.
        palette_roles = (compiled_context.get("brand_visual_brief", {}) or {}).get("palette_roles", {}) or {}
        allowed = {
            normalized.casefold()
            for value in palette_roles.values()
            if (normalized := normalize_hex(value))
        }
        allowed.update({"#ffffff", "#000000", "#111111", "#f8f8f8", "#f4f4f4", "#1f2937"})
        return allowed

    @staticmethod
    def _style_color_is_palette_safe(value: Any, allowed_palette: set[str]) -> bool:
        # Derives style color palette safe from input value and allowed palette for AIOrchestratorService.
        # This turns source evidence into a stable planning hint.
        normalized = normalize_hex(value)
        if not normalized:
            return True
        lowered = normalized.casefold()
        if lowered in allowed_palette:
            return True
        rgb = hex_to_rgb(normalized)
        if not rgb:
            return False
        if is_soft_neutral_color(rgb):
            return True
        for allowed in allowed_palette:
            allowed_rgb = hex_to_rgb(allowed)
            if not allowed_rgb or is_soft_neutral_color(allowed_rgb):
                continue
            distance = sum((channel - allowed_channel) ** 2 for channel, allowed_channel in zip(rgb, allowed_rgb)) ** 0.5
            if distance <= 120:
                return True
        return False

    @classmethod
    def _normalize_topic_anchor_token(cls, token: str) -> str:
        # Normalizes topic anchor token from token for AIOrchestratorService.
        # Later prompt and metadata code can compare the cleaned value directly.
        text = str(token or "").strip().casefold()
        text = re.sub(r"^[^a-z0-9]+|[^a-z0-9]+$", "", text)
        if len(text) <= 3:
            return ""
        if text.endswith("ies") and len(text) > 4:
            text = f"{text[:-3]}y"
        elif text.endswith("ing") and len(text) > 5:
            text = text[:-3]
        elif text.endswith("ed") and len(text) > 4:
            text = text[:-2]
        elif text.endswith("s") and len(text) > 4:
            text = text[:-1]
        return text if text and text not in cls.TOPIC_ANCHOR_STOPWORDS else ""

    @classmethod
    def _topic_anchor_keywords(cls, value: Any, *, limit: int = 8) -> set[str]:
        # Centralizes topic anchor keywords from input value and limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        words = re.findall(r"[A-Za-z][A-Za-z0-9]{2,}", cls._coerce_text_value(value))
        keywords: list[str] = []
        for word in words:
            token = cls._normalize_topic_anchor_token(word)
            if not token or token in keywords:
                continue
            keywords.append(token)
            if len(keywords) >= limit:
                break
        return set(keywords)

    @classmethod
    def _scene_graph_text_blob(cls, scene_graph: GenerationSceneGraph) -> str:
        # Extracts scene graph text blob from scene graph for AIOrchestratorService.
        # It calls _coerce_text_value to turn raw evidence into the structured signal the caller needs.
        parts: list[str] = []

        def collect(element: Any) -> None:
            # Centralizes collect from element for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if getattr(element, "text", None):
                parts.append(cls._coerce_text_value(element.text))
            if getattr(element, "content", None):
                parts.append(cls._coerce_text_value(element.content))
            for child in getattr(element, "elements", None) or []:
                collect(child)

        for element in scene_graph.elements:
            collect(element)
        return " ".join(part for part in parts if part)

    def validate_scene_graph(
        self,
        *,
        scene_graph: GenerationSceneGraph,
        creative_decision: CreativeDecisionPayload,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any],
    ) -> SceneGraphValidationReport:
        # Centralizes scene graph from scene graph, creative decision, and request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        issues: list[SceneGraphValidationIssue] = []
        allowed_palette = self._allowed_palette_values(compiled_context)
        allowed_fonts = {
            str(item).strip().casefold()
            for item in (compiled_context.get("brand_visual_brief", {}) or {}).get("font_families", []) or []
            if str(item).strip()
        }
        asset_trust: dict[str, str] = {}
        for asset in (request.asset_catalog or request.reference_assets):
            if not isinstance(asset, dict):
                continue
            asset_id = str(asset.get("asset_id") or "").strip()
            storage_path = str(asset.get("storage_path") or "").strip()
            trust_level = str(asset.get("trust_level") or "").strip().lower()
            if asset_id:
                asset_trust[asset_id] = trust_level
            if storage_path:
                asset_trust[storage_path] = trust_level

        requested_size = request.studio_panel.get("size") or {}
        if requested_size and (
            int(scene_graph.canvas.width) != int(requested_size.get("width") or scene_graph.canvas.width)
            or int(scene_graph.canvas.height) != int(requested_size.get("height") or scene_graph.canvas.height)
        ):
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="platform_canvas_mismatch",
                    message="Scene graph canvas does not match the requested platform size.",
                    expected_correction="Align the canvas width and height to the studio panel size.",
                    repairable=True,
                )
            )

        has_logo = any(element.visible and element.role == "logo" for element in scene_graph.elements)
        has_headline = any(element.visible and element.role == "headline" for element in scene_graph.elements)
        visible_elements = self._scene_graph_visible_elements(scene_graph)
        content_elements = [element for element in visible_elements if element.role != "background"]
        text_by_role: dict[str, set[str]] = {}
        for element in visible_elements:
            role = str(getattr(element, "role", "") or "").strip().lower()
            if role not in {"headline", "supporting_line", "body", "cta", "proof_point"}:
                continue
            text_value = self._normalize_metadata_text(getattr(element, "text", None), limit=220).casefold()
            if not text_value:
                continue
            text_by_role.setdefault(text_value, set()).add(role)
        if any(len(roles) >= 2 for roles in text_by_role.values()):
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="repeated_content_roles",
                    message="The same visible copy is assigned to multiple content roles.",
                    expected_correction="Keep headline, support copy, body, CTA, and proof roles distinct.",
                    repairable=True,
                )
            )
        logo_geometry = self._logo_safe_zone_geometry(scene_graph)
        logo_position_hint = self._normalize_metadata_text(
            (scene_graph.styles or {}).get("logo_position")
            or (scene_graph.validation_hints or {}).get("logo_position"),
            limit=80,
        )
        logo_background_tone = self._resolve_logo_background_tone(
            creative_decision=creative_decision,
            scene_graph=scene_graph,
        )
        has_visual_emphasis = any(
            element.role in {"image", "icon", "decorative_shape", "logo"}
            or element.element_type in {"image", "icon", "decorative_shape", "background_shape", "logo"}
            for element in content_elements
        )
        session_brief = compiled_context.get("session_brief", {}) or {}
        follow_up_mode = str(session_brief.get("follow_up_mode") or "").strip().casefold()
        prior_layout_archetype = str(session_brief.get("prior_layout_archetype") or "").strip().casefold()
        current_layout_archetype = str((scene_graph.styles or {}).get("layout_archetype") or "").strip().casefold()
        identity = request.resolved_brand_context.get("identity", {}) or {}
        prompt_topic_keywords = self._topic_anchor_keywords(request.prompt)
        scene_text_blob = self._scene_graph_text_blob(scene_graph)
        if not has_headline:
            issues.append(
                SceneGraphValidationIssue(
                    severity="error",
                    rule_id="missing_headline",
                    message="Scene graph must include a visible headline element.",
                    expected_correction="Add a headline element with concrete geometry and styling.",
                    repairable=True,
                )
            )
        if (identity.get("logo_asset_id") or identity.get("logo_asset_ids") or identity.get("logo_assets")) and not has_logo:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="logo_required",
                    message="Brand identity requires a logo element in the scene graph.",
                    expected_correction="Add a visible logo element in a safe brand placement.",
                    repairable=True,
                )
            )
        if has_logo and logo_geometry is None:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="logo_geometry_unclear",
                    message="Logo element is present but the reserved overlay geometry is missing or invalid.",
                    expected_correction="Provide a concrete corner-based logo box with usable geometry for overlay.",
                    repairable=True,
                )
            )
        if has_logo and logo_geometry is not None and logo_position_hint:
            current_anchor = self._anchor_from_logo_geometry(logo_geometry)
            hint_anchor = self._logo_anchor_from_hint(logo_position_hint)
            if hint_anchor and current_anchor != f"{hint_anchor[0]}-{hint_anchor[1]}":
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id="logo_position_mismatch",
                        message="Logo geometry does not match the requested or stored logo position hint.",
                        expected_correction="Align the logo box to the intended logo_position corner.",
                        repairable=True,
                    )
                )
        if has_logo and not logo_background_tone:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="logo_surface_tone_missing",
                    message="Logo reservation does not specify whether the surface behind the exact logo should be light, dark, or neutral.",
                    expected_correction="Set logo_background_tone or a matching logo_variant so the overlay uses a clearer contrast strategy.",
                    repairable=True,
                )
            )
        preset = str(request.studio_panel.get("platform_preset") or "").strip().lower()
        if preset in {"instagram", "linkedin", "x", "youtube_thumbnail"} and len(content_elements) < 4:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="insufficient_scene_graph_structure",
                    message="Scene graph is too sparse for a social creative and will render as an under-designed poster.",
                    expected_correction="Add structured supporting elements such as proof_points, icons, decorative shapes, image, and logo.",
                    repairable=True,
                )
            )
        if preset in {"instagram", "linkedin"} and not has_visual_emphasis:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="missing_visual_emphasis",
                    message="Scene graph lacks a primary visual or decorative emphasis element.",
                    expected_correction="Add an image, icon sequence, decorative shapes, or a prominent logo treatment.",
                    repairable=True,
                )
            )

        # Validate template format compatibility for carousel requests
        format_name = request.studio_panel.get("format", "").strip().lower()
        selected_template_id = creative_decision.selected_template_id

        if format_name in ("carousel", "instagram_carousel", "linkedin_carousel"):
            # Check if template supports multi-frame format
            if selected_template_id and len(scene_graph.elements) < 6:
                # Carousels should have richer element structure (typically 6+ elements per slide)
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id="template_format_mismatch",
                        element_path="scene_graph",
                        message=f"Template '{selected_template_id}' may not support carousel format (sparse element count: {len(scene_graph.elements)}). Carousels typically need 6+ elements per slide for rich structure.",
                        expected_correction="Use a carousel-compatible template or enrich the scene graph with more structural elements (background, headline, supporting_line, body, cta, logo, image, etc.).",
                        repairable=True,
                    )
                )

        if follow_up_mode == "variant_of_previous" and prior_layout_archetype and current_layout_archetype == prior_layout_archetype:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="repeated_layout_archetype",
                    message="Variant/regenerate request reused the prior layout archetype.",
                    expected_correction="Choose a materially different layout archetype from the previous creative.",
                    repairable=True,
                )
            )
        if (
            follow_up_mode == "new_content"
            and prompt_topic_keywords
            and not (prompt_topic_keywords & self._topic_anchor_keywords(scene_text_blob))
        ):
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="topic_anchor_missing",
                    message="The current copy and layout drift away from the user's requested topic.",
                    expected_correction="Keep the user's topic visible in the headline, supporting copy, proof points, and overall campaign framing.",
                    repairable=True,
                )
            )

        style_reference_without_geometry = (
            self._style_reference_only_carousel_active(request, creative_decision)
            and not self._request_has_authoritative_reference_zone_maps(request)
        )
        reference_family_match = (
            {"score": 1.0, "issues": [], "deferred_to_sample_page_blueprint": True}
            if style_reference_without_geometry
            else self._reference_family_closeness(
                scene_graph,
                compiled_context=compiled_context,
            )
        )
        if reference_family_match.get("issues"):
            validation_issue_map = {
                "reference_family_zone_drift": (
                    "The scene graph is drifting away from the approved reference-family zone grammar.",
                    "Re-anchor the headline, proof, CTA, and visual regions to the approved family roles instead of using a generic layout.",
                ),
                "reference_family_image_zone_drift": (
                    "The scene graph is not keeping imagery inside the approved reference-family image regions.",
                    "Move generated imagery back into the approved family image zones and keep text regions clean.",
                ),
                "reference_family_layout_drift": (
                    "The scene graph layout archetype is drifting away from the approved reference family.",
                    "Use the reference family's layout archetype and spacing rhythm instead of a generic poster structure.",
                ),
                "reference_family_module_drift": (
                    "The scene graph is missing too much of the approved reference-family module grammar.",
                    "Recompose the scene using the expected family modules such as hero split, proof grid, comparison, or CTA strip.",
                ),
                "reference_family_geometry_drift": (
                    "The scene graph geometry is drifting away from the approved reference-family region structure.",
                    "Bring the element boxes back toward the sample-driven zone map and preserve its spacing and negative-space rhythm.",
                ),
            }
            existing_rule_ids = {str(issue.rule_id or "").strip() for issue in issues}
            for issue_id in reference_family_match.get("issues", []):
                if issue_id in existing_rule_ids:
                    continue
                message, correction = validation_issue_map.get(
                    str(issue_id),
                    (
                        "The scene graph is drifting away from the approved reference-family contract.",
                        "Bring the scene graph back toward the approved sample-driven structure.",
                    ),
                )
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id=str(issue_id),
                        message=message,
                        expected_correction=correction,
                        repairable=True,
                    )
                )
                existing_rule_ids.add(str(issue_id))

        asset_strategy = creative_decision.asset_strategy or {}
        asset_flags = self._major_asset_strategy_flags(asset_strategy)
        if sum(1 for enabled in asset_flags.values() if enabled) >= 3:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="asset_strategy_overloaded",
                    message="Creative decision is mixing too many major visual systems in one social creative.",
                    expected_correction="Choose one dominant visual system and at most one supporting system.",
                    repairable=True,
                )
            )
        if creative_decision.layout_mode == "synthesized_layout" and asset_flags.get("template_background"):
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="asset_strategy_overloaded",
                    message="Synthesized layouts should not depend on a template background surface.",
                    expected_correction="Either switch to a template-led layout or remove template background usage.",
                    repairable=True,
                )
            )
        if asset_strategy.get("icon_sequence") and not any(element.role == "icon" for element in content_elements):
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="asset_strategy_unfulfilled",
                    message="Creative decision requested icons, but the scene graph does not include icon elements.",
                    expected_correction="Add icon elements or an explicit proof_points section supported by icons.",
                    repairable=True,
                )
            )
        if asset_strategy.get("background_element") and not any(
            element.role == "decorative_shape" or element.element_type in {"decorative_shape", "background_shape"}
            for element in content_elements
        ):
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="asset_strategy_unfulfilled",
                    message="Creative decision requested a background or decorative element, but the scene graph does not include one.",
                    expected_correction="Add decorative_shape or background_shape elements to carry the intended visual emphasis.",
                    repairable=True,
                )
            )
        icon_elements = [
            element
            for element in content_elements
            if element.role == "icon"
            and element.geometry.units == "normalized"
            and element.geometry.x is not None
            and element.geometry.y is not None
        ]
        if any(element.role == "image" for element in content_elements) and len(icon_elements) >= 3:
            issues.append(
                SceneGraphValidationIssue(
                    severity="warning",
                    rule_id="icon_overuse_with_hero_image",
                    message="Scene graph mixes a hero image with too many standalone icons, which reads as stamped rather than designed.",
                    expected_correction="Use either an image-led composition with restrained accents or integrate icons into structured proof cards.",
                    repairable=True,
                )
            )
        if len(icon_elements) >= 3:
            xs = [float(element.geometry.x or 0) for element in icon_elements]
            ys = sorted(float(element.geometry.y or 0) for element in icon_elements)
            if (max(xs) - min(xs)) <= 0.04 and (ys[-1] - ys[0]) >= 0.12:
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id="icon_stamp_column",
                        message="Icon elements are arranged as a simplistic vertical stamp column.",
                        expected_correction="Integrate icons into cards, proof rows, or a more composed visual rhythm.",
                        repairable=True,
                    )
                )

        for element in scene_graph.elements:
            geometry = element.geometry
            text_value = self._coerce_text_value(element.text)
            if text_value and self._contains_disallowed_glyphs(text_value):
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id="disallowed_text_glyphs",
                        element_id=element.element_id,
                        message=f"Element '{element.element_id}' contains emoji or symbol glyphs that are unsafe for clean brand rendering.",
                        expected_correction="Remove emoji-like glyphs and represent benefits with structured proof_points or icons instead.",
                        repairable=True,
                    )
                )
            if element.role in {"headline", "supporting_line", "body", "cta"} and self._looks_like_prompt_echo(text_value, request.prompt):
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id="prompt_echo_copy",
                        element_id=element.element_id,
                        message=f"Element '{element.element_id}' repeats the user's instruction instead of audience-facing copy.",
                        expected_correction="Rewrite the element into polished campaign language without repeating imperative prompt wording.",
                        repairable=True,
                    )
                )
            for style_key in ("fill", "primary_fill", "gradient_from", "gradient_to", "stroke", "background_fill"):
                style_value = str((element.style or {}).get(style_key) or "").strip()
                if (
                    style_value
                    and style_value.startswith("#")
                    and not self._style_color_is_palette_safe(style_value, allowed_palette)
                ):
                    issues.append(
                        SceneGraphValidationIssue(
                            severity="warning",
                            rule_id="color_palette_violation",
                            element_id=element.element_id,
                            message=f"Element '{element.element_id}' uses color {normalize_hex(style_value) or style_value} outside the validated palette.",
                            expected_correction="Use validated palette roles or approved neutral colors.",
                            repairable=True,
                        )
                    )
            font_family = str((element.style or {}).get("font_family") or "").strip()
            if font_family and allowed_fonts and font_family.casefold() not in allowed_fonts:
                issues.append(
                    SceneGraphValidationIssue(
                        severity="warning",
                        rule_id="font_usage_violation",
                        element_id=element.element_id,
                        message=f"Element '{element.element_id}' uses font '{font_family}' outside the approved brand fonts.",
                        expected_correction="Use one of the validated brand font families.",
                        repairable=True,
                    )
                )
            if geometry.units == "normalized":
                numeric_values = [geometry.x, geometry.y, geometry.width, geometry.height]
                if any(value is None for value in numeric_values):
                    issues.append(
                        SceneGraphValidationIssue(
                            severity="error",
                            rule_id="missing_geometry",
                            element_id=element.element_id,
                            message=f"Element '{element.element_id}' is missing normalized geometry.",
                            expected_correction="Provide x, y, width, and height for the element.",
                            repairable=True,
                        )
                    )
                    continue
                # Check if element extends outside normalized canvas bounds
                # Use 3% tolerance (1.03) to avoid false positives from floating point rounding
                x_end = float((geometry.x or 0) + (geometry.width or 0))
                y_end = float((geometry.y or 0) + (geometry.height or 0))
                if (
                    float(geometry.x or 0) < -0.01  # Allow small negative tolerance
                    or float(geometry.y or 0) < -0.01
                    or float(geometry.width or 0) <= 0
                    or float(geometry.height or 0) <= 0
                    or x_end > 1.03  # 3% tolerance for rounding
                    or y_end > 1.03
                ):
                    issues.append(
                        SceneGraphValidationIssue(
                            severity="warning",
                            rule_id="layout_bounds_violation",
                            element_id=element.element_id,
                            message=f"Element '{element.element_id}' extends outside the normalized canvas bounds (x: {geometry.x}, y: {geometry.y}, width: {geometry.width}, height: {geometry.height}, x_end: {x_end:.4f}, y_end: {y_end:.4f}).",
                            expected_correction="Keep all element geometry within the 0..1 normalized canvas.",
                            repairable=True,
                        )
                    )
            if element.asset:
                asset_keys = [element.asset.asset_id, element.asset.storage_path]
                trust = next((asset_trust.get(str(key)) for key in asset_keys if key and asset_trust.get(str(key))), None)
                if trust in {"excluded", "reference_only"}:
                    issues.append(
                        SceneGraphValidationIssue(
                            severity="warning",
                            rule_id="asset_trust_violation",
                            element_id=element.element_id,
                            message=f"Element '{element.element_id}' references an asset that is not approved for direct use.",
                            expected_correction="Replace the asset with a trusted or usable-with-warning asset.",
                            repairable=True,
                        )
                    )

        return SceneGraphValidationReport(
            status="clean" if not issues else "needs_repair",
            issues=issues,
            summary=[issue.message for issue in issues[:6]],
            repairable=all(issue.repairable for issue in issues),
        )

    @staticmethod
    def _needs_generated_image(scene_graph: GenerationSceneGraph, creative_decision: CreativeDecisionPayload) -> bool:
        # Centralizes needs generated image from scene graph and creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not creative_decision.asset_strategy.get("use_generated_image", True):
            return False
        return any(
            element.visible
            and element.role == "image"
            and ((not element.asset) or ((element.asset.asset_role or "").strip() in {"", "ai_image"}))
            for element in scene_graph.elements
        )

    def _needs_generated_image_with_storage(
        self,
        scene_graph: GenerationSceneGraph,
        creative_decision: CreativeDecisionPayload,
    ) -> bool:
        # Centralizes needs generated image storage from scene graph and creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not creative_decision.asset_strategy.get("use_generated_image", True):
            return False
        for element in scene_graph.elements:
            if not element.visible or element.role != "image":
                continue
            asset = element.asset
            if not asset:
                return True
            asset_role = str(asset.asset_role or "").strip().lower()
            storage_path = str(asset.storage_path or "").strip()
            if asset_role in {"", "ai_image"}:
                return True
            if storage_path and self.storage.exists(storage_path):
                continue
            return True
        return False

    @staticmethod
    def bind_generated_assets(
        scene_graph: GenerationSceneGraph,
        generated_assets: list[GeneratedImageAsset],
    ) -> GenerationSceneGraph:
        # Extracts bind generated assets from scene graph and generated assets for AIOrchestratorService.
        # Later planning can reuse the structured value instead of scanning the source again.
        if not generated_assets:
            return scene_graph
        scene_graph_data = scene_graph.model_dump(mode="json")
        asset_payload = generated_assets[0].model_dump(mode="json")
        existing_assets = list(scene_graph_data.get("assets") or [])
        existing_assets.append(
            {
                "asset_id": str(asset_payload["asset_id"]),
                "asset_role": asset_payload["asset_role"],
                "storage_path": asset_payload["storage_path"],
                "trust_level": "trusted",
            }
        )
        scene_graph_data["assets"] = existing_assets
        for element in scene_graph_data.get("elements", []):
            if element.get("role") != "image":
                continue
            asset = dict(element.get("asset") or {})
            asset.setdefault("asset_id", str(asset_payload["asset_id"]))
            asset["asset_role"] = asset_payload["asset_role"]
            asset["storage_path"] = asset_payload["storage_path"]
            asset["trust_level"] = "trusted"
            element["asset"] = asset
            break
        return GenerationSceneGraph.model_validate(scene_graph_data)

    def _generate_image_with_retries(
        self,
        *,
        image_provider,
        tenant_id,
        brand_space_id,
        prompt: str,
        size: str | None,
        trace_id: str | None,
        trace_label: str,
    ) -> dict[str, Any]:
        # Centralizes image retries from image provider, tenant id, and brand space id for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        last_error: Exception | None = None
        for attempt in range(1, max(int(self.settings.image_retry_attempts or 1), 1) + 1):
            try:
                payload = image_provider.generate(
                    tenant_id=tenant_id,
                    brand_space_id=brand_space_id,
                    prompt=prompt,
                    size=size,
                )
                if attempt > 1:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"{trace_label}_retry_{attempt:02d}",
                        {"attempt": attempt, "status": "success"},
                    )
                return payload
            except Exception as exc:  # noqa: BLE001
                last_error = exc
                self._trace_payload(
                    trace_id,
                    self.trace,
                    f"{trace_label}_retry_{attempt:02d}",
                    {
                        "attempt": attempt,
                        "status": "error",
                        "error": str(exc),
                        "non_retryable": self._is_non_retryable_image_error(exc),
                    },
                )
                if self._is_non_retryable_image_error(exc):
                    break
        assert last_error is not None
        raise last_error

    def _edit_image_with_retries(
        self,
        *,
        image_provider,
        tenant_id,
        brand_space_id,
        prompt: str,
        image_paths: list[str],
        size: str | None,
        mask_png_bytes: bytes | None,
        trace_id: str | None,
        trace_label: str,
    ) -> dict[str, Any]:
        # Edits edit image retries from image provider, tenant id, and brand space id for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        last_error: Exception | None = None
        for attempt in range(1, max(int(self.settings.image_retry_attempts or 1), 1) + 1):
            try:
                payload = image_provider.edit(
                    tenant_id=tenant_id,
                    brand_space_id=brand_space_id,
                    prompt=prompt,
                    image_paths=image_paths,
                    size=size,
                    mask_png_bytes=mask_png_bytes,
                )
                if attempt > 1:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"{trace_label}_retry_{attempt:02d}",
                        {"attempt": attempt, "status": "success"},
                    )
                return payload
            except Exception as exc:  # noqa: BLE001
                last_error = exc
                self._trace_payload(
                    trace_id,
                    self.trace,
                    f"{trace_label}_retry_{attempt:02d}",
                    {
                        "attempt": attempt,
                        "status": "error",
                        "error": str(exc),
                        "non_retryable": self._is_non_retryable_image_error(exc),
                    },
                )
                if self._is_non_retryable_image_error(exc):
                    break
        assert last_error is not None
        raise last_error

    @staticmethod
    def _requested_canvas_dimensions(studio_panel: dict[str, Any] | None) -> tuple[int, int]:
        # Measures requested canvas dimensions from studio settings for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        size = (studio_panel or {}).get("size") if isinstance(studio_panel, dict) else {}
        if not isinstance(size, dict):
            size = {}
        try:
            width = int(size.get("width") or 1080)
            height = int(size.get("height") or 1080)
        except (TypeError, ValueError):
            width, height = 1080, 1080
        return max(width, 1), max(height, 1)

    @classmethod
    def _final_render_subject_tokens(cls, value: Any) -> set[str]:
        # Centralizes final subject tokens from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        empty_subject_tokens = {"none", "null", "unknown", "unspecified"}
        return {
            token
            for token in cls._normalized_prompt_tokens(cls._coerce_text_value(value))
            if len(token) >= 3
            and any(ch.isalpha() for ch in token)
            and token not in cls.TOPIC_MATCH_NOISE_TOKENS
            and token not in empty_subject_tokens
            and not re.fullmatch(r"[a-f0-9]{8,}", token)
        }

    @classmethod
    def _final_render_request_topic_tokens(cls, request: AIOrchestrationRequest | None) -> set[str]:
        # Centralizes final request topic tokens from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None:
            return set()
        return cls._final_render_subject_tokens(getattr(request, "prompt", ""))

    @classmethod
    def _final_render_topic_alignment_report(
        cls,
        *,
        request: AIOrchestrationRequest,
        subject_blob: str,
    ) -> dict[str, Any]:
        # Centralizes final topic alignment report from request state and subject blob for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        request_tokens = cls._final_render_request_topic_tokens(request)
        subject_tokens = cls._final_render_subject_tokens(subject_blob)
        overlap = request_tokens & subject_tokens
        if not request_tokens:
            status = "unknown"
        elif not subject_tokens:
            status = "missing"
        elif cls._reference_tokens_align_with_request_topic(subject_tokens, request=request):
            status = "aligned"
        else:
            status = "weak"
        return {
            "status": status,
            "request_tokens": sorted(request_tokens)[:12],
            "subject_tokens": sorted(subject_tokens)[:16],
            "overlap": sorted(overlap)[:8],
            "missing_request_tokens": sorted(request_tokens - subject_tokens)[:8],
        }

    @classmethod
    def _final_render_visible_claim_hallucination_report(
        cls,
        *,
        request: AIOrchestrationRequest,
        ocr_structure: dict[str, Any],
        approved_text_payload: Any | None = None,
    ) -> dict[str, Any]:
        # Centralizes final visible claim hallucination report from request state, ocr structure, and approved text payload for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not isinstance(ocr_structure, dict) or not ocr_structure:
            return {}
        visible_parts: list[str] = []
        for key in ("headline_text", "full_text", "ocr_text", "detected_text", "recognized_text", "visible_text", "text_preview"):
            text = cls._normalize_metadata_text(ocr_structure.get(key), limit=360)
            if text:
                visible_parts.append(text)
        block_summary = ocr_structure.get("block_summary")
        if isinstance(block_summary, list):
            for block in block_summary[:12]:
                if not isinstance(block, dict):
                    continue
                text = cls._normalize_metadata_text(
                    block.get("text_excerpt") or block.get("text") or block.get("label"),
                    limit=220,
                )
                if text:
                    visible_parts.append(text)
        visible_text = " ".join(visible_parts).strip()
        if not visible_text:
            return {}

        allowed_parts = [cls._coerce_text_value(getattr(request, "prompt", ""))]
        allowed_parts.extend(cls._final_render_approved_text_claim_parts(approved_text_payload))
        brand_context = getattr(request, "resolved_brand_context", None)
        if isinstance(brand_context, dict):
            allowed_parts.append(cls._coerce_text_value(brand_context.get("brand_name")))
            guardrails = brand_context.get("guardrails") if isinstance(brand_context.get("guardrails"), dict) else {}
            allowed_parts.append(cls._coerce_text_value(guardrails.get("required_disclaimer")))
        retrieved_knowledge = getattr(request, "retrieved_knowledge", None)
        if isinstance(retrieved_knowledge, (dict, list)) and retrieved_knowledge:
            allowed_parts.append(json.dumps(retrieved_knowledge, ensure_ascii=True, default=str))
        allowed_blob = " ".join(part for part in allowed_parts if part).casefold()
        specific_claim_fragments = [
            match.group(0).strip()
            for match in re.finditer(
                r"(?<!\w)(?:\d+(?:\.\d+)?\s?%|(?:19|20)\d{2}|[$₹€£]\s?\d[\d,]*(?:\.\d+)?|#\s?\d+|rank\s+\d+|top\s+\d+|source\s*:|https?://\S+|www\.\S+)",
                visible_text,
                flags=re.IGNORECASE,
            )
            if match.group(0).strip()
        ]
        unsupported_specific_claims = [
            claim
            for claim in specific_claim_fragments
            if claim.casefold() not in allowed_blob
        ][:8]
        allowed_tokens = cls._final_render_subject_tokens(allowed_blob)
        factual_claim_markers = {
            "act",
            "acts",
            "bank",
            "banks",
            "bill",
            "bills",
            "commission",
            "commissions",
            "country",
            "countries",
            "court",
            "courts",
            "department",
            "departments",
            "exchange",
            "exchanges",
            "government",
            "governments",
            "law",
            "laws",
            "legal",
            "ministry",
            "ministries",
            "nation",
            "nations",
            "policy",
            "policies",
            "regulation",
            "regulations",
            "tariff",
            "tariffs",
            "tax",
            "taxes",
            "treaty",
            "treaties",
            "union",
            "unions",
        }
        high_risk_acronyms = {"BRICS", "EU", "FDI", "GDP", "GST", "RBI", "SEBI", "UAE", "UK", "UN", "US", "USA"}
        unsupported_named_claims: list[str] = []
        for visible_part in visible_parts:
            for match in re.finditer(
                r"\b(?:[A-Z]{2,}|[A-Z][a-z]+)(?:\s+(?:[A-Z]{2,}|[A-Z][a-z]+)){0,4}\b",
                visible_part,
            ):
                claim = match.group(0).strip()
                if not claim or claim.casefold() in allowed_blob:
                    continue
                claim_tokens = cls._final_render_subject_tokens(claim)
                if not claim_tokens or claim_tokens <= allowed_tokens:
                    continue
                raw_acronyms = {token for token in re.findall(r"\b[A-Z]{2,}\b", claim)}
                has_high_risk_acronym = bool(raw_acronyms & high_risk_acronyms)
                has_factual_marker = bool(claim_tokens & factual_claim_markers)
                if not has_high_risk_acronym and not has_factual_marker:
                    continue
                unsupported_named_claims.append(claim)
                if len(unsupported_named_claims) >= 8:
                    break
            if len(unsupported_named_claims) >= 8:
                break
        visible_tokens = cls._final_render_subject_tokens(visible_text)
        overlap_tokens = sorted(visible_tokens & allowed_tokens)
        unsupported_topic_tokens = sorted(visible_tokens - allowed_tokens)[:12]
        low_topic_overlap = (
            len(visible_tokens) >= 5
            and len(overlap_tokens) <= 1
            and len(unsupported_topic_tokens) >= 4
        )
        if unsupported_specific_claims:
            status = "unsupported_specificity"
        elif unsupported_named_claims:
            status = "unsupported_named_claim"
        elif low_topic_overlap:
            status = "topic_drift_risk"
        else:
            status = "ok"
        return {
            "status": status,
            "visible_text_excerpt": cls._normalize_metadata_text(visible_text, limit=420),
            "unsupported_specific_claims": unsupported_specific_claims,
            "unsupported_named_claims": unsupported_named_claims,
            "unsupported_topic_tokens": unsupported_topic_tokens,
            "overlap_tokens": overlap_tokens[:8],
        }

    @classmethod
    def _final_render_approved_text_claim_parts(cls, approved_text_payload: Any | None) -> list[str]:
        # Extracts final approved text claim parts from approved text payload for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        if approved_text_payload is None:
            return []

        parts: list[str] = []

        def add_value(value: Any) -> None:
            # Centralizes add from input value for AIOrchestratorService.
            # The main branch stays readable while this function handles the local edge case.
            if value is None:
                return
            if isinstance(value, str):
                text = cls._normalize_metadata_text(value, limit=700)
                if text:
                    parts.append(text)
                return
            if isinstance(value, (int, float, bool)):
                parts.append(str(value))
                return
            if isinstance(value, dict):
                for item in value.values():
                    add_value(item)
                return
            if isinstance(value, (list, tuple, set)):
                for item in value:
                    add_value(item)

        for attr in ("headline", "body", "cta", "hashtags"):
            add_value(getattr(approved_text_payload, attr, None))

        metadata = getattr(approved_text_payload, "metadata", None)
        if isinstance(metadata, dict):
            for key in (
                "claim_evidence_pairs",
                "data_points",
                "disclaimer",
                "infographic_section_specs",
                "legal_footer",
                "proof_points",
                "stat_highlights",
                "static_panel_spec",
                "supporting_line",
            ):
                add_value(metadata.get(key))

        return parts[:80]

    @classmethod
    def _final_render_topic_fit_contract(
        cls,
        *,
        request: AIOrchestrationRequest,
        format_name: str,
    ) -> str:
        # Builds final topic fit contract from request state and format name for AIOrchestratorService.
        # It calls _normalize_metadata_text and _explicit_top_n_item_count while assembling the payload or prompt text.
        if not cls._llm_led_static_infographic_format(format_name):
            return ""
        topic_anchors = sorted(cls._final_render_request_topic_tokens(request))[:10]
        prompt_text = cls._normalize_metadata_text(getattr(request, "prompt", ""), limit=420)
        requested_count = cls._explicit_top_n_item_count(prompt_text)
        structure_hints: list[str] = []
        lowered_prompt = prompt_text.casefold()
        if requested_count:
            structure_hints.append(f"include a visible {requested_count}-item comparison/ranking/list structure")
        if re.search(r"\b(compare|comparison|versus|vs\.?|ranking|ranked|top\s+\d{1,2})\b", lowered_prompt):
            structure_hints.append("use comparison/ranking hierarchy instead of a generic hero photo")
        if re.search(r"\b(infographic|chart|table|data|inflow|rate|rates|fee|fees|percentage|percent)\b", lowered_prompt):
            structure_hints.append("use structured modules, rows, cards, bars, or callouts that explain the requested data concept")
        structure_summary = "; ".join(structure_hints) or "choose a topic-specific explanatory structure that matches the prompt intent"
        anchors_summary = ", ".join(topic_anchors) if topic_anchors else "derive concrete topic anchors from the current user prompt"
        return (
            "Static/infographic topic-fit checklist: "
            f"current prompt='{prompt_text}'. "
            f"Strong topic anchors={anchors_summary}. "
            f"Required visual/content behavior: {structure_summary}. "
            "The finished image must visibly communicate these anchors through headline wording, module labels, visual metaphor, objects, icons, or chart-like surfaces. "
            "Reject generic finance decoration, generic people photos, stale template subjects, previous-request concepts, and unrelated macro/policy/trade scenes unless those concepts are explicitly present in the current prompt."
        )

    @classmethod
    def _final_render_requested_structure_report(
        cls,
        *,
        request: AIOrchestrationRequest,
        module_counts: dict[str, Any],
        visual_permissions: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes final requested structure report from request state, module counts, and visual permissions for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        prompt_text = cls._coerce_text_value(getattr(request, "prompt", ""))
        requested_count = cls._explicit_top_n_item_count(prompt_text)
        if not requested_count:
            return {}
        lowered_prompt = prompt_text.casefold()
        if not re.search(r"\b(top\s+\d{1,2}|rank(?:ed|ing)?|compare|comparison|versus|vs\.?)\b", lowered_prompt):
            return {}

        def count_value(*keys: str) -> int:
            # Counts count for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            values: list[int] = []
            for key in keys:
                try:
                    values.append(int(module_counts.get(key) or 0))
                except (TypeError, ValueError):
                    values.append(0)
            return max(values or [0])

        visible_item_modules = max(
            count_value("row_module_count", "horizontal_band_count"),
            count_value("card_module_count", "card_like_count"),
            count_value("metric_tile_count", "metric_tiles_count"),
            count_value("small_icon_like_count", "icon_count"),
            count_value("text_block_count"),
        )
        table_or_chart_count = max(
            count_value("table_count"),
            count_value("chart_count"),
        )
        table_or_chart_visible = table_or_chart_count > 0 and any(
            bool(visual_permissions.get(key))
            for key in ("table_allowed", "chart_or_graph_allowed", "metric_tiles_allowed")
        )
        required_modules = max(3, min(requested_count, 8))
        if table_or_chart_visible or visible_item_modules >= required_modules:
            status = "satisfied"
        else:
            status = "underbuilt"
        return {
            "status": status,
            "requested_count": requested_count,
            "required_visible_modules": required_modules,
            "visible_item_modules": visible_item_modules,
            "table_or_chart_visible": table_or_chart_visible,
            "table_or_chart_count": table_or_chart_count,
        }

    @classmethod
    def _final_render_data_surface_report(
        cls,
        *,
        request: AIOrchestrationRequest,
        format_name: str,
        module_counts: dict[str, Any],
        visual_permissions: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes final surface report from request state, format name, and module counts for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_format = str(format_name or "").strip().casefold()
        if normalized_format not in {"static", "infographic"}:
            return {}
        prompt_text = cls._coerce_text_value(getattr(request, "prompt", ""))
        if not cls._data_list_surface_intent(
            prompt=prompt_text,
            format_name=normalized_format,
        ):
            return {}
        lowered_prompt = prompt_text.casefold()

        def count_value(*keys: str) -> int:
            # Counts count for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            values: list[int] = []
            for key in keys:
                try:
                    values.append(int(module_counts.get(key) or 0))
                except (TypeError, ValueError):
                    values.append(0)
            return max(values or [0])

        row_modules = count_value("row_module_count", "horizontal_band_count")
        card_modules = count_value("card_module_count", "card_like_count")
        metric_modules = count_value("metric_tile_count", "metric_tiles_count")
        text_blocks = count_value("text_block_count")
        table_count = count_value("table_count")
        chart_count = count_value("chart_count")
        visible_structured_modules = max(row_modules, card_modules, metric_modules, text_blocks)
        table_or_chart_visible = max(table_count, chart_count) > 0 and any(
            bool(visual_permissions.get(key))
            for key in ("table_allowed", "chart_or_graph_allowed", "metric_tiles_allowed")
        )
        metric_surface_visible = metric_modules > 0 and bool(visual_permissions.get("metric_tiles_allowed"))
        explicit_chart_or_table_request = bool(
            re.search(r"\b(chart|graph|table|matrix|dashboard|scorecard)\b", lowered_prompt)
        )
        requested_count = cls._explicit_top_n_item_count(prompt_text)
        if requested_count:
            required_structured_modules = max(3, min(requested_count, 8))
        elif normalized_format == "infographic":
            required_structured_modules = 4
        else:
            required_structured_modules = 3
        if explicit_chart_or_table_request and not table_or_chart_visible:
            status = "missing_requested_chart_or_table"
        elif table_or_chart_visible or metric_surface_visible or visible_structured_modules >= required_structured_modules:
            status = "satisfied"
        else:
            status = "underbuilt"
        return {
            "status": status,
            "required_structured_modules": required_structured_modules,
            "visible_structured_modules": visible_structured_modules,
            "row_module_count": row_modules,
            "card_module_count": card_modules,
            "metric_tile_count": metric_modules,
            "text_block_count": text_blocks,
            "table_or_chart_visible": table_or_chart_visible,
            "metric_surface_visible": metric_surface_visible,
            "explicit_chart_or_table_request": explicit_chart_or_table_request,
        }

    @classmethod
    def _final_render_content_visual_balance_report(
        cls,
        *,
        request: AIOrchestrationRequest,
        format_name: str,
        module_counts: dict[str, Any],
        subject_semantics: dict[str, Any],
        ocr_structure: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes final content visual balance report from request state, format name, and module counts for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_format = str(format_name or "").strip().casefold()
        if normalized_format not in {"static", "infographic"}:
            return {}

        def count_value(*keys: str) -> int:
            # Counts count for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            values: list[int] = []
            for key in keys:
                try:
                    values.append(int(module_counts.get(key) or 0))
                except (TypeError, ValueError):
                    values.append(0)
            return max(values or [0])

        try:
            readable_blocks = int(ocr_structure.get("readable_text_blocks") or 0)
        except (TypeError, ValueError):
            readable_blocks = 0
        text_blocks = max(readable_blocks, count_value("text_block_count", "headline_block_count", "support_block_count"))
        icon_count = count_value("small_icon_like_count", "icon_count")
        large_visual_count = count_value("large_visual_count", "image_count")
        chart_count = count_value("chart_count", "graph_count")
        table_count = count_value("table_count")
        metric_tile_count = count_value("metric_tile_count", "metric_tiles_count")
        explanatory_visual_units = icon_count + large_visual_count + chart_count + table_count + metric_tile_count
        subject_blob = " ".join(
            [
                cls._coerce_text_value(subject_semantics.get("scene_type")),
                cls._coerce_text_value(subject_semantics.get("environment")),
                cls._coerce_text_value(subject_semantics.get("abstraction_level")),
                cls._compact_named_items(subject_semantics.get("primary_subjects"), limit=6),
                cls._compact_named_items(subject_semantics.get("domain_cues"), limit=6),
                cls._compact_named_items(subject_semantics.get("financial_objects"), limit=6),
            ]
        ).strip()
        topic_alignment = (
            cls._final_render_topic_alignment_report(request=request, subject_blob=subject_blob)
            if subject_blob
            else {"status": "missing", "overlap": []}
        )
        has_topic_visual_semantics = (
            topic_alignment.get("status") == "aligned"
            and bool(topic_alignment.get("overlap"))
        )
        if text_blocks <= 0 and explanatory_visual_units > 0:
            status = "image_only"
        elif text_blocks > 0 and explanatory_visual_units <= 0 and not has_topic_visual_semantics:
            status = "text_heavy"
        elif (
            normalized_format == "infographic"
            and text_blocks >= 4
            and explanatory_visual_units < 2
            and not has_topic_visual_semantics
        ):
            status = "visual_explanation_thin"
        else:
            status = "satisfied"
        return {
            "status": status,
            "text_block_count": text_blocks,
            "icon_count": icon_count,
            "large_visual_count": large_visual_count,
            "chart_count": chart_count,
            "table_count": table_count,
            "metric_tile_count": metric_tile_count,
            "explanatory_visual_units": explanatory_visual_units,
            "topic_visual_alignment": topic_alignment,
        }

    @classmethod
    def _final_render_visual_metaphor_report(
        cls,
        *,
        request: AIOrchestrationRequest,
        subject_blob: str,
        data_surface: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Centralizes final visual metaphor report from request state, subject blob, and data surface for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        prompt_text = cls._coerce_text_value(getattr(request, "prompt", ""))
        data_surface = data_surface if isinstance(data_surface, dict) else {}
        if not (
            data_surface
            or cls._data_list_surface_intent(
                prompt=prompt_text,
                format_name=str((getattr(request, "studio_panel", {}) or {}).get("format") or ""),
            )
        ):
            return {}
        request_tokens = cls._final_render_request_topic_tokens(request)
        subject_tokens = cls._final_render_subject_tokens(subject_blob)
        if not request_tokens or not subject_tokens:
            return {}
        weak_overlap_tokens = {
            "compare",
            "compared",
            "compares",
            "comparing",
            "comparison",
            "country",
            "countries",
            "global",
            "india",
            "indian",
            "rank",
            "ranking",
            "rankings",
            "rate",
            "rates",
            "static",
            "top",
            "world",
            "worldwide",
        }
        generic_visual_tokens = {
            "analytics",
            "arrow",
            "arrows",
            "bars",
            "business",
            "chart",
            "charts",
            "coin",
            "coins",
            "currency",
            "dashboard",
            "dashboards",
            "document",
            "documents",
            "finance",
            "financial",
            "graph",
            "graphs",
            "growth",
            "money",
            "office",
            "person",
            "portrait",
            "rupee",
            "rupees",
            "stock",
            "stocks",
            "symbol",
            "symbols",
        }
        request_domain_tokens = request_tokens - weak_overlap_tokens - generic_visual_tokens
        if not request_domain_tokens:
            return {}
        generic_subject_tokens = sorted((subject_tokens & generic_visual_tokens) - request_tokens)
        domain_overlap = sorted(request_domain_tokens & subject_tokens)
        missing_domain_tokens = sorted(request_domain_tokens - subject_tokens)
        required_overlap = min(2, len(request_domain_tokens))
        if generic_subject_tokens and len(domain_overlap) < required_overlap:
            status = "generic"
        elif generic_subject_tokens and missing_domain_tokens and len(domain_overlap) <= required_overlap:
            status = "weak"
        else:
            status = "specific"
        return {
            "status": status,
            "domain_overlap": domain_overlap[:8],
            "missing_domain_tokens": missing_domain_tokens[:8],
            "generic_subject_tokens": generic_subject_tokens[:8],
            "required_domain_overlap": required_overlap,
        }

    def _assess_final_render_output_image(
        self,
        *,
        request: AIOrchestrationRequest,
        asset: dict[str, Any],
        image_size: str | None,
        approved_text_payload: Any | None = None,
    ) -> dict[str, Any] | None:
        # Centralizes assess final output image from request state, asset record, and image size for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        format_name = str((request.studio_panel or {}).get("format") or "").strip().lower()
        if format_name not in {"static", "infographic"}:
            return None
        storage_path = str(asset.get("storage_path") or "").strip()
        if not storage_path:
            return {
                "score": 0.0,
                "issues": ["missing_output_storage_path"],
                "retry_recommended": False,
                "format": format_name,
                "requested_image_size": image_size,
            }
        if not self.storage.exists(storage_path):
            return {
                "score": 0.0,
                "issues": ["output_asset_not_found"],
                "retry_recommended": False,
                "format": format_name,
                "storage_path": storage_path,
                "requested_image_size": image_size,
            }
        try:
            with Image.open(self.storage.absolute_path(storage_path)) as raw:
                image = raw.convert("RGB")
                width, height = image.size
                sample = image.resize((64, 64), Image.Resampling.BILINEAR).convert("L")
                luminance_stddev = float(ImageStat.Stat(sample).stddev[0])
                edge_mean = float(ImageStat.Stat(sample.filter(ImageFilter.FIND_EDGES)).mean[0])
        except Exception as exc:  # noqa: BLE001
            return {
                "score": 0.0,
                "issues": ["output_asset_unreadable"],
                "retry_recommended": False,
                "format": format_name,
                "storage_path": storage_path,
                "error": str(exc),
                "requested_image_size": image_size,
            }

        target_width, target_height = self._requested_canvas_dimensions(request.studio_panel)
        generated_ratio = width / max(height, 1)
        target_ratio = target_width / max(target_height, 1)
        aspect_delta = abs(generated_ratio - target_ratio) / max(target_ratio, 0.01)
        main_text_expected = self._llm_led_static_infographic_format(format_name)
        score = 0.92
        issues: list[str] = []
        if min(width, height) < 768:
            score -= 0.2
            issues.append("low_resolution_output")
        if luminance_stddev < 2.0 or (luminance_stddev < 5.0 and edge_mean < 3.0):
            score -= 0.28
            issues.append("low_detail_or_blank_output")
        elif luminance_stddev < 8.0 and edge_mean < 4.5:
            score -= 0.12
            issues.append("weak_visual_detail")
        if aspect_delta > 0.28:
            score -= 0.1
            issues.append("generated_target_aspect_mismatch")
        vision_summary: dict[str, Any] = {}
        vision_retry_issues: set[str] = set()
        visible_claims: dict[str, Any] = {}
        content_visual_balance: dict[str, Any] = {}
        if (
            bool(getattr(self.settings, "final_render_output_vision_quality_enabled", True))
            and getattr(getattr(self, "template_vision", None), "client", None)
        ):
            base_blueprint = {
                "layout_category": "editorial_explainer" if format_name == "static" else "stacked_sections",
                "density": "balanced" if format_name == "static" else "dense",
                "module_counts": {},
                "visual_permissions": {"cta_allowed": False},
            }
            try:
                vision_blueprint = self._vision_enhance_page_blueprint(
                    image_path=self.storage.absolute_path(storage_path),
                    base_blueprint=base_blueprint,
                )
            except Exception:  # noqa: BLE001
                vision_blueprint = {}
            if isinstance(vision_blueprint, dict) and vision_blueprint.get("vision_analysis_status") == "openai_vision_enhanced":
                premium_quality = vision_blueprint.get("premium_quality") if isinstance(vision_blueprint.get("premium_quality"), dict) else {}
                ocr_structure = vision_blueprint.get("ocr_structure") if isinstance(vision_blueprint.get("ocr_structure"), dict) else {}
                visual_craft = vision_blueprint.get("visual_craft_dna") if isinstance(vision_blueprint.get("visual_craft_dna"), dict) else {}
                subject_semantics = vision_blueprint.get("subject_semantics") if isinstance(vision_blueprint.get("subject_semantics"), dict) else {}
                module_counts = vision_blueprint.get("module_counts") if isinstance(vision_blueprint.get("module_counts"), dict) else {}
                page_permissions = vision_blueprint.get("visual_permissions") if isinstance(vision_blueprint.get("visual_permissions"), dict) else {}

                def _quality_value(key: str) -> float | None:
                    # Checks quality from key for AIOrchestratorService.
                    # This keeps the allowed/blocked rule in one place.
                    raw = premium_quality.get(key)
                    try:
                        value = float(raw)
                    except (TypeError, ValueError):
                        return None
                    if value > 1.0:
                        value = value / 100.0
                    return max(0.0, min(value, 1.0))

                overall_quality = _quality_value("overall_score")
                craft_quality = _quality_value("craft_score")
                spacing_quality = _quality_value("spacing_score")
                if overall_quality is not None and overall_quality < 0.68:
                    score -= 0.16
                    issues.append("vision_low_premium_quality")
                    if overall_quality < 0.58:
                        vision_retry_issues.add("vision_low_premium_quality")
                if craft_quality is not None and craft_quality < 0.66:
                    score -= 0.08
                    issues.append("vision_weak_visual_craft")
                if spacing_quality is not None and spacing_quality < 0.62:
                    score -= 0.08
                    issues.append("vision_weak_spacing")
                try:
                    readable_blocks = int(ocr_structure.get("readable_text_blocks") or 0)
                except (TypeError, ValueError):
                    readable_blocks = 0
                if readable_blocks > 0 and not main_text_expected:
                    score -= min(0.3, 0.12 + (readable_blocks * 0.025))
                    issues.append("vision_readable_text_in_substrate")
                    vision_retry_issues.add("vision_readable_text_in_substrate")
                if bool(ocr_structure.get("logo_text_present")):
                    score -= 0.14
                    issues.append("vision_ai_generated_logo_text")
                    vision_retry_issues.add("vision_ai_generated_logo_text")
                collision_risk = self._normalize_metadata_text(ocr_structure.get("text_overlap_or_collision_risk"), limit=24).casefold()
                if collision_risk in {"medium", "high"}:
                    score -= 0.08 if collision_risk == "medium" else 0.14
                    issues.append("vision_text_collision_risk")
                    if collision_risk == "high":
                        vision_retry_issues.add("vision_text_collision_risk")
                truncation_risk = self._normalize_metadata_text(ocr_structure.get("text_truncation_or_crop_risk"), limit=24).casefold()
                if truncation_risk in {"medium", "high"}:
                    score -= 0.1 if truncation_risk == "medium" else 0.16
                    issues.append("vision_text_truncation_risk")
                    vision_retry_issues.add("vision_text_truncation_risk")

                subject_blob = " ".join(
                    [
                        self._coerce_text_value(subject_semantics.get("scene_type")),
                        self._coerce_text_value(subject_semantics.get("environment")),
                        self._coerce_text_value(subject_semantics.get("abstraction_level")),
                        self._compact_named_items(subject_semantics.get("primary_subjects"), limit=6),
                        self._compact_named_items(subject_semantics.get("domain_cues"), limit=6),
                        self._compact_named_items(subject_semantics.get("financial_objects"), limit=6),
                        json.dumps(subject_semantics, default=str),
                    ]
                )
                topic_alignment = self._final_render_topic_alignment_report(
                    request=request,
                    subject_blob=subject_blob,
                )
                if topic_alignment.get("status") in {"missing", "weak"}:
                    score -= 0.16 if topic_alignment.get("status") == "missing" else 0.18
                    issue_code = (
                        "vision_subject_relevance_missing"
                        if topic_alignment.get("status") == "missing"
                        else "vision_subject_relevance_weak"
                    )
                    issues.append(issue_code)
                    vision_retry_issues.add(issue_code)

                visible_claims = self._final_render_visible_claim_hallucination_report(
                    request=request,
                    ocr_structure=ocr_structure,
                    approved_text_payload=approved_text_payload,
                )
                if visible_claims.get("status") in {"unsupported_specificity", "unsupported_named_claim"}:
                    score -= 0.18
                    issues.append("vision_visible_claim_hallucination")
                    vision_retry_issues.add("vision_visible_claim_hallucination")
                elif (
                    visible_claims.get("status") == "topic_drift_risk"
                    and topic_alignment.get("status") in {"missing", "weak"}
                ):
                    score -= 0.14
                    issues.append("vision_visible_text_topic_drift")
                    vision_retry_issues.add("vision_visible_text_topic_drift")

                if format_name == "infographic":
                    structured_modules = sum(
                        int(module_counts.get(key) or 0)
                        for key in ("row_module_count", "card_like_count", "card_module_count", "horizontal_band_count", "text_block_count")
                    )
                    if structured_modules < 3 and not any(bool(page_permissions.get(key)) for key in ("chart_or_graph_allowed", "table_allowed", "metric_tiles_allowed")):
                        score -= 0.1
                        issues.append("vision_infographic_structure_weak")

                requested_structure = self._final_render_requested_structure_report(
                    request=request,
                    module_counts=module_counts,
                    visual_permissions=page_permissions,
                )
                if requested_structure.get("status") == "underbuilt":
                    score -= 0.14
                    issues.append("vision_requested_structure_underbuilt")
                    vision_retry_issues.add("vision_requested_structure_underbuilt")

                data_surface = self._final_render_data_surface_report(
                    request=request,
                    format_name=format_name,
                    module_counts=module_counts,
                    visual_permissions=page_permissions,
                )
                if data_surface.get("status") in {"underbuilt", "missing_requested_chart_or_table"}:
                    score -= 0.12
                    issues.append("vision_data_surface_underbuilt")
                    vision_retry_issues.add("vision_data_surface_underbuilt")

                content_visual_balance = self._final_render_content_visual_balance_report(
                    request=request,
                    format_name=format_name,
                    module_counts=module_counts,
                    subject_semantics=subject_semantics,
                    ocr_structure=ocr_structure,
                )
                if content_visual_balance.get("status") in {"text_heavy", "image_only"}:
                    score -= 0.14
                    issues.append("vision_content_visual_balance_weak")
                    vision_retry_issues.add("vision_content_visual_balance_weak")
                elif content_visual_balance.get("status") == "visual_explanation_thin":
                    score -= 0.08
                    issues.append("vision_content_visual_balance_thin")

                visual_metaphor = self._final_render_visual_metaphor_report(
                    request=request,
                    subject_blob=subject_blob,
                    data_surface=data_surface,
                )
                if visual_metaphor.get("status") in {"generic", "weak"}:
                    score -= 0.12
                    issues.append("vision_visual_metaphor_generic")
                    vision_retry_issues.add("vision_visual_metaphor_generic")

                vision_summary = {
                    "status": "openai_vision_enhanced",
                    "provider_usage": vision_blueprint.get("provider_usage")
                    if isinstance(vision_blueprint.get("provider_usage"), dict)
                    else None,
                    "vision_cache": vision_blueprint.get("vision_cache")
                    if isinstance(vision_blueprint.get("vision_cache"), dict)
                    else None,
                    "premium_quality": premium_quality,
                    "ocr_structure": {
                        "readable_text_blocks": readable_blocks,
                        "text_overlap_or_collision_risk": collision_risk or None,
                        "text_truncation_or_crop_risk": truncation_risk or None,
                        "logo_text_present": bool(ocr_structure.get("logo_text_present")),
                    },
                    "visual_craft_dna": visual_craft,
                    "subject_semantics": subject_semantics,
                    "topic_alignment": topic_alignment,
                    "visible_claims": visible_claims or None,
                    "requested_structure": requested_structure or None,
                    "data_surface": data_surface or None,
                    "content_visual_balance": content_visual_balance or None,
                    "visual_metaphor": visual_metaphor or None,
                    "module_counts": module_counts,
                    "visual_permissions": page_permissions,
                }
        retry_recommended = any(
            issue in {"low_resolution_output", "low_detail_or_blank_output"}
            for issue in issues
        ) or bool(vision_retry_issues)
        return {
            "score": max(0.0, round(score, 2)),
            "issues": issues,
            "hard_retry_issues": sorted(vision_retry_issues),
            "retry_recommended": retry_recommended,
            "format": format_name,
            "generated_size": {"width": width, "height": height},
            "target_size": {"width": target_width, "height": target_height},
            "requested_image_size": image_size,
            "aspect_delta": round(aspect_delta, 4),
            "luminance_stddev": round(luminance_stddev, 3),
            "edge_mean": round(edge_mean, 3),
            "vision_quality": vision_summary or None,
        }

    @classmethod
    def _append_final_render_output_quality_repair_prompt(
        cls,
        prompt: str,
        *,
        quality_report: dict[str, Any],
        request: AIOrchestrationRequest | None = None,
    ) -> str:
        # Builds append final output quality prompt from prompt text, quality report, and request state for AIOrchestratorService.
        # It calls _strip_leading_final_render_output_quality_repair_prompt and _llm_led_static_infographic_format while assembling the payload or promp.
        prompt = cls._strip_leading_final_render_output_quality_repair_prompt(prompt)
        format_name = cls._format_name_from_request(request) if request is not None else ""
        llm_led_static_infographic = cls._llm_led_static_infographic_format(format_name)
        current_prompt = cls._normalize_metadata_text(
            getattr(request, "prompt", "") if request is not None else "",
            limit=360,
        )
        topic_anchors = sorted(cls._final_render_request_topic_tokens(request))[:10] if request is not None else []
        vision_quality = quality_report.get("vision_quality") if isinstance(quality_report.get("vision_quality"), dict) else {}
        topic_alignment = (
            vision_quality.get("topic_alignment")
            if isinstance(vision_quality.get("topic_alignment"), dict)
            else {}
        )
        requested_structure = (
            vision_quality.get("requested_structure")
            if isinstance(vision_quality.get("requested_structure"), dict)
            else {}
        )
        data_surface = (
            vision_quality.get("data_surface")
            if isinstance(vision_quality.get("data_surface"), dict)
            else {}
        )
        visual_metaphor = (
            vision_quality.get("visual_metaphor")
            if isinstance(vision_quality.get("visual_metaphor"), dict)
            else {}
        )
        visible_claims = (
            vision_quality.get("visible_claims")
            if isinstance(vision_quality.get("visible_claims"), dict)
            else {}
        )
        ocr_structure = (
            vision_quality.get("ocr_structure")
            if isinstance(vision_quality.get("ocr_structure"), dict)
            else {}
        )
        content_visual_balance = (
            vision_quality.get("content_visual_balance")
            if isinstance(vision_quality.get("content_visual_balance"), dict)
            else {}
        )
        premium_quality = (
            vision_quality.get("premium_quality")
            if isinstance(vision_quality.get("premium_quality"), dict)
            else {}
        )
        quality_floor_contract = cls._final_render_static_infographic_quality_floor_contract(
            request=request,
            format_name=format_name,
        )
        quality_issues = {
            str(issue)
            for issue in (quality_report.get("issues") or [])
            if str(issue or "").strip()
        }
        structure_repair_instruction = ""
        if (
            llm_led_static_infographic
            and (
                "vision_requested_structure_underbuilt" in quality_issues
                or requested_structure.get("status") == "underbuilt"
            )
        ):
            requested_count = cls._int_or_none(requested_structure.get("requested_count"))
            required_modules = cls._int_or_none(requested_structure.get("required_visible_modules"))
            visible_modules = cls._int_or_none(requested_structure.get("visible_item_modules"))
            if requested_count and required_modules:
                structure_requirement = (
                    f"Build at least {required_modules} distinct visible item modules for the requested "
                    f"{requested_count}-item comparison/ranking/list"
                )
            elif required_modules:
                structure_requirement = f"Build at least {required_modules} distinct visible comparison/ranking/list modules"
            else:
                structure_requirement = "Build distinct visible comparison/ranking/list modules"
            if visible_modules is not None:
                previous_attempt_note = (
                    f" The previous attempt had about {visible_modules} visible item module(s), which was underbuilt."
                )
            else:
                previous_attempt_note = ""
            structure_repair_instruction = (
                f" {structure_requirement}, or use one clearly readable real chart/table surface that visibly carries "
                f"the requested comparison.{previous_attempt_note}"
            )
        data_surface_repair_instruction = ""
        if (
            llm_led_static_infographic
            and (
                "vision_data_surface_underbuilt" in quality_issues
                or data_surface.get("status") in {"underbuilt", "missing_requested_chart_or_table"}
            )
        ):
            required_modules = cls._int_or_none(data_surface.get("required_structured_modules"))
            visible_modules = cls._int_or_none(data_surface.get("visible_structured_modules"))
            if data_surface.get("status") == "missing_requested_chart_or_table":
                data_requirement = "Include the requested readable chart/table/matrix-style data surface"
            elif required_modules:
                data_requirement = (
                    f"Use a data-first layout with at least {required_modules} visible rows, cards, metric tiles, "
                    "bars, or grouped comparison modules"
                )
            else:
                data_requirement = "Use a data-first layout with visible rows, cards, metric tiles, bars, or grouped comparison modules"
            if visible_modules is not None:
                data_attempt_note = f" The previous attempt showed about {visible_modules} structured module(s)."
            else:
                data_attempt_note = ""
            data_surface_repair_instruction = (
                f" {data_requirement}; keep decorative imagery secondary to the requested data/comparison surface."
                f"{data_attempt_note}"
            )
        visual_metaphor_repair_instruction = ""
        if (
            llm_led_static_infographic
            and (
                "vision_visual_metaphor_generic" in quality_issues
                or visual_metaphor.get("status") in {"generic", "weak"}
            )
        ):
            missing_tokens = [
                str(token).strip()
                for token in (visual_metaphor.get("missing_domain_tokens") or [])
                if str(token).strip()
            ][:6]
            generic_tokens = [
                str(token).strip()
                for token in (visual_metaphor.get("generic_subject_tokens") or [])
                if str(token).strip()
            ][:6]
            missing_summary = ", ".join(missing_tokens) if missing_tokens else "the strongest current-prompt domain anchors"
            generic_summary = ", ".join(generic_tokens) if generic_tokens else "generic finance decoration"
            visual_metaphor_repair_instruction = (
                f" Replace generic visual subjects ({generic_summary}) with prompt-specific visual anchors tied to: "
                f"{missing_summary}. Use objects, icons, modules, chart labels, and scene logic that make those anchors visible."
            )
        content_visual_repair_instruction = ""
        if (
            llm_led_static_infographic
            and (
                "vision_content_visual_balance_weak" in quality_issues
                or "vision_content_visual_balance_thin" in quality_issues
                or content_visual_balance.get("status") in {"text_heavy", "image_only", "visual_explanation_thin"}
            )
        ):
            content_visual_repair_instruction = (
                " Rebalance the creative so content and imagery explain the same topic together: pair concise readable text "
                "with a topic-specific hero visual, icon set, mini chart, diagram, comparison modules, or visual metaphor. "
                "Avoid text-only poster layouts and avoid image-only scenes with weak token copy."
            )
        craft_repair_instruction = ""
        if (
            "low_detail_or_blank_output" in quality_issues
            or "weak_visual_detail" in quality_issues
            or "vision_low_premium_quality" in quality_issues
            or "vision_weak_visual_craft" in quality_issues
            or "vision_weak_spacing" in quality_issues
        ):
            craft_repair_instruction = (
                " Raise the craft level before changing the message: add clearer visual hierarchy, cleaner module surfaces, "
                "more deliberate spacing, stronger contrast, crisp edges, and a coherent foreground/midground/background structure. "
                "The next image must visibly satisfy the minimum output quality floor."
            )
        text_fit_repair_instruction = ""
        if (
            "vision_text_truncation_risk" in quality_issues
            or cls._normalize_metadata_text(ocr_structure.get("text_truncation_or_crop_risk"), limit=24).casefold() in {"medium", "high"}
        ):
            text_fit_repair_instruction = (
                " Fix text fit before improving decoration: every headline, label, row, number, CTA, and proof line must be fully visible inside its card/module/canvas boundaries with padding. "
                "No cropped letters, half words, fading edge text, hidden rows, or text running under the logo/footer safe zones. Shorten, wrap, reduce modules, or enlarge text containers when needed."
            )
        if llm_led_static_infographic:
            instruction = (
                "Regenerate the finished static/infographic creative, not a blank substrate. Keep the main headline, "
                "modules, labels, comparison/ranking content, and CTA as readable finished image text. Improve visual craft, "
                "spacing, hierarchy, and target-aspect fit while keeping the logo-safe corner and footer/disclaimer strip empty. "
                "Every visual metaphor, icon, chart-like module, object, scene, and headline must directly match the current user prompt. "
                "Remove stale template subjects, previous-request topics, generic portraits, unrelated policy/trade themes, placeholder rows, "
                "unsupported visible words, numbers, source labels, logos, brand-like marks, fake values, and off-topic decorative objects. "
                f"Do not replace missing facts with generic fallback imagery or model-memory claims. {quality_floor_contract}"
                f"{craft_repair_instruction}{text_fit_repair_instruction}{structure_repair_instruction}{data_surface_repair_instruction}{visual_metaphor_repair_instruction}{content_visual_repair_instruction}"
            )
        else:
            instruction = (
                "Regenerate the premium text-safe substrate with richer visual craft, clearer module surfaces, "
                "stronger topic-specific focal detail, and stable centered target-aspect composition. "
                "Do not add readable text, pseudo-text, logos, watermarks, or placeholder lettering."
            )
        repair_payload = {
            "post_generation_output_quality_repair": {
                "issues": quality_report.get("issues", []),
                "score": quality_report.get("score"),
                "target_size": quality_report.get("target_size"),
                "generated_size": quality_report.get("generated_size"),
                "current_user_prompt": current_prompt,
                "topic_anchors": topic_anchors,
                "topic_alignment": topic_alignment,
                "visible_claims": visible_claims,
                "ocr_structure": ocr_structure,
                "content_visual_balance": content_visual_balance,
                "premium_quality": premium_quality,
                "quality_floor": quality_floor_contract,
                "requested_structure": requested_structure,
                "data_surface": data_surface,
                "visual_metaphor": visual_metaphor,
                "instruction": instruction,
            }
        }
        return cls._trim_prompt(
            f"{json.dumps(repair_payload, ensure_ascii=True)} {prompt}",
            cls.IMAGE_PROMPT_MAX_LENGTH,
        )

    @classmethod
    def _strip_leading_final_render_output_quality_repair_prompt(cls, prompt: str) -> str:
        # Strips strip leading final output quality prompt from prompt text for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = str(prompt or "")
        stripped = text.lstrip()
        leading_whitespace = len(text) - len(stripped)
        if not stripped.startswith("{"):
            return text
        try:
            parsed, end_index = json.JSONDecoder().raw_decode(stripped)
        except json.JSONDecodeError:
            return text
        if not isinstance(parsed, dict) or "post_generation_output_quality_repair" not in parsed:
            return text
        return text[leading_whitespace + end_index :].lstrip()

    @classmethod
    def _final_render_output_quality_blocking_issues(cls, report: dict[str, Any] | None) -> set[str]:
        # Checks final output quality blocking issues from report for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        if not isinstance(report, dict):
            return set()
        issues = {
            cls._normalize_metadata_text(issue, limit=80)
            for issue in (report.get("issues") or [])
            if cls._normalize_metadata_text(issue, limit=80)
        }
        hard_issues = {
            cls._normalize_metadata_text(issue, limit=80)
            for issue in (report.get("hard_retry_issues") or [])
            if cls._normalize_metadata_text(issue, limit=80)
        }
        blocking_base_issues = {
            "low_resolution_output",
            "low_detail_or_blank_output",
        }
        return hard_issues | (issues & blocking_base_issues)

    @classmethod
    def _mark_final_render_output_quality_visible_with_warning(
        cls,
        asset: dict[str, Any],
        *,
        report: dict[str, Any],
        retry_attempts: int,
    ) -> dict[str, Any]:
        # Checks mark final output quality visible warning from asset record, report, and retry attempts for AIOrchestratorService.
        # It reuses _final_render_output_quality_blocking_issues so related checks follow the same rule.
        blocking_issues = sorted(cls._final_render_output_quality_blocking_issues(report))
        asset["output_quality_assessment"] = report
        asset["output_quality_retry_attempts"] = retry_attempts
        asset["output_quality_status"] = "visible_with_warnings"
        asset["output_quality_blocking_issues"] = blocking_issues
        asset["output_quality_user_warning"] = (
            "The generated image is visible, but it did not meet the final static/infographic quality bar."
        )
        return asset

    def _generate_final_render_image_with_sample_guard(
        self,
        *,
        image_provider,
        request: AIOrchestrationRequest,
        prompt: str,
        reference_image_paths: list[str],
        image_size: str | None,
        trace_id: str | None,
        trace_suffix: str,
        sample_blueprint: dict[str, Any] | None,
        slide_index: int,
        slide_count: int,
        approved_text_payload: Any | None = None,
        exact_logo_overlay_deferred: bool = False,
        legal_footer_overlay_deferred: bool = False,
        sample_reference_image_paths: list[str] | None = None,
    ) -> tuple[dict[str, Any], dict[str, Any] | None, int, str]:
        # Centralizes final image sample guard from image provider, request state, and prompt text for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        current_prompt = prompt
        similarity_report: dict[str, Any] | None = None
        similarity_retry_attempts = 0
        static_infographic_no_retry = self._llm_led_static_infographic_format(self._format_name_from_request(request))
        configured_similarity_retries = max(
            int(getattr(self.settings, "ai_final_render_carousel_sample_similarity_retries", 0) or 0),
            0,
        )
        max_similarity_retries = 0 if static_infographic_no_retry else configured_similarity_retries
        output_quality_retry_attempts = 0
        configured_output_quality_retries = max(int(self.settings.image_quality_retry_attempts or 1), 0)
        max_output_quality_retries = 0 if static_infographic_no_retry else configured_output_quality_retries
        max_generation_attempts = max(max_similarity_retries, max_output_quality_retries) + 1
        best_asset: dict[str, Any] | None = None
        best_similarity_report: dict[str, Any] | None = None
        best_prompt = current_prompt
        best_score = -1.0
        stable_sample_blueprint = sample_blueprint
        sample_reference_paths = [
            str(path)
            for path in (sample_reference_image_paths or reference_image_paths or [])
            if str(path or "").strip()
        ]
        if sample_reference_paths and isinstance(sample_blueprint, dict) and sample_blueprint:
            stable_sample_blueprint = (
                sample_blueprint
                if sample_blueprint.get("vision_analysis_status") == "openai_vision_enhanced"
                else self._vision_enhance_page_blueprint(
                    image_path=sample_reference_paths[0],
                    base_blueprint=sample_blueprint,
                    )
            )
        sample_similarity_active = bool(
            sample_reference_paths
            and isinstance(stable_sample_blueprint, dict)
            and stable_sample_blueprint
        )
        for attempt in range(1, max_generation_attempts + 1):
            attempt_started_at = perf_counter()
            trace_label = (
                f"final_render_conditioned_generation{trace_suffix}"
                if reference_image_paths and attempt == 1
                else f"final_render_sample_similarity_repair_generation{trace_suffix}_attempt_{attempt:02d}"
                if reference_image_paths
                else f"final_render_generation{trace_suffix}"
                if attempt == 1
                else f"final_render_sample_similarity_repair_generation{trace_suffix}_attempt_{attempt:02d}"
            )
            image_call_started_at = perf_counter()
            if reference_image_paths:
                asset = self._edit_image_with_retries(
                    image_provider=image_provider,
                    tenant_id=request.tenant_id,
                    brand_space_id=request.brand_space_id,
                    prompt=current_prompt,
                    image_paths=reference_image_paths,
                    size=image_size,
                    mask_png_bytes=None,
                    trace_id=trace_id,
                    trace_label=trace_label,
                )
            else:
                asset = self._generate_image_with_retries(
                    image_provider=image_provider,
                    tenant_id=request.tenant_id,
                    brand_space_id=request.brand_space_id,
                    prompt=current_prompt,
                    size=image_size,
                    trace_id=trace_id,
                    trace_label=trace_label,
                )
            image_call_ms = round((perf_counter() - image_call_started_at) * 1000, 2)
            output_quality_started_at = perf_counter()
            output_quality_report = self._assess_final_render_output_image(
                request=request,
                asset=asset,
                image_size=image_size,
                approved_text_payload=approved_text_payload,
            )
            output_quality_ms = round((perf_counter() - output_quality_started_at) * 1000, 2)
            if output_quality_report:
                asset["output_quality_assessment"] = output_quality_report
                asset["output_quality_retry_attempts"] = output_quality_retry_attempts
                try:
                    output_quality_score = float(output_quality_report.get("score") or 0.0)
                except (TypeError, ValueError):
                    output_quality_score = 0.0
                output_quality_blocking = self._final_render_output_quality_blocking_issues(output_quality_report)
                best_quality_report = (
                    best_asset.get("output_quality_assessment")
                    if isinstance(best_asset, dict) and isinstance(best_asset.get("output_quality_assessment"), dict)
                    else None
                )
                best_quality_blocking = self._final_render_output_quality_blocking_issues(best_quality_report)
                if (
                    not sample_similarity_active
                    and (
                        best_asset is None
                        or (best_quality_blocking and not output_quality_blocking)
                        or (bool(best_quality_blocking) == bool(output_quality_blocking) and output_quality_score > best_score)
                    )
                ):
                    best_asset = asset
                    best_similarity_report = similarity_report
                    best_prompt = current_prompt
                    best_score = output_quality_score
                self._trace_payload(
                    trace_id,
                    self.trace,
                    f"final_render_output_quality{trace_suffix}_attempt_{attempt:02d}",
                    {
                        "attempt": attempt,
                        "asset_storage_path": str(asset.get("storage_path") or "").strip(),
                        "report": output_quality_report,
                        "slide_index": slide_index,
                        "slide_count": slide_count,
                        "timing_ms": {
                            "image_provider_call_ms": image_call_ms,
                            "output_quality_assessment_ms": output_quality_ms,
                            "attempt_total_ms": round((perf_counter() - attempt_started_at) * 1000, 2),
                        },
                    },
                )
                if (
                    bool(output_quality_report.get("retry_recommended"))
                    and output_quality_retry_attempts < max_output_quality_retries
                ):
                    output_quality_retry_attempts += 1
                    current_prompt = self._append_final_render_output_quality_repair_prompt(
                        current_prompt,
                        quality_report=output_quality_report,
                        request=request,
                    )
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"final_render_output_quality_repair_prompt{trace_suffix}_attempt_{attempt + 1:02d}",
                        {
                            "prompt": current_prompt,
                            **self._prompt_length_metrics(current_prompt),
                            "previous_quality_report": output_quality_report,
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                        },
                    )
                    continue
                if output_quality_blocking and sample_similarity_active:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"final_render_output_quality_visible_with_warnings{trace_suffix}",
                        {
                            "blocking_issues": sorted(output_quality_blocking),
                            "quality_report": output_quality_report,
                            "output_quality_retry_attempts": output_quality_retry_attempts,
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                        },
                    )
                    return self._mark_final_render_output_quality_visible_with_warning(
                        asset,
                        report=output_quality_report,
                        retry_attempts=output_quality_retry_attempts,
                    ), similarity_report, similarity_retry_attempts, current_prompt
            if not sample_similarity_active:
                selected_asset = best_asset or asset
                selected_asset["output_quality_retry_attempts"] = output_quality_retry_attempts
                selected_quality_report = (
                    selected_asset.get("output_quality_assessment")
                    if isinstance(selected_asset.get("output_quality_assessment"), dict)
                    else output_quality_report
                    if isinstance(output_quality_report, dict)
                    else {}
                )
                blocking_issues = self._final_render_output_quality_blocking_issues(selected_quality_report)
                if blocking_issues:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"final_render_output_quality_visible_with_warnings{trace_suffix}",
                        {
                            "blocking_issues": sorted(blocking_issues),
                            "quality_report": selected_quality_report,
                            "output_quality_retry_attempts": output_quality_retry_attempts,
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                        },
                    )
                    selected_asset = self._mark_final_render_output_quality_visible_with_warning(
                        selected_asset,
                        report=selected_quality_report,
                        retry_attempts=output_quality_retry_attempts,
                    )
                return selected_asset, similarity_report, similarity_retry_attempts, best_prompt or current_prompt
            output_storage_path = str(asset.get("storage_path") or "").strip()
            if not output_storage_path or not self.storage.exists(output_storage_path):
                return asset, similarity_report, similarity_retry_attempts, current_prompt
            similarity_started_at = perf_counter()
            similarity_report = self._sample_output_similarity_from_paths_with_vision(
                sample_image_path=sample_reference_paths[0],
                output_image_path=self.storage.absolute_path(output_storage_path),
                sample_blueprint=stable_sample_blueprint,
                slide_index=slide_index,
                slide_count=slide_count,
                exact_logo_overlay_deferred=exact_logo_overlay_deferred,
                legal_footer_overlay_deferred=legal_footer_overlay_deferred,
            )
            try:
                similarity_score = float(similarity_report.get("score") or 0.0)
            except (TypeError, ValueError):
                similarity_score = 0.0
            if best_asset is None or similarity_score > best_score:
                best_asset = asset
                best_similarity_report = similarity_report
                best_prompt = current_prompt
                best_score = similarity_score
            accepted = not bool(similarity_report.get("retry_recommended"))
            self._trace_payload(
                trace_id,
                self.trace,
                f"final_render_sample_similarity{trace_suffix}_attempt_{attempt:02d}",
                {
                    "attempt": attempt,
                    "accepted": accepted,
                    "asset_storage_path": output_storage_path,
                    "report": similarity_report,
                    "slide_index": slide_index,
                    "slide_count": slide_count,
                    "timing_ms": {
                        "image_provider_call_ms": image_call_ms,
                        "output_quality_assessment_ms": output_quality_ms,
                        "sample_similarity_assessment_ms": round((perf_counter() - similarity_started_at) * 1000, 2),
                        "attempt_total_ms": round((perf_counter() - attempt_started_at) * 1000, 2),
                    },
                },
            )
            if accepted:
                return asset, similarity_report, similarity_retry_attempts, current_prompt
            if attempt > max_similarity_retries:
                selected_asset = best_asset or asset
                selected_report = best_similarity_report or similarity_report
                selected_prompt = best_prompt or current_prompt
                if selected_asset is not asset:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"final_render_sample_similarity_best_attempt{trace_suffix}",
                        {
                            "selected_score": best_score,
                            "discarded_score": similarity_score,
                            "selected_asset_storage_path": str(selected_asset.get("storage_path") or "").strip(),
                            "discarded_asset_storage_path": output_storage_path,
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                        },
                    )
                return selected_asset, selected_report, similarity_retry_attempts, selected_prompt
            similarity_retry_attempts += 1
            current_prompt = self._append_sample_similarity_repair_prompt(
                current_prompt,
                similarity_report=similarity_report,
            )
            self._trace_payload(
                trace_id,
                self.trace,
                f"final_render_sample_similarity_repair_prompt{trace_suffix}_attempt_{attempt + 1:02d}",
                {
                    "prompt": current_prompt,
                    **self._prompt_length_metrics(current_prompt),
                    "previous_similarity_report": similarity_report,
                    "slide_index": slide_index,
                    "slide_count": slide_count,
                },
            )
        return best_asset or asset, best_similarity_report or similarity_report, similarity_retry_attempts, best_prompt

    def render_final_assets_only(
        self,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        creative_decision: CreativeDecisionPayload,
        scene_graph: GenerationSceneGraph,
        message_strategy: MessageStrategyPayload | None = None,
        validation_report: SceneGraphValidationReport | None = None,
        generation_path: str | None = None,
        selected_reference_images: list[dict[str, Any]] | None = None,
        conditioning_reference_images: list[dict[str, Any]] | None = None,
        compiled_context: dict[str, Any] | None = None,
        quality_assessment: dict[str, Any] | None = None,
        quality_retry_attempts: int = 0,
        fresh_replan_attempted: bool = False,
    ) -> tuple[list[GeneratedImageAsset], GeneratedImageAsset | None, StructuredTextPayload, GenerationSceneGraph]:
        # Extracts final assets only from request state, copy payload, and creative decision for AIOrchestratorService.
        # It calls _should_ignore_scene_graph_for_final_render and _visual_explanation_plan to turn raw evidence into the structured signal the caller n.
        trace_id = request.generation_trace_id
        image_provider = self.providers.get_image_provider()
        generation_path = str(generation_path or "image_led_social").strip().lower() or "image_led_social"
        message_strategy = message_strategy or MessageStrategyPayload()
        validation_report = validation_report or SceneGraphValidationReport()
        compiled_context = dict(compiled_context or {})
        quality_assessment = dict(quality_assessment or {})
        selected_reference_images = [dict(asset) for asset in (selected_reference_images or []) if isinstance(asset, dict)]
        if not selected_reference_images:
            selected_reference_images = self._select_reference_image_assets(
                request=request,
                creative_decision=creative_decision,
            )
            selected_reference_images, _missing_reference_images = self._filter_available_reference_image_assets(
                selected_reference_images,
            )
        conditioning_reference_images = [dict(asset) for asset in (conditioning_reference_images or []) if isinstance(asset, dict)]
        if not conditioning_reference_images:
            conditioning_reference_images = self._conditioning_reference_image_assets(
                selected_reference_images,
                creative_decision=creative_decision,
                request=request,
            )
        final_render_format_name = str(request.studio_panel.get("format") or "").strip().lower()
        is_carousel_render = final_render_format_name == "carousel"
        carousel_slide_specs = (
            self._build_carousel_slide_specs(
                text_payload,
                request=request,
                creative_decision=creative_decision,
            )
            if is_carousel_render
            else []
        )
        if carousel_slide_specs:
            text_payload = text_payload.model_copy(
                update={
                    "metadata": {
                        **(text_payload.metadata or {}),
                        "carousel_slide_specs": carousel_slide_specs,
                    }
                }
            )

        scene_graph_reference_images = (
            conditioning_reference_images
            if self._should_use_ai_final_render(request, generation_path, creative_decision)
            else selected_reference_images
        )
        scene_graph = self.bind_reference_assets(scene_graph, scene_graph_reference_images)
        validation_report = self.validate_scene_graph(
            scene_graph=scene_graph,
            creative_decision=creative_decision,
            request=request,
            compiled_context=compiled_context,
        )
        recomputed_quality_assessment = self.assess_creative_quality(
            scene_graph=scene_graph,
            creative_decision=creative_decision,
            validation_report=validation_report,
            request=request,
            selected_reference_images=scene_graph_reference_images,
            used_support_fallback=False,
            compiled_context=compiled_context,
        )
        if (
            not quality_assessment
            or self._should_block_low_quality_final_render(quality_assessment)
            or float(recomputed_quality_assessment.get("score") or 0.0)
            >= float(quality_assessment.get("score") or 0.0)
        ):
            quality_assessment = recomputed_quality_assessment

        scene_graph_ignored_for_final_render = self._should_ignore_scene_graph_for_final_render(
            generation_path=generation_path,
            fresh_replan_attempted=fresh_replan_attempted,
            validation_report=validation_report,
            scene_graph=scene_graph,
            compiled_context=compiled_context,
            style_reference_final_render_active=self._style_reference_only_carousel_active(request, creative_decision)
            and self._should_use_ai_final_render(request, generation_path, creative_decision),
        )
        final_render_scene_graph = scene_graph
        final_render_retry_note: str | None = None
        if scene_graph_ignored_for_final_render:
            final_render_retry_note = self._final_render_ignore_note(validation_report)
            fallback_scene_graph = self._fallback_image_led_scene_graph(
                request=request,
                text_payload=text_payload.model_dump(mode="json"),
                creative_decision=creative_decision.model_dump(mode="json"),
                compiled_context=compiled_context,
            )
            final_render_scene_graph = self.normalize_scene_graph_payload(
                fallback_scene_graph,
                fallback=scene_graph.model_dump(mode="json"),
                creative_decision=creative_decision,
                text_payload=text_payload.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_scene_graph_override",
                {
                    "reason": "render_only_scene_graph_rejected",
                    "issues": [issue.rule_id for issue in validation_report.issues],
                    "retry_note": final_render_retry_note,
                    "scene_graph": final_render_scene_graph.model_dump(mode="json"),
                },
            )

        visual_explanation_plan = self._visual_explanation_plan(
            request,
            text_payload,
            creative_decision,
            selected_reference_images,
            message_strategy,
        )
        low_quality_plan_deferred_to_final_render = (
            request.generate_image
            and not scene_graph_ignored_for_final_render
            and self._should_block_low_quality_final_render(quality_assessment)
            and self._should_defer_low_quality_plan_to_llm_final_render(
                request=request,
                generation_path=generation_path,
                creative_decision=creative_decision,
            )
        )
        if low_quality_plan_deferred_to_final_render:
            quality_improvement_note = self._final_render_quality_improvement_note(quality_assessment)
            final_render_retry_note = " ".join(
                part
                for part in [
                    final_render_retry_note,
                    quality_improvement_note,
                ]
                if part
            ).strip()
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_low_quality_plan_deferred",
                {
                    "quality_assessment": quality_assessment,
                    "reason": "static_infographic_llm_led_final_render_can_recompose",
                    "retry_note": final_render_retry_note,
                },
            )
        if (
            request.generate_image
            and not scene_graph_ignored_for_final_render
            and self._should_block_low_quality_final_render(quality_assessment)
            and not low_quality_plan_deferred_to_final_render
        ):
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_blocked_low_quality",
                {
                    "quality_assessment": quality_assessment,
                    "reason": "quality_below_hard_threshold_before_render",
                },
            )
            raise GenerationFailureError(
                "Final render blocked because the planned creative is still below the minimum premium-quality threshold.",
                failure_type="validation_failure",
                reason_code="final_render_quality_blocked",
                user_safe_message="The current creative draft did not meet the minimum quality threshold, so generation was stopped instead of returning a weak result.",
                retryable=True,
                suggested_next_action="Refine the scene graph, reference conditioning, or slide structure and try generation again.",
                details={"quality_assessment": quality_assessment},
            )
        self._trace_payload(
            trace_id,
            self.trace,
            "visual_explanation_plan",
            visual_explanation_plan,
        )

        image_size = self._image_generation_size(request.studio_panel)
        slide_specs = (
            carousel_slide_specs
            if is_carousel_render
            else [
                {
                    "role": "single",
                    "headline": text_payload.headline,
                    "supporting_line": (text_payload.metadata or {}).get("supporting_line") or text_payload.body,
                    "proof_points": self._normalize_metadata_list((text_payload.metadata or {}).get("proof_points"), limit=3),
                    "cta": text_payload.cta,
                    "slide_index": 1,
                    "slide_count": 1,
                }
            ]
        )
        logo_storage_path, requested_logo_variant = self._select_logo_candidate_for_render(
            request=request,
            creative_decision=creative_decision,
            scene_graph=final_render_scene_graph,
        )
        exact_logo_overlay_required = bool(logo_storage_path and self.storage.exists(logo_storage_path))
        layout_prompt_reference_images = selected_reference_images if final_render_format_name in {"static", "infographic"} else []
        prompt_reference_images = self._merge_reference_asset_lists(
            conditioning_reference_images,
            layout_prompt_reference_images,
        )
        conditioning_reference_images, skipped_logo_reference_images = self._filter_logo_bearing_conditioning_reference_images(
            conditioning_reference_images,
            exact_logo_overlay_required=exact_logo_overlay_required,
        )
        # If the logo will be overlaid exactly, logo-bearing references are prompt context only, not conditioning inputs.
        topic_reference_images = self._topic_relevant_reference_assets(
            request,
            creative_decision=creative_decision,
            limit=2,
        )
        if self._reference_layout_adaptation_active(
            request,
            creative_decision=creative_decision,
            compiled_context=compiled_context,
            reference_images=conditioning_reference_images,
        ):
            topic_reference_images = []
        if topic_reference_images:
            prompt_reference_images = self._merge_reference_asset_lists(topic_reference_images, prompt_reference_images)
            conditioning_topic_reference_images = self._conditioning_reference_image_assets(
                topic_reference_images,
                creative_decision=creative_decision,
                request=request,
            )
            conditioning_reference_images = self._merge_reference_asset_lists(
                conditioning_topic_reference_images,
                conditioning_reference_images,
            )
            conditioning_reference_images, topic_skipped_logo_reference_images = self._filter_logo_bearing_conditioning_reference_images(
                conditioning_reference_images,
                exact_logo_overlay_required=exact_logo_overlay_required,
            )
            if topic_skipped_logo_reference_images:
                skipped_logo_reference_images = self._merge_reference_asset_lists(
                    skipped_logo_reference_images,
                    topic_skipped_logo_reference_images,
                )
        if skipped_logo_reference_images:
            prompt_reference_images = self._merge_reference_asset_lists(
                conditioning_reference_images,
                skipped_logo_reference_images,
                layout_prompt_reference_images,
            )
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_logo_bearing_reference_images_skipped",
                {
                    "reason": "exact_logo_overlay_requires_logo_free_conditioning",
                    "skipped_storage_paths": [
                        str(asset.get("storage_path") or "")
                        for asset in skipped_logo_reference_images
                    ],
                    "logo_storage_path": logo_storage_path,
                },
            )

        final_render_assets: list[GeneratedImageAsset] = []
        image_generation_error: str | None = None
        pdf_reference_conditioning_cache: dict[str, str] = {}
        try:
            for slide in slide_specs:
                slide_index = int(slide.get("slide_index") or 1)
                slide_count = int(slide.get("slide_count") or len(slide_specs))
                text_overlay_strategy = self._ai_final_render_text_strategy(
                    request,
                    creative_decision,
                )
                if self._style_reference_only_carousel_active(request, creative_decision):
                    text_overlay_strategy = "ai_renders_finished_slide_with_exact_copy"
                use_backend_text_overlay = (
                    text_overlay_strategy == "backend_exact_text_on_ai_text_safe_substrate"
                )
                trace_suffix = f"_slide_{slide_index:02d}" if is_carousel_render else ""
                slide_prompt_reference_images = (
                    self._slide_reference_images(
                        slide,
                        prompt_reference_images,
                        request=request,
                        creative_decision=creative_decision,
                    )
                    if is_carousel_render
                    else [dict(asset) for asset in prompt_reference_images if isinstance(asset, dict)]
                )
                slide_prompt_reference_images = self._merge_slide_layout_anchor_reference(
                    slide,
                    slide_prompt_reference_images,
                    request=request,
                    creative_decision=creative_decision,
                )
                # Prompt references explain intent; conditioning references are stricter because the image model sees them directly.
                slide_conditioning_reference_images = (
                    self._slide_reference_images(
                        slide,
                        conditioning_reference_images,
                        request=request,
                        creative_decision=creative_decision,
                    )
                    if is_carousel_render
                    else [dict(asset) for asset in conditioning_reference_images if isinstance(asset, dict)]
                )
                slide_conditioning_reference_images = self._merge_slide_layout_anchor_reference(
                    slide,
                    slide_conditioning_reference_images,
                    request=request,
                    creative_decision=creative_decision,
                    for_conditioning=True,
                )
                slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                desired_reference_path = str(slide_metadata.get("reference_asset_path") or "").strip()
                if desired_reference_path and not slide_prompt_reference_images:
                    matched_reference_asset = self._reference_asset_by_storage_path(request, desired_reference_path)
                    if matched_reference_asset is not None:
                        slide_prompt_reference_images = [matched_reference_asset]
                if desired_reference_path and not slide_conditioning_reference_images:
                    matched_reference_asset = self._reference_asset_by_storage_path(request, desired_reference_path)
                    if (
                        matched_reference_asset is not None
                        and self._is_conditioning_safe_reference_image_asset(
                            matched_reference_asset,
                            creative_decision=creative_decision,
                            request=request,
                        )
                    ):
                        slide_conditioning_reference_images = [matched_reference_asset]
                if (
                    is_carousel_render
                    and not slide_conditioning_reference_images
                    and not str(slide_metadata.get("reference_asset_path") or "").strip()
                ):
                    slide_conditioning_reference_images = [
                        dict(asset) for asset in conditioning_reference_images if isinstance(asset, dict)
                    ]
                reference_image_paths = self._conditioning_reference_image_paths(
                    slide_conditioning_reference_images,
                    request=request,
                    cache=pdf_reference_conditioning_cache,
                )
                layout_analysis_reference_images = (
                    self._layout_analysis_reference_image_assets(
                        self._merge_reference_asset_lists(
                            slide_prompt_reference_images,
                            slide_conditioning_reference_images,
                        ),
                        request=request,
                    )
                    if not is_carousel_render
                    else [dict(asset) for asset in slide_conditioning_reference_images if isinstance(asset, dict)]
                )
                layout_reference_image_paths = self._conditioning_reference_image_paths(
                    layout_analysis_reference_images,
                    request=request,
                    cache=pdf_reference_conditioning_cache,
                )
                reference_layout_adaptation_active = self._reference_layout_adaptation_active(
                    request,
                    creative_decision=creative_decision,
                    metadata=slide_metadata,
                    compiled_context=compiled_context,
                    reference_images=layout_analysis_reference_images,
                )
                if reference_layout_adaptation_active:
                    # Feature 1 layout adaptation attaches the selected sample blueprint before prompt construction.
                    slide = self._annotate_slide_with_sample_page_blueprint(
                        slide,
                        reference_image_paths=layout_reference_image_paths,
                    )
                    if layout_reference_image_paths:
                        metadata = dict(slide.get("metadata") or {})
                        blueprint = metadata.get("sample_page_blueprint") if isinstance(metadata.get("sample_page_blueprint"), dict) else {}
                        enhanced_blueprint = self._vision_enhance_page_blueprint(
                            image_path=layout_reference_image_paths[0],
                            base_blueprint=blueprint,
                        )
                        if enhanced_blueprint:
                            metadata["sample_page_blueprint"] = enhanced_blueprint
                            slide = {**slide, "metadata": metadata}
                    slide = self._adapt_slide_copy_to_sample_blueprint(
                        slide,
                        content_metadata=text_payload.metadata if isinstance(text_payload.metadata, dict) else {},
                    )
                    # Copy may change to fit the sample density, so refresh metadata before building the render prompt.
                    slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                if is_carousel_render:
                    final_render_prompt = self.build_carousel_slide_render_prompt(
                        request=request,
                        creative_decision=creative_decision,
                        message_strategy=message_strategy,
                        slide=slide,
                        scene_graph=final_render_scene_graph,
                        reference_images=slide_prompt_reference_images,
                        retry_note=final_render_retry_note,
                        visual_explanation_plan=visual_explanation_plan,
                        compiled_context=compiled_context,
                    )
                else:
                    prompt_text_payload = text_payload
                    if slide_metadata:
                        prompt_text_payload = text_payload.model_copy(
                            update={
                                "metadata": {
                                    **(text_payload.metadata or {}),
                                    **slide_metadata,
                                }
                            }
                        )
                    final_render_prompt = self.build_final_render_prompt(
                        request=request,
                        text_payload=prompt_text_payload,
                        creative_decision=creative_decision,
                        scene_graph=final_render_scene_graph,
                        message_strategy=message_strategy,
                        reference_images=prompt_reference_images,
                        retry_note=final_render_retry_note,
                        visual_explanation_plan=visual_explanation_plan,
                        compiled_context=compiled_context,
                    )
                final_render_prompt = self._enforce_style_reference_only_final_prompt(
                    final_render_prompt,
                    request=request,
                    creative_decision=creative_decision,
                    metadata=slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {},
                )
                # Tracing stores the exact prompt and routing metadata used for this render attempt.
                self._trace_payload(
                    trace_id,
                    self.trace,
                    f"final_render_prompt{trace_suffix}",
                    {
                        "prompt": final_render_prompt,
                        "size": image_size,
                        "model": self.settings.image_model,
                        **self._prompt_length_metrics(final_render_prompt),
                        "text_overlay_strategy": text_overlay_strategy,
                        "slide_index": slide_index,
                        "slide_count": slide_count,
                        "role": slide.get("role"),
                        "render_only": True,
                    },
                )
                sample_page_blueprint = (
                    slide_metadata.get("sample_page_blueprint")
                    if isinstance(slide_metadata.get("sample_page_blueprint"), dict)
                    else None
                )
                legal_footer_text = self._scene_graph_legal_footer_text(final_render_scene_graph)
                asset, sample_similarity_report, sample_similarity_retry_attempts, final_render_prompt = (
                    self._generate_final_render_image_with_sample_guard(
                        image_provider=image_provider,
                        request=request,
                        prompt=final_render_prompt,
                        reference_image_paths=reference_image_paths,
                        image_size=image_size,
                        trace_id=trace_id,
                        trace_suffix=trace_suffix,
                        sample_blueprint=sample_page_blueprint,
                        slide_index=slide_index,
                        slide_count=slide_count,
                        approved_text_payload=text_payload,
                        exact_logo_overlay_deferred=exact_logo_overlay_required,
                        legal_footer_overlay_deferred=bool(legal_footer_text),
                        sample_reference_image_paths=layout_reference_image_paths,
                    )
                )
                base_asset = GeneratedImageAsset(
                    asset_id=uuid4(),
                    mime_type=asset["mime_type"],
                    storage_path=asset["storage_path"],
                    width=asset["width"],
                    height=asset["height"],
                    asset_role=asset["asset_role"],
                    metadata={
                        "provider": asset.get("provider"),
                        "model": asset.get("model") or self.settings.image_model,
                        "requested_size": asset.get("size", image_size),
                        "generation_path": generation_path,
                        "generation_stage": "base_final_render",
                        "reference_conditioned": bool(reference_image_paths),
                        "slide_index": slide_index,
                        "slide_count": slide_count,
                        "carousel_role": slide.get("role"),
                    },
                )
                slide_text_payload = StructuredTextPayload(
                    headline=str(slide.get("headline") or text_payload.headline),
                    body=self._carousel_slide_body_text(
                        slide,
                        fallback_text=str(slide.get("supporting_line") or text_payload.body),
                    ),
                    cta=str(slide.get("cta") or text_payload.cta),
                    hashtags=text_payload.hashtags,
                    metadata={
                        **(text_payload.metadata or {}),
                        "source": (
                            "structured_slide_spec"
                            if any(
                                str(slide.get(field) or "").strip()
                                for field in ("headline", "supporting_line", "body", "visual_focus", "cta")
                            )
                            or bool(slide.get("proof_points") or slide.get("body_points") or slide.get("stat_highlights"))
                            else str(((text_payload.metadata or {}) if isinstance(text_payload.metadata, dict) else {}).get("source") or "fallback")
                        ),
                        "supporting_line": str(slide.get("supporting_line") or text_payload.body),
                        "proof_points": list(slide.get("proof_points") or []),
                        "body_points": list(slide.get("body_points") or []),
                        "stat_highlights": list(slide.get("stat_highlights") or []),
                        "visual_focus": str(slide.get("visual_focus") or ""),
                        "transition_note": str(slide.get("transition_note") or ""),
                        "slide_role": str(slide.get("role") or ""),
                        "slide_index": slide_index,
                        "slide_count": slide_count,
                    },
                )
                final_asset_metadata = {
                    "render_source": "ai",
                    "generation_stage": "final_render",
                    "provider": asset.get("provider"),
                    "model": asset.get("model") or self.settings.image_model,
                    **self._prompt_length_metrics(final_render_prompt),
                    "text_overlay_strategy": text_overlay_strategy,
                    "requested_size": asset.get("size", image_size),
                    "generation_path": generation_path,
                    "layout_mode": creative_decision.layout_mode,
                    "scene_graph_used": not scene_graph_ignored_for_final_render,
                    "scene_graph_ignored_for_final_render": scene_graph_ignored_for_final_render,
                    "logo_composited_by_ai": False,
                    "base_storage_path": base_asset.storage_path,
                    "reference_conditioned_by_ai": bool(reference_image_paths),
                    "reference_image_storage_paths": [str(reference_asset.get("storage_path") or "") for reference_asset in slide_conditioning_reference_images],
                    "logo_bearing_reference_image_storage_paths_skipped": [str(reference_asset.get("storage_path") or "") for reference_asset in skipped_logo_reference_images],
                    "visual_explanation_mode": visual_explanation_plan.get("mode"),
                    "visual_explanation_need": visual_explanation_plan.get("need"),
                    "visual_explanation_density": visual_explanation_plan.get("density"),
                    "visual_explanation_rationale": visual_explanation_plan.get("rationale"),
                    "quality_assessment": quality_assessment,
                    "quality_retry_attempts": quality_retry_attempts,
                    "output_quality_assessment": asset.get("output_quality_assessment"),
                    "output_quality_retry_attempts": asset.get("output_quality_retry_attempts", 0),
                    "output_quality_status": asset.get("output_quality_status"),
                    "output_quality_blocking_issues": asset.get("output_quality_blocking_issues", []),
                    "output_quality_user_warning": asset.get("output_quality_user_warning"),
                    "sample_page_blueprint": sample_page_blueprint,
                    "sample_page_conditioning_image_path": reference_image_paths[0] if reference_image_paths else None,
                    "sample_page_layout_image_path": layout_reference_image_paths[0] if layout_reference_image_paths else None,
                    "sample_visual_similarity": sample_similarity_report,
                    "sample_visual_similarity_retry_attempts": sample_similarity_retry_attempts,
                    "slide_index": slide_index,
                    "slide_count": slide_count,
                    "carousel_role": slide.get("role"),
                }
                if use_backend_text_overlay:
                    filtered_scene = final_render_scene_graph.model_copy(deep=True)
                    if final_render_format_name not in self.BACKEND_TEXT_OVERLAY_SCENE_GRAPH_FORMATS:
                        filtered_scene.elements = [e for e in filtered_scene.elements if e.role in ("legal", "footer", "disclaimer")]
                    final_asset_metadata["render_overlay_scene_graph"] = filtered_scene.model_dump(mode="json")

                    filtered_text = slide_text_payload.model_copy(deep=True)
                    filtered_metadata = (filtered_text.metadata or {}).copy()
                    filtered_text.metadata = filtered_metadata
                    final_asset_metadata["render_overlay_text"] = filtered_text.model_dump(mode="json")
                final_asset_metadata = self._enforce_style_reference_only_final_asset_metadata(
                    final_asset_metadata,
                    request=request,
                    creative_decision=creative_decision,
                    slide_metadata=slide_metadata,
                )
                if requested_logo_variant:
                    final_asset_metadata["requested_logo_variant"] = requested_logo_variant
                if logo_storage_path and self.storage.exists(logo_storage_path):
                    final_asset_metadata["logo_source_storage_path"] = logo_storage_path
                    final_asset_metadata["logo_overlay_strategy"] = "exact_asset_overlay"
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"logo_overlay_deferred_to_export{trace_suffix}",
                        {
                            "reason": "preserve_exact_logo_asset",
                            "logo_storage_path": logo_storage_path,
                            "requested_logo_variant": requested_logo_variant,
                            "base_storage_path": base_asset.storage_path,
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                            "render_only": True,
                        },
                    )
                final_render_assets.append(
                    GeneratedImageAsset(
                        asset_id=uuid4(),
                        mime_type=asset["mime_type"],
                        storage_path=asset["storage_path"],
                        width=asset["width"],
                        height=asset["height"],
                        asset_role="render_preview" if slide_index == 1 else "render_export",
                        metadata=final_asset_metadata,
                    )
                )
        except GenerationFailureError:
            raise
        except Exception as exc:  # pragma: no cover - resilience path
            image_generation_error = str(exc)
            logger.warning(
                "orchestrator.render_only.final_render_failed brand_space_id=%s error=%s",
                request.brand_space_id,
                image_generation_error,
            )
            self._trace_payload(trace_id, self.trace, "final_render_error", {"error": image_generation_error, "render_only": True})

        final_render_asset = final_render_assets[0] if final_render_assets else None
        if final_render_asset is None:
            raise GenerationFailureError(
                self._ai_final_render_failure_message(),
                failure_type="provider_failure",
                reason_code="ai_final_render_failed",
                user_safe_message="I couldn't generate the visual this time. Please regenerate.",
                retryable=True,
                rule_source="system",
                suggested_next_action="Regenerate the creative.",
                details={
                    "generation_path": generation_path,
                    "underlying_error": image_generation_error,
                    "stage": "orchestrator.render_only",
                },
            )
        self._trace_payload(
            trace_id,
            self.trace,
            "final_render_generation",
            {
                "slide_count": len(final_render_assets),
                "assets": [asset.model_dump(mode="json") for asset in final_render_assets],
                "render_only": True,
            },
        )
        return final_render_assets, final_render_asset, text_payload, final_render_scene_graph

    @classmethod
    def _clean_content_semantic_issue(
        cls,
        *,
        code: str,
        message: str,
        targeted_fields: list[str] | None = None,
        slide_indexes: list[int] | None = None,
        slide_targets: list[str] | None = None,
    ) -> dict[str, Any]:
        # Centralizes clean content semantic issue from code, message, and targeted fields for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return {
            "code": cls._normalize_metadata_text(code, limit=64),
            "message": cls._normalize_metadata_text(message, limit=240),
            "targeted_fields": [
                field
                for field in [cls._normalize_metadata_text(item, limit=48) for item in (targeted_fields or [])]
                if field
            ],
            "slide_indexes": [int(item) for item in (slide_indexes or []) if int(item) > 0],
            "slide_targets": [
                target
                for target in [cls._normalize_metadata_text(item, limit=64) for item in (slide_targets or [])]
                if target
            ],
        }

    @classmethod
    def _format_name_from_request(cls, request: AIOrchestrationRequest) -> str:
        # Formats name request from request state for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        return cls._normalize_metadata_text(request.studio_panel.get("format"), limit=32).casefold()

    @classmethod
    def _llm_led_static_infographic_format(cls, format_name: Any) -> bool:
        # Centralizes llm led static infographic from format name for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return cls._normalize_metadata_text(format_name, limit=32).casefold() in {"static", "infographic"}

    @classmethod
    def _needs_content_semantic_validation(cls, request: AIOrchestrationRequest) -> bool:
        # Centralizes needs content semantic validation from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return cls._format_name_from_request(request) in {"carousel", "infographic", "static"}

    DATA_SURFACE_BLOCKING_ISSUE_CODES: set[str] = set()

    @classmethod
    def _unresolved_data_surface_issue_codes(cls, report: dict[str, Any] | None) -> set[str]:
        # Centralizes unresolved surface issue codes from report for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(report, dict) or str(report.get("status") or "").casefold() == "clean":
            return set()
        return {
            code
            for code in [
                cls._normalize_metadata_text(issue.get("code"), limit=80)
                for issue in (report.get("issues") or [])
                if isinstance(issue, dict)
            ]
            if code in cls.DATA_SURFACE_BLOCKING_ISSUE_CODES
        }

    @classmethod
    def _data_surface_contract_active(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
    ) -> bool:
        # Builds surface contract active from request state and copy payload for AIOrchestratorService.
        # It calls _data_list_surface_contract and _format_name_from_request while assembling the payload or prompt text.
        return bool(
            cls._data_list_surface_contract(
                prompt=request.prompt,
                format_name=cls._format_name_from_request(request),
                metadata=text_payload.metadata if isinstance(text_payload.metadata, dict) else {},
                text_payload=text_payload,
            )
        )

    @classmethod
    def _should_defer_data_surface_layout_validation(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        validation_report: SceneGraphValidationReport,
        ) -> bool:
        # Checks defer surface layout validation from request state, copy payload, and validation report for AIOrchestratorService.
        # It reuses _data_surface_contract_active and _format_name_from_request so related checks follow the same rule.
        if validation_report.status == "clean":
            return False
        if cls._format_name_from_request(request) not in {"static", "infographic"}:
            return False
        return cls._data_surface_contract_active(request=request, text_payload=text_payload)

    @classmethod
    def _should_defer_low_quality_plan_to_llm_final_render(
        cls,
        *,
        request: AIOrchestrationRequest,
        generation_path: str | None,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> bool:
        # Checks defer low quality plan LLM final from request state, generation path, and creative decision for AIOrchestratorService.
        # It reuses _should_use_ai_final_render and _llm_led_static_infographic_format so related checks follow the same rule.
        if not cls._llm_led_static_infographic_format(cls._format_name_from_request(request)):
            return False
        return cls._should_use_ai_final_render(request, generation_path, creative_decision)

    @classmethod
    def _final_render_quality_improvement_note(cls, quality_assessment: dict[str, Any] | None) -> str:
        # Checks final quality improvement note from quality assessment for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        assessment = quality_assessment if isinstance(quality_assessment, dict) else {}
        try:
            score = float(assessment.get("score") or 0.0)
        except (TypeError, ValueError):
            score = 0.0
        issues = [
            cls._normalize_metadata_text(issue, limit=80)
            for issue in (assessment.get("issues") or [])
            if cls._normalize_metadata_text(issue, limit=80)
        ]
        issue_text = ", ".join(issues[:6]) if issues else "unspecified quality issues"
        return (
            f"Quality improvement note: the planning draft scored {score:.2f}, below the premium threshold. "
            f"Resolve these issues in the final image model composition: {issue_text}. "
            "Use a stronger topic-specific focal idea, clearer hierarchy, better evidence grouping, premium spacing, high contrast, and richer craft. "
            "Do not fall back to generic people, stale templates, blank cards, weak decorative icons, or unrelated visual topics."
        )

    @classmethod
    def _data_surface_generation_failure(
        cls,
        *,
        report: dict[str, Any],
        reason_code: str = "data_surface_content_unresolved",
    ) -> GenerationFailureError:
        # Centralizes surface generation failure from report and reason code for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        issue_codes = sorted(cls._unresolved_data_surface_issue_codes(report))
        summary = (
            "Data/ranking surface generation blocked because exact values or claims are unresolved; "
            "generic visual fallback is disabled for this request."
        )
        return GenerationFailureError(
            summary,
            failure_type="content_semantic_validation",
            reason_code=reason_code,
            user_safe_message=(
                "Verified exact values or claims were missing for this ranking/table request, so generation stopped before inventing data. "
                "Attach source data or run live research upstream before final rendering."
            ),
            retryable=True,
            rule_source="data_surface_no_fallback",
            suggested_next_action="Provide verified exact values for the requested ranking/table, or run the research step before image generation.",
            details={"issue_codes": issue_codes, "report": report},
        )

    @classmethod
    def _build_content_semantic_rewrite_instruction(
        cls,
        *,
        format_name: str,
        issues: list[dict[str, Any]],
    ) -> str:
        # Builds content semantic rewrite instruction from format name and issues for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        issue_messages = [
            cls._normalize_metadata_text(issue.get("message"), limit=220)
            for issue in issues
            if isinstance(issue, dict)
        ]
        slide_scope = sorted(
            {
                f"slide {int(index)}"
                for issue in issues
                if isinstance(issue, dict)
                for index in (issue.get("slide_indexes") or [])
                if str(index).strip().isdigit()
            }
        )
        slide_targets = sorted(
            {
                cls._normalize_metadata_text(target, limit=48)
                for issue in issues
                if isinstance(issue, dict)
                for target in (issue.get("slide_targets") or [])
                if cls._normalize_metadata_text(target, limit=48)
            }
        )
        scope_text = ""
        if slide_scope or slide_targets:
            scope_text = f" Keep the rewrite scoped to {', '.join([*slide_scope, *slide_targets])} where possible."
        if format_name == "carousel":
            return (
                "Rewrite the structured carousel content so each slide carries one distinct editorial job and the sequence reads like a real narrative, not repeated summaries."
                f" Fix these issues: {'; '.join(issue_messages)}."
                " Strengthen metadata.carousel_slide_specs, body, proof_points, stat_highlights, and claim_evidence_pairs."
                " Make the copy strategic, audience-facing, platform-native, and sample-aware without copying generic sample headings verbatim."
                " Preserve factual accuracy: exact dates, numbers, percentages, currency amounts, and regulatory claims must come only from verified facts or the user prompt."
                " Give every slide a specific visual_focus that chooses a premium 3D, 2.5D/isometric, flat editorial, dashboard/product, chart/module, document/evidence, icon-system, or brand-led treatment based on slide role and sample style."
                " visual_focus must be natural-language visual direction only; never return reference_image objects, storage paths, asset ids, filenames, or uploaded-reference handles as the visual idea."
                " If any slide uses a table, chart, graph, dashboard, matrix, timeline, scorecard, or metric module, tie every label/value/bucket to approved proof_points, stat_highlights, claim_evidence_pairs, body_points, verified facts, or the user prompt; otherwise downgrade it to qualitative cards or evidence modules."
                " Remove CTA treatment from non-final slides and keep the closing slide distinct."
                " Do not change the campaign topic or visual direction unless required to support the copy structure."
                f"{scope_text}"
            )
        if format_name == "infographic":
            return (
                "Rewrite the structured infographic content so each section has a distinct job, distinct evidence, and a clear progression from overview to evidence to takeaway."
                f" Fix these issues: {'; '.join(issue_messages)}."
                " Strengthen metadata.infographic_section_specs, body, proof_points, stat_highlights, and claim_evidence_pairs."
                " If any section uses a table, chart, graph, dashboard, matrix, timeline, scorecard, or metric module, tie every label/value/bucket to approved section facts; otherwise downgrade it to qualitative comparison cards, process blocks, or evidence modules."
                " Do not collapse the sections back into one repeated summary block."
                f"{scope_text}"
            )
        return (
            "Rewrite the structured static content so the panel has one dominant message with concrete support instead of generic or repeated lines."
            f" Fix these issues: {'; '.join(issue_messages)}."
            " Strengthen metadata.static_panel_spec, body, proof_points, stat_highlights, and claim_evidence_pairs."
            " If the user requested a top-N/ranking/list count, preserve that requested count as distinct row-ready metadata entries whenever verified/user-provided facts support it."
            " If the static panel uses a table, chart, graph, dashboard, matrix, timeline, scorecard, or metric module, tie every label/value/bucket to approved panel facts; otherwise downgrade it to qualitative cards or a single evidence module."
            " Keep the message concise and keep CTA treatment secondary to the main claim."
        )

    @classmethod
    def _content_semantic_revision_scope(
        cls,
        *,
        issues: list[dict[str, Any]],
    ) -> dict[str, Any]:
        # Centralizes content semantic revision scope from issues for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        targeted_fields = sorted(
            {
                field
                for issue in issues
                if isinstance(issue, dict)
                for field in (issue.get("targeted_fields") or [])
                if cls._normalize_metadata_text(field, limit=48)
            }
        )
        slide_indexes = sorted(
            {
                int(index)
                for issue in issues
                if isinstance(issue, dict)
                for index in (issue.get("slide_indexes") or [])
                if str(index).strip().isdigit() and int(index) > 0
            }
        )
        slide_targets = sorted(
            {
                cls._normalize_metadata_text(target, limit=64)
                for issue in issues
                if isinstance(issue, dict)
                for target in (issue.get("slide_targets") or [])
                if cls._normalize_metadata_text(target, limit=64)
            }
        )
        scope: dict[str, Any] = {
            "targeted_fields": targeted_fields or ["body", "metadata"],
            "only_targeted": True,
            "preserve_visuals": True,
            "change_layout": False,
            "change_tone": False,
            "preserve_copy": False,
        }
        if slide_indexes:
            scope["slide_indexes"] = slide_indexes
        if slide_targets:
            scope["slide_targets"] = slide_targets
        return scope

    @classmethod
    def _content_semantic_rewrite_field_plan(
        cls,
        *,
        text_payload: StructuredTextPayload,
        issues: list[dict[str, Any]],
        format_name: str,
        request: AIOrchestrationRequest | None = None,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> dict[str, Any]:
        # Builds content semantic rewrite field plan from copy payload, issues, and format name for AIOrchestratorService.
        # It calls _build_carousel_slide_specs and _normalize_metadata_text while assembling the payload or prompt text.
        metadata = text_payload.metadata or {}
        plan = {
            "format": format_name,
            "headline": text_payload.headline,
            "cta": text_payload.cta,
            "must_preserve": {
                "hashtags": list(text_payload.hashtags or []),
                "supporting_line": cls._normalize_metadata_text(metadata.get("supporting_line"), limit=180),
                "preferred_slide_count": metadata.get("preferred_slide_count"),
            },
            "issues": issues,
        }
        if format_name == "carousel" and request is not None:
            normalized_slides = cls._build_carousel_slide_specs(
                text_payload,
                request=request,
                creative_decision=creative_decision,
            )
            targeted_slide_indexes = sorted(
                {
                    int(index)
                    for issue in issues
                    if isinstance(issue, dict)
                    for index in (issue.get("slide_indexes") or [])
                    if str(index).strip().isdigit() and int(index) > 0
                }
            )
            slide_contracts: list[dict[str, Any]] = []
            for index, slide in enumerate(normalized_slides, start=1):
                if targeted_slide_indexes and index not in targeted_slide_indexes:
                    continue
                slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                slide_contracts.append(
                    {
                        "slide_number": index,
                        "story_role": cls._normalize_metadata_text(
                            slide_metadata.get("story_role") or slide.get("role"),
                            limit=48,
                        ),
                        "preferred_headline": cls._normalize_metadata_text(slide.get("headline"), limit=120),
                        "preferred_supporting_line": cls._normalize_metadata_text(slide.get("supporting_line"), limit=220),
                        "preferred_body": cls._normalize_metadata_text(
                            cls._carousel_slide_body_text(slide, fallback_text=""),
                            limit=320,
                        ),
                        "preferred_proof_points": cls._normalize_metadata_list(slide.get("proof_points"), limit=3),
                        "preferred_stat_highlights": cls._normalize_metadata_list(slide.get("stat_highlights"), limit=3),
                        "preferred_claim_evidence_pairs": cls._normalize_claim_evidence_pairs(
                            slide.get("claim_evidence_pairs"),
                            limit=2,
                        ),
                        "cta_must_be_empty": index < len(normalized_slides),
                    }
                )
            if slide_contracts:
                plan["preferred_slide_contracts"] = slide_contracts
        return plan

    @classmethod
    def _carousel_topic_label(cls, request: AIOrchestrationRequest, *, limit: int = 76) -> str:
        # Extracts carousel topic label from request state and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text and _content_topic_tokens to turn raw evidence into the structured signal the caller needs.
        prompt = " ".join(str(request.prompt or "").split())
        topic = prompt
        for marker in (" on ", " about ", " regarding "):
            if marker in prompt.casefold():
                topic = prompt[prompt.casefold().rfind(marker) + len(marker):]
                break
        topic = re.sub(
            r"\b(?:write|create|generate|make|linkedin|instagram|carousel|static|infographic|for|an?|the|by|using|sample|brand)\b",
            " ",
            topic,
            flags=re.IGNORECASE,
        )
        topic = re.sub(r"\s+", " ", topic).strip(" .,:;-")
        if not topic:
            tokens = list(cls._content_topic_tokens(request))
            topic = " ".join(tokens[:6])
        return cls._normalize_metadata_text(topic or "this topic", limit=limit) or "this topic"

    @classmethod
    def _sample_editorial_contract_for_slide(
        cls,
        *,
        request: AIOrchestrationRequest,
        index: int,
        slide_count: int,
    ) -> dict[str, Any]:
        # Builds sample editorial contract slide from request state, index, and slide count for AIOrchestratorService.
        # It calls _template_sequence_pack and _proportional_sequence_reference_index while assembling the payload or prompt text.
        sequence_pack = cls._template_sequence_pack(request)
        if not isinstance(sequence_pack, dict):
            return {}
        pack_slides = [dict(item) for item in (sequence_pack.get("slides") or []) if isinstance(item, dict)]
        if not pack_slides:
            return {}
        reference_index = cls._proportional_sequence_reference_index(
            index,
            output_slide_count=max(slide_count, 1),
            reference_slide_count=len(pack_slides),
        )
        if reference_index < 1 or reference_index > len(pack_slides):
            return {}
        return dict(pack_slides[reference_index - 1])

    @classmethod
    def _sample_editorial_overlaps_request(
        cls,
        sample_contract: dict[str, Any],
        *,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None = None,
    ) -> bool:
        # Centralizes sample editorial overlaps request from sample contract, request state, and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        sample_text = " ".join(
            cls._coerce_text_value(sample_contract.get(key))
            for key in (
                "sample_page_headline",
                "sample_page_supporting",
                "sample_page_copy",
                "headline_hint",
                "sequence_summary",
            )
            if cls._coerce_text_value(sample_contract.get(key))
        )
        if not sample_text:
            return False
        request_text_parts = [request.prompt]
        for source in (
            request.research_editorial_brief,
            request.live_research,
            (compiled_context or {}).get("research_editorial_brief"),
            (compiled_context or {}).get("live_research"),
            (compiled_context or {}).get("research_summary"),
        ):
            if source:
                request_text_parts.append(source)
        request_text = " ".join(cls._coerce_text_value(part) for part in request_text_parts if cls._coerce_text_value(part))
        sample_markers = cls._exact_claim_markers(sample_text)
        request_markers = cls._exact_claim_markers(request_text)
        if sample_markers and request_markers and sample_markers & request_markers:
            return True
        sample_tokens = cls._sequence_pack_semantic_tokens(sample_text)
        request_tokens = cls._sequence_pack_semantic_tokens(request_text)
        if not sample_tokens or not request_tokens:
            return False
        high_signal_overlap = {
            token
            for token in sample_tokens & request_tokens
            if len(token) <= 4 or token in cls._content_topic_tokens(request)
        }
        return len(sample_tokens & request_tokens) >= 2 or bool(high_signal_overlap)

    @classmethod
    def _topic_specific_headline_from_sample_behavior(
        cls,
        *,
        topic: str,
        role: str,
        sample_contract: dict[str, Any],
    ) -> str:
        # Centralizes topic specific headline sample behavior from topic, role, and sample contract for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        behavior = cls._normalize_metadata_text(sample_contract.get("sample_page_copy_behavior"), limit=48).casefold()
        editorial_role = cls._normalize_metadata_text(sample_contract.get("sample_page_editorial_role"), limit=48).casefold()
        has_question = bool(sample_contract.get("sample_page_has_question_hook"))
        normalized_role = role.casefold().replace(" ", "_")
        if has_question and (normalized_role in {"hook", "cover", "opening"} or editorial_role == "hook"):
            return cls._normalize_metadata_text(f"Why does {topic} matter now?", limit=96)
        if behavior in {"curiosity_gap", "curiosity_to_strategic_signal"} or editorial_role == "undercovered_angle":
            return cls._normalize_metadata_text(f"What most people miss about {topic}", limit=96)
        if behavior == "deal_mechanics" or editorial_role == "mechanism":
            return cls._normalize_metadata_text(f"What's actually inside {topic}", limit=96)
        if behavior == "strategic_signal" or editorial_role in {"strategic_meaning", "macro_takeaway"}:
            return cls._normalize_metadata_text(f"{topic}: the bigger signal", limit=96)
        if normalized_role in {"takeaway", "closing", "cta", "final"}:
            return cls._normalize_metadata_text(f"What {topic} signals next", limit=96)
        if normalized_role in {"structure", "context", "detail"}:
            return cls._normalize_metadata_text(f"The mechanics behind {topic}", limit=96)
        return cls._normalize_metadata_text(f"One sharper read on {topic}", limit=96)

    @classmethod
    def _sample_aware_headline_for_slide(
        cls,
        *,
        request: AIOrchestrationRequest,
        slide: dict[str, Any],
        index: int,
        slide_count: int,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Centralizes sample aware headline slide from request state, slide, and index for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        topic = cls._carousel_topic_label(request)
        role = cls._normalize_metadata_text(
            slide.get("slide_role") or slide.get("role"),
            limit=48,
        ).casefold().replace(" ", "_")
        sample_contract = cls._sample_editorial_contract_for_slide(
            request=request,
            index=index,
            slide_count=slide_count,
        )
        sample_headline = cls._normalize_metadata_text(sample_contract.get("sample_page_headline"), limit=120)
        allow_literal_sample_headline = not cls._request_uses_template_adaptance(request)
        if (
            allow_literal_sample_headline
            and sample_headline
            and not cls._is_generic_carousel_education_label(sample_headline)
            and cls._sample_editorial_overlaps_request(
                sample_contract,
                request=request,
                compiled_context=compiled_context,
            )
        ):
            return sample_headline
        if sample_contract:
            return cls._topic_specific_headline_from_sample_behavior(
                topic=topic,
                role=role,
                sample_contract=sample_contract,
            )
        if index == 1 or role == "hook":
            return cls._normalize_metadata_text(f"What {topic} really changes", limit=96)
        if index == slide_count or role in {"takeaway", "closing", "cta"}:
            return cls._normalize_metadata_text(f"How to read {topic} as an investor", limit=96)
        if role in {"structure", "context", "detail"}:
            return cls._normalize_metadata_text(f"The core mechanics behind {topic}", limit=96)
        if role in {"strategic_meaning", "undercovered_angle", "analysis"}:
            return cls._normalize_metadata_text(f"The investor angle inside {topic}", limit=96)
        return cls._normalize_metadata_text(f"One key layer of {topic}", limit=96)

    @classmethod
    def _text_contains_unsupported_marker(cls, value: Any, unsupported_markers: set[str]) -> bool:
        # Extracts text contains unsupported marker from input value and unsupported markers for AIOrchestratorService.
        # It calls _exact_claim_markers to turn raw evidence into the structured signal the caller needs.
        if not unsupported_markers:
            return False
        return bool(cls._exact_claim_markers(value).intersection(unsupported_markers))

    @classmethod
    def _data_visualization_visual_focus(cls, value: Any) -> bool:
        # Centralizes visualization visual focus from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._normalize_metadata_text(value, limit=420).casefold()
        return any(
            marker in text
            for marker in (
                "table",
                "chart",
                "graph",
                "dashboard",
                "matrix",
                "scorecard",
                "timeline",
                "bar",
                "axis",
                "metric",
                "percentage",
                "data visual",
            )
        )

    @classmethod
    def _remove_unsupported_exact_claim_lines(
        cls,
        value: Any,
        unsupported_markers: set[str],
    ) -> str:
        # Extracts remove unsupported exact claim lines from input value and unsupported markers for AIOrchestratorService.
        # It calls _coerce_text_value and _text_contains_unsupported_marker to turn raw evidence into the structured signal the caller needs.
        text = cls._coerce_text_value(value)
        if not text or not unsupported_markers:
            return text
        sentence_parts = re.split(r"(?<=[.!?])\s+", text)
        kept = [
            part.strip()
            for part in sentence_parts
            if part.strip() and not cls._text_contains_unsupported_marker(part, unsupported_markers)
        ]
        return " ".join(kept).strip()

    @classmethod
    def _filter_unsupported_exact_claim_items(
        cls,
        value: Any,
        unsupported_markers: set[str],
        *,
        limit: int = 10,
    ) -> list[str]:
        # Extracts filter unsupported exact claim items from input value, unsupported markers, and limit for AIOrchestratorService.
        # It calls _normalize_metadata_list and _text_contains_unsupported_marker to turn raw evidence into the structured signal the caller needs.
        values = cls._normalize_metadata_list(value, limit=limit)
        if not unsupported_markers:
            return values
        return [
            line
            for line in values
            if not cls._text_contains_unsupported_marker(line, unsupported_markers)
        ]

    @classmethod
    def _filter_unsupported_claim_evidence_pairs(
        cls,
        value: Any,
        unsupported_markers: set[str],
        *,
        limit: int = 10,
    ) -> list[dict[str, str]]:
        # Centralizes filter unsupported claim evidence pairs from input value, unsupported markers, and limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        pairs = cls._normalize_claim_evidence_pairs(value, limit=limit)
        if not unsupported_markers:
            return pairs
        return [
            pair
            for pair in pairs
            if not cls._text_contains_unsupported_marker(
                " ".join(
                    [
                        cls._coerce_text_value(pair.get("claim")),
                        cls._coerce_text_value(pair.get("evidence")),
                    ]
                ),
                unsupported_markers,
            )
        ]

    @classmethod
    def _qualitative_data_surface_visual_focus(
        cls,
        *,
        request: AIOrchestrationRequest,
        format_name: str,
    ) -> str:
        # Centralizes qualitative surface visual focus from request state and format name for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        requested_count = cls._explicit_top_n_item_count(request.prompt)
        count_phrase = f"{requested_count} clear slots" if requested_count else "clear grouped slots"
        surface = "sectioned explainer" if format_name == "infographic" else "single-panel creative"
        return (
            f"Topic-specific qualitative comparison modules for a {surface}, with {count_phrase}, concise labels, "
            "icons, dividers, and grouped cards; avoid exact numbers or measured labels unless verified anchors are available."
        )

    @classmethod
    def _sanitize_structured_visual_data_anchors(
        cls,
        value: Any,
        unsupported_markers: set[str],
    ) -> tuple[Any, bool]:
        # Sanitizes structured visual anchors from input value and unsupported markers for AIOrchestratorService.
        # It delegates shared cleanup to _text_contains_unsupported_marker before returning the cleaned value.
        if not isinstance(value, dict) or not isinstance(value.get("data_anchors"), list):
            return value, False
        cleaned = dict(value)
        original = list(value.get("data_anchors") or [])
        filtered: list[Any] = []
        for item in original:
            text = item.get("text") if isinstance(item, dict) else item
            if unsupported_markers and cls._text_contains_unsupported_marker(text, unsupported_markers):
                continue
            filtered.append(item)
        if filtered == original:
            return value, False
        cleaned["data_anchors"] = filtered
        for key in ("table_intent", "chart_intent"):
            intent = cleaned.get(key)
            if isinstance(intent, dict) and not filtered:
                cleaned[key] = {
                    **intent,
                    "allowed": False,
                    "anchor_count": 0,
                }
        return cleaned, True

    @classmethod
    def _line_is_research_process_filler(cls, value: Any) -> bool:
        # Extracts line research process filler from input value for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=220).casefold()
        if not text:
            return False
        return bool(
            re.search(
                r"\b(verified facts? from|sourced from|source(?:d)? by|according to|fact[- ]checked from|research from|data from)\b",
                text,
            )
        )

    @classmethod
    def _strip_research_process_filler_lines(
        cls,
        value: Any,
        *,
        sample_allows_source_labels: bool,
    ) -> Any:
        # Strips strip research process filler lines from input value and sample allows source labels for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if sample_allows_source_labels:
            return value
        if isinstance(value, (list, tuple, set)):
            return [
                item
                for item in value
                if not cls._line_is_research_process_filler(item)
            ]
        text = cls._coerce_text_value(value)
        if not text:
            return value
        sentence_parts = re.split(r"(?<=[.!?])\s+", text)
        kept = [
            part.strip()
            for part in sentence_parts
            if part.strip() and not cls._line_is_research_process_filler(part)
        ]
        return " ".join(kept).strip()

    @classmethod
    def _sample_contract_expected_module_min(cls, sample_contract: dict[str, Any]) -> int:
        # Builds sample contract expected module min from sample contract for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        if not sample_contract:
            return 0
        copy_density = cls._normalize_metadata_text(
            sample_contract.get("sample_page_copy_density"),
            limit=32,
        ).casefold()
        behavior = cls._normalize_metadata_text(
            sample_contract.get("sample_page_copy_behavior"),
            limit=48,
        ).casefold()
        if copy_density == "high" or behavior in {"deal_mechanics", "curiosity_gap", "strategic_signal"}:
            return 3
        if copy_density == "medium":
            return 2
        return 0

    @classmethod
    def _sample_density_module_candidate_lines(
        cls,
        *,
        slide: dict[str, Any],
        metadata: dict[str, Any],
        text_payload: StructuredTextPayload,
        request: AIOrchestrationRequest,
        compiled_context: dict[str, Any] | None,
        sample_contract: dict[str, Any],
        limit: int,
    ) -> list[str]:
        # Selects sample density module candidate lines from slide, metadata, and copy payload for AIOrchestratorService.
        # It reuses _carousel_topic_label and _dedupe_metadata_collection so related checks follow the same rule.
        candidates: list[str] = []
        for key in ("proof_points", "body_points", "stat_highlights"):
            candidates.extend(cls._normalize_metadata_list(slide.get(key), limit=8))
        candidates.extend(
            cls._claim_evidence_pair_lines(
                cls._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=4),
                limit=4,
            )
        )
        for value in (slide.get("body"), slide.get("supporting_line")):
            candidates.extend(cls._sentences(value)[:4])
        for key in ("proof_points", "body_points", "stat_highlights"):
            candidates.extend(cls._normalize_metadata_list(metadata.get(key), limit=8))
        candidates.extend(
            cls._claim_evidence_pair_lines(
                cls._normalize_claim_evidence_pairs(metadata.get("claim_evidence_pairs"), limit=6),
                limit=6,
            )
        )
        for brief in (
            request.research_editorial_brief,
            (compiled_context or {}).get("research_editorial_brief") if isinstance(compiled_context, dict) else None,
        ):
            candidates.extend(cls._claim_evidence_pair_lines(cls._claim_evidence_pairs_from_research_brief(brief, limit=4), limit=4))
        candidates.extend(cls._sentences(text_payload.body)[:6])

        topic = cls._carousel_topic_label(request, limit=64)
        role = cls._normalize_metadata_text(slide.get("slide_role") or slide.get("role"), limit=48).casefold().replace(" ", "_")
        behavior = cls._normalize_metadata_text(sample_contract.get("sample_page_copy_behavior"), limit=48).casefold()
        if behavior == "deal_mechanics":
            candidates.extend(
                [
                    f"The mechanism matters more than the headline around {topic}.",
                    f"Look at what {topic} changes in access, timing, or incentives.",
                    f"The practical signal is in the clauses, not the announcement.",
                ]
            )
        elif behavior in {"curiosity_gap", "curiosity_to_strategic_signal"}:
            candidates.extend(
                [
                    f"The missed layer is what {topic} quietly changes underneath.",
                    f"The obvious story is only the first read on {topic}.",
                    f"The sharper question is what becomes possible next.",
                ]
            )
        elif role in {"takeaway", "closing", "final", "strategic_meaning"} or behavior == "strategic_signal":
            candidates.extend(
                [
                    f"The bigger signal is how {topic} shapes the next decision.",
                    f"The takeaway is not urgency; it is a clearer planning lens.",
                    f"Read the story as a long-term signal, not a one-day update.",
                ]
            )
        else:
            candidates.extend(
                [
                    f"The first-order story around {topic} is only one layer.",
                    f"The real value is in what changes next.",
                    f"Turn the headline into a sharper decision lens.",
                ]
            )

        blocked_texts = [slide.get("headline"), slide.get("supporting_line"), slide.get("body"), slide.get("cta")]
        cleaned: list[str] = []
        for line in cls._dedupe_metadata_collection(candidates, blocked_texts=blocked_texts, limit=max(limit * 3, 8)):
            if cls._line_is_research_process_filler(line):
                continue
            if cls._is_product_cta_like_line(line, request=request):
                continue
            cleaned.append(line)
            if len(cleaned) >= limit:
                break
        return cleaned

    @classmethod
    def _sample_contract_allows_product_cta(cls, sample_contract: dict[str, Any]) -> bool:
        # Builds sample contract allows product cta from sample contract for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        if not sample_contract:
            return True
        closing_grammar = cls._normalize_metadata_text(sample_contract.get("sample_page_closing_grammar"), limit=40).casefold()
        editorial_role = cls._normalize_metadata_text(sample_contract.get("sample_page_editorial_role"), limit=40).casefold()
        behavior = cls._normalize_metadata_text(sample_contract.get("sample_page_copy_behavior"), limit=48).casefold()
        if not (closing_grammar or editorial_role or behavior):
            return True
        return "product" in {closing_grammar, editorial_role, behavior} or closing_grammar == "product_cta" or behavior == "product_cta"

    @classmethod
    def _is_product_cta_like_line(
        cls,
        value: Any,
        *,
        request: AIOrchestrationRequest | None = None,
    ) -> bool:
        # Checks product cta line from input value and request state for AIOrchestratorService.
        # It reuses _normalize_metadata_text and _is_promotional_line so related checks follow the same rule.
        text = cls._normalize_metadata_text(value, limit=220)
        if not text:
            return False
        lowered = text.casefold()
        if cls._is_promotional_line(text):
            return True
        if re.search(r"\b(where to invest|what to do with|why .{1,80} helps|use .{1,80} platform)\b", lowered):
            return True
        brand_name = ""
        if request is not None and isinstance(request.resolved_brand_context, dict):
            brand_name = cls._normalize_metadata_text(request.resolved_brand_context.get("brand_name"), limit=60)
        if brand_name and brand_name.casefold() in lowered and re.search(r"\b(invest|platform|helps?|options?|opportunit(?:y|ies))\b", lowered):
            return True
        return False

    @classmethod
    def _preflight_static_infographic_text_payload_semantics(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        compiled_context: dict[str, Any] | None,
        format_name: str,
    ) -> StructuredTextPayload:
        # Derives preflight static infographic text semantics from request state, copy payload, and compiled context for AIOrchestratorService.
        # It calls _unsupported_exact_claim_markers and _coerce_text_value to turn raw evidence into the structured signal the caller needs.
        payload = text_payload.model_dump(mode="json")
        metadata = deepcopy(payload.get("metadata") if isinstance(payload.get("metadata"), dict) else {})
        unsupported_markers = cls._unsupported_exact_claim_markers(
            json.dumps(payload, ensure_ascii=False, default=str),
            request=request,
            compiled_context=compiled_context,
        )
        changed = False

        for key in ("body", "cta"):
            cleaned = cls._remove_unsupported_exact_claim_lines(payload.get(key), unsupported_markers)
            if cleaned != cls._coerce_text_value(payload.get(key)):
                payload[key] = cleaned
                changed = True
        headline = cls._coerce_text_value(payload.get("headline"))
        cleaned_headline = cls._remove_unsupported_exact_claim_lines(headline, unsupported_markers)
        if cleaned_headline and cleaned_headline != headline:
            payload["headline"] = cleaned_headline
            changed = True

        for key in ("proof_points", "stat_highlights", "trust_builders"):
            if key not in metadata:
                continue
            original = cls._normalize_metadata_list(metadata.get(key), limit=10)
            filtered = cls._filter_unsupported_exact_claim_items(metadata.get(key), unsupported_markers, limit=10)
            if filtered != original:
                metadata[key] = filtered
                changed = True
        if "claim_evidence_pairs" in metadata:
            original_pairs = cls._normalize_claim_evidence_pairs(metadata.get("claim_evidence_pairs"), limit=10)
            filtered_pairs = cls._filter_unsupported_claim_evidence_pairs(
                metadata.get("claim_evidence_pairs"),
                unsupported_markers,
                limit=10,
            )
            if filtered_pairs != original_pairs:
                metadata["claim_evidence_pairs"] = filtered_pairs
                changed = True

        structured_visual_metadata, structured_changed = cls._sanitize_structured_visual_data_anchors(
            metadata.get("structured_visual_metadata"),
            unsupported_markers,
        )
        if structured_changed:
            metadata["structured_visual_metadata"] = structured_visual_metadata
            changed = True

        def sanitize_surface_container(container: dict[str, Any]) -> bool:
            # Centralizes surface container from container for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            local_changed = False
            for key in ("body", "supporting_line", "dominant_message"):
                if key not in container:
                    continue
                cleaned = cls._remove_unsupported_exact_claim_lines(container.get(key), unsupported_markers)
                if cleaned != cls._coerce_text_value(container.get(key)):
                    container[key] = cleaned
                    local_changed = True
            for key in ("proof_points", "body_points", "supporting_lines", "stat_highlights"):
                if key not in container:
                    continue
                original = cls._normalize_metadata_list(container.get(key), limit=10)
                filtered = cls._filter_unsupported_exact_claim_items(container.get(key), unsupported_markers, limit=10)
                if filtered != original:
                    container[key] = filtered
                    local_changed = True
            if "claim_evidence_pairs" in container:
                original_pairs = cls._normalize_claim_evidence_pairs(container.get("claim_evidence_pairs"), limit=10)
                filtered_pairs = cls._filter_unsupported_claim_evidence_pairs(
                    container.get("claim_evidence_pairs"),
                    unsupported_markers,
                    limit=10,
                )
                if filtered_pairs != original_pairs:
                    container["claim_evidence_pairs"] = filtered_pairs
                    local_changed = True
            return local_changed

        panel = metadata.get("static_panel_spec")
        if isinstance(panel, dict):
            panel = dict(panel)
            if sanitize_surface_container(panel):
                changed = True
            metadata["static_panel_spec"] = panel

        sections = metadata.get("infographic_section_specs")
        if isinstance(sections, list):
            cleaned_sections: list[Any] = []
            for section in sections:
                if not isinstance(section, dict):
                    cleaned_sections.append(section)
                    continue
                section_copy = dict(section)
                if sanitize_surface_container(section_copy):
                    changed = True
                cleaned_sections.append(section_copy)
            if cleaned_sections != sections:
                metadata["infographic_section_specs"] = cleaned_sections
                changed = True

        surface_contract = cls._data_list_surface_contract(
            prompt=request.prompt,
            format_name=format_name,
            metadata=metadata,
            text_payload=StructuredTextPayload(**{**payload, "metadata": metadata}),
        )
        if surface_contract:
            anchors = cls._data_visualization_anchor_lines(
                metadata.get("proof_points"),
                metadata.get("stat_highlights"),
                metadata.get("claim_evidence_pairs"),
                metadata.get("static_panel_spec"),
                metadata.get("infographic_section_specs"),
                payload.get("body"),
                limit=max(cls._int_or_none(surface_contract.get("module_count")) or 0, 8),
            )
            has_exact_anchor = any(cls._exact_claim_markers(anchor) for anchor in anchors)
            if not has_exact_anchor:
                qualitative_focus = cls._qualitative_data_surface_visual_focus(
                    request=request,
                    format_name=format_name,
                )
                for key in ("visual_focus", "visual_direction", "image_prompt"):
                    if cls._data_visualization_visual_focus(metadata.get(key)):
                        metadata[key] = qualitative_focus
                        changed = True
                panel = metadata.get("static_panel_spec")
                if isinstance(panel, dict) and cls._data_visualization_visual_focus(panel.get("visual_focus")):
                    panel = dict(panel)
                    panel["visual_focus"] = qualitative_focus
                    metadata["static_panel_spec"] = panel
                    changed = True
                sections = metadata.get("infographic_section_specs")
                if isinstance(sections, list):
                    cleaned_sections = []
                    for section in sections:
                        if not isinstance(section, dict):
                            cleaned_sections.append(section)
                            continue
                        section_copy = dict(section)
                        for key in ("visual_focus", "visual_direction", "image_prompt"):
                            if cls._data_visualization_visual_focus(section_copy.get(key)):
                                section_copy[key] = qualitative_focus
                                changed = True
                        cleaned_sections.append(section_copy)
                    if cleaned_sections != sections:
                        metadata["infographic_section_specs"] = cleaned_sections
            if changed:
                metadata["data_surface_exact_values_sanitized"] = bool(unsupported_markers)
                metadata["data_surface_visual_mode"] = "qualitative_until_verified"

        if not changed:
            return text_payload
        payload["metadata"] = metadata
        return StructuredTextPayload(**payload)

    @classmethod
    def _preflight_text_payload_semantics(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        compiled_context: dict[str, Any] | None,
    ) -> StructuredTextPayload:
        # Derives preflight text semantics from request state, copy payload, and compiled context for AIOrchestratorService.
        # It calls _format_name_from_request and _effective_template_surface_policy to turn raw evidence into the structured signal the caller needs.
        format_name = cls._format_name_from_request(request)
        if format_name in {"static", "infographic"}:
            return cls._preflight_static_infographic_text_payload_semantics(
                request=request,
                text_payload=text_payload,
                compiled_context=compiled_context,
                format_name=format_name,
            )
        if format_name != "carousel":
            return text_payload
        payload = text_payload.model_dump(mode="json")
        metadata = deepcopy(payload.get("metadata") if isinstance(payload.get("metadata"), dict) else {})
        slides = [dict(item) for item in metadata.get("carousel_slide_specs") or [] if isinstance(item, dict)]
        if not slides:
            return text_payload
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            metadata=metadata,
        )
        changed = False
        unsupported_global_markers = cls._unsupported_exact_claim_markers(
            " ".join(
                [
                    cls._coerce_text_value(payload.get("headline")),
                    cls._coerce_text_value(payload.get("body")),
                    cls._coerce_text_value(payload.get("cta")),
                    " ".join(cls._normalize_metadata_list(metadata.get("proof_points"), limit=8)),
                    " ".join(cls._normalize_metadata_list(metadata.get("stat_highlights"), limit=8)),
                    " ".join(cls._normalize_metadata_list(metadata.get("trust_builders"), limit=8)),
                    " ".join(
                        cls._claim_evidence_pair_lines(
                            cls._normalize_claim_evidence_pairs(metadata.get("claim_evidence_pairs"), limit=8),
                            limit=8,
                        )
                    ),
                ]
            ),
            request=request,
            compiled_context=compiled_context,
        )
        if unsupported_global_markers:
            for key in ("proof_points", "stat_highlights", "trust_builders"):
                values = cls._normalize_metadata_list(metadata.get(key), limit=8)
                filtered = [line for line in values if not cls._text_contains_unsupported_marker(line, unsupported_global_markers)]
                if filtered != values:
                    metadata[key] = filtered
                    changed = True
            for key in ("body", "cta"):
                cleaned = cls._remove_unsupported_exact_claim_lines(payload.get(key), unsupported_global_markers)
                if cleaned != cls._coerce_text_value(payload.get(key)):
                    payload[key] = cleaned
                    changed = True
        for index, slide in enumerate(slides, start=1):
            sample_contract = cls._sample_editorial_contract_for_slide(
                request=request,
                index=index,
                slide_count=len(slides),
            )
            sample_allows_source_labels = bool(sample_contract.get("sample_page_has_source_labels"))
            headline = cls._normalize_metadata_text(slide.get("headline") or slide.get("title"), limit=120)
            if (
                cls._is_promotional_line(headline)
                or cls._is_generic_carousel_education_label(headline)
                or cls._headline_lacks_topic_specificity(headline, request=request)
            ):
                slide["headline"] = cls._sample_aware_headline_for_slide(
                    request=request,
                    slide=slide,
                    index=index,
                    slide_count=len(slides),
                    compiled_context=compiled_context,
                )
                changed = True
            if index < len(slides) and cls._normalize_metadata_text(slide.get("cta"), limit=90):
                slide["cta"] = ""
                changed = True
            if index == len(slides) and sample_contract and not cls._sample_contract_allows_product_cta(sample_contract):
                if cls._normalize_metadata_text(slide.get("cta"), limit=90):
                    slide["cta"] = ""
                    changed = True
                if cls._is_product_cta_like_line(slide.get("headline"), request=request):
                    slide["headline"] = cls._sample_aware_headline_for_slide(
                        request=request,
                        slide=slide,
                        index=index,
                        slide_count=len(slides),
                        compiled_context=compiled_context,
                    )
                    changed = True

            for key in ("supporting_line", "body"):
                cleaned_value = cls._strip_research_process_filler_lines(
                    slide.get(key),
                    sample_allows_source_labels=sample_allows_source_labels,
                )
                if cleaned_value != slide.get(key):
                    slide[key] = cleaned_value
                    changed = True
            for key in ("body_points", "proof_points", "stat_highlights"):
                cleaned_items = cls._strip_research_process_filler_lines(
                    slide.get(key),
                    sample_allows_source_labels=sample_allows_source_labels,
                )
                if cleaned_items != slide.get(key):
                    slide[key] = cleaned_items
                    changed = True

            expected_module_min = cls._sample_contract_expected_module_min(sample_contract)
            normalized_role = cls._normalize_metadata_text(
                slide.get("slide_role") or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            if (
                template_surface_policy == "style_reference_only"
                and expected_module_min
                and normalized_role not in {"hook", "cover", "opening"}
            ):
                existing_module_lines = cls._carousel_slide_visible_module_lines(slide, limit=8)
                if len(existing_module_lines) < expected_module_min:
                    needed = expected_module_min - len(existing_module_lines)
                    candidates = cls._sample_density_module_candidate_lines(
                        slide=slide,
                        metadata=metadata,
                        text_payload=text_payload,
                        request=request,
                        compiled_context=compiled_context,
                        sample_contract=sample_contract,
                        limit=expected_module_min + 3,
                    )
                    body_points = cls._normalize_metadata_list(slide.get("body_points"), limit=8)
                    seen = {item.casefold() for item in [*existing_module_lines, *body_points]}
                    added = 0
                    for candidate in candidates:
                        key = candidate.casefold()
                        if key in seen:
                            continue
                        body_points.append(candidate)
                        seen.add(key)
                        added += 1
                        if added >= needed:
                            break
                    if added:
                        slide["body_points"] = body_points[:8]
                        slide["sample_density_preflight_filled"] = True
                        changed = True

            unsupported = cls._unsupported_exact_claim_markers(
                " ".join(
                    [
                        cls._coerce_text_value(slide.get("headline")),
                        cls._coerce_text_value(slide.get("supporting_line")),
                        cls._coerce_text_value(slide.get("body")),
                        " ".join(cls._normalize_metadata_list(slide.get("body_points"), limit=8)),
                        " ".join(cls._normalize_metadata_list(slide.get("proof_points"), limit=8)),
                        " ".join(cls._normalize_metadata_list(slide.get("stat_highlights"), limit=8)),
                    ]
                ),
                request=request,
                compiled_context=compiled_context,
            )
            if unsupported:
                for key in ("supporting_line", "body"):
                    cleaned = cls._remove_unsupported_exact_claim_lines(slide.get(key), unsupported)
                    if cleaned != cls._coerce_text_value(slide.get(key)):
                        slide[key] = cleaned
                        changed = True
                for key in ("body_points", "proof_points", "stat_highlights"):
                    values = cls._normalize_metadata_list(slide.get(key), limit=8)
                    filtered = [line for line in values if not cls._text_contains_unsupported_marker(line, unsupported)]
                    if filtered != values:
                        slide[key] = filtered
                        changed = True
                visual_focus = cls._normalize_metadata_text(slide.get("visual_focus"), limit=420)
                if cls._data_visualization_visual_focus(visual_focus):
                    if template_surface_policy == "style_reference_only":
                        visual_slide = dict(slide)
                        visual_metadata = dict(visual_slide.get("metadata") if isinstance(visual_slide.get("metadata"), dict) else {})
                        visual_metadata.update(sample_contract)
                        visual_metadata.setdefault("reference_slide_index", index)
                        visual_slide["metadata"] = visual_metadata
                        slide["visual_focus"] = cls._sample_matched_visual_focus(visual_slide, request=request)
                    else:
                        slide["visual_focus"] = (
                            "Sample-matched qualitative evidence modules using only approved facts; avoid invented chart values, "
                            "unverified axes, or numeric graph labels."
                        )
                    changed = True
            slides[index - 1] = slide

        if not changed:
            return text_payload
        metadata["carousel_slide_specs"] = slides
        payload["metadata"] = metadata
        return StructuredTextPayload(**payload)

    @classmethod
    def _merge_targeted_carousel_rewrite_payload(
        cls,
        *,
        previous_payload: StructuredTextPayload,
        rewritten_payload: dict[str, Any],
        revision_scope: dict[str, Any],
    ) -> dict[str, Any]:
        # Merges targeted carousel rewrite from previous payload, rewritten payload, and revision scope for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(rewritten_payload, dict):
            return rewritten_payload
        if not bool(revision_scope.get("only_targeted")):
            return rewritten_payload
        previous_metadata = previous_payload.metadata if isinstance(previous_payload.metadata, dict) else {}
        rewritten_metadata = rewritten_payload.get("metadata") if isinstance(rewritten_payload.get("metadata"), dict) else {}
        previous_slides = [
            dict(item)
            for item in (previous_metadata.get("carousel_slide_specs") or previous_metadata.get("slides") or [])
            if isinstance(item, dict)
        ]
        rewritten_slides = [
            dict(item)
            for item in (rewritten_metadata.get("carousel_slide_specs") or rewritten_metadata.get("slides") or [])
            if isinstance(item, dict)
        ]
        if not previous_slides or not rewritten_slides:
            return rewritten_payload
        rewritten_payload = cls._preserve_carousel_rewrite_exact_claims(
            previous_slides=previous_slides,
            rewritten_payload=rewritten_payload,
            rewritten_slides=rewritten_slides,
        )
        rewritten_metadata = rewritten_payload.get("metadata") if isinstance(rewritten_payload.get("metadata"), dict) else {}
        rewritten_slides = [
            dict(item)
            for item in (rewritten_metadata.get("carousel_slide_specs") or rewritten_metadata.get("slides") or [])
            if isinstance(item, dict)
        ]
        if len(rewritten_slides) >= len(previous_slides):
            return rewritten_payload
        slide_indexes = [
            int(value)
            for value in revision_scope.get("slide_indexes") or []
            if isinstance(value, int) or (isinstance(value, str) and value.isdigit())
        ]
        merged_slides = [dict(item) for item in previous_slides]
        for offset, rewritten_slide in enumerate(rewritten_slides):
            slide_number = (
                cls._int_or_none(rewritten_slide.get("slide_number"))
                or cls._int_or_none(rewritten_slide.get("slide_index"))
                or (slide_indexes[offset] if offset < len(slide_indexes) else None)
            )
            if not slide_number or slide_number < 1 or slide_number > len(merged_slides):
                role = cls._normalize_metadata_text(
                    rewritten_slide.get("slide_role") or rewritten_slide.get("role"),
                    limit=48,
                ).casefold().replace(" ", "_")
                for candidate_index, existing_slide in enumerate(merged_slides, start=1):
                    existing_role = cls._normalize_metadata_text(
                        existing_slide.get("slide_role") or existing_slide.get("role"),
                        limit=48,
                    ).casefold().replace(" ", "_")
                    if role and role == existing_role:
                        slide_number = candidate_index
                        break
            if not slide_number or slide_number < 1 or slide_number > len(merged_slides):
                continue
            original_slide = dict(merged_slides[slide_number - 1])
            merged_slides[slide_number - 1] = {
                **original_slide,
                **rewritten_slide,
                "slide_number": slide_number,
            }
        merged_payload = dict(rewritten_payload)
        merged_metadata = {
            **previous_metadata,
            **rewritten_metadata,
            "carousel_slide_specs": merged_slides,
        }
        merged_payload["metadata"] = merged_metadata
        return merged_payload

    @classmethod
    def _preserve_carousel_rewrite_exact_claims(
        cls,
        *,
        previous_slides: list[dict[str, Any]],
        rewritten_payload: dict[str, Any],
        rewritten_slides: list[dict[str, Any]],
    ) -> dict[str, Any]:
        # Centralizes preserve carousel rewrite exact claims from previous slides, rewritten payload, and rewritten slides for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        previous_by_number: dict[int, dict[str, Any]] = {}
        for index, slide in enumerate(previous_slides, start=1):
            slide_number = cls._int_or_none(slide.get("slide_number")) or cls._int_or_none(slide.get("slide_index")) or index
            previous_by_number[slide_number] = slide

        preserved_slides: list[dict[str, Any]] = []
        for index, rewritten_slide in enumerate(rewritten_slides, start=1):
            slide_number = (
                cls._int_or_none(rewritten_slide.get("slide_number"))
                or cls._int_or_none(rewritten_slide.get("slide_index"))
                or index
            )
            previous_slide = previous_by_number.get(slide_number) or (previous_slides[index - 1] if index - 1 < len(previous_slides) else {})
            preserved_slides.append(
                cls._preserve_carousel_slide_exact_claims(
                    previous_slide=previous_slide,
                    rewritten_slide=rewritten_slide,
                )
            )

        merged_payload = dict(rewritten_payload)
        for key in ("headline", "body", "cta"):
            if key in merged_payload:
                merged_payload[key] = cls._restore_corrupted_currency_claims(
                    previous_source=previous_slides,
                    rewritten_value=merged_payload.get(key),
                )
        metadata = dict(merged_payload.get("metadata") if isinstance(merged_payload.get("metadata"), dict) else {})
        metadata["carousel_slide_specs"] = preserved_slides
        for key in ("proof_points", "body_points", "stat_highlights", "claim_evidence_pairs"):
            if key in metadata:
                metadata[key] = cls._restore_corrupted_currency_claims(
                    previous_source=previous_slides,
                    rewritten_value=metadata.get(key),
                )
        merged_payload["metadata"] = metadata
        return merged_payload

    @classmethod
    def _preserve_carousel_slide_exact_claims(
        cls,
        *,
        previous_slide: dict[str, Any],
        rewritten_slide: dict[str, Any],
    ) -> dict[str, Any]:
        # Centralizes preserve carousel slide exact claims from previous slide and rewritten slide for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not previous_slide:
            return dict(rewritten_slide)
        preserved = dict(rewritten_slide)
        for key in ("headline", "supporting_line", "body", "cta"):
            if key in preserved:
                preserved[key] = cls._restore_corrupted_currency_claims(
                    previous_source=previous_slide,
                    rewritten_value=preserved.get(key),
                )
            if key in previous_slide and key in preserved:
                preserved[key] = cls._preserve_rewrite_exact_claim_value(
                    previous_value=previous_slide.get(key),
                    rewritten_value=preserved.get(key),
                )
        for key in ("body_points", "proof_points", "stat_highlights"):
            if key in preserved:
                preserved[key] = cls._restore_corrupted_currency_claims(
                    previous_source=previous_slide,
                    rewritten_value=preserved.get(key),
                )
            if key in previous_slide and key in preserved:
                preserved[key] = cls._preserve_rewrite_exact_claim_value(
                    previous_value=previous_slide.get(key),
                    rewritten_value=preserved.get(key),
                )
        if "claim_evidence_pairs" in preserved:
            preserved["claim_evidence_pairs"] = cls._restore_corrupted_currency_claims(
                previous_source=previous_slide,
                rewritten_value=preserved.get("claim_evidence_pairs"),
            )
        if "claim_evidence_pairs" in previous_slide and "claim_evidence_pairs" in preserved:
            preserved["claim_evidence_pairs"] = cls._preserve_rewrite_exact_claim_value(
                previous_value=previous_slide.get("claim_evidence_pairs"),
                rewritten_value=preserved.get("claim_evidence_pairs"),
            )
        return preserved

    @classmethod
    def _restore_corrupted_currency_claims(cls, *, previous_source: Any, rewritten_value: Any) -> Any:
        # Centralizes restore corrupted currency claims from previous source and rewritten value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        currency_map = cls._currency_amount_display_map(previous_source)
        if not currency_map:
            return rewritten_value
        if isinstance(rewritten_value, str):
            return cls._restore_corrupted_currency_text(rewritten_value, currency_map=currency_map)
        if isinstance(rewritten_value, list):
            return [
                cls._restore_corrupted_currency_claims(previous_source=previous_source, rewritten_value=item)
                for item in rewritten_value
            ]
        if isinstance(rewritten_value, dict):
            restored = dict(rewritten_value)
            for key, value in list(restored.items()):
                restored[key] = cls._restore_corrupted_currency_claims(
                    previous_source=previous_source,
                    rewritten_value=value,
                )
            return restored
        return rewritten_value

    @classmethod
    def _currency_amount_display_map(cls, value: Any) -> dict[str, str]:
        # Centralizes currency amount display map from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._raw_text_for_exact_claims(value)
        if not text:
            return {}
        pattern = re.compile(
            r"(?<!\w)(?:\u20b9|â‚¹|rs\.?|inr|usd|us\$|nzd|nz\$|\$)\s?(?P<amount>\d[\d,]*(?:\.\d+)?)(?:\s?(?:billion|million|crore|lakh|trillion))?",
            flags=re.IGNORECASE,
        )
        amounts: dict[str, str] = {}
        for match in pattern.finditer(text):
            amount_digits = re.sub(r"[^\d.]", "", match.group("amount") or "")
            display = match.group(0).strip()
            if amount_digits and display:
                amounts.setdefault(amount_digits, display)
        return amounts

    @classmethod
    def _restore_corrupted_currency_text(cls, text: str, *, currency_map: dict[str, str]) -> str:
        # Extracts restore corrupted currency text from text and currency map for AIOrchestratorService.
        # The extracted signal becomes prompt context, metadata, or ranking input.
        if not text or not currency_map:
            return text

        def replace(match: re.Match[str]) -> str:
            # Centralizes replace from match for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            raw = match.group(0)
            digits = re.sub(r"[^\d.]", "", raw)
            if not digits.startswith("9") or len(digits) <= 1:
                return raw
            replacement = currency_map.get(digits[1:])
            return replacement or raw

        return re.sub(
            r"(?<!\d)9\d[\d,]*(?:\.\d+)?(?:\s?(?:billion|million|crore|lakh|trillion))?(?!\d)",
            replace,
            text,
            flags=re.IGNORECASE,
        )

    @classmethod
    def _preserve_rewrite_exact_claim_value(cls, *, previous_value: Any, rewritten_value: Any) -> Any:
        # Centralizes preserve rewrite exact claim from previous value and rewritten value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        previous_markers = cls._exact_claim_markers(previous_value)
        if not previous_markers:
            return rewritten_value
        rewritten_markers = cls._exact_claim_markers(rewritten_value)
        if previous_markers.issubset(rewritten_markers):
            return rewritten_value
        return previous_value

    @classmethod
    def _validate_content_semantics(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        compiled_context: dict[str, Any] | None = None,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> dict[str, Any]:
        # Validates content semantics from request state, copy payload, and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        format_name = cls._format_name_from_request(request)
        if format_name not in {"carousel", "infographic", "static"}:
            return {
                "status": "clean",
                "repairable": False,
                "format": format_name,
                "issues": [],
                "summary": [],
                "targeted_fields": [],
                "revision_scope": {},
            }

        issues: list[dict[str, Any]] = []
        metadata = text_payload.metadata or {}

        if format_name == "carousel":
            hook_type = cls._normalize_metadata_text(metadata.get("hook_type"), limit=48)
            claim_evidence_pairs = cls._normalize_claim_evidence_pairs(
                metadata.get("claim_evidence_pairs"),
                limit=6,
            )
            # Carousel copy needs an explicit hook strategy so repair prompts can ask for a sharper narrative opening.
            if not hook_type:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="carousel_missing_campaign_hook_type",
                        message="The carousel does not expose a clear campaign hook strategy such as problem-led, proof-led, contrast-led, or myth-busting.",
                        targeted_fields=["metadata"],
                    )
                )
            has_research_fact_context = cls._has_research_fact_context(
                request=request,
                compiled_context=compiled_context,
            )
            # If verified facts exist, claims must be paired with evidence rather than left as unsupported marketing copy.
            if not claim_evidence_pairs and has_research_fact_context:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="carousel_missing_claim_evidence_pairs",
                        message="The carousel has research context but no claim_evidence_pairs tying strategic claims to support.",
                        targeted_fields=["metadata"],
                    )
                )
            unsupported_global_markers = cls._unsupported_exact_claim_markers(
                " ".join(
                    [
                        text_payload.headline,
                        text_payload.body,
                        text_payload.cta,
                        *cls._normalize_metadata_list(metadata.get("proof_points"), limit=6),
                        *cls._normalize_metadata_list(metadata.get("stat_highlights"), limit=6),
                    ]
                ),
                request=request,
                compiled_context=compiled_context,
            )
            if unsupported_global_markers:
                # Exact numbers and dates are high-risk, so unsupported markers trigger repair before image generation.
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="carousel_unsupported_exact_claim",
                        message=(
                            "The carousel includes exact numeric/date/currency claims that are not present in verified facts or the user prompt: "
                            + ", ".join(sorted(unsupported_global_markers)[:4])
                        ),
                        targeted_fields=["body", "metadata"],
                    )
                )
            raw_structured_slides = [dict(item) for item in (metadata.get("carousel_slide_specs") or []) if isinstance(item, dict)]
            template_surface_policy = cls._effective_template_surface_policy(
                request,
                creative_decision=creative_decision,
                metadata=metadata if isinstance(metadata, dict) else {},
            )
            sequence_pack = cls._template_sequence_pack(
                request,
                creative_decision=creative_decision,
            )
            sequence_pack_slide_count = (
                cls._int_or_none(sequence_pack.get("slide_count"))
                if isinstance(sequence_pack, dict)
                else None
            ) or (
                len([item for item in sequence_pack.get("slides", []) if isinstance(item, dict)])
                if isinstance(sequence_pack, dict)
                else 0
            )
            # Style-reference carousels map one generated slide to one sample page unless the user asked for a different count.
            raw_sequence_count_matches_sample = not (
                template_surface_policy == "style_reference_only"
                and sequence_pack_slide_count
                and raw_structured_slides
                and len(raw_structured_slides) != sequence_pack_slide_count
                and not cls._explicit_requested_slide_count(request)
            )
            if (
                template_surface_policy == "style_reference_only"
                and sequence_pack_slide_count
                and raw_structured_slides
                and len(raw_structured_slides) != sequence_pack_slide_count
                and not cls._explicit_requested_slide_count(request)
            ):
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="carousel_sample_sequence_count_drift",
                        message=(
                            "Generated carousel slide count does not match the selected sample/reference sequence count; "
                            "for style-reference carousels each output slide must map to the same-position sample page unless the user explicitly requested a different count."
                        ),
                        targeted_fields=["metadata"],
                    )
                )
            normalized_slides_for_raw_checks = cls._build_carousel_slide_specs(
                text_payload,
                request=request,
                creative_decision=creative_decision,
            )
            normalized_slide_by_index = {
                index: slide
                for index, slide in enumerate(normalized_slides_for_raw_checks, start=1)
                if isinstance(slide, dict)
            }
            raw_seen_supports: set[str] = set()
            # Raw slide checks catch model output problems before normalization hides or repairs them.
            for index, raw_slide in enumerate(raw_structured_slides, start=1):
                normalized_slide = normalized_slide_by_index.get(index) or {}
                raw_sample_contract = cls._sample_editorial_contract_for_slide(
                    request=request,
                    index=index,
                    slide_count=len(raw_structured_slides),
                )
                raw_sample_allows_source_labels = bool(raw_sample_contract.get("sample_page_has_source_labels"))
                raw_story_role = cls._normalize_metadata_text(
                    raw_slide.get("slide_role") or raw_slide.get("role"),
                    limit=48,
                ).casefold().replace(" ", "_")
                raw_headline = cls._normalize_metadata_text(
                    raw_slide.get("headline") or raw_slide.get("title"),
                    limit=120,
                )
                raw_support = cls._normalize_metadata_text(
                    raw_slide.get("supporting_line") or raw_slide.get("body"),
                    limit=220,
                )
                raw_cta = cls._normalize_metadata_text(raw_slide.get("cta"), limit=90)
                normalized_headline = cls._normalize_metadata_text(
                    normalized_slide.get("headline"),
                    limit=120,
                )
                normalized_cta = cls._normalize_metadata_text(normalized_slide.get("cta"), limit=90)
                normalized_visual_focus = normalized_slide.get("visual_focus")
                raw_visual_focus = raw_slide.get("visual_focus")
                raw_visible_lines = [
                    raw_headline,
                    raw_support,
                    cls._coerce_text_value(raw_slide.get("body")),
                    *cls._normalize_metadata_list(raw_slide.get("body_points"), limit=8),
                    *cls._normalize_metadata_list(raw_slide.get("proof_points"), limit=8),
                    *cls._normalize_metadata_list(raw_slide.get("stat_highlights"), limit=8),
                ]
                raw_module_lines = cls._dedupe_metadata_collection(
                    [
                        *cls._normalize_metadata_list(raw_slide.get("proof_points"), limit=8),
                        *cls._normalize_metadata_list(raw_slide.get("body_points"), limit=8),
                        *cls._normalize_metadata_list(raw_slide.get("stat_highlights"), limit=8),
                    ],
                    blocked_texts=[raw_headline, raw_support, raw_cta],
                    limit=8,
                )
                if index < len(raw_structured_slides) and raw_cta and normalized_cta:
                    # Interior CTAs break the carousel arc; only the final slide should close or convert.
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_interior_cta",
                            message=f"Generated slide {index} still includes CTA copy before the closing slide.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if (
                    index == len(raw_structured_slides)
                    and raw_sample_contract
                    and not cls._sample_contract_allows_product_cta(raw_sample_contract)
                    and (raw_cta or cls._is_product_cta_like_line(raw_headline, request=request))
                    and (
                        not raw_sequence_count_matches_sample
                        or normalized_cta
                        or cls._is_product_cta_like_line(normalized_headline, request=request)
                    )
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_sample_closing_cta_drift",
                            message=f"Generated slide {index} turns a sample macro/editorial close into product or platform promotion.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if cls._is_promotional_line(raw_headline) or cls._is_generic_carousel_education_label(raw_headline):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_generic_headline",
                            message=f"Generated slide {index} uses a generic or CTA-like headline instead of a specific editorial label.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if cls._visual_focus_is_raw_reference_payload(raw_visual_focus):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_reference_visual_focus",
                            message=f"Generated slide {index} uses a raw reference image/path as visual_focus instead of a content-specific visual direction.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if (
                    template_surface_policy == "style_reference_only"
                    and raw_sample_contract
                    and cls._style_reference_visual_focus_needs_sample_reset(
                        raw_visual_focus,
                        request=request,
                        sample_allows_data_or_product=cls._sample_contract_allows_product_cta(raw_sample_contract),
                    )
                    and (
                        not raw_sequence_count_matches_sample
                        or cls._style_reference_visual_focus_needs_sample_reset(
                            normalized_visual_focus,
                            request=request,
                            sample_allows_data_or_product=cls._sample_page_visualizes_data_or_product(normalized_slide)
                            or cls._sample_contract_allows_product_cta(raw_sample_contract),
                        )
                    )
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_sample_disallowed_visual_system",
                            message=f"Generated slide {index} asks for chart/dashboard/laptop/product-style imagery not permitted by the selected sample page grammar.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                raw_expected_module_min = cls._sample_contract_expected_module_min(raw_sample_contract)
                if (
                    template_surface_policy == "style_reference_only"
                    and raw_expected_module_min
                    and raw_story_role not in {"hook", "cover", "opening"}
                    and len(raw_module_lines) < raw_expected_module_min
                ):
                    # Sample-led slides must preserve the sample's density so adapted pages do not collapse into sparse posters.
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_sample_density_drift",
                            message=f"Generated slide {index} is too thin for the selected sample page's copy density and module rhythm.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if cls._headline_lacks_topic_specificity(raw_headline, request=request):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_generic_sample_headline",
                            message=f"Generated slide {index} uses sample-grammar headline copy without adapting it to the user's topic or audience.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if raw_support and raw_support.casefold() in raw_seen_supports and index > 1:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_raw_repeated_support",
                            message=f"Generated slide {index} repeats supporting copy from an earlier slide.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
                if raw_support:
                    raw_seen_supports.add(raw_support.casefold())
                if (
                    not raw_sample_allows_source_labels
                    and any(cls._line_is_research_process_filler(line) for line in raw_visible_lines)
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_research_process_filler",
                            message=f"Generated slide {index} uses source-process wording as visible creative copy instead of turning evidence into brand-facing insight.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[raw_story_role or f"slide_{index}"],
                        )
                    )
            slides = cls._build_carousel_slide_specs(
                text_payload,
                request=request,
                creative_decision=creative_decision,
            )
            # Normalized slides are checked after raw checks because this is the sequence the renderer will actually consume.
            if len(slides) < 3:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="carousel_too_short",
                        message="The carousel sequence is too short to carry a real hook, middle progression, and close.",
                        targeted_fields=["body", "metadata"],
                    )
                )
            expected_roles = cls._carousel_expected_story_roles(
                request=request,
                metadata=metadata,
                fallback_slides=slides,
                slide_count=len(slides),
            )
            actual_roles = [
                cls._normalize_metadata_text(
                    ((slide.get("metadata") or {}).get("story_role") if isinstance(slide.get("metadata"), dict) else slide.get("role")),
                    limit=48,
                ).casefold().replace(" ", "_")
                for slide in slides
            ]
            # Expected roles ensure the generated sequence has a real campaign progression, not repeated standalone panels.
            for normalized_role in [
                cls._normalize_metadata_text(role, limit=48).casefold().replace(" ", "_")
                for role in expected_roles
            ]:
                if normalized_role and normalized_role not in actual_roles:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_missing_story_role",
                            message=f"The carousel is missing a clear {normalized_role} slide in the intended sequence.",
                            targeted_fields=["body", "metadata"],
                            slide_targets=[normalized_role],
                        )
                    )
            seen_supports: set[str] = set()
            for index, slide in enumerate(slides, start=1):
                slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                sample_contract = cls._sample_editorial_contract_for_slide(
                    request=request,
                    index=index,
                    slide_count=len(slides),
                )
                story_role = cls._normalize_metadata_text(
                    slide_metadata.get("story_role") or slide.get("role"),
                    limit=48,
                ).casefold().replace(" ", "_")
                support = cls._normalize_metadata_text(slide.get("supporting_line"), limit=220)
                headline = cls._normalize_metadata_text(slide.get("headline"), limit=120)
                proof_points = cls._normalize_metadata_list(slide.get("proof_points"), limit=3)
                body_points = cls._normalize_metadata_list(slide.get("body_points"), limit=4)
                stat_highlights = cls._normalize_metadata_list(slide.get("stat_highlights"), limit=4)
                claim_pairs = cls._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=2)
                raw_slide_visual_focus = slide.get("visual_focus")
                visual_focus = cls._normalize_metadata_text(raw_slide_visual_focus, limit=420)
                body_text_for_evidence = cls._carousel_slide_body_text(slide, fallback_text=support)
                data_visual_anchors = cls._data_visualization_anchor_lines(
                    stat_highlights,
                    claim_pairs,
                    proof_points,
                    body_points,
                    support,
                    body_text_for_evidence,
                    limit=8,
                )
                slide_text_for_facts = " ".join(
                    [
                        headline,
                        support,
                        body_text_for_evidence,
                        *proof_points,
                        *body_points,
                        *stat_highlights,
                        *cls._claim_evidence_pair_lines(claim_pairs, limit=2),
                    ]
                )
                if (
                    not bool(sample_contract.get("sample_page_has_source_labels"))
                    and any(
                        cls._line_is_research_process_filler(line)
                        for line in [
                            headline,
                            support,
                            body_text_for_evidence,
                            *proof_points,
                            *body_points,
                            *stat_highlights,
                        ]
                    )
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_research_process_filler",
                            message=f"Slide {index} uses source-process wording as visible creative copy instead of brand-facing insight.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                unsupported_slide_markers = cls._unsupported_exact_claim_markers(
                    slide_text_for_facts,
                    request=request,
                    compiled_context=compiled_context,
                )
                if index < len(slides) and cls._normalize_metadata_text(slide.get("cta"), limit=90):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_interior_cta",
                            message=f"Slide {index} still contains CTA treatment before the closing slide.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if cls._headline_lacks_topic_specificity(headline, request=request):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_generic_sample_headline",
                            message=f"Slide {index} uses sample-grammar headline copy without a concrete topic, tension, or audience payoff.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if support and support.casefold() in seen_supports and story_role not in {"hook", "closing", "takeaway"}:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_repeated_support",
                            message=f"Slide {index} repeats supporting copy instead of carrying a distinct idea.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if support:
                    seen_supports.add(support.casefold())
                if index > 1 and cls._is_generic_carousel_education_label(headline):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_generic_headline",
                            message=f"Slide {index} still uses a generic educational label instead of a specific editorial job.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if story_role in {"structure", "undercovered_angle", "strategic_meaning", "analysis", "what_matters"} and not (
                    claim_pairs
                    or proof_points
                    or body_points
                    or stat_highlights
                    or cls._content_line_is_evidence_anchor(body_text_for_evidence or support, request=request)
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_missing_evidence_anchor",
                            message=f"Slide {index} lacks a concrete evidence anchor for its analytical role.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                data_visual_requested = cls._data_visualization_requested(
                    visual_focus,
                    slide_metadata.get("visual_direction"),
                    slide_metadata.get("image_prompt"),
                )
                if data_visual_requested and not cls._data_visualization_has_content_anchor(
                    request=request,
                    anchors=data_visual_anchors,
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_unbacked_data_visual",
                            message=f"Slide {index} asks for a table/chart/graph/dashboard-style visual without content anchors that define what the visual should show.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if (
                    data_visual_requested
                    and cls._numeric_data_visualization_requested(visual_focus, slide_metadata.get("image_prompt"))
                    and not any(cls._exact_claim_markers(anchor) for anchor in data_visual_anchors)
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_unsupported_numeric_data_visual",
                            message=f"Slide {index} implies a numeric chart/axis/graph but has no approved exact values to chart; use qualitative modules or evidence cards instead.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if data_visual_requested and unsupported_slide_markers:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_data_visual_unsupported_claims",
                            message=f"Slide {index} data visual would rely on exact claims not backed by verified facts or the user prompt.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if unsupported_slide_markers:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_slide_unsupported_exact_claim",
                            message=(
                                f"Slide {index} includes exact claims not backed by verified facts or the user prompt: "
                                + ", ".join(sorted(unsupported_slide_markers)[:3])
                            ),
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if cls._visual_focus_is_weak_for_carousel(visual_focus):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_weak_visual_focus",
                            message=f"Slide {index} lacks a specific 3D/2D/product/data/evidence visual direction tied to its story role and brand/sample style.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if (
                    template_surface_policy == "style_reference_only"
                    and sample_contract
                    and cls._style_reference_visual_focus_needs_sample_reset(
                        visual_focus,
                        request=request,
                        sample_allows_data_or_product=cls._sample_page_visualizes_data_or_product(slide)
                        or cls._sample_contract_allows_product_cta(sample_contract),
                    )
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_sample_disallowed_visual_system",
                            message=f"Slide {index} asks for chart/dashboard/laptop/product-style imagery that the selected sample page does not authorize.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if cls._visual_focus_is_raw_reference_payload(raw_slide_visual_focus):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_reference_visual_focus",
                            message=f"Slide {index} still uses a raw reference image/path as visual_focus instead of a content-specific visual direction.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                if story_role in {"hook", "cover", "opening", "title"} and len(headline.split()) < 4:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_weak_entry_hook",
                            message=f"Slide {index} hook headline is too thin to create strategic reader tension.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )
                closing_allows_product_cta = cls._sample_contract_allows_product_cta(sample_contract)
                closing_needs_cta = closing_allows_product_cta or not sample_contract
                if story_role in {"takeaway", "close", "closing", "cta", "final"} and (
                    (closing_needs_cta and not cls._normalize_metadata_text(slide.get("cta"), limit=90))
                    or (not closing_allows_product_cta and (cls._is_product_cta_like_line(headline, request=request) or cls._normalize_metadata_text(slide.get("cta"), limit=90)))
                    or (closing_allows_product_cta and cls._is_promotional_line(headline))
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="carousel_weak_closing_payoff",
                            message=(
                                f"Slide {index} closing slide drifts from the selected sample's closing grammar."
                                if sample_contract
                                else f"Slide {index} closing slide lacks a distinct decision payoff and final CTA posture."
                            ),
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[story_role],
                        )
                    )

        elif format_name == "infographic":
            sections = [dict(item) for item in (metadata.get("infographic_section_specs") or []) if isinstance(item, dict)]
            surface_contract = cls._data_list_surface_contract(
                prompt=request.prompt,
                format_name=format_name,
                metadata=metadata,
                text_payload=text_payload,
            )
            requested_count = cls._int_or_none((surface_contract or {}).get("requested_count")) or 0
            module_count = cls._int_or_none((surface_contract or {}).get("module_count")) or requested_count
            surface_rows = cls._data_visualization_anchor_lines(
                metadata.get("proof_points"),
                metadata.get("stat_highlights"),
                metadata.get("claim_evidence_pairs"),
                metadata.get("infographic_section_specs"),
                limit=max(module_count, 8),
            )
            if surface_contract and requested_count and len(surface_rows) < min(requested_count, module_count or requested_count):
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="infographic_missing_requested_ranking_rows",
                        message=f"The infographic request asks for {requested_count} list/ranking item(s), but the structured metadata only provides {len(surface_rows)} usable row anchor(s).",
                        targeted_fields=["body", "metadata"],
                    )
                )
            unsupported_infographic_markers = cls._unsupported_exact_claim_markers(
                " ".join([text_payload.headline, text_payload.body, text_payload.cta, *surface_rows]),
                request=request,
                compiled_context=compiled_context,
            )
            if unsupported_infographic_markers:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="infographic_unsupported_exact_claim",
                        message=(
                            "The infographic includes exact numeric/date/currency claims that are not present in verified facts or the user prompt: "
                            + ", ".join(sorted(unsupported_infographic_markers)[:4])
                        ),
                        targeted_fields=["body", "metadata"],
                    )
                )
            if len(sections) < 2:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="infographic_too_flat",
                        message="The infographic still reads like one flat message instead of a structured sectioned explainer.",
                        targeted_fields=["body", "metadata"],
                    )
                )
            evidence_sections = 0
            seen_section_heads: set[str] = set()
            for index, section in enumerate(sections, start=1):
                role = cls._normalize_metadata_text(section.get("section_role"), limit=48).casefold().replace(" ", "_")
                headline = cls._normalize_metadata_text(section.get("headline") or section.get("section_label"), limit=120)
                claim_pairs = cls._normalize_claim_evidence_pairs(section.get("claim_evidence_pairs"), limit=2)
                proof_points = cls._normalize_metadata_list(section.get("proof_points"), limit=3)
                body_points = cls._normalize_metadata_list(section.get("body_points"), limit=4)
                stat_highlights = cls._normalize_metadata_list(section.get("stat_highlights"), limit=3)
                visual_focus = cls._normalize_metadata_text(section.get("visual_focus"), limit=420)
                data_visual_anchors = cls._data_visualization_anchor_lines(
                    stat_highlights,
                    claim_pairs,
                    proof_points,
                    body_points,
                    section.get("body"),
                    headline,
                    limit=8,
                )
                section_text_for_facts = " ".join(
                    [
                        headline,
                        cls._coerce_text_value(section.get("body")),
                        *proof_points,
                        *body_points,
                        *stat_highlights,
                        *cls._claim_evidence_pair_lines(claim_pairs, limit=2),
                    ]
                )
                unsupported_section_markers = cls._unsupported_exact_claim_markers(
                    section_text_for_facts,
                    request=request,
                    compiled_context=compiled_context,
                )
                if role == "evidence":
                    evidence_sections += 1
                if headline and headline.casefold() in seen_section_heads and role not in {"overview", "takeaway"}:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="infographic_repeated_section",
                            message=f"Section {index} repeats a prior section instead of advancing the explainer.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[role],
                        )
                    )
                if headline:
                    seen_section_heads.add(headline.casefold())
                if role in {"evidence", "detail", "analysis", "takeaway"} and not (claim_pairs or proof_points):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="infographic_missing_evidence",
                            message=f"Section {index} lacks a distinct proof or claim/evidence anchor.",
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[role],
                        )
                    )
                if unsupported_section_markers:
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="infographic_section_unsupported_exact_claim",
                            message=(
                                f"Section {index} includes exact claims not backed by verified facts or the user prompt: "
                                + ", ".join(sorted(unsupported_section_markers)[:3])
                            ),
                            targeted_fields=["body", "metadata"],
                            slide_indexes=[index],
                            slide_targets=[role],
                        )
                    )
                data_visual_requested = cls._data_visualization_requested(
                    visual_focus,
                    section.get("visual_direction"),
                    section.get("image_prompt"),
                )
                if data_visual_requested and not cls._data_visualization_has_content_anchor(
                    request=request,
                    anchors=data_visual_anchors,
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="infographic_unbacked_data_visual",
                            message=f"Section {index} asks for a table/chart/graph/dashboard-style visual without content anchors that define what it should show.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[role],
                        )
                    )
                if (
                    data_visual_requested
                    and cls._numeric_data_visualization_requested(visual_focus, section.get("image_prompt"))
                    and not any(cls._exact_claim_markers(anchor) for anchor in data_visual_anchors)
                ):
                    issues.append(
                        cls._clean_content_semantic_issue(
                            code="infographic_unsupported_numeric_data_visual",
                            message=f"Section {index} implies a numeric chart/axis/graph but has no approved exact values to chart; use qualitative modules or evidence cards instead.",
                            targeted_fields=["metadata"],
                            slide_indexes=[index],
                            slide_targets=[role],
                        )
                    )
            if sections and evidence_sections == 0:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="infographic_missing_evidence_section",
                        message="The infographic has no clear evidence section to carry the factual payload.",
                        targeted_fields=["body", "metadata"],
                        slide_targets=["evidence"],
                    )
                )

        elif format_name == "static":
            panel = metadata.get("static_panel_spec") if isinstance(metadata.get("static_panel_spec"), dict) else {}
            dominant_message = cls._normalize_metadata_text(panel.get("dominant_message"), limit=180)
            supporting_line = cls._normalize_metadata_text(metadata.get("supporting_line"), limit=180)
            surface_contract = cls._data_list_surface_contract(
                prompt=request.prompt,
                format_name=format_name,
                metadata=metadata,
                text_payload=text_payload,
            )
            requested_count = cls._int_or_none((surface_contract or {}).get("requested_count")) or 0
            module_count = cls._int_or_none((surface_contract or {}).get("module_count")) or requested_count
            row_limit = max(module_count, 8)
            proof_points = cls._normalize_metadata_list(
                metadata.get("proof_points") or panel.get("proof_points"),
                limit=row_limit,
            )
            claim_pairs = cls._normalize_claim_evidence_pairs(
                panel.get("claim_evidence_pairs") or metadata.get("claim_evidence_pairs"),
                limit=row_limit,
            )
            stat_highlights = cls._normalize_metadata_list(
                panel.get("stat_highlights") or metadata.get("stat_highlights"),
                limit=row_limit,
            )
            visual_focus = cls._normalize_metadata_text(
                panel.get("visual_focus")
                or metadata.get("visual_focus")
                or metadata.get("visual_direction")
                or metadata.get("image_prompt"),
                limit=420,
            )
            data_visual_anchors = cls._data_visualization_anchor_lines(
                stat_highlights,
                claim_pairs,
                proof_points,
                panel.get("supporting_lines"),
                supporting_line,
                dominant_message,
                text_payload.body,
                metadata.get("static_panel_spec"),
                limit=row_limit,
            )
            if surface_contract and requested_count and len(data_visual_anchors) < min(requested_count, module_count or requested_count):
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_missing_requested_ranking_rows",
                        message=f"The static request asks for {requested_count} list/ranking item(s), but the structured metadata only provides {len(data_visual_anchors)} usable row anchor(s).",
                        targeted_fields=["body", "metadata"],
                    )
                )
            unsupported_static_markers = cls._unsupported_exact_claim_markers(
                " ".join([text_payload.headline, text_payload.body, text_payload.cta, *data_visual_anchors]),
                request=request,
                compiled_context=compiled_context,
            )
            if unsupported_static_markers:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_unsupported_exact_claim",
                        message=(
                            "The static creative includes exact numeric/date/currency claims that are not present in verified facts or the user prompt: "
                            + ", ".join(sorted(unsupported_static_markers)[:4])
                        ),
                        targeted_fields=["body", "metadata"],
                    )
                )
            if not dominant_message:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_missing_dominant_message",
                        message="The static creative does not yet have one dominant message.",
                        targeted_fields=["body", "metadata"],
                    )
                )
            if dominant_message and supporting_line and dominant_message.casefold() == supporting_line.casefold() and not proof_points:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_too_repetitive",
                        message="The static creative repeats the same line without concrete supporting proof.",
                        targeted_fields=["body", "metadata"],
                    )
                )
            if not proof_points and not claim_pairs:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_missing_support",
                        message="The static creative still needs a concrete proof or claim/evidence support line.",
                        targeted_fields=["body", "metadata"],
                    )
                )
            if unsupported_static_markers:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_unsupported_exact_claim",
                        message=(
                            "The static creative includes exact claims not backed by verified facts or the user prompt: "
                            + ", ".join(sorted(unsupported_static_markers)[:3])
                        ),
                        targeted_fields=["body", "metadata"],
                    )
                )
            data_visual_requested = cls._data_visualization_requested(visual_focus)
            if data_visual_requested and not cls._data_visualization_has_content_anchor(
                request=request,
                anchors=data_visual_anchors,
            ):
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_unbacked_data_visual",
                        message="The static creative asks for a table/chart/graph/dashboard-style visual without content anchors that define what it should show.",
                        targeted_fields=["metadata"],
                    )
                )
            if (
                data_visual_requested
                and cls._numeric_data_visualization_requested(visual_focus)
                and not any(cls._exact_claim_markers(anchor) for anchor in data_visual_anchors)
            ):
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_unsupported_numeric_data_visual",
                        message="The static creative implies a numeric chart/axis/graph but has no approved exact values to chart; use qualitative modules or evidence cards instead.",
                        targeted_fields=["metadata"],
                    )
                )
            if data_visual_requested and unsupported_static_markers:
                issues.append(
                    cls._clean_content_semantic_issue(
                        code="static_data_visual_unsupported_claims",
                        message="The static data/list visual would rely on exact values not backed by verified facts or the user prompt.",
                        targeted_fields=["metadata"],
                    )
                )

        summary = [
            cls._normalize_metadata_text(issue.get("message"), limit=220)
            for issue in issues[:6]
            if isinstance(issue, dict)
        ]
        revision_scope = cls._content_semantic_revision_scope(issues=issues) if issues else {}
        targeted_fields = list(revision_scope.get("targeted_fields") or [])
        return {
            "status": "needs_rewrite" if issues else "clean",
            "repairable": bool(issues),
            "format": format_name,
            "issues": issues,
            "summary": summary,
            "targeted_fields": targeted_fields,
            "revision_scope": revision_scope,
            "rewrite_instruction": (
                cls._build_content_semantic_rewrite_instruction(
                    format_name=format_name,
                    issues=issues,
                )
                if issues
                else ""
            ),
        }

    def _repair_text_payload_semantics_if_needed(
        self,
        *,
        request: AIOrchestrationRequest,
        generation_provider: Any,
        text_payload: StructuredTextPayload,
        compiled_context: dict[str, Any],
        message_strategy: MessageStrategyPayload,
        creative_decision: CreativeDecisionPayload | None,
        brand_name: str,
        trace_id: str,
    ) -> tuple[StructuredTextPayload, dict[str, Any], int]:
        # Repairs text semantics if needed from request state, generation provider, and copy payload for AIOrchestratorService.
        # It delegates shared cleanup to _preflight_text_payload_semantics and _validate_content_semantics before returning the cleaned value.
        text_payload = self._preflight_text_payload_semantics(
            request=request,
            text_payload=text_payload,
            compiled_context=compiled_context,
        )
        report = self._validate_content_semantics(
            request=request,
            text_payload=text_payload,
            compiled_context=compiled_context,
            creative_decision=creative_decision,
        )
        attempts = 0
        prompt_context = self.compiler.compact_generation_prompt_context(compiled_context)
        prompt_context_budget = self.compiler.generation_context_budget_report(
            full_context=compiled_context,
            prompt_context=prompt_context,
        )
        while (
            report.get("status") != "clean"
            and bool(report.get("repairable"))
            and attempts < self.CONTENT_SEMANTIC_REPAIR_ATTEMPTS
        ):
            attempts += 1
            rewrite_envelope = self.prompts.compose_rewrite_envelope(
                original_prompt=request.prompt,
                rewrite_instruction=str(report.get("rewrite_instruction") or "").strip(),
                current_payload=text_payload.model_dump(mode="json"),
                compiled_context=prompt_context,
                message_strategy=message_strategy.model_dump(mode="json"),
                tone_analysis={},
                rewrite_field_plan=self._content_semantic_rewrite_field_plan(
                    text_payload=text_payload,
                    issues=list(report.get("issues") or []),
                    format_name=str(report.get("format") or self._format_name_from_request(request)),
                    request=request,
                    creative_decision=creative_decision,
                ),
                studio_panel=request.studio_panel,
                targeted_fields=list(report.get("targeted_fields") or []),
                revision_scope=report.get("revision_scope") if isinstance(report.get("revision_scope"), dict) else {},
            )
            rewrite_response = generation_provider.generate_structured_json(
                rewrite_envelope,
                fallback=text_payload.model_dump(mode="json"),
            )
            provider_usage = getattr(generation_provider, "last_usage", None)
            self._trace_payload(
                trace_id,
                self.trace,
                f"content_semantic_repair_{attempts:02d}",
                {
                    "report": report,
                    "compiled_context_budget": prompt_context_budget,
                    "prompt": {
                        "system": rewrite_envelope.system,
                        "user": rewrite_envelope.user,
                    },
                    "response": rewrite_response,
                    "provider_usage": provider_usage if isinstance(provider_usage, dict) else None,
                },
            )
            text_dict = self.normalize_text_payload(
                rewrite_response,
                text_payload.model_dump(mode="json"),
                brand_name=brand_name,
                compiled_context=compiled_context,
                prompt=request.prompt,
            )
            text_dict = self._merge_targeted_carousel_rewrite_payload(
                previous_payload=text_payload,
                rewritten_payload=text_dict,
                revision_scope=report.get("revision_scope") if isinstance(report.get("revision_scope"), dict) else {},
            )
            text_payload = StructuredTextPayload(**text_dict)
            text_payload = self._repair_prompt_echo_text_payload(text_payload, prompt=request.prompt)
            text_payload = self._preflight_text_payload_semantics(
                request=request,
                text_payload=text_payload,
                compiled_context=compiled_context,
            )
            report = self._validate_content_semantics(
                request=request,
                text_payload=text_payload,
                compiled_context=compiled_context,
                creative_decision=creative_decision,
            )
        return text_payload, report, attempts

    @classmethod
    def _request_has_sample_creative(cls, request: AIOrchestrationRequest) -> bool:
        # Centralizes request sample creative from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        reference_sources = [
            *(request.reference_assets or []),
            *(request.asset_catalog or []),
        ]
        sample_markers = {
            "reference_creative",
            "reference_sample",
            "uploaded_sample",
            "sample_authority",
            "sample_blueprint",
            "template_preview",
        }
        for asset in reference_sources:
            if not isinstance(asset, dict):
                continue
            tags = asset.get("tags") if isinstance(asset.get("tags"), list) else []
            marker_values = [
                asset.get("asset_role"),
                asset.get("role"),
                asset.get("kind"),
                asset.get("origin_category"),
                asset.get("source_type"),
                asset.get("sample_usage"),
                *tags,
            ]
            metadata = asset.get("metadata") if isinstance(asset.get("metadata"), dict) else {}
            normalized_markers = {
                str(value or "").strip().casefold()
                for value in [*marker_values, metadata.get("asset_role"), metadata.get("origin_category")]
                if str(value or "").strip()
            }
            if normalized_markers.intersection(sample_markers):
                return True
            if any(marker in value for marker in sample_markers for value in normalized_markers):
                return True
            if isinstance(asset.get("sample_page_blueprint"), dict) and asset.get("sample_page_blueprint"):
                return True
        template_context = request.template_context if isinstance(request.template_context, dict) else {}
        sequence_pack = template_context.get("sequence_pack") if isinstance(template_context.get("sequence_pack"), dict) else {}
        return any(isinstance(slide, dict) and slide for slide in sequence_pack.get("slides", []) or [])

    @classmethod
    def _request_has_template(cls, request: AIOrchestrationRequest) -> bool:
        # Centralizes request template from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        studio_panel = request.studio_panel if isinstance(request.studio_panel, dict) else {}
        layout_decision = request.layout_decision if isinstance(request.layout_decision, dict) else {}
        template_context = request.template_context if isinstance(request.template_context, dict) else {}
        sequence_pack = template_context.get("sequence_pack") if isinstance(template_context.get("sequence_pack"), dict) else {}
        candidate_ids = [
            studio_panel.get("pinned_template_id"),
            layout_decision.get("template_id"),
            layout_decision.get("selected_template_id"),
            layout_decision.get("primary_adaptation_template_id"),
            sequence_pack.get("selected_template_id"),
        ]
        if any(str(value or "").strip() for value in candidate_ids):
            return True
        return bool(request.template_candidates)

    @classmethod
    def _request_uses_pinned_template(cls, request: AIOrchestrationRequest) -> bool:
        # Centralizes request uses pinned template from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        studio_panel = request.studio_panel if isinstance(request.studio_panel, dict) else {}
        if str(studio_panel.get("pinned_template_id") or "").strip():
            return True
        layout_decision = request.layout_decision if isinstance(request.layout_decision, dict) else {}
        if layout_decision.get("primary_adaptation_matches_selected_template") is True:
            return bool(
                str(
                    layout_decision.get("template_id")
                    or layout_decision.get("selected_template_id")
                    or layout_decision.get("primary_adaptation_template_id")
                    or ""
                ).strip()
            )
        return False

    @classmethod
    def _request_uses_auto_template_selection(cls, request: AIOrchestrationRequest) -> bool:
        # Centralizes request uses auto template selection from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if cls._request_uses_pinned_template(request):
            return False
        return cls._request_has_template(request)

    @classmethod
    def _select_generation_strategy_for_request(cls, request: AIOrchestrationRequest) -> GenerationStrategy:
        # Selects generation strategy request from request state for AIOrchestratorService.
        # It reuses _request_uses_pinned_template and _request_uses_auto_template_selection so related checks follow the same rule.
        studio_panel = request.studio_panel if isinstance(request.studio_panel, dict) else {}
        return select_generation_engine(
            studio_panel.get("format"),
            cls._request_uses_pinned_template(request),
            cls._request_uses_auto_template_selection(request),
            cls._request_has_sample_creative(request),
            cls._request_has_template(request),
        )

    @classmethod
    def _request_uses_template_adaptance(cls, request: AIOrchestrationRequest | None) -> bool:
        # Centralizes request uses template adaptance from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if request is None:
            return False
        return cls._select_generation_strategy_for_request(request) == GenerationStrategy.TEMPLATE_ADAPTANCE

    @classmethod
    def _request_uses_content_intelligence(cls, request: AIOrchestrationRequest | None) -> bool:
        # Centralizes request uses content intelligence from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None:
            return False
        studio_panel = request.studio_panel if isinstance(request.studio_panel, dict) else {}
        normalized_format = str(studio_panel.get("format") or "").strip().casefold()
        return (
            normalized_format == "carousel"
            and cls._request_uses_auto_template_selection(request)
            and not cls._request_uses_pinned_template(request)
        )

    def generate(self, request: AIOrchestrationRequest) -> AIOrchestrationResponse:
        # Generates generate from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        strategy = self._select_generation_strategy_for_request(request)
        self._trace_payload(
            request.generation_trace_id,
            self.trace,
            "generation_strategy_router",
            {
                "strategy": strategy.value,
                "format": (request.studio_panel or {}).get("format") if isinstance(request.studio_panel, dict) else None,
                "pin": self._request_uses_pinned_template(request),
                "auto": self._request_uses_auto_template_selection(request),
                "has_sample_creative": self._request_has_sample_creative(request),
                "has_template": self._request_has_template(request),
            },
        )
        return self._dispatch_generation_strategy(strategy, request)

    def _dispatch_generation_strategy(
        self,
        strategy: GenerationStrategy,
        request: AIOrchestrationRequest,
    ) -> AIOrchestrationResponse:
        # Centralizes dispatch generation strategy from strategy and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if strategy == GenerationStrategy.STATIC_INFOGRAPHIC_REFERENCE:
            # static/infographic behavior is integrated in the guarded main pipeline.
            return self._generate_main_ai(request)
        if strategy == GenerationStrategy.TEMPLATE_ADAPTANCE:
            # TODO: integrate template-adaptance carousel
            return self._generate_template_adaptance(request)
        if strategy == GenerationStrategy.CONTENT_INTELLIGENCE:
            # TODO: integrate content-intelligence carousel
            return self._generate_content_intelligence(request)
        if strategy == GenerationStrategy.WITHOUT_REFERENCE:
            # TODO: integrate without-reference fallback
            return self._generate_main_ai(request)
        return self._generate_main_ai(request)

    def _generate_template_adaptance(self, request: AIOrchestrationRequest) -> AIOrchestrationResponse:
        # Centralizes template adaptance from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not self._request_uses_template_adaptance(request):
            return self._generate_main_ai(request)
        content_plan = deepcopy(request.content_plan if isinstance(request.content_plan, dict) else {})
        if not str(content_plan.get("format_family") or "").strip():
            content_plan["format_family"] = self._format_name_from_request(request)
        content_plan["_template_adaptance_enabled"] = True
        content_plan["generation_strategy"] = GenerationStrategy.TEMPLATE_ADAPTANCE.value
        content_plan["template_authority_mode"] = "visual_layout_only"
        return self._generate_main_ai(request.model_copy(update={"content_plan": content_plan}))

    def _generate_content_intelligence(self, request: AIOrchestrationRequest) -> AIOrchestrationResponse:
        # Centralizes content intelligence from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not self._request_uses_content_intelligence(request):
            return self._generate_main_ai(request)
        format_family_plan = deepcopy(request.format_family_plan if isinstance(request.format_family_plan, dict) else {})
        if not str(format_family_plan.get("family") or "").strip():
            format_family_plan["family"] = "carousel"
        content_plan = deepcopy(request.content_plan if isinstance(request.content_plan, dict) else {})
        enriched_plan = ContentPlanningService.derive_content_plan(
            deliverable_type=content_plan.get("deliverable_type") or "visual_generation",
            format_family_plan=format_family_plan,
            research_editorial_brief=request.research_editorial_brief if isinstance(request.research_editorial_brief, dict) else {},
            planning_family=content_plan.get("planning_family") or "content",
            enable_semantic_carousel_plan=True,
        )
        for key in (
            "carousel_slide_grammar",
            "carousel_slide_contracts",
            "carousel_archetype_rules",
            "semantic_carousel_plan",
            "preferred_slide_count",
            "sequence_contract",
            "sequence_expectation",
        ):
            value = enriched_plan.get(key)
            if value:
                content_plan[key] = value
            elif key not in content_plan:
                content_plan[key] = value
        for key, value in enriched_plan.items():
            content_plan.setdefault(key, value)
        content_plan["_content_intelligence_enabled"] = True
        content_plan["generation_strategy"] = GenerationStrategy.CONTENT_INTELLIGENCE.value
        return self._generate_main_ai(request.model_copy(update={"content_plan": content_plan}))

    def _generate_main_ai(self, request: AIOrchestrationRequest) -> AIOrchestrationResponse:
        # Centralizes main ai from request state for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        # This is the full request pipeline; every later helper feeds back into this response-building flow.
        started_at = perf_counter()
        latency_ms: dict[str, float] = {}
        trace_id = request.generation_trace_id
        # Trace the raw request first so failures can be debugged even if a later provider or renderer branch stops early.
        self._trace_payload(
            trace_id,
            self.trace,
            "orchestrator_request",
            {
                "prompt": request.prompt,
                "studio_panel": request.studio_panel,
                "template_candidates": request.template_candidates,
                "layout_decision": request.layout_decision,
                "reference_assets": request.reference_assets,
                "asset_catalog": request.asset_catalog,
                "platform_constraints": request.platform_constraints,
                "resolved_brand_context": request.resolved_brand_context,
                "persona_context": request.persona_context,
                "objective_context": request.objective_context,
                "validation_report": request.validation_report,
                "template_context": request.template_context,
                "content_format_guide": request.content_format_guide,
                "live_research": request.live_research,
                "research_editorial_brief": request.research_editorial_brief,
                "template_context_used": bool(request.template_context),
                "template_sequence_pack_slide_count": (
                    len(
                        [
                            item
                            for item in (((request.template_context or {}).get("sequence_pack") or {}) if isinstance((request.template_context or {}).get("sequence_pack"), dict) else {}).get("slides", [])
                            if isinstance(item, dict)
                        ]
                    )
                    if isinstance(request.template_context, dict)
                    else 0
                ),
            },
        )
        self._trace_event(trace_id, self.trace, "orchestrator.generate.start", {"brand_space_id": str(request.brand_space_id)})
        input_access_tracker = request.input_access_tracker if isinstance(request.input_access_tracker, InputAccessTracker) else InputAccessTracker()
        # Wrap source dictionaries so later debug traces can show which inputs were actually read by the pipeline.
        request.resolved_brand_context = input_access_tracker.wrap_source("brand_context", request.resolved_brand_context)
        request.persona_context = input_access_tracker.wrap_source("persona_context", request.persona_context)
        request.objective_context = input_access_tracker.wrap_source("objective_context", request.objective_context)
        request.retrieved_knowledge = input_access_tracker.wrap_source("retrieved_knowledge", request.retrieved_knowledge)
        request.template_context = input_access_tracker.wrap_source("template_context", request.template_context or {})
        if not request.content_format_guide and hasattr(self, "content_format_guide"):
            try:
                loaded_content_format_guide = self.content_format_guide.load()
            except Exception:
                loaded_content_format_guide = {}
            if isinstance(loaded_content_format_guide, dict):
                request.content_format_guide = loaded_content_format_guide
        request.content_format_guide = input_access_tracker.wrap_source("content_format_guide", request.content_format_guide or {})
        request.template_candidates = input_access_tracker.wrap_source("template_candidates", request.template_candidates)
        request.reference_assets = input_access_tracker.wrap_source("reference_assets", request.reference_assets)
        request.asset_catalog = input_access_tracker.wrap_source("asset_catalog", request.asset_catalog)
        request.logo_asset_candidates = input_access_tracker.wrap_source("logo_candidates", request.logo_asset_candidates)
        request.live_research = input_access_tracker.wrap_source("live_research", request.live_research or {})
        self.guardrails.validate_prompt(request.prompt, request.resolved_brand_context.get("guardrails", {}))
        plan_started_at = perf_counter()
        # Phase 1 resolves source priority: brand and explicit request data win before retrieved knowledge or memory can contribute.
        # Resolution establishes source priority before compilation so lower-priority RAG never overrides brand rules.
        plan = self.resolution.build_plan(
            brand_context=request.resolved_brand_context,
            persona_context=request.persona_context,
            objective_context=request.objective_context,
            retrieved_knowledge=request.retrieved_knowledge,
        )
        latency_ms["resolution_ms"] = round((perf_counter() - plan_started_at) * 1000, 2)
        # Providers are resolved once up front so the rest of the flow can call capability interfaces, not vendor classes.
        research_provider = self.providers.get_text_provider("research")
        generation_provider = self.providers.get_text_provider("generation")
        image_provider = self.providers.get_image_provider()

        # Initialize generation trace for decision tracking
        generation_trace = GenerationTrace(
            provider=generation_provider.__class__.__name__,
            model=get_settings().llm_model,
            fallback_used=False,
            layout_source="synthesized",  # Will be updated based on template_context
            layout_reason="Default initialization",
            background_source="synthesized",
            cta_source="text_payload",  # Will be updated if brand CTA template is used
            legal_source="none",  # Will be updated if legal assets are injected
            rag_embedding_type="openai" if get_settings().openai_api_key else "hash",
            renderer_policy="instagram_square_default",  # TODO: Extract from platform_constraints
        )

        compile_started_at = perf_counter()
        # Phase 2 compiles all resolved context into one bounded object that every LLM prompt reads from.
        # Compile all raw brand/RAG/template/session inputs into the compact context used by every prompt stage.
        compiled_context = self.compiler.compile(
            prompt=request.prompt,
            brand_context=request.resolved_brand_context,
            persona_context=request.persona_context,
            objective_context=request.objective_context,
            ordered_knowledge=plan.ordered_knowledge,
            studio_panel=request.studio_panel,
            conversation_context=request.conversation_context,
            session_memory=request.session_memory,
            template_context=request.template_context,
            layout_decision=request.layout_decision,
            reference_assets=request.reference_assets,
            asset_catalog=request.asset_catalog,
            content_format_guide=request.content_format_guide,
            research_editorial_brief=request.research_editorial_brief,
            format_family_plan=request.format_family_plan,
            content_plan=request.content_plan,
            visual_plan=request.visual_plan,
            resolution_instructions=plan.instructions,
            research_summary="",
            live_research=request.live_research,
        )
        if not request.live_research and hasattr(self, "live_research"):
            try:
                # Live research is gathered after the first compile because it needs the already-resolved brand/context brief.
                gathered_live_research = self.live_research.gather_sync(
                    prompt=request.prompt,
                    studio_panel=request.studio_panel,
                    compiled_context=compiled_context,
                )
            except Exception:
                gathered_live_research = {}
            if isinstance(gathered_live_research, dict) and gathered_live_research:
                request.live_research = input_access_tracker.wrap_source("live_research", gathered_live_research)
                provider_usage = gathered_live_research.get("provider_usage")
                if isinstance(provider_usage, list) and provider_usage:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        "live_research_provider_usage",
                        {
                            "usage": provider_usage,
                            "status": gathered_live_research.get("status"),
                            "queries": gathered_live_research.get("queries", []),
                        },
                    )
                # Recompile after live research so verified facts join the same prompt context as saved brand inputs.
                compiled_context = self.compiler.compile(
                    prompt=request.prompt,
                    brand_context=request.resolved_brand_context,
                    persona_context=request.persona_context,
                    objective_context=request.objective_context,
                    ordered_knowledge=plan.ordered_knowledge,
                    studio_panel=request.studio_panel,
                    conversation_context=request.conversation_context,
                    session_memory=request.session_memory,
                    template_context=request.template_context,
                    layout_decision=request.layout_decision,
                    reference_assets=request.reference_assets,
                    asset_catalog=request.asset_catalog,
                    content_format_guide=request.content_format_guide,
                    research_editorial_brief=request.research_editorial_brief,
                    format_family_plan=request.format_family_plan,
                    content_plan=request.content_plan,
                    visual_plan=request.visual_plan,
                    resolution_instructions=plan.instructions,
                    research_summary="",
                    live_research=request.live_research,
                )
        latency_ms["context_compile_ms"] = round((perf_counter() - compile_started_at) * 1000, 2)

        research_started_at = perf_counter()
        # Phase 3 asks for a compact research memo so generation sees a human-readable summary, not scattered raw evidence.
        research_prompt_context = self.compiler.compact_generation_prompt_context(compiled_context)
        research_context_payload = {
            "brand_copy_brief": research_prompt_context.get("brand_copy_brief", {}),
            "audience_brief": research_prompt_context.get("audience_brief", {}),
            "objective_brief": research_prompt_context.get("objective_brief", {}),
            "knowledge_brief": research_prompt_context.get("knowledge_brief", []),
            "template_fit_brief": research_prompt_context.get("template_fit_brief", {}),
            "prompt_intelligence_brief": research_prompt_context.get("prompt_intelligence_brief", {}),
            "research_editorial_brief": research_prompt_context.get("research_editorial_brief", {}),
            "format_family_plan": research_prompt_context.get("format_family_plan", {}),
            "content_plan": research_prompt_context.get("content_plan", {}),
            "visual_plan": research_prompt_context.get("visual_plan", {}),
            "live_research": research_prompt_context.get("live_research", {}),
        }
        research_envelope = PromptEnvelope(
            system=(
                "Synthesize the provided brand and audience context into a compact downstream research memo for generation. "
                "Preserve concrete audience motivations, pain points, objections, preferences, behaviors, differentiators, proof cues, "
                "and non-redundant specifics when present. If a research-editorial brief is active, preserve its thesis, angle, insight hierarchy, "
                "and outline rather than collapsing the topic into generic social commentary. Keep it brand-safe, but do not genericize the audience into vague filler. "
                "Prefer 4-6 short sentences or semicolon-separated lines with concrete guidance."
            ),
            user=(
                f"Prompt: {request.prompt}\n"
                f"Research context: {research_context_payload}\n"
                f"Conflict resolution policy: {plan.instructions}"
            ),
        )
        self._trace_payload(
            trace_id,
            self.trace,
            "research_summary_prompt",
            {
                "system": research_envelope.system,
                "user": research_envelope.user,
                "prompt_token_measurement": self._trace_prompt_token_measurement(
                    {"system": research_envelope.system, "user": research_envelope.user}
                ),
                "research_context_token_measurement": self._trace_mapping_token_measurement(research_context_payload),
            },
        )
        research_summary = research_provider.generate_text(
            research_envelope,
            fallback=(
                "Preserve concrete audience motivations, pain points, objections, preferences, and retrieved knowledge in a concise brand-safe memo."
            ),
        )
        provider_usage = getattr(research_provider, "last_usage", None)
        if isinstance(provider_usage, dict):
            self._trace_payload(
                trace_id,
                self.trace,
                "research_summary_provider_usage",
                {
                    "provider": getattr(research_provider, "provider_name", ""),
                    "usage": provider_usage,
                    "prompt_token_measurement": self._trace_prompt_token_measurement(
                        {"system": research_envelope.system, "user": research_envelope.user}
                    ),
                },
            )
        latency_ms["research_ms"] = round((perf_counter() - research_started_at) * 1000, 2)
        compiled_context["research_summary"] = research_summary
        # Compact prompt context is the token-budgeted version of compiled_context used by LLM prompt builders.
        generation_prompt_context = self.compiler.compact_generation_prompt_context(compiled_context)
        generation_context_budget = self.compiler.generation_context_budget_report(
            full_context=compiled_context,
            prompt_context=generation_prompt_context,
        )
        self._trace_payload(trace_id, self.trace, "compiled_context", compiled_context)
        self._trace_payload(trace_id, self.trace, "compiled_prompt_context_budget", generation_context_budget)
        self._trace_payload(trace_id, self.trace, "compiled_prompt_context", generation_prompt_context)
        brand_name = request.resolved_brand_context.get("brand_name", "Violyt")
        platform_preset = request.studio_panel.get("platform_preset", "social")
        format_name = request.studio_panel.get("format", "static")
        fallback_message_strategy = self._fallback_message_strategy(request, compiled_context)
        audience_brief = self._coerce_mapping(compiled_context.get("audience_brief"))
        audience_insights = self._coerce_mapping(request.resolved_brand_context.get("audience_insights"))
        persona_context = self._coerce_mapping(request.persona_context)
        audience_research_highlights = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("research_highlights"), limit=3),
                *self._normalize_metadata_list(audience_insights.get("research_highlights"), limit=3),
            ],
            limit=3,
        )
        audience_objections = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(
                    audience_brief.get("audience_research_objections") or audience_brief.get("objections"),
                    limit=3,
                ),
                *self._normalize_metadata_list(audience_insights.get("objections"), limit=3),
            ],
            limit=3,
        )
        audience_desired_outcomes = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("desired_outcomes"), limit=3),
                *self._normalize_metadata_list(audience_insights.get("desired_outcomes"), limit=3),
            ],
            limit=3,
        )
        audience_preferences = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("preferences"), limit=3),
                *self._normalize_metadata_list(audience_insights.get("preferences"), limit=3),
            ],
            limit=3,
        )
        audience_pain_points = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(
                    audience_brief.get("audience_research_pain_points") or audience_brief.get("pain_points"),
                    limit=3,
                ),
                *self._normalize_metadata_list(audience_insights.get("pain_points"), limit=3),
            ],
            limit=3,
        )
        persona_objections = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("persona_objections"), limit=3),
                *self._normalize_metadata_list(persona_context.get("objections"), limit=3),
            ],
            limit=3,
        )
        persona_pain_points = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("persona_pain_points"), limit=3),
                *self._normalize_metadata_list(persona_context.get("pain_points"), limit=3),
                *self._normalize_metadata_list(persona_context.get("fears_and_pain_points"), limit=3),
            ],
            limit=3,
        )
        resolved_objections = audience_objections or persona_objections
        resolved_pain_points = audience_pain_points or persona_pain_points
        audience_trust_signals = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("trust_signals"), limit=3),
                *self._normalize_metadata_list(audience_insights.get("trust_signals"), limit=3),
            ],
            limit=3,
        )
        audience_proof_cues = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("proof_cues"), limit=3),
                *self._normalize_metadata_list(audience_insights.get("proof_cues"), limit=3),
            ],
            limit=3,
        )
        audience_comparison_points = self._normalize_metadata_list(
            [
                *self._normalize_metadata_list(audience_brief.get("comparison_points"), limit=3),
                *self._normalize_metadata_list(audience_insights.get("comparison_points"), limit=3),
            ],
            limit=3,
        )
        fallback_evidence_pool = [
            *audience_proof_cues,
            *audience_trust_signals,
            *audience_comparison_points,
            *audience_research_highlights,
        ]
        # Fallback evidence pairs keep deterministic copy persuasive when the LLM returns weak or unusable content.
        fallback_claim_sources = audience_desired_outcomes or [fallback_message_strategy.get("key_value_proposition")]
        fallback_claim_evidence_pairs = self._normalize_claim_evidence_pairs(
            [
                {
                    "claim": claim,
                    "evidence": fallback_evidence_pool[index:index + 1]
                    or fallback_evidence_pool[:1]
                    or [compiled_context.get("research_summary") or fallback_message_strategy.get("core_audience_message")],
                }
                for index, claim in enumerate(fallback_claim_sources[:2])
                if claim
            ],
            limit=2,
        )
        fallback_hook_type = (
            "comparison-led"
            if audience_comparison_points
            else "proof-led"
            if audience_proof_cues or audience_trust_signals or audience_research_highlights
            else "problem-solution"
            if resolved_objections or resolved_pain_points
            else "benefit-led"
            if audience_desired_outcomes or audience_preferences
            else "brand-led"
        )
        fallback_claim_evidence_lines = self._claim_evidence_pair_lines(
            fallback_claim_evidence_pairs,
            limit=2,
        )
        fallback_headline = self._fallback_headline_copy(
            prompt=request.prompt,
            claim_evidence_pairs=fallback_claim_evidence_pairs,
            desired_outcomes=audience_desired_outcomes,
            key_value_proposition=fallback_message_strategy.get("key_value_proposition"),
            headline_direction=fallback_message_strategy.get("headline_direction"),
            primary_campaign_theme=fallback_message_strategy.get("primary_campaign_theme"),
            core_audience_message=fallback_message_strategy.get("core_audience_message"),
        )
        fallback_body = self._fallback_body_copy(
            prompt=request.prompt,
            headline=fallback_headline,
            core_audience_message=fallback_message_strategy.get("core_audience_message"),
            claim_evidence_pairs=fallback_claim_evidence_pairs,
            trust_builders=audience_trust_signals[:2] or audience_proof_cues[:2],
            proof_points=audience_proof_cues[:2] or audience_research_highlights[:2],
            research_highlights=audience_research_highlights,
            supporting_copy_direction=fallback_message_strategy.get("supporting_copy_direction"),
            key_value_proposition=fallback_message_strategy.get("key_value_proposition"),
        )
        fallback_cta = self._fallback_cta_copy(
            prompt=request.prompt,
            cta_intent=fallback_message_strategy.get("cta_intent"),
            primary_goal=(compiled_context.get("objective_brief", {}) or {}).get("primary_goal"),
            topic_focus=(
                self._fallback_topic_focus(request.prompt, compiled_context=compiled_context)
                or fallback_message_strategy.get("primary_campaign_theme")
                or self._coerce_text_value(request.prompt)
            ),
            comparison_points=audience_comparison_points,
            proof_cues=audience_proof_cues,
            trust_signals=audience_trust_signals,
        )
        fallback_supporting_line = self._normalize_metadata_text(
            fallback_claim_evidence_lines[0]
            if fallback_claim_evidence_lines
            else audience_research_highlights[0]
            if audience_research_highlights
            else fallback_body,
            limit=180,
        )
        fallback_proof_points = self._normalize_metadata_list(
            [
                *fallback_claim_evidence_lines,
                *audience_proof_cues[:2],
                *audience_trust_signals[:2],
                *audience_research_highlights[:2],
            ],
            limit=4,
        )
        fallback_objection_handling_line = self._fallback_objection_handling_copy(
            objections=resolved_objections,
            pain_points=resolved_pain_points,
            proof_cues=audience_proof_cues,
            trust_signals=audience_trust_signals,
            comparison_points=audience_comparison_points,
            research_highlights=audience_research_highlights,
        )
        fallback_metadata = {
            "source": "fallback",
            "research_summary": research_summary,
            "section_label": platform_preset.replace("_", " ").title(),
            "supporting_line": fallback_supporting_line,
            "proof_points": fallback_proof_points,
            "stat_highlights": [],
            "hook_type": fallback_hook_type,
            "objection_handling": self._normalize_metadata_list(
                ([fallback_objection_handling_line] if self._looks_like_objection_response(fallback_objection_handling_line) else [])
                or ([fallback_message_strategy.get("supporting_copy_direction")] if self._looks_like_objection_response(fallback_message_strategy.get("supporting_copy_direction")) else []),
                limit=2,
            ),
            "trust_builders": audience_trust_signals[:2] or audience_proof_cues[:2] or audience_research_highlights[:2] or audience_preferences[:2],
            "claim_evidence_pairs": fallback_claim_evidence_pairs,
            "visual_direction": f"Premium brand-safe {platform_preset} visual that explains the requested content with a clear, content-specific focal concept.",
            "design_style": "editorial brand campaign creative" if format_name != "infographic" else "structured premium infographic",
            "image_prompt": "Premium brand campaign visual with no text, built around the concrete idea in the user prompt.",
        }
        # This fallback payload is not just an error fallback; it also anchors normalization and repair defaults.
        fallback_text = {
            "headline": fallback_headline,
            "body": fallback_body,
            "cta": fallback_cta,
            "hashtags": [f"#{brand_name.replace(' ', '')}"],
            "metadata": fallback_metadata,
        }
        use_image_led_social = self._should_use_image_led_social(request, compiled_context)
        generation_path = "image_led_social" if use_image_led_social else "scene_graph_social"
        message_strategy = MessageStrategyPayload.model_validate(fallback_message_strategy)
        fallback_creative_decision = self._fallback_creative_decision(request, compiled_context)
        # Phase 4 chooses the planning route: image-led treats the image model as designer, scene-graph keeps backend geometry central.
        if use_image_led_social:
            # Image-led generation uses the final image model as the composition authority, so templates become style references.
            fallback_creative_decision = {
                **fallback_creative_decision,
                "layout_mode": "synthesized_layout" if not fallback_creative_decision.get("selected_template_id") else "adapted_template",
                "asset_strategy": {
                    **self._coerce_mapping(fallback_creative_decision.get("asset_strategy")),
                    "use_generated_image": True,
                    "use_template_background": False,
                    "use_brand_reference_assets": False,
                    "logo_injection_required": True,
                    "background_element": True,
                    "type_led": True,
                    "dominant_visual_system": "generated_image",
                    "template_surface_policy": "style_reference_only",
                },
            }
            message_strategy_started_at = perf_counter()
            # Message strategy is generated separately so the image-led path has a campaign idea before visual planning.
            message_strategy_envelope = self.prompts.compose_message_strategy_envelope(
                user_prompt=request.prompt,
                compiled_context=generation_prompt_context,
                studio_panel=request.studio_panel,
            )
            self._trace_payload(
                trace_id,
                self.trace,
                "message_strategy_prompt",
                {
                    "system": message_strategy_envelope.system,
                    "user": message_strategy_envelope.user,
                    "prompt_token_measurement": self._trace_prompt_token_measurement(
                        {
                            "system": message_strategy_envelope.system,
                            "user": message_strategy_envelope.user,
                        }
                    ),
                    "compiled_context_token_measurement": self._trace_mapping_token_measurement(generation_prompt_context),
                    "compiled_context_budget": generation_context_budget,
                    "compiled_sections": sorted(generation_prompt_context.keys()),
                },
            )
            message_strategy_response = generation_provider.generate_structured_json(
                message_strategy_envelope,
                fallback=fallback_message_strategy,
            )
            latency_ms["message_strategy_ms"] = round((perf_counter() - message_strategy_started_at) * 1000, 2)
            provider_usage = getattr(generation_provider, "last_usage", None)
            if isinstance(provider_usage, dict):
                self._trace_payload(
                    trace_id,
                    self.trace,
                    "message_strategy_provider_usage",
                    {
                        "provider": getattr(generation_provider, "provider_name", ""),
                        "usage": provider_usage,
                        "prompt_token_measurement": self._trace_prompt_token_measurement(
                            {
                                "system": message_strategy_envelope.system,
                                "user": message_strategy_envelope.user,
                            }
                        ),
                    },
                )
            self._trace_payload(trace_id, self.trace, "message_strategy_response", message_strategy_response)
            message_strategy = self.normalize_message_strategy_payload(
                message_strategy_response,
                fallback_message_strategy,
            )
            # The fallback scene graph still matters in image-led mode because validation and tracing expect a graph shape.
            fallback_scene_graph = self._fallback_image_led_scene_graph(
                request=request,
                text_payload=fallback_text,
                creative_decision=fallback_creative_decision,
                compiled_context=compiled_context,
            )
            planning_envelope = self.prompts.compose_image_led_social_envelope(
                user_prompt=request.prompt,
                compiled_context=generation_prompt_context,
                studio_panel=request.studio_panel,
                message_strategy=message_strategy.model_dump(mode="json"),
                validation_report=request.validation_report,
            )
        else:
            # Scene-graph social keeps layout decisions in the planning response and renderer validation loop.
            fallback_scene_graph = self._fallback_scene_graph(
                request=request,
                text_payload=fallback_text,
                creative_decision=fallback_creative_decision,
                compiled_context=compiled_context,
            )
            planning_envelope = self.prompts.compose_creative_planning_envelope(
                user_prompt=request.prompt,
                compiled_context=generation_prompt_context,
                studio_panel=request.studio_panel,
                validation_report=request.validation_report,
            )
        self._trace_payload(
            trace_id,
            self.trace,
            "planning_prompt",
            {
                "system": planning_envelope.system,
                "user": planning_envelope.user,
                "prompt_token_measurement": self._trace_prompt_token_measurement(
                    {
                        "system": planning_envelope.system,
                        "user": planning_envelope.user,
                    }
                ),
                "compiled_context_token_measurement": self._trace_mapping_token_measurement(generation_prompt_context),
                "compiled_context_budget": generation_context_budget,
                "compiled_sections": sorted(generation_prompt_context.keys()),
                "generation_path": generation_path,
            },
        )
        text_started_at = perf_counter()
        # Phase 5 asks the generation provider for the first complete creative plan, copy payload, and scene graph.
        planning_response = generation_provider.generate_structured_json(
            planning_envelope,
            fallback={
                **fallback_text,
                "creative_decision": fallback_creative_decision,
                "scene_graph": fallback_scene_graph,
            },
        )
        provider_usage = getattr(generation_provider, "last_usage", None)
        if isinstance(provider_usage, dict):
            self._trace_payload(
                trace_id,
                self.trace,
                "planning_provider_usage",
                {
                    "provider": getattr(generation_provider, "provider_name", ""),
                    "usage": provider_usage,
                    "prompt_token_measurement": self._trace_prompt_token_measurement(
                        {
                            "system": planning_envelope.system,
                            "user": planning_envelope.user,
                        }
                    ),
                },
            )
        self._trace_payload(trace_id, self.trace, "planning_response", planning_response)
        text_dict = self.normalize_text_payload(
            planning_response,
            fallback_text,
            brand_name=brand_name,
            compiled_context=compiled_context,
            prompt=request.prompt,
        )
        # Normalize before model construction so missing/invalid LLM fields are repaired into the shared payload shape.
        text_payload = StructuredTextPayload(**text_dict)
        text_payload = self._repair_prompt_echo_text_payload(text_payload, prompt=request.prompt)
        latency_ms["text_generation_ms"] = round((perf_counter() - text_started_at) * 1000, 2)
        if isinstance(planning_response.get("message_strategy"), dict):
            message_strategy = self.normalize_message_strategy_payload(
                planning_response.get("message_strategy"),
                fallback_message_strategy,
            )
        creative_decision = self.normalize_creative_decision_payload(
            planning_response.get("creative_decision"),
            fallback_creative_decision,
            request=request,
            compiled_context=compiled_context,
        )

        # Update generation trace with layout decision
        # The trace translates creative decisions into human-readable provenance for debugging generated outputs.
        if creative_decision.layout_mode == "exact_template":
            generation_trace.layout_source = "brand_design_system"
            generation_trace.layout_reason = f"Using exact template: {creative_decision.selected_template_id or 'unknown'}"
        elif creative_decision.layout_mode == "adapted_template":
            generation_trace.layout_source = "reference_template"
            generation_trace.layout_reason = f"Adapted from template: {creative_decision.selected_template_id or 'unknown'}"
        else:
            generation_trace.layout_source = "synthesized"
            generation_trace.layout_reason = "No template selected, synthesizing layout from brand guidelines"

        # Update background source based on asset strategy
        asset_strategy = creative_decision.asset_strategy
        if asset_strategy.get("use_template_background"):
            generation_trace.background_source = "reference_template"
        elif asset_strategy.get("use_generated_image"):
            generation_trace.background_source = "ai_generated"
        elif asset_strategy.get("use_brand_reference_assets"):
            generation_trace.background_source = "brand_assets"

        logger.info(f"Layout source: {generation_trace.layout_source} - {generation_trace.layout_reason}")

        content_semantic_report = {
            "status": "clean",
            "repairable": False,
            "format": self._format_name_from_request(request),
            "issues": [],
            "summary": [],
            "targeted_fields": [],
            "revision_scope": {},
        }
        content_semantic_repair_attempts = 0
        if self._needs_content_semantic_validation(request):
            # Data-heavy and research-led formats get a semantic preflight before visual rendering can proceed.
            text_payload, content_semantic_report, content_semantic_repair_attempts = self._repair_text_payload_semantics_if_needed(
                request=request,
                generation_provider=generation_provider,
                text_payload=text_payload,
                compiled_context=compiled_context,
                message_strategy=message_strategy,
                creative_decision=creative_decision,
                brand_name=brand_name,
                trace_id=trace_id,
            )
            unresolved_data_surface_codes = self._unresolved_data_surface_issue_codes(content_semantic_report)
            if unresolved_data_surface_codes:
                # Bad table/chart/list semantics are generation blockers because later render stages would amplify them.
                self._trace_payload(
                    trace_id,
                    self.trace,
                    "data_surface_generation_blocked",
                    {
                        "reason": "unresolved_content_semantics",
                        "issue_codes": sorted(unresolved_data_surface_codes),
                        "report": content_semantic_report,
                    },
                )
                raise self._data_surface_generation_failure(report=content_semantic_report)
        # The scene graph is normalized after text repair so text, geometry, and creative decision agree.
        scene_graph = self.normalize_scene_graph_payload(
            planning_response.get("scene_graph"),
            fallback=fallback_scene_graph,
            creative_decision=creative_decision,
            text_payload=text_payload.model_dump(mode="json"),
            request=request,
            compiled_context=compiled_context,
        )

        # Update CTA source in trace
        brand_assets = request.resolved_brand_context.get("brand_assets", {})
        cta_templates = brand_assets.get("cta_templates", [])
        if cta_templates and isinstance(cta_templates, list):
            generation_trace.cta_source = "brand_cta_template"
            logger.info("CTA source: brand_cta_template")
        else:
            generation_trace.cta_source = "text_payload"
            logger.info("CTA source: text_payload (no brand CTA template available)")

        # Check for legal assets
        legal_assets = brand_assets.get("legal_text", [])
        if legal_assets:
            generation_trace.legal_source = "brand_legal_asset"
            logger.info("Legal source: brand_legal_asset")
        else:
            generation_trace.legal_source = "none"

        # Update renderer policy from platform
        platform = str(request.studio_panel.get("platform_preset", "instagram")).strip().lower()
        canvas_size = scene_graph.canvas.width if scene_graph.canvas else 1080
        generation_trace.renderer_policy = f"{platform}_{canvas_size}x{canvas_size}_default"
        logger.info(f"Renderer policy: {generation_trace.renderer_policy}")

        validation_report = self.validate_scene_graph(
            scene_graph=scene_graph,
            creative_decision=creative_decision,
            request=request,
            compiled_context=compiled_context,
        )
        # Phase 6 validates the planned geometry, but some final-render routes intentionally defer layout authority to images.
        style_reference_final_render_active = (
            self._style_reference_only_carousel_active(request, creative_decision)
            and self._should_use_ai_final_render(request, generation_path, creative_decision)
        )
        ai_final_render_static_infographic = (
            generation_path == "image_led_social"
            and self._llm_led_static_infographic_format(self._format_name_from_request(request))
            and self._should_use_ai_final_render(request, generation_path, creative_decision)
        )
        if style_reference_final_render_active and validation_report.status != "clean":
            # Style-reference carousel layout is judged against the selected sample during final render, not this advisory graph.
            self._trace_payload(
                trace_id,
                self.trace,
                "scene_graph_validation_deferred_to_style_reference_final_render",
                {
                    "reason": "style_reference_only_carousel_uses_sample_conditioned_final_render",
                    "deferred_issues": [issue.rule_id for issue in validation_report.issues],
                },
            )
            validation_report = SceneGraphValidationReport(
                status="clean",
                issues=[],
                summary=[
                    "Scene graph validation deferred because style-reference carousel final render uses selected sample pages as the layout authority."
                ],
                repairable=False,
            )
        elif (
            ai_final_render_static_infographic
            and bool(getattr(self.settings, "ai_final_render_skip_advisory_scene_repairs", True))
            and validation_report.status != "clean"
        ):
            # LLM-led static/infographic output is validated after image generation, so graph repairs are deferred here.
            self._trace_payload(
                trace_id,
                self.trace,
                "scene_graph_validation_deferred_to_ai_final_render",
                {
                    "reason": "static_infographic_ai_final_render_uses_final_prompt_and_output_vision_as_authority",
                    "deferred_issues": [issue.rule_id for issue in validation_report.issues],
                },
            )
            validation_report = SceneGraphValidationReport(
                status="clean",
                issues=[],
                summary=[
                    "Scene graph validation deferred because static/infographic AI final render uses the final render prompt and output vision QA as the visual authority."
                ],
                repairable=False,
            )

        # Track quality history for circuit breaker
        repair_quality_history: list[float] = []
        initial_quality = self.assess_creative_quality(
            scene_graph=scene_graph,
            creative_decision=creative_decision,
            validation_report=validation_report,
            request=request,
            selected_reference_images=[],  # Reference images not selected yet at this point
            used_support_fallback=False,
            compiled_context=compiled_context,
        )
        repair_quality_history.append(initial_quality["score"])
        logger.info(f"Initial quality score before repairs: {initial_quality['score']}")

        repair_attempts = 0
        # Phase 7 tries surgical scene-graph repair before giving up or asking for a fresh replan.
        while (
            validation_report.status != "clean"
            and validation_report.repairable
            and repair_attempts < self.SCENE_GRAPH_REPAIR_ATTEMPTS
        ):
            repair_attempts += 1

            # Cache current state before repair attempt for potential rollback
            # Repair prompts can be partial, so every attempt keeps a known-good graph/decision snapshot.
            previous_scene_graph = scene_graph.model_copy(deep=True)
            previous_creative_decision = creative_decision.model_copy(deep=True)

            repair_envelope = self.prompts.compose_scene_graph_repair_envelope(
                user_prompt=request.prompt,
                compiled_context=generation_prompt_context,
                studio_panel=request.studio_panel,
                current_scene_graph=scene_graph.model_dump(mode="json"),
                creative_decision=creative_decision.model_dump(mode="json"),
                validation_report=validation_report.model_dump(mode="json"),
                repair_quality_history=repair_quality_history,
            )
            repair_response = generation_provider.generate_structured_json(
                repair_envelope,
                fallback={
                    "creative_decision": creative_decision.model_dump(mode="json"),
                    "scene_graph": scene_graph.model_dump(mode="json"),
                },
            )
            provider_usage = getattr(generation_provider, "last_usage", None)
            self._trace_payload(
                trace_id,
                self.trace,
                f"repair_{repair_attempts:02d}",
                {
                    "validation_report": validation_report.model_dump(mode="json"),
                    "compiled_context_budget": generation_context_budget,
                    "prompt": {
                        "system": repair_envelope.system,
                        "user": repair_envelope.user,
                    },
                    "response": repair_response,
                    "provider_usage": provider_usage if isinstance(provider_usage, dict) else None,
                },
            )
            creative_decision = self.normalize_creative_decision_payload(
                repair_response.get("creative_decision"),
                creative_decision.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )

            # CRITICAL FIX: Merge repair response into existing scene graph instead of replacing
            # LLM may return only repaired elements, not the complete scene graph
            repaired_scene_graph_dict = repair_response.get("scene_graph", {})
            merged_scene_graph_dict = self._merge_repair_into_scene_graph(
                existing_scene_graph=scene_graph.model_dump(mode="json"),
                repair_scene_graph=repaired_scene_graph_dict,
                repair_attempt=repair_attempts,
            )

            scene_graph = self.normalize_scene_graph_payload(
                merged_scene_graph_dict,
                fallback=scene_graph.model_dump(mode="json"),
                creative_decision=creative_decision,
                text_payload=text_payload.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )
            validation_report = self.validate_scene_graph(
                scene_graph=scene_graph,
                creative_decision=creative_decision,
                request=request,
                compiled_context=compiled_context,
            )

            # Quality circuit breaker: assess quality and stop if degrading
            current_quality = self.assess_creative_quality(
                scene_graph=scene_graph,
                creative_decision=creative_decision,
                validation_report=validation_report,
                request=request,
                selected_reference_images=[],  # Reference images not selected yet at this point
                used_support_fallback=False,
                compiled_context=compiled_context,
            )
            current_score = current_quality["score"]
            previous_score = repair_quality_history[-1]
            repair_quality_history.append(current_score)

            logger.info(f"Repair attempt {repair_attempts} quality: {current_score:.2f} (previous: {previous_score:.2f}, delta: {current_score - previous_score:+.2f})")

            # Circuit breaker: stop if quality degrades significantly
            if current_score < previous_score - 0.15:
                logger.warning(
                    f"Quality degraded by {previous_score - current_score:.2f} "
                    f"(from {previous_score:.2f} to {current_score:.2f}). Breaking repair loop and reverting to previous state."
                )
                # Revert to previous state
                scene_graph = previous_scene_graph
                creative_decision = previous_creative_decision
                validation_report = self.validate_scene_graph(
                    scene_graph=scene_graph,
                    creative_decision=creative_decision,
                    request=request,
                    compiled_context=compiled_context,
                )
                # Remove the degraded score from history
                repair_quality_history.pop()
                logger.info(f"Reverted to previous state with quality score: {previous_score:.2f}")
                break

        fresh_replan_attempted = False
        if self._should_apply_support_fallback(scene_graph=scene_graph, validation_report=validation_report) and self._should_request_fresh_replan(
            validation_report
        ):
            fresh_replan_attempted = True
            replan_note = self._fresh_replan_note(validation_report)
            # Fresh replan is used when patch-style repairs are unlikely to fix the shape of the graph.
            if generation_path == "image_led_social":
                fresh_replan_envelope = self.prompts.compose_image_led_social_envelope(
                    user_prompt=request.prompt,
                    compiled_context=generation_prompt_context,
                    studio_panel=request.studio_panel,
                    message_strategy=message_strategy.model_dump(mode="json"),
                    validation_report=validation_report.model_dump(mode="json"),
                    replan_note=replan_note,
                )
            else:
                fresh_replan_envelope = self.prompts.compose_creative_planning_envelope(
                    user_prompt=request.prompt,
                    compiled_context=generation_prompt_context,
                    studio_panel=request.studio_panel,
                    validation_report=validation_report.model_dump(mode="json"),
                    replan_note=replan_note,
                )
            fresh_replan_response = generation_provider.generate_structured_json(
                fresh_replan_envelope,
                fallback={
                    **fallback_text,
                    "creative_decision": fallback_creative_decision,
                    "scene_graph": fallback_scene_graph,
                },
            )
            provider_usage = getattr(generation_provider, "last_usage", None)
            self._trace_payload(
                trace_id,
                self.trace,
                "fresh_replan_01",
                {
                    "validation_report": validation_report.model_dump(mode="json"),
                    "compiled_context_budget": generation_context_budget,
                    "prompt": {
                        "system": fresh_replan_envelope.system,
                        "user": fresh_replan_envelope.user,
                    },
                    "response": fresh_replan_response,
                    "provider_usage": provider_usage if isinstance(provider_usage, dict) else None,
                },
            )
            text_dict = self.normalize_text_payload(
                fresh_replan_response,
                fallback_text,
                brand_name=brand_name,
                compiled_context=compiled_context,
                prompt=request.prompt,
            )
            text_payload = StructuredTextPayload(**text_dict)
            text_payload = self._repair_prompt_echo_text_payload(text_payload, prompt=request.prompt)
            if not use_image_led_social and isinstance(fresh_replan_response.get("message_strategy"), dict):
                message_strategy = self.normalize_message_strategy_payload(
                    fresh_replan_response.get("message_strategy"),
                    fallback_message_strategy,
                )
            creative_decision = self.normalize_creative_decision_payload(
                fresh_replan_response.get("creative_decision"),
                fallback_creative_decision,
                request=request,
                compiled_context=compiled_context,
            )
            if self._needs_content_semantic_validation(request):
                # Replanned copy still goes through semantic validation because table/list facts may have changed.
                text_payload, content_semantic_report, extra_semantic_attempts = self._repair_text_payload_semantics_if_needed(
                    request=request,
                    generation_provider=generation_provider,
                    text_payload=text_payload,
                    compiled_context=compiled_context,
                    message_strategy=message_strategy,
                    creative_decision=creative_decision,
                    brand_name=brand_name,
                    trace_id=trace_id,
                )
                content_semantic_repair_attempts += extra_semantic_attempts
                unresolved_data_surface_codes = self._unresolved_data_surface_issue_codes(content_semantic_report)
                if unresolved_data_surface_codes:
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        "data_surface_generation_blocked",
                        {
                            "reason": "unresolved_content_semantics_after_replan",
                            "issue_codes": sorted(unresolved_data_surface_codes),
                            "report": content_semantic_report,
                        },
                    )
                    raise self._data_surface_generation_failure(report=content_semantic_report)
            scene_graph = self.normalize_scene_graph_payload(
                fresh_replan_response.get("scene_graph"),
                fallback=fallback_scene_graph,
                creative_decision=creative_decision,
                text_payload=text_payload.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )
            validation_report = self.validate_scene_graph(
                scene_graph=scene_graph,
                creative_decision=creative_decision,
                request=request,
                compiled_context=compiled_context,
            )
        if self._data_surface_contract_active(request=request, text_payload=text_payload) and validation_report.status != "clean":
            validation_issues = [issue.rule_id for issue in validation_report.issues]
            if self._should_defer_data_surface_layout_validation(
                request=request,
                text_payload=text_payload,
                validation_report=validation_report,
            ):
                # Some data layouts are enforced by the final render prompt, but unsupported exact values stay blocked earlier.
                self._trace_payload(
                    trace_id,
                    self.trace,
                    "data_surface_layout_validation_deferred_to_final_render",
                    {
                        "reason": "static_infographic_final_render_uses_data_surface_prompt_contract",
                        "issue_codes": validation_issues,
                        "status": validation_report.status,
                    },
                )
                validation_report = SceneGraphValidationReport(
                    status="clean",
                    issues=[],
                    summary=[
                        "Data/list surface layout validation deferred to final render prompt contract; exact unsupported values remain blocked by semantic validation."
                    ],
                    repairable=False,
                )
            else:
                # Data/ranking prompts are intentionally not allowed to fall back to generic imagery.
                self._trace_payload(
                    trace_id,
                    self.trace,
                    "data_surface_generation_blocked",
                    {
                        "reason": "unresolved_layout_validation",
                        "issue_codes": validation_issues,
                        "status": validation_report.status,
                    },
                )
                raise GenerationFailureError(
                    "Data/ranking surface layout remained unresolved; generic visual fallback is disabled for this request.",
                    failure_type="layout_validation",
                    reason_code="data_surface_layout_unresolved",
                    user_safe_message=(
                        "I could not safely lay out this ranking/table image because the required rows or exact values are not verified. "
                        "I will not replace it with a generic fallback photo. Provide approved row data or enable research for this request."
                    ),
                    retryable=True,
                    rule_source="data_surface_no_fallback",
                    suggested_next_action="Provide verified rows/values, attach source data, or run the research step before image generation.",
                    details={"validation_status": validation_report.status, "issue_codes": validation_issues},
                )
        used_support_fallback = False
        if self._should_apply_support_fallback(scene_graph=scene_graph, validation_report=validation_report):
            # Validation is advisory for some creative routes, but unresolved issues are still traced for debugging.
            issues = [issue.rule_id for issue in validation_report.issues]
            logger.warning(
                "orchestrator.generate.unresolved_scene_graph brand_space_id=%s issues=%s",
                request.brand_space_id,
                issues,
            )
            self._trace_event(
                trace_id,
                self.trace,
                "orchestrator.generate.unresolved_scene_graph",
                {"issues": issues, "generation_path": generation_path},
            )
            self._trace_payload(
                trace_id,
                self.trace,
                "validation_advisory.json",
                {
                    "issues": issues,
                    "status": validation_report.status,
                    "repair_attempts": repair_attempts,
                    "fresh_replan_attempted": fresh_replan_attempted,
                    "generation_path": generation_path,
                    "note": "Application-side validation remained unresolved, but AI rendering will continue because validation is advisory for generation.",
                },
            )
        # Reference selection happens after repairs so asset strategy and scene graph requests are both up to date.
        # Phase 8 resolves the concrete reference images that later prompts or backend rendering may use.
        selected_reference_images = self._select_reference_image_assets(
            request=request,
            creative_decision=creative_decision,
        )
        selected_reference_images, missing_reference_images = self._filter_available_reference_image_assets(
            selected_reference_images,
        )
        if not selected_reference_images:
            explicit_reference_images = self._scene_graph_explicit_reference_assets(
                scene_graph,
                request=request,
            )
            explicit_reference_images, explicit_missing_reference_images = self._filter_available_reference_image_assets(
                explicit_reference_images,
            )
            if explicit_reference_images:
                selected_reference_images = explicit_reference_images
            missing_reference_images.extend(explicit_missing_reference_images)
        conditioning_reference_images = self._conditioning_reference_image_assets(
            selected_reference_images,
            creative_decision=creative_decision,
            request=request,
        )
        scene_graph_reference_images = (
            conditioning_reference_images
            if self._should_use_ai_final_render(request, generation_path, creative_decision)
            else selected_reference_images
        )
        # AI final render gets stricter conditioning assets; backend rendering can bind the broader selected set.
        if missing_reference_images:
            missing_paths = [
                str(asset.get("storage_path") or "").strip()
                for asset in missing_reference_images
                if str(asset.get("storage_path") or "").strip()
            ]
            logger.warning(
                "orchestrator.generate.reference_assets_missing brand_space_id=%s count=%s",
                request.brand_space_id,
                len(missing_reference_images),
            )
            self._trace_payload(
                trace_id,
                self.trace,
                "missing_reference_images",
                {"count": len(missing_reference_images), "storage_paths": missing_paths},
            )
        scene_graph = self.bind_reference_assets(scene_graph, scene_graph_reference_images)
        quality_assessment = self.assess_creative_quality(
            scene_graph=scene_graph,
            creative_decision=creative_decision,
            validation_report=validation_report,
            request=request,
            selected_reference_images=scene_graph_reference_images,
            used_support_fallback=used_support_fallback,
            compiled_context=compiled_context,
        )
        quality_retry_attempts = 0
        skip_pre_image_quality_retries = (
            generation_path == "image_led_social"
            and self._llm_led_static_infographic_format(self._format_name_from_request(request))
            and self._should_use_ai_final_render(request, generation_path, creative_decision)
            and bool(getattr(self.settings, "ai_final_render_skip_pre_image_quality_retries", True))
        )
        if skip_pre_image_quality_retries and float(quality_assessment.get("score") or 0.0) < float(self.settings.image_quality_min_score or self.IMAGE_QUALITY_MIN_SCORE):
            # LLM-led final renders get their quality pass after image generation, so pre-image retries are skipped.
            self._trace_payload(
                trace_id,
                self.trace,
                "pre_image_quality_retry_deferred_to_final_render",
                {
                    "reason": "static_infographic_ai_final_render_uses_output_vision_quality_check",
                    "quality_assessment": quality_assessment,
                    "threshold": float(self.settings.image_quality_min_score or self.IMAGE_QUALITY_MIN_SCORE),
                },
            )
        while (
            generation_path == "image_led_social"
            and not skip_pre_image_quality_retries
            and float(quality_assessment.get("score") or 0.0) < float(self.settings.image_quality_min_score or self.IMAGE_QUALITY_MIN_SCORE)
            and quality_retry_attempts < max(int(self.settings.image_quality_retry_attempts or 1), 0)
        ):
            quality_retry_attempts += 1
            # Quality retry reuses the repair prompt but is driven by premium-quality assessment, not hard validation.
            quality_report = self._quality_report_from_assessment(quality_assessment)
            repair_envelope = self.prompts.compose_scene_graph_repair_envelope(
                user_prompt=request.prompt,
                compiled_context=generation_prompt_context,
                studio_panel=request.studio_panel,
                current_scene_graph=scene_graph.model_dump(mode="json"),
                creative_decision=creative_decision.model_dump(mode="json"),
                validation_report=quality_report.model_dump(mode="json"),
            )
            repair_response = generation_provider.generate_structured_json(
                repair_envelope,
                fallback={
                    "creative_decision": creative_decision.model_dump(mode="json"),
                    "scene_graph": scene_graph.model_dump(mode="json"),
                },
            )
            provider_usage = getattr(generation_provider, "last_usage", None)
            self._trace_payload(
                trace_id,
                self.trace,
                f"quality_retry_{quality_retry_attempts:02d}",
                {
                    "quality_assessment": quality_assessment,
                    "validation_report": quality_report.model_dump(mode="json"),
                    "compiled_context_budget": generation_context_budget,
                    "prompt": {
                        "system": repair_envelope.system,
                        "user": repair_envelope.user,
                    },
                    "response": repair_response,
                    "provider_usage": provider_usage if isinstance(provider_usage, dict) else None,
                },
            )
            creative_decision = self.normalize_creative_decision_payload(
                repair_response.get("creative_decision"),
                creative_decision.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )

            # CRITICAL FIX: Merge repair response into existing scene graph instead of replacing
            # LLM may return only repaired elements, not the complete scene graph
            repaired_scene_graph_dict = repair_response.get("scene_graph", {})
            merged_scene_graph_dict = self._merge_repair_into_scene_graph(
                existing_scene_graph=scene_graph.model_dump(mode="json"),
                repair_scene_graph=repaired_scene_graph_dict,
                repair_attempt=quality_retry_attempts,
            )

            scene_graph = self.normalize_scene_graph_payload(
                merged_scene_graph_dict,
                fallback=scene_graph.model_dump(mode="json"),
                creative_decision=creative_decision,
                text_payload=text_payload.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )
            selected_reference_images = self._select_reference_image_assets(
                request=request,
                creative_decision=creative_decision,
            )
            selected_reference_images, retry_missing_reference_images = self._filter_available_reference_image_assets(
                selected_reference_images,
            )
            if not selected_reference_images:
                explicit_reference_images = self._scene_graph_explicit_reference_assets(
                    scene_graph,
                    request=request,
                )
                explicit_reference_images, explicit_missing_reference_images = self._filter_available_reference_image_assets(
                    explicit_reference_images,
                )
                if explicit_reference_images:
                    selected_reference_images = explicit_reference_images
                retry_missing_reference_images.extend(explicit_missing_reference_images)
            conditioning_reference_images = self._conditioning_reference_image_assets(
                selected_reference_images,
                creative_decision=creative_decision,
                request=request,
            )
            scene_graph_reference_images = (
                conditioning_reference_images
                if self._should_use_ai_final_render(request, generation_path, creative_decision)
                else selected_reference_images
            )
            scene_graph = self.bind_reference_assets(scene_graph, scene_graph_reference_images)
            validation_report = self.validate_scene_graph(
                scene_graph=scene_graph,
                creative_decision=creative_decision,
                request=request,
                compiled_context=compiled_context,
            )
            quality_assessment = self.assess_creative_quality(
                scene_graph=scene_graph,
                creative_decision=creative_decision,
                validation_report=validation_report,
                request=request,
                selected_reference_images=scene_graph_reference_images,
                used_support_fallback=used_support_fallback,
                compiled_context=compiled_context,
            )
        self.guardrails.validate_output(
            f"{text_payload.headline}\n{text_payload.body}\n{text_payload.cta}",
            request.resolved_brand_context.get("guardrails", {}),
        )

        # Phase 9 decides whether final image generation should trust the repaired graph or restart from a safer fallback graph.
        scene_graph_ignored_for_final_render = self._should_ignore_scene_graph_for_final_render(
            generation_path=generation_path,
            fresh_replan_attempted=fresh_replan_attempted,
            validation_report=validation_report,
            scene_graph=scene_graph,
            compiled_context=compiled_context,
            style_reference_final_render_active=self._style_reference_only_carousel_active(request, creative_decision)
            and self._should_use_ai_final_render(request, generation_path, creative_decision),
        )
        final_render_scene_graph = scene_graph
        final_render_retry_note: str | None = None
        if scene_graph_ignored_for_final_render:
            # When the graph remains unreliable, final render receives a clean fallback graph plus an explicit retry note.
            final_render_retry_note = self._final_render_ignore_note(validation_report)
            final_render_scene_graph_payload = self._fallback_image_led_scene_graph(
                request=request,
                text_payload=text_payload.model_dump(mode="json"),
                creative_decision=creative_decision.model_dump(mode="json"),
                compiled_context=compiled_context,
            )
            final_render_scene_graph = self.normalize_scene_graph_payload(
                final_render_scene_graph_payload,
                fallback=fallback_scene_graph,
                creative_decision=creative_decision,
                text_payload=text_payload.model_dump(mode="json"),
                request=request,
                compiled_context=compiled_context,
            )
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_scene_graph_override",
                {
                    "reason": "scene_graph_rejected_after_fresh_replan",
                    "issues": [issue.rule_id for issue in validation_report.issues],
                    "retry_note": final_render_retry_note,
                    "scene_graph": final_render_scene_graph.model_dump(mode="json"),
                },
            )

        visual_explanation_plan = self._visual_explanation_plan(
            request,
            text_payload,
            creative_decision,
            selected_reference_images,
            message_strategy,
        )
        # The visual explanation plan bridges copy strategy into image prompt language and metadata.
        low_quality_plan_deferred_to_final_render = (
            request.generate_image
            and not scene_graph_ignored_for_final_render
            and self._should_block_low_quality_final_render(quality_assessment)
            and self._should_defer_low_quality_plan_to_llm_final_render(
                request=request,
                generation_path=generation_path,
                creative_decision=creative_decision,
            )
        )
        if low_quality_plan_deferred_to_final_render:
            quality_improvement_note = self._final_render_quality_improvement_note(quality_assessment)
            final_render_retry_note = " ".join(
                part
                for part in [
                    final_render_retry_note,
                    quality_improvement_note,
                ]
                if part
            ).strip()
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_low_quality_plan_deferred",
                {
                    "quality_assessment": quality_assessment,
                    "reason": "static_infographic_llm_led_final_render_can_recompose",
                    "retry_note": final_render_retry_note,
                },
            )
        if (
            request.generate_image
            and not scene_graph_ignored_for_final_render
            and self._should_block_low_quality_final_render(quality_assessment)
            and not low_quality_plan_deferred_to_final_render
        ):
            self._trace_payload(
                trace_id,
                self.trace,
                "final_render_blocked_low_quality",
                {
                    "quality_assessment": quality_assessment,
                    "reason": "quality_below_hard_threshold_before_render",
                },
            )
            raise GenerationFailureError(
                "Final render blocked because the planned creative is still below the minimum premium-quality threshold.",
                failure_type="validation_failure",
                reason_code="final_render_quality_blocked",
                user_safe_message="The current creative draft did not meet the minimum quality threshold, so generation was stopped instead of returning a weak result.",
                retryable=True,
                suggested_next_action="Refine the scene graph, reference conditioning, or slide structure and try generation again.",
                details={"quality_assessment": quality_assessment},
            )
        self._trace_payload(
            trace_id,
            self.trace,
            "visual_explanation_plan",
            visual_explanation_plan,
        )

        tone_started_at = perf_counter()
        # Tone evaluation runs after visual planning so rewrite guidance can include the final message strategy.
        tone_analysis = self.tone.evaluate(
            content=f"{text_payload.headline}. {text_payload.body}. {text_payload.cta}",
            brand_context=request.resolved_brand_context,
            persona_context=request.persona_context,
            content_payload=text_payload.model_dump(mode="json"),
            message_strategy=message_strategy.model_dump(mode="json"),
            objective_context=request.objective_context,
        )
        latency_ms["tone_ms"] = round((perf_counter() - tone_started_at) * 1000, 2)

        blueprint_started_at = perf_counter()
        # Blueprint converts the selected scene graph into deterministic render zones for the response contract.
        blueprint = self.blueprints.from_scene_graph(
            scene_graph=final_render_scene_graph if scene_graph_ignored_for_final_render else scene_graph,
            studio_panel=request.studio_panel,
            text_payload=text_payload.model_dump(mode="json"),
            brand_rules_applied=BlueprintService._brand_rules_applied(request.resolved_brand_context),
        )
        latency_ms["blueprint_ms"] = round((perf_counter() - blueprint_started_at) * 1000, 2)

        generated_assets: list[GeneratedImageAsset] = []
        final_render_assets: list[GeneratedImageAsset] = []
        final_render_asset: GeneratedImageAsset | None = None
        render_authority = "backend"
        image_generation_error: str | None = None
        # Phase 10 either lets AI render final assets or keeps backend rendering as the authority for the response.
        image_size = self._image_generation_size(request.studio_panel)
        final_render_format_name = str(request.studio_panel.get("format") or "").strip().lower()
        is_carousel_render = final_render_format_name == "carousel"
        carousel_slide_specs = (
            self._build_carousel_slide_specs(
                text_payload,
                request=request,
                creative_decision=creative_decision,
            )
            if is_carousel_render
            else []
        )
        if carousel_slide_specs:
            # Store normalized carousel specs back into metadata so render/export paths use the same slide contract.
            text_payload = text_payload.model_copy(
                update={
                    "metadata": {
                        **(text_payload.metadata or {}),
                        "carousel_slide_specs": carousel_slide_specs,
                    }
                }
            )
        ai_final_render_required = self._should_use_ai_final_render(request, generation_path, creative_decision)
        if ai_final_render_required:
            try:
                final_render_started_at = perf_counter()
                # Final render loops per slide so carousel pages can each carry their own prompt, sample, and overlay metadata.
                # Non-carousel renders are treated as a one-slide sequence so final render code can stay unified.
                slide_specs = (
                    carousel_slide_specs
                    if is_carousel_render
                    else [
                        {
                            "role": "single",
                            "headline": text_payload.headline,
                            "supporting_line": (text_payload.metadata or {}).get("supporting_line") or text_payload.body,
                            "proof_points": self._normalize_metadata_list((text_payload.metadata or {}).get("proof_points"), limit=3),
                            "cta": text_payload.cta,
                            "slide_index": 1,
                            "slide_count": 1,
                        }
                    ]
                )
                logo_storage_path, requested_logo_variant = self._select_logo_candidate_for_render(
                    request=request,
                    creative_decision=creative_decision,
                    scene_graph=final_render_scene_graph,
                )
                exact_logo_overlay_required = bool(logo_storage_path and self.storage.exists(logo_storage_path))
                layout_prompt_reference_images = selected_reference_images if final_render_format_name in {"static", "infographic"} else []
                # Prompt references may include layout context, while conditioning references must be safe for direct image input.
                prompt_reference_images = self._merge_reference_asset_lists(
                    conditioning_reference_images,
                    layout_prompt_reference_images,
                )
                conditioning_reference_images, skipped_logo_reference_images = self._filter_logo_bearing_conditioning_reference_images(
                    conditioning_reference_images,
                    exact_logo_overlay_required=exact_logo_overlay_required,
                )
                topic_reference_images = self._topic_relevant_reference_assets(
                    request,
                    creative_decision=creative_decision,
                    limit=2,
                )
                if self._reference_layout_adaptation_active(
                    request,
                    creative_decision=creative_decision,
                    compiled_context=compiled_context,
                    reference_images=conditioning_reference_images,
                ):
                    # When adapting to a reference layout, topic references are suppressed to avoid competing structures.
                    topic_reference_images = []
                if topic_reference_images:
                    prompt_reference_images = self._merge_reference_asset_lists(topic_reference_images, prompt_reference_images)
                    conditioning_topic_reference_images = self._conditioning_reference_image_assets(
                        topic_reference_images,
                        creative_decision=creative_decision,
                        request=request,
                    )
                    conditioning_reference_images = self._merge_reference_asset_lists(
                        conditioning_topic_reference_images,
                        conditioning_reference_images,
                    )
                    conditioning_reference_images, topic_skipped_logo_reference_images = self._filter_logo_bearing_conditioning_reference_images(
                        conditioning_reference_images,
                        exact_logo_overlay_required=exact_logo_overlay_required,
                    )
                    if topic_skipped_logo_reference_images:
                        skipped_logo_reference_images = self._merge_reference_asset_lists(
                            skipped_logo_reference_images,
                            topic_skipped_logo_reference_images,
                        )
                if skipped_logo_reference_images:
                    prompt_reference_images = self._merge_reference_asset_lists(
                        conditioning_reference_images,
                        skipped_logo_reference_images,
                        layout_prompt_reference_images,
                    )
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        "final_render_logo_bearing_reference_images_skipped",
                        {
                            "reason": "exact_logo_overlay_requires_logo_free_conditioning",
                            "skipped_storage_paths": [
                                str(asset.get("storage_path") or "")
                                for asset in skipped_logo_reference_images
                            ],
                            "logo_storage_path": logo_storage_path,
                        },
                    )
                pdf_reference_conditioning_cache: dict[str, str] = {}
                for slide in slide_specs:
                    slide_index = int(slide.get("slide_index") or 1)
                    slide_count = int(slide.get("slide_count") or len(slide_specs))
                    text_overlay_strategy = self._ai_final_render_text_strategy(
                        request,
                        creative_decision,
                    )
                    if self._style_reference_only_carousel_active(request, creative_decision):
                        text_overlay_strategy = "ai_renders_finished_slide_with_exact_copy"
                    use_backend_text_overlay = (
                        text_overlay_strategy == "backend_exact_text_on_ai_text_safe_substrate"
                    )
                    trace_suffix = f"_slide_{slide_index:02d}" if is_carousel_render else ""
                    slide_prompt_reference_images = (
                        self._slide_reference_images(
                            slide,
                            prompt_reference_images,
                            request=request,
                            creative_decision=creative_decision,
                        )
                        if is_carousel_render
                        else [dict(asset) for asset in prompt_reference_images if isinstance(asset, dict)]
                    )
                    slide_prompt_reference_images = self._merge_slide_layout_anchor_reference(
                        slide,
                        slide_prompt_reference_images,
                        request=request,
                        creative_decision=creative_decision,
                    )
                    slide_conditioning_reference_images = (
                        self._slide_reference_images(
                            slide,
                            conditioning_reference_images,
                            request=request,
                            creative_decision=creative_decision,
                        )
                        if is_carousel_render
                        else [dict(asset) for asset in conditioning_reference_images if isinstance(asset, dict)]
                    )
                    slide_conditioning_reference_images = self._merge_slide_layout_anchor_reference(
                        slide,
                        slide_conditioning_reference_images,
                        request=request,
                        creative_decision=creative_decision,
                        for_conditioning=True,
                    )
                    slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                    desired_reference_path = str(slide_metadata.get("reference_asset_path") or "").strip()
                    if desired_reference_path and not slide_prompt_reference_images:
                        # Slide metadata can pin a specific reference asset even if generic selection returned nothing.
                        matched_reference_asset = self._reference_asset_by_storage_path(request, desired_reference_path)
                        if matched_reference_asset is not None:
                            slide_prompt_reference_images = [matched_reference_asset]
                    if desired_reference_path and not slide_conditioning_reference_images:
                        matched_reference_asset = self._reference_asset_by_storage_path(request, desired_reference_path)
                        if (
                            matched_reference_asset is not None
                            and self._is_conditioning_safe_reference_image_asset(
                                matched_reference_asset,
                                creative_decision=creative_decision,
                                request=request,
                            )
                        ):
                            slide_conditioning_reference_images = [matched_reference_asset]
                    if (
                        is_carousel_render
                        and not slide_conditioning_reference_images
                        and not str(slide_metadata.get("reference_asset_path") or "").strip()
                    ):
                        slide_conditioning_reference_images = [
                            dict(asset) for asset in conditioning_reference_images if isinstance(asset, dict)
                        ]
                    reference_image_paths = self._conditioning_reference_image_paths(
                        slide_conditioning_reference_images,
                        request=request,
                        cache=pdf_reference_conditioning_cache,
                    )
                    # Layout analysis can use prompt plus conditioning references to build or enhance sample blueprints.
                    layout_analysis_reference_images = (
                        self._layout_analysis_reference_image_assets(
                            self._merge_reference_asset_lists(
                                slide_prompt_reference_images,
                                slide_conditioning_reference_images,
                            ),
                            request=request,
                        )
                        if not is_carousel_render
                        else [dict(asset) for asset in slide_conditioning_reference_images if isinstance(asset, dict)]
                    )
                    layout_reference_image_paths = self._conditioning_reference_image_paths(
                        layout_analysis_reference_images,
                        request=request,
                        cache=pdf_reference_conditioning_cache,
                    )
                    reference_layout_adaptation_active = self._reference_layout_adaptation_active(
                        request,
                        creative_decision=creative_decision,
                        metadata=slide_metadata,
                        compiled_context=compiled_context,
                        reference_images=layout_analysis_reference_images,
                    )
                    if reference_layout_adaptation_active:
                        # Feature 1 adapts slide layout/copy to the selected sample before render prompt creation.
                        slide = self._annotate_slide_with_sample_page_blueprint(
                            slide,
                            reference_image_paths=layout_reference_image_paths,
                        )
                        if layout_reference_image_paths:
                            metadata = dict(slide.get("metadata") or {})
                            blueprint = metadata.get("sample_page_blueprint") if isinstance(metadata.get("sample_page_blueprint"), dict) else {}
                            enhanced_blueprint = self._vision_enhance_page_blueprint(
                                image_path=layout_reference_image_paths[0],
                                base_blueprint=blueprint,
                            )
                            if enhanced_blueprint:
                                metadata["sample_page_blueprint"] = enhanced_blueprint
                                slide = {**slide, "metadata": metadata}
                        slide = self._adapt_slide_copy_to_sample_blueprint(
                            slide,
                            content_metadata=text_payload.metadata if isinstance(text_payload.metadata, dict) else {},
                        )
                        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                    if is_carousel_render:
                        # Carousel flow: this branch returns one prompt per slide because each page has its own role, copy, and reference sample.
                        # Carousel slides get slide-specific prompts because copy, role, and reference image can differ.
                        final_render_prompt = self.build_carousel_slide_render_prompt(
                            request=request,
                            creative_decision=creative_decision,
                            message_strategy=message_strategy,
                            slide=slide,
                            scene_graph=final_render_scene_graph,
                            reference_images=slide_prompt_reference_images,
                            retry_note=final_render_retry_note,
                            visual_explanation_plan=visual_explanation_plan,
                            compiled_context=compiled_context,
                        )
                    else:
                        # Static/infographic flow: this branch returns one whole-canvas prompt, with metadata controlling poster vs modular explainer behavior.
                        prompt_text_payload = text_payload
                        if slide_metadata:
                            # Single-frame prompts merge slide metadata so sample/layout signals stay visible.
                            prompt_text_payload = text_payload.model_copy(
                                update={
                                    "metadata": {
                                        **(text_payload.metadata or {}),
                                        **slide_metadata,
                                    }
                                }
                            )
                        final_render_prompt = self.build_final_render_prompt(
                            request=request,
                            text_payload=prompt_text_payload,
                            creative_decision=creative_decision,
                            scene_graph=final_render_scene_graph,
                            message_strategy=message_strategy,
                            reference_images=prompt_reference_images,
                            retry_note=final_render_retry_note,
                            visual_explanation_plan=visual_explanation_plan,
                            compiled_context=compiled_context,
                        )
                    final_render_prompt = self._enforce_style_reference_only_final_prompt(
                        final_render_prompt,
                        request=request,
                        creative_decision=creative_decision,
                        metadata=slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {},
                    )
                    self._trace_payload(
                        trace_id,
                        self.trace,
                        f"final_render_prompt{trace_suffix}",
                    {
                        "prompt": final_render_prompt,
                        "size": image_size,
                        "model": self.settings.image_model,
                        **self._prompt_length_metrics(final_render_prompt),
                        "text_overlay_strategy": text_overlay_strategy,
                        "slide_index": slide_index,
                        "slide_count": slide_count,
                        "role": slide.get("role"),
                    },
                    )
                    sample_page_blueprint = (
                        slide_metadata.get("sample_page_blueprint")
                        if isinstance(slide_metadata.get("sample_page_blueprint"), dict)
                        else None
                    )
                    legal_footer_text = self._scene_graph_legal_footer_text(final_render_scene_graph)
                    # The sample guard retries image generation when output drifts too far from the selected reference.
                    asset, sample_similarity_report, sample_similarity_retry_attempts, final_render_prompt = (
                        self._generate_final_render_image_with_sample_guard(
                            image_provider=image_provider,
                            request=request,
                            prompt=final_render_prompt,
                            reference_image_paths=reference_image_paths,
                            image_size=image_size,
                            trace_id=trace_id,
                            trace_suffix=trace_suffix,
                            sample_blueprint=sample_page_blueprint,
                            slide_index=slide_index,
                            slide_count=slide_count,
                            approved_text_payload=text_payload,
                            exact_logo_overlay_deferred=exact_logo_overlay_required,
                            legal_footer_overlay_deferred=bool(legal_footer_text),
                            sample_reference_image_paths=layout_reference_image_paths,
                        )
                    )
                    base_asset = GeneratedImageAsset(
                        asset_id=uuid4(),
                        mime_type=asset["mime_type"],
                        storage_path=asset["storage_path"],
                        width=asset["width"],
                        height=asset["height"],
                        asset_role=asset["asset_role"],
                        metadata={
                            "provider": asset.get("provider"),
                            "model": asset.get("model") or self.settings.image_model,
                            "requested_size": asset.get("size", image_size),
                            "generation_path": generation_path,
                            "generation_stage": "base_final_render",
                            "reference_conditioned": bool(reference_image_paths),
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                            "carousel_role": slide.get("role"),
                            "provider_usage": asset.get("provider_usage"),
                        },
                    )
                    slide_text_payload = StructuredTextPayload(
                        headline=str(slide.get("headline") or text_payload.headline),
                        body=self._carousel_slide_body_text(
                            slide,
                            fallback_text=str(slide.get("supporting_line") or text_payload.body),
                        ),
                        cta=str(slide.get("cta") or text_payload.cta),
                        hashtags=text_payload.hashtags,
                        metadata={
                            **(text_payload.metadata or {}),
                            "source": (
                                "structured_slide_spec"
                                if any(
                                    str(slide.get(field) or "").strip()
                                    for field in ("headline", "supporting_line", "body", "visual_focus", "cta")
                                )
                                or bool(slide.get("proof_points") or slide.get("body_points") or slide.get("stat_highlights"))
                                else str(((text_payload.metadata or {}) if isinstance(text_payload.metadata, dict) else {}).get("source") or "fallback")
                            ),
                            "supporting_line": str(slide.get("supporting_line") or text_payload.body),
                            "proof_points": list(slide.get("proof_points") or []),
                            "body_points": list(slide.get("body_points") or []),
                            "stat_highlights": list(slide.get("stat_highlights") or []),
                            "visual_focus": str(slide.get("visual_focus") or ""),
                            "transition_note": str(slide.get("transition_note") or ""),
                            "slide_role": str(slide.get("role") or ""),
                            "slide_index": slide_index,
                            "slide_count": slide_count,
                        },
                    )
                    final_asset_payload = asset
                    # Metadata here is the export-time authority for overlays, references, QA, and sample alignment.
                    final_asset_metadata = {
                        "render_source": "ai",
                        "generation_stage": "final_render",
                        "provider": asset.get("provider"),
                        "model": asset.get("model") or self.settings.image_model,
                        **self._prompt_length_metrics(final_render_prompt),
                        "text_overlay_strategy": text_overlay_strategy,
                        "requested_size": asset.get("size", image_size),
                        "provider_usage": asset.get("provider_usage"),
                        "generation_path": generation_path,
                        "layout_mode": creative_decision.layout_mode,
                        "scene_graph_used": not scene_graph_ignored_for_final_render,
                        "scene_graph_ignored_for_final_render": scene_graph_ignored_for_final_render,
                        "logo_composited_by_ai": False,
                        "base_storage_path": base_asset.storage_path,
                        "reference_conditioned_by_ai": bool(reference_image_paths),
                        "reference_image_storage_paths": [str(reference_asset.get("storage_path") or "") for reference_asset in slide_conditioning_reference_images],
                        "logo_bearing_reference_image_storage_paths_skipped": [str(reference_asset.get("storage_path") or "") for reference_asset in skipped_logo_reference_images],
                        "visual_explanation_mode": visual_explanation_plan.get("mode"),
                        "visual_explanation_need": visual_explanation_plan.get("need"),
                        "visual_explanation_density": visual_explanation_plan.get("density"),
                        "visual_explanation_rationale": visual_explanation_plan.get("rationale"),
                        "quality_assessment": quality_assessment,
                        "quality_retry_attempts": quality_retry_attempts,
                        "output_quality_assessment": asset.get("output_quality_assessment"),
                        "output_quality_retry_attempts": asset.get("output_quality_retry_attempts", 0),
                        "output_quality_status": asset.get("output_quality_status"),
                        "output_quality_blocking_issues": asset.get("output_quality_blocking_issues", []),
                        "output_quality_user_warning": asset.get("output_quality_user_warning"),
                        "sample_page_blueprint": sample_page_blueprint,
                        "sample_page_conditioning_image_path": reference_image_paths[0] if reference_image_paths else None,
                        "sample_page_layout_image_path": layout_reference_image_paths[0] if layout_reference_image_paths else None,
                        "sample_visual_similarity": sample_similarity_report,
                        "sample_visual_similarity_retry_attempts": sample_similarity_retry_attempts,
                        "slide_index": slide_index,
                        "slide_count": slide_count,
                        "carousel_role": slide.get("role"),
                    }
                    if use_backend_text_overlay:
                        # Backend overlay keeps exact approved text out of the image model when that strategy is selected.
                        filtered_scene = final_render_scene_graph.model_copy(deep=True)
                        if final_render_format_name not in self.BACKEND_TEXT_OVERLAY_SCENE_GRAPH_FORMATS:
                            filtered_scene.elements = [e for e in filtered_scene.elements if e.role in ("legal", "footer", "disclaimer")]
                        final_asset_metadata["render_overlay_scene_graph"] = filtered_scene.model_dump(mode="json")

                        filtered_text = slide_text_payload.model_copy(deep=True)
                        filtered_metadata = (filtered_text.metadata or {}).copy()
                        filtered_text.metadata = filtered_metadata
                        final_asset_metadata["render_overlay_text"] = filtered_text.model_dump(mode="json")
                    final_asset_metadata = self._enforce_style_reference_only_final_asset_metadata(
                        final_asset_metadata,
                        request=request,
                        creative_decision=creative_decision,
                        slide_metadata=slide_metadata,
                    )
                    if requested_logo_variant:
                        final_asset_metadata["requested_logo_variant"] = requested_logo_variant
                    if logo_storage_path and self.storage.exists(logo_storage_path):
                        # Exact logos are deferred to export so the image model cannot redraw or distort them.
                        final_asset_metadata["logo_source_storage_path"] = logo_storage_path
                        final_asset_metadata["logo_overlay_strategy"] = "exact_asset_overlay"
                        self._trace_payload(
                            trace_id,
                            self.trace,
                            f"logo_overlay_deferred_to_export{trace_suffix}",
                            {
                                "reason": "preserve_exact_logo_asset",
                                "logo_storage_path": logo_storage_path,
                                "requested_logo_variant": requested_logo_variant,
                                "base_storage_path": base_asset.storage_path,
                                "slide_index": slide_index,
                                "slide_count": slide_count,
                            },
                        )
                    final_render_assets.append(
                        GeneratedImageAsset(
                            asset_id=uuid4(),
                            mime_type=final_asset_payload["mime_type"],
                            storage_path=final_asset_payload["storage_path"],
                            width=final_asset_payload["width"],
                            height=final_asset_payload["height"],
                            asset_role="render_preview" if slide_index == 1 else "render_export",
                            metadata=final_asset_metadata,
                        )
                    )
                latency_ms["final_render_ms"] = round((perf_counter() - final_render_started_at) * 1000, 2)
                final_render_asset = final_render_assets[0] if final_render_assets else None
                render_authority = "ai"
                self._trace_payload(
                    trace_id,
                    self.trace,
                    "final_render_generation",
                    {
                        "slide_count": len(final_render_assets),
                        "assets": [asset.model_dump(mode="json") for asset in final_render_assets],
                    },
                )
            except GenerationFailureError:
                raise
            except Exception as exc:  # pragma: no cover - resilience path
                image_generation_error = str(exc)
                latency_ms["final_render_ms"] = round(latency_ms.get("final_render_ms", 0.0), 2)
                logger.warning(
                    "orchestrator.generate.final_render_failed brand_space_id=%s error=%s",
                    request.brand_space_id,
                    image_generation_error,
                )
                self._trace_payload(trace_id, self.trace, "final_render_error", {"error": image_generation_error})
        if ai_final_render_required and final_render_asset is None:
            raise GenerationFailureError(
                self._ai_final_render_failure_message(),
                failure_type="provider_failure",
                reason_code="ai_final_render_failed",
                user_safe_message="I couldn't generate the visual this time. Please regenerate.",
                retryable=True,
                rule_source="system",
                suggested_next_action="Regenerate the creative.",
                details={
                    "generation_path": generation_path,
                    "underlying_error": image_generation_error,
                },
            )
        if (
            self._image_generation_requested(request)
            and not ai_final_render_required
            and final_render_asset is None
            and self._needs_generated_image_with_storage(scene_graph, creative_decision)
        ):
            try:
                image_started_at = perf_counter()
                # This path creates supporting generated assets for backend-rendered layouts, not final AI composites.
                image_prompt = self.build_image_prompt(
                    request,
                    text_payload,
                    creative_decision,
                    message_strategy,
                    compiled_context=compiled_context,
                    visual_explanation_plan=visual_explanation_plan,
                )
                self._trace_payload(
                    trace_id,
                    self.trace,
                    "image_prompt",
                    {"prompt": image_prompt, **self._prompt_length_metrics(image_prompt)},
                )
                asset = self._generate_image_with_retries(
                    image_provider=image_provider,
                    tenant_id=request.tenant_id,
                    brand_space_id=request.brand_space_id,
                    prompt=image_prompt,
                    size=image_size,
                    trace_id=trace_id,
                    trace_label="image_generation",
                )
                latency_ms["image_generation_ms"] = round((perf_counter() - image_started_at) * 1000, 2)
                self._trace_payload(trace_id, self.trace, "image_generation", asset)
                generated_assets.append(
                    GeneratedImageAsset(
                        asset_id=uuid4(),
                        mime_type=asset["mime_type"],
                        storage_path=asset["storage_path"],
                        width=asset["width"],
                        height=asset["height"],
                        asset_role=asset["asset_role"],
                        metadata={
                            "provider": asset.get("provider"),
                            "model": asset.get("model") or self.settings.image_model,
                            "requested_size": asset.get("size", image_size),
                            "provider_usage": asset.get("provider_usage"),
                            "generation_path": generation_path,
                            **self._prompt_length_metrics(image_prompt),
                            "visual_explanation_mode": visual_explanation_plan.get("mode"),
                            "visual_explanation_need": visual_explanation_plan.get("need"),
                            "visual_explanation_density": visual_explanation_plan.get("density"),
                            "visual_explanation_rationale": visual_explanation_plan.get("rationale"),
                        },
                    )
                )
            except Exception as exc:  # pragma: no cover - resilience path
                image_generation_error = str(exc)
                latency_ms["image_generation_ms"] = round(latency_ms.get("image_generation_ms", 0.0), 2)
                logger.warning(
                    "orchestrator.generate.image_failed brand_space_id=%s error=%s",
                    request.brand_space_id,
                    image_generation_error,
                )
                self._trace_payload(trace_id, self.trace, "image_generation_error", {"error": image_generation_error})
        scene_graph = self.bind_generated_assets(scene_graph, generated_assets)
        # Rebuild the blueprint after generated assets are bound so the returned contract matches final graph state.
        blueprint = self.blueprints.from_scene_graph(
            scene_graph=scene_graph,
            studio_panel=request.studio_panel,
            text_payload=text_payload.model_dump(mode="json"),
            brand_rules_applied=BlueprintService._brand_rules_applied(request.resolved_brand_context),
        )

        explainability = {
            "retrieval_channels": [channel for channel, items in request.retrieved_knowledge.items() if items],
            "retrieval_match_counts": {
                channel: len(items)
                for channel, items in request.retrieved_knowledge.items()
            },
            "guardrails_applied": request.resolved_brand_context.get("guardrails", {}),
            "selected_persona": request.persona_context,
            "selected_objective": request.objective_context,
            "brand_context_snapshot": request.resolved_brand_context,
            "research_summary": research_summary,
            "conversation_context": request.conversation_context,
            "session_memory": request.session_memory,
            "template_context_used": bool(request.template_context),
            "context_resolution": plan.metadata,
            "generation_path": generation_path,
            "message_strategy": message_strategy.model_dump(mode="json"),
            "layout_decision": creative_decision.model_dump(mode="json"),
            "creative_decision": creative_decision.model_dump(mode="json"),
            "scene_graph": scene_graph.model_dump(mode="json"),
            "final_render_scene_graph": final_render_scene_graph.model_dump(mode="json") if scene_graph_ignored_for_final_render else None,
            "validation_report": validation_report.model_dump(mode="json"),
            "content_semantic_validation": content_semantic_report,
            "content_semantic_repair_attempts": content_semantic_repair_attempts,
            "repair_attempts": repair_attempts,
            "fresh_replan_attempted": fresh_replan_attempted,
            "scene_graph_ignored_for_final_render": scene_graph_ignored_for_final_render,
            "render_authority": render_authority,
            "planning_hints": request.layout_decision,
            "compiled_context": compiled_context,
            "visual_grounding": compiled_context.get("visual_grounding_diagnostics")
            or compiled_context.get("visual_knowledge_brief")
            or {},
            "selected_reference_images": selected_reference_images,
            "conditioning_reference_images": conditioning_reference_images,
            "visual_explanation_plan": visual_explanation_plan,
            "visual_explanation_mode": visual_explanation_plan.get("mode"),
            "visual_explanation_need": visual_explanation_plan.get("need"),
            "visual_explanation_density": visual_explanation_plan.get("density"),
            "quality_assessment": quality_assessment,
            "quality_retry_attempts": quality_retry_attempts,
            "providers": {
                "research": research_provider.provider_name,
                "generation": generation_provider.provider_name,
                "image": image_provider.provider_name,
            },
            "generation_trace": generation_trace.model_dump(mode="json"),
            "input_access_summary": input_access_tracker.build_summary(),
        }
        explainability["token_usage"] = self.estimate_token_usage(
            input_segments=[
                json.dumps(message_strategy.model_dump(mode="json"), default=str),
                planning_envelope.system,
                planning_envelope.user,
                research_summary,
                json.dumps(request.reference_assets, default=str),
                json.dumps(selected_reference_images, default=str),
            ],
            output_segments=[
                text_payload.headline,
                text_payload.body,
                text_payload.cta,
                " ".join(text_payload.hashtags),
                json.dumps(text_payload.metadata, default=str),
            ],
        )
        explainability["latency_ms"] = {
            **latency_ms,
            "total_ms": round((perf_counter() - started_at) * 1000, 2),
        }
        explainability["generation_trace_id"] = trace_id
        if image_generation_error:
            explainability["image_generation_error"] = image_generation_error
        if final_render_asset is not None:
            explainability["final_render_asset"] = final_render_asset.model_dump(mode="json")
        if final_render_assets:
            explainability["final_render_assets"] = [
                asset.model_dump(mode="json") for asset in final_render_assets
            ]
        # The response is the final pipeline handoff: API/worker code persists it and render/export code reads its contracts.
        # Static/infographic usually return one final_render_asset; carousel may return one final_render_asset plus all slide assets.
        self._trace_payload(
            trace_id,
            self.trace,
            "orchestrator_final",
            {
                "message_strategy": message_strategy.model_dump(mode="json"),
                "text": text_payload.model_dump(mode="json"),
                "creative_decision": creative_decision.model_dump(mode="json"),
                "scene_graph": scene_graph.model_dump(mode="json"),
                "validation_report": validation_report.model_dump(mode="json"),
                "content_semantic_validation": content_semantic_report,
                "content_semantic_repair_attempts": content_semantic_repair_attempts,
                "blueprint": blueprint.model_dump(mode="json"),
                "image_assets": [asset.model_dump(mode="json") for asset in generated_assets],
                "final_render_asset": final_render_asset.model_dump(mode="json") if final_render_asset else None,
                "final_render_assets": [asset.model_dump(mode="json") for asset in final_render_assets],
                "render_authority": render_authority,
                "explainability": explainability,
                "tone_analysis": tone_analysis,
            },
        )
        cost_estimation = self.trace.build_cost_estimation(trace_id)
        if cost_estimation:
            explainability["cost_estimation"] = cost_estimation
            self._trace_payload(trace_id, self.trace, "cost_estimation", cost_estimation)
        # This is why the method returns AIOrchestrationResponse: it carries the common output contract for all formats.
        # Static/infographic consumers usually read final_render_asset; carousel consumers can read final_render_assets per slide.
        return AIOrchestrationResponse(
            message_strategy=message_strategy,
            text=text_payload,
            creative_decision=creative_decision,
            scene_graph=scene_graph,
            validation_report=validation_report,
            repair_attempts=repair_attempts,
            blueprint=BlueprintPayload(**blueprint.model_dump()),
            image_assets=generated_assets,
            final_render_assets=final_render_assets,
            final_render_asset=final_render_asset,
            render_authority=render_authority,
            explainability=explainability,
            tone_analysis=tone_analysis,
            generation_trace=generation_trace,
        )

    @staticmethod
    def _structure_text_payload_for_layout(
        text_payload: StructuredTextPayload,
        *,
        studio_panel: dict[str, Any] | None = None,
        compiled_context: dict[str, Any] | None = None,
    ) -> StructuredTextPayload:
        # Builds structure text layout from copy payload, studio settings, and compiled context for AIOrchestratorService.
        # It calls _strip_blocked_compliance_phrases and _dedupe_metadata_collection while assembling the payload or prompt text.
        studio_panel = studio_panel if isinstance(studio_panel, dict) else {}
        context = dict(compiled_context or {})
        format_name = str(studio_panel.get("format") or "").strip().casefold() or "static"
        content_format_brief = (
            dict(context.get("content_format_brief"))
            if isinstance(context.get("content_format_brief"), dict)
            else {}
        )
        content_format_brief.setdefault("format", format_name)
        context["content_format_brief"] = content_format_brief

        source_metadata = dict(text_payload.metadata or {})
        source_metadata.setdefault("headline", text_payload.headline)
        source_metadata.setdefault("cta", text_payload.cta)
        source = {
            "headline": text_payload.headline,
            "body": text_payload.body,
            "cta": text_payload.cta,
            "hashtags": list(text_payload.hashtags or []),
            "metadata": source_metadata,
        }
        normalized = AIOrchestratorService.normalize_text_payload(
            source,
            source,
            compiled_context=context,
        )

        def strip_text(value: Any, *, limit: int = 260) -> str:
            # Extracts strip text from input value and limit for AIOrchestratorService.
            # It calls _normalize_metadata_text and _strip_blocked_compliance_phrases to turn raw evidence into the structured signal the caller needs.
            return AIOrchestratorService._normalize_metadata_text(
                AIOrchestratorService._strip_blocked_compliance_phrases(
                    AIOrchestratorService._coerce_text_value(value)
                ),
                limit=limit,
            )

        headline = strip_text(normalized.get("headline"), limit=180)
        body = AIOrchestratorService._strip_blocked_compliance_phrases(normalized.get("body") or "")
        cta = strip_text(normalized.get("cta"), limit=90)
        hashtags = [
            tag
            for tag in [
                AIOrchestratorService._strip_blocked_compliance_phrases(tag)
                for tag in normalized.get("hashtags", [])
            ]
            if tag
        ]
        metadata = dict(normalized.get("metadata") or {})
        supporting_line = strip_text(metadata.get("supporting_line"), limit=180)
        sentences = [
            strip_text(sentence, limit=220)
            for sentence in AIOrchestratorService._sentences(body)
            if strip_text(sentence, limit=220)
        ]
        blocked = [headline, supporting_line, cta]
        proof_points = [
            strip_text(item, limit=180)
            for item in AIOrchestratorService._normalize_metadata_list(metadata.get("proof_points"), limit=6)
        ]
        proof_points = AIOrchestratorService._dedupe_metadata_collection(
            [*proof_points, *sentences],
            blocked_texts=blocked,
            limit=4 if format_name == "infographic" else 3,
        )
        numeric_candidates = [
            sentence
            for sentence in sentences
            if re.search(r"(?:\d|%|₹|rs\.?|\$|lakh|crore|million|billion)", sentence, flags=re.IGNORECASE)
        ]
        stat_highlights = [
            strip_text(item, limit=180)
            for item in AIOrchestratorService._normalize_metadata_list(metadata.get("stat_highlights"), limit=4)
        ]
        stat_highlights = AIOrchestratorService._dedupe_metadata_collection(
            [*stat_highlights, *numeric_candidates],
            blocked_texts=[headline, cta],
            limit=4 if format_name == "infographic" else 2,
        )
        if not supporting_line:
            supporting_line = next(
                (
                    candidate
                    for candidate in [*sentences, *proof_points]
                    if candidate.casefold() not in {headline.casefold(), cta.casefold()}
                ),
                "",
            )
        body_display = next(
            (
                candidate
                for candidate in [*sentences, *proof_points]
                if candidate
                and candidate.casefold()
                not in {headline.casefold(), supporting_line.casefold(), cta.casefold()}
            ),
            body,
        )

        metadata["supporting_line"] = supporting_line
        metadata["proof_points"] = proof_points
        metadata["stat_highlights"] = stat_highlights
        metadata["render_sections"] = {
            "creative_type": format_name,
            "supporting_line": supporting_line,
            "body_display": body_display,
            "proof_points": proof_points,
            "stat_highlights": stat_highlights,
            "cta": cta,
        }
        metadata["content_structure"] = {
            "creative_type": format_name,
            "repetition_flags": {
                "support_matches_headline": bool(supporting_line)
                and supporting_line.casefold() == headline.casefold(),
                "body_matches_support": bool(body_display)
                and body_display.casefold() == supporting_line.casefold(),
                "proof_repeats_support": any(
                    point.casefold() == supporting_line.casefold()
                    for point in proof_points
                    if point and supporting_line
                ),
            },
        }

        structured = StructuredTextPayload(
            headline=headline,
            body=body,
            cta=cta,
            hashtags=hashtags,
            metadata=metadata,
        )
        if format_name == "carousel":
            carousel_slides = AIOrchestratorService._build_carousel_slide_specs(structured)
            if carousel_slides:
                if carousel_slides[0].get("role") == "cover":
                    carousel_slides[0]["role"] = "hook"
                if cta:
                    carousel_slides[-1]["role"] = "closing"
                    carousel_slides[-1]["headline"] = cta
            structured.metadata["carousel_slide_specs"] = carousel_slides
        return structured

    @staticmethod
    def normalize_text_payload(
        text_dict: dict,
        fallback: dict,
        brand_name: str | None = None,
        compiled_context: dict[str, Any] | None = None,
        prompt: str | None = None,
    ) -> dict:
        # Builds text from text dict, fallback payload, and brand name for AIOrchestratorService.
        # It calls _coerce_text_value while assembling the payload or prompt text.
        normalized = dict(fallback)
        normalized.update(text_dict or {})

        hashtags = normalized.get("hashtags")
        if isinstance(hashtags, str):
            tokens = re.split(r"[\s,]+", hashtags.strip())
            normalized["hashtags"] = [token for token in tokens if token]
        elif isinstance(hashtags, tuple):
            normalized["hashtags"] = list(hashtags)
        elif not isinstance(hashtags, list):
            normalized["hashtags"] = list(fallback.get("hashtags", []))

        normalized["hashtags"] = [
            str(tag).strip()
            for tag in normalized["hashtags"]
            if str(tag).strip()
        ]

        for field in ("headline", "body", "cta"):
            normalized[field] = AIOrchestratorService._coerce_text_value(
                normalized.get(field),
                fallback.get(field, ""),
            )

        metadata = normalized.get("metadata")
        normalized["metadata"] = AIOrchestratorService.normalize_metadata_payload(
            metadata,
            fallback.get("metadata", {}),
            body=normalized.get("body"),
            brand_name=brand_name,
            compiled_context=compiled_context,
            prompt=prompt,
        )
        research_editorial_brief = (
            compiled_context.get("research_editorial_brief")
            if isinstance(compiled_context, dict) and isinstance(compiled_context.get("research_editorial_brief"), dict)
            else {}
        )
        normalized = ResearchEditorialPlanningService.enforce_source_backing(
            normalized,
            prompt_text=str(prompt or ""),
            brief=research_editorial_brief,
        )

        # Returns the common text payload shape used by static, carousel, and infographic flows before format-specific rendering.
        # Later branches read metadata to decide slide specs, infographic sections, static panels, proof points, and visual focus.
        return normalized

    @staticmethod
    def _int_or_none(value: Any) -> int | None:
        # Centralizes int none from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if isinstance(value, bool):
            return None
        if isinstance(value, int):
            return value if value > 0 else None
        text = str(value or "").strip()
        return int(text) if text.isdigit() and int(text) > 0 else None

    @classmethod
    def _rough_collection_length(cls, value: Any) -> int:
        # Centralizes rough collection length from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if value is None:
            return 0
        if isinstance(value, str):
            chunks = cls._normalize_metadata_list(value, limit=64)
            return len(chunks)
        if isinstance(value, dict):
            return len(
                [
                    item
                    for item in value.values()
                    if item is not None and item != "" and item != [] and item != {}
                ]
            )
        if isinstance(value, (list, tuple, set)):
            return len(
                [
                    item
                    for item in value
                    if item is not None and item != "" and item != [] and item != {}
                ]
            )
        return 1

    @classmethod
    def _dynamic_metadata_limits(
        cls,
        *,
        metadata: dict[str, Any],
        body: str | None = None,
        compiled_context: dict[str, Any] | None = None,
        prompt: str | None = None,
    ) -> dict[str, int]:
        # Centralizes dynamic metadata limits from metadata, body, and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        content_format_brief = (
            compiled_context.get("content_format_brief")
            if isinstance(compiled_context, dict) and isinstance(compiled_context.get("content_format_brief"), dict)
            else {}
        )
        format_name = str(content_format_brief.get("format") or "").strip().casefold()
        preferred_slide_count = cls._int_or_none(
            metadata.get("preferred_slide_count")
            or metadata.get("slide_count")
            or content_format_brief.get("preferred_slide_count")
        )
        sentence_count = len(cls._sentences(body))
        proof_count = cls._rough_collection_length(metadata.get("proof_points"))
        stat_count = cls._rough_collection_length(metadata.get("stat_highlights"))
        objection_count = cls._rough_collection_length(metadata.get("objection_handling"))
        trust_count = cls._rough_collection_length(metadata.get("trust_builders"))
        claim_count = cls._rough_collection_length(metadata.get("claim_evidence_pairs"))
        data_surface_contract = cls._data_list_surface_contract(
            prompt=prompt,
            format_name=format_name,
            metadata=metadata,
        )
        data_surface_count = (
            int(data_surface_contract.get("module_count") or 0)
            if isinstance(data_surface_contract, dict)
            else 0
        )
        if format_name == "carousel":
            story_span = max(
                preferred_slide_count or 0,
                sentence_count,
                proof_count,
                stat_count,
                trust_count,
            )
            return {
                "proof_points": max(proof_count, sentence_count, (preferred_slide_count or 0) * 2, max(story_span, 4)),
                "stat_highlights": max(stat_count, preferred_slide_count or 0, min(sentence_count, max(story_span, 3))),
                "objection_handling": max(objection_count, preferred_slide_count or 0, 3),
                "trust_builders": max(trust_count, preferred_slide_count or 0, 4),
                "claim_evidence_pairs": max(claim_count, preferred_slide_count or 0, 3),
            }
        if format_name == "infographic":
            info_span = max(sentence_count, proof_count, stat_count, preferred_slide_count or 0)
            return {
                "proof_points": max(proof_count, info_span, data_surface_count, 4),
                "stat_highlights": max(stat_count, preferred_slide_count or 0, data_surface_count, 4),
                "objection_handling": max(objection_count, 3),
                "trust_builders": max(trust_count, 4),
                "claim_evidence_pairs": max(claim_count, data_surface_count, 3),
            }
        return {
            "proof_points": max(proof_count, min(max(sentence_count, 1), 6), data_surface_count, 3),
            "stat_highlights": max(stat_count, min(sentence_count, 4), data_surface_count, 2),
            "objection_handling": max(objection_count, 3),
            "trust_builders": max(trust_count, 3),
            "claim_evidence_pairs": max(claim_count, data_surface_count, 3),
        }

    @staticmethod
    def normalize_metadata_payload(
        metadata: Any,
        fallback: dict[str, Any],
        body: str | None = None,
        brand_name: str | None = None,
        compiled_context: dict[str, Any] | None = None,
        prompt: str | None = None,
    ) -> dict[str, Any]:
        # Builds metadata from metadata, fallback payload, and body for AIOrchestratorService.
        # It calls _dynamic_metadata_limits and _normalize_metadata_list while assembling the payload or prompt text.
        base = dict(fallback or {})
        if isinstance(metadata, dict):
            merged_metadata = dict(metadata)
            if isinstance(base.get("carousel_slide_specs"), list) and isinstance(metadata.get("carousel_slide_specs"), list):
                merged_metadata["carousel_slide_specs"] = AIOrchestratorService._merge_partial_sequence_specs(
                    metadata.get("carousel_slide_specs"),
                    base.get("carousel_slide_specs"),
                    number_keys=("slide_number", "slide_index"),
                )
            if isinstance(base.get("infographic_section_specs"), list) and isinstance(metadata.get("infographic_section_specs"), list):
                merged_metadata["infographic_section_specs"] = AIOrchestratorService._merge_partial_sequence_specs(
                    metadata.get("infographic_section_specs"),
                    base.get("infographic_section_specs"),
                    number_keys=("section_number", "index"),
                )
            base.update(merged_metadata)

        limits = AIOrchestratorService._dynamic_metadata_limits(
            metadata=base,
            body=body,
            compiled_context=compiled_context,
            prompt=prompt,
        )
        base["proof_points"] = AIOrchestratorService._normalize_metadata_list(
            base.get("proof_points"),
            limit=limits["proof_points"],
        )
        base["stat_highlights"] = AIOrchestratorService._normalize_metadata_list(
            base.get("stat_highlights"),
            limit=limits["stat_highlights"],
        )
        base["hook_type"] = AIOrchestratorService._normalize_metadata_text(base.get("hook_type"), limit=40)
        base["objection_handling"] = AIOrchestratorService._normalize_metadata_list(
            base.get("objection_handling"),
            limit=limits["objection_handling"],
        )
        research_claim_pairs = AIOrchestratorService._claim_evidence_pairs_from_research_brief(
            (compiled_context or {}).get("research_editorial_brief"),
            limit=limits["claim_evidence_pairs"],
        )
        base["claim_evidence_pairs"] = AIOrchestratorService._merge_claim_evidence_pairs(
            base.get("claim_evidence_pairs"),
            research_claim_pairs,
            limit=limits["claim_evidence_pairs"],
        )
        base["trust_builders"] = AIOrchestratorService._normalize_metadata_list(
            base.get("trust_builders"),
            limit=limits["trust_builders"],
        )
        claim_evidence_lines = AIOrchestratorService._claim_evidence_pair_lines(
            base["claim_evidence_pairs"],
            limit=limits["claim_evidence_pairs"],
        )

        sentences = AIOrchestratorService._sentences(body)
        if not base["proof_points"] and claim_evidence_lines:
            base["proof_points"] = claim_evidence_lines[: limits["proof_points"]]
        if not base["proof_points"] and base["trust_builders"]:
            base["proof_points"] = base["trust_builders"][: limits["proof_points"]]
        if not base["proof_points"] and sentences:
            base["proof_points"] = sentences[: limits["proof_points"]]

        supporting_line = (
            base.get("supporting_line")
            or base.get("subheadline")
            or (claim_evidence_lines[0] if claim_evidence_lines else "")
            or (base["trust_builders"][0] if base["trust_builders"] else "")
            or (sentences[0] if sentences else (base["proof_points"][0] if base["proof_points"] else ""))
        )
        base["supporting_line"] = AIOrchestratorService._normalize_metadata_text(supporting_line, limit=180)
        base["subheadline"] = AIOrchestratorService._normalize_metadata_text(base.get("subheadline"), limit=110)
        base["section_label"] = AIOrchestratorService._normalize_metadata_text(
            base.get("section_label") or base.get("campaign_label") or brand_name or "",
            limit=26,
        )
        base["logo_position"] = AIOrchestratorService._normalize_metadata_text(base.get("logo_position"), limit=40)
        base["logo_background_tone"] = AIOrchestratorService._normalize_logo_background_tone(
            base.get("logo_background_tone")
        )
        base["visual_direction"] = AIOrchestratorService._normalize_metadata_text(
            base.get("visual_direction"),
            limit=180,
        )
        base["design_style"] = AIOrchestratorService._normalize_metadata_text(base.get("design_style"), limit=80)
        base["image_prompt"] = AIOrchestratorService._normalize_metadata_text(base.get("image_prompt"), limit=220)

        colors = base.get("brand_colors") or base.get("colors")
        if isinstance(colors, dict):
            base["brand_colors"] = {str(key): str(value) for key, value in colors.items()}

        base, _ = AIOrchestratorService._sanitize_visual_metadata_fields(
            base,
            compiled_context=compiled_context,
        )
        format_name = AIOrchestratorService._normalized_output_format_name(compiled_context)
        if format_name == "static":
            base = AIOrchestratorService._repair_static_metadata_semantics(
                base,
                body=body or "",
                prompt=prompt,
            )
        elif format_name == "infographic":
            base = AIOrchestratorService._repair_infographic_metadata_semantics(
                base,
                body=body or "",
                prompt=prompt,
            )

        structured_anchor_sources = AIOrchestratorService._structured_visual_data_anchor_sources(
            base,
            prompt=prompt,
        )
        base["structured_visual_metadata"] = AIOrchestratorService._normalize_structured_visual_metadata(
            metadata=base,
            visual_intent=" ".join(
                [
                    AIOrchestratorService._coerce_text_value(prompt),
                    AIOrchestratorService._coerce_text_value(base.get("visual_focus")),
                    AIOrchestratorService._coerce_text_value(base.get("visual_direction")),
                    AIOrchestratorService._coerce_text_value(base.get("design_style")),
                    AIOrchestratorService._coerce_text_value(base.get("image_prompt")),
                ]
            ),
            anchor_entries=AIOrchestratorService._structured_visual_anchor_entries(
                structured_anchor_sources,
                request=None,
                limit=8,
            ),
            format_name=format_name,
            sample_page_blueprint=base.get("sample_page_blueprint"),
            visual_identity=(compiled_context or {}).get("brand_visual_brief") if isinstance(compiled_context, dict) else None,
            compiled_context=compiled_context,
        )

        return base

    @staticmethod
    def _normalized_output_format_name(compiled_context: dict[str, Any] | None) -> str:
        # Normalizes output name from compiled context for AIOrchestratorService.
        # Later prompt and metadata code can compare the cleaned value directly.
        content_format_brief = (compiled_context or {}).get("content_format_brief")
        if isinstance(content_format_brief, dict):
            format_name = str(content_format_brief.get("format") or "").strip().casefold()
            if format_name:
                return format_name
        format_family_plan = (compiled_context or {}).get("format_family_plan")
        if isinstance(format_family_plan, dict):
            family = str(format_family_plan.get("family") or "").strip().casefold()
            if family in {"static", "infographic", "carousel"}:
                return family
        return ""

    @classmethod
    def _dedupe_metadata_collection(
        cls,
        items: list[str],
        *,
        blocked_texts: list[str],
        limit: int,
    ) -> list[str]:
        # Deduplicates dedupe metadata collection from items, blocked texts, and limit for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_text before returning the cleaned value.
        blocked = {
            cls._normalize_metadata_text(text, limit=180).casefold()
            for text in blocked_texts
            if cls._normalize_metadata_text(text, limit=180)
        }
        cleaned: list[str] = []
        for item in items:
            normalized = cls._normalize_metadata_text(item, limit=180)
            if not normalized:
                continue
            lowered = normalized.casefold()
            if lowered in blocked:
                continue
            if any(lowered == existing.casefold() for existing in cleaned):
                continue
            cleaned.append(normalized)
            if len(cleaned) >= limit:
                break
        return cleaned

    @classmethod
    def _normalize_static_panel_spec(
        cls,
        value: Any,
        *,
        headline: str,
        supporting_line: str,
        proof_points: list[str],
        stat_highlights: list[str],
        claim_evidence_pairs: list[dict[str, str]],
        cta: str,
        body: str,
        surface_contract: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # Normalizes static panel spec from input value, headline, and supporting line for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_text and _int_or_none before returning the cleaned value.
        raw = value if isinstance(value, dict) else {}
        contract = surface_contract if isinstance(surface_contract, dict) else {}
        module_limit = cls._int_or_none(contract.get("module_count")) or 2
        module_limit = max(1, min(module_limit, 12))
        fallback_panel_goal = (
            "ranked_list"
            if contract.get("intent") == "top_n_list"
            else "comparison_or_list" if contract else "single_dominant_message"
        )
        dominant_message = cls._normalize_metadata_text(
            raw.get("dominant_message") or supporting_line or headline or body,
            limit=180,
        )
        supporting_lines = cls._normalize_metadata_list(
            raw.get("supporting_lines"),
            limit=2,
        ) or ([supporting_line] if supporting_line else [])
        return {
            "panel_goal": cls._normalize_metadata_text(raw.get("panel_goal"), limit=48) or fallback_panel_goal,
            "dominant_message": dominant_message,
            "supporting_lines": supporting_lines[:2],
            "proof_points": proof_points[:module_limit],
            "stat_highlights": stat_highlights[:module_limit],
            "claim_evidence_pairs": cls._normalize_claim_evidence_pairs(raw.get("claim_evidence_pairs"), limit=module_limit)
            or claim_evidence_pairs[:module_limit],
            "visual_focus": cls._normalize_metadata_text(raw.get("visual_focus"), limit=160)
            or cls._normalize_metadata_text(raw.get("visual_direction"), limit=160),
            "cta_mode": cls._normalize_metadata_text(raw.get("cta_mode"), limit=48)
            or ("integrated_cta" if cta else "no_cta"),
        }

    @classmethod
    def _normalize_infographic_section_specs(
        cls,
        value: Any,
        *,
        headline: str,
        supporting_line: str,
        proof_points: list[str],
        stat_highlights: list[str],
        claim_evidence_pairs: list[dict[str, str]],
        body: str,
        section_limit: int = 4,
        data_list_surface: bool = False,
    ) -> list[dict[str, Any]]:
        # Normalizes infographic section specs from input value, headline, and supporting line for AIOrchestratorService.
        # It delegates shared cleanup to _sentences and _claim_evidence_pair_lines before returning the cleaned value.
        section_limit = max(1, min(int(section_limit or 4), 10))
        raw_sections = [dict(item) for item in value if isinstance(item, dict)] if isinstance(value, list) else []
        normalized_sections: list[dict[str, Any]] = []
        if raw_sections:
            for index, item in enumerate(raw_sections, start=1):
                section_headline = cls._normalize_metadata_text(
                    item.get("headline") or item.get("section_label") or item.get("title"),
                    limit=120,
                )
                section_body = cls._normalize_metadata_text(
                    item.get("body") or item.get("description") or item.get("summary"),
                    limit=260,
                )
                section_points = cls._normalize_metadata_list(item.get("body_points"), limit=3)
                section_proof = cls._normalize_metadata_list(item.get("proof_points"), limit=3)
                section_stats = cls._normalize_metadata_list(item.get("stat_highlights"), limit=3)
                section_claims = cls._normalize_claim_evidence_pairs(item.get("claim_evidence_pairs"), limit=2)
                normalized_sections.append(
                    {
                        "section_number": int(item.get("section_number") or item.get("index") or index),
                        "section_role": cls._normalize_metadata_text(item.get("section_role") or item.get("role"), limit=48)
                        or ("overview" if index == 1 else "takeaway" if index == len(raw_sections) else "detail"),
                        "section_label": cls._normalize_metadata_text(item.get("section_label") or item.get("label"), limit=40),
                        "headline": section_headline,
                        "body": section_body,
                        "body_points": section_points,
                        "proof_points": section_proof,
                        "stat_highlights": section_stats,
                        "claim_evidence_pairs": section_claims,
                        "visual_focus": cls._normalize_metadata_text(item.get("visual_focus"), limit=160),
                    }
                )
        if normalized_sections:
            return normalized_sections[:section_limit]

        sentences = cls._sentences(body)
        claim_lines = cls._claim_evidence_pair_lines(claim_evidence_pairs, limit=section_limit)
        if data_list_surface and proof_points:
            sections: list[dict[str, Any]] = []
            for index, proof_point in enumerate(proof_points[:section_limit], start=1):
                stat_slice = stat_highlights[index - 1 : index]
                claim_slice = claim_evidence_pairs[index - 1 : index]
                sections.append(
                    {
                        "section_number": index,
                        "section_role": "comparison_item",
                        "section_label": "",
                        "headline": proof_point,
                        "body": stat_slice[0] if stat_slice else "",
                        "body_points": [],
                        "proof_points": [proof_point],
                        "stat_highlights": stat_slice,
                        "claim_evidence_pairs": claim_slice,
                        "visual_focus": "",
                    }
                )
            if sections:
                return sections
        sections: list[dict[str, Any]] = [
            {
                "section_number": 1,
                "section_role": "overview",
                "section_label": "Overview",
                "headline": supporting_line or headline,
                "body": sentences[0] if sentences else supporting_line or headline,
                "body_points": [],
                "proof_points": proof_points[:2],
                "stat_highlights": stat_highlights[:1],
                "claim_evidence_pairs": [],
                "visual_focus": "",
            }
        ]
        if stat_highlights or claim_lines:
            sections.append(
                {
                    "section_number": len(sections) + 1,
                    "section_role": "evidence",
                    "section_label": "Key numbers" if stat_highlights else "Evidence",
                    "headline": "Key numbers" if stat_highlights else "Evidence",
                    "body": claim_lines[0] if claim_lines else (sentences[1] if len(sentences) > 1 else ""),
                    "body_points": [],
                    "proof_points": proof_points[2:4],
                    "stat_highlights": stat_highlights[:3],
                    "claim_evidence_pairs": claim_evidence_pairs[:2],
                    "visual_focus": "",
                }
            )
        sections.append(
            {
                "section_number": len(sections) + 1,
                "section_role": "takeaway",
                "section_label": "What to know",
                "headline": "What to know",
                "body": sentences[-1] if len(sentences) > 1 else supporting_line or headline,
                "body_points": proof_points[:2],
                "proof_points": proof_points[:3],
                "stat_highlights": [],
                "claim_evidence_pairs": [],
                "visual_focus": "",
            }
        )
        return sections[:section_limit]

    @classmethod
    def _repair_static_metadata_semantics(
        cls,
        metadata: dict[str, Any],
        *,
        body: str,
        prompt: str | None = None,
    ) -> dict[str, Any]:
        # Repairs static metadata semantics from metadata, body, and prompt text for AIOrchestratorService.
        # It delegates shared cleanup to _data_list_surface_contract and _sentences before returning the cleaned value.
        repaired = dict(metadata or {})
        surface_contract = cls._data_list_surface_contract(
            prompt=prompt or body,
            format_name="static",
            metadata=repaired,
        )
        row_limit = (
            max(2, min(cls._int_or_none(surface_contract.get("module_count")) or 2, 12))
            if surface_contract
            else 2
        )
        sentences = cls._sentences(body)
        headline = cls._normalize_metadata_text(repaired.get("headline"), limit=180)
        cta = cls._normalize_metadata_text(repaired.get("cta"), limit=90)
        original_supporting_line = cls._normalize_metadata_text(repaired.get("supporting_line"), limit=180)
        supporting_line = original_supporting_line
        raw_static_panel_spec = repaired.get("static_panel_spec") if isinstance(repaired.get("static_panel_spec"), dict) else {}
        raw_proof_points = cls._normalize_metadata_list(
            repaired.get("proof_points") or raw_static_panel_spec.get("proof_points"),
            limit=max(3, row_limit),
        )
        raw_stat_highlights = cls._normalize_metadata_list(
            repaired.get("stat_highlights") or raw_static_panel_spec.get("stat_highlights"),
            limit=max(3, row_limit),
        )
        if (
            not supporting_line
            or (cta and supporting_line.casefold() == cta.casefold())
            or any(supporting_line.casefold() == item.casefold() for item in [*raw_proof_points, *raw_stat_highlights] if item)
        ):
            supporting_line = next(
                (
                    cls._normalize_metadata_text(candidate, limit=180)
                    for candidate in [*sentences[1:3], *repaired.get("proof_points", [])]
                    if cls._normalize_metadata_text(candidate, limit=180)
                    and cls._normalize_metadata_text(candidate, limit=180).casefold() not in {
                        headline.casefold(),
                        cta.casefold() if cta else "",
                    }
                ),
                supporting_line or "",
            )
        repaired["supporting_line"] = supporting_line
        repaired["proof_points"] = cls._dedupe_metadata_collection(
            raw_proof_points,
            blocked_texts=[headline, original_supporting_line, supporting_line, cta],
            limit=row_limit,
        )
        repaired["stat_highlights"] = cls._dedupe_metadata_collection(
            raw_stat_highlights,
            blocked_texts=[headline, original_supporting_line, supporting_line, cta, *repaired["proof_points"]],
            limit=row_limit,
        )
        normalized_claim_pairs = cls._normalize_claim_evidence_pairs(
            repaired.get("claim_evidence_pairs") or raw_static_panel_spec.get("claim_evidence_pairs"),
            limit=max(3, row_limit),
        )
        if normalized_claim_pairs:
            allocations = cls._allocate_claim_evidence_pairs_to_slots(
                [
                    {
                        "role": "static",
                        "headline": headline,
                        "supporting_line": supporting_line,
                        "body": body,
                        "proof_points": repaired["proof_points"],
                        "stat_highlights": repaired["stat_highlights"],
                    }
                ],
                claim_evidence_pairs=normalized_claim_pairs,
                format_family="static",
            )
            assigned_pairs = allocations[0] if allocations else []
            if assigned_pairs:
                claim_lines = cls._claim_evidence_pair_lines(assigned_pairs, limit=row_limit)
                repaired["proof_points"] = cls._dedupe_metadata_collection(
                    [*claim_lines, *repaired["proof_points"]],
                    blocked_texts=[headline, original_supporting_line, supporting_line, cta],
                    limit=row_limit,
                )
                repaired["claim_evidence_pairs"] = assigned_pairs
        repaired["static_panel_spec"] = cls._normalize_static_panel_spec(
            repaired.get("static_panel_spec"),
            headline=headline,
            supporting_line=supporting_line,
            proof_points=repaired["proof_points"],
            stat_highlights=repaired["stat_highlights"],
            claim_evidence_pairs=cls._normalize_claim_evidence_pairs(repaired.get("claim_evidence_pairs"), limit=row_limit),
            cta=cta,
            body=body,
            surface_contract=surface_contract,
        )
        if surface_contract:
            available_count = max(
                surface_contract.get("available_count") or 0,
                len(repaired["proof_points"]),
                len(repaired["stat_highlights"]),
                len(cls._normalize_claim_evidence_pairs(repaired.get("claim_evidence_pairs"), limit=row_limit)),
            )
            repaired["data_list_surface_contract"] = {
                **surface_contract,
                "available_count": available_count,
            }
        return repaired

    @classmethod
    def _repair_infographic_metadata_semantics(
        cls,
        metadata: dict[str, Any],
        *,
        body: str,
        prompt: str | None = None,
    ) -> dict[str, Any]:
        # Repairs infographic metadata semantics from metadata, body, and prompt text for AIOrchestratorService.
        # It delegates shared cleanup to _data_list_surface_contract and _sentences before returning the cleaned value.
        repaired = dict(metadata or {})
        surface_contract = cls._data_list_surface_contract(
            prompt=prompt or body,
            format_name="infographic",
            metadata=repaired,
        )
        row_limit = (
            max(4, min(cls._int_or_none(surface_contract.get("module_count")) or 4, 10))
            if surface_contract
            else 4
        )
        sentences = cls._sentences(body)
        headline = cls._normalize_metadata_text(repaired.get("headline"), limit=180)
        cta = cls._normalize_metadata_text(repaired.get("cta"), limit=90)
        supporting_line = cls._normalize_metadata_text(repaired.get("supporting_line"), limit=180)
        claim_lines = cls._claim_evidence_pair_lines(
            cls._normalize_claim_evidence_pairs(repaired.get("claim_evidence_pairs"), limit=row_limit),
            limit=row_limit,
        )
        repaired["proof_points"] = cls._dedupe_metadata_collection(
            cls._normalize_metadata_list(repaired.get("proof_points"), limit=max(5, row_limit)) or claim_lines or sentences[1 : row_limit + 1],
            blocked_texts=[headline, supporting_line, cta],
            limit=row_limit,
        )
        repaired["stat_highlights"] = cls._dedupe_metadata_collection(
            cls._normalize_metadata_list(repaired.get("stat_highlights"), limit=row_limit),
            blocked_texts=[headline, supporting_line, cta, *repaired["proof_points"]],
            limit=row_limit,
        )
        normalized_claim_pairs = cls._normalize_claim_evidence_pairs(repaired.get("claim_evidence_pairs"), limit=row_limit)
        repaired["claim_evidence_pairs"] = normalized_claim_pairs
        if not supporting_line:
            supporting_line = next(
                (
                    cls._normalize_metadata_text(candidate, limit=180)
                    for candidate in [*sentences[:2], *repaired["proof_points"]]
                    if cls._normalize_metadata_text(candidate, limit=180)
                    and cls._normalize_metadata_text(candidate, limit=180).casefold() != headline.casefold()
                ),
                "",
            )
        repaired["supporting_line"] = supporting_line
        section_label = cls._normalize_metadata_text(repaired.get("section_label"), limit=26)
        if not section_label or section_label.casefold() in {"insights", "highlights"}:
            if repaired["stat_highlights"]:
                section_label = "Key numbers"
            elif repaired["proof_points"]:
                section_label = "What to know"
        repaired["section_label"] = section_label
        repaired["infographic_section_specs"] = cls._normalize_infographic_section_specs(
            repaired.get("infographic_section_specs"),
            headline=headline,
            supporting_line=supporting_line,
            proof_points=repaired["proof_points"],
            stat_highlights=repaired["stat_highlights"],
            claim_evidence_pairs=normalized_claim_pairs,
            body=body,
            section_limit=row_limit,
            data_list_surface=bool(surface_contract),
        )
        section_allocations = cls._allocate_claim_evidence_pairs_to_slots(
            repaired["infographic_section_specs"],
            claim_evidence_pairs=normalized_claim_pairs,
            format_family="infographic",
        )
        for index, section in enumerate(repaired["infographic_section_specs"]):
            assigned_pairs = section_allocations[index] if index < len(section_allocations) else []
            section["claim_evidence_pairs"] = assigned_pairs
            claim_lines = cls._claim_evidence_pair_lines(assigned_pairs, limit=2)
            if claim_lines:
                section["proof_points"] = cls._dedupe_metadata_collection(
                    [*claim_lines, *cls._normalize_metadata_list(section.get("proof_points"), limit=3)],
                    blocked_texts=[
                        section.get("headline"),
                        section.get("section_label"),
                        section.get("body"),
                    ],
                    limit=3,
                )
                section["body_points"] = cls._dedupe_metadata_collection(
                    [*cls._normalize_metadata_list(section.get("body_points"), limit=3), *claim_lines],
                    blocked_texts=[section.get("headline"), section.get("section_label")],
                    limit=3,
                )
        if surface_contract:
            repaired["data_list_surface_contract"] = {
                **surface_contract,
                "available_count": max(
                    surface_contract.get("available_count") or 0,
                    len(repaired["proof_points"]),
                    len(repaired["stat_highlights"]),
                    len(normalized_claim_pairs),
                    len(repaired["infographic_section_specs"]),
                ),
            }
        return repaired

    @classmethod
    def _visual_metadata_keywords(cls, value: Any, *, limit: int = 12) -> set[str]:
        # Centralizes visual metadata keywords from input value and limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        keywords: list[str] = []
        for word in re.findall(r"[A-Za-z][A-Za-z0-9'-]{2,}", cls._coerce_text_value(value).casefold()):
            normalized = word.rstrip("'")
            if not normalized or normalized in cls.VISUAL_METADATA_COMPATIBILITY_STOPWORDS:
                continue
            if normalized.isdigit():
                continue
            keywords.append(normalized)
            if len(keywords) >= limit:
                break
        return set(keywords)

    @classmethod
    def _visual_metadata_evidence_keywords(
        cls,
        *,
        brief: dict[str, Any],
        compiled_context: dict[str, Any] | None = None,
    ) -> set[str]:
        # Centralizes visual metadata evidence keywords from brief and compiled context for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        evidence_keywords: set[str] = set()
        evidence_texts: list[Any] = [brief.get("summary"), (compiled_context or {}).get("brand_visual_brief")]
        for item in (brief.get("items") or [])[:5]:
            if isinstance(item, dict):
                evidence_texts.append(item.get("content"))
        for entry in evidence_texts:
            evidence_keywords.update(cls._visual_metadata_keywords(entry, limit=16))
        return evidence_keywords

    @classmethod
    def _sanitize_visual_metadata_fields(
        cls,
        metadata: dict[str, Any],
        *,
        compiled_context: dict[str, Any] | None = None,
    ) -> tuple[dict[str, Any], list[str]]:
        # Sanitizes visual metadata fields from metadata and compiled context for AIOrchestratorService.
        # It delegates shared cleanup to _visual_metadata_evidence_keywords and _normalize_metadata_text before returning the cleaned value.
        cleaned = dict(metadata or {})
        if not cleaned or not compiled_context:
            return cleaned, []

        brief = ContextCompilerService.coerce_visual_knowledge_brief(
            compiled_context.get("visual_knowledge_brief"),
        )
        if cls._normalize_metadata_text(brief.get("grounding_mode"), limit=32) != "brand_knowledge":
            return cleaned, []

        items = brief.get("items") if isinstance(brief.get("items"), list) else []
        if not items:
            return cleaned, []

        evidence_keywords = cls._visual_metadata_evidence_keywords(
            brief=brief,
            compiled_context=compiled_context,
        )
        if not evidence_keywords:
            return cleaned, []

        grounding_strength = cls._normalize_metadata_text(brief.get("grounding_strength"), limit=32) or "supported"
        removed_fields: list[str] = []
        for field, limit in cls.VISUAL_METADATA_FIELD_LIMITS.items():
            text = cls._normalize_metadata_text(cleaned.get(field), limit=limit)
            cleaned[field] = text
            if not text:
                continue
            field_keywords = cls._visual_metadata_keywords(text, limit=10)
            if not field_keywords:
                cleaned[field] = ""
                removed_fields.append(field)
                continue
            if field_keywords & evidence_keywords:
                continue
            if grounding_strength in {"strong", "supported", "fallback_only"}:
                cleaned[field] = ""
                removed_fields.append(field)
        return cleaned, removed_fields

    @staticmethod
    def _normalize_metadata_text(value: Any, limit: int) -> str:
        # Normalizes metadata text from input value and limit for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_text_value and _sentences before returning the cleaned value.
        text = AIOrchestratorService._coerce_text_value(value)
        text = " ".join(text.strip().split())
        if not text:
            return ""
        if len(text) <= limit:
            return text
        sentences = AIOrchestratorService._sentences(text)
        if len(sentences) > 1:
            collected: list[str] = []
            for sentence in sentences:
                joined = " ".join([*collected, sentence]).strip()
                if len(joined) > limit:
                    break
                collected.append(sentence)
            if collected:
                return " ".join(collected).strip()
        truncation_limit = max(limit - 3, 1)
        candidate = text[:truncation_limit].rstrip(" ,.;:")
        if " " in candidate:
            candidate = candidate.rsplit(" ", 1)[0].rstrip(" ,.;:")
        candidate = candidate or text[:truncation_limit].rstrip(" ,.;:")
        return f"{candidate}..." if candidate else text[:limit].rstrip(" ,.;:")

    @staticmethod
    def _normalize_metadata_list(value: Any, limit: int) -> list[str]:
        # Normalizes metadata from input value and limit for AIOrchestratorService.
        # It delegates shared cleanup to _sanitize_text_for_canvas and _normalize_metadata_text before returning the cleaned value.
        if value is None:
            return []

        raw_items: list[str] = []
        if isinstance(value, str):
            chunks = re.split(r"(?:\r?\n|[;|])+|\s*•\s*|\s*[-*]\s+|(?:✔️|✓|✅|➜|➡️|âœ”ï¸|â€¢)\s*", value)
            if len(chunks) == 1:
                chunks = re.split(r",\s+|(?<=[.!?])\s+", value)
            raw_items = chunks
        elif isinstance(value, (list, tuple, set)):
            for item in value:
                if isinstance(item, dict):
                    raw_items.append(str(item.get("label") or item.get("text") or item.get("value") or "").strip())
                else:
                    raw_items.append(str(item).strip())
        else:
            raw_items = [str(value).strip()]

        cleaned: list[str] = []
        seen: set[str] = set()
        for item in raw_items:
            # Treat only real numbered bullets ("1. item", "1) item") as
            # list markers. Decimal facts like "70.03%" must stay intact.
            text = re.sub(r"^\s*(?:[#*\-]+|\d+[.)]\s+)", "", item).strip()
            if not text:
                continue
            text = AIOrchestratorService._sanitize_text_for_canvas(text)
            key = text.casefold()
            if key in seen:
                continue
            seen.add(key)
            cleaned.append(AIOrchestratorService._normalize_metadata_text(text, limit=90))
            if len(cleaned) >= limit:
                break
        return cleaned

    @classmethod
    def _normalize_carousel_proof_lines(cls, value: Any, *, limit: int, item_limit: int = 180) -> list[str]:
        # Normalizes carousel proof lines from input value, limit, and item limit for AIOrchestratorService.
        # It delegates shared cleanup to _sanitize_text_for_canvas and _normalize_metadata_text before returning the cleaned value.
        if value is None:
            return []
        raw_items: list[str] = []
        if isinstance(value, str):
            chunks = re.split(r"(?:\r?\n|[;|])+|\s*[-*]\s+|,\s+(?=[A-Z0-9])", value)
            raw_items = chunks if len(chunks) > 1 else [value]
        elif isinstance(value, (list, tuple, set)):
            for item in value:
                if isinstance(item, dict):
                    raw_items.append(str(item.get("label") or item.get("text") or item.get("value") or "").strip())
                else:
                    raw_items.append(str(item).strip())
        else:
            raw_items = [str(value).strip()]

        cleaned: list[str] = []
        seen: set[str] = set()
        for item in raw_items:
            text = re.sub(r"^\s*(?:[#*\-]+|\d+[.)]\s+)", "", item).strip()
            if not text:
                continue
            text = cls._sanitize_text_for_canvas(text)
            text = cls._normalize_metadata_text(text, limit=item_limit)
            key = text.casefold()
            if not text or key in seen:
                continue
            seen.add(key)
            cleaned.append(text)
            if len(cleaned) >= limit:
                break
        return cleaned

    @classmethod
    def _normalize_claim_evidence_pairs(cls, value: Any, limit: int) -> list[dict[str, str]]:
        # Normalizes claim evidence pairs from input value and limit for AIOrchestratorService.
        # It delegates shared cleanup to _coerce_list and _normalize_metadata_text before returning the cleaned value.
        items = cls._coerce_list(value)
        pairs: list[dict[str, str]] = []
        seen: set[str] = set()
        for item in items:
            claim = ""
            evidence = ""
            if isinstance(item, dict):
                claim = cls._normalize_metadata_text(
                    item.get("claim") or item.get("headline") or item.get("proposition") or item.get("text"),
                    limit=96,
                )
                evidence = cls._normalize_metadata_text(
                    item.get("evidence") or item.get("proof") or item.get("support") or item.get("reason"),
                    limit=140,
                )
            else:
                text = cls._coerce_text_value(item)
                if not text:
                    continue
                text = re.sub(r"(?i)^\s*claim\s*:\s*", "", text).strip()
                if "|" in text:
                    left, right = text.split("|", 1)
                    claim = cls._normalize_metadata_text(left, limit=96)
                    evidence = cls._normalize_metadata_text(
                        re.sub(r"(?i)^\s*evidence\s*:\s*", "", right).strip(),
                        limit=140,
                    )
                else:
                    claim = cls._normalize_metadata_text(text, limit=96)
            if claim.upper() == "MISSING":
                claim = ""
            if evidence.upper() == "MISSING":
                evidence = ""
            if not claim and not evidence:
                continue
            key = f"{claim.casefold()}|{evidence.casefold()}"
            if key in seen:
                continue
            seen.add(key)
            pairs.append({"claim": claim, "evidence": evidence})
            if len(pairs) >= limit:
                break
        return pairs

    @classmethod
    def _claim_evidence_pairs_from_research_brief(
        cls,
        brief: Any,
        *,
        limit: int,
    ) -> list[dict[str, str]]:
        # Centralizes claim evidence pairs research brief from brief and limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not isinstance(brief, dict):
            return []
        candidates: list[dict[str, str]] = []
        fact_model = brief.get("fact_model") if isinstance(brief.get("fact_model"), dict) else {}
        for fact in (fact_model.get("verified_facts") or [])[: max(limit * 2, 4)]:
            if not isinstance(fact, dict):
                continue
            claim = cls._normalize_metadata_text(
                fact.get("label") or fact.get("claim") or fact.get("headline") or fact.get("title"),
                limit=96,
            )
            evidence = cls._normalize_metadata_text(
                fact.get("value") or fact.get("detail") or fact.get("source_title") or fact.get("source"),
                limit=140,
            )
            if claim or evidence:
                candidates.append({"claim": claim, "evidence": evidence})
        for item in (brief.get("source_pack") or [])[: max(limit * 2, 4)]:
            if not isinstance(item, dict):
                continue
            if cls._normalize_metadata_text(item.get("type"), limit=32) not in {"verified_fact", "ranked_source"}:
                continue
            claim = cls._normalize_metadata_text(
                item.get("label") or item.get("headline") or item.get("title"),
                limit=96,
            )
            evidence = cls._normalize_metadata_text(
                item.get("detail") or item.get("source"),
                limit=140,
            )
            if claim or evidence:
                candidates.append({"claim": claim, "evidence": evidence})
        return cls._normalize_claim_evidence_pairs(candidates, limit=limit)

    @classmethod
    def _merge_claim_evidence_pairs(
        cls,
        primary: Any,
        secondary: Any,
        *,
        limit: int,
    ) -> list[dict[str, str]]:
        # Merges claim evidence pairs from primary, secondary, and limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        return cls._normalize_claim_evidence_pairs(
            [*cls._coerce_list(primary), *cls._coerce_list(secondary)],
            limit=limit,
        )

    @classmethod
    def _claim_evidence_pair_lines(cls, pairs: list[dict[str, str]], limit: int) -> list[str]:
        # Extracts claim evidence pair lines from pairs and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        lines: list[str] = []
        for pair in pairs[:limit]:
            if not isinstance(pair, dict):
                continue
            claim = cls._normalize_metadata_text(pair.get("claim"), limit=96)
            evidence = cls._normalize_metadata_text(pair.get("evidence"), limit=160)
            line = claim
            if claim and evidence:
                line = cls._normalize_metadata_text(f"{claim}: {evidence}", limit=180)
            elif evidence:
                line = evidence
            if line:
                lines.append(line)
        return lines

    @classmethod
    def _content_allocation_keywords(cls, *values: Any, limit: int = 18) -> set[str]:
        # Centralizes content allocation keywords from limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        keywords: list[str] = []
        for value in values:
            for word in re.findall(r"[A-Za-z][A-Za-z0-9'-]{2,}", cls._coerce_text_value(value).casefold()):
                normalized = word.rstrip("'")
                if not normalized or normalized in cls.CONTENT_ALLOCATION_STOPWORDS:
                    continue
                if normalized.isdigit():
                    continue
                keywords.append(normalized)
                if len(keywords) >= limit:
                    return set(keywords)
        return set(keywords)

    @classmethod
    def _claim_evidence_slot_capacity(cls, role: str, *, format_family: str) -> int:
        # Centralizes claim evidence slot capacity from role and format family for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized = cls._normalize_metadata_text(role, limit=48).casefold().replace(" ", "_")
        if format_family == "static":
            return 1
        if format_family == "infographic":
            if normalized == "evidence":
                return 2
            if normalized in {"overview", "analysis", "detail", "takeaway"}:
                return 1
            return 1
        if normalized in {"hook", "cover", "opening", "title", "context", "setup", "intro"}:
            return 0
        if normalized in {"close", "closing", "cta", "final"}:
            return 0
        if normalized == "takeaway":
            return 1
        return 1

    @classmethod
    def _claim_evidence_slot_priority(cls, role: str, *, format_family: str) -> int:
        # Selects claim evidence slot priority from role and format family for AIOrchestratorService.
        # It reuses _normalize_metadata_text so related checks follow the same rule.
        normalized = cls._normalize_metadata_text(role, limit=48).casefold().replace(" ", "_")
        if format_family == "static":
            return 5
        if normalized in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown", "evidence"}:
            return 5
        if normalized in {"undercovered_angle", "missed_angle", "angle", "strategic_meaning", "implications", "implication", "analysis", "what_matters"}:
            return 4
        if normalized in {"detail", "overview", "takeaway"}:
            return 3
        if normalized in {"hook", "cover", "opening", "title"}:
            return 1
        if normalized in {"close", "closing", "cta", "final"}:
            return 0
        return 2

    @classmethod
    def _claim_evidence_pair_role_hint(cls, pair: dict[str, str]) -> str:
        # Centralizes claim evidence pair role hint from pair for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = " ".join(
            part
            for part in [
                cls._normalize_metadata_text(pair.get("claim"), limit=96),
                cls._normalize_metadata_text(pair.get("evidence"), limit=160),
            ]
            if part
        ).casefold()
        if re.search(r"strategic|signal|alignment|position|shape|long[- ]term|implication", text):
            return "strategic_meaning"
        if re.search(r"mobility|services|miss|overlook|hidden|undercovered|clause|condition", text):
            return "undercovered_angle"
        if re.search(r"tariff|goods?|access|terms?|cuts?|structure|mechanic|breakdown", text):
            return "structure"
        return "detail"

    @classmethod
    def _score_claim_evidence_pair_for_slot(
        cls,
        pair: dict[str, str],
        *,
        role: str,
        slot_texts: list[Any],
    ) -> int:
        # Scores claim evidence pair slot from pair, role, and slot texts for AIOrchestratorService.
        # It reuses _normalize_metadata_text and _content_allocation_keywords so related checks follow the same rule.
        claim = cls._normalize_metadata_text(pair.get("claim"), limit=96)
        evidence = cls._normalize_metadata_text(pair.get("evidence"), limit=160)
        pair_text = " ".join(part for part in [claim, evidence] if part).strip()
        if not pair_text:
            return -1
        slot_text = " ".join(cls._coerce_text_value(item) for item in slot_texts if cls._coerce_text_value(item)).strip()
        slot_keywords = cls._content_allocation_keywords(slot_text, limit=18)
        pair_keywords = cls._content_allocation_keywords(pair_text, limit=12)
        score = len(slot_keywords & pair_keywords) * 3
        lowered_pair = pair_text.casefold()
        lowered_slot = slot_text.casefold()
        if claim and claim.casefold() in lowered_slot:
            score += 4
        if evidence and evidence.casefold() in lowered_slot:
            score += 3
        if re.search(r"\d", pair_text) and re.search(r"\d", slot_text):
            score += 2

        normalized_role = cls._normalize_metadata_text(role, limit=48).casefold().replace(" ", "_")
        role_hint = cls._claim_evidence_pair_role_hint(pair)
        if role_hint == normalized_role:
            score += 12
        elif (
            normalized_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown", "undercovered_angle", "missed_angle", "angle", "strategic_meaning", "implications", "implication", "analysis", "what_matters"}
            and role_hint in {"structure", "undercovered_angle", "strategic_meaning"}
            and role_hint != normalized_role
        ):
            score -= 4
        if normalized_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown", "evidence"} and re.search(r"\d|%|tariff|access|goods?|terms?|cuts?|structure|mechanic", lowered_pair):
            score += 3
        if normalized_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"} and re.search(r"tariff|goods?|access|terms?|cuts?|structure", lowered_pair):
            score += 4
        if normalized_role in {"undercovered_angle", "missed_angle", "angle"} and re.search(r"miss|overlook|hidden|clause|condition|but|however|unless|asymmetr", lowered_pair):
            score += 3
        if normalized_role in {"undercovered_angle", "missed_angle", "angle"} and re.search(r"mobility|services|miss|overlook|hidden|clause", lowered_pair):
            score += 4
        if normalized_role in {"strategic_meaning", "implications", "implication", "analysis", "what_matters", "takeaway"} and re.search(r"signal|strategy|strategic|long[- ]term|alignment|shape|implication|position", lowered_pair):
            score += 3
        if normalized_role in {"strategic_meaning", "implications", "implication", "analysis", "what_matters"} and re.search(r"signal|strategic|alignment|position|shape", lowered_pair):
            score += 4
        return score

    @classmethod
    def _allocate_claim_evidence_pairs_to_slots(
        cls,
        slots: list[dict[str, Any]],
        *,
        claim_evidence_pairs: list[dict[str, str]],
        format_family: str,
    ) -> list[list[dict[str, str]]]:
        # Centralizes allocate claim evidence pairs slots from slots, claim evidence pairs, and format family for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        allocations: list[list[dict[str, str]]] = [[] for _ in slots]
        if not slots or not claim_evidence_pairs:
            return allocations

        slot_specs: list[dict[str, Any]] = []
        for index, slot in enumerate(slots):
            role = cls._normalize_metadata_text(
                slot.get("story_role") or slot.get("section_role") or slot.get("role"),
                limit=48,
            )
            slot_specs.append(
                {
                    "index": index,
                    "role": role,
                    "capacity": cls._claim_evidence_slot_capacity(role, format_family=format_family),
                    "priority": cls._claim_evidence_slot_priority(role, format_family=format_family),
                    "texts": [
                        slot.get("headline"),
                        slot.get("section_label"),
                        slot.get("supporting_line"),
                        slot.get("body"),
                        slot.get("body_points"),
                        slot.get("proof_points"),
                        slot.get("stat_highlights"),
                    ],
                }
            )

        unused = list(claim_evidence_pairs)
        ordered_specs = sorted(slot_specs, key=lambda item: (-item["priority"], item["index"]))
        for spec in ordered_specs:
            if spec["capacity"] <= 0 or not unused:
                continue
            scored = sorted(
                (
                    (
                        cls._score_claim_evidence_pair_for_slot(
                            pair,
                            role=spec["role"],
                            slot_texts=spec["texts"],
                        ),
                        pair_index,
                        pair,
                    )
                    for pair_index, pair in enumerate(unused)
                ),
                key=lambda item: (item[0], -item[1]),
                reverse=True,
            )
            best_score, best_index, best_pair = scored[0]
            if best_score <= 0 and spec["priority"] < 4 and format_family != "static":
                continue
            allocations[spec["index"]].append(best_pair)
            unused.pop(best_index)

        while unused:
            expandable = [
                spec
                for spec in ordered_specs
                if spec["capacity"] > len(allocations[spec["index"]])
            ]
            if not expandable:
                break
            best_spec = expandable[0]
            best_pair_index = 0
            best_score = -1
            for pair_index, pair in enumerate(unused):
                score = cls._score_claim_evidence_pair_for_slot(
                    pair,
                    role=best_spec["role"],
                    slot_texts=best_spec["texts"],
                )
                if score > best_score:
                    best_score = score
                    best_pair_index = pair_index
            allocations[best_spec["index"]].append(unused.pop(best_pair_index))
        return allocations

    @staticmethod
    def _sentences(value: Any) -> list[str]:
        # Centralizes sentences from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        structured_items = AIOrchestratorService._structured_list_items(value)
        if structured_items:
            return structured_items
        text = AIOrchestratorService._coerce_text_value(value)
        if not text:
            return []
        return [
            sentence.strip()
            for sentence in re.split(r"(?<=[.!?])\s+", text.strip())
            if sentence.strip()
        ]

    @staticmethod
    def _coerce_text_value(value: Any, fallback: str = "") -> str:
        # Coerces text from input value and fallback payload for AIOrchestratorService.
        # It delegates shared cleanup to _sanitize_text_for_canvas before returning the cleaned value.
        if value is None:
            return fallback
        if isinstance(value, str):
            return AIOrchestratorService._sanitize_text_for_canvas(value)
        if isinstance(value, dict):
            preferred = value.get("text") or value.get("label") or value.get("value") or value.get("content")
            if preferred is not None:
                return AIOrchestratorService._coerce_text_value(preferred, fallback)
            serialized = json.dumps(value, default=str)
            return AIOrchestratorService._sanitize_text_for_canvas(serialized) if serialized != "{}" else fallback
        if isinstance(value, (list, tuple, set)):
            pieces = [
                AIOrchestratorService._coerce_text_value(item).strip()
                for item in value
            ]
            joined = " ".join(piece for piece in pieces if piece)
            return AIOrchestratorService._sanitize_text_for_canvas(joined) or fallback
        return AIOrchestratorService._sanitize_text_for_canvas(str(value))

    @staticmethod
    def _compact_infographic_section_specs(value: Any, *, section_limit: int = 4, item_limit: int = 2) -> str:
        # Compacts infographic section specs from input value, section limit, and item limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(value, list):
            return ""
        section_limit = max(1, min(int(section_limit or 4), 10))
        item_limit = max(1, min(int(item_limit or 2), 12))
        sections: list[str] = []
        for item in value[:section_limit]:
            if not isinstance(item, dict):
                continue
            role = AIOrchestratorService._normalize_metadata_text(item.get("section_role"), limit=32)
            headline = AIOrchestratorService._normalize_metadata_text(
                item.get("headline") or item.get("section_label"),
                limit=80,
            )
            proof = AIOrchestratorService._compact_named_items(item.get("proof_points"), limit=item_limit)
            stats = AIOrchestratorService._compact_named_items(item.get("stat_highlights"), limit=item_limit)
            claims = AIOrchestratorService._compact_named_items(
                AIOrchestratorService._claim_evidence_pair_lines(
                    AIOrchestratorService._normalize_claim_evidence_pairs(item.get("claim_evidence_pairs"), limit=item_limit),
                    limit=item_limit,
                ),
                limit=item_limit,
            )
            parts = [part for part in [role, headline, proof, stats, claims] if part]
            if parts:
                sections.append(" | ".join(parts))
        return "; ".join(sections[:section_limit])

    @staticmethod
    def _compact_static_panel_spec(value: Any, *, item_limit: int = 2) -> str:
        # Compacts static panel spec from input value and item limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not isinstance(value, dict):
            return ""
        item_limit = max(1, min(int(item_limit or 2), 12))
        dominant = AIOrchestratorService._normalize_metadata_text(value.get("dominant_message"), limit=120)
        supporting = AIOrchestratorService._compact_named_items(value.get("supporting_lines"), limit=2)
        proof = AIOrchestratorService._compact_named_items(value.get("proof_points"), limit=item_limit)
        stats = AIOrchestratorService._compact_named_items(value.get("stat_highlights"), limit=item_limit)
        claims = AIOrchestratorService._compact_named_items(
            AIOrchestratorService._claim_evidence_pair_lines(
                AIOrchestratorService._normalize_claim_evidence_pairs(value.get("claim_evidence_pairs"), limit=item_limit),
                limit=item_limit,
            ),
            limit=item_limit,
        )
        return " | ".join(part for part in [dominant, supporting, proof, stats, claims] if part)

    @classmethod
    def _data_list_surface_prompt_section(
        cls,
        *,
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        metadata: dict[str, Any],
        format_name: str,
        use_backend_text_overlay: bool,
        visual_plan: dict[str, Any] | None = None,
    ) -> str:
        # Builds surface prompt section from request state, copy payload, and metadata for AIOrchestratorService.
        # It calls _data_list_surface_contract and _int_or_none while assembling the payload or prompt text.
        contract = metadata.get("data_list_surface_contract") if isinstance(metadata.get("data_list_surface_contract"), dict) else None
        if not contract:
            contract = cls._data_list_surface_contract(
                prompt=getattr(request, "prompt", ""),
                format_name=format_name,
                metadata=metadata,
                text_payload=text_payload,
                visual_plan=visual_plan,
            )
        if not contract:
            return ""
        requested_count = cls._int_or_none(contract.get("requested_count")) or cls._int_or_none(contract.get("module_count")) or 1
        module_count = cls._int_or_none(contract.get("module_count")) or requested_count
        available_count = cls._int_or_none(contract.get("available_count")) or 0
        surface_mode = cls._normalize_metadata_text(contract.get("surface_mode"), limit=40).casefold()
        text_mode = (
            "Backend overlay will place the exact row/card copy afterward; the AI image must create only empty, high-contrast row/card/module shells."
            if use_backend_text_overlay
            else "Use only approved copy/data anchors for any visible row/card labels and keep each module readable."
        )
        overflow_guidance = (
            f"The request asks for {requested_count} item(s), but this format should visibly reserve {module_count} module slot(s); group or compress any overflow without inventing labels."
            if requested_count > module_count
            else f"Reserve {module_count} distinct module slot(s), matching the requested item count when layout space allows."
        )
        anchor_gap_guidance = (
            f"Only {available_count} approved row/card anchor(s) are available: still build the requested {module_count}-slot structure as clean blank overlay-safe shells, dividers, ranking markers, or card containers; do not invent missing row labels, values, countries, banks, dates, percentages, axes, or legends."
            if available_count < module_count
            else "The available anchors satisfy the requested module structure; keep every row/card slot aligned to those approved anchors."
        )
        surface_mode_guidance = (
            "Use a dense ranking-table composition: compact title/subtitle region, one large table/list panel, consistent row dividers, clear left-to-right scan order, and no oversized hero illustration, paragraph card, CTA block, fake chart, or decorative metric tiles."
            if surface_mode == "ranking_table"
            else "Use repeated cards, rows, compact comparison blocks, separators, or grouped callout shells with consistent spacing and clear scan order."
        )
        return (
            "DATA/LIST SURFACE CONTRACT: this request needs a structured list, comparison, rule, rate, fee, scorecard, or ranked surface; do not collapse it into a single generic hero illustration. "
            f"{overflow_guidance} "
            f"Approved content anchor count available to downstream overlay: {available_count}. "
            f"{anchor_gap_guidance} "
            f"{surface_mode_guidance} "
            "Do not use a human portrait, lifestyle scene, worried/emotional person, abstract mood photo, or generic finance reaction image as a fallback for this structured surface. "
            f"{text_mode}"
        )

    @staticmethod
    def _disclaimer_overlay_guidance(request: AIOrchestrationRequest) -> str:
        # Builds disclaimer overlay guidance from request state for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        request_brief = getattr(request, "research_editorial_brief", None)
        brief = request_brief if isinstance(request_brief, dict) else {}
        request_brief = getattr(request, "research_editorial_brief", None)
        brief = request_brief if isinstance(request_brief, dict) else {}
        requested = bool(brief.get("disclaimer_requested"))
        if not requested:
            prompt_lower = str(getattr(request, "prompt", "") or "").casefold()
            requested = "disclaimer" in prompt_lower
        if not requested:
            return ""
        placement = AIOrchestratorService._normalize_metadata_text(
            brief.get("disclaimer_placement"),
            limit=24,
        ) or "bottom_footer"
        style = AIOrchestratorService._normalize_metadata_text(
            brief.get("disclaimer_style"),
            limit=24,
        ) or "subtle"
        if placement == "bottom_footer":
            return (
                "Reserve the bottom footer-safe strip for a small legal disclaimer overlay. "
                "Leave that bottom strip completely blank: no text, icons, objects, charts, cards, shadows, patterns, or important visual details. "
                f"Keep it clean and high-contrast for the backend footer. Disclaimer style: {style}."
            )
        return (
            f"Reserve a small compliant disclaimer zone in the requested placement ({placement}). "
            f"Keep that zone visually quiet and readable. Disclaimer style: {style}."
        )

    @classmethod
    def _brand_legal_footer_text_for_format(cls, request: AIOrchestrationRequest) -> str:
        # Extracts brand legal footer text from request state for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        brand_context = getattr(request, "resolved_brand_context", {}) if request is not None else {}
        brand_context = brand_context if isinstance(brand_context, dict) else {}
        brand_assets = brand_context.get("brand_assets") if isinstance(brand_context.get("brand_assets"), dict) else {}
        legal_disclaimers = brand_assets.get("legal_disclaimers")
        if not isinstance(legal_disclaimers, list):
            return ""

        studio_panel = getattr(request, "studio_panel", {}) if request is not None else {}
        studio_panel = studio_panel if isinstance(studio_panel, dict) else {}
        format_name = cls._normalize_metadata_text(studio_panel.get("format"), limit=32).casefold()
        brief = getattr(request, "research_editorial_brief", {}) if request is not None else {}
        brief = brief if isinstance(brief, dict) else {}
        prompt_lower = str(getattr(request, "prompt", "") or "").casefold()
        disclaimer_requested = bool(brief.get("disclaimer_requested")) or ("disclaimer" in prompt_lower)

        applicable: list[dict[str, Any]] = []
        fallback: list[dict[str, Any]] = []
        for disclaimer in legal_disclaimers:
            if not isinstance(disclaimer, dict):
                continue
            text = cls._normalize_metadata_text(
                disclaimer.get("text_template")
                or disclaimer.get("text")
                or disclaimer.get("content"),
                limit=420,
            )
            if not text:
                continue
            fallback.append(disclaimer)
            applies_to_formats = {
                cls._normalize_metadata_text(item, limit=32).casefold()
                for item in (disclaimer.get("applies_to_formats") or [])
                if cls._normalize_metadata_text(item, limit=32)
            }
            if format_name and format_name in applies_to_formats:
                applicable.append(disclaimer)

        selected = applicable[0] if applicable else fallback[0] if disclaimer_requested and fallback else None
        if not selected:
            return ""
        return cls._normalize_metadata_text(
            selected.get("text_template") or selected.get("text") or selected.get("content"),
            limit=420,
        )

    @classmethod
    def _bottom_footer_overlay_requested(
        cls,
        request: AIOrchestrationRequest,
        *,
        legal_footer_text: Any = "",
    ) -> bool:
        # Centralizes bottom footer overlay requested from request state and legal footer text for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if cls._normalize_metadata_text(legal_footer_text, limit=420):
            return True
        brief = getattr(request, "research_editorial_brief", {}) if request is not None else {}
        brief = brief if isinstance(brief, dict) else {}
        requested = bool(brief.get("disclaimer_requested"))
        if not requested:
            prompt_lower = str(getattr(request, "prompt", "") or "").casefold()
            requested = "disclaimer" in prompt_lower
        if not requested:
            return False
        placement = cls._normalize_metadata_text(brief.get("disclaimer_placement"), limit=24).casefold()
        return not placement or placement == "bottom_footer"

    @classmethod
    def _infographic_footer_safe_layout_contract(
        cls,
        *,
        format_name: str,
        footer_requested: bool,
        footer_safe_area: dict[str, Any] | None = None,
    ) -> str:
        # Builds infographic footer safe layout contract from format name, footer requested, and footer safe area for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        if not footer_requested:
            return ""
        normalized_format = cls._normalize_metadata_text(format_name, limit=32).casefold()
        if normalized_format not in {"static", "infographic"}:
            return ""
        safe_area = footer_safe_area if isinstance(footer_safe_area, dict) else {}
        try:
            reserved_height = int(safe_area.get("reserved_height") or 0)
        except (TypeError, ValueError):
            reserved_height = 0
        try:
            content_canvas_height = int(safe_area.get("content_canvas_height") or 0)
        except (TypeError, ValueError):
            content_canvas_height = 0
        reserved_ratio = cls._normalize_metadata_text(safe_area.get("reserved_ratio"), limit=24)
        size_note = (
            f" Calculated footer reserve: {reserved_height}px"
            + (f" ({reserved_ratio})" if reserved_ratio else "")
            + (f"; content-safe height: {content_canvas_height}px." if content_canvas_height else ".")
            if reserved_height
            else ""
        )
        return (
            "Static/infographic footer-safe layout contract: reserve the calculated bottom footer strip before composing sections; "
            "treat it as a forbidden content zone and leave calculated footer strip blank for backend footer. "
            "STATIC/INFOGRAPHIC FOOTER BLANK-SPACE RULE: Leave the bottom strip plain background only; no modules, icons, charts, labels, CTA, fine print, shadows, patterns, or visual details may enter it. "
            "CTA buttons, CTA labels, chart captions, and closing messages must sit above the footer-safe strip with visible separation. "
            "Use at most 3 compact content sections/modules above the footer-safe area; compress overflow into approved summary modules instead of extending into the footer."
            f"{size_note}"
        )

    @staticmethod
    def _image_text_containment_contract(*, format_name: str) -> list[str]:
        # Builds image text containment contract from format name for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        normalized_format = str(format_name or "").strip().casefold()
        if normalized_format == "carousel":
            surface = "carousel slide"
        elif normalized_format == "infographic":
            surface = "infographic"
        elif normalized_format == "story":
            surface = "story frame"
        else:
            surface = "static social creative"
        rule_label = (
            "STATIC/INFOGRAPHIC TEXT NO-TRUNCATION RULE"
            if normalized_format in {"static", "infographic"}
            else "TEXT NO-TRUNCATION RULE"
        )
        return [
            (
                f"{rule_label}: keep every approved readable word, number, chart labels, CTA text, or footer/legal text fully inside the {surface} canvas. "
                "No text may be clipped, hidden behind visual elements, pushed into crop edges, or reduced to unreadable microtype."
            ),
            (
                "If the copy or data feels dense, reduce module count, simplify the illustration, increase quiet space, and prioritize the strongest approved lines instead of overflowing the layout."
            ),
        ]

    @staticmethod
    def _ad_post_visual_quality_contract(format_name: str) -> str:
        # Builds ad post visual quality contract from format name for AIOrchestratorService.
        # This keeps payload shape decisions close to the code that understands the inputs.
        normalized_format = str(format_name or "").strip().casefold()
        if normalized_format not in {"static", "story", "poster", "infographic", "carousel"}:
            return ""
        if normalized_format == "infographic":
            return (
                "Ad-post visual quality rule: campaign-ready social ad creative; Paid-social attractiveness standard: visually attractive, premium, scroll-stopping, mobile-readable; clear emotional hook; foreground/midground/background layering; tactile dimensional hero objects or visual metaphors tied to the topic; Avoid plain educational-poster composition; topic-relevant dimensional/3D hero treatment when brand/reference evidence allows it; do not invent unsupported facts, numbers, labels, UI, or readable text just to make the ad feel more exciting; keep research discipline."
            )
        if normalized_format == "carousel":
            return (
                "Ad-post visual quality contract: create a premium carousel slide with strong narrative hierarchy, consistent series styling, and a distinct visual role for this panel."
            )
        if normalized_format == "static":
            return (
            "Ad-post visual quality rule: campaign-ready social ad creative; Paid-social attractiveness standard: visually attractive, premium, scroll-stopping, mobile-readable; clear emotional hook; foreground/midground/background layering; tactile dimensional hero objects or visual metaphors tied to the topic; Avoid plain educational-poster composition; topic-relevant dimensional/3D hero treatment when brand/reference evidence allows it; do not invent unsupported facts, numbers, labels, UI, or readable text just to make the ad feel more exciting; keep research discipline."
            )
        return (
            "Ad-post visual quality contract: create a premium, feed-native social creative with one clear focal idea, polished brand-safe craft, and scroll-stopping but readable composition."
        )

    @staticmethod
    def _text_overlay_substrate_contract(
        *,
        headline: Any,
        supporting_line: Any = "",
        body: Any = "",
        proof_points: Any = None,
        claim_evidence_pairs: Any = None,
        cta: Any = "",
        slide_role: Any = "",
        proof_points_limit: int = 3,
    ) -> list[str]:
        # Builds text overlay substrate contract from headline, supporting line, and body for AIOrchestratorService.
        # It calls _normalize_metadata_text and _normalize_metadata_list while assembling the payload or prompt text.
        normalized_headline = AIOrchestratorService._normalize_metadata_text(headline, limit=180)
        normalized_supporting = AIOrchestratorService._normalize_metadata_text(supporting_line, limit=220)
        normalized_body = AIOrchestratorService._normalize_metadata_text(body, limit=260)
        normalized_proof_items = AIOrchestratorService._normalize_metadata_list(
            proof_points,
            limit=max(1, min(int(proof_points_limit or 3), 12)),
        )
        claim_lines = AIOrchestratorService._compact_named_items(
            AIOrchestratorService._claim_evidence_pair_lines(
                AIOrchestratorService._normalize_claim_evidence_pairs(claim_evidence_pairs, limit=2),
                limit=2,
            ),
            limit=2,
        )
        normalized_cta = AIOrchestratorService._normalize_metadata_text(cta, limit=80)
        normalized_role = AIOrchestratorService._normalize_metadata_text(slide_role, limit=48)
        semantic_parts: list[str] = []
        if normalized_role:
            semantic_parts.append(f"role={normalized_role}")
        if normalized_headline:
            semantic_parts.append("headline zone requires a large editorial title surface")
        if normalized_supporting:
            semantic_parts.append("support zone requires a compact explanatory line surface")
        if normalized_body and normalized_body.casefold() != normalized_supporting.casefold():
            semantic_parts.append("body zone requires a calm paragraph or callout surface")
        if normalized_proof_items:
            semantic_parts.append(f"proof module count={len(normalized_proof_items)}")
        if claim_lines:
            semantic_parts.append("evidence zones require distinct proof/callout shells")
        if normalized_cta:
            semantic_parts.append("CTA zone requires an empty button or footer shell")
        semantic_summary = " | ".join(semantic_parts) if semantic_parts else "reserve clean copy surfaces"
        cta_guidance = (
            "If a CTA exists, create only an empty premium CTA-safe shell or footer surface in the reserved CTA region; do not write CTA words inside it."
            if normalized_cta
            else "Do not create a visible CTA button unless the layout contract reserves a CTA region; keep any unused CTA area quiet."
        )
        return [
            "TEXT OVERLAY CONTRACT: create a premium visual substrate for exact backend text overlay, not a finished text poster.",
            "Do not render any readable words, letters, numbers, bullets, labels, captions, CTA text, legal text, sample text, or pseudo-text anywhere in the AI image.",
            f"Copy surface plan, with exact words intentionally withheld from image generation: {semantic_summary}.",
            "Design clean empty text-safe regions that visibly belong to the layout: quiet panels, soft cards, divider rules, accent tabs, callout shells, and calm negative space.",
            AIOrchestratorService._brand_color_text_overlay_preparation_contract(),
            cta_guidance,
            "The backend will place the exact approved headline, body, proof, CTA, legal copy, and logo after image generation, so preserve stable contrast, alignment, and clean surfaces in every reserved text zone.",
        ]

    @staticmethod
    def _brand_color_text_overlay_preparation_contract() -> str:
        # Derives brand color text overlay preparation contract for AIOrchestratorService.
        # This turns source evidence into a stable planning hint.
        return (
            "Brand-color text overlay preparation rule: reserve high-contrast blank surfaces suitable for backend-rendered headline keywords and key phrases in approved brand primary/accent colors. "
            "Do not render, color, highlight, outline, shadow, or invent any readable words, letters, numbers, labels, keyword chips, underlines, or pseudo-text yourself; only prepare the empty surfaces."
        )

    @staticmethod
    def _brand_color_final_text_emphasis_contract() -> str:
        # Derives brand color final text emphasis contract for AIOrchestratorService.
        # This turns source evidence into a stable planning hint.
        return (
            "Brand-color approved-copy emphasis rule: use approved brand primary/accent colors only to emphasize important words already present in the exact approved headline, proof, or CTA copy below. "
            "Do not add, recolor, highlight, underline, outline, shadow, or invent any extra words, numbers, labels, keyword chips, badges, or pseudo-text."
        )

    @classmethod
    def _should_use_backend_text_overlay_for_ai_final_render(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> bool:
        # Checks use backend text overlay ai final from request state and creative decision for AIOrchestratorService.
        # It reuses _effective_template_surface_policy so related checks follow the same rule.
        format_name = str((request.studio_panel or {}).get("format") or "").strip().lower()
        if format_name in {"static", "infographic"}:
            return False
        if format_name != "carousel":
            return True
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
        )
        return template_surface_policy != "style_reference_only"

    @classmethod
    def _ai_final_render_text_strategy(
        cls,
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> str:
        # Extracts ai final text strategy from request state and creative decision for AIOrchestratorService.
        # It calls _should_use_backend_text_overlay_for_ai_final_render to turn raw evidence into the structured signal the caller needs.
        if cls._should_use_backend_text_overlay_for_ai_final_render(request, creative_decision):
            return "backend_exact_text_on_ai_text_safe_substrate"
        return "ai_renders_finished_slide_with_exact_copy"

    @staticmethod
    def _final_text_render_contract(
        *,
        headline: Any,
        supporting_line: Any = "",
        body: Any = "",
        proof_points: Any = None,
        claim_evidence_pairs: Any = None,
        cta: Any = "",
        slide_role: Any = "",
        legal_footer: Any = "",
        proof_points_limit: int = 3,
    ) -> list[str]:
        # Builds final text contract from headline, supporting line, and body for AIOrchestratorService.
        # It calls _normalize_metadata_text and _normalize_metadata_list while assembling the payload or prompt text.
        normalized_headline = AIOrchestratorService._normalize_metadata_text(headline, limit=180)
        normalized_supporting = AIOrchestratorService._normalize_metadata_text(supporting_line, limit=220)
        normalized_body = AIOrchestratorService._normalize_metadata_text(body, limit=260)
        normalized_proof_items = AIOrchestratorService._normalize_metadata_list(
            proof_points,
            limit=max(1, min(int(proof_points_limit or 3), 12)),
        )
        claim_lines = AIOrchestratorService._claim_evidence_pair_lines(
            AIOrchestratorService._normalize_claim_evidence_pairs(claim_evidence_pairs, limit=2),
            limit=2,
        )
        normalized_cta = AIOrchestratorService._normalize_metadata_text(cta, limit=80)
        normalized_role = AIOrchestratorService._normalize_metadata_text(slide_role, limit=48)
        normalized_footer = AIOrchestratorService._normalize_metadata_text(legal_footer, limit=360)

        guidance = [
            "FINAL TEXT RENDER CONTRACT: render the finished slide with the exact approved readable copy inside the intended text regions.",
            "Do not add any extra readable words, repeated shadow headlines, watermark text, background typography, pseudo-text, lorem ipsum, sample labels, random numbers, or stray glyphs anywhere else in the image.",
            "Keep all readable text confined to the reserved text-safe zones with clean hierarchy, stable alignment, strong contrast, and correct spelling.",
            AIOrchestratorService._brand_color_final_text_emphasis_contract(),
        ]
        if normalized_footer:
            guidance.append(
                "Reserve a thin quiet bottom strip for exact post-processing compliance text."
            )
            guidance.append("Keep that bottom strip completely blank: no words, no fine print, no disclaimer label, no pseudo-text.")
            guidance.append(
                "Keep the bottom strip clean, low-noise, and high-contrast enough for a small exact compliance overlay without overpowering the composition."
            )
        else:
            guidance.append("Do not invent a legal footer, disclaimer paragraph, or CTA copy beyond the approved text below.")
        if normalized_role:
            guidance.append(f"Approved slide role for this exact copy: {normalized_role}.")
        if normalized_headline:
            guidance.append(f'Use this headline verbatim in the headline zone: "{normalized_headline}".')
        if normalized_supporting:
            guidance.append(f'Use this supporting line verbatim in the support zone: "{normalized_supporting}".')
        if normalized_body and normalized_body.casefold() != normalized_supporting.casefold():
            guidance.append(f'Use this body copy verbatim in the body zone: "{normalized_body}".')
        if normalized_proof_items:
            guidance.append(
                "Use these exact proof or callout lines as readable on-canvas copy in distinct modules: "
                + "; ".join(f'"{item}"' for item in normalized_proof_items)
                + "."
            )
        elif claim_lines:
            guidance.append(
                "If the slide needs evidence callouts, use only these approved claim/evidence anchors as readable copy: "
                + "; ".join(f'"{item}"' for item in claim_lines)
                + "."
            )
        if normalized_cta:
            guidance.append(f'Use this CTA verbatim only in the CTA zone: "{normalized_cta}".')
        else:
            guidance.append("Do not invent a CTA button or footer strip on this slide.")
        guidance.append(
            "Render the copy as part of the finished design, not as empty shells for later backend overlay."
        )
        return guidance

    @classmethod
    def _carousel_slide_visible_module_lines(
        cls,
        slide: dict[str, Any],
        *,
        limit: int,
    ) -> list[str]:
        # Extracts carousel slide visible module lines from slide and limit for AIOrchestratorService.
        # It calls _dedupe_metadata_collection and _claim_evidence_pair_lines to turn raw evidence into the structured signal the caller needs.
        normalized_limit = max(1, min(int(limit or 3), 8))
        approved_lines: list[str] = []
        for key, item_limit in (
            ("proof_points", 8),
            ("body_points", 8),
            ("stat_highlights", 8),
        ):
            approved_lines.extend(cls._normalize_metadata_list(slide.get(key), limit=item_limit))
        approved_lines.extend(
            cls._claim_evidence_pair_lines(
                cls._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=3),
                limit=3,
            )
        )
        return cls._dedupe_metadata_collection(
            approved_lines,
            blocked_texts=[
                slide.get("headline"),
                slide.get("supporting_line"),
                slide.get("body"),
                slide.get("cta"),
            ],
            limit=normalized_limit,
        )

    @staticmethod
    def _scene_graph_legal_footer_text(scene_graph: GenerationSceneGraph) -> str:
        # Extracts scene graph legal footer text from scene graph for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        for element in scene_graph.elements:
            role = str(element.role or "").strip().casefold()
            if role not in {"legal", "footer", "disclaimer"}:
                continue
            text = AIOrchestratorService._normalize_metadata_text(element.text, limit=420)
            if text:
                return text
        return ""

    @staticmethod
    def _dedupe_prompt_sections(sections: list[str]) -> list[str]:
        # Deduplicates dedupe prompt sections from sections for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        cleaned: list[str] = []
        seen: set[str] = set()
        for section in sections:
            text = " ".join(str(section or "").split()).strip()
            if not text or text.endswith(": ."):
                continue
            key = text.casefold()
            if key in seen:
                continue
            seen.add(key)
            cleaned.append(text)
        return cleaned

    @staticmethod
    def _compose_prompt_sections(
        *,
        required_sections: list[str],
        optional_sections: list[str],
        limit: int,
    ) -> str:
        # Composes compose prompt sections from required sections, optional sections, and limit for AIOrchestratorService.
        # It calls _dedupe_prompt_sections and _trim_prompt while assembling the payload or prompt text.
        parts: list[str] = []
        current_length = 0
        required = AIOrchestratorService._dedupe_prompt_sections(required_sections)
        required_keys = {section.casefold() for section in required}
        optional = [
            section
            for section in AIOrchestratorService._dedupe_prompt_sections(optional_sections)
            if section.casefold() not in required_keys
        ]
        deferred_required: list[str] = []
        for section in required:
            separator = 1 if parts else 0
            section_length = len(section) + separator
            if current_length + section_length > limit:
                deferred_required.append(section)
                continue
            if separator:
                current_length += 1
            parts.append(section)
            current_length += len(section)
        for section in deferred_required:
            separator = 1 if parts else 0
            remaining = limit - current_length - separator
            if remaining <= 96:
                continue
            trimmed = AIOrchestratorService._trim_prompt(section, remaining)
            if separator:
                current_length += 1
            parts.append(trimmed)
            current_length += len(trimmed)
        for section in optional:
            separator = 1 if parts else 0
            section_length = len(section) + separator
            if current_length + section_length > limit:
                continue
            if separator:
                current_length += 1
            parts.append(section)
            current_length += len(section)
        return AIOrchestratorService._trim_prompt(" ".join(parts), limit)


    @staticmethod
    def build_final_render_prompt(
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        creative_decision: CreativeDecisionPayload,
        scene_graph: GenerationSceneGraph,
        message_strategy: MessageStrategyPayload | None = None,
        reference_images: list[dict[str, Any]] | None = None,
        retry_note: str | None = None,
        visual_explanation_plan: dict[str, Any] | None = None,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Builds final prompt from request state, copy payload, and creative decision for AIOrchestratorService.
        # It calls _design_system_prompt_guidance and _compact_palette_summary while assembling the payload or prompt text.
        compiled_context = dict(compiled_context or {})
        metadata = text_payload.metadata or {}
        visual_identity = request.resolved_brand_context.get("visual_identity", {}) or {}
        design_system_guidance = AIOrchestratorService._design_system_prompt_guidance(visual_identity)
        palette = AIOrchestratorService._compact_palette_summary(visual_identity)
        palette_guidance = AIOrchestratorService._palette_role_guidance(visual_identity)
        strict_palette_contract = AIOrchestratorService._strict_palette_contract(visual_identity)
        typography = AIOrchestratorService._compact_typography_summary(visual_identity)
        asset_strategy = creative_decision.asset_strategy if isinstance(creative_decision.asset_strategy, dict) else {}
        template_surface_policy = AIOrchestratorService._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata if isinstance(metadata, dict) else {},
        )
        early_format_name = str(request.studio_panel.get("format") or "static")
        data_list_surface_contract = AIOrchestratorService._data_list_surface_contract(
            prompt=getattr(request, "prompt", ""),
            format_name=early_format_name,
            metadata=metadata if isinstance(metadata, dict) else {},
            text_payload=text_payload,
        )
        data_list_module_count = (
            AIOrchestratorService._int_or_none(data_list_surface_contract.get("module_count"))
            if isinstance(data_list_surface_contract, dict)
            else None
        ) or 0
        # Data-list content controls how many proof points and sections are allowed into the final image prompt.
        proof_points = AIOrchestratorService._strip_blocked_compliance_phrases(
            AIOrchestratorService._compact_named_items(metadata.get("proof_points"), limit=max(4, data_list_module_count))
        )
        infographic_sections = AIOrchestratorService._compact_infographic_section_specs(
            metadata.get("infographic_section_specs"),
            section_limit=max(4, data_list_module_count or 4),
            item_limit=max(2, min(data_list_module_count or 2, 4)),
        )
        static_panel_spec = AIOrchestratorService._compact_static_panel_spec(
            metadata.get("static_panel_spec"),
            item_limit=max(2, data_list_module_count or 2),
        )
        geometry_contract = AIOrchestratorService._compact_scene_graph_geometry(scene_graph)
        layout_dna_contract = AIOrchestratorService._compact_layout_dna_contract(compiled_context)
        layout_archetype = AIOrchestratorService._normalize_metadata_text(
            (scene_graph.styles or {}).get("layout_archetype")
            or (creative_decision.planning_hints or {}).get("layout_archetype")
            or creative_decision.layout_mode,
            limit=120,
        )
        message_theme = ""
        emotional_direction = ""
        if isinstance(message_strategy, MessageStrategyPayload):
            message_theme = AIOrchestratorService._normalize_metadata_text(
                message_strategy.primary_campaign_theme,
                limit=180,
            )
            emotional_direction = AIOrchestratorService._normalize_metadata_text(
                message_strategy.emotional_messaging_direction,
                limit=100,
            )
        brand_name = AIOrchestratorService._normalize_metadata_text(
            request.resolved_brand_context.get("brand_name") or "",
            limit=80,
        )
        file_type = str(request.studio_panel.get("file_type") or "png").upper()
        platform = str(request.studio_panel.get("platform_preset") or "social")
        format_name = early_format_name
        legal_footer_text = AIOrchestratorService._scene_graph_legal_footer_text(scene_graph)
        use_backend_text_overlay = AIOrchestratorService._should_use_backend_text_overlay_for_ai_final_render(
            request,
            creative_decision,
        )
        explicit_data_visual_request = AIOrchestratorService._has_explicit_data_visual_request(
            getattr(request, "prompt", ""),
            text_payload=text_payload,
        )
        legal_footer_text = (
            AIOrchestratorService._scene_graph_legal_footer_text(scene_graph)
            or AIOrchestratorService._brand_legal_footer_text_for_format(request)
        )
        bottom_footer_requested = AIOrchestratorService._bottom_footer_overlay_requested(
            request,
            legal_footer_text=legal_footer_text,
        )
        canvas = getattr(scene_graph, "canvas", None)
        # Footer geometry is calculated before prompt assembly so the image model leaves enough empty space for backend overlay.
        footer_safe_area = (
            calculate_footer_safe_area(
                canvas_width=int(getattr(canvas, "width", 0) or 1),
                canvas_height=int(getattr(canvas, "height", 0) or 1),
                footer_text=legal_footer_text,
            )
            if bottom_footer_requested
            else {}
        )
        infographic_footer_layout_contract = AIOrchestratorService._infographic_footer_safe_layout_contract(
            format_name=format_name,
            footer_requested=bottom_footer_requested,
            footer_safe_area=footer_safe_area,
        )
        canvas_fit_guidance = AIOrchestratorService._canvas_fit_guidance(request.studio_panel)
        generation_to_export_fit_guidance = AIOrchestratorService._generation_to_export_fit_guidance(request.studio_panel)
        supporting_visual_system = AIOrchestratorService._normalized_supporting_visual_system(creative_decision.asset_strategy or {})
        iconography_supported = (
            format_name in {"carousel", "infographic"}
            or supporting_visual_system == "icon_sequence"
            or bool((creative_decision.asset_strategy or {}).get("icon_sequence"))
        )
        logo_position_hint = AIOrchestratorService._effective_logo_position_hint(
            request=request,
            creative_decision=creative_decision,
            text_payload=AIOrchestratorService._text_payload_prompt_dict(text_payload),
        )
        logo_safe_zone_guidance = AIOrchestratorService._logo_safe_zone_guidance(
            request,
            scene_graph,
            hint=logo_position_hint,
        )
        reserved_logo_area = AIOrchestratorService._logo_reserved_area_label(logo_position_hint)
        logo_surface_guidance = AIOrchestratorService._logo_surface_guidance(
            background_tone=AIOrchestratorService._resolve_logo_background_tone(
                metadata=metadata,
                creative_decision=creative_decision,
                scene_graph=scene_graph,
            )
        )
        reference_template_blueprint = AIOrchestratorService._reference_template_sample_page_blueprint(
            compiled_context,
            format_name=format_name,
        )
        active_sample_page_blueprint = (
            metadata.get("sample_page_blueprint")
            if isinstance(metadata.get("sample_page_blueprint"), dict)
            else reference_template_blueprint
        )
        normalized_format_name = AIOrchestratorService._normalize_metadata_text(format_name, limit=32).casefold()
        llm_led_static_infographic = AIOrchestratorService._llm_led_static_infographic_format(normalized_format_name)
        # Static and infographic share this final-render builder; this flag decides when the image model owns layout/content composition.
        # LLM-led static/infographic renders deliberately drop old template geometry and let the current prompt drive layout.
        scene_graph_text_region_count = sum(
            bool(getattr(element, "visible", True)) and AIOrchestratorService._scene_graph_text_like_element(element)
            for element in (scene_graph.elements or [])
        )
        scene_graph_has_text_regions = scene_graph_text_region_count > 0
        strict_static_final_text_contract = (
            normalized_format_name == "static"
            and scene_graph_text_region_count <= 1
        )
        reference_template_structure_active = bool(active_sample_page_blueprint) and normalized_format_name in {
            "static",
            "infographic",
        } and not llm_led_static_infographic
        # The content budget is the bridge between text payload size, sample layout, logo space, and footer/disclaimer space.
        final_render_layout_content_budget = AIOrchestratorService._dynamic_layout_content_budget(
            request=request,
            format_name=format_name,
            text_payload=text_payload,
            metadata=metadata,
            sample_page_blueprint=active_sample_page_blueprint,
            scene_graph=scene_graph,
            legal_footer_text=legal_footer_text,
            disclaimer_guidance=AIOrchestratorService._disclaimer_overlay_guidance(request),
            logo_position_hint=logo_position_hint,
        )
        default_format_guidance = (
            "Treat this as one premium branded visual substrate with final text reserved for backend overlay."
            if use_backend_text_overlay
            else "Treat this as one finished branded social image."
        )
        # Format guidance is the main common-vs-independent split inside this prompt: carousel panels, infographic modules, or static social art.
        format_guidance = {
            "carousel": (
                "Treat this as a premium carousel panel or cover image with strong sectioning, "
                "slide-safe hierarchy, concise card-like structure, and a self-contained visual story panel rather than a plain poster."
            ),
            "infographic": (
                "Treat this as a genuine infographic, not a plain headline poster. Build a modular visual explainer "
                f"with {'up to 3' if infographic_footer_layout_contract else '3-4'} clearly separated sections and strong vertical pacing. Only use chart, icon, or diagram callouts "
                "when the supplied content clearly requires them. Do not default to stock growth arrows, rising bars, or generic finance stickers."
            ),
        }.get(format_name, default_format_guidance)
        if llm_led_static_infographic:
            # Static/infographic LLM-led flow returns a prompt that asks the image model to design the full creative, not just fill a slot.
            format_guidance = (
                "Treat this as a finished static/infographic social creative. Let the image model choose the composition, module count, visual metaphor, table/list/ranking treatment, and content hierarchy required by the current prompt while keeping only the logo-safe corner and footer/disclaimer strip reserved."
                + (
                    " Build a modular visual explainer with up to 3 clearly separated sections and strong vertical pacing."
                    if format_name == "infographic"
                    else ""
                )
            )
        reference_summary = AIOrchestratorService._compact_reference_assets(reference_images or [])
        grounding_sections = AIOrchestratorService._final_render_grounding_sections(
            request=request,
            creative_decision=creative_decision,
            text_payload=text_payload,
            compiled_context=compiled_context,
            reference_assets=reference_images,
        )
        if llm_led_static_infographic:
            # In LLM-led mode, old reference grounding is cleared so stale template subjects do not leak into the final ad.
            grounding_sections = []
            reference_summary = "None"
        research_quality_section = AIOrchestratorService._research_editorial_prompt_section(
            request,
            compiled_context,
        )
        consultant_contract = AIOrchestratorService._consultant_quality_contract(
            for_visual_only=False,
            for_carousel=format_name == "carousel",
            for_static_infographic_ad=llm_led_static_infographic,
        )
        ad_post_visual_quality_contract = AIOrchestratorService._ad_post_visual_quality_contract(format_name)
        text_containment_contract = AIOrchestratorService._image_text_containment_contract(format_name=format_name)
        multimodal_balance_contract = AIOrchestratorService._multimodal_balance_contract(
            format_name=format_name,
            supporting_line=AIOrchestratorService._normalize_metadata_text(metadata.get("supporting_line") or text_payload.body, limit=220),
            proof_points=AIOrchestratorService._normalize_metadata_list(metadata.get("proof_points"), limit=4),
            claim_evidence_pairs=AIOrchestratorService._normalize_claim_evidence_pairs(metadata.get("claim_evidence_pairs"), limit=3),
            for_visual_only=False,
        )
        reference_family_contract = AIOrchestratorService._reference_family_contract_sections(
            compiled_context,
            for_visual_only=False,
        )
        reference_adaptation_profile_section = AIOrchestratorService._reference_adaptation_profile_prompt_section(
            compiled_context.get("reference_adaptation_profile")
        )
        generation_surface_contract_section = AIOrchestratorService._generation_surface_contract_prompt_section(
            compiled_context.get("generation_surface_contract")
        )
        reference_template_adaptation_sections = (
            []
            if llm_led_static_infographic
            else AIOrchestratorService._reference_template_adaptation_prompt_sections(
                compiled_context,
                format_name=format_name,
                layout_content_budget=final_render_layout_content_budget,
                sample_page_blueprint=active_sample_page_blueprint,
            )
        )
        # Reference/template sections are kept out of LLM-led mode to avoid leaking stale sample structure.
        visual_style_policy_sections = AIOrchestratorService._visual_style_policy_prompt_sections(
            compiled_context,
            format_name=format_name,
            reference_assets=reference_images,
        )
        sequence_alignment_sections = AIOrchestratorService._sequence_blueprint_alignment_sections(
            compiled_context,
            for_carousel=format_name == "carousel",
            for_visual_only=False,
        )
        visual_plan = visual_explanation_plan or AIOrchestratorService._visual_explanation_plan(
            request,
            text_payload,
            creative_decision,
            reference_images,
            message_strategy,
        )
        # The visual plan summarizes how the content should become imagery before all guardrail sections are composed.
        infographic_surface_enforcement = (
            ""
            if llm_led_static_infographic
            else AIOrchestratorService._infographic_surface_enforcement_section(
                request=request,
                format_name=format_name,
                structured_visual_metadata=metadata.get("structured_visual_metadata"),
                visual_plan=visual_plan,
            )
        )
        data_list_surface_guidance = (
            ""
            if llm_led_static_infographic
            else AIOrchestratorService._data_list_surface_prompt_section(
                request=request,
                text_payload=text_payload,
                metadata=metadata if isinstance(metadata, dict) else {},
                format_name=format_name,
                use_backend_text_overlay=use_backend_text_overlay,
                visual_plan=visual_plan,
            )
        )
        visual_plan_guidance = AIOrchestratorService._visual_explanation_guidance(visual_plan)
        disclaimer_overlay_guidance = AIOrchestratorService._disclaimer_overlay_guidance(request)
        content_format_guide_section = AIOrchestratorService._content_format_guide_prompt_section(request)
        live_research_facts_section = AIOrchestratorService._live_research_verified_facts_prompt_section(request)
        anti_hallucination_section = AIOrchestratorService._image_generation_anti_hallucination_prompt_section(
            request=request,
            compiled_context=compiled_context,
        )
        retry_instruction = AIOrchestratorService._normalize_metadata_text(retry_note, limit=260)
        proof_points_limit = max(3, data_list_module_count or 3)
        llm_led_current_prompt_instruction = (
            f"Current user prompt is the subject authority: {AIOrchestratorService._normalize_metadata_text(request.prompt, limit=420)}."
            if llm_led_static_infographic
            else ""
        )
        llm_led_topic_fit_contract = AIOrchestratorService._final_render_topic_fit_contract(
            request=request,
            format_name=format_name,
        )
        llm_led_composition_instruction = (
            "LLM-led composition authority for static/infographic: design the complete content hierarchy, wording, modules, comparison/ranking treatment, topic-specific imagery, spacing, and visual emphasis from the current user prompt and approved context. Backend controls only the logo-safe corner and the footer/disclaimer strip; do not preserve sample/template geometry, scene-graph regions, extracted layout controls, or old scaffold content."
            if llm_led_static_infographic
            else ""
        )
        social_media_ad_contract = (
            AIOrchestratorService._social_media_ad_composition_contract(
                format_name=format_name,
                platform=platform,
            )
            if llm_led_static_infographic
            else ""
        )
        content_visual_explanation_contract = (
            AIOrchestratorService._content_visual_explanation_contract(
                request=request,
                format_name=format_name,
                platform=platform,
            )
            if llm_led_static_infographic
            else ""
        )
        creative_topic_3d_visual_contract = (
            AIOrchestratorService._creative_topic_3d_visual_contract(
                request=request,
                format_name=format_name,
                compiled_context=compiled_context,
                reference_assets=reference_images,
            )
            if llm_led_static_infographic
            else ""
        )
        quality_floor_contract = (
            AIOrchestratorService._final_render_static_infographic_quality_floor_contract(
                request=request,
                format_name=format_name,
            )
            if llm_led_static_infographic
            else ""
        )
        compact_hallucination_guard = (
            AIOrchestratorService._compact_static_infographic_hallucination_guard(
                request=request,
                format_name=format_name,
            )
            if llm_led_static_infographic
            else ""
        )
        hallucination_control_contract = (
            AIOrchestratorService._image_hallucination_control_contract(
                format_name=format_name,
                use_backend_text_overlay=use_backend_text_overlay,
            )
            if llm_led_static_infographic or use_backend_text_overlay
            else ""
        )
        llm_led_text_contract = [
            # These rules prevent static infographic renders from inventing facts while still allowing rewritten visual copy.
            "Static/infographic content authority: render topic-correct main content directly in the image. Use upstream headline, body, proof points, CTA, and metadata only as optional semantic hints; if they conflict with the current user prompt, rewrite them to match the current prompt.",
            "Do not carry over unrelated previous-request topics, sample headlines, template copy, sample labels, old section titles, placeholder rows, or stale visual objects.",
            "Do not invent exact dates, percentages, currency amounts, rankings, table values, regulatory claims, or source attributions unless they are present in the current user prompt, approved text payload, retrieved verified facts, or live-research facts. If exact values are unavailable, use qualitative comparison/ranking language instead of fake numbers.",
            f"Optional upstream semantic hints: headline={AIOrchestratorService._normalize_metadata_text(text_payload.headline, limit=160)}; body={AIOrchestratorService._normalize_metadata_text(text_payload.body, limit=260)}; cta={AIOrchestratorService._normalize_metadata_text(text_payload.cta, limit=120)}.",
            f"Optional approved proof/list hints: {proof_points}.",
        ]
        if format_name == "infographic":
            _infographic_problem = AIOrchestratorService._normalize_metadata_text(metadata.get("problem_statement"), limit=300)
            _infographic_solution = AIOrchestratorService._normalize_metadata_text(metadata.get("solution_statement"), limit=300)
            _infographic_quote = AIOrchestratorService._normalize_metadata_text(metadata.get("customer_quote"), limit=300)
            _infographic_quote_name = AIOrchestratorService._normalize_metadata_text(metadata.get("customer_name"), limit=80)
            _infographic_process_steps = AIOrchestratorService._normalize_metadata_list(metadata.get("process_steps"), limit=6)
            _infographic_stat_highlights = AIOrchestratorService._normalize_metadata_list(metadata.get("stat_highlights"), limit=6)
            _infographic_extra_content: list[str] = []
            if _infographic_problem:
                _infographic_extra_content.append(f"Problem statement to render: {_infographic_problem}.")
            if _infographic_solution:
                _infographic_extra_content.append(f"Solution statement to render: {_infographic_solution}.")
            if _infographic_quote:
                _infographic_extra_content.append(f"Customer testimonial to render: \"{_infographic_quote}\" — {_infographic_quote_name}.")
            if _infographic_process_steps:
                _infographic_extra_content.append(f"Process steps to render as numbered timeline: {'; '.join(_infographic_process_steps)}.")
            if _infographic_stat_highlights:
                _infographic_extra_content.append(f"Stat highlights to render as metric cards: {'; '.join(_infographic_stat_highlights)}.")
            if infographic_sections:
                _infographic_extra_content.append(f"Infographic section plan to render: {infographic_sections}.")
            if _infographic_extra_content:
                _infographic_extra_content.insert(0, "Infographic content authority: render ALL the following content sections directly in the image with perfect spelling, crisp legibility, and proper visual hierarchy. Every word must appear exactly as written:")
                llm_led_text_contract.extend(_infographic_extra_content)
        text_overlay_contract = (
            AIOrchestratorService._text_overlay_substrate_contract(
                headline=text_payload.headline,
                supporting_line=metadata.get("supporting_line") or text_payload.body,
                body=text_payload.body,
                proof_points=metadata.get("proof_points"),
                claim_evidence_pairs=metadata.get("claim_evidence_pairs"),
                cta=text_payload.cta,
                proof_points_limit=proof_points_limit,
            )
            if use_backend_text_overlay
            else AIOrchestratorService._final_text_render_contract(
                headline=text_payload.headline,
                supporting_line=metadata.get("supporting_line") or text_payload.body,
                body=text_payload.body,
                proof_points=metadata.get("proof_points"),
                claim_evidence_pairs=metadata.get("claim_evidence_pairs"),
                cta=text_payload.cta,
                legal_footer=legal_footer_text,
                proof_points_limit=proof_points_limit,
            )
            if llm_led_static_infographic and scene_graph_has_text_regions
            else llm_led_text_contract
            if llm_led_static_infographic
            else AIOrchestratorService._final_text_render_contract(
                headline=text_payload.headline,
                supporting_line=metadata.get("supporting_line") or text_payload.body,
                body=text_payload.body,
                proof_points=metadata.get("proof_points"),
                claim_evidence_pairs=metadata.get("claim_evidence_pairs"),
                cta=text_payload.cta,
                legal_footer=legal_footer_text,
                proof_points_limit=proof_points_limit,
            )
        )
        opening_instruction = (
            "Create a premium, client-ready visual substrate for backend text and logo overlay."
            if use_backend_text_overlay
            else "Create one finished premium branded social creative."
        )
        cohesive_visual_instruction = (
            "Render a cohesive, modern, client-ready visual substrate with clean hierarchy, premium spacing, clear focal path, and no readable text."
            if use_backend_text_overlay
            else "Render a cohesive, modern, client-ready finished visual with clean hierarchy, premium spacing, and a clear focal path."
        )
        final_text_safety_instruction = (
            "Backend overlay collision guard: do not render headline, body, CTA, legal copy, labels, bullets, letters, numbers, pseudo-text, or any visible lettering. Leave clean high-contrast surfaces inside the contracted text zones so the renderer can place exact copy afterward."
            if use_backend_text_overlay
            else "CRITICAL TEXT WRAPPING RULE: Render the approved headline, body/proof content, labels, and CTA as final readable image text inside their designated zones. Keep every letter, word, number, label, row, CTA, and footer-safe boundary fully visible with generous padding; never clip, crop, truncate, fade out, hide, or continue text past a card/panel/canvas edge. If space is tight, shorten/wrap the copy, reduce module count, or simplify visuals before allowing cut-off text. Keep the corner-safe brand area and footer/disclaimer strip blank because only those exact elements are composited afterward."
        )
        main_text_authority_instruction = (
            (
                "Main content render authority: the image model must render headline, body, proof/list/table rows, labels, and CTA as finished readable content. Backend post-processing is limited to exact stored brand asset placement and legal footer/disclaimer only."
                if scene_graph_has_text_regions
                else llm_led_composition_instruction
            )
            if llm_led_static_infographic
            else ""
            if use_backend_text_overlay
            else "Main content render authority: the image model must render headline, body, proof/list/table rows, labels, and CTA as finished readable content. Backend post-processing is limited to exact stored brand asset placement and legal footer/disclaimer only."
        )
        proof_point_render_instruction = (
            "Proof points are optional semantic hints for the model; current prompt fit is more important than preserving their exact layout."
            if llm_led_static_infographic
            else f"Use these proof points only as semantic visual cues, not readable bullets: {proof_points}."
            if use_backend_text_overlay
            else f"Use these proof points as approved readable content only where they fit the layout; keep wording concise and do not invent unsupported values: {proof_points}."
        )
        if llm_led_static_infographic:
            reference_adaptation_profile_section = ""
            generation_surface_contract_section = ""
            reference_family_contract = []
            sequence_alignment_sections = []
            geometry_contract = ""
            layout_dna_contract = ""
            static_panel_spec = ""
            visual_plan_guidance = ""
            if format_name != "infographic":
                infographic_sections = ""
        brand_design_system_guidance_sections = [
            f"Brand design-system layout guidance: {design_system_guidance.get('layout')}." if design_system_guidance.get("layout") else "",
            f"Preferred zone roles from the brand system: {design_system_guidance.get('zones')}." if design_system_guidance.get("zones") else "",
            f"Background style guidance from the brand system: {design_system_guidance.get('background')}." if design_system_guidance.get("background") else "",
            f"Motif guidance from the brand system: {design_system_guidance.get('motifs')}." if design_system_guidance.get("motifs") else "",
            f"Hierarchy guidance from the brand system: {design_system_guidance.get('hierarchy')}." if design_system_guidance.get("hierarchy") else "",
            f"Content-structure guidance from the brand system: {design_system_guidance.get('content_structure')}." if design_system_guidance.get("content_structure") else "",
            f"Image-treatment guidance from the brand system: {design_system_guidance.get('image_treatment')}." if design_system_guidance.get("image_treatment") else "",
            f"Visual-craft guidance from the brand system: {design_system_guidance.get('visual_craft')}." if design_system_guidance.get("visual_craft") else "",
            f"Composition guidance from the brand system: {design_system_guidance.get('composition')}." if design_system_guidance.get("composition") else "",
            f"Subject guidance from the brand system: {design_system_guidance.get('subjects')}." if design_system_guidance.get("subjects") else "",
            f"Editorial rhythm guidance from the brand system: {design_system_guidance.get('editorial')}." if design_system_guidance.get("editorial") else "",
            f"Brand-cue guidance from the brand system: {design_system_guidance.get('brand_cues')}." if design_system_guidance.get("brand_cues") else "",
        ]
        crop_crowding_instruction = (
            (
                "Do not crop or crowd any reserved text, CTA, logo-safe corner, or footer/disclaimer strip. All other layout, text placement, and visual hierarchy are model-led."
                if scene_graph_has_text_regions
                else "Do not crop or crowd the logo-safe corner or footer/disclaimer strip. All other layout, text placement, and visual hierarchy are model-led."
            )
            if llm_led_static_infographic
            else "Do not crop or crowd any reserved text, CTA, corner-safe, or legal-safe region. If space feels tight, simplify the visual substrate instead of pushing zones to the edge."
        )
        export_frame_fit_instruction = (
            "All reserved overlay regions must remain fully inside the export frame; do not let cards, shells, dividers, or image subjects touch or cross the crop boundary."
            if use_backend_text_overlay
            else (
                "All rendered text regions plus the reserved corner-safe and legal/footer areas must remain fully inside the export frame; do not let cards, shells, dividers, or image subjects touch or cross the crop boundary."
                if llm_led_static_infographic and scene_graph_has_text_regions
                else (
                    "Keep the logo-safe corner and legal/footer strip fully inside the export frame; keep model-rendered content inside the canvas without crowding."
                    if llm_led_static_infographic
                    else "All rendered text regions plus the reserved corner-safe and legal/footer areas must remain fully inside the export frame; do not let cards, shells, dividers, or image subjects touch or cross the crop boundary."
                )
            )
        )
        static_infographic_backend_overlay_contract = (
            "Backend overlay contract for static/infographic: the image model must not draw, typeset, imitate, or place any standalone brand asset, brand-name signature, watermark, masthead, registration text, disclaimer, or footer text. Keep the brand-asset safe corner and bottom footer/disclaimer strip plain and empty; backend post-processing alone will place the exact stored brand asset and legal footer."
            if llm_led_static_infographic
            else ""
        )
        sections = [
            opening_instruction,
            *(
                []
                if llm_led_static_infographic
                else [
                    f"Keep the {reserved_logo_area} area completely empty and visually clean. Do not place any headline, body copy, supporting text, proof point, CTA, icon, or visual element inside or immediately adjacent to this corner.",
                    "Do not typeset the brand name as a top-corner signature.",
                    *AIOrchestratorService._logo_non_generation_contract(
                        brand_name=brand_name,
                        reserved_logo_area=reserved_logo_area,
                    ),
                ]
            ),
            logo_surface_guidance,
            disclaimer_overlay_guidance,
            infographic_footer_layout_contract,
            *text_containment_contract,
            *(
                [
                    format_guidance,
                    content_visual_explanation_contract,
                    ad_post_visual_quality_contract,
                    final_text_safety_instruction,
                ]
                if llm_led_static_infographic
                else []
            ),
            f"Platform: {platform}.",
            f"Format: {format_name}.",
            f"Output type: {file_type}.",
            llm_led_current_prompt_instruction,
            llm_led_topic_fit_contract,
            social_media_ad_contract,
            content_visual_explanation_contract,
            creative_topic_3d_visual_contract,
            quality_floor_contract,
            hallucination_control_contract,
            format_guidance,
            infographic_surface_enforcement,
            content_format_guide_section,
            live_research_facts_section,
            retry_instruction,
            canvas_fit_guidance,
            generation_to_export_fit_guidance,
            "" if llm_led_static_infographic else f"Creative mode: {creative_decision.layout_mode}.",
            "" if llm_led_static_infographic else f"Layout archetype: {layout_archetype}.",
            main_text_authority_instruction,
            *reference_template_adaptation_sections,
            (
                (
                    f"Scene-graph geometry contract JSON: {geometry_contract}. Preserve these normalized regions closely as empty overlay-safe surfaces; do not render lettering inside them and never recompose from scratch."
                    if use_backend_text_overlay
                    else f"Scene-graph geometry contract JSON: {geometry_contract}. Preserve these normalized regions closely; only make tiny adjustments for readability and never recompose from scratch."
                )
                if geometry_contract
                else ""
            ),
            (
                f"Template/layout DNA contract JSON: {layout_dna_contract}. Use this as the authoritative composition skeleton for region balance, hierarchy, and spacing."
                if layout_dna_contract
                else ""
            ),
            "" if llm_led_static_infographic else "Respect the reference/template layout strictly: preserve the contracted regions, spacing rhythm, text-safe negative space, and image-zone discipline before adding decorative detail.",
            f"Campaign theme: {message_theme}.",
            f"Emotional direction: {emotional_direction}.",
            *text_overlay_contract,
            data_list_surface_guidance,
            proof_point_render_instruction,
            (
                f"Infographic section plan to preserve: {infographic_sections}."
                if infographic_sections and format_name == "infographic"
                else ""
            ),
            (
                f"Static panel plan to preserve: {static_panel_spec}."
                if static_panel_spec and format_name == "static"
                else ""
            ),
            research_quality_section,
            *consultant_contract,
            ad_post_visual_quality_contract,
            *multimodal_balance_contract,
            *reference_family_contract,
            reference_adaptation_profile_section,
            generation_surface_contract_section,
            *reference_template_adaptation_sections,
            *visual_style_policy_sections,
            *sequence_alignment_sections,
            f"Brand palette to honor: {palette}.",
            f"Palette role guidance: {palette_guidance}.",
            strict_palette_contract,
            f"Typography direction: {typography}.",
            *brand_design_system_guidance_sections,
            *grounding_sections,
            "" if llm_led_static_infographic else f"Reference images available for composition: {reference_summary}.",
            (
                "Reference/template material is inspiration only for static/infographic. Do not preserve its geometry or scaffold; generate a new topic-correct design from the current prompt while only reserving the logo-safe corner and footer/disclaimer strip."
                if llm_led_static_infographic
                else "Current request subject matter overrides reference subject matter: treat reference/template assets as layout DNA only; preserve approved palette, spacing, layout rhythm, and visual craft, but replace sample headlines, body, labels, icons, illustrations, charts, objects, and scenes with topic-specific content and visuals from the current prompt; never import unrelated objects, industries, products, or scenes from a template."
            ),
            cohesive_visual_instruction,
            crop_crowding_instruction,
            export_frame_fit_instruction,
            "Avoid defaulting to a plain text poster when the requested format calls for a richer explanatory layout.",
            (
                "Template-active fallback policy: preserve the observed template scaffold as the primary composition. Any generated photo, illustration, or object scene may only occupy an approved visual slot; it must not replace module/card/panel/row/callout structure or reserved text-safe surfaces."
                if reference_template_structure_active
                else (
                    "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while rendering the main approved words directly in the image."
                    if not use_backend_text_overlay
                    else "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while leaving all words for backend overlay."
                )
            ),
            visual_plan_guidance,
            (
                "Use content-specific explanatory imagery only inside approved template image/module slots, while keeping the full layout skeleton visible."
                if reference_template_structure_active
                else "Prefer content-specific explanatory imagery: product or process metaphors, comparison setups, outcome-focused objects, or structured visual anchors directly implied by the copy."
            ),
            "Do not default to a standalone business portrait, generic investor, or unrelated editorial person. Use people only when their action clearly explains the message.",
            (
                "Keep template modules, blank text-safe shells, card/panel boundaries, section rhythm, and callout surfaces visible; do not collapse them into one oversized visual subject."
                if reference_template_structure_active
                else (
                "When the composition supports explanation, use refined iconography, diagram cues, comparison motifs, or modular visual anchors to make the piece informative rather than decorative. Avoid repetitive stock finance motifs such as rising bars with arrows unless they are semantically required."
                if iconography_supported
                else "Favor one clear content-led visual focal subject so the base does not collapse into a generic portrait, blank gradient, or empty color field."
                )
            ),
            (
                "Do not add bar-chart icons, rising-arrow symbols, dashboard tiles, comparison stickers, decorative finance mini-graphics, or a chart/graph/bar-and-arrow hero image unless the user explicitly asked for a chart, graph, table, timeline, diagram, or infographic data visualization."
                if not explicit_data_visual_request
                else "If a chart or diagram is explicitly requested, keep it literal, sparse, and directly tied to the supplied data instead of using generic symbolic finance icons."
            ),
            (
                "If the brand design system implies airy hierarchy or generous whitespace, preserve clean negative space instead of packing every proof point into the frame."
                if "airy" in str(design_system_guidance.get("hierarchy") or "").casefold() or "generous" in str(design_system_guidance.get("hierarchy") or "").casefold()
                else ""
            ),
            (
                "If the brand design system implies dense editorial comparison or data-story structure, allow a richer multi-block composition instead of collapsing into one oversized hero subject."
                if any(token in str(design_system_guidance.get("content_structure") or "").casefold() for token in ("comparison", "data", "benefit", "steps"))
                else ""
            ),
            (
                "Bias away from generic business-person imagery when the brand design system points toward diagram-led, icon-led, editorial, or abstract treatment."
                if any(token in str(design_system_guidance.get("image_treatment") or "").casefold() for token in ("diagram", "icon", "editorial", "abstract", "illustration"))
                else ""
            ),
            "" if llm_led_static_infographic and not scene_graph_has_text_regions else "Respect the reference/template layout strictly: place visual emphasis only where the selected layout reserves image space, preserve text-safe negative space, and keep the composition skeleton stable.",
            (
                "When sequence guidance is style-reference-only, preserve the sample's region proportions, spacing rhythm, negative space, and composition balance as closely as possible while rebuilding the artwork from scratch. Do not copy the literal sample surface, but do imitate its structural discipline."
                if template_surface_policy == "style_reference_only" and not llm_led_static_infographic
                else ""
            ),
            "Use one coherent composition, not a collage of unrelated stickers, icons, or panels.",
            (
                "Keep every future text-overlay surface clean, calm, high-contrast, and intentionally aligned with premium editorial spacing."
                if use_backend_text_overlay
                else "Keep every rendered text zone clean, calm, high-contrast, and intentionally aligned with premium editorial spacing."
            ),
            "Avoid generic clip-art, fake logos, placeholder UI, awkward overlaps, washed-out buttons, repeated poster templates, or low-contrast pale text on pale backgrounds.",
            final_text_safety_instruction,
        ]
        required_sections = [
            opening_instruction,
            *(
                [
                    static_infographic_backend_overlay_contract,
                    logo_safe_zone_guidance,
                    format_guidance,
                    social_media_ad_contract,
                    content_visual_explanation_contract,
                    ad_post_visual_quality_contract,
                    hallucination_control_contract,
                    compact_hallucination_guard,
                    *text_containment_contract,
                    final_text_safety_instruction,
                    main_text_authority_instruction,
                    retry_instruction,
                    *llm_led_text_contract,
                    *(text_overlay_contract if strict_static_final_text_contract else []),
                    canvas_fit_guidance,
                    generation_to_export_fit_guidance,
                    crop_crowding_instruction,
                    export_frame_fit_instruction,
                    (
                        "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while rendering the main approved words directly in the image."
                        if not use_backend_text_overlay
                        else "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while leaving all words for backend overlay."
                    ),
                    "Do not default to a standalone business portrait, generic investor, or unrelated editorial person. Use people only when their action clearly explains the message.",
                    "" if llm_led_static_infographic and not scene_graph_has_text_regions else "Respect the reference/template layout strictly: preserve the contracted regions, spacing rhythm, text-safe negative space, and image-zone discipline before adding decorative detail.",
                    infographic_footer_layout_contract,
                    f"Brand palette to honor: {palette}.",
                    f"Palette role guidance: {palette_guidance}.",
                    strict_palette_contract,
                ]
                if llm_led_static_infographic
                else []
            ),
            *(
                []
                if llm_led_static_infographic
                else [
                    f"Keep the {reserved_logo_area} area completely empty and visually clean. Do not place any headline, body copy, supporting text, proof point, CTA, icon, or visual element inside or immediately adjacent to this corner.",
                    "Do not typeset the brand name as a top-corner signature.",
                    *AIOrchestratorService._logo_non_generation_contract(
                        brand_name=brand_name,
                        reserved_logo_area=reserved_logo_area,
                    ),
                ]
            ),
            logo_surface_guidance,
            disclaimer_overlay_guidance,
            infographic_footer_layout_contract,
            *text_containment_contract,
            f"Platform: {platform}.",
            f"Format: {format_name}.",
            f"Output type: {file_type}.",
            llm_led_current_prompt_instruction,
            llm_led_topic_fit_contract,
            social_media_ad_contract,
            creative_topic_3d_visual_contract,
            quality_floor_contract,
            compact_hallucination_guard,
            format_guidance,
            content_format_guide_section,
            live_research_facts_section,
            *visual_style_policy_sections,
            *brand_design_system_guidance_sections,
            (
                "If the brand design system implies airy hierarchy or generous whitespace, preserve clean negative space instead of packing every proof point into the frame."
                if "airy" in str(design_system_guidance.get("hierarchy") or "").casefold() or "generous" in str(design_system_guidance.get("hierarchy") or "").casefold()
                else ""
            ),
            (
                "Bias away from generic business-person imagery when the brand design system points toward diagram-led, icon-led, editorial, or abstract treatment."
                if any(token in str(design_system_guidance.get("image_treatment") or "").casefold() for token in ("diagram", "icon", "editorial", "abstract", "illustration"))
                else ""
            ),
            reference_adaptation_profile_section,
            generation_surface_contract_section,
            canvas_fit_guidance,
            generation_to_export_fit_guidance,
            crop_crowding_instruction,
            export_frame_fit_instruction,
            "" if llm_led_static_infographic else f"Creative mode: {creative_decision.layout_mode}.",
            "" if llm_led_static_infographic else f"Layout archetype: {layout_archetype}.",
            main_text_authority_instruction,
            *reference_template_adaptation_sections,
            (
                "Template-active fallback policy: preserve the observed template scaffold as the primary composition. Any generated photo, illustration, or object scene may only occupy an approved visual slot; it must not replace module/card/panel/row/callout structure or reserved text-safe surfaces."
                if reference_template_structure_active
                else (
                    "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while rendering the main approved words directly in the image."
                    if not use_backend_text_overlay
                    else "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while leaving all words for backend overlay."
                )
            ),
            (
                "Reference/template material is inspiration only for static/infographic. Do not preserve its geometry or scaffold; generate a new topic-correct design from the current prompt while only reserving the logo-safe corner and footer/disclaimer strip."
                if llm_led_static_infographic
                else "Current request subject matter overrides reference subject matter: treat reference/template assets as layout DNA only; preserve approved palette, spacing, layout rhythm, and visual craft, but replace sample headlines, body, labels, icons, illustrations, charts, objects, and scenes with topic-specific content and visuals from the current prompt; never import unrelated objects, industries, products, or scenes from a template."
            ),
            "Do not default to a standalone business portrait, generic investor, or unrelated editorial person. Use people only when their action clearly explains the message.",
            (
                (
                    f"Scene-graph geometry contract JSON: {geometry_contract}. Preserve these normalized regions closely as empty overlay-safe surfaces; do not render lettering inside them and never recompose from scratch."
                    if use_backend_text_overlay
                    else f"Scene-graph geometry contract JSON: {geometry_contract}. Preserve these normalized regions closely; only make tiny adjustments for readability and never recompose from scratch."
                )
                if geometry_contract
                else ""
            ),
            (
                f"Template/layout DNA contract JSON: {layout_dna_contract}. Use this as the authoritative composition skeleton for region balance, hierarchy, and spacing."
                if layout_dna_contract
                else ""
            ),
            "" if llm_led_static_infographic and not scene_graph_has_text_regions else "Respect the reference/template layout strictly: preserve the contracted regions, spacing rhythm, text-safe negative space, and image-zone discipline before adding decorative detail.",
            *(text_overlay_contract if not llm_led_static_infographic else []),
            data_list_surface_guidance,
            proof_point_render_instruction,
            (
                f"Infographic section plan to preserve: {infographic_sections}."
                if infographic_sections and format_name == "infographic"
                else ""
            ),
            (
                f"Static panel plan to preserve: {static_panel_spec}."
                if static_panel_spec and format_name == "static"
                else ""
            ),
            research_quality_section,
            *consultant_contract,
            *multimodal_balance_contract,
            *reference_family_contract,
            reference_adaptation_profile_section,
            generation_surface_contract_section,
            f"Brand palette to honor: {palette}.",
            f"Palette role guidance: {palette_guidance}.",
            strict_palette_contract,
            f"Typography direction: {typography}.",
            *sequence_alignment_sections,
            cohesive_visual_instruction,
            crop_crowding_instruction,
            export_frame_fit_instruction,
            (
                "Template-active fallback policy: preserve the observed template scaffold as the primary composition. Any generated photo, illustration, or object scene may only occupy an approved visual slot; it must not replace module/card/panel/row/callout structure or reserved text-safe surfaces."
                if reference_template_structure_active
                else (
                    "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while rendering the main approved words directly in the image."
                    if not use_backend_text_overlay
                    else "Make the supporting visual explain the exact topic and benefit from the approved copy intent and user prompt, while leaving all words for backend overlay."
                )
            ),
            visual_plan_guidance,
            (
                "Keep template modules, blank text-safe shells, card/panel boundaries, section rhythm, and callout surfaces visible; do not collapse them into one oversized visual subject."
                if reference_template_structure_active
                else (
                "When the composition supports explanation, use refined iconography, diagram cues, comparison motifs, or modular visual anchors to make the piece informative rather than decorative. Avoid repetitive stock finance motifs such as rising bars with arrows unless they are semantically required."
                if iconography_supported
                else "Favor one clear content-led visual focal subject so the base does not collapse into a generic portrait, blank gradient, or empty color field."
                )
            ),
            (
                "Do not add bar-chart icons, rising-arrow symbols, dashboard tiles, comparison stickers, decorative finance mini-graphics, or a chart/graph/bar-and-arrow hero image unless the user explicitly asked for a chart, graph, table, timeline, diagram, or infographic data visualization."
                if not explicit_data_visual_request
                else "If a chart or diagram is explicitly requested, keep it literal, sparse, and directly tied to the supplied data instead of using generic symbolic finance icons."
            ),
            final_text_safety_instruction,
        ]
        prompt = AIOrchestratorService._compose_prompt_sections(
            required_sections=required_sections,
            optional_sections=sections,
            limit=AIOrchestratorService.IMAGE_PROMPT_MAX_LENGTH,
        )
        prompt = AIOrchestratorService._strip_blocked_compliance_phrases(prompt)
        prompt = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(prompt)
        # Returns the exact string sent to the image provider for one static/infographic canvas or one non-carousel final render.
        # The orchestrator stores prompt metrics in asset metadata so export/debug code can trace what produced the image.
        return AIOrchestratorService._trim_prompt(prompt, AIOrchestratorService.IMAGE_PROMPT_MAX_LENGTH)

    @classmethod
    def _collapse_carousel_segments(cls, segments: list[str], max_slides: int) -> list[str]:
        # Centralizes collapse carousel segments from segments and max slides for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        cleaned: list[str] = []
        seen: set[str] = set()
        for segment in segments:
            normalized = " ".join(str(segment or "").split()).strip()
            if not normalized:
                continue
            key = normalized.casefold()
            if key in seen:
                continue
            seen.add(key)
            cleaned.append(normalized)
        if not cleaned:
            return [""]
        if len(cleaned) <= max_slides:
            return cleaned
        collapsed = cleaned[: max_slides - 1]
        collapsed.append(" ".join(cleaned[max_slides - 1 :]).strip())
        return collapsed

    @classmethod
    def _carousel_semantic_tokens(cls, *values: Any) -> set[str]:
        # Centralizes carousel semantic tokens for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        combined = " ".join(
            cls._normalize_metadata_text(value, limit=260)
            for value in values
            if cls._normalize_metadata_text(value, limit=260)
        )
        if not combined:
            return set()
        cleaned = re.sub(r"[^a-z0-9\s]+", " ", combined.casefold())
        stopwords = set(cls.PROMPT_ECHO_STOPWORDS) | {
            "slide",
            "slides",
            "carousel",
            "cover",
            "hook",
            "intro",
            "detail",
            "closing",
            "close",
            "final",
            "point",
            "points",
            "section",
            "sections",
            "insight",
            "insights",
            "what",
            "why",
            "how",
            "this",
            "that",
            "these",
            "those",
            "with",
            "from",
            "into",
            "over",
            "under",
        }
        return {
            token
            for token in cleaned.split()
            if len(token) > 2 and token not in stopwords
        }

    @classmethod
    def _carousel_slides_semantically_overlap(
        cls,
        first: dict[str, Any],
        second: dict[str, Any],
    ) -> bool:
        # Centralizes carousel slides semantically overlap from first and second for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        first_headline = cls._normalize_metadata_text(first.get("headline"), limit=140)
        second_headline = cls._normalize_metadata_text(second.get("headline"), limit=140)
        first_support = cls._normalize_metadata_text(first.get("supporting_line"), limit=220)
        second_support = cls._normalize_metadata_text(second.get("supporting_line"), limit=220)
        if first_headline and first_headline.casefold() == second_headline.casefold():
            if not first_support or not second_support or first_support.casefold() == second_support.casefold():
                return True
        if first_support and first_support.casefold() == second_support.casefold():
            return True

        first_tokens = cls._carousel_semantic_tokens(
            first_headline,
            first_support,
            *(first.get("proof_points") or []),
        )
        second_tokens = cls._carousel_semantic_tokens(
            second_headline,
            second_support,
            *(second.get("proof_points") or []),
        )
        if not first_tokens or not second_tokens:
            return False
        overlap = len(first_tokens & second_tokens)
        if overlap == 0:
            return False
        jaccard = overlap / max(1, len(first_tokens | second_tokens))
        containment = overlap / max(1, min(len(first_tokens), len(second_tokens)))
        return jaccard >= 0.84 or containment >= 0.9

    @classmethod
    def _carousel_headline_subject(cls, headline: str) -> str:
        # Centralizes carousel headline subject from headline for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        text = cls._normalize_metadata_text(headline, limit=160)
        if not text:
            return ""
        patterns = (
            r"\binside the (?P<subject>[^?!.]+)",
            r"\bhow (?P<subject>.+?) could impact\b",
            r"\bhow (?P<subject>.+?) works\b",
            r"\bwhy (?P<subject>.+?) matters\b",
        )
        for pattern in patterns:
            match = re.search(pattern, text, re.IGNORECASE)
            if not match:
                continue
            subject = cls._normalize_metadata_text(match.group("subject"), limit=96)
            if subject:
                return subject
        stripped = re.sub(
            r"^(?:what(?:'s| is)?|why|how|beyond|balancing|inside)\s+",
            "",
            text,
            flags=re.IGNORECASE,
        ).strip(" ,.;:-")
        if stripped:
            words = stripped.split()
            return " ".join(words[:6]).strip(" ,.;:-")
        return ""

    @classmethod
    def _carousel_contextual_role_title(cls, role: str, *, headline: str, cta: str) -> str:
        # Centralizes carousel contextual role title from role, headline, and cta for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized_role = str(role or "").strip().casefold().replace(" ", "_")
        subject = cls._carousel_headline_subject(headline)
        if not subject:
            return ""
        if normalized_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return f"How {subject} works"
        if normalized_role in {"undercovered_angle", "missed_angle", "angle"}:
            return f"What {subject} reveals"
        if normalized_role in {"implications", "implication", "strategic_meaning", "analysis"}:
            return f"Why {subject} matters"
        if normalized_role in {"takeaway", "close", "closing", "cta", "final"}:
            return f"What {subject} means next"
        if normalized_role in {"solution_intro"}:
            return f"How {subject} helps"
        return ""

    @classmethod
    def _carousel_role_title(cls, role: str, *, headline: str, cta: str) -> str:
        # Centralizes carousel role title from role, headline, and cta for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_role = str(role or "").strip().casefold().replace(" ", "_")
        contextual_title = cls._carousel_contextual_role_title(normalized_role, headline=headline, cta=cta)
        if normalized_role in {"hook", "cover", "opening", "title"}:
            return headline or "Why this matters now"
        if normalized_role in {"intro", "context", "setup"}:
            return "What happened"
        if normalized_role in {"list_item"}:
            return "One item to understand"
        if normalized_role in {"comparison_item"}:
            return "How this option works"
        if normalized_role in {"problem_frame"}:
            return "The problem to solve"
        if normalized_role in {"solution_intro"}:
            return "How the solution responds"
        if normalized_role in {"feature_cluster"}:
            return "What this helps you see"
        if normalized_role in {"value_close"}:
            return contextual_title or "Why this matters in practice"
        if normalized_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return contextual_title or "What actually changed"
        if normalized_role in {"undercovered_angle", "missed_angle", "angle"}:
            return contextual_title or "What most coverage missed"
        if normalized_role in {"implications", "implication", "strategic_meaning", "analysis"}:
            return contextual_title or "Why it matters beyond the headline"
        if normalized_role in {"takeaway", "close", "closing", "cta", "final"}:
            return contextual_title or "What to do with this insight"
        return "What to know next"

    @classmethod
    def _clean_carousel_cta_text(
        cls,
        value: Any,
        *,
        headline: str = "",
        supporting_line: str = "",
    ) -> str:
        # Extracts clean carousel cta text from input value, headline, and supporting line for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=90).rstrip(" .")
        text = re.sub(r"\s*(?:\.{2,}|…)\s*$", "", text).strip()
        if not text:
            return ""
        words = text.split()
        lowered = text.casefold()
        if len(words) <= 10 and not re.search(r"\b(learn more|discover more)\b", lowered):
            return text
        combined = " ".join(
            cls._normalize_metadata_text(part, limit=180).casefold()
            for part in (text, headline, supporting_line)
            if cls._normalize_metadata_text(part, limit=180)
        )
        if "fixed" in combined and "income" in combined:
            return "Explore fixed-income options"
        if "bond" in combined:
            return "Explore bond options"
        if "invest" in combined or "portfolio" in combined:
            return "Explore investment options"
        if re.search(r"\b(read|guide|report|article|analysis)\b", combined):
            return "Read the full analysis"
        return "Explore the next step"

    @classmethod
    def _carousel_story_role(
        cls,
        role: Any,
        *,
        index: int,
        slide_count: int,
        has_cta: bool = False,
    ) -> str:
        # Centralizes carousel story role from role, index, and slide count for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized = cls._normalize_metadata_text(role, limit=40).casefold().replace(" ", "_")
        if normalized in {"hook", "cover", "opening", "title"}:
            return "hook" if index == 1 else "cover"
        if normalized in {"intro", "context", "setup"}:
            return "intro"
        if normalized in {"close", "closing", "cta", "final"}:
            return "closing"
        if index == 1 and slide_count >= 3:
            return "hook"
        if index == slide_count or has_cta:
            return "closing"
        return "detail"

    @classmethod
    def _carousel_story_role_token(cls, story_role: Any) -> str:
        # Extracts carousel story role token from story role for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        return cls._normalize_metadata_text(story_role, limit=48).casefold().replace(" ", "_")

    @staticmethod
    def _carousel_generic_story_roles() -> set[str]:
        # Centralizes carousel generic story roles for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        return {"", "detail", "details", "intro", "context", "setup"}

    @classmethod
    def _carousel_infer_archetype(
        cls,
        *,
        request: AIOrchestrationRequest | None,
        metadata: dict[str, Any],
    ) -> str:
        # Centralizes carousel infer archetype from request state and metadata for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if isinstance(metadata.get("carousel_archetype"), str) and str(metadata.get("carousel_archetype") or "").strip():
            return str(metadata.get("carousel_archetype") or "").strip().casefold()
        content_plan = getattr(request, "content_plan", {}) if request is not None else {}
        if isinstance(content_plan, dict):
            explicit = str(content_plan.get("carousel_archetype") or "").strip().casefold()
            if explicit:
                return explicit

        text_fragments: list[str] = []
        role_tokens: set[str] = set()
        for candidate in cls._carousel_explicit_outline_candidates(request, metadata):
            if not isinstance(candidate, dict):
                continue
            text_fragments.extend(
                [
                    str(candidate.get("title") or ""),
                    str(candidate.get("description") or ""),
                    str(candidate.get("role") or ""),
                    str(candidate.get("purpose") or ""),
                ]
            )
            role_token = cls._carousel_story_role_token(
                candidate.get("role") or candidate.get("slide_role") or candidate.get("purpose")
            )
            if role_token:
                role_tokens.add(role_token)
        if request is not None:
            text_fragments.append(str(getattr(request, "prompt", "") or ""))
            sequence_pack = cls._template_sequence_pack(request, creative_decision=None)
            if isinstance(sequence_pack, dict):
                text_fragments.extend(
                    [
                        str(sequence_pack.get("family_name") or ""),
                        str(sequence_pack.get("surface_policy") or ""),
                    ]
                )
                for slide in [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]:
                    text_fragments.extend(
                        [
                            str(slide.get("template_name") or ""),
                            str(slide.get("headline_hint") or ""),
                            str(slide.get("sequence_summary") or ""),
                            str(slide.get("story_role") or ""),
                        ]
                    )
                    role_token = cls._carousel_story_role_token(slide.get("story_role"))
                    if role_token:
                        role_tokens.add(role_token)
        if role_tokens & {"undercovered_angle", "missed_angle", "strategic_meaning", "what_happened", "deal_structure"}:
            return "editorial_reveal"
        if role_tokens & {"problem_frame", "solution_intro", "feature_cluster", "value_close"}:
            return "problem_solution_feature"
        if role_tokens & {"comparison_item"}:
            return "comparison_framework"
        if role_tokens & {"list_item"}:
            return "list_teaching"

        lowered = " ".join(fragment.casefold() for fragment in text_fragments if fragment).strip()
        if re.search(r"\b(bias|biases|mistake|mistakes|myth|myths|habit|habits|pitfall|pitfalls|behavioural|behavioral)\b", lowered):
            return "list_teaching"
        if re.search(r"\b(compare|comparison|versus|vs\.?|barbell|bullet|ladder|option|options|strategy|strategies)\b", lowered):
            return "comparison_framework"
        if re.search(r"\b(analyzer|tool|dashboard|feature|features|workflow|solution|problem|platform|decision)\b", lowered):
            return "problem_solution_feature"
        if re.search(r"\b(coverage|headline|undercovered|strategic|deal|implication|trade|missed|overlooked)\b", lowered):
            return "editorial_reveal"
        return ""

    @classmethod
    def _carousel_archetype_role_sequence(
        cls,
        archetype: str,
        *,
        slide_count: int,
    ) -> list[str]:
        # Centralizes carousel archetype role sequence from archetype and slide count for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized = str(archetype or "").strip().casefold()
        if slide_count <= 0:
            return []
        if normalized == "editorial_reveal":
            if slide_count == 1:
                return ["hook"]
            if slide_count == 2:
                return ["hook", "takeaway"]
            if slide_count == 3:
                return ["hook", "structure", "takeaway"]
            if slide_count == 4:
                return ["hook", "structure", "undercovered_angle", "strategic_meaning"]
            return ["hook", "structure", "undercovered_angle", "strategic_meaning"] + ["takeaway"] * (slide_count - 4)
        if normalized == "list_teaching":
            if slide_count == 1:
                return ["hook"]
            return ["hook"] + ["list_item"] * max(slide_count - 2, 0) + ["takeaway"]
        if normalized == "comparison_framework":
            if slide_count == 1:
                return ["hook"]
            return ["hook"] + ["comparison_item"] * max(slide_count - 2, 0) + ["takeaway"]
        if normalized == "problem_solution_feature":
            if slide_count == 1:
                return ["problem_frame"]
            if slide_count == 2:
                return ["problem_frame", "value_close"]
            if slide_count == 3:
                return ["problem_frame", "solution_intro", "value_close"]
            return ["problem_frame", "solution_intro"] + ["feature_cluster"] * max(slide_count - 3, 0) + ["value_close"]
        if normalized == "ordered_story":
            if slide_count == 1:
                return ["hook"]
            return ["hook"] + ["detail"] * max(slide_count - 2, 0) + ["takeaway"]
        return []

    @classmethod
    def _carousel_archetype_outline(
        cls,
        archetype: str,
        *,
        slide_count: int,
    ) -> list[dict[str, Any]]:
        # Centralizes carousel archetype outline from archetype and slide count for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        roles = cls._carousel_archetype_role_sequence(archetype, slide_count=slide_count)
        outline: list[dict[str, Any]] = []
        for role in roles:
            outline.append(
                {
                    "title": "",
                    "description": "",
                    "role": role,
                }
            )
        return outline

    @classmethod
    def _carousel_role_for_archetype(
        cls,
        explicit_role: Any,
        *,
        archetype: str,
        index: int,
        slide_count: int,
    ) -> str:
        # Centralizes carousel role archetype from explicit role, archetype, and index for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_explicit = cls._carousel_story_role_token(explicit_role)
        role_sequence = cls._carousel_archetype_role_sequence(archetype, slide_count=slide_count)
        archetype_role = role_sequence[index - 1] if index - 1 < len(role_sequence) else ""
        if archetype_role and normalized_explicit in cls._carousel_generic_story_roles():
            return archetype_role
        return normalized_explicit or archetype_role

    @classmethod
    def _carousel_body_point_budget(
        cls,
        story_role: Any,
        *,
        is_final_slide: bool = False,
    ) -> int:
        # Centralizes carousel body point budget from story role and is final slide for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized = cls._carousel_story_role_token(story_role)
        if normalized in {"hook", "cover", "opening", "title"}:
            return 0
        if normalized in {"takeaway", "close", "closing", "cta", "final", "value_close"} or is_final_slide:
            return 2
        if normalized in {"implications", "implication", "strategic_meaning", "analysis", "what_matters"}:
            return 2
        if normalized in {"list_item", "comparison_item", "feature_cluster"}:
            return 3
        if normalized in {"problem_frame", "solution_intro", "undercovered_angle", "missed_angle", "angle", "structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return 2
        return 2

    @classmethod
    def _carousel_role_point_budget(
        cls,
        story_role: Any,
        *,
        is_final_slide: bool = False,
    ) -> int:
        # Centralizes carousel role point budget from story role and is final slide for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        normalized = cls._carousel_story_role_token(story_role)
        if normalized in {"hook", "cover", "opening", "title"}:
            return 0
        if normalized in {"takeaway", "close", "closing", "cta", "final"} or is_final_slide:
            return 0
        if normalized in {"value_close"}:
            return 0
        if normalized in {"implications", "implication", "strategic_meaning", "analysis", "what_matters"}:
            return 0
        if normalized in {"list_item", "comparison_item", "problem_frame", "solution_intro"}:
            return 1
        if normalized in {"feature_cluster"}:
            return 2
        if normalized in {"undercovered_angle", "missed_angle", "angle"}:
            return 2
        if normalized in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return 3
        if normalized in {"intro", "context", "setup"}:
            return 1
        return 2

    @classmethod
    def _carousel_support_score(
        cls,
        story_role: Any,
        text: Any,
    ) -> int:
        # Centralizes carousel support from story role and text for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized_role = cls._carousel_story_role_token(story_role)
        lowered = cls._normalize_metadata_text(text, limit=220).casefold()
        if not lowered:
            return 0
        score = 0
        if normalized_role in {"hook", "cover", "opening", "title"} and re.search(
            r"missed|overlook|headline|really means|what .*dont tell|what .*don't tell|beyond the headline",
            lowered,
        ):
            score += 4
        if normalized_role in {"hook", "cover", "opening", "title"} and re.search(
            r"mobility|services|tariff|duty|70%|100%|signal|strategic|alignment|clause|condition",
            lowered,
        ):
            score -= 3
        if normalized_role in {"hook", "cover", "opening", "title"} and re.search(
            r"not just|beyond|really means|what the headline",
            lowered,
        ):
            score += 2
        if normalized_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"} and re.search(
            r"tariff|duty|access|open|protected|trade volume|what changed|deal terms|phased",
            lowered,
        ):
            score += 5
        if normalized_role in {"list_item"} and re.search(
            r"bias|mistake|myth|habit|example|consequence|trap|tendency",
            lowered,
        ):
            score += 5
        if normalized_role in {"comparison_item"} and re.search(
            r"compare|versus|vs\.?|barbell|bullet|ladder|duration|yield|fit|works",
            lowered,
        ):
            score += 5
        if normalized_role in {"problem_frame"} and re.search(
            r"problem|pain|friction|manual|messy|hard to|blind spot|inefficient",
            lowered,
        ):
            score += 5
        if normalized_role in {"solution_intro"} and re.search(
            r"solution|responds|helps|simplifies|brings together|introduces",
            lowered,
        ):
            score += 5
        if normalized_role in {"feature_cluster"} and re.search(
            r"feature|screen|dashboard|analyzer|monitor|track|filter|compare|workflow",
            lowered,
        ):
            score += 5
        if normalized_role in {"value_close"} and re.search(
            r"value|decision|confidence|clarity|why it matters|what you gain",
            lowered,
        ):
            score += 5
        if normalized_role in {"undercovered_angle", "missed_angle", "angle"} and re.search(
            r"missed|overlook|hidden|mobility|services|clause|condition|string|accountability|rare",
            lowered,
        ):
            score += 5
        if normalized_role in {"implications", "implication", "strategic_meaning", "analysis", "what_matters"} and re.search(
            r"signal|strategic|alignment|template|future|gateway|position|bigger|shape|why it matters",
            lowered,
        ):
            score += 5
        if normalized_role in {"takeaway", "close", "closing", "cta", "final"} and re.search(
            r"watch|next|what to do|read the full|explore|learn more",
            lowered,
        ):
            score += 4
        return score

    @classmethod
    def _carousel_bullet_lines(cls, value: Any, *, limit: int = 4) -> list[str]:
        # Extracts carousel bullet lines from input value and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text and _normalize_metadata_list to turn raw evidence into the structured signal the caller needs.
        if isinstance(value, list):
            return cls._normalize_metadata_list(value, limit=limit)
        text = cls._normalize_metadata_text(value, limit=600)
        if not text:
            return []
        fragments = re.split(r"(?:\r?\n|[;•]| - )+", text)
        lines: list[str] = []
        for fragment in fragments:
            normalized = cls._normalize_metadata_text(fragment.strip(" -*"), limit=180)
            if normalized:
                lines.append(normalized)
        if not lines:
            lines = cls._sentences(text)
        return cls._normalize_metadata_list(lines, limit=limit)

    @staticmethod
    def _looks_like_carousel_instruction_text(value: Any) -> bool:
        # Checks carousel instruction text from input value for AIOrchestratorService.
        # It reuses _coerce_text_value so related checks follow the same rule.
        text = AIOrchestratorService._coerce_text_value(value).casefold()
        if not text:
            return False
        instruction_markers = (
            "sample cue",
            "user-requested",
            "follow the ",
            "open with a strong hook",
            "close with the final takeaway",
            "explain the next core idea",
            "let each interior slide",
            "preserve the user-requested",
            "dedicate one core story beat",
        )
        return any(marker in text for marker in instruction_markers)

    @classmethod
    def _carousel_explicit_outline_candidates(
        cls,
        request: AIOrchestrationRequest | None,
        metadata: dict[str, Any],
    ) -> list[dict[str, Any]]:
        # Centralizes carousel explicit outline candidates from request state and metadata for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        candidate_lists: list[Any] = [
            metadata.get("carousel_outline"),
            metadata.get("outline"),
        ]
        if request is not None:
            template_context = request.template_context if isinstance(request.template_context, dict) else {}
            sequence_pack = template_context.get("sequence_pack") if isinstance(template_context, dict) else {}
            sequence_outline = []
            if isinstance(sequence_pack, dict):
                for slide in [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]:
                    headline_hint = str(
                        slide.get("headline_hint")
                        or slide.get("slide_title")
                        or slide.get("page_title")
                        or ""
                    ).strip()
                    sequence_summary = str(slide.get("sequence_summary") or "").strip()
                    story_role = str(slide.get("story_role") or "").strip()
                    if not (headline_hint or sequence_summary or story_role):
                        continue
                    sequence_outline.append(
                        {
                            "title": headline_hint,
                            "description": sequence_summary,
                            "role": story_role,
                        }
                    )
            candidate_lists.extend(
                [
                    getattr(request, "content_plan", {}).get("slides") if isinstance(getattr(request, "content_plan", {}), dict) else None,
                    getattr(request, "content_plan", {}).get("story_outline") if isinstance(getattr(request, "content_plan", {}), dict) else None,
                    request.research_editorial_brief.get("outline") if isinstance(request.research_editorial_brief, dict) else None,
                    sequence_outline,
                ]
            )
        for candidate in candidate_lists:
            if not isinstance(candidate, list):
                continue
            normalized_items = [item for item in candidate if isinstance(item, (dict, str)) and item]
            if normalized_items:
                return [dict(item) if isinstance(item, dict) else {"title": str(item)} for item in normalized_items]
        return []

    @classmethod
    def _carousel_outline_candidates(
        cls,
        request: AIOrchestrationRequest | None,
        metadata: dict[str, Any],
    ) -> list[dict[str, Any]]:
        # Centralizes carousel outline candidates from request state and metadata for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        explicit_outline = cls._carousel_explicit_outline_candidates(request, metadata)
        if explicit_outline:
            return explicit_outline
        carousel_archetype = cls._carousel_infer_archetype(request=request, metadata=metadata)
        if carousel_archetype:
            sequence_count = 0
            if request is not None:
                sequence_count = cls._explicit_requested_slide_count(request) or 0
                content_plan = getattr(request, "content_plan", {})
                if isinstance(content_plan, dict):
                    sequence_count = sequence_count or cls._int_or_none(content_plan.get("preferred_slide_count")) or 0
                sequence_pack = cls._template_sequence_pack(request, creative_decision=None)
                if isinstance(sequence_pack, dict):
                    sequence_count = sequence_count or cls._int_or_none(sequence_pack.get("slide_count")) or 0
                if isinstance(request.research_editorial_brief, dict):
                    sequence_count = sequence_count or cls._int_or_none(request.research_editorial_brief.get("preferred_slide_count")) or 0
            sequence_count = sequence_count or cls._int_or_none(metadata.get("preferred_slide_count")) or 0
            sequence_count = sequence_count or 5
            return cls._carousel_archetype_outline(carousel_archetype, slide_count=sequence_count)
        return []

    @staticmethod
    def _metadata_text_has_truncation_marker(value: Any) -> bool:
        # Extracts metadata text truncation marker from input value for AIOrchestratorService.
        # It calls _coerce_text_value to turn raw evidence into the structured signal the caller needs.
        text = AIOrchestratorService._coerce_text_value(value)
        return "..." in text or "…" in text

    @classmethod
    def _metadata_text_without_truncation_marker(cls, value: Any, *, limit: int) -> str:
        # Extracts metadata text without truncation marker from input value and limit for AIOrchestratorService.
        # It calls _normalize_metadata_text and _metadata_text_has_truncation_marker to turn raw evidence into the structured signal the caller needs.
        text = cls._normalize_metadata_text(value, limit=limit)
        if not cls._metadata_text_has_truncation_marker(text):
            return text
        cleaned = re.split(r"(?:\.{3}|…)", text, maxsplit=1)[0].strip(" ,.;:-")
        return cleaned if len(cleaned.split()) >= 3 else ""

    @classmethod
    def _carousel_texts_semantically_overlap(cls, first: Any, second: Any) -> bool:
        # Centralizes carousel texts semantically overlap from first and second for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        first_tokens = cls._carousel_semantic_tokens(first)
        second_tokens = cls._carousel_semantic_tokens(second)
        if not first_tokens or not second_tokens:
            return False
        overlap = len(first_tokens & second_tokens)
        if overlap == 0:
            return False
        containment = overlap / max(1, min(len(first_tokens), len(second_tokens)))
        return containment >= 0.82

    @classmethod
    def _sanitize_carousel_slide_specs(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None = None,
    ) -> list[dict[str, Any]]:
        # Sanitizes carousel slide specs from slides and request state for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_text and _brand_subject_focus_terms before returning the cleaned value.
        sanitized: list[dict[str, Any]] = []
        prompt_topic = cls._normalize_metadata_text((request.prompt if request is not None else ""), limit=180)
        request_topic_tokens = cls._request_topic_tokens(request) if request is not None else set()
        topic_token_candidates = request_topic_tokens or cls._topic_anchor_keywords(prompt_topic)
        if not topic_token_candidates and prompt_topic:
            topic_token_candidates = {
                token
                for token in cls._normalized_prompt_tokens(prompt_topic)
                if len(token) >= 3
                and token not in cls.TOPIC_MATCH_NOISE_TOKENS
                and not any(ch.isdigit() for ch in token)
                and not re.fullmatch(r"[a-f0-9]{8,}", token)
            }
        topic_keywords = ", ".join(sorted(topic_token_candidates)[:8])
        brand_subject_terms = cls._brand_subject_focus_terms(request)
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            cleaned = dict(slide)
            for key, limit in (
                ("headline", 120),
                ("supporting_line", 220),
                ("body", 320),
                ("cta", 90),
                ("transition_note", 160),
            ):
                if key in cleaned:
                    cleaned[key] = cls._metadata_text_without_truncation_marker(cleaned.get(key), limit=limit)

            body_text = cls._normalize_metadata_text(cleaned.get("body"), limit=320)
            support_text = cls._normalize_metadata_text(cleaned.get("supporting_line"), limit=220)
            semantic_anchors = [body_text, support_text]
            slide_metadata = cleaned.get("metadata") if isinstance(cleaned.get("metadata"), dict) else {}
            story_role_token = cls._normalize_metadata_text(
                slide_metadata.get("story_role") or cleaned.get("role") or cleaned.get("slide_role"),
                limit=48,
            ).casefold().replace(" ", "_")
            for key, item_limit in (("proof_points", 180), ("body_points", 160), ("stat_highlights", 120)):
                values = []
                for item in cleaned.get(key) or []:
                    item_text = cls._metadata_text_without_truncation_marker(item, limit=item_limit)
                    if not item_text:
                        continue
                    is_claim_evidence_line = key == "proof_points" and ":" in item_text
                    is_archetype_body_point = key == "body_points" and story_role_token in {
                        "list_item",
                        "comparison_item",
                        "problem_frame",
                        "solution_intro",
                        "feature_cluster",
                        "value_close",
                    }
                    if (
                        not is_claim_evidence_line
                        and not is_archetype_body_point
                        and any(cls._carousel_texts_semantically_overlap(item_text, anchor) for anchor in semantic_anchors if anchor)
                    ):
                        continue
                    if any(cls._carousel_texts_semantically_overlap(item_text, existing) for existing in values):
                        continue
                    values.append(item_text)
                cleaned[key] = values

            pairs = []
            for pair in cleaned.get("claim_evidence_pairs") or []:
                if not isinstance(pair, dict):
                    continue
                claim = cls._metadata_text_without_truncation_marker(pair.get("claim"), limit=120)
                evidence = cls._metadata_text_without_truncation_marker(pair.get("evidence"), limit=180)
                if claim and evidence:
                    pairs.append({"claim": claim, "evidence": evidence})
            if "claim_evidence_pairs" in cleaned:
                cleaned["claim_evidence_pairs"] = pairs

            raw_visual_focus = cleaned.get("visual_focus")
            visual_focus_from_reference = isinstance(raw_visual_focus, dict)
            if isinstance(raw_visual_focus, dict):
                visual_focus = cls._normalize_metadata_text(
                    raw_visual_focus.get("description")
                    or raw_visual_focus.get("visual_focus")
                    or raw_visual_focus.get("text")
                    or raw_visual_focus.get("label"),
                    limit=180,
                )
            else:
                visual_focus = cls._normalize_metadata_text(raw_visual_focus, limit=180)
            lowered_focus = visual_focus.casefold()
            generic_visual_focus = bool(
                lowered_focus
                and any(
                    token in lowered_focus
                    for token in (
                        "conceptual map",
                        "formal signing",
                        "signing ceremony",
                        "diplomatic handshake",
                        "handshake",
                        "professional photo or illustration",
                        "symbolizing international trade",
                        "trade icons",
                        "icons representing",
                        "vector icons showing",
                        "scale icon",
                        "analytics visual",
                        "clean, inviting graphic",
                        "infographic showing",
                        "platform interface",
                        "bond investment visual",
                        "curated investment opportunities",
                        "visual highlighting",
                        "reassurance symbols",
                        "shields or checkmarks",
                        "platform interface",
                        "conceptual infographic",
                        "showing india",
                        "showing trade links",
                        "reference_image",
                        "storage_path",
                    )
                )
            )
            if (
                not visual_focus
                or lowered_focus in {"unknown", "none", "n/a"}
                or '"storage_path": "unknown"' in lowered_focus
                or "'storage_path': 'unknown'" in lowered_focus
                or generic_visual_focus
                or visual_focus_from_reference
            ):
                story_role = cls._normalize_metadata_text(
                    (
                        (cleaned.get("metadata") or {}).get("story_role")
                        if isinstance(cleaned.get("metadata"), dict)
                        else ""
                    )
                    or cleaned.get("role")
                    or cleaned.get("slide_role"),
                    limit=48,
                )
                evidence_cues = cls._normalize_metadata_list(
                    [
                        *(cleaned.get("proof_points") or []),
                        *(cleaned.get("body_points") or []),
                        *(cleaned.get("stat_highlights") or []),
                    ],
                    limit=3,
                )
                semantic_subjects = cls._normalize_metadata_list(
                    [
                        cleaned.get("headline"),
                        cleaned.get("supporting_line"),
                        *(cleaned.get("body_points") or []),
                        *(cleaned.get("proof_points") or []),
                    ],
                    limit=3,
                )
                role_focus_line = {
                    "hook": "Build a bold opening metaphor tied directly to the story and spread the hero across the lower or side canvas instead of reducing it to a tiny badge cluster.",
                    "cover": "Build a bold opening metaphor tied directly to the story and spread the hero across the lower or side canvas instead of reducing it to a tiny badge cluster.",
                    "opening": "Build a bold opening metaphor tied directly to the story and spread the hero across the lower or side canvas instead of reducing it to a tiny badge cluster.",
                    "structure": "Show the mechanism explicitly with one analytical centerpiece, segmented comparison, or process-led composition rather than a decorative symbol.",
                    "undercovered_angle": "Reveal the overlooked layer with evidence-led objects, sector cues, document fragments, or hidden-detail metaphors instead of generic cooperation symbols.",
                    "strategic_meaning": "Show systems, linkages, networks, or second-order outcomes so the visual explains why the fact matters beyond the event itself.",
                    "takeaway": "Use a concrete decision-support or product-context surface that makes the next action feel informed and credible rather than ceremonial or abstract.",
                    "closing": "Use a concrete decision-support or product-context surface that makes the next action feel informed and credible rather than ceremonial or abstract.",
                }.get(story_role.casefold(), "Choose one strong explanatory composition that advances the story instead of repeating the previous slide's visual formula.")
                focus_parts = [
                    f"Visual brief for this slide: {cleaned.get('headline')}",
                    (
                        "visual evidence cues: "
                        + "; ".join(evidence_cues)
                        if evidence_cues
                        else ""
                    ),
                    role_focus_line,
                    f"primary concepts: {'; '.join(semantic_subjects)}" if semantic_subjects else "",
                    f"story role: {story_role}" if story_role else "",
                    (
                        "brand subject cues: "
                        + ", ".join(brand_subject_terms)
                        if brand_subject_terms
                        else ""
                    ),
                    f"supporting idea: {cleaned.get('supporting_line')}" if cleaned.get("supporting_line") else "",
                    f"topic anchors: {topic_keywords}" if topic_keywords else "",
                    cls._story_role_visual_execution_guidance(story_role),
                    "avoid importing objects or labels from unrelated reference templates",
                ]
                cleaned["visual_focus"] = cls._normalize_metadata_text(
                    ". ".join(part for part in focus_parts if part),
                    limit=420,
                )
            else:
                cleaned["visual_focus"] = visual_focus
            sanitized.append(cleaned)
        return sanitized

    @classmethod
    def _apply_sequence_pack_slide_metadata(
        cls,
        slides: list[dict[str, Any]],
        *,
        sequence_pack_slides: list[dict[str, Any]],
        carousel_archetype: str = "",
        include_sample_copy_authority: bool = True,
    ) -> list[dict[str, Any]]:
        # Applies apply sequence pack slide metadata from slides, sequence pack slides, and carousel archetype for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not slides or not sequence_pack_slides:
            return [dict(slide) for slide in slides if isinstance(slide, dict)]
        enriched: list[dict[str, Any]] = []
        reference_slide_count = len(sequence_pack_slides)
        output_slide_count = len([slide for slide in slides if isinstance(slide, dict)])
        for index, slide in enumerate(slides):
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            slide_metadata = slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {}
            pack_reference_index = cls._proportional_sequence_reference_index(
                index + 1,
                output_slide_count=output_slide_count,
                reference_slide_count=reference_slide_count,
            )
            if 1 <= pack_reference_index <= reference_slide_count:
                pack_slide = dict(sequence_pack_slides[pack_reference_index - 1] or {})
                reference_slide_index = int(pack_slide.get("slide_index") or pack_reference_index)
                slide_metadata = {
                    **slide_metadata,
                    "reference_slide_index": reference_slide_index,
                    "reference_slide_count": reference_slide_count,
                    "reference_template_id": str(pack_slide.get("template_id") or ""),
                    "reference_template_name": str(pack_slide.get("template_name") or ""),
                    "reference_template_asset_path": str(pack_slide.get("template_asset_path") or ""),
                    "reference_layout_asset_path": str(
                        pack_slide.get("template_asset_path")
                        or pack_slide.get("reference_asset_path")
                        or ""
                    ),
                    "reference_asset_path": str(pack_slide.get("reference_asset_path") or ""),
                    "reference_zone_map": pack_slide.get("zone_map"),
                    "reference_editable_fields": list(pack_slide.get("editable_fields") or []),
                    "reference_structural_cues": list(pack_slide.get("structural_cues") or []),
                    "reference_sequence_summary": str(pack_slide.get("sequence_summary") or ""),
                    "reference_headline_hint": str(pack_slide.get("headline_hint") or ""),
                    "sample_page_editorial_role": str(pack_slide.get("sample_page_editorial_role") or ""),
                    "sample_page_copy_behavior": str(pack_slide.get("sample_page_copy_behavior") or ""),
                    "sample_page_copy_density": str(pack_slide.get("sample_page_copy_density") or ""),
                    "sample_page_closing_grammar": str(pack_slide.get("sample_page_closing_grammar") or ""),
                    "sample_page_has_question_hook": bool(pack_slide.get("sample_page_has_question_hook")),
                    "sample_page_has_source_labels": bool(pack_slide.get("sample_page_has_source_labels")),
                    "reference_composition_logic": pack_slide.get("composition_logic"),
                    "reference_visual_craft": pack_slide.get("visual_craft"),
                    "reference_subject_semantics": pack_slide.get("subject_semantics"),
                    "reference_editorial_dna": pack_slide.get("editorial_dna"),
                    "logo_position": str(pack_slide.get("logo_position") or "").strip() or None,
                    "conditioning_page_index": int(
                        pack_slide.get("conditioning_page_index")
                        or pack_slide.get("reference_page_index")
                        or pack_slide.get("page_index")
                        or pack_slide.get("page_number")
                        or reference_slide_index
                    ),
                }
                pack_blueprint = pack_slide.get("sample_page_blueprint")
                if isinstance(pack_blueprint, dict) and pack_blueprint:
                    slide_metadata["sample_page_blueprint"] = pack_blueprint
                if include_sample_copy_authority:
                    slide_metadata.update(
                        {
                            "sample_page_headline": str(pack_slide.get("sample_page_headline") or ""),
                            "sample_page_supporting": str(pack_slide.get("sample_page_supporting") or ""),
                            "sample_page_copy": str(pack_slide.get("sample_page_copy") or ""),
                        }
                    )
            slide_metadata["carousel_archetype"] = slide_metadata.get("carousel_archetype") or carousel_archetype
            slide_copy["metadata"] = slide_metadata
            enriched.append(slide_copy)
        return enriched

    @classmethod
    def _slide_sample_editorial_contract(cls, slide: dict[str, Any]) -> dict[str, Any]:
        # Builds slide sample editorial contract from slide for AIOrchestratorService.
        # The returned payload is the compact contract consumed by the next branch.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        return {
            "sample_page_headline": metadata.get("sample_page_headline"),
            "sample_page_supporting": metadata.get("sample_page_supporting"),
            "sample_page_copy": metadata.get("sample_page_copy"),
            "sample_page_editorial_role": metadata.get("sample_page_editorial_role"),
            "sample_page_copy_behavior": metadata.get("sample_page_copy_behavior"),
            "sample_page_copy_density": metadata.get("sample_page_copy_density"),
            "sample_page_closing_grammar": metadata.get("sample_page_closing_grammar"),
            "sample_page_has_question_hook": metadata.get("sample_page_has_question_hook"),
            "sample_page_has_source_labels": metadata.get("sample_page_has_source_labels"),
        }

    @classmethod
    def _sample_page_visualizes_data_or_product(cls, slide: dict[str, Any]) -> bool:
        # Centralizes sample page visualizes product from slide for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        blueprint = metadata.get("sample_page_blueprint") if isinstance(metadata.get("sample_page_blueprint"), dict) else {}
        category = cls._normalize_metadata_text(
            blueprint.get("layout_category") or blueprint.get("layout_mode"),
            limit=64,
        ).casefold()
        if category in {
            "data_dashboard",
            "dashboard_or_product_surface",
            "product_surface",
            "chart_or_graph",
            "table_or_matrix",
        }:
            return True
        sample_text = " ".join(
            cls._coerce_text_value(metadata.get(key))
            for key in (
                "sample_page_headline",
                "sample_page_supporting",
                "sample_page_copy",
                "reference_sequence_summary",
            )
        ).casefold()
        return bool(
            re.search(
                r"\b(chart|graph|dashboard|table|matrix|screen|interface|terminal|portfolio|product)\b",
                sample_text,
            )
        )

    @classmethod
    def _style_reference_visual_focus_needs_sample_reset(
        cls,
        visual_focus: Any,
        *,
        request: AIOrchestrationRequest | None = None,
        sample_allows_data_or_product: bool,
    ) -> bool:
        # Derives style reference visual focus needs sample reset from visual focus, request state, and sample allows data or product for AIOrchestrator.
        # It calls _data_visualization_requested and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        focus = cls._normalize_metadata_text(visual_focus, limit=520).casefold()
        if not focus:
            return True
        if "follow selected sample page" in focus and "visual authority" in focus:
            return False
        if sample_allows_data_or_product:
            return False
        if cls._data_visualization_requested(request.prompt if request is not None else ""):
            return False
        return bool(
            re.search(
                r"\b("
                r"chart|charts|graph|graphs|dashboard|table|matrix|axis|axes|"
                r"bar chart|line chart|pie chart|analytics|metric dashboard|"
                r"laptop|screen|interface|terminal|trading|portfolio|investment platform|"
                r"product|product surface|growth arrow|generic infographic"
                r")\b",
                focus,
            )
        )

    @classmethod
    def _sample_matched_visual_focus(
        cls,
        slide: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None = None,
    ) -> str:
        # Centralizes sample matched visual focus from slide and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        sample_contract = cls._slide_sample_editorial_contract(slide)
        role = cls._normalize_metadata_text(
            sample_contract.get("sample_page_editorial_role")
            or metadata.get("story_role")
            or slide.get("role"),
            limit=48,
        ).casefold().replace(" ", "_")
        behavior = cls._normalize_metadata_text(
            sample_contract.get("sample_page_copy_behavior"),
            limit=48,
        ).casefold().replace(" ", "_")
        density = cls._normalize_metadata_text(
            sample_contract.get("sample_page_copy_density"),
            limit=32,
        )
        reference_index = cls._int_or_none(metadata.get("reference_slide_index")) or cls._int_or_none(slide.get("slide_index"))
        headline = cls._normalize_metadata_text(slide.get("headline"), limit=120)
        supporting_line = cls._normalize_metadata_text(slide.get("supporting_line"), limit=180)
        module_lines = cls._carousel_slide_visible_module_lines(slide, limit=5)
        sample_allows_data_or_product = cls._sample_page_visualizes_data_or_product(slide)
        if role in {"hook", "cover", "opening"}:
            execution = (
                "use the selected sample page's opening hero grammar, image scale, negative space, "
                "brand/logo reservation, and headline tension"
            )
        elif behavior in {"deal_mechanics", "mechanism"} or role in {"structure", "mechanism"}:
            execution = (
                "use the selected sample page's mechanism grammar: the same row/card/module count, "
                "specific clauses or sectors as separate scanable units, and no repeated filler"
            )
        elif behavior in {"curiosity_gap", "undercovered_angle"} or role in {"undercovered_angle", "analysis"}:
            execution = (
                "use the selected sample page's reveal grammar: hidden implications, non-obvious clauses, "
                "and analytical callouts instead of an obvious summary"
            )
        elif behavior in {"strategic_signal", "curiosity_to_strategic_signal"} or role in {
            "takeaway",
            "strategic_meaning",
            "macro_takeaway",
            "closing",
        }:
            execution = (
                "use the selected sample page's macro-takeaway grammar: wider strategic consequence, "
                "future signal, and subtle brand presence without platform promotion"
            )
        else:
            execution = "translate the approved topic into the selected sample page's exact visual grammar"
        avoid = (
            ""
            if sample_allows_data_or_product
            else " Avoid laptop, dashboard, chart, table, trading-screen, and product-surface imagery unless the selected sample page visibly uses it."
        )
        topic = cls._carousel_topic_label(request) if request is not None else ""
        parts = [
            f"Follow selected sample page {reference_index} as visual authority" if reference_index else "Follow the selected sample page as visual authority",
            f"editorial behavior: {behavior}" if behavior else "",
            f"copy density: {density}" if density else "",
            execution,
            f"approved headline: {headline}" if headline else "",
            f"supporting idea: {supporting_line}" if supporting_line else "",
            f"module content: {'; '.join(module_lines)}" if module_lines else "",
            f"topic: {topic}" if topic else "",
            avoid,
        ]
        return cls._normalize_metadata_text(". ".join(part for part in parts if part), limit=520)

    @classmethod
    def _sanitize_style_reference_sample_slide_specs(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None = None,
    ) -> list[dict[str, Any]]:
        # Sanitizes style reference sample slide specs from slides and request state for AIOrchestratorService.
        # It delegates shared cleanup to _slide_sample_editorial_contract and _sample_page_visualizes_data_or_product before returning the cleaned value.
        if not slides:
            return []
        sanitized: list[dict[str, Any]] = []
        slide_count = len([slide for slide in slides if isinstance(slide, dict)])
        for index, slide in enumerate(slides, start=1):
            if not isinstance(slide, dict):
                continue
            cleaned = dict(slide)
            sample_contract = cls._slide_sample_editorial_contract(cleaned)
            if index < slide_count and cls._normalize_metadata_text(cleaned.get("cta"), limit=90):
                cleaned["cta"] = ""
            if index == slide_count and sample_contract and not cls._sample_contract_allows_product_cta(sample_contract):
                cleaned["cta"] = ""
                if cls._is_product_cta_like_line(cleaned.get("headline"), request=request):
                    cleaned["headline"] = cls._topic_specific_headline_from_sample_behavior(
                        topic=cls._carousel_topic_label(request) if request is not None else "this story",
                        role=str(cleaned.get("role") or "closing"),
                        sample_contract=sample_contract,
                    )
            sample_allows_data_or_product = cls._sample_page_visualizes_data_or_product(cleaned)
            if cls._style_reference_visual_focus_needs_sample_reset(
                cleaned.get("visual_focus"),
                request=request,
                sample_allows_data_or_product=sample_allows_data_or_product,
            ):
                cleaned["visual_focus"] = cls._sample_matched_visual_focus(
                    cleaned,
                    request=request,
                )
            sanitized.append(cleaned)
        return sanitized

    @classmethod
    def _align_carousel_slide_specs_to_selected_references(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None = None,
        creative_decision: CreativeDecisionPayload | None = None,
    ) -> list[dict[str, Any]]:
        # Aligns align carousel slide specs selected references from slides, request state, and creative decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if request is None or not slides:
            return [dict(slide) for slide in slides if isinstance(slide, dict)]
        if str(request.studio_panel.get("format") or "").strip().lower() != "carousel":
            return [dict(slide) for slide in slides if isinstance(slide, dict)]
        decision = creative_decision or CreativeDecisionPayload()
        selected_reference_images = cls._select_reference_image_assets(
            request=request,
            creative_decision=decision,
        )
        aligned: list[dict[str, Any]] = []
        for position, slide in enumerate(slides, start=1):
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            slide_copy["slide_index"] = int(slide_copy.get("slide_index") or position)
            slide_metadata = slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {}
            template_surface_policy = cls._effective_template_surface_policy(
                request,
                creative_decision=decision,
                metadata=slide_metadata,
            )
            existing_layout_path = str(
                slide_metadata.get("reference_layout_asset_path")
                or slide_metadata.get("reference_template_asset_path")
                or slide_metadata.get("reference_asset_path")
                or ""
            ).strip()
            routed_assets = cls._slide_reference_images(
                slide_copy,
                selected_reference_images,
                request=request,
                creative_decision=decision,
            )
            authoritative_asset = dict(routed_assets[0]) if routed_assets else None
            if authoritative_asset is None:
                fallback_path = str(
                    slide_metadata.get("reference_asset_path")
                    or slide_metadata.get("reference_template_asset_path")
                    or ""
                ).strip()
                if fallback_path:
                    matched = cls._reference_asset_by_storage_path(request, fallback_path)
                    if matched is not None:
                        authoritative_asset = dict(matched)
            if authoritative_asset is not None:
                asset_metadata = authoritative_asset.get("metadata") if isinstance(authoritative_asset.get("metadata"), dict) else {}
                authoritative_path = str(authoritative_asset.get("storage_path") or "").strip()
                if authoritative_path and template_surface_policy == "style_reference_only":
                    if existing_layout_path and authoritative_path != existing_layout_path:
                        slide_metadata["reference_topic_asset_path"] = authoritative_path
                    elif not existing_layout_path:
                        slide_metadata["reference_asset_path"] = authoritative_path
                elif authoritative_path:
                    slide_metadata["reference_asset_path"] = authoritative_path
                if not slide_metadata.get("reference_template_name") and not (
                    template_surface_policy == "style_reference_only" and existing_layout_path
                ):
                    slide_metadata["reference_template_name"] = str(
                        asset_metadata.get("family_name")
                        or asset_metadata.get("label")
                        or authoritative_asset.get("asset_id")
                        or ""
                    )
                for key in ("conditioning_page_index", "reference_page_index", "page_index", "page_number"):
                    page_value = asset_metadata.get(key)
                    if isinstance(page_value, int) and page_value > 0:
                        if (
                            template_surface_policy == "style_reference_only"
                            and existing_layout_path
                            and authoritative_path
                            and authoritative_path != existing_layout_path
                        ):
                            slide_metadata["reference_topic_page_index"] = page_value
                        else:
                            slide_metadata["conditioning_page_index"] = page_value
                        break
            slide_copy["metadata"] = slide_metadata
            aligned.append(slide_copy)
        return aligned

    @classmethod
    def _normalize_structured_carousel_slides(
        cls,
        raw_slides: list[Any],
        *,
        headline: str,
        supporting_line: str,
        cta: str,
        proof_points: list[str],
        stat_highlights: list[str],
        target_slide_count: int,
        mistake_style: bool,
        fallback_slides: list[dict[str, Any]],
        carousel_archetype: str = "",
    ) -> list[dict[str, Any]]:
        # Normalizes structured carousel slides from raw slides, headline, and supporting line for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_text and _normalize_carousel_proof_lines before returning the cleaned value.
        normalized_slides: list[dict[str, Any]] = []
        fallback_count = len(fallback_slides)
        slide_count_hint = target_slide_count or len(raw_slides) or fallback_count
        for index, raw_slide in enumerate(raw_slides, start=1):
            if not isinstance(raw_slide, dict):
                continue
            fallback_slide = fallback_slides[index - 1] if index - 1 < fallback_count else {}
            slide_metadata = raw_slide.get("metadata") if isinstance(raw_slide.get("metadata"), dict) else {}
            explicit_title = cls._normalize_metadata_text(
                raw_slide.get("headline")
                or raw_slide.get("title")
                or raw_slide.get("label")
                or raw_slide.get("section_title"),
                limit=120,
            )
            support_candidates = [
                raw_slide.get("supporting_line"),
                raw_slide.get("body"),
                raw_slide.get("summary"),
                raw_slide.get("description"),
                raw_slide.get("detail"),
                fallback_slide.get("supporting_line"),
                supporting_line,
            ]
            resolved_support = next(
                (
                    cls._normalize_metadata_text(candidate, limit=220)
                    for candidate in support_candidates
                    if cls._normalize_metadata_text(candidate, limit=220)
                ),
                "",
            )
            raw_points = (
                raw_slide.get("proof_points")
                or raw_slide.get("bullets")
                or raw_slide.get("key_points")
                or raw_slide.get("stat_highlights")
                or raw_slide.get("facts")
                or raw_slide.get("callouts")
            )
            slide_points = cls._normalize_carousel_proof_lines(raw_points, limit=4)
            if not slide_points:
                slide_points = cls._normalize_metadata_list(fallback_slide.get("proof_points"), limit=3)
            if not slide_points and resolved_support:
                slide_points = [
                    point
                    for point in [resolved_support, *proof_points[:2], *stat_highlights[:1]]
                    if point and point.casefold() != resolved_support.casefold()
                ][:3]
            role = cls._carousel_story_role(
                cls._carousel_role_for_archetype(
                    raw_slide.get("role") or raw_slide.get("slide_role") or slide_metadata.get("story_role"),
                    archetype=carousel_archetype,
                    index=index,
                    slide_count=slide_count_hint,
                ),
                index=index,
                slide_count=slide_count_hint,
                has_cta=False,
            )
            story_role = cls._normalize_metadata_text(
                cls._carousel_role_for_archetype(
                    raw_slide.get("role") or raw_slide.get("slide_role") or slide_metadata.get("story_role"),
                    archetype=carousel_archetype,
                    index=index,
                    slide_count=slide_count_hint,
                ),
                limit=48,
            )
            normalized_story_role = cls._carousel_story_role_token(story_role or role)
            point_budget = cls._carousel_role_point_budget(
                normalized_story_role,
                is_final_slide=index == slide_count_hint,
            )
            body_point_budget = cls._carousel_body_point_budget(
                normalized_story_role,
                is_final_slide=index == slide_count_hint,
            )
            if explicit_title and cls._is_generic_carousel_education_label(explicit_title):
                explicit_title = ""
            if mistake_style:
                resolved_title = explicit_title or cls._infer_mistake_headline(
                    raw_slide.get("headline") or resolved_support or slide_points[:1],
                    fallback_focus=supporting_line or headline,
                    index=index,
                )
            else:
                resolved_title = (
                    explicit_title
                    or cls._normalize_metadata_text(fallback_slide.get("headline"), limit=120)
                    or cls._carousel_role_title(story_role or role, headline=headline, cta=cta)
                )
            slide = {
                "role": role,
                "headline": resolved_title,
                "supporting_line": resolved_support,
                "body": cls._carousel_slide_body_text(
                    raw_slide,
                    fallback_text=cls._normalize_metadata_text(fallback_slide.get("body"), limit=320) or resolved_support,
                ),
                "body_points": cls._normalize_metadata_list(
                    raw_slide.get("body_points"),
                    limit=max(body_point_budget, 1),
                ),
                "proof_points": [
                    point
                    for point in slide_points
                    if not resolved_support or point.casefold() != resolved_support.casefold()
                ][:point_budget],
                "stat_highlights": cls._normalize_metadata_list(raw_slide.get("stat_highlights"), limit=3),
                "cta": cls._normalize_metadata_text(raw_slide.get("cta"), limit=90) or (cta if role == "closing" else ""),
                "visual_focus": cls._normalize_metadata_text(raw_slide.get("visual_focus"), limit=160),
                "transition_note": cls._normalize_metadata_text(raw_slide.get("transition_note"), limit=160),
                "metadata": {
                    **slide_metadata,
                    "story_role": story_role or role,
                    "carousel_archetype": carousel_archetype or slide_metadata.get("carousel_archetype") or "",
                },
            }
            if not slide["body_points"] and body_point_budget:
                slide["body_points"] = cls._normalize_metadata_list(
                    raw_slide.get("proof_points")
                    or raw_slide.get("bullets")
                    or raw_slide.get("key_points")
                    or fallback_slide.get("body_points")
                    or fallback_slide.get("proof_points"),
                    limit=body_point_budget,
                )
            if not slide["body_points"] and body_point_budget and slide["supporting_line"]:
                slide["body_points"] = [slide["supporting_line"]]
            if not slide["headline"]:
                continue
            if not slide["supporting_line"] and slide["proof_points"]:
                slide["supporting_line"] = slide["proof_points"][0]
                slide["proof_points"] = slide["proof_points"][1:4]
            normalized_slides.append(slide)

        deduped: list[dict[str, Any]] = []
        for slide in normalized_slides:
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            slide_story_role = cls._normalize_metadata_text(
                slide_metadata.get("story_role") or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            if any(
                cls._carousel_slides_semantically_overlap(slide, existing)
                and cls._normalize_metadata_text(
                    (
                        existing.get("metadata").get("story_role")
                        if isinstance(existing.get("metadata"), dict)
                        else existing.get("role")
                    )
                    or existing.get("role"),
                    limit=48,
                ).casefold().replace(" ", "_")
                == slide_story_role
                for existing in deduped
            ):
                continue
            deduped.append(slide)
        desired_slide_count = max(
            3,
            slide_count_hint or fallback_count or len(deduped),
        )
        if len(deduped) < desired_slide_count:
            for fallback_slide in fallback_slides:
                if any(cls._carousel_slides_semantically_overlap(fallback_slide, existing) for existing in deduped):
                    continue
                deduped.append(dict(fallback_slide))
                if len(deduped) >= desired_slide_count:
                    break
        if len(deduped) > desired_slide_count:
            if desired_slide_count <= 1:
                deduped = deduped[:1]
            elif desired_slide_count == 2:
                deduped = [deduped[0], deduped[-1]]
            else:
                selected_positions = {
                    cls._proportional_sequence_reference_index(
                        output_index,
                        output_slide_count=desired_slide_count,
                        reference_slide_count=len(deduped),
                    )
                    for output_index in range(1, desired_slide_count + 1)
                }
                trimmed = [
                    dict(slide)
                    for source_index, slide in enumerate(deduped, start=1)
                    if source_index in selected_positions
                ]
                deduped = trimmed[:desired_slide_count] if trimmed else deduped[:desired_slide_count]
                if len(deduped) < desired_slide_count:
                    for candidate in normalized_slides:
                        if any(candidate is existing or cls._carousel_slides_semantically_overlap(candidate, existing) for existing in deduped):
                            continue
                        deduped.append(dict(candidate))
                        if len(deduped) >= desired_slide_count:
                            break
                if len(deduped) < desired_slide_count:
                    for candidate in normalized_slides:
                        candidate_dict = dict(candidate)
                        if any(cls._carousel_slides_semantically_overlap(candidate_dict, existing) for existing in deduped):
                            continue
                        deduped.append(candidate_dict)
                        if len(deduped) >= desired_slide_count:
                            break
        return deduped

    @classmethod
    def _overlay_prompt_safe_visual_focus(cls, slide: dict[str, Any]) -> str:
        # Builds overlay prompt safe visual focus from slide for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        raw_focus = cls._normalize_metadata_text(slide.get("visual_focus"), limit=420)
        if not raw_focus:
            return ""
        sanitized = raw_focus
        for candidate in [
            slide.get("headline"),
            slide.get("supporting_line"),
            slide.get("body"),
            slide.get("cta"),
            *(slide.get("proof_points") or []),
            *(slide.get("body_points") or []),
            *(slide.get("stat_highlights") or []),
        ]:
            text = cls._normalize_metadata_text(candidate, limit=220)
            if text:
                sanitized = sanitized.replace(text, "")
        sanitized = re.sub(r"(?i)\bvisual brief for this slide:\s*", "", sanitized)
        sanitized = re.sub(r"(?i)\bvisual evidence cues:\s*", "Focus on the approved evidence cues only: ", sanitized)
        sanitized = re.sub(r"\s{2,}", " ", sanitized).strip(" .;:-")
        return cls._normalize_metadata_text(sanitized, limit=320)

    @classmethod
    def _outline_to_carousel_slides(
        cls,
        outline: list[dict[str, Any]],
        *,
        headline: str,
        supporting_line: str,
        cta: str,
        proof_points: list[str],
        stat_highlights: list[str],
        body_sentences: list[str],
        request: AIOrchestrationRequest | None,
        target_slide_count: int,
        carousel_archetype: str = "",
    ) -> list[dict[str, Any]]:
        # Centralizes outline carousel slides from outline, headline, and supporting line for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not outline:
            return []
        insight_hierarchy = []
        if request is not None and isinstance(request.research_editorial_brief, dict):
            insight_hierarchy = cls._normalize_metadata_list(
                request.research_editorial_brief.get("insight_hierarchy"),
                limit=8,
            )
        support_pool = cls._normalize_metadata_list(
            [*body_sentences, supporting_line, *insight_hierarchy],
            limit=max(target_slide_count * 2, 8) if target_slide_count else 8,
        )
        proof_pool = cls._normalize_metadata_list(
            [*stat_highlights, *proof_points, *insight_hierarchy],
            limit=max(target_slide_count * 2, 8) if target_slide_count else 8,
        )
        slides: list[dict[str, Any]] = []
        used_supports: list[str] = []
        used_proof_points: set[str] = set()
        limited_outline = outline[:target_slide_count] if target_slide_count else outline
        slide_count = len(limited_outline)
        for index, item in enumerate(limited_outline, start=1):
            role_source = cls._carousel_role_for_archetype(
                item.get("role") or item.get("slide_role") or item.get("purpose"),
                archetype=carousel_archetype,
                index=index,
                slide_count=slide_count,
            ) or item.get("role") or item.get("slide_role") or item.get("purpose")
            role = cls._carousel_story_role(role_source, index=index, slide_count=slide_count)
            story_role = cls._normalize_metadata_text(role_source, limit=48) or role
            normalized_story_role = cls._carousel_story_role_token(story_role)
            title = cls._normalize_metadata_text(
                item.get("headline")
                or item.get("title")
                or item.get("label"),
                limit=120,
            )
            if title and cls._is_generic_carousel_education_label(title):
                title = ""
            item_bullets = cls._carousel_bullet_lines(
                item.get("proof_points")
                or item.get("bullets")
                or item.get("description"),
                limit=4,
            )
            item_bullets = [
                bullet
                for bullet in item_bullets
                if not cls._looks_like_carousel_instruction_text(bullet)
            ]
            item_notes = cls._carousel_bullet_lines(item.get("purpose"), limit=3)
            item_notes = [
                note
                for note in item_notes
                if not cls._looks_like_carousel_instruction_text(note)
            ]
            support_candidates = [
                *item_bullets,
                *item_notes,
                cls._normalize_metadata_text(item.get("supporting_line"), limit=220),
                cls._normalize_metadata_text(item.get("summary"), limit=220),
                cls._normalize_metadata_text(item.get("description"), limit=220),
            ]
            support_candidates = [
                candidate
                for candidate in support_candidates
                if not cls._looks_like_carousel_instruction_text(candidate)
            ]
            resolved_support = ""
            ranked_supports: list[tuple[int, int, str]] = []
            for position, candidate in enumerate([*support_candidates, *support_pool]):
                normalized = cls._normalize_metadata_text(candidate, limit=220)
                if not normalized:
                    continue
                if any(normalized.casefold() == existing.casefold() for existing in used_supports):
                    continue
                ranked_supports.append(
                    (
                        cls._carousel_support_score(normalized_story_role, normalized),
                        -position,
                        normalized,
                    )
                )
            if ranked_supports:
                ranked_supports.sort(reverse=True)
                resolved_support = ranked_supports[0][2]
                used_supports.append(resolved_support)
            explicit_points: list[str] = []
            for candidate in item_bullets:
                normalized = cls._normalize_metadata_text(candidate, limit=180)
                if not normalized:
                    continue
                if resolved_support and normalized.casefold() == resolved_support.casefold():
                    continue
                if normalized in explicit_points:
                    continue
                explicit_points.append(normalized)
                if len(explicit_points) >= 3:
                    break
            point_budget = cls._carousel_role_point_budget(
                normalized_story_role,
                is_final_slide=index == slide_count,
            )
            body_point_budget = cls._carousel_body_point_budget(
                normalized_story_role,
                is_final_slide=index == slide_count,
            )
            slide_points = explicit_points[:point_budget]
            if point_budget > len(slide_points) and normalized_story_role not in {
                "hook",
                "cover",
                "opening",
                "title",
                "implications",
                "implication",
                "strategic_meaning",
                "analysis",
                "what_matters",
                "takeaway",
                "close",
                "closing",
                "cta",
                "final",
            }:
                for candidate in proof_pool:
                    normalized = cls._normalize_metadata_text(candidate, limit=180)
                    if not normalized:
                        continue
                    lowered = normalized.casefold()
                    if resolved_support and lowered == resolved_support.casefold():
                        continue
                    if lowered in used_proof_points:
                        continue
                    if normalized in slide_points:
                        continue
                    slide_points.append(normalized)
                    used_proof_points.add(lowered)
                    if len(slide_points) >= point_budget:
                        break
            body_text = cls._normalize_metadata_text(
                item.get("body")
                or item.get("detail")
                or item.get("summary")
                or item.get("description")
                or item.get("purpose"),
                limit=320,
            )
            if cls._looks_like_carousel_instruction_text(body_text):
                body_text = ""
            body_points = item_notes[:body_point_budget]
            if not body_points and body_point_budget:
                body_points = explicit_points[:body_point_budget]
            if not body_points and body_point_budget and resolved_support:
                body_points = [resolved_support]
            resolved_title = title or cls._carousel_role_title(str(role_source or role), headline=headline, cta=cta)
            slide = {
                "role": role,
                "headline": resolved_title,
                "supporting_line": resolved_support or supporting_line or headline,
                "body": body_text or resolved_support or "",
                "body_points": body_points,
                "proof_points": slide_points,
                "cta": cta if role == "closing" else "",
                "metadata": {"story_role": story_role, "carousel_archetype": carousel_archetype},
            }
            slides.append(slide)

        deduped: list[dict[str, Any]] = []
        for slide in slides:
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            slide_story_role = cls._normalize_metadata_text(
                slide_metadata.get("story_role") or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            if any(
                cls._carousel_slides_semantically_overlap(slide, existing)
                and cls._normalize_metadata_text(
                    (
                        existing.get("metadata").get("story_role")
                        if isinstance(existing.get("metadata"), dict)
                        else existing.get("role")
                    )
                    or existing.get("role"),
                    limit=48,
                ).casefold().replace(" ", "_")
                == slide_story_role
                for existing in deduped
            ):
                continue
            deduped.append(slide)
        return deduped

    @classmethod
    def _carousel_slide_body_text(
        cls,
        slide: dict[str, Any],
        *,
        fallback_text: str = "",
    ) -> str:
        # Extracts carousel slide body text from slide and fallback text for AIOrchestratorService.
        # It calls _normalize_metadata_list and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        for key in ("body", "detail", "summary", "description", "narrative"):
            text = cls._normalize_metadata_text(slide.get(key), limit=320)
            if text:
                return text
        body_points = cls._normalize_metadata_list(slide.get("body_points"), limit=4)
        if body_points:
            return ". ".join(body_points).strip()[:320]
        supporting_line = cls._normalize_metadata_text(slide.get("supporting_line"), limit=220)
        proof_points = cls._normalize_metadata_list(slide.get("proof_points"), limit=3)
        if supporting_line and proof_points:
            return f"{supporting_line} {' '.join(proof_points[:2])}".strip()[:320]
        if supporting_line:
            return supporting_line
        if proof_points:
            return ". ".join(proof_points).strip()[:320]
        return cls._normalize_metadata_text(fallback_text, limit=320)

    @classmethod
    def _carousel_expected_story_roles(
        cls,
        *,
        request: AIOrchestrationRequest | None,
        metadata: dict[str, Any],
        fallback_slides: list[dict[str, Any]],
        slide_count: int,
    ) -> list[str]:
        # Centralizes carousel expected story roles from request state, metadata, and fallback slides for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        carousel_archetype = cls._carousel_infer_archetype(request=request, metadata=metadata)
        archetype_roles = cls._carousel_archetype_role_sequence(
            carousel_archetype,
            slide_count=slide_count,
        )
        outline = cls._carousel_outline_candidates(request, metadata)
        if outline:
            roles: list[str] = []
            for index, item in enumerate(outline[:slide_count], start=1):
                role = cls._carousel_role_for_archetype(
                    item.get("role") or item.get("slide_role") or item.get("purpose"),
                    archetype=carousel_archetype,
                    index=index,
                    slide_count=slide_count,
                )
                if not role:
                    if index == 1:
                        role = "hook"
                    elif index == slide_count:
                        role = "takeaway"
                    else:
                        role = "detail"
                roles.append(role)
            return roles
        if archetype_roles:
            return archetype_roles
        roles = []
        for index, slide in enumerate(fallback_slides[:slide_count], start=1):
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            role = cls._carousel_role_for_archetype(
                slide_metadata.get("story_role") or slide.get("role"),
                archetype=carousel_archetype,
                index=index,
                slide_count=slide_count,
            )
            if not role:
                if index == 1:
                    role = "hook"
                elif index == slide_count:
                    role = "takeaway"
                else:
                    role = "detail"
            roles.append(role)
        return roles

    @classmethod
    def _carousel_story_role_guidance(cls, story_role: str) -> str:
        # Builds carousel story role guidance from story role for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        normalized = cls._normalize_metadata_text(story_role, limit=48).casefold().replace(" ", "_")
        if normalized in {"hook", "cover", "opening", "title"}:
            return "This slide must hook the reader immediately with the hidden angle, not recap the entire story."
        if normalized in {"context", "setup", "intro"}:
            return "This slide should frame the context cleanly and set up the analytical sequence."
        if normalized in {"list_item"}:
            return "This slide should teach exactly one item using a repeated mini-structure such as concept, explanation, example, and consequence."
        if normalized in {"comparison_item"}:
            return "This slide should cover one option only using what it is, how it works, and where it fits."
        if normalized in {"problem_frame"}:
            return "This slide should define the pain point clearly before the solution appears."
        if normalized in {"solution_intro"}:
            return "This slide should introduce the solution and response pattern without turning into a feature dump."
        if normalized in {"feature_cluster"}:
            return "This slide should explain one capability cluster or workflow clearly, not the entire product story."
        if normalized in {"value_close"}:
            return "This slide should close on practical value or decision payoff, not generic hype."
        if normalized in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return "This slide should unpack the factual structure or mechanics clearly, not jump to generic implications."
        if normalized in {"undercovered_angle", "missed_angle", "angle"}:
            return "This slide should surface what most coverage missed, overlooked, or simplified."
        if normalized in {"implications", "implication", "strategic_meaning", "analysis", "what_matters"}:
            return "This slide should explain the strategic implication or second-order meaning, not repeat the hook."
        if normalized in {"takeaway", "close", "closing", "cta", "final"}:
            return "This slide should close the sequence cleanly and contain the only CTA treatment if one is used."
        return "This slide should advance the story with one distinct idea."

    @classmethod
    def _carousel_is_editorial_close_role(cls, story_role: str) -> bool:
        # Centralizes carousel editorial close role from story role for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        normalized = cls._normalize_metadata_text(story_role, limit=48).casefold().replace(" ", "_")
        return normalized not in {"", "takeaway", "close", "closing", "cta", "final"}

    @classmethod
    def _enforce_carousel_editorial_progression(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        metadata: dict[str, Any],
        fallback_slides: list[dict[str, Any]],
        headline: str,
        supporting_line: str,
        cta: str,
    ) -> list[dict[str, Any]]:
        # Centralizes enforce carousel editorial progression from slides, request state, and metadata for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not slides:
            return []
        repaired = [deepcopy(slide) for slide in slides]
        slide_count = len(repaired)
        expected_story_roles = cls._carousel_expected_story_roles(
            request=request,
            metadata=metadata,
            fallback_slides=fallback_slides,
            slide_count=slide_count,
        )
        first_headline = cls._normalize_metadata_text(repaired[0].get("headline"), limit=140)
        used_supports: set[str] = set()
        for index, slide in enumerate(repaired, start=1):
            fallback_slide = fallback_slides[index - 1] if index - 1 < len(fallback_slides) else {}
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            expected_story_role = expected_story_roles[index - 1] if index - 1 < len(expected_story_roles) else ""
            broad_role = "hook" if index == 1 else "closing" if index == slide_count else "intro" if expected_story_role == "context" else "detail"
            current_story_role = cls._normalize_metadata_text(
                slide_metadata.get("story_role") or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            story_role = expected_story_role or current_story_role or broad_role
            if index == 1 and current_story_role in {"cover", "hook", "opening", "title"}:
                broad_role = current_story_role
            slide["role"] = broad_role
            slide_metadata["story_role"] = story_role
            slide_metadata["role_guidance"] = cls._carousel_story_role_guidance(story_role)

            normalized_headline = cls._normalize_metadata_text(slide.get("headline"), limit=120)
            needs_role_title = (
                not normalized_headline
                or cls._is_generic_carousel_education_label(normalized_headline)
                or (index > 1 and first_headline and normalized_headline.casefold() == first_headline.casefold())
                or (index < slide_count and cls._is_promotional_line(normalized_headline))
            )
            if needs_role_title:
                slide["headline"] = (
                    cls._normalize_metadata_text(fallback_slide.get("headline"), limit=120)
                    or cls._carousel_role_title(story_role, headline=headline, cta=cta)
                )

            slide_support = cls._normalize_metadata_text(slide.get("supporting_line"), limit=220)
            fallback_support = cls._normalize_metadata_text(fallback_slide.get("supporting_line"), limit=220)
            if (
                not slide_support
                or slide_support.casefold() in used_supports
                or (index > 1 and supporting_line and slide_support.casefold() == supporting_line.casefold())
            ):
                slide_support = fallback_support or slide_support or supporting_line or headline
                slide["supporting_line"] = slide_support
            if slide_support:
                used_supports.add(slide_support.casefold())

            body_text = cls._carousel_slide_body_text(slide, fallback_text=cls._carousel_slide_body_text(fallback_slide, fallback_text=slide_support))
            if body_text:
                slide["body"] = body_text

            if index < slide_count:
                slide["cta"] = ""
            else:
                slide["cta"] = cls._normalize_metadata_text(cta or slide.get("cta"), limit=90)
                if cls._carousel_is_editorial_close_role(story_role):
                    if not slide["headline"] or cls._is_promotional_line(slide["headline"]):
                        fallback_headline = cls._normalize_metadata_text(fallback_slide.get("headline"), limit=120)
                        if cls._is_promotional_line(fallback_headline):
                            fallback_headline = ""
                        slide["headline"] = (
                            fallback_headline
                            or cls._carousel_role_title(story_role, headline=headline, cta=cta)
                        )
                elif not slide["headline"] or cls._is_promotional_line(slide["headline"]):
                    slide["headline"] = cls._carousel_role_title("closing", headline=headline, cta=cta)

            if not cls._normalize_metadata_list(slide.get("proof_points"), limit=3):
                fallback_points = cls._normalize_metadata_list(fallback_slide.get("proof_points"), limit=3)
                if fallback_points:
                    slide["proof_points"] = fallback_points

            fallback_stats = cls._normalize_metadata_list(fallback_slide.get("stat_highlights"), limit=3)
            current_stats = cls._normalize_metadata_list(slide.get("stat_highlights"), limit=3)
            slide["stat_highlights"] = current_stats or fallback_stats
            slide["metadata"] = slide_metadata
        return repaired

    @classmethod
    def _validate_carousel_semantic_progression(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        metadata: dict[str, Any],
        fallback_slides: list[dict[str, Any]],
        headline: str,
        supporting_line: str,
        cta: str,
    ) -> list[dict[str, Any]]:
        # Validates carousel semantic progression from slides, request state, and metadata for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not slides:
            return []
        repaired = [deepcopy(slide) for slide in slides]
        expected_story_roles = cls._carousel_expected_story_roles(
            request=request,
            metadata=metadata,
            fallback_slides=fallback_slides,
            slide_count=max(len(repaired), len(fallback_slides)),
        )
        desired_count = max(
            len(repaired),
            len(expected_story_roles),
            min(max(len(fallback_slides), 0), max(len(expected_story_roles), 0)),
        )
        if desired_count > len(repaired):
            for index in range(len(repaired), desired_count):
                fallback_slide = fallback_slides[index] if index < len(fallback_slides) else {}
                fallback_copy = dict(fallback_slide) if isinstance(fallback_slide, dict) else {}
                if not fallback_copy:
                    role = expected_story_roles[index] if index < len(expected_story_roles) else ("takeaway" if index == desired_count - 1 else "detail")
                    fallback_copy = {
                        "role": "closing" if index == desired_count - 1 else "detail",
                        "headline": cls._carousel_role_title(role, headline=headline, cta=cta),
                        "supporting_line": supporting_line or headline,
                        "proof_points": [],
                        "cta": cls._normalize_metadata_text(cta, limit=90) if index == desired_count - 1 else "",
                        "metadata": {"story_role": role},
                    }
                repaired.append(fallback_copy)

        hook_slide = repaired[0]
        prior_slides: list[dict[str, Any]] = []
        seen_roles: dict[str, int] = {}
        for index, slide in enumerate(repaired, start=1):
            fallback_slide = fallback_slides[index - 1] if index - 1 < len(fallback_slides) else {}
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            expected_story_role = expected_story_roles[index - 1] if index - 1 < len(expected_story_roles) else ""
            current_story_role = cls._normalize_metadata_text(
                slide_metadata.get("story_role") or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            story_role = expected_story_role or current_story_role or ("takeaway" if index == len(repaired) else "detail")
            slide_metadata["story_role"] = story_role
            slide_metadata["role_guidance"] = cls._carousel_story_role_guidance(story_role)
            seen_roles[story_role] = seen_roles.get(story_role, 0) + 1

            semantic_overlap_with_hook = index > 1 and cls._carousel_slides_semantically_overlap(slide, hook_slide)
            semantic_overlap_with_prior = any(
                cls._carousel_slides_semantically_overlap(slide, prior)
                for prior in prior_slides
            )
            duplicate_non_detail_role = story_role not in {"detail", "details"} and seen_roles[story_role] > 1
            needs_semantic_repair = semantic_overlap_with_hook or semantic_overlap_with_prior or duplicate_non_detail_role

            if needs_semantic_repair:
                fallback_headline = cls._normalize_metadata_text(fallback_slide.get("headline"), limit=120)
                fallback_support = cls._normalize_metadata_text(fallback_slide.get("supporting_line"), limit=220)
                fallback_body = cls._carousel_slide_body_text(
                    fallback_slide,
                    fallback_text=fallback_support or supporting_line or headline,
                )
                fallback_points = cls._normalize_metadata_list(fallback_slide.get("proof_points"), limit=3)
                fallback_stats = cls._normalize_metadata_list(fallback_slide.get("stat_highlights"), limit=3)
                preferred_headline = fallback_headline or cls._carousel_role_title(story_role, headline=headline, cta=cta)
                if story_role in {"undercovered_angle", "missed_angle", "angle"}:
                    normalized_preferred = cls._normalize_metadata_text(preferred_headline, limit=120).casefold()
                    if normalized_preferred in {
                        cls._normalize_metadata_text(headline, limit=120).casefold(),
                        cls._normalize_metadata_text(hook_slide.get("headline"), limit=120).casefold(),
                        "the overlooked headline",
                    }:
                        preferred_headline = cls._carousel_role_title("undercovered_angle", headline=headline, cta=cta)
                if story_role in {"implications", "implication", "strategic_meaning", "analysis", "what_matters"}:
                    normalized_preferred = cls._normalize_metadata_text(preferred_headline, limit=120).casefold()
                    if normalized_preferred in {
                        cls._normalize_metadata_text(headline, limit=120).casefold(),
                        cls._normalize_metadata_text(hook_slide.get("headline"), limit=120).casefold(),
                    }:
                        preferred_headline = cls._carousel_role_title("strategic_meaning", headline=headline, cta=cta)
                slide["headline"] = preferred_headline
                if fallback_support:
                    slide["supporting_line"] = fallback_support
                elif semantic_overlap_with_hook or semantic_overlap_with_prior:
                    slide["supporting_line"] = cls._normalize_metadata_text(
                        cls._carousel_slide_body_text(slide, fallback_text=supporting_line or headline),
                        limit=220,
                    ) or cls._carousel_role_title(story_role, headline=headline, cta=cta)
                if fallback_body:
                    slide["body"] = fallback_body
                if fallback_points:
                    slide["proof_points"] = fallback_points
                if fallback_stats:
                    slide["stat_highlights"] = fallback_stats

            if index < len(repaired):
                slide["cta"] = ""
            else:
                slide["cta"] = cls._normalize_metadata_text(cta or slide.get("cta"), limit=90)
                if cls._carousel_is_editorial_close_role(story_role):
                    slide_metadata["story_role"] = story_role
                    slide_metadata["role_guidance"] = cls._carousel_story_role_guidance(story_role)
                    if (
                        not cls._normalize_metadata_text(slide.get("headline"), limit=120)
                        or cls._is_promotional_line(str(slide.get("headline") or ""))
                    ):
                        fallback_headline = cls._normalize_metadata_text(fallback_slide.get("headline"), limit=120)
                        if cls._is_promotional_line(fallback_headline):
                            fallback_headline = ""
                        slide["headline"] = (
                            fallback_headline
                            or cls._carousel_role_title(story_role, headline=headline, cta=cta)
                        )
                else:
                    slide_metadata["story_role"] = "takeaway"
                    slide_metadata["role_guidance"] = cls._carousel_story_role_guidance("takeaway")
                    headline_text = cls._normalize_metadata_text(slide.get("headline"), limit=120)
                    cta_text = cls._normalize_metadata_text(cta or slide.get("cta"), limit=120)
                    if (
                        not headline_text
                        or (cls._is_promotional_line(headline_text) and headline_text.casefold() != cta_text.casefold())
                    ):
                        slide["headline"] = cls._carousel_role_title("takeaway", headline=headline, cta=cta)

            if story_role in {"undercovered_angle", "missed_angle", "angle"} and (
                not cls._normalize_metadata_text(slide.get("headline"), limit=120)
                or cls._is_promotional_line(str(slide.get("headline") or ""))
                or cls._normalize_metadata_text(slide.get("headline"), limit=120).casefold() == "the overlooked headline"
            ):
                slide["headline"] = cls._carousel_role_title("undercovered_angle", headline=headline, cta=cta)
            if story_role in {"implications", "implication", "strategic_meaning", "analysis", "what_matters"} and (
                not cls._normalize_metadata_text(slide.get("headline"), limit=120)
                or cls._is_promotional_line(str(slide.get("headline") or ""))
            ):
                slide["headline"] = cls._carousel_role_title("strategic_meaning", headline=headline, cta=cta)

            slide["metadata"] = slide_metadata
            prior_slides.append(slide)
        return repaired

    @staticmethod
    def _carousel_story_role_label(story_role: str) -> str:
        # Extracts carousel story role label from story role for AIOrchestratorService.
        # It calls _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        normalized = AIOrchestratorService._normalize_metadata_text(story_role, limit=48).casefold().replace(" ", "_")
        if normalized in {"hook", "cover", "opening", "title"}:
            return "opening hook"
        if normalized in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return "mechanism explainer"
        if normalized in {"undercovered_angle", "missed_angle", "angle"}:
            return "hidden-angle reveal"
        if normalized in {"strategic_meaning", "implications", "implication", "analysis", "what_matters"}:
            return "implication payoff"
        if normalized in {"takeaway", "close", "closing", "cta", "final"}:
            return "closing payoff"
        return normalized.replace("_", " ") or "story beat"

    @staticmethod
    def _carousel_slide_text_pressure(slide: dict[str, Any]) -> str:
        # Extracts carousel slide text pressure from slide for AIOrchestratorService.
        # It calls _normalize_metadata_text and _carousel_slide_body_text to turn raw evidence into the structured signal the caller needs.
        supporting_line = AIOrchestratorService._normalize_metadata_text(
            slide.get("supporting_line"),
            limit=240,
        )
        body = AIOrchestratorService._carousel_slide_body_text(
            slide,
            fallback_text=supporting_line,
        )
        proof_points = AIOrchestratorService._normalize_metadata_list(slide.get("proof_points"), limit=4)
        body_points = AIOrchestratorService._normalize_metadata_list(slide.get("body_points"), limit=4)
        stat_highlights = AIOrchestratorService._normalize_metadata_list(slide.get("stat_highlights"), limit=4)
        claim_pairs = AIOrchestratorService._normalize_claim_evidence_pairs(
            slide.get("claim_evidence_pairs"),
            limit=3,
        )
        module_count = max(
            len(proof_points),
            len(body_points),
            len(stat_highlights),
            len(claim_pairs),
        )
        body_length = len(body)
        if body_length >= 210 or module_count >= 3:
            return "dense"
        if body_length <= 80 and module_count <= 1:
            return "airy"
        return "balanced"

    @staticmethod
    def _word_count(value: Any) -> int:
        # Counts word count from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        return len(re.findall(r"\b[\w%$./+-]+\b", str(value or "")))

    @classmethod
    def _truncate_to_words(cls, value: Any, *, max_words: int, min_words: int = 5) -> str:
        # Shortens truncate words from input value, max words, and min words for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_text and _sentences before returning the cleaned value.
        text = cls._normalize_metadata_text(value, limit=360)
        if not text:
            return ""
        if cls._word_count(text) <= max_words:
            return text
        sentence_candidates = cls._sentences(text)
        for candidate in sentence_candidates[:2]:
            if min_words <= cls._word_count(candidate) <= max_words:
                return candidate.strip(" ,.;:-")
        for separator in (" - ", ";", ",", " because ", " while ", " together "):
            for candidate in text.split(separator):
                cleaned = candidate.strip(" ,.;:-")
                if min_words <= cls._word_count(cleaned) <= max_words:
                    return cleaned
        words = re.findall(r"\S+", text)
        meaningful_words = words[:max_words]
        weak_endings = {
            "a",
            "an",
            "and",
            "as",
            "at",
            "by",
            "for",
            "from",
            "in",
            "into",
            "of",
            "on",
            "or",
            "the",
            "to",
            "with",
            "without",
        }
        while len(meaningful_words) > min_words and meaningful_words[-1].strip(" ,.;:-").casefold() in weak_endings:
            meaningful_words.pop()
        return " ".join(meaningful_words).strip(" ,.;:-")

    @classmethod
    def _compact_carousel_metric_line(cls, value: Any, *, max_words: int) -> str:
        # Compacts carousel metric line from input value and max words for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._normalize_metadata_text(value, limit=220)
        if not text:
            return ""
        if ":" in text:
            label, metric = [part.strip(" ,.;:-") for part in text.split(":", 1)]
            if metric and re.search(r"\d|%", metric):
                label = re.sub(r"^(?:percentage|estimated|daily|number)\s+of\s+", "", label, flags=re.IGNORECASE)
                label = re.sub(r"^estimated\s+", "", label, flags=re.IGNORECASE)
                label = re.split(r"\b(?:due to|if|in|during)\b", label, maxsplit=1, flags=re.IGNORECASE)[0]
                label = label.strip(" ,.;:-")
                text = f"{metric} {label.lower()}".strip()
        return cls._truncate_to_words(text, max_words=max_words)

    @classmethod
    def _carousel_slide_needs_comparison_structure(cls, slide: dict[str, Any]) -> bool:
        # Centralizes carousel slide needs comparison structure from slide for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        fields: list[Any] = [
            slide.get("headline"),
            slide.get("supporting_line"),
            slide.get("body"),
            slide.get("cta"),
            *(slide.get("proof_points") or []),
            *(slide.get("body_points") or []),
            *(slide.get("stat_highlights") or []),
        ]
        text = " ".join(cls._normalize_metadata_text(item, limit=220) for item in fields if item).casefold()
        if not text:
            return False
        has_a_b_pair = bool(
            re.search(r"\b(?:person|individual|investor|founder)\s+a\b", text)
            and re.search(r"\b(?:person|individual|investor|founder)\s+b\b", text)
        )
        has_comparison = bool(
            "same income" in text
            or "identical income" in text
            or "identical earnings" in text
            or "different spending" in text
            or " vs " in text
            or " versus " in text
        )
        has_money_behavior = any(token in text for token in ("spend", "save", "invest", "wealth", "surplus"))
        return has_money_behavior and (has_a_b_pair or has_comparison)

    @classmethod
    def _restore_compacted_currency_claims(cls, *, source: Any, value: Any) -> Any:
        # Centralizes restore compacted currency claims from source and input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        return cls._restore_corrupted_currency_claims(previous_source=source, rewritten_value=value)

    @classmethod
    def _compact_comparison_gap_line(cls, value: Any) -> str:
        # Compacts comparison gap line from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        text = cls._normalize_metadata_text(value, limit=160)
        if not text:
            return ""
        match = re.search(
            r"(?P<amount>(?:\u20b9|Ã¢â€šÂ¹|rs\.?|inr|usd|us\$|nzd|nz\$|\$)?\s?\d[\d,]*(?:\.\d+)?)",
            text,
            flags=re.IGNORECASE,
        )
        lowered = text.casefold()
        if match and any(token in lowered for token in ("additional", "extra", "frees up", "free up", "surplus")):
            return f"B invests extra {match.group('amount').strip()} monthly"
        return text

    @classmethod
    def _repair_missing_indian_currency_markers(cls, value: Any) -> Any:
        # Repairs missing indian currency markers from input value for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if isinstance(value, str):
            def replace(match: re.Match[str]) -> str:
                # Centralizes replace from match for AIOrchestratorService.
                # The helper owns a small rule that would distract from the surrounding flow.
                prefix = match.group("prefix") or ""
                amount = match.group("amount") or ""
                unit = match.group("unit") or ""
                if "," not in amount and not unit.strip():
                    return match.group(0)
                return f"{prefix}\u20b9{amount}{unit}"

            return re.sub(
                r"(?P<prefix>^|[\s(])9(?P<amount>\d[\d,]*(?:\.\d+)?)(?P<unit>\s*(?:crore|lakh|million|billion|trillion))?",
                replace,
                value,
                flags=re.IGNORECASE,
            )
        if isinstance(value, list):
            return [cls._repair_missing_indian_currency_markers(item) for item in value]
        if isinstance(value, dict):
            return {key: cls._repair_missing_indian_currency_markers(item) for key, item in value.items()}
        return value

    @classmethod
    def _carousel_visible_text_word_count(cls, slide: dict[str, Any]) -> int:
        # Counts carousel visible text word count from slide for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        fields: list[Any] = [
            slide.get("headline"),
            slide.get("supporting_line"),
            slide.get("body"),
            slide.get("cta"),
        ]
        for key in ("proof_points", "body_points", "stat_highlights"):
            fields.extend(cls._normalize_metadata_list(slide.get(key), limit=8))
        fields.extend(
            cls._claim_evidence_pair_lines(
                cls._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=4),
                limit=4,
            )
        )
        return sum(cls._word_count(item) for item in fields if item)

    @classmethod
    def _mobile_carousel_copy_budget(cls, slide: dict[str, Any], *, slide_index: int, slide_count: int) -> dict[str, int]:
        # Centralizes mobile carousel copy budget from slide, slide index, and slide count for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        story_role = cls._normalize_metadata_text(
            metadata.get("story_role") or slide.get("role") or slide.get("slide_role"),
            limit=48,
        ).casefold().replace(" ", "_")
        sample_density = cls._normalize_metadata_text(metadata.get("sample_page_copy_density"), limit=32).casefold()
        is_cover = slide_index <= 1 or story_role in {"hook", "cover", "opening", "title"}
        is_closing = slide_index >= slide_count or story_role in {"takeaway", "close", "closing", "cta", "final"}
        max_total = 30 if is_cover else 34 if is_closing else 38
        if sample_density in {"airy", "low", "minimal"}:
            max_total = min(max_total, 32)
        elif sample_density in {"dense", "high"}:
            max_total = min(max_total + 4, 40)
        return {
            "headline": 11 if is_cover else 10,
            "supporting_line": 12,
            "body": 14 if is_cover or is_closing else 16,
            "proof_line": 10,
            "cta": 9,
            "max_total": max_total,
            "max_modules": 2 if is_cover or is_closing else 3,
        }

    @classmethod
    def _compact_carousel_slide_for_mobile(
        cls,
        slide: dict[str, Any],
        *,
        slide_index: int,
        slide_count: int,
    ) -> dict[str, Any]:
        # Compacts carousel slide mobile from slide, slide index, and slide count for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        compacted = dict(slide)
        metadata = dict(compacted.get("metadata") if isinstance(compacted.get("metadata"), dict) else {})
        original_snapshot = {
            key: compacted.get(key)
            for key in (
                "headline",
                "supporting_line",
                "body",
                "proof_points",
                "body_points",
                "stat_highlights",
                "claim_evidence_pairs",
                "cta",
            )
            if compacted.get(key)
        }
        original_word_count = cls._carousel_visible_text_word_count(compacted)
        budget = cls._mobile_carousel_copy_budget(compacted, slide_index=slide_index, slide_count=slide_count)
        comparison_driven = cls._carousel_slide_needs_comparison_structure(compacted)
        if comparison_driven:
            for key in ("headline", "supporting_line", "body", "cta", "proof_points", "body_points", "stat_highlights"):
                if key in compacted:
                    compacted[key] = cls._repair_missing_indian_currency_markers(compacted.get(key))
            original_snapshot = cls._repair_missing_indian_currency_markers(original_snapshot)

        compacted["headline"] = cls._truncate_to_words(compacted.get("headline"), max_words=budget["headline"])
        compacted["supporting_line"] = cls._truncate_to_words(
            compacted.get("supporting_line"),
            max_words=budget["supporting_line"],
        )
        compacted["body"] = cls._truncate_to_words(
            compacted.get("body"),
            max_words=budget["body"] + 8 if comparison_driven else budget["body"],
        )
        compacted["cta"] = cls._truncate_to_words(compacted.get("cta"), max_words=budget["cta"])
        if compacted.get("body") and cls._carousel_texts_semantically_overlap(
            compacted.get("body"),
            compacted.get("supporting_line"),
        ):
            compacted["body"] = ""

        proof_lines: list[str] = []
        for key in ("stat_highlights", "proof_points", "body_points"):
            proof_lines.extend(
                cls._compact_carousel_metric_line(item, max_words=budget["proof_line"])
                for item in cls._normalize_carousel_proof_lines(compacted.get(key), limit=8)
            )
        proof_lines = sorted(
            [line for line in proof_lines if line],
            key=lambda line: 0 if re.search(r"\d|%|\b(?:barrels?|bps|crore|lakh|million|billion|trillion)\b", line, re.IGNORECASE) else 1,
        )
        proof_lines = cls._restore_compacted_currency_claims(source=original_snapshot, value=proof_lines)
        proof_lines = cls._dedupe_metadata_collection(
            proof_lines,
            blocked_texts=[
                compacted.get("headline"),
                compacted.get("supporting_line"),
                compacted.get("cta"),
            ],
            limit=budget["max_modules"],
        )
        comparison_body_points: list[str] = []
        if comparison_driven:
            comparison_body_points = [
                cls._compact_carousel_metric_line(item, max_words=budget["proof_line"])
                for item in cls._normalize_carousel_proof_lines(compacted.get("body_points"), limit=3)
            ]
            comparison_body_points = cls._restore_compacted_currency_claims(
                source=original_snapshot,
                value=[line for line in comparison_body_points if line],
            )
            comparison_body_points = [
                cls._compact_comparison_gap_line(line)
                for line in comparison_body_points
                if cls._compact_comparison_gap_line(line)
            ]
            comparison_body_points = cls._dedupe_metadata_collection(
                comparison_body_points,
                blocked_texts=[
                    compacted.get("headline"),
                    compacted.get("supporting_line"),
                    compacted.get("body"),
                    compacted.get("cta"),
                ],
                limit=1,
            )
        compacted["headline"] = cls._restore_compacted_currency_claims(source=original_snapshot, value=compacted.get("headline"))
        compacted["supporting_line"] = cls._restore_compacted_currency_claims(source=original_snapshot, value=compacted.get("supporting_line"))
        compacted["body"] = cls._restore_compacted_currency_claims(source=original_snapshot, value=compacted.get("body"))
        compacted["cta"] = cls._restore_compacted_currency_claims(source=original_snapshot, value=compacted.get("cta"))
        compacted["proof_points"] = proof_lines
        compacted["body_points"] = comparison_body_points if comparison_driven else []
        compacted["stat_highlights"] = []

        if (
            proof_lines
            and compacted.get("body")
            and cls._carousel_visible_text_word_count(compacted) > budget["max_total"]
        ):
            if comparison_driven:
                if len(compacted.get("proof_points") or []) > 1:
                    compacted["proof_points"] = list(compacted.get("proof_points") or [])[:1]
                elif compacted.get("proof_points"):
                    compacted["proof_points"] = []
            else:
                compacted["body"] = ""
        while (
            not comparison_driven
            and cls._carousel_visible_text_word_count(compacted) > budget["max_total"]
            and compacted.get("body")
        ):
            compacted["body"] = cls._truncate_to_words(compacted.get("body"), max_words=max(budget["body"] - 4, 8))
            break
        if comparison_driven and cls._carousel_visible_text_word_count(compacted) > budget["max_total"] and compacted.get("proof_points"):
            compacted["proof_points"] = []
        if comparison_driven and cls._carousel_visible_text_word_count(compacted) > budget["max_total"]:
            comparison_support = "Same income, different surplus"
            if not any(token in " ".join(
                [
                    str(original_snapshot.get("supporting_line") or ""),
                    str(original_snapshot.get("body") or ""),
                ]
            ).casefold() for token in ("income", "earn", "earning", "salary")):
                comparison_support = "A spends more; B keeps more"
            compacted["supporting_line"] = comparison_support
        if cls._carousel_visible_text_word_count(compacted) > budget["max_total"] and len(compacted.get("proof_points") or []) > 1:
            compacted["proof_points"] = list(compacted.get("proof_points") or [])[:1]
        if cls._carousel_visible_text_word_count(compacted) > budget["max_total"]:
            compacted["supporting_line"] = cls._truncate_to_words(compacted.get("supporting_line"), max_words=9)

        compacted_word_count = cls._carousel_visible_text_word_count(compacted)
        if compacted_word_count < original_word_count:
            metadata["pre_mobile_compaction"] = original_snapshot
            metadata["mobile_copy_budget"] = {
                "max_visible_words": budget["max_total"],
                "original_visible_words": original_word_count,
                "compacted_visible_words": compacted_word_count,
            }
        compacted["metadata"] = metadata
        return compacted

    @classmethod
    def _same_income_comparison_requested(cls, request: AIOrchestrationRequest | None) -> bool:
        # Centralizes same income comparison requested from request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        prompt = cls._normalize_metadata_text(getattr(request, "prompt", "") if request is not None else "", limit=260).casefold()
        return bool(
            prompt
            and "same income" in prompt
            and ("spending habit" in prompt or "different spending" in prompt)
            and ("wealth" in prompt or "long-term" in prompt or "long term" in prompt)
        )

    @classmethod
    def _extract_same_income_comparison_amounts(
        cls,
        *,
        slides: list[dict[str, Any]],
        source_slides: list[dict[str, Any]] | None = None,
    ) -> dict[str, str]:
        # Extracts same income comparison amounts from slides and source slides for AIOrchestratorService.
        # It calls _repair_missing_indian_currency_markers and _normalize_metadata_text to turn raw evidence into the structured signal the caller needs.
        source = source_slides if source_slides else slides
        raw_text = json.dumps(source, ensure_ascii=False).replace("\\n", "\n")
        repaired_text = cls._repair_missing_indian_currency_markers(raw_text)

        def amount_after(pattern: str) -> str:
            # Centralizes amount after from pattern for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            match = re.search(pattern, repaired_text, flags=re.IGNORECASE | re.DOTALL)
            return cls._normalize_metadata_text(match.group("amount"), limit=40).rstrip(" ,.;:-") if match else ""

        amounts = {
            "income": amount_after(r"(?:same|identical|earn(?:ing)?|income)[^₹$]{0,80}(?P<amount>₹\s?\d[\d,]*(?:\.\d+)?)"),
            "a_spend": amount_after(r"(?:investor|person|individual|founder)\s+a\s+spends?\s+(?P<amount>₹\s?\d[\d,]*(?:\.\d+)?)"),
            "b_spend": amount_after(r"(?:investor|person|individual|founder)\s+b\s+spends?\s+(?P<amount>₹\s?\d[\d,]*(?:\.\d+)?)"),
            "a_save": amount_after(r"(?:investor|person|individual|founder)\s+a[^.]{0,100}sav(?:e|es|ing)\s+(?P<amount>₹\s?\d[\d,]*(?:\.\d+)?)"),
            "b_save": amount_after(r"(?:investor|person|individual|founder)\s+b[^.]{0,100}sav(?:e|es|ing)\s+(?P<amount>₹\s?\d[\d,]*(?:\.\d+)?)"),
            "gap": amount_after(r"(?:extra|additional|difference|surplus)[^₹$]{0,80}(?P<amount>₹\s?\d[\d,]*(?:\.\d+)?)"),
        }
        vs_match = re.search(
            r"(?P<first>₹\s?\d[\d,]*(?:\.\d+)?)\s+monthly\s+vs\.?\s+(?P<second>₹\s?\d[\d,]*(?:\.\d+)?)\s+monthly",
            repaired_text,
            flags=re.IGNORECASE,
        )
        if vs_match:
            amounts["b_save"] = amounts.get("b_save") or cls._normalize_metadata_text(vs_match.group("first"), limit=40)
            amounts["a_save"] = amounts.get("a_save") or cls._normalize_metadata_text(vs_match.group("second"), limit=40)
        return {key: value for key, value in amounts.items() if value}

    @classmethod
    def _apply_same_income_comparison_story_contract(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        source_slides: list[dict[str, Any]] | None = None,
    ) -> list[dict[str, Any]]:
        # Applies apply same income comparison story contract from slides, request state, and source slides for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not slides or not cls._same_income_comparison_requested(request):
            return slides
        amounts = cls._extract_same_income_comparison_amounts(slides=slides, source_slides=source_slides)
        repaired = [dict(slide) for slide in slides if isinstance(slide, dict)]
        if not repaired:
            return slides

        def update_slide(index: int, *, headline: str, supporting_line: str, body: str = "", body_points: list[str] | None = None, proof_points: list[str] | None = None, cta: str | None = None) -> None:
            # Centralizes update slide from index, headline, and supporting line for AIOrchestratorService.
            # The helper owns a small rule that would distract from the surrounding flow.
            if index >= len(repaired):
                return
            slide = dict(repaired[index])
            metadata = dict(slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {})
            metadata["same_income_comparison_contract_applied"] = True
            slide.update(
                {
                    "headline": headline,
                    "supporting_line": supporting_line,
                    "body": body,
                    "body_points": body_points or [],
                    "proof_points": proof_points or [],
                    "stat_highlights": [],
                    "claim_evidence_pairs": [],
                    "metadata": metadata,
                }
            )
            if cta is not None:
                slide["cta"] = cta
            repaired[index] = slide

        income = amounts.get("income") or "the same income"
        a_spend = amounts.get("a_spend")
        b_spend = amounts.get("b_spend")
        a_save = amounts.get("a_save")
        b_save = amounts.get("b_save")
        gap = amounts.get("gap")

        update_slide(
            0,
            headline="Same Income. Different Wealth.",
            supporting_line=f"Both earn {income}. The gap starts with what they keep." if income.startswith("₹") else "The gap starts with what each person keeps.",
            body="",
            proof_points=[],
        )
        if a_spend and b_spend:
            update_slide(
                1,
                headline="Where The Gap Starts",
                supporting_line=f"A spends {a_spend}. B spends {b_spend}.",
                body=f"B keeps {gap} more every month." if gap else "B keeps more investable surplus every month.",
                body_points=[],
                proof_points=[],
            )
        if a_save and b_save:
            update_slide(
                2,
                headline="The Investment Gap",
                supporting_line=f"A invests {a_save}. B invests {b_save}.",
                body=f"Same salary, but {gap} more works for B each month." if gap else "Same salary, more money working each month.",
                body_points=[],
                proof_points=[],
            )
        update_slide(
            3,
            headline="Why It Compounds",
            supporting_line=f"The extra {gap} repeats every month." if gap else "The surplus repeats every month.",
            body="Over years, the surplus becomes the wealth gap.",
            body_points=[],
            proof_points=[],
        )
        update_slide(
            len(repaired) - 1,
            headline="The Habit That Changes The Curve",
            supporting_line="Spend consciously. Invest the surplus.",
            body="",
            body_points=[],
            proof_points=["Compare spending", "Invest surplus"],
            cta="Explore fixed-income options",
        )
        return repaired

    @classmethod
    def _compact_carousel_slides_for_mobile(cls, slides: list[dict[str, Any]]) -> list[dict[str, Any]]:
        # Compacts carousel slides mobile from slides for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        slide_count = len([slide for slide in slides if isinstance(slide, dict)])
        compacted: list[dict[str, Any]] = []
        for index, slide in enumerate(slides, start=1):
            if not isinstance(slide, dict):
                continue
            compacted.append(
                cls._compact_carousel_slide_for_mobile(
                    slide,
                    slide_index=index,
                    slide_count=slide_count,
                )
            )
        return compacted

    @staticmethod
    def _carousel_sequence_position(index: int, slide_count: int) -> str:
        # Measures carousel sequence position from index and slide count for AIOrchestratorService.
        # The measurement feeds layout, crop, overlap, or density decisions.
        if slide_count <= 1 or index <= 1:
            return "opening"
        if index >= slide_count:
            return "closing"
        if slide_count <= 3:
            return "middle"
        midpoint = (slide_count + 1) / 2
        if index < midpoint:
            return "early"
        if index > midpoint:
            return "late"
        return "middle"

    @classmethod
    def _carousel_continuity_profile(
        cls,
        *,
        slide: dict[str, Any],
        slide_index: int,
        slide_count: int,
        previous_story_role: str,
        next_story_role: str,
    ) -> dict[str, str]:
        # Builds carousel continuity profile from slide, slide index, and slide count for AIOrchestratorService.
        # It calls _carousel_sequence_position and _carousel_slide_text_pressure while assembling the payload or prompt text.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        story_role = cls._normalize_metadata_text(
            slide_metadata.get("story_role") or slide.get("role"),
            limit=48,
        ).casefold().replace(" ", "_")
        sequence_position = cls._carousel_sequence_position(slide_index, slide_count)
        text_pressure = cls._carousel_slide_text_pressure(slide)
        previous_label = cls._carousel_story_role_label(previous_story_role) if previous_story_role else ""
        next_label = cls._carousel_story_role_label(next_story_role) if next_story_role else ""

        if story_role in {"hook", "cover", "opening", "title"}:
            visual_weight = "hero_dominant"
            composition_mode = "single_anchor_metaphor"
            pacing_target = "Start with the highest-contrast visual hook in the series, then leave room for the next slide to narrow into explanation."
            layout_balance_target = "Keep the slide visually led at roughly a 70/30 image-to-text feel with one dominant focal zone and broad copy-safe negative space."
            whitespace_target = "Use generous negative space so the opening feels premium, immediate, and easy to scan."
        elif story_role in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            visual_weight = "balanced_explainer"
            composition_mode = "guided_mechanism_modules"
            pacing_target = "Step down from the opener into a clearer mechanism or explainer surface with more structure and less spectacle."
            layout_balance_target = "Aim for a balanced 55/45 image-to-text feel with modular regions that can hold explanation shells without crowding."
            whitespace_target = "Keep moderate whitespace between explainer modules so the slide reads as a system, not a dense poster."
        elif story_role in {"undercovered_angle", "missed_angle", "angle"}:
            visual_weight = "evidence_focus"
            composition_mode = "evidence_reveal"
            pacing_target = "Increase specificity here by revealing proof, hidden layers, or contextual evidence instead of repeating the previous hero grammar."
            layout_balance_target = "Keep a 50/50 balance between visual evidence and text-safe explainer zones so the hidden-angle reveal feels deliberate."
            whitespace_target = "Use restrained whitespace around evidence modules so the slide feels focused rather than sparse."
        elif story_role in {"strategic_meaning", "implications", "implication", "analysis", "what_matters"}:
            visual_weight = "systemic_payoff"
            composition_mode = "network_or_outcome_map"
            pacing_target = "Widen the story into consequences, systems, or outcome pathways so the carousel feels like it is building toward a payoff."
            layout_balance_target = "Aim for a balanced explanatory composition with one central relationship map or payoff structure rather than another standalone hero visual."
            whitespace_target = "Preserve enough whitespace to keep systems and payoff relationships legible, but do not let the slide feel empty."
        elif story_role in {"takeaway", "close", "closing", "cta", "final"}:
            visual_weight = "cta_resolved"
            composition_mode = "decision_support_surface"
            pacing_target = "Resolve the sequence with a calmer decision-support or product-context surface that clearly feels like a closing beat."
            layout_balance_target = "Keep the closing slide cleaner and more resolved than the middle slides, with explicit CTA-safe space and a calmer 50/50 composition."
            whitespace_target = "Reserve guarded whitespace around the CTA-safe and corner-safe regions so the close feels intentional and premium."
        else:
            if text_pressure == "dense":
                visual_weight = "modular_explainer"
                composition_mode = "multi_module_explainer"
                layout_balance_target = "Use a modular 50/50 composition with disciplined cards or callout regions so the slide can carry explanation without turning into a poster."
                whitespace_target = "Maintain consistent gutters and card spacing to keep dense information readable."
            elif text_pressure == "airy":
                visual_weight = "supporting_scene"
                composition_mode = "focused_support_visual"
                layout_balance_target = "Let the visual lead slightly while protecting clean copy-safe space for a lighter reading load."
                whitespace_target = "Use calm whitespace so this lighter slide creates pacing variety inside the carousel."
            else:
                visual_weight = "balanced_explainer"
                composition_mode = "single_focus_plus_modules"
                layout_balance_target = "Keep image and text zones balanced with one focal subject and one supporting module family."
                whitespace_target = "Use measured whitespace between the focal visual and supporting explanation shells."
            pacing_target = "Advance the story with a visibly different composition scale and module rhythm from adjacent slides."

        adjacency_parts: list[str] = []
        if previous_label:
            adjacency_parts.append(f"Move forward from the previous {previous_label}")
        else:
            adjacency_parts.append("Open the carousel with the strongest visual beat")
        if next_label:
            adjacency_parts.append(f"and set up the next {next_label}")
        else:
            adjacency_parts.append("and end on a clear resolved finish")

        return {
            "sequence_position": sequence_position,
            "text_pressure": text_pressure,
            "visual_weight": visual_weight,
            "composition_mode": composition_mode,
            "pacing_target": pacing_target,
            "layout_balance_target": layout_balance_target,
            "whitespace_target": whitespace_target,
            "continuity_anchor": "Keep palette roles, material/rendering language, spacing rhythm, module styling, and brand motifs consistent across the carousel while changing the focal composition per slide.",
            "adjacency_contract": " ".join(adjacency_parts) + ".",
            "previous_story_role": previous_story_role,
            "next_story_role": next_story_role,
            "anti_poster_rule": "Do not solve this slide as a standalone static poster; it must read as one beat inside a coordinated carousel sequence.",
            "transition_note": (
                f"{'Move from the previous ' + previous_label if previous_label else 'Start with a high-contrast hook'}"
                f"{' into this ' + cls._carousel_story_role_label(story_role) if story_role else ' into this slide'}"
                f"{' and tee up the next ' + next_label if next_label else ' and resolve the sequence cleanly'}."
            ),
        }

    @classmethod
    def _apply_carousel_continuity_contract(
        cls,
        slides: list[dict[str, Any]],
    ) -> list[dict[str, Any]]:
        # Applies apply carousel continuity contract from slides for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not slides:
            return []
        repaired = [deepcopy(slide) for slide in slides]
        slide_count = len(repaired)
        story_roles = [
            cls._normalize_metadata_text(
                ((slide.get("metadata") or {}) if isinstance(slide.get("metadata"), dict) else {}).get("story_role")
                or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            for slide in repaired
        ]
        for index, slide in enumerate(repaired, start=1):
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            continuity_profile = cls._carousel_continuity_profile(
                slide=slide,
                slide_index=index,
                slide_count=slide_count,
                previous_story_role=story_roles[index - 2] if index > 1 else "",
                next_story_role=story_roles[index] if index < slide_count else "",
            )
            slide_metadata.update(continuity_profile)
            slide["metadata"] = slide_metadata
            if not cls._normalize_metadata_text(slide.get("transition_note"), limit=180):
                slide["transition_note"] = continuity_profile["transition_note"]
        return repaired

    @classmethod
    def _apply_claim_evidence_to_carousel_slides(
        cls,
        slides: list[dict[str, Any]],
        *,
        claim_evidence_pairs: list[dict[str, str]],
    ) -> list[dict[str, Any]]:
        # Applies apply claim evidence carousel slides from slides and claim evidence pairs for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not slides or not claim_evidence_pairs:
            return slides
        repaired = [deepcopy(slide) for slide in slides]
        slot_views = []
        for slide in repaired:
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            slot_views.append(
                {
                    "story_role": slide_metadata.get("story_role") or slide.get("role"),
                    "headline": slide.get("headline"),
                    "supporting_line": slide.get("supporting_line"),
                    "body": slide.get("body"),
                    "body_points": slide.get("body_points"),
                    "proof_points": slide.get("proof_points"),
                    "stat_highlights": slide.get("stat_highlights"),
                }
            )
        allocations = cls._allocate_claim_evidence_pairs_to_slots(
            slot_views,
            claim_evidence_pairs=claim_evidence_pairs,
            format_family="carousel",
        )
        for index, slide in enumerate(repaired):
            assigned_pairs = allocations[index] if index < len(allocations) else []
            slide["claim_evidence_pairs"] = assigned_pairs
            claim_lines = cls._claim_evidence_pair_lines(assigned_pairs, limit=2)
            if not claim_lines:
                continue
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            story_role = cls._normalize_metadata_text(
                slide_metadata.get("story_role") or slide.get("role"),
                limit=48,
            ).casefold().replace(" ", "_")
            existing_proof_points = cls._normalize_metadata_list(slide.get("proof_points"), limit=3)
            existing_body_points = cls._normalize_metadata_list(slide.get("body_points"), limit=3)
            if story_role in {"hook", "cover", "opening", "title"}:
                slide["proof_points"] = []
                slide["body_points"] = existing_body_points[:1]
            elif story_role in {"implications", "implication", "strategic_meaning", "analysis", "what_matters", "takeaway", "close", "closing", "cta", "final"}:
                slide["proof_points"] = []
                slide["body_points"] = cls._dedupe_metadata_collection(
                    [
                        *existing_body_points,
                        *claim_lines,
                    ],
                    blocked_texts=[slide.get("headline"), slide.get("supporting_line")],
                    limit=2,
                )
            else:
                slide["proof_points"] = cls._dedupe_metadata_collection(
                    [
                        *existing_proof_points,
                        *claim_lines,
                    ],
                    blocked_texts=[slide.get("headline"), slide.get("supporting_line"), slide.get("cta")],
                    limit=3,
                )
                slide["body_points"] = cls._dedupe_metadata_collection(
                    [
                        *existing_body_points,
                        *claim_lines,
                    ],
                    blocked_texts=[slide.get("headline"), slide.get("supporting_line")],
                    limit=3,
                )
            if story_role in {
                "structure",
                "deal_structure",
                "what_happened",
                "mechanics",
                "breakdown",
                "undercovered_angle",
                "missed_angle",
                "angle",
                "strategic_meaning",
                "implications",
                "implication",
                "analysis",
                "what_matters",
            }:
                body_text = cls._normalize_metadata_text(slide.get("body"), limit=320)
                supporting = cls._normalize_metadata_text(slide.get("supporting_line"), limit=220)
                if not body_text or body_text.casefold() == supporting.casefold():
                    slide["body"] = claim_lines[0]
        return repaired

    @classmethod
    def _fallback_carousel_slide_specs(
        cls,
        *,
        text_payload: StructuredTextPayload,
        request: AIOrchestrationRequest | None,
        creative_decision: CreativeDecisionPayload | None,
        metadata: dict[str, Any],
        headline: str,
        body_sentences: list[str],
        supporting_line: str,
        proof_points: list[str],
        stat_highlights: list[str],
        target_slide_count: int,
        sequence_pack_slides: list[dict[str, Any]],
    ) -> list[dict[str, Any]]:
        # Centralizes fallback carousel slide specs from copy payload, request state, and creative decision for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        mistake_style = cls._has_mistake_carousel_signals(
            headline,
            supporting_line,
            text_payload.body,
            metadata.get("section_label"),
            *proof_points,
            *stat_highlights,
        )
        if mistake_style:
            # Mistake carousels need a different rhythm: cover, one or more mistake/fix details, then a closing slide.
            wants_multiple = cls._prefers_multiple_mistake_slides(
                headline,
                supporting_line,
                text_payload.body,
                *proof_points,
                *stat_highlights,
            )
            mistake_groups = cls._mistake_detail_groups(
                headline=headline,
                supporting_line=supporting_line,
                proof_points=proof_points,
                stat_highlights=stat_highlights,
                body_sentences=body_sentences,
            )
            minimum_detail_count = 3 if wants_multiple else 1
            detail_slot_count = (
                max(target_slide_count - 2, len(mistake_groups), minimum_detail_count)
                if target_slide_count
                else max(len(mistake_groups), minimum_detail_count)
            )
            # Fill missing mistake groups from the available proof lines so the carousel still has enough middle slides.
            while len(mistake_groups) < detail_slot_count:
                fallback_index = len(mistake_groups) + 1
                fallback_focus = (
                    cls._strip_mistake_markers(
                        stat_highlights[fallback_index - 1]
                        if fallback_index - 1 < len(stat_highlights)
                        else proof_points[min((fallback_index - 1) * 2, len(proof_points) - 1)] if proof_points else supporting_line or headline
                    )
                    or cls._strip_mistake_markers(supporting_line)
                    or cls._strip_mistake_markers(headline)
                )
                heuristic_points = []
                for point in proof_points[(fallback_index - 1) * 2 : (fallback_index - 1) * 2 + 4]:
                    normalized = cls._normalize_metadata_text(point, limit=180)
                    if normalized and not cls._is_promotional_line(normalized):
                        heuristic_points.append(normalized)
                mistake_groups.append(
                    {
                        "headline": cls._infer_mistake_headline(fallback_focus, fallback_focus=fallback_focus, index=fallback_index),
                        "supporting_line": cls._mistake_supporting_line(
                            *body_sentences[(fallback_index - 1) * 2 : (fallback_index - 1) * 2 + 2],
                            supporting_line,
                        ),
                        "proof_points": heuristic_points,
                    }
                )
            cover_headline = headline
            if not cover_headline or cls._is_generic_carousel_education_label(cover_headline):
                cover_headline = cls._mistake_cover_headline(headline, supporting_line, text_payload.body, *proof_points, *stat_highlights)
            slides = [
                {
                    "role": "hook",
                    "headline": cover_headline or headline,
                    "supporting_line": supporting_line,
                    "proof_points": [],
                    "cta": "",
                }
            ]
            for index, group in enumerate(mistake_groups[:detail_slot_count], start=1):
                points = cls._normalize_metadata_list(group.get("proof_points"), limit=3)
                impact_lines = [point for point in points if cls.IMPACT_LINE_PATTERN.match(point)]
                fix_lines = [point for point in points if cls.FIX_LINE_PATTERN.match(point)]
                # Impact/fix lines are prioritized because they make each mistake slide actionable instead of just descriptive.
                if not impact_lines:
                    inferred_impact = next(
                        (
                            cls._normalize_metadata_text(sentence, limit=180)
                            for sentence in body_sentences
                            if cls.IMPACT_LINE_PATTERN.match(cls._normalize_metadata_text(sentence, limit=180))
                        ),
                        "",
                    )
                    if inferred_impact:
                        impact_lines.append(inferred_impact)
                if not fix_lines:
                    inferred_fix = next(
                        (
                            cls._normalize_metadata_text(sentence, limit=180)
                            for sentence in body_sentences
                            if cls.FIX_LINE_PATTERN.match(cls._normalize_metadata_text(sentence, limit=180))
                        ),
                        "",
                    )
                    if inferred_fix:
                        fix_lines.append(inferred_fix)
                detail_points = [*impact_lines[:1], *fix_lines[:1]]
                remaining_points = [point for point in points if point not in detail_points]
                detail_points.extend(remaining_points[: max(0, 3 - len(detail_points))])
                slides.append(
                    {
                        "role": "detail",
                        "headline": cls._normalize_metadata_text(group.get("headline"), limit=90)
                        or cls._infer_mistake_headline("", fallback_focus=supporting_line or headline, index=index),
                        "supporting_line": cls._mistake_supporting_line(group.get("supporting_line"), supporting_line),
                        "proof_points": detail_points[:3],
                        "cta": "",
                    }
                )
            closing_headline = cls._normalize_metadata_text(
                text_payload.cta or supporting_line or headline,
                limit=120,
            ) or headline
            closing_line = cls._normalize_metadata_text(
                next(
                    (
                        candidate
                        for candidate in [
                            supporting_line,
                            *(sentence for sentence in reversed(body_sentences) if sentence.strip()),
                            text_payload.body,
                        ]
                        if cls._normalize_metadata_text(candidate, limit=200)
                        and cls._normalize_metadata_text(candidate, limit=200).casefold() != closing_headline.casefold()
                    ),
                    "",
                ),
                limit=200,
            )
            slides.append(
                {
                    "role": "closing",
                    "headline": closing_headline,
                    "supporting_line": closing_line,
                    "proof_points": [],
                    "cta": cls._normalize_metadata_text(text_payload.cta or closing_headline, limit=90),
                }
            )
            return slides

        outline_slides = cls._outline_to_carousel_slides(
            cls._carousel_outline_candidates(request, metadata),
            headline=headline,
            supporting_line=supporting_line,
            cta=cls._normalize_metadata_text(text_payload.cta, limit=90),
            proof_points=proof_points,
            stat_highlights=stat_highlights,
            body_sentences=body_sentences,
            request=request,
            target_slide_count=target_slide_count or len(sequence_pack_slides),
            carousel_archetype=cls._carousel_infer_archetype(request=request, metadata=metadata),
        )
        if outline_slides:
            # User or model-supplied outlines are respected first because they carry intentional slide ordering.
            return outline_slides

        detail_segments = cls._collapse_carousel_segments(
            body_sentences,
            max_slides=max(target_slide_count - 2, 1) if target_slide_count else max(len(body_sentences), 1),
        )
        if not detail_segments:
            # Proof points become the fallback middle-slide source when the body has no usable sentence structure.
            detail_segments = cls._collapse_carousel_segments(proof_points, max_slides=max(target_slide_count - 2, 1) if target_slide_count else max(len(proof_points), 1))
        unique_support_candidates = cls._normalize_metadata_list(
            [
                *detail_segments,
                *stat_highlights,
                *proof_points,
                supporting_line,
                headline,
            ],
            limit=max((target_slide_count or 4) * 3, 8),
        )
        include_intro_slide = bool(
            (target_slide_count and target_slide_count >= 5)
            or (len(stat_highlights) > 1 and len(detail_segments) > 2)
        )
        # Longer, evidence-heavy carousels get an intro slide to set context before the detail sequence starts.
        detail_slot_count = (
            max(target_slide_count - (3 if include_intro_slide else 2), len(detail_segments), 1)
            if target_slide_count
            else max(len(detail_segments), 1)
        )
        while len(detail_segments) < detail_slot_count and unique_support_candidates:
            next_candidate = next(
                (
                    candidate
                    for candidate in unique_support_candidates
                    if candidate and candidate not in detail_segments and candidate.casefold() != supporting_line.casefold()
                ),
                "",
            )
            if not next_candidate:
                break
            detail_segments.append(next_candidate)
        slides = [
            {
                "role": "cover",
                "headline": headline,
                "supporting_line": supporting_line,
                "proof_points": proof_points[: min(len(proof_points), 3) or 0],
                "cta": "",
            }
        ]
        if include_intro_slide:
            intro_headline = cls._normalize_metadata_text(stat_highlights[0] if stat_highlights else "", limit=120)
            if not intro_headline or cls._is_generic_carousel_education_label(intro_headline):
                intro_headline = "What changed"
            intro_support = next(
                (
                    candidate
                    for candidate in unique_support_candidates
                    if candidate.casefold() not in {headline.casefold(), supporting_line.casefold()}
                ),
                supporting_line,
            )
            slides.append(
                {
                    "role": "intro",
                    "headline": intro_headline,
                    "supporting_line": intro_support,
                    "proof_points": [
                        point
                        for point in proof_points[:3]
                        if point.casefold() != cls._normalize_metadata_text(intro_support, limit=180).casefold()
                    ][:3],
                    "cta": "",
                }
            )

        used_titles = {headline.casefold()} if headline else set()
        used_supports = {supporting_line.casefold()} if supporting_line else set()
        for index, segment in enumerate(detail_segments[:detail_slot_count], start=1):
            segment_text = cls._normalize_metadata_text(segment, limit=220)
            title_candidates = [
                stat_highlights[index - 1] if index - 1 < len(stat_highlights) else "",
                proof_points[(index - 1) * 2] if (index - 1) * 2 < len(proof_points) else "",
                segment_text,
            ]
            detail_title = next(
                (
                    cls._normalize_metadata_text(candidate, limit=90)
                    for candidate in title_candidates
                    if cls._normalize_metadata_text(candidate, limit=90)
                    and not cls._is_generic_carousel_education_label(candidate)
                    and cls._normalize_metadata_text(candidate, limit=90).casefold() not in used_titles
                ),
                "",
            )
            if not detail_title:
                detail_title = cls._carousel_role_title("detail", headline=headline, cta=text_payload.cta)
                if index > 1:
                    detail_title = f"{detail_title} {index}"
            used_titles.add(detail_title.casefold())
            detail_support = segment_text
            if not detail_support or detail_support.casefold() in used_supports:
                detail_support = next(
                    (
                        candidate
                        for candidate in unique_support_candidates
                        if candidate.casefold() not in used_supports
                    ),
                    supporting_line or headline,
                )
            used_supports.add(detail_support.casefold())
            detail_points = [
                point
                for point in proof_points[(index - 1) * 2 : (index - 1) * 2 + 3]
                if cls._normalize_metadata_text(point, limit=180)
                and cls._normalize_metadata_text(point, limit=180).casefold() != detail_support.casefold()
            ]
            if not detail_points:
                detail_points = [
                    candidate
                    for candidate in unique_support_candidates
                    if candidate.casefold() != detail_support.casefold()
                ][:2]
            slides.append(
                {
                    "role": "detail",
                    "headline": detail_title,
                    "supporting_line": detail_support,
                    "proof_points": detail_points[:3],
                    "cta": "",
                }
            )

        closing_headline = cls._normalize_metadata_text(text_payload.cta or headline, limit=120) or headline
        closing_line = next(
            (
                candidate
                for candidate in reversed(unique_support_candidates)
                if candidate
                and candidate.casefold() not in {closing_headline.casefold(), (text_payload.cta or "").casefold()}
                and candidate.casefold() not in used_supports
            ),
            "",
        )
        if not closing_line:
            closing_line = next(
                (
                    candidate
                    for candidate in reversed(unique_support_candidates)
                    if candidate
                    and candidate.casefold() not in {closing_headline.casefold(), (text_payload.cta or "").casefold()}
                ),
                supporting_line or headline,
            )
        closing_points = [
            candidate
            for candidate in [*stat_highlights[-2:], *proof_points[-2:]]
            if cls._normalize_metadata_text(candidate, limit=180)
            and cls._normalize_metadata_text(candidate, limit=180).casefold() != cls._normalize_metadata_text(closing_line, limit=180).casefold()
        ][:3]
        slides.append(
            {
                "role": "closing",
                "headline": closing_headline,
                "supporting_line": cls._normalize_metadata_text(closing_line, limit=200),
                "proof_points": closing_points,
                "cta": cls._normalize_metadata_text(text_payload.cta, limit=90),
            }
        )
        return slides

    @classmethod
    def _carousel_semantic_slide_contracts(
        cls,
        *,
        compiled_context: dict[str, Any] | None,
        request: AIOrchestrationRequest | None = None,
        slide_count: int = 0,
    ) -> list[dict[str, Any]]:
        # Centralizes carousel semantic slide contracts from compiled context, request state, and slide count for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        content_plan = (
            compiled_context.get("content_plan")
            if isinstance(compiled_context, dict) and isinstance(compiled_context.get("content_plan"), dict)
            else request.content_plan
            if request is not None and isinstance(request.content_plan, dict)
            else {}
        )
        research_brief = (
            compiled_context.get("research_editorial_brief")
            if isinstance(compiled_context, dict) and isinstance(compiled_context.get("research_editorial_brief"), dict)
            else request.research_editorial_brief
            if request is not None and isinstance(request.research_editorial_brief, dict)
            else {}
        )
        raw_contracts = content_plan.get("carousel_slide_contracts") if isinstance(content_plan, dict) else []
        if not raw_contracts:
            semantic_plan = research_brief.get("semantic_carousel_plan") if isinstance(research_brief, dict) and isinstance(research_brief.get("semantic_carousel_plan"), dict) else {}
            raw_contracts = semantic_plan.get("story_map") if isinstance(semantic_plan.get("story_map"), list) else []
        contracts: list[dict[str, Any]] = []
        for item in (raw_contracts or [])[: max(slide_count or 0, 12)]:
            if not isinstance(item, dict):
                continue
            contract = {
                "slide_number": len(contracts) + 1,
                "role": cls._normalize_metadata_text(item.get("role") or item.get("story_role"), limit=48),
                "purpose": cls._normalize_metadata_text(item.get("purpose") or item.get("headline_intent"), limit=220),
                "notes": cls._normalize_metadata_text(item.get("notes"), limit=180),
                "section_focus": cls._normalize_metadata_text(item.get("section_focus"), limit=140),
                "representation_hint": cls._normalize_metadata_text(
                    item.get("representation_hint") or item.get("representation_intent"),
                    limit=48,
                ).casefold().replace(" ", "_"),
            }
            if any(value for key, value in contract.items() if key != "slide_number"):
                contracts.append(contract)
        return contracts[:slide_count] if slide_count and contracts else contracts

    @classmethod
    def _carousel_required_concepts_from_contract(cls, contract: dict[str, Any]) -> list[str]:
        # Builds carousel required concepts contract from contract for AIOrchestratorService.
        # It calls _dedupe_metadata_collection and _normalize_metadata_text while assembling the payload or prompt text.
        concepts: list[str] = []
        for value in (contract.get("section_focus"), contract.get("purpose"), contract.get("notes")):
            text = cls._normalize_metadata_text(value, limit=140)
            if text and not cls._looks_like_carousel_instruction_text(text):
                concepts.append(text)
        return cls._dedupe_metadata_collection(concepts, blocked_texts=[], limit=6)

    @classmethod
    def _slide_preserved_required_concepts(
        cls,
        slide: dict[str, Any],
        required_concepts: list[str],
    ) -> tuple[list[str], list[str]]:
        # Centralizes slide preserved required concepts from slide and required concepts for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        visible_text = " ".join(
            [
                cls._normalize_metadata_text(slide.get("headline"), limit=140),
                cls._normalize_metadata_text(slide.get("supporting_line"), limit=220),
                cls._normalize_metadata_text(slide.get("body"), limit=320),
                *cls._normalize_metadata_list(slide.get("body_points"), limit=8),
                *cls._normalize_metadata_list(slide.get("proof_points"), limit=8),
                *cls._normalize_metadata_list(slide.get("stat_highlights"), limit=6),
            ]
        )
        preserved: list[str] = []
        dropped: list[str] = []
        for concept in required_concepts:
            if cls._carousel_texts_semantically_overlap(visible_text, concept):
                preserved.append(concept)
            else:
                dropped.append(concept)
        return preserved, dropped

    @classmethod
    def _attach_carousel_story_contracts(
        cls,
        slides: list[dict[str, Any]],
        *,
        story_contracts: list[dict[str, Any]],
    ) -> list[dict[str, Any]]:
        # Centralizes attach carousel story contracts from slides and story contracts for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not story_contracts:
            return [dict(slide) for slide in slides if isinstance(slide, dict)]
        attached: list[dict[str, Any]] = []
        for index, slide in enumerate(slides, start=1):
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            metadata = dict(slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {})
            contract = story_contracts[index - 1] if index - 1 < len(story_contracts) else {}
            if contract:
                role = cls._normalize_metadata_text(contract.get("role"), limit=48)
                representation_hint = cls._normalize_metadata_text(contract.get("representation_hint"), limit=48)
                required_concepts = cls._carousel_required_concepts_from_contract(contract)
                preserved, dropped = cls._slide_preserved_required_concepts(slide_copy, required_concepts)
                if role:
                    metadata["story_role"] = metadata.get("story_role") or role
                    metadata["planner_story_role"] = role
                if representation_hint:
                    metadata["representation_hint"] = metadata.get("representation_hint") or representation_hint
                    metadata["planner_representation_hint"] = representation_hint
                    metadata["semantic_representation_authority"] = True
                metadata["carousel_story_contract"] = contract
                metadata["source_planner_slide_id"] = f"planner_slide_{index}"
                metadata["required_concepts"] = required_concepts
                metadata["preserved_required_concepts"] = preserved
                metadata["dropped_required_concepts"] = dropped
                metadata["semantic_lineage"] = {
                    "source_planner_slide_id": metadata["source_planner_slide_id"],
                    "preserved_required_concepts": preserved,
                    "dropped_required_concepts": dropped,
                    "semantic_delta_reason": "no_required_concept_loss" if not dropped else "required_concepts_missing_from_visible_copy",
                }
            slide_copy["metadata"] = metadata
            attached.append(slide_copy)
        return attached

    @classmethod
    def _repair_carousel_story_contract_concept_gaps(cls, slides: list[dict[str, Any]]) -> list[dict[str, Any]]:
        # Repairs carousel story contract concept gaps from slides for AIOrchestratorService.
        # It delegates shared cleanup to _normalize_metadata_list and _carousel_texts_semantically_overlap before returning the cleaned value.
        repaired: list[dict[str, Any]] = []
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            metadata = dict(slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {})
            dropped = cls._normalize_metadata_list(metadata.get("dropped_required_concepts"), limit=6)
            if dropped:
                body_points = cls._normalize_metadata_list(slide_copy.get("body_points"), limit=8)
                added: list[str] = []
                for concept in dropped:
                    if any(cls._carousel_texts_semantically_overlap(concept, existing) for existing in body_points):
                        continue
                    body_points.append(concept)
                    added.append(concept)
                    if len(body_points) >= 8:
                        break
                if added:
                    slide_copy["body_points"] = body_points
                    metadata["content_intelligence_repair_applied"] = True
                    metadata["content_intelligence_repair_added_concepts"] = added
            slide_copy["metadata"] = metadata
            repaired.append(slide_copy)
        return repaired

    @classmethod
    def _restore_planner_intent_in_carousel_slides(cls, slides: list[dict[str, Any]]) -> list[dict[str, Any]]:
        # Centralizes restore planner intent in carousel slides from slides for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        restored: list[dict[str, Any]] = []
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            metadata = dict(slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {})
            contract = metadata.get("carousel_story_contract") if isinstance(metadata.get("carousel_story_contract"), dict) else {}
            purpose = cls._normalize_metadata_text(contract.get("purpose"), limit=180)
            if purpose and not cls._normalize_metadata_text(slide_copy.get("supporting_line"), limit=220):
                slide_copy["supporting_line"] = purpose
                metadata["planner_intent_restored_fields"] = ["supporting_line"]
            slide_copy["metadata"] = metadata
            restored.append(slide_copy)
        return restored

    @classmethod
    def _carousel_story_pacing_resolution(
        cls,
        *,
        story_contracts: list[dict[str, Any]],
        prompt_requested_slide_count: int | None,
        preferred_slide_count: int | None,
        initial_target_slide_count: int,
        structured_slide_count: int,
        outline_slide_count: int,
        sequence_pack_slide_count: int,
    ) -> dict[str, Any]:
        # Centralizes carousel story pacing resolution from story contracts, prompt requested slide count, and preferred slide count for AIOrchestratorS.
        # The helper owns a small rule that would distract from the surrounding flow.
        contract_slide_count = len([item for item in story_contracts if isinstance(item, dict)])
        required_slide_count = max(
            contract_slide_count,
            int(prompt_requested_slide_count or 0),
            int(outline_slide_count or 0),
            int(structured_slide_count or 0),
        )
        return {
            "required_slide_count": required_slide_count,
            "selected_slide_count": max(int(initial_target_slide_count or 0), required_slide_count or 0),
            "compression_allowed": not bool(preferred_slide_count and required_slide_count and int(preferred_slide_count) < required_slide_count),
            "evidence_counts": {
                "planner_contract_slide_count": contract_slide_count,
                "prompt_requested_slide_count": int(prompt_requested_slide_count or 0),
                "preferred_slide_count": int(preferred_slide_count or 0),
                "initial_target_slide_count": int(initial_target_slide_count or 0),
                "structured_slide_count": int(structured_slide_count or 0),
                "outline_slide_count": int(outline_slide_count or 0),
                "sequence_pack_slide_count": int(sequence_pack_slide_count or 0),
            },
        }

    @classmethod
    def _apply_carousel_sequence_label_contracts(cls, slides: list[dict[str, Any]]) -> list[dict[str, Any]]:
        # Applies apply carousel sequence label contracts from slides for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        repaired: list[dict[str, Any]] = []
        slide_count = len([slide for slide in slides if isinstance(slide, dict)])
        for index, slide in enumerate(slides, start=1):
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            metadata = dict(slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {})
            metadata["sequence_label_contract"] = {
                "contract_version": "1.0",
                "status": "pass",
                "slide_index": index,
                "slide_count": slide_count,
                "expected_number": index,
                "render_sequence_label": True,
                "expected_visible_label": f"{index}",
                "source": "content_intelligence_sequence",
            }
            slide_copy["metadata"] = metadata
            repaired.append(slide_copy)
        return repaired

    @classmethod
    def _carousel_sequence_coherence_report(cls, slides: list[dict[str, Any]]) -> dict[str, Any]:
        # Centralizes carousel sequence coherence report from slides for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        roles: list[str] = []
        repeated: list[str] = []
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            role = cls._carousel_story_role_token(metadata.get("story_role") or slide.get("role"))
            if role in roles and role not in {"detail", "list_item", "comparison_item", "feature_cluster"}:
                repeated.append(role)
            roles.append(role)
        issues = []
        if roles and roles[0] not in {"hook", "cover", "opening", "title", "problem_frame"}:
            issues.append("missing_opening_role")
        if roles and roles[-1] not in {"takeaway", "close", "closing", "cta", "final", "value_close"}:
            issues.append("missing_closing_role")
        if repeated:
            issues.append("repeated_story_roles")
        return {"status": "pass" if not issues else "needs_attention", "slide_count": len(roles), "roles": roles, "issues": issues, "repeated_roles": repeated[:6]}

    @classmethod
    def _carousel_slide_representation_plan(
        cls,
        slide: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None = None,
    ) -> dict[str, Any]:
        # Builds carousel slide representation plan from slide and request state for AIOrchestratorService.
        # It calls _carousel_story_role_token and _normalize_metadata_text while assembling the payload or prompt text.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        local_text = " ".join(
            [
                cls._normalize_metadata_text(slide.get("headline"), limit=140),
                cls._normalize_metadata_text(slide.get("supporting_line"), limit=220),
                cls._normalize_metadata_text(slide.get("body"), limit=320),
                cls._normalize_metadata_text(slide.get("visual_focus"), limit=320),
                *cls._normalize_metadata_list(slide.get("body_points"), limit=8),
                *cls._normalize_metadata_list(slide.get("proof_points"), limit=8),
                *cls._normalize_metadata_list(slide.get("stat_highlights"), limit=6),
            ]
        ).casefold()
        story_role = cls._carousel_story_role_token(metadata.get("story_role") or slide.get("role"))
        representation_hint = cls._normalize_metadata_text(metadata.get("representation_hint"), limit=48).casefold().replace(" ", "_")
        representation = representation_hint or "editorial_explainer"
        evidence: list[str] = []
        if representation_hint:
            evidence.append("semantic representation hint")
        elif story_role in {"hook", "cover", "opening", "title", "problem_frame"}:
            representation = "hero_metaphor"
            evidence.append("opening hook")
        elif re.search(r"\b(process|workflow|steps?|sequence|path|mechanism|how it works)\b", local_text):
            representation = "process_path"
            evidence.append("process signal")
        elif re.search(r"\b(table|matrix|rows?|columns?|scenario)\b", local_text):
            representation = "table"
            evidence.append("table signal")
        elif cls._data_visualization_requested(local_text):
            representation = "chart"
            evidence.append("data/chart signal")
        elif re.search(r"\b(compare|comparison|versus|vs\.?|option|trade[- ]off)\b", local_text):
            representation = "comparison_grid"
            evidence.append("comparison signal")
        elif story_role in {"takeaway", "close", "closing", "cta", "final", "value_close"}:
            representation = "closing_question"
            evidence.append("closing beat")
        return {
            "slide_index": int(slide.get("slide_index") or 0),
            "story_role": story_role,
            "requested_representation": representation,
            "selected_representation": representation,
            "status": "pass",
            "sample_capability_blocked": False,
            "evidence": evidence[:5],
            "representation_decision": {
                "requested_representation": representation,
                "semantic_best_representation": representation,
                "selected_representation": representation,
                "semantic_loss_score": 0.0,
                "fallback_required": False,
            },
        }

    @classmethod
    def _representation_hint_matches_visual_focus(cls, representation_hint: Any, visual_focus: Any) -> bool:
        # Centralizes representation hint visual focus from representation hint and visual focus for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        hint = cls._normalize_metadata_text(representation_hint, limit=48).casefold().replace(" ", "_")
        focus = cls._normalize_metadata_text(visual_focus, limit=320).casefold()
        if not hint or not focus:
            return False
        aliases = {
            "process_path": ("process", "path", "workflow", "steps", "mechanism"),
            "hero_metaphor": ("hero", "metaphor", "symbol", "dominant"),
            "comparison_grid": ("comparison", "grid", "versus", "options"),
            "closing_question": ("question", "closing", "cta", "decision"),
            "product_decision_surface": ("dashboard", "surface", "decision", "interface"),
            "icon_row": ("icon", "row", "modules"),
        }
        return hint.replace("_", " ") in focus or any(token in focus for token in aliases.get(hint, ()))

    @classmethod
    def _carousel_visual_systems_for_representation(cls, representation: str, *, story_role: str = "") -> list[str]:
        # Centralizes carousel visual systems representation from representation and story role for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        selected = cls._normalize_metadata_text(representation, limit=64).casefold() or "editorial_explainer"
        systems_by_representation = {
            "hero_metaphor": ["dominant hero object", "headline-safe negative space"],
            "process_path": ["step path", "connector modules", "sequence markers"],
            "table": ["table surface", "row hierarchy", "data-safe cells"],
            "chart": ["chart surface", "axis-safe data region", "callout labels"],
            "comparison_grid": ["comparison cards", "parallel columns", "decision lens"],
            "closing_question": ["decision-support close", "restrained CTA-safe region"],
            "product_decision_surface": ["dashboard surface", "metric modules", "decision cards"],
            "icon_row": ["icon row", "modular evidence cells"],
            "editorial_explainer": ["editorial hero region", "evidence callout modules"],
        }
        systems = list(systems_by_representation.get(selected, systems_by_representation["editorial_explainer"]))
        if story_role in {"hook", "cover", "opening", "title", "problem_frame"} and "headline-safe negative space" not in systems:
            systems.append("headline-safe negative space")
        return systems[:5]

    @classmethod
    def _carousel_slide_visual_execution_contract(
        cls,
        slide: dict[str, Any],
        *,
        request: AIOrchestrationRequest | None = None,
    ) -> dict[str, Any]:
        # Builds carousel slide visual execution contract from slide and request state for AIOrchestratorService.
        # It calls _carousel_story_role_token and _carousel_slide_representation_plan while assembling the payload or prompt text.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        representation_plan = metadata.get("representation_plan") if isinstance(metadata.get("representation_plan"), dict) else cls._carousel_slide_representation_plan(slide, request=request)
        story_role = cls._carousel_story_role_token(metadata.get("story_role") or slide.get("role"))
        selected_representation = cls._normalize_metadata_text(representation_plan.get("selected_representation") or representation_plan.get("requested_representation"), limit=64).casefold() or "editorial_explainer"
        slide_index = int(slide.get("slide_index") or 0)
        slide_count = int(slide.get("slide_count") or 0)
        sequence_position = "opening" if slide_index <= 1 else "closing" if slide_count and slide_index >= slide_count else "middle"
        minimum_visual_modules = 1 if sequence_position == "opening" else 2
        if selected_representation in {"table", "formula", "chart", "comparison_grid", "process_path", "icon_row", "product_decision_surface"}:
            minimum_visual_modules = max(minimum_visual_modules, 3 if sequence_position == "middle" else 2)
        return {
            "slide_index": slide_index,
            "story_role": story_role,
            "sequence_position": sequence_position,
            "selected_representation": selected_representation,
            "minimum_visual_modules": minimum_visual_modules,
            "module_density_target": "premium_multi_module_3_to_5_surfaces" if minimum_visual_modules >= 3 else "balanced_editorial_surfaces",
            "surface_treatment": "structured_surface_priority" if minimum_visual_modules >= 2 else "hero_or_editorial_priority",
            "preferred_visual_systems": cls._carousel_visual_systems_for_representation(selected_representation, story_role=story_role),
            "anti_flat_layout_guard": f"Do not flatten this slide into a generic poster; render at least {minimum_visual_modules} structured visual module(s).",
        }

    @classmethod
    def _attach_carousel_visual_execution_contracts(cls, slides: list[dict[str, Any]], *, request: AIOrchestrationRequest | None = None) -> list[dict[str, Any]]:
        # Centralizes attach carousel visual execution contracts from slides and request state for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        attached: list[dict[str, Any]] = []
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            copied = dict(slide)
            metadata = dict(copied.get("metadata") if isinstance(copied.get("metadata"), dict) else {})
            representation_plan = cls._carousel_slide_representation_plan(copied, request=request)
            metadata["representation_plan"] = representation_plan
            if isinstance(representation_plan.get("representation_decision"), dict):
                metadata["representation_decision"] = representation_plan["representation_decision"]
            metadata["visual_execution_contract"] = cls._carousel_slide_visual_execution_contract({**copied, "metadata": metadata}, request=request)
            copied["metadata"] = metadata
            attached.append(copied)
        return attached

    @classmethod
    def _carousel_render_execution_contract(cls, slide: dict[str, Any]) -> dict[str, Any]:
        # Builds carousel execution contract from slide for AIOrchestratorService.
        # It calls _normalize_metadata_text and _normalize_metadata_list while assembling the payload or prompt text.
        metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        visible_copy = {
            key: value
            for key, value in {
                "headline": cls._normalize_metadata_text(slide.get("headline"), limit=140),
                "supporting_line": cls._normalize_metadata_text(slide.get("supporting_line"), limit=220),
                "body": cls._normalize_metadata_text(slide.get("body"), limit=320),
                "body_points": cls._normalize_metadata_list(slide.get("body_points"), limit=8),
                "proof_points": cls._normalize_metadata_list(slide.get("proof_points"), limit=8),
                "stat_highlights": cls._normalize_metadata_list(slide.get("stat_highlights"), limit=6),
                "cta": cls._normalize_metadata_text(slide.get("cta"), limit=90),
            }.items()
            if value
        }
        representation_decision = metadata.get("representation_decision") if isinstance(metadata.get("representation_decision"), dict) else {}
        return {
            "contract_version": "1.0",
            "canvas": {"slide_index": int(slide.get("slide_index") or 0), "slide_count": int(slide.get("slide_count") or 0)},
            "visible_copy": visible_copy,
            "copy_hierarchy": [key for key in ("headline", "supporting_line", "body", "body_points", "proof_points", "stat_highlights", "cta") if visible_copy.get(key)],
            "visual_modules": {
                "representation": representation_decision.get("selected_representation") or metadata.get("representation_hint") or "",
                "semantic_best_representation": representation_decision.get("semantic_best_representation") or "",
                "semantic_loss_score": representation_decision.get("semantic_loss_score"),
            },
            "sequence_label": metadata.get("sequence_label_contract") if isinstance(metadata.get("sequence_label_contract"), dict) else {},
            "negative_constraints": [
                "do not render internal strategy/planner/reference notes as visible copy",
                "do not invent extra readable copy beyond visible_copy",
                "do not duplicate approved readable text",
            ],
        }

    @classmethod
    def _attach_carousel_render_execution_contracts(cls, slides: list[dict[str, Any]]) -> list[dict[str, Any]]:
        # Centralizes attach carousel execution contracts from slides for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        attached: list[dict[str, Any]] = []
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            slide_copy = dict(slide)
            metadata = dict(slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {})
            metadata["render_execution_contract"] = cls._carousel_render_execution_contract(slide_copy)
            slide_copy["metadata"] = metadata
            attached.append(slide_copy)
        return attached

    @classmethod
    def _apply_content_intelligence_carousel_contracts(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        story_contracts: list[dict[str, Any]],
        pacing_resolution: dict[str, Any],
    ) -> list[dict[str, Any]]:
        # Applies apply content intelligence carousel contracts from slides, request state, and story contracts for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        contracted = cls._attach_carousel_story_contracts(slides, story_contracts=story_contracts)
        contracted = cls._repair_carousel_story_contract_concept_gaps(contracted)
        contracted = cls._restore_planner_intent_in_carousel_slides(contracted)
        contracted = cls._apply_carousel_sequence_label_contracts(contracted)
        contracted = cls._attach_carousel_visual_execution_contracts(contracted, request=request)
        contracted = cls._attach_carousel_render_execution_contracts(contracted)
        coherence_report = cls._carousel_sequence_coherence_report(contracted)
        attached: list[dict[str, Any]] = []
        for index, slide in enumerate(contracted, start=1):
            slide_copy = dict(slide)
            metadata = dict(slide_copy.get("metadata") if isinstance(slide_copy.get("metadata"), dict) else {})
            metadata["content_intelligence_enabled"] = True
            metadata["story_pacing_resolution"] = pacing_resolution
            metadata["sequence_coherence_report"] = coherence_report if index == 1 else {"status": coherence_report.get("status"), "slide_count": coherence_report.get("slide_count"), "issues": coherence_report.get("issues", [])}
            slide_copy["metadata"] = metadata
            attached.append(slide_copy)
        return attached

    @classmethod
    def _pinned_template_subject_anchor(cls, *values: Any) -> str:
        # Centralizes pinned template subject anchor for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        for value in values:
            text = cls._normalize_metadata_text(value, limit=180).strip(" .")
            if not text:
                continue
            subject = cls._carousel_headline_subject(text)
            if subject:
                return subject
            text = re.sub(
                r"^(?:create|write|generate|make|draft|design)\s+(?:a|an|the)?\s*"
                r"(?:(?:linkedin|instagram|facebook|social)\s+)?"
                r"(?:carousel|post|slides?|deck|creative|content)\s+(?:about|on|for)\s+",
                "",
                text,
                flags=re.IGNORECASE,
            ).strip(" .,:;-")
            text = re.sub(r"^(?:about|on|for)\s+", "", text, flags=re.IGNORECASE).strip(" .,:;-")
            words = [
                word
                for word in text.split()
                if word.casefold() not in cls.PROMPT_ECHO_STOPWORDS
            ]
            if words:
                return " ".join(words[:7]).strip(" .,:;-")
        return ""

    @classmethod
    def _pinned_template_headline_needs_story_hook(cls, value: Any, *, source_headline: str = "") -> bool:
        # Centralizes pinned template headline needs story hook from input value and source headline for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        text = cls._normalize_metadata_text(value, limit=140)
        if not text:
            return True
        lowered = text.casefold().strip(" .")
        source = cls._normalize_metadata_text(source_headline, limit=140).casefold().strip(" .")
        if source and lowered == source:
            return True
        if cls._is_generic_carousel_education_label(text):
            return True
        generic_lines = {
            "why this matters now",
            "what to know next",
            "what happened",
            "key insight",
            "introduction",
            "overview",
            "the key takeaway",
        }
        if lowered in generic_lines:
            return True
        if re.match(r"^(?:create|write|generate|make|draft)\b", lowered):
            return True
        has_hook_language = bool(
            re.search(
                r"\b(hidden|silent|surprising|signal|shift|tension|risk|cost|truth|missed|unseen|inside|behind|next)\b",
                lowered,
            )
            or "?" in text
        )
        return len(text.split()) <= 5 and not has_hook_language

    @classmethod
    def _carousel_visual_focus_is_generic(cls, value: Any) -> bool:
        # Centralizes carousel visual focus generic from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        text = cls._normalize_metadata_text(value, limit=260).casefold()
        if not text:
            return True
        generic_patterns = (
            r"\bgeneric\b",
            r"\bbusiness icons?\b",
            r"\bflat icons?\b",
            r"\bvector icons?\b",
            r"\bsmall scattered icons?\b",
            r"\bclean (?:and )?inviting graphic\b",
            r"\babstract illustration\b",
            r"\bdecorative illustration\b",
            r"\bplatform interface\b",
            r"\bdashboard graphic\b",
        )
        return any(re.search(pattern, text) for pattern in generic_patterns)

    @classmethod
    def _dynamic_pinned_template_hook_headline(
        cls,
        *,
        headline: str,
        supporting_line: str,
        prompt: str,
        proof_points: list[str],
        stat_highlights: list[str],
    ) -> str:
        # Centralizes dynamic pinned template hook headline from headline, supporting line, and prompt text for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        subject = cls._pinned_template_subject_anchor(headline, prompt, supporting_line)
        combined = " ".join([headline, supporting_line, prompt, *proof_points[:2], *stat_highlights[:2]]).casefold()
        if not subject:
            return "The shift hiding in plain sight"
        if re.search(r"\b(hidden|silent|missed|overlooked|undercovered|unseen)\b", subject, flags=re.IGNORECASE):
            return subject
        if re.search(r"\b(silent|hidden|missed|overlooked|undercovered|unseen)\b", combined):
            return f"The hidden shift behind {subject}"
        if re.search(r"\b(risk|cost|pressure|threat|crisis|decline|loss)\b", combined):
            return f"The risk inside {subject}"
        if stat_highlights or re.search(r"\b\d", combined):
            return f"The signal inside {subject}"
        if re.search(r"\b(change|shift|reshap\w*|transform|new|next|future)\b", combined):
            return f"The shift behind {subject}"
        return f"What {subject} changes next"

    @classmethod
    def _dynamic_pinned_template_closing_headline(
        cls,
        *,
        headline: str,
        supporting_line: str,
        prompt: str,
        cta: str,
    ) -> str:
        # Centralizes dynamic pinned template closing headline from headline, supporting line, and prompt text for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        subject = cls._pinned_template_subject_anchor(headline, prompt, supporting_line)
        if cta:
            return "The next move is the point"
        if subject:
            return f"What {subject} means next"
        return "What to do with this insight"

    @classmethod
    def _pinned_template_story_roles(cls, slide_count: int) -> list[str]:
        # Centralizes pinned template story roles from slide count for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if slide_count <= 0:
            return []
        if slide_count == 1:
            return ["hook"]
        if slide_count == 2:
            return ["hook", "closing"]
        if slide_count == 3:
            return ["hook", "analysis", "closing"]
        if slide_count == 4:
            return ["hook", "context", "analysis", "closing"]
        if slide_count == 5:
            return ["hook", "context", "key_insight", "analysis", "closing"]
        roles = ["hook", "context"]
        middle_count = slide_count - 3
        middle_pattern = ["key_insight", "analysis", "synthesis"]
        for index in range(middle_count):
            roles.append(middle_pattern[index % len(middle_pattern)])
        roles.append("closing")
        return roles[:slide_count]

    @classmethod
    def _derive_pinned_template_adaptation_profile(cls, slides: list[dict[str, Any]]) -> dict[str, Any]:
        # Derives pinned template adaptation profile from slides for AIOrchestratorService.
        # It calls _normalize_metadata_text and _sample_blueprint_layout_mode to turn raw evidence into the structured signal the caller needs.
        if not slides:
            return {}
        craft_fragments: list[str] = []
        blueprint: dict[str, Any] = {}
        for slide in slides:
            if not isinstance(slide, dict):
                continue
            metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            for key in (
                "reference_visual_craft",
                "reference_editorial_dna",
                "reference_composition_logic",
                "sample_page_copy_behavior",
                "sample_page_editorial_role",
                "sample_page_closing_grammar",
            ):
                fragment = cls._normalize_metadata_text(metadata.get(key), limit=160)
                if fragment:
                    craft_fragments.append(fragment)
            candidate_blueprint = metadata.get("sample_page_blueprint")
            if isinstance(candidate_blueprint, dict) and not blueprint:
                blueprint = cls._sample_blueprint_without_rendered_footer_text(candidate_blueprint)
        counts = blueprint.get("module_counts") if isinstance(blueprint.get("module_counts"), dict) else {}
        layout_mode = cls._sample_blueprint_layout_mode(blueprint) if blueprint else ""
        visual_permissions = cls._sample_visual_permissions(blueprint) if blueprint else {}
        craft_text = " ".join(craft_fragments).casefold()
        large_visual_count = cls._int_or_none(counts.get("large_visual_count")) or 0
        small_icon_count = cls._int_or_none(counts.get("small_icon_like_count")) or 0
        card_count = cls._int_or_none(counts.get("card_like_count")) or 0
        text_count = cls._int_or_none(counts.get("text_block_count")) or 0

        premium_rendered = bool(
            re.search(r"\b(premium|rendered|render|3d|three[- ]?d|dimensional|depth|photoreal|photo[- ]?real|cinematic|studio|composite|glass|shadow)\b", craft_text)
        )
        illustrated = bool(re.search(r"\b(illustration|illustrated|vector|flat|line art|icon)\b", craft_text))
        minimal = bool(re.search(r"\b(minimal|minimalist|sparse|clean|quiet|simple)\b", craft_text))
        if premium_rendered:
            sophistication = "premium_rendered"
        elif illustrated and not premium_rendered:
            sophistication = "illustrated"
        elif minimal:
            sophistication = "minimal"
        elif large_visual_count:
            sophistication = "hero_visual"
        else:
            sophistication = "brand_led"

        hero_strategy = "dominant_hero_visual" if premium_rendered or large_visual_count else "layout_matched_visual_modules"
        if small_icon_count > max(large_visual_count, 0) and not premium_rendered:
            hero_strategy = "sample_matched_icon_or_module_system"
        density = "dense" if (text_count + card_count) >= 5 else "balanced" if (text_count + card_count) >= 3 else "spacious"
        visual_language = cls._normalize_metadata_text(
            " | ".join(
                part
                for part in [
                    f"sophistication={sophistication}",
                    f"hero_strategy={hero_strategy}",
                    f"density={density}",
                    f"layout={layout_mode}",
                    "; ".join(craft_fragments[:3]),
                ]
                if part
            ),
            limit=420,
        )
        return {
            "source": "pinned_template",
            "visual_sophistication": sophistication,
            "hero_strategy": hero_strategy,
            "design_density": density,
            "layout_mode": layout_mode,
            "module_counts": {
                key: value
                for key, value in {
                    "large_visual_count": large_visual_count,
                    "small_icon_like_count": small_icon_count,
                    "card_like_count": card_count,
                    "text_block_count": text_count,
                }.items()
                if value
            },
            "visual_permissions": visual_permissions,
            "visual_language": visual_language,
            "opening_strategy": "start with a dynamic curiosity, tension, urgency, or insight hook derived from the prompt",
            "closing_strategy": "end with synthesis, key takeaway, CTA, or next step; never another informational detail slide",
            "content_visual_rule": "derive visuals from each slide's approved message and avoid generic business icons unless the pinned template visibly uses icon modules",
        }

    @classmethod
    def _pinned_template_content_visual_focus(
        cls,
        slide: dict[str, Any],
        *,
        profile: dict[str, Any],
        story_role: str,
    ) -> str:
        # Centralizes pinned template content visual focus from slide, profile, and story role for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        headline = cls._normalize_metadata_text(slide.get("headline"), limit=140)
        support = cls._normalize_metadata_text(slide.get("supporting_line") or slide.get("body"), limit=180)
        proof = cls._normalize_metadata_list(slide.get("proof_points"), limit=2)
        subject = cls._pinned_template_subject_anchor(headline, support, *proof)
        sophistication = cls._normalize_metadata_text(profile.get("visual_sophistication"), limit=40)
        hero_strategy = cls._normalize_metadata_text(profile.get("hero_strategy"), limit=60)
        visual_language = cls._normalize_metadata_text(profile.get("visual_language"), limit=220)
        role_token = cls._carousel_story_role_token(story_role)
        focus_parts: list[str] = []
        if role_token in {"hook", "cover", "opening", "title"}:
            focus_parts.append("Large hero visual that makes the opening hook the primary focal point")
        elif role_token in {"takeaway", "close", "closing", "cta", "final"}:
            focus_parts.append("Resolved hero or decision-support visual that delivers the conclusion or next step")
        else:
            focus_parts.append("Content-specific visual module that advances this narrative beat")
        if subject:
            focus_parts.append(f"subject: {subject}")
        if sophistication == "premium_rendered":
            focus_parts.append("premium rendered dimensional craft with depth, lighting, and polished object detail")
        elif sophistication == "illustrated":
            focus_parts.append("illustration treatment matched to the pinned template")
        elif sophistication == "minimal":
            focus_parts.append("minimal visual treatment matched to the pinned template")
        if hero_strategy:
            focus_parts.append(f"template hero strategy: {hero_strategy}")
        if visual_language:
            focus_parts.append(f"template visual language: {visual_language}")
        focus_parts.append("avoid generic business icons; every object must reinforce the slide message")
        return cls._normalize_metadata_text("; ".join(focus_parts), limit=520)

    @classmethod
    def _pinned_template_adaptation_profile_prompt_section(cls, profile: Any) -> str:
        # Builds pinned template adaptation profile prompt section from profile for AIOrchestratorService.
        # The returned payload is the compact contract consumed by the next branch.
        if not isinstance(profile, dict) or not profile:
            return ""
        compact = json.dumps(profile, ensure_ascii=True, separators=(",", ":"))
        return (
            f"Pinned template adaptation profile JSON: {compact}. "
            "Use this as mandatory visual/story DNA when a template is pinned: preserve the sample layout, match its sophistication, density, depth, hierarchy, and hero strategy, generate visuals from the current slide content, and keep hook-to-close narrative continuity. "
            "Do not copy sample text or topic objects."
        )

    @classmethod
    def _apply_pinned_template_storytelling_contract(
        cls,
        slides: list[dict[str, Any]],
        *,
        request: AIOrchestrationRequest | None,
        headline: str,
        supporting_line: str,
        cta: str,
        proof_points: list[str],
        stat_highlights: list[str],
    ) -> list[dict[str, Any]]:
        # Applies apply pinned template storytelling contract from slides, request state, and headline for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not slides or not cls._request_uses_template_adaptance(request):
            return slides
        repaired = [deepcopy(slide) for slide in slides if isinstance(slide, dict)]
        slide_count = len(repaired)
        if slide_count <= 0:
            return []
        profile = cls._derive_pinned_template_adaptation_profile(repaired)
        story_roles = cls._pinned_template_story_roles(slide_count)
        prompt = cls._normalize_metadata_text(getattr(request, "prompt", "") if request is not None else "", limit=300)
        dynamic_hook = cls._dynamic_pinned_template_hook_headline(
            headline=headline,
            supporting_line=supporting_line,
            prompt=prompt,
            proof_points=proof_points,
            stat_highlights=stat_highlights,
        )
        dynamic_close = cls._dynamic_pinned_template_closing_headline(
            headline=headline,
            supporting_line=supporting_line,
            prompt=prompt,
            cta=cta,
        )
        for index, slide in enumerate(repaired, start=1):
            story_role = story_roles[index - 1] if index - 1 < len(story_roles) else ("closing" if index == slide_count else "detail")
            metadata = dict(slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {})
            existing_role = cls._carousel_story_role_token(metadata.get("story_role") or slide.get("role"))
            if index == 1:
                slide["role"] = "hook"
                if cls._pinned_template_headline_needs_story_hook(slide.get("headline"), source_headline=headline):
                    slide["headline"] = dynamic_hook
            elif index == slide_count:
                slide["role"] = "closing"
                if cta:
                    slide["cta"] = cta
                if cls._pinned_template_headline_needs_story_hook(slide.get("headline"), source_headline=headline) or existing_role not in {"takeaway", "close", "closing", "cta", "final"}:
                    slide["headline"] = dynamic_close
            elif existing_role in cls._carousel_generic_story_roles() or not existing_role:
                slide["role"] = "intro" if story_role == "context" else "detail"

            metadata["story_role"] = story_role
            metadata["role_guidance"] = cls._carousel_story_role_guidance(story_role)
            metadata["pinned_template_adaptation_profile"] = profile
            metadata["pinned_template_story_contract"] = {
                "sequence_pattern": story_roles,
                "current_beat": story_role,
                "opening_requirement": profile.get("opening_strategy"),
                "closing_requirement": profile.get("closing_strategy"),
                "visual_rule": profile.get("content_visual_rule"),
            }
            visual_focus = cls._normalize_metadata_text(slide.get("visual_focus"), limit=420)
            if (
                index in {1, slide_count}
                or not visual_focus
                or cls._carousel_visual_focus_is_generic(visual_focus)
            ):
                slide["visual_focus"] = cls._pinned_template_content_visual_focus(
                    slide,
                    profile=profile,
                    story_role=story_role,
                )
            metadata["template_adaptation_visual_focus"] = slide.get("visual_focus")
            slide["metadata"] = metadata
        for slide in repaired[:-1]:
            slide["cta"] = ""
        return repaired

    @classmethod
    def _build_carousel_slide_specs(
        cls,
        text_payload: StructuredTextPayload,
        *,
        request: AIOrchestrationRequest | None = None,
        creative_decision: CreativeDecisionPayload | None = None,
        enable_content_intelligence: bool = False,
    ) -> list[dict[str, Any]]:
        # Builds carousel slide specs from copy payload, request state, and creative decision for AIOrchestratorService.
        # It calls _normalize_metadata_text and _sentences while assembling the payload or prompt text.
        metadata = text_payload.metadata or {}
        if (
            not enable_content_intelligence
            and request is not None
            and isinstance(request.content_plan, dict)
            and request.content_plan.get("_content_intelligence_enabled") is True
        ):
            enable_content_intelligence = True
        headline = cls._normalize_metadata_text(text_payload.headline, limit=180)
        body_sentences = cls._sentences(text_payload.body)
        proof_point_limit = max(
            cls._rough_collection_length(metadata.get("proof_points")),
            len(body_sentences),
            6,
        )
        stat_highlight_limit = max(
            cls._rough_collection_length(metadata.get("stat_highlights")),
            min(len(body_sentences), 8),
            4,
        )
        supporting_line = cls._normalize_metadata_text(
            metadata.get("supporting_line") or text_payload.body,
            limit=220,
        )
        clean_cta = cls._clean_carousel_cta_text(
            text_payload.cta,
            headline=headline,
            supporting_line=supporting_line,
        )
        proof_points = cls._normalize_metadata_list(metadata.get("proof_points"), limit=proof_point_limit)
        stat_highlights = cls._normalize_metadata_list(metadata.get("stat_highlights"), limit=stat_highlight_limit)
        claim_evidence_pairs = cls._merge_claim_evidence_pairs(
            metadata.get("claim_evidence_pairs"),
            cls._claim_evidence_pairs_from_research_brief(
                (request.research_editorial_brief if request is not None else None),
                limit=max(proof_point_limit, 4),
            ),
            limit=max(proof_point_limit, 4),
        )
        sequence_pack = (
            cls._template_sequence_pack(request, creative_decision=creative_decision)
            if request is not None
            else None
        )
        sequence_pack_slides = (
            [dict(item) for item in sequence_pack.get("slides", []) if isinstance(item, dict)]
            if isinstance(sequence_pack, dict)
            else []
        )
        sequence_pack_slide_count = (
            cls._int_or_none(sequence_pack.get("slide_count"))
            if isinstance(sequence_pack, dict)
            else None
        ) or len(sequence_pack_slides)
        prompt_requested_slide_count = cls._explicit_requested_slide_count(request)
        force_sequence_pack = bool(request is not None and cls._should_force_sequence_pack(request, creative_decision=creative_decision))
        carousel_archetype = cls._carousel_infer_archetype(request=request, metadata=metadata)
        explicit_outline_candidates = cls._carousel_explicit_outline_candidates(request, metadata)
        outline_slide_count = len(explicit_outline_candidates or cls._carousel_outline_candidates(request, metadata))
        structured_source = metadata.get("carousel_slide_specs") or metadata.get("slides")
        structured_slide_count = (
            len([item for item in structured_source if isinstance(item, dict)])
            if isinstance(structured_source, list)
            else 0
        )
        template_surface_policy = cls._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata if isinstance(metadata, dict) else {},
        )
        explicit_style_reference_policy = bool(
            creative_decision
            and isinstance(getattr(creative_decision, "asset_strategy", None), dict)
            and str((creative_decision.asset_strategy or {}).get("template_surface_policy") or "").strip().lower() == "style_reference_only"
        )
        preferred_editorial_slide_count = cls._int_or_none(
            metadata.get("preferred_slide_count")
            or metadata.get("slide_count")
            or ((request.content_plan or {}).get("preferred_slide_count") if request and isinstance(request.content_plan, dict) else None)
            or ((request.research_editorial_brief or {}).get("preferred_slide_count") if request and isinstance(request.research_editorial_brief, dict) else None)
        )
        # Slide count is resolved from the strongest authority first: forced sequence, prompt, structured specs, then fallback plans.
        if (force_sequence_pack or explicit_style_reference_policy) and sequence_pack_slide_count:
            raw_target_slide_count = sequence_pack_slide_count
        elif template_surface_policy == "style_reference_only":
            if prompt_requested_slide_count:
                raw_target_slide_count = prompt_requested_slide_count
            elif structured_slide_count:
                bounded_targets = [
                    count
                    for count in (preferred_editorial_slide_count, outline_slide_count)
                    if count and count <= structured_slide_count
                ]
                raw_target_slide_count = bounded_targets[0] if bounded_targets else structured_slide_count
            elif sequence_pack_slide_count:
                raw_target_slide_count = sequence_pack_slide_count
            else:
                raw_target_slide_count = cls._int_or_none(
                    outline_slide_count
                    or sequence_pack_slide_count
                    or preferred_editorial_slide_count
                )
        else:
            raw_target_slide_count = cls._int_or_none(
                prompt_requested_slide_count
                or outline_slide_count
                or preferred_editorial_slide_count
                or sequence_pack_slide_count
                or structured_slide_count
            )
        should_enforce_target_slide_count = bool(raw_target_slide_count)
        target_slide_count = max(int(raw_target_slide_count or 0), 3) if raw_target_slide_count else 0
        mistake_style = cls._has_mistake_carousel_signals(
            headline,
            supporting_line,
            text_payload.body,
            metadata.get("section_label"),
            *proof_points,
            *stat_highlights,
        )
        fallback_slides = cls._fallback_carousel_slide_specs(
            text_payload=text_payload,
            request=request,
            creative_decision=creative_decision,
            metadata=metadata,
            headline=headline,
            body_sentences=body_sentences,
            supporting_line=supporting_line,
            proof_points=proof_points,
            stat_highlights=stat_highlights,
            target_slide_count=target_slide_count,
            sequence_pack_slides=sequence_pack_slides,
        )
        if isinstance(structured_source, list) and structured_source:
            # Structured slide specs are respected, then repaired/sanitized against the same fallback story arc.
            structured_fallback_slides = fallback_slides if explicit_outline_candidates else []
            normalized_slides = cls._normalize_structured_carousel_slides(
                structured_source,
                headline=headline,
                supporting_line=supporting_line,
                cta=clean_cta,
                proof_points=proof_points,
                stat_highlights=stat_highlights,
                target_slide_count=target_slide_count or len(structured_source),
                mistake_style=mistake_style,
                fallback_slides=structured_fallback_slides,
                carousel_archetype=carousel_archetype,
            )
        else:
            normalized_slides = [dict(slide) for slide in fallback_slides]

        if len(normalized_slides) < 3:
            # Carousels need at least a cover/detail/close rhythm; insert a middle beat when upstream data is too thin.
            normalized_slides.insert(
                1,
                {
                    "role": "detail",
                    "headline": (
                        cls._infer_mistake_headline(stat_highlights[0] if stat_highlights else supporting_line, fallback_focus=headline, index=1)
                        if mistake_style
                        else stat_highlights[0] if stat_highlights else "Why it matters"
                    ),
                    "supporting_line": cls._mistake_supporting_line(supporting_line, headline) if mistake_style else supporting_line or headline,
                    "proof_points": ([] if mistake_style else proof_points[:2]),
                    "cta": "",
                },
            )

        fallback_for_progression = (
            fallback_slides
            if (explicit_outline_candidates or mistake_style or carousel_archetype)
            else []
        )
        normalized_slides = cls._enforce_carousel_editorial_progression(
            normalized_slides,
            request=request,
            metadata=metadata,
            fallback_slides=fallback_for_progression,
            headline=headline,
            supporting_line=supporting_line,
            cta=clean_cta,
        )
        normalized_slides = cls._validate_carousel_semantic_progression(
            normalized_slides,
            request=request,
            metadata=metadata,
            fallback_slides=fallback_for_progression,
            headline=headline,
            supporting_line=supporting_line,
            cta=clean_cta,
        )
        normalized_slides = cls._apply_claim_evidence_to_carousel_slides(
            normalized_slides,
            claim_evidence_pairs=claim_evidence_pairs,
        )
        normalized_slides = cls._sanitize_carousel_slide_specs(
            normalized_slides,
            request=request,
        )
        if target_slide_count and len(normalized_slides) < target_slide_count:
            # Fill short sequences from fallback slides without duplicating the same slide index/headline pair.
            for fallback_slide in fallback_slides:
                candidate = dict(fallback_slide)
                if any(
                    (
                        int(existing.get("slide_index") or 0) == int(candidate.get("slide_index") or 0)
                        and cls._normalize_metadata_text(existing.get("headline"), limit=120)
                        == cls._normalize_metadata_text(candidate.get("headline"), limit=120)
                    )
                    for existing in normalized_slides
                    if isinstance(existing, dict)
                ):
                    continue
                normalized_slides.append(candidate)
                if len(normalized_slides) >= target_slide_count:
                    break
        if should_enforce_target_slide_count and target_slide_count and len(normalized_slides) > target_slide_count:
            # Downsampling uses proportional positions so long reference sequences keep their beginning/middle/end rhythm.
            selected_positions = {
                cls._proportional_sequence_reference_index(
                    output_index,
                    output_slide_count=target_slide_count,
                    reference_slide_count=len(normalized_slides),
                )
                for output_index in range(1, target_slide_count + 1)
            }
            normalized_slides = [
                dict(slide)
                for source_index, slide in enumerate(normalized_slides, start=1)
                if source_index in selected_positions
            ][:target_slide_count]
        if template_surface_policy == "style_reference_only" and len(normalized_slides) > 1:
            # In style-reference carousels, only the final slide should carry the CTA grammar.
            closing_index: int | None = None
            for index, slide in enumerate(normalized_slides):
                slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
                role_token = cls._carousel_story_role_token(
                    slide_metadata.get("story_role") or slide.get("role")
                )
                has_cta = bool(cls._normalize_metadata_text(slide.get("cta"), limit=90))
                if role_token in {"takeaway", "close", "closing", "cta", "final"} or has_cta:
                    closing_index = index
            if closing_index is not None and closing_index != len(normalized_slides) - 1:
                normalized_slides.append(normalized_slides.pop(closing_index))
            for slide in normalized_slides[:-1]:
                slide["cta"] = ""
        normalized_slides = cls._apply_carousel_continuity_contract(normalized_slides)
        normalized_slides = cls._apply_sequence_pack_slide_metadata(
            normalized_slides,
            sequence_pack_slides=sequence_pack_slides,
            carousel_archetype=carousel_archetype,
            include_sample_copy_authority=not cls._request_uses_template_adaptance(request),
        )
        if template_surface_policy == "style_reference_only":
            normalized_slides = cls._sanitize_style_reference_sample_slide_specs(
                normalized_slides,
                request=request,
            )
        normalized_slides = cls._align_carousel_slide_specs_to_selected_references(
            normalized_slides,
            request=request,
            creative_decision=creative_decision,
        )
        normalized_slides = cls._apply_pinned_template_storytelling_contract(
            normalized_slides,
            request=request,
            headline=headline,
            supporting_line=supporting_line,
            cta=clean_cta,
            proof_points=proof_points,
            stat_highlights=stat_highlights,
        )

        slide_count = len(normalized_slides)
        for index, slide in enumerate(normalized_slides, start=1):
            slide["slide_index"] = index
            slide["slide_count"] = slide_count
            slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
            slide["metadata"] = {
                **slide_metadata,
                "carousel_archetype": slide_metadata.get("carousel_archetype") or carousel_archetype,
            }
        if enable_content_intelligence:
            # Content intelligence adds semantic contracts after structural cleanup so it works with final slide count.
            compiled_context = {
                "content_plan": request.content_plan if request is not None and isinstance(request.content_plan, dict) else {},
                "research_editorial_brief": request.research_editorial_brief if request is not None and isinstance(request.research_editorial_brief, dict) else {},
            }
            story_contracts = cls._carousel_semantic_slide_contracts(
                compiled_context=compiled_context,
                request=request,
                slide_count=len(normalized_slides),
            )
            pacing_resolution = cls._carousel_story_pacing_resolution(
                story_contracts=story_contracts,
                prompt_requested_slide_count=prompt_requested_slide_count,
                preferred_slide_count=preferred_editorial_slide_count,
                initial_target_slide_count=target_slide_count,
                structured_slide_count=structured_slide_count,
                outline_slide_count=outline_slide_count,
                sequence_pack_slide_count=sequence_pack_slide_count,
            )
            normalized_slides = cls._apply_content_intelligence_carousel_contracts(
                normalized_slides,
                request=request,
                story_contracts=story_contracts,
                pacing_resolution=pacing_resolution,
            )
        normalized_slides = cls._apply_same_income_comparison_story_contract(
            normalized_slides,
            request=request,
            source_slides=structured_source if isinstance(structured_source, list) else [],
        )
        normalized_slides = cls._compact_carousel_slides_for_mobile(normalized_slides)
        # Returns the carousel's per-slide execution contract consumed by validation, slide prompts, metadata, and export.
        # Static and infographic flows do not use this list; they rely on static_panel_spec or infographic_section_specs instead.
        return normalized_slides

    @staticmethod
    def _carousel_continuity_prompt_sections(slide: dict[str, Any]) -> list[str]:
        # Builds carousel continuity prompt sections from slide for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        sequence_position = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("sequence_position"),
            limit=32,
        )
        visual_weight = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("visual_weight"),
            limit=48,
        )
        composition_mode = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("composition_mode"),
            limit=64,
        )
        text_pressure = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("text_pressure"),
            limit=24,
        )
        pacing_target = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("pacing_target"),
            limit=220,
        )
        layout_balance_target = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("layout_balance_target"),
            limit=240,
        )
        whitespace_target = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("whitespace_target"),
            limit=220,
        )
        continuity_anchor = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("continuity_anchor"),
            limit=240,
        )
        adjacency_contract = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("adjacency_contract"),
            limit=220,
        )
        anti_poster_rule = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("anti_poster_rule"),
            limit=200,
        )

        sections: list[str] = []
        if sequence_position or continuity_anchor:
            sections.append(
                "Carousel continuity contract: "
                + " ".join(
                    part
                    for part in [
                        f"This is the {sequence_position} beat in the sequence." if sequence_position else "",
                        continuity_anchor,
                    ]
                    if part
                )
            )
        if pacing_target:
            sections.append(f"Carousel pacing target: {pacing_target}")
        if composition_mode or visual_weight or text_pressure:
            descriptors = ", ".join(
                part
                for part in [
                    f"composition mode {composition_mode}" if composition_mode else "",
                    f"visual weight {visual_weight}" if visual_weight else "",
                    f"text pressure {text_pressure}" if text_pressure else "",
                ]
                if part
            )
            sections.append(f"Composition evolution target: {descriptors}.")
        if layout_balance_target:
            sections.append(f"Image/text balance target: {layout_balance_target}")
        if whitespace_target:
            sections.append(f"Whitespace target: {whitespace_target}")
        if adjacency_contract:
            sections.append(f"Adjacency contract: {adjacency_contract}")
        if anti_poster_rule:
            sections.append(f"Anti-poster rule: {anti_poster_rule}")
        return sections

    @staticmethod
    def build_carousel_slide_render_prompt(
        request: AIOrchestrationRequest,
        creative_decision: CreativeDecisionPayload,
        message_strategy: MessageStrategyPayload | None,
        slide: dict[str, Any],
        scene_graph: GenerationSceneGraph | None = None,
        reference_images: list[dict[str, Any]] | None = None,
        retry_note: str | None = None,
        visual_explanation_plan: dict[str, Any] | None = None,
        compiled_context: dict[str, Any] | None = None,
    ) -> str:
        # Builds carousel slide prompt from request state, creative decision, and message strategy for AIOrchestratorService.
        # It calls _design_system_prompt_guidance and _compact_palette_summary while assembling the payload or prompt text.
        compiled_context = dict(compiled_context or {})
        if scene_graph is None:
            # A minimal scene graph keeps prompt helpers working even when carousel final render is sample-led.
            size = request.studio_panel.get("size") if isinstance(request.studio_panel.get("size"), dict) else {}
            scene_graph = GenerationSceneGraph.model_validate(
                {
                    "canvas": {
                        "width": int(size.get("width") or 1080),
                        "height": int(size.get("height") or 1350),
                        "platform": request.studio_panel.get("platform_preset") or "social",
                        "file_type": request.studio_panel.get("file_type") or "png",
                    },
                    "elements": [],
                }
            )
        visual_identity = request.resolved_brand_context.get("visual_identity", {}) or {}
        design_system_guidance = AIOrchestratorService._design_system_prompt_guidance(visual_identity)
        palette = AIOrchestratorService._compact_palette_summary(visual_identity)
        palette_guidance = AIOrchestratorService._palette_role_guidance(visual_identity)
        strict_palette_contract = AIOrchestratorService._strict_palette_contract(visual_identity)
        typography = AIOrchestratorService._compact_typography_summary(visual_identity)
        brand_name = AIOrchestratorService._normalize_metadata_text(
            request.resolved_brand_context.get("brand_name") or "",
            limit=80,
        )
        platform = str(request.studio_panel.get("platform_preset") or "social")
        format_name = str(request.studio_panel.get("format") or "carousel").strip().lower()
        file_type = str(request.studio_panel.get("file_type") or "png").upper()
        canvas_fit_guidance = AIOrchestratorService._canvas_fit_guidance(request.studio_panel)
        reference_summary = AIOrchestratorService._compact_reference_assets(reference_images or [])
        slide_metadata = slide.get("metadata") if isinstance(slide.get("metadata"), dict) else {}
        layout_anchor_asset_path = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("reference_layout_asset_path")
            or slide_metadata.get("reference_template_asset_path")
            or slide_metadata.get("reference_asset_path"),
            limit=240,
        )
        topic_anchor_asset_path = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("reference_topic_asset_path"),
            limit=240,
        )
        planning_hints = creative_decision.planning_hints if isinstance(creative_decision.planning_hints, dict) else {}
        asset_strategy = creative_decision.asset_strategy if isinstance(creative_decision.asset_strategy, dict) else {}
        template_surface_policy = AIOrchestratorService._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=slide_metadata,
        )
        # Contamination checks prevent stale reference metadata from overruling the selected sample image.
        sample_metadata_contaminated = AIOrchestratorService._slide_sample_metadata_semantically_contaminated(
            slide,
            request=request,
            compiled_context=compiled_context,
        )
        has_sample_page_blueprint = isinstance(slide_metadata.get("sample_page_blueprint"), dict) and bool(
            slide_metadata.get("sample_page_blueprint")
        )
        sample_page_blueprint = (
            slide_metadata.get("sample_page_blueprint")
            if isinstance(slide_metadata.get("sample_page_blueprint"), dict)
            else {}
        )
        style_reference_sample_active = (
            template_surface_policy == "style_reference_only"
            and (bool(layout_anchor_asset_path) or bool(reference_images))
        )
        has_reference_zone_map = (
            not sample_metadata_contaminated
            and isinstance(slide_metadata.get("reference_zone_map"), dict)
            and bool((slide_metadata.get("reference_zone_map") or {}).get("zones"))
        )
        geometry_contract = (
            ""
            if template_surface_policy == "style_reference_only" and (sample_metadata_contaminated or has_sample_page_blueprint)
            else AIOrchestratorService._compact_slide_geometry_contract(slide, scene_graph)
        )
        # Style-reference samples use sample blueprints instead of scene-graph geometry when those conflict.
        reference_zone_guidance = (
            ""
            if template_surface_policy == "style_reference_only" and sample_metadata_contaminated
            else AIOrchestratorService._reference_zone_layout_guidance(slide)
        )
        layout_dna_contract = (
            ""
            if template_surface_policy == "style_reference_only" and sample_metadata_contaminated
            else AIOrchestratorService._compact_layout_dna_contract(compiled_context)
        )
        if layout_dna_contract:
            # Layout DNA is useful for spacing and rhythm, but logo trigger words are scrubbed before image prompts.
            layout_dna_contract = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(layout_dna_contract)
        logo_position_hint = AIOrchestratorService._effective_logo_position_hint(
            request=request,
            creative_decision=creative_decision,
            text_payload={
                "headline": slide.get("headline"),
                "body": slide.get("supporting_line"),
                "cta": slide.get("cta"),
                "metadata": slide_metadata,
            },
        )
        # Logo guidance reserves an empty corner because the exact logo asset is handled outside the image model.
        logo_safe_zone_guidance = AIOrchestratorService._logo_safe_zone_guidance(
            request,
            hint=logo_position_hint,
        )
        reserved_logo_area = AIOrchestratorService._logo_reserved_area_label(logo_position_hint)
        logo_surface_guidance = AIOrchestratorService._logo_surface_guidance(
            background_tone=AIOrchestratorService._resolve_logo_background_tone(
                metadata=slide_metadata,
                creative_decision=creative_decision,
            )
        )
        logo_non_generation_contract = AIOrchestratorService._logo_non_generation_contract(
            brand_name=brand_name,
            reserved_logo_area=reserved_logo_area,
        )
        text_containment_contract = AIOrchestratorService._image_text_containment_contract(format_name=format_name)
        slide_index = int(slide.get("slide_index") or 1)
        slide_count = int(slide.get("slide_count") or 1)
        story_role = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("story_role") or slide.get("role"),
            limit=48,
        )
        carousel_archetype = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("carousel_archetype"),
            limit=64,
        )
        message_theme = ""
        emotional_direction = ""
        if isinstance(message_strategy, MessageStrategyPayload):
            message_theme = AIOrchestratorService._normalize_metadata_text(
                message_strategy.primary_campaign_theme,
                limit=180,
            )
            emotional_direction = AIOrchestratorService._normalize_metadata_text(
                message_strategy.emotional_messaging_direction,
                limit=100,
            )
        slide_text_payload = StructuredTextPayload(
            headline=AIOrchestratorService._normalize_metadata_text(slide.get("headline"), limit=180),
            body=AIOrchestratorService._carousel_slide_body_text(
                slide,
                fallback_text=AIOrchestratorService._normalize_metadata_text(slide.get("supporting_line"), limit=220),
            ),
            cta=AIOrchestratorService._normalize_metadata_text(slide.get("cta"), limit=80),
            hashtags=[],
            metadata={
                "supporting_line": slide.get("supporting_line"),
                "proof_points": slide.get("proof_points"),
                "body_points": slide.get("body_points"),
                "stat_highlights": slide.get("stat_highlights"),
                "claim_evidence_pairs": slide.get("claim_evidence_pairs"),
                "visual_focus": slide.get("visual_focus"),
                "transition_note": slide.get("transition_note"),
                **slide_metadata,
            },
        )
        # Convert the slide dict into the same payload shape used by final-render helper contracts.
        visual_plan = visual_explanation_plan or AIOrchestratorService._visual_explanation_plan(
            request,
            slide_text_payload,
            creative_decision,
            reference_images,
            message_strategy,
        )
        # Grounding sections carry approved brand/reference evidence into the prompt without exposing raw asset payloads.
        grounding_sections = AIOrchestratorService._final_render_grounding_sections(
            request=request,
            creative_decision=creative_decision,
            text_payload=slide_text_payload,
            compiled_context=compiled_context,
            reference_assets=reference_images,
        )
        research_quality_section = AIOrchestratorService._research_editorial_prompt_section(
            request,
            compiled_context,
        )
        anti_hallucination_section = AIOrchestratorService._image_generation_anti_hallucination_prompt_section(
            request=request,
            compiled_context=compiled_context,
        )
        consultant_contract = AIOrchestratorService._consultant_quality_contract(
            for_visual_only=True,
            for_carousel=True,
        )
        multimodal_balance_contract = AIOrchestratorService._multimodal_balance_contract(
            format_name="carousel",
            supporting_line=AIOrchestratorService._normalize_metadata_text(slide.get("supporting_line"), limit=220),
            proof_points=AIOrchestratorService._normalize_metadata_list(slide.get("proof_points"), limit=3),
            claim_evidence_pairs=AIOrchestratorService._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=2),
            for_visual_only=True,
        )
        reference_family_contract = AIOrchestratorService._reference_family_contract_sections(
            compiled_context,
            slide=slide,
            for_visual_only=True,
        )
        sample_alignment_sections = AIOrchestratorService._sample_visual_alignment_sections(
            compiled_context,
            for_carousel=True,
        )
        visual_style_policy_sections = AIOrchestratorService._visual_style_policy_prompt_sections(
            compiled_context,
            format_name="carousel",
            reference_assets=reference_images,
            slide=slide,
        )
        continuity_sections = AIOrchestratorService._carousel_continuity_prompt_sections(slide)
        sequence_alignment_sections = AIOrchestratorService._sequence_blueprint_alignment_sections(
            compiled_context,
            slide=slide,
            for_carousel=True,
            for_visual_only=True,
        )
        strict_sample_layout_contract = AIOrchestratorService._strict_sample_layout_contract(
            slide=slide,
            request=request,
            creative_decision=creative_decision,
            compiled_context=compiled_context,
            scene_graph=scene_graph,
        )
        if strict_sample_layout_contract:
            strict_sample_layout_contract = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(strict_sample_layout_contract)
        thin_sample_conditioning_contract = AIOrchestratorService._thin_sample_conditioning_contract(
            slide=slide,
            request=request,
            creative_decision=creative_decision,
            scene_graph=scene_graph,
        )
        if thin_sample_conditioning_contract:
            # The thin contract gives the image model just enough sample geometry when full metadata would be noisy.
            thin_sample_conditioning_contract = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(thin_sample_conditioning_contract)
        visual_plan_guidance = AIOrchestratorService._visual_explanation_guidance(visual_plan)
        disclaimer_overlay_guidance = AIOrchestratorService._disclaimer_overlay_guidance(request)
        legal_footer_text = AIOrchestratorService._scene_graph_legal_footer_text(scene_graph)
        layout_content_budget = AIOrchestratorService._dynamic_layout_content_budget(
            request=request,
            format_name="carousel slide",
            text_payload=slide_text_payload,
            slide=slide,
            metadata={**slide_metadata, "sample_page_blueprint": sample_page_blueprint},
            sample_page_blueprint=sample_page_blueprint,
            scene_graph=scene_graph,
            legal_footer_text=legal_footer_text,
            disclaimer_guidance=disclaimer_overlay_guidance,
            logo_position_hint=logo_position_hint,
        )
        # Layout budget decides which text is safe to ask the image model to render for this sample/page density.
        prompt_slide = AIOrchestratorService._prompt_visible_slide_from_layout_budget(slide, layout_content_budget)
        prompt_slide_metadata = {
            **slide_text_payload.metadata,
            **(layout_content_budget.get("prompt_metadata") if isinstance(layout_content_budget.get("prompt_metadata"), dict) else {}),
        }
        prompt_slide_text_payload = StructuredTextPayload(
            headline=slide_text_payload.headline,
            body=AIOrchestratorService._carousel_slide_body_text(
                prompt_slide,
                fallback_text=AIOrchestratorService._normalize_metadata_text(prompt_slide.get("supporting_line"), limit=220),
            ),
            cta=layout_content_budget.get("prompt_cta", slide_text_payload.cta),
            hashtags=list(slide_text_payload.hashtags or []),
            metadata=prompt_slide_metadata,
        )
        # Palette and role execution guidance is built after budget trimming so CTA/footer requirements are accurate.
        palette_execution_contract = AIOrchestratorService._palette_role_execution_contract(
            visual_identity,
            has_cta=bool(AIOrchestratorService._normalize_metadata_text(prompt_slide.get("cta"), limit=80)),
            has_legal_footer=bool(legal_footer_text),
        )
        story_role_visual_execution = AIOrchestratorService._story_role_visual_execution_guidance(
            story_role,
            has_cta=bool(AIOrchestratorService._normalize_metadata_text(prompt_slide.get("cta"), limit=80)),
        )
        sample_layout_category = AIOrchestratorService._normalize_metadata_text(
            sample_page_blueprint.get("layout_category"),
            limit=80,
        ).casefold()
        sample_layout_mode = AIOrchestratorService._sample_blueprint_layout_mode(sample_page_blueprint)
        # The sample layout mode controls which module-count lock gets inserted into the render prompt.
        story_role_normalized = AIOrchestratorService._normalize_metadata_text(story_role, limit=48).casefold().replace(" ", "_")
        sample_contract_for_prompt = {
            "sample_page_editorial_role": slide_metadata.get("sample_page_editorial_role"),
            "sample_page_copy_behavior": slide_metadata.get("sample_page_copy_behavior"),
            "sample_page_copy_density": slide_metadata.get("sample_page_copy_density"),
            "sample_page_closing_grammar": slide_metadata.get("sample_page_closing_grammar"),
        }
        sample_allows_product_cta = AIOrchestratorService._sample_contract_allows_product_cta(sample_contract_for_prompt)
        closing_sample_disallows_product = (
            style_reference_sample_active
            and not sample_allows_product_cta
            and story_role_normalized in {"takeaway", "close", "closing", "cta", "final", "macro_takeaway", "strategic_meaning"}
        )
        if closing_sample_disallows_product:
            # Some selected samples close editorially, so product/CTA language is stripped from final-slide visuals.
            editorial_structure_lock = (
                " Preserve the editorial explainer structure: large visual region(s), supporting text blocks, and explanatory hierarchy must stay distinct."
                if sample_layout_mode == "editorial_explainer"
                else ""
            )
            story_role_visual_execution = (
                "Execution guidance from selected sample page: preserve the sample's macro-takeaway closing grammar. "
                "Show wider strategic consequence, future signal, and subtle brand presence without platform, product, dashboard, laptop, or investment-CTA treatment."
                + editorial_structure_lock
            )
        elif style_reference_sample_active and sample_layout_mode == "numbered_or_icon_row_list":
            story_role_visual_execution = (
                "Execution guidance from selected sample page: "
                + AIOrchestratorService._sample_blueprint_layout_instruction(sample_page_blueprint)
                + " Render one concise approved factual line per observed row/list module."
            )
        elif style_reference_sample_active and sample_layout_mode == "card_callout_grid":
            story_role_visual_execution = (
                "Execution guidance from selected sample page: "
                + AIOrchestratorService._sample_blueprint_layout_instruction(sample_page_blueprint)
                + " Render one concise approved factual line per observed card/callout module."
            )
        elif style_reference_sample_active and sample_layout_mode == "vertical_icon_explainer":
            story_role_visual_execution = (
                "Execution guidance from selected sample page: "
                + AIOrchestratorService._sample_blueprint_layout_instruction(sample_page_blueprint)
                + " Render one concise approved factual line per observed icon/text module."
            )
        elif style_reference_sample_active and sample_layout_mode == "editorial_explainer":
            story_role_visual_execution = (
                "Execution guidance from selected sample page: "
                + AIOrchestratorService._sample_blueprint_layout_instruction(sample_page_blueprint)
                + " Preserve the editorial explainer structure: large visual region(s), supporting text blocks, and explanatory hierarchy must stay distinct. "
                "Do not convert this page into a card grid, numbered list, dashboard, product surface, or generic finance poster."
            )
        visual_focus_for_prompt = AIOrchestratorService._overlay_prompt_safe_visual_focus(slide)
        if closing_sample_disallows_product and (
            AIOrchestratorService._is_product_cta_like_line(visual_focus_for_prompt, request=request)
            or AIOrchestratorService._style_reference_visual_focus_needs_sample_reset(
                visual_focus_for_prompt,
                request=request,
                sample_allows_data_or_product=False,
            )
        ):
            # Reset visual focus when the planner drifted toward product surfaces that the selected sample does not allow.
            visual_focus_for_prompt = AIOrchestratorService._sample_matched_visual_focus(
                slide,
                request=request,
            )
        transition_note_for_prompt = AIOrchestratorService._normalize_metadata_text(slide.get("transition_note"), limit=160)
        if closing_sample_disallows_product and (
            AIOrchestratorService._is_product_cta_like_line(transition_note_for_prompt, request=request)
            or AIOrchestratorService._style_reference_visual_focus_needs_sample_reset(
                transition_note_for_prompt,
                request=request,
                sample_allows_data_or_product=False,
            )
        ):
            transition_note_for_prompt = ""
        use_backend_text_overlay = AIOrchestratorService._should_use_backend_text_overlay_for_ai_final_render(
            request,
            creative_decision,
        )
        proof_points_limit = 3
        if style_reference_sample_active and sample_page_blueprint:
            counts = sample_page_blueprint.get("module_counts") if isinstance(sample_page_blueprint.get("module_counts"), dict) else {}
            # Proof-point limits mirror the sample's module capacity instead of using a generic carousel count.
            if sample_layout_mode == "numbered_or_icon_row_list":
                proof_points_limit = max(3, min(int(counts.get("horizontal_band_count") or 3), 8))
            elif sample_layout_mode == "card_callout_grid":
                proof_points_limit = max(
                    2,
                    min(
                        int(
                            counts.get("card_like_count")
                            or counts.get("horizontal_band_count")
                            or 3
                        ),
                        4,
                    ),
                )
            elif sample_layout_mode == "vertical_icon_explainer":
                proof_points_limit = max(
                    3,
                    min(
                        int(
                            counts.get("horizontal_band_count")
                            or counts.get("small_icon_like_count")
                            or counts.get("text_block_count")
                            or 3
                        ),
                        5,
                    ),
                )
            elif sample_layout_mode == "editorial_explainer":
                proof_points_limit = max(2, min(int(counts.get("text_block_count") or 3), 5))
        sample_module_execution_contract = ""
        if style_reference_sample_active and sample_page_blueprint:
            counts = sample_page_blueprint.get("module_counts") if isinstance(sample_page_blueprint.get("module_counts"), dict) else {}
            large_visual_count = int(counts.get("large_visual_count") or 0)
            # Module execution locks protect the selected sample's structure from being replaced by generic visuals.
            if sample_layout_mode == "numbered_or_icon_row_list":
                row_count = max(1, min(int(counts.get("horizontal_band_count") or proof_points_limit or 3), 8))
                sample_module_execution_contract = (
                    f"SAMPLE MODULE COUNT LOCK: render exactly {row_count} visible row/list module(s) matching the selected sample page. "
                    "Do not merge, drop, compress, or replace these rows with fewer cards, a hero illustration, a chart, a table, or a generic infographic. "
                    "If an approved text line is short, keep the row as a visual row with its badge/icon and concise text rather than deleting the module."
                )
            elif sample_layout_mode == "card_callout_grid":
                card_count = max(1, min(int(counts.get("card_like_count") or proof_points_limit or 3), 6))
                sample_module_execution_contract = (
                    f"SAMPLE MODULE COUNT LOCK: render exactly {card_count} distinct card/callout module(s) matching the selected sample page. "
                    "Each card needs its own boundary, spacing, icon/mark if present in the sample, and one approved factual line. "
                    "Do not convert the card grid into a standalone hero scene, person scene, laptop/dashboard scene, two-column explainer, row list, or generic poster. "
                    "Topic concepts such as people, mobility, products, screens, documents, or market objects must be translated into small module-level icons or card illustrations unless the selected sample page itself has a large standalone visual."
                )
            elif sample_layout_mode == "vertical_icon_explainer":
                node_count = max(
                    1,
                    min(
                        int(
                            counts.get("horizontal_band_count")
                            or counts.get("small_icon_like_count")
                            or counts.get("text_block_count")
                            or proof_points_limit
                            or 3
                        ),
                        6,
                    ),
                )
                sample_module_execution_contract = (
                    f"SAMPLE MODULE COUNT LOCK: render exactly {node_count} visible icon/text node(s) in the selected sample page's observed flow. "
                    "Each node must keep its own icon/mark plus adjacent body text in the sample's observed alignment, connector/path/stack rhythm, and CTA/footer posture. "
                    "Do not replace the node sequence with a standalone hero scene, closing-product panel, screen mockup, chart, table, metric dashboard, or generic finance motif. "
                    "Keep topic imagery inside the observed node/icon structure unless the selected sample page has a large standalone visual."
                )
            elif sample_layout_mode in {"cover_or_hero_visual", "photo_led_cover"} and (
                int(counts.get("horizontal_band_count") or 0) >= 3
                or (
                    int(counts.get("card_like_count") or 0) >= 1
                    and int(counts.get("small_icon_like_count") or 0) >= 3
                )
            ):
                internal_count = max(
                    1,
                    min(
                        int(
                            counts.get("horizontal_band_count")
                            or counts.get("card_like_count")
                            or counts.get("small_icon_like_count")
                            or proof_points_limit
                            or 3
                        ),
                        6,
                    ),
                )
                sample_module_execution_contract = (
                    f"SAMPLE MODULE COUNT LOCK: preserve the cover page's {internal_count} internal content band/module area(s) while keeping the observed hero/cover frame. "
                    "Do not flatten the page into a single illustration, chart, graph, dashboard, poster, or generic finance visual. "
                    "If the sample has a notebook/body block, icon list, or CTA band inside the cover composition, keep those internal structures visible and aligned."
                )
            elif sample_layout_mode == "editorial_explainer":
                visual_count = max(1, min(int(counts.get("large_visual_count") or 1), 4))
                text_count = max(1, min(int(counts.get("text_block_count") or proof_points_limit or 3), 6))
                sample_module_execution_contract = (
                    f"SAMPLE MODULE COUNT LOCK: preserve the selected sample page's editorial explainer structure with about {visual_count} large visual region(s) and {text_count} explanatory text block(s). "
                    "Keep the visual region(s), headline/support/body hierarchy, and explanatory text areas separated in the same spatial rhythm as the sample. "
                    "Do not convert this editorial explainer into a card/callout grid, row list, CTA/product panel, dashboard, chart, or generic poster. "
                    "If topic concepts need icons or data, place them inside the observed editorial visual/text regions rather than creating new card modules."
                )
            elif large_visual_count <= 0 and sample_layout_mode not in {"cover_or_hero_visual", "photo_led_cover"}:
                sample_module_execution_contract = (
                    "SAMPLE MODULE COUNT LOCK: this selected sample page has no large standalone hero visual. "
                    "Do not introduce one; express the topic through the observed sample modules, cards, rows, icons, or callouts."
                )
        sample_layout_instruction = (
            AIOrchestratorService._sample_blueprint_layout_instruction(sample_page_blueprint)
            if style_reference_sample_active and sample_page_blueprint
            else ""
        )
        sample_primary_render_contract = (
            "SELECTED SAMPLE PAGE PRIMARY RENDER CONTRACT: "
            + sample_layout_instruction
            + " This selected sample page contract outranks planner visual-focus text, story-role visual metaphors, generic finance motifs, and reference-family grammar. Match the observed sample module counts and zones before interpreting the topic."
            if sample_layout_instruction
            else ""
        )
        sample_text_block_count = int(
            (sample_page_blueprint.get("module_counts") if isinstance(sample_page_blueprint.get("module_counts"), dict) else {}).get("text_block_count")
            or 0
        )
        sample_support_only_cover = (
            style_reference_sample_active
            and sample_layout_mode in {"cover_or_hero_visual", "photo_led_cover"}
            and 0 < sample_text_block_count <= 2
        )
        carousel_body_for_text_contract = (
            ""
            if (
                style_reference_sample_active
                and sample_layout_mode in {"numbered_or_icon_row_list", "card_callout_grid", "vertical_icon_explainer"}
            )
            or sample_support_only_cover
            else AIOrchestratorService._carousel_slide_body_text(prompt_slide, fallback_text="")
        )
        # Dense sample-led pages push body detail into modules, so the text contract avoids duplicating it as a paragraph.
        text_render_contract = (
            AIOrchestratorService._text_overlay_substrate_contract(
                headline=prompt_slide.get("headline"),
                supporting_line=prompt_slide.get("supporting_line"),
                body=carousel_body_for_text_contract,
                proof_points=AIOrchestratorService._carousel_slide_visible_module_lines(
                    prompt_slide,
                    limit=proof_points_limit,
                ),
                claim_evidence_pairs=prompt_slide.get("claim_evidence_pairs"),
                cta=prompt_slide.get("cta"),
                slide_role=story_role or prompt_slide.get("role"),
            )
            if use_backend_text_overlay
            else AIOrchestratorService._final_text_render_contract(
                headline=prompt_slide.get("headline"),
                supporting_line=prompt_slide.get("supporting_line"),
                body=carousel_body_for_text_contract,
                proof_points=AIOrchestratorService._carousel_slide_visible_module_lines(
                    prompt_slide,
                    limit=proof_points_limit,
                ),
                claim_evidence_pairs=prompt_slide.get("claim_evidence_pairs"),
                cta=prompt_slide.get("cta"),
                slide_role=story_role or prompt_slide.get("role"),
                legal_footer=legal_footer_text,
                proof_points_limit=proof_points_limit,
            )
        )
        factual_visual_anchors = AIOrchestratorService._dedupe_metadata_collection(
            [
                *AIOrchestratorService._normalize_metadata_list(prompt_slide.get("stat_highlights"), limit=3),
                *AIOrchestratorService._claim_evidence_pair_lines(
                    AIOrchestratorService._normalize_claim_evidence_pairs(prompt_slide.get("claim_evidence_pairs"), limit=2),
                    limit=2,
                ),
                *AIOrchestratorService._normalize_metadata_list(prompt_slide.get("proof_points"), limit=3),
                *AIOrchestratorService._normalize_metadata_list(prompt_slide.get("body_points"), limit=2),
            ],
            blocked_texts=[
                prompt_slide.get("headline"),
                prompt_slide.get("supporting_line"),
                prompt_slide.get("cta"),
            ],
            limit=4,
        )
        data_visualization_anchor_sources: list[tuple[str, Any]] = [
            ("stat_highlights", slide.get("stat_highlights")),
            ("claim_evidence_pairs", slide.get("claim_evidence_pairs")),
            ("proof_points", slide.get("proof_points")),
            ("body_points", slide.get("body_points")),
            ("supporting_line", slide.get("supporting_line")),
            (
                "body",
                ""
                if sample_support_only_cover
                else AIOrchestratorService._carousel_slide_body_text(slide, fallback_text=""),
            ),
        ]
        if AIOrchestratorService._prompt_has_explicit_data_anchor(getattr(request, "prompt", "")):
            data_visualization_anchor_sources.insert(0, ("user_prompt", getattr(request, "prompt", "")))
        data_visualization_anchor_lines = AIOrchestratorService._data_visualization_anchor_lines(
            *[value for _, value in data_visualization_anchor_sources],
            limit=8,
        )
        prompt_visible_context_lines = AIOrchestratorService._data_visualization_anchor_lines(
            prompt_slide.get("stat_highlights"),
            prompt_slide.get("claim_evidence_pairs"),
            prompt_slide.get("proof_points"),
            prompt_slide.get("body_points"),
            prompt_slide.get("supporting_line"),
            "" if sample_support_only_cover else AIOrchestratorService._carousel_slide_body_text(prompt_slide, fallback_text=""),
            limit=5,
        )
        broad_visual_context_section = AIOrchestratorService._broad_visual_context_prompt_section(
            prompt_visible_context_lines,
            limit=5,
        )
        structured_anchor_sources = AIOrchestratorService._structured_visual_data_anchor_sources(
            slide,
            prompt=getattr(request, "prompt", ""),
            request=request,
        )
        carousel_visual_intent = " ".join(
            [
                AIOrchestratorService._normalize_metadata_text(slide.get("visual_focus"), limit=420),
                AIOrchestratorService._normalize_metadata_text(slide_metadata.get("visual_direction"), limit=220),
                AIOrchestratorService._normalize_metadata_text(slide_metadata.get("image_prompt"), limit=220),
            ]
        )
        slide_structured_metadata = {
            **slide_metadata,
            "visual_focus": slide.get("visual_focus"),
            "structured_visual_metadata": slide_metadata.get("structured_visual_metadata")
            or slide.get("structured_visual_metadata"),
        }
        structured_visual_metadata = AIOrchestratorService._normalize_structured_visual_metadata(
            metadata=slide_structured_metadata,
            request=request,
            visual_intent=carousel_visual_intent,
            anchor_entries=AIOrchestratorService._structured_visual_anchor_entries(
                structured_anchor_sources,
                request=request,
                limit=8,
            ),
            format_name="carousel slide",
            sample_page_blueprint=sample_page_blueprint,
            visual_plan=visual_plan,
            visual_identity=visual_identity,
            reference_assets=reference_images,
            asset_catalog=request.asset_catalog,
            compiled_context=compiled_context,
        )
        # Structured visual metadata turns scattered slide facts into explicit anchors and permissions for prompts.
        structured_visual_metadata_section = AIOrchestratorService._structured_visual_metadata_prompt_section(
            structured_visual_metadata
        )
        strict_data_visualization_anchor_lines = [
            AIOrchestratorService._normalize_metadata_text(anchor.get("text"), limit=260)
            for anchor in structured_visual_metadata.get("data_anchors", [])
            if isinstance(anchor, dict)
            and AIOrchestratorService._normalize_metadata_text(anchor.get("text"), limit=260)
        ]
        data_visualization_contract = AIOrchestratorService._data_visualization_contract(
            request=request,
            visual_intent=carousel_visual_intent,
            anchors=strict_data_visualization_anchor_lines,
            format_name="carousel slide",
        )
        slide_semantic_visual_brief = AIOrchestratorService._normalize_metadata_text(
            " ".join(
                part
                for part in [
                    getattr(request, "prompt", ""),
                    prompt_slide.get("headline"),
                    prompt_slide.get("supporting_line"),
                    carousel_body_for_text_contract,
                    prompt_slide.get("visual_focus"),
                    "; ".join(factual_visual_anchors),
                ]
                if AIOrchestratorService._coerce_text_value(part).strip()
            ),
            limit=340,
        )
        dynamic_visual_art_direction = AIOrchestratorService._dynamic_visual_art_direction_section(
            request=request,
            format_name="carousel slide",
            semantic_visual_brief=slide_semantic_visual_brief,
            visual_intent=carousel_visual_intent,
            structured_visual_metadata=structured_visual_metadata,
            visual_plan=visual_plan,
            visual_identity=visual_identity,
            data_visualization_anchor_lines=data_visualization_anchor_lines,
            sample_page_blueprint=sample_page_blueprint,
            slide_purpose=" ".join(
                part
                for part in [story_role, carousel_archetype, slide.get("role")]
                if AIOrchestratorService._coerce_text_value(part).strip()
            ),
            reference_assets=reference_images,
            asset_catalog=request.asset_catalog,
            compiled_context=compiled_context,
        )
        # Dynamic art direction combines the current slide content, sample permissions, and allowed visual systems.
        reference_creative_profile = AIOrchestratorService._resolve_reference_creative_profile(
            request=request,
            format_name="carousel slide",
            sample_page_blueprint=sample_page_blueprint,
            visual_identity=visual_identity,
            visual_plan=visual_plan,
            structured_visual_metadata=structured_visual_metadata,
            layout_budget=layout_content_budget,
            slide=prompt_slide,
            metadata=prompt_slide_metadata,
            slide_purpose=" ".join(
                part
                for part in [story_role, carousel_archetype, prompt_slide.get("role")]
                if AIOrchestratorService._coerce_text_value(part).strip()
            ),
            reference_assets=reference_images,
            asset_catalog=request.asset_catalog,
            compiled_context=compiled_context,
            logo_position_hint=logo_position_hint,
        )
        # The reference profile decides what can be borrowed visually without copying literal reference content.
        reference_creative_profile_section = AIOrchestratorService._reference_creative_profile_prompt_section(
            reference_creative_profile
        )
        pinned_template_adaptation_profile_section = AIOrchestratorService._pinned_template_adaptation_profile_prompt_section(
            prompt_slide_metadata.get("pinned_template_adaptation_profile")
            or slide_metadata.get("pinned_template_adaptation_profile")
        )
        reference_adaptation_profile_section = AIOrchestratorService._reference_adaptation_profile_prompt_section(
            compiled_context.get("reference_adaptation_profile")
        )
        generation_surface_contract_section = AIOrchestratorService._generation_surface_contract_prompt_section(
            compiled_context.get("generation_surface_contract")
        )
        layout_quality_guard = AIOrchestratorService._dynamic_layout_quality_guard_section(
            request=request,
            format_name="carousel slide",
            text_payload=prompt_slide_text_payload,
            metadata={
                **slide_metadata,
                "supporting_line": prompt_slide.get("supporting_line"),
                "proof_points": prompt_slide.get("proof_points"),
                "body_points": prompt_slide.get("body_points"),
                "stat_highlights": prompt_slide.get("stat_highlights"),
                "claim_evidence_pairs": prompt_slide.get("claim_evidence_pairs"),
                "cta": prompt_slide.get("cta"),
            },
            sample_page_blueprint=sample_page_blueprint,
            structured_visual_metadata=structured_visual_metadata,
            data_visualization_anchor_lines=data_visualization_anchor_lines,
            visual_intent=carousel_visual_intent,
            visual_plan=visual_plan,
            visual_identity=visual_identity,
            reference_assets=reference_images,
            asset_catalog=request.asset_catalog,
            scene_graph=scene_graph,
            legal_footer_text=legal_footer_text,
            disclaimer_guidance=disclaimer_overlay_guidance,
            layout_budget=layout_content_budget,
        )
        if (
            style_reference_sample_active
            and not AIOrchestratorService._has_explicit_data_visual_request(
                getattr(request, "prompt", ""),
                text_payload=None,
            )
        ):
            # Style-reference samples should not suddenly gain charts just because the topic has numbers.
            data_visualization_contract = (
                "Style-reference data-visual guard: do not invent charts, graphs, tables, dashboards, metric tiles, axes, or numeric plots from topic numbers alone. "
                "Use those structures only when they are visibly present on the selected sample page; otherwise express facts through the same non-data modules, image slots, cards, rows, or callouts used by that sample page."
            )
        reference_template_name = AIOrchestratorService._normalize_metadata_text(
            slide_metadata.get("reference_template_name"),
            limit=96,
        )
        sample_template_relevant = AIOrchestratorService._template_label_is_semantically_relevant(
            request,
            reference_template_name
            or ((getattr(creative_decision, "selected_template_name", "") if isinstance(getattr(creative_decision, "selected_template_name", ""), str) else "") or "")
            or (((compiled_context.get("template_fit_brief") or {}) if isinstance(compiled_context.get("template_fit_brief"), dict) else {}).get("template_name") or ""),
        )
        request_template_context = request.template_context if isinstance(request.template_context, dict) else {}
        has_request_sequence_pack = isinstance(request_template_context.get("sequence_pack"), dict)
        resolved_request_sequence_pack = AIOrchestratorService._template_sequence_pack(
            request,
            creative_decision=creative_decision,
        )
        sequence_pack_relevant = bool(
            resolved_request_sequence_pack
            or (
                AIOrchestratorService._prompt_sequence_pack(compiled_context)
                and not has_request_sequence_pack
            )
        )
        if not sequence_pack_relevant:
            # If no relevant sequence pack remains, remove family/sequence guidance so it cannot pollute this slide.
            sequence_alignment_sections = []
            reference_family_contract = []
        if not sample_template_relevant:
            sample_alignment_sections = []
        if sample_metadata_contaminated and template_surface_policy == "style_reference_only":
            # Contaminated metadata means the selected image is trusted, but derived family alignment is not.
            reference_family_contract = []
            sample_alignment_sections = []
        reference_slide_index = int(slide_metadata.get("reference_slide_index") or slide_index)
        reference_slide_count = int(slide_metadata.get("reference_slide_count") or slide_count)
        layout_reference_page_index = int(
            slide_metadata.get("conditioning_page_index")
            or slide_metadata.get("reference_page_index")
            or slide_metadata.get("page_index")
            or reference_slide_index
            or 1
        )
        topic_reference_page_index = int(slide_metadata.get("reference_topic_page_index") or 0)
        reference_image_guidance = (
            (
                "Reference image execution: the selected sample page is the primary layout/style reference. "
                "Preserve its visible spatial grammar, object-count rhythm, image/text balance, section partitions, and module structure; replace the literal copy and topic-specific objects with the approved current-slide content."
            )
            if style_reference_sample_active and sequence_pack_relevant
            else f"Reference images available for composition: {reference_summary}."
        )
        layout_anchor_guidance = (
            (
                f"Authoritative layout anchor for this slide: use the selected sample surface from '{reference_template_name or layout_anchor_asset_path}' "
                f"as slide {reference_slide_index} of {reference_slide_count} (condition on page {layout_reference_page_index} when the anchor is a PDF). "
                + (
                    "This anchor controls geometry, spacing rhythm, module cadence, and negative-space structure."
                    if has_reference_zone_map
                    else "This anchor controls page-specific layout cadence, module hierarchy, whitespace behavior, and closing-slide/CTA posture even when an explicit zone-map is thin."
                )
            )
            if layout_anchor_asset_path and template_surface_policy == "style_reference_only"
            else ""
        )
        topic_anchor_guidance = (
            (
                f"Supporting topic-evidence reference: '{topic_anchor_asset_path}'"
                + (
                    f" (condition on page {topic_reference_page_index})"
                    if topic_reference_page_index > 0
                    else ""
                )
                + ". Use it only for topical objects, evidence cues, and subject-matter plausibility; it must not replace the selected sample's layout skeleton."
            )
            if topic_anchor_asset_path
            else ""
        )
        sample_page_visual_obedience = (
            (
                "Sample-page visual obedience rule: the supplied conditioning image for this slide is the selected sample page, not a loose mood board. "
                "Match its visible composition category, visual-slot count, section partitions, typography scale rhythm, color blocking, and object family before inventing any new motif. "
                "If the sample page is a photo-led cover, use a photo/composite-led cover; if it is a numbered row list, use numbered row/list modules; if it is card/callout-led, use matching card/callout modules; if it is timeline/path-led, keep that path structure. "
                "Do not replace the sample page with generic finance imagery such as rupee symbols, rising bars, pie charts, documents, magnifiers, dashboards, or random icons unless that visual category is visibly present in the selected sample page or explicitly requested by the user."
            )
            if template_surface_policy == "style_reference_only" and layout_anchor_asset_path
            else ""
        )
        sample_aware_data_visual_rule = (
            (
                "Sample-aware data visual rule: for a style-reference carousel, a table, chart, graph, dashboard, map, metric tile, or numeric plot is allowed only when both conditions are true: the approved slide copy has valid data anchors and the selected sample page visibly uses that kind of data/module structure, unless the user explicitly requested that visual type. "
                "Numbers in the topic alone are not permission to invent bar charts, axes, percentages, pie slices, or dashboard furniture. When the sample page uses rows, cards, icons, photo composites, or callout panels, express the approved facts through those same structures instead."
            )
            if template_surface_policy == "style_reference_only" and layout_anchor_asset_path
            else ""
        )
        reference_anchor_guidance = (
            (
                f"Primary layout anchor for this slide: uploaded reference '{reference_template_name}' "
                f"(reference slide {reference_slide_index} of {reference_slide_count})."
            )
            if reference_template_name and sample_template_relevant and template_surface_policy != "style_reference_only"
            else (
                f"Preserve the narrative position of slide {reference_slide_index} of {reference_slide_count}, "
                "and adapt the selected sample page's layout skeleton closely while replacing only the content, literal text, and topic-specific objects."
                if style_reference_sample_active and sequence_pack_relevant
                else ""
            )
        )
        sample_metadata_guard = (
            "Selected sample metadata guard: stale or cross-sample text-derived DNA was detected and ignored. "
            "Use the selected sample page image itself as authority for layout, visual-slot count, section partitioning, spacing, object scale, and content density."
            if style_reference_sample_active and sample_metadata_contaminated
            else ""
        )
        sample_surface_adaptation_guard = (
            "Style-reference sample adaptation: adapt the selected sample page's layout skeleton closely. "
            "Preserve its region proportions, visual-slot count, section partitions, object scale, spacing rhythm, image/text balance, and content density while replacing literal copy and topic-specific objects."
            if style_reference_sample_active
            else ""
        )
        template_copy_authority_guard = (
            "Pinned template copy authority guard: treat selected templates as visual/layout references only. "
            "Do not copy or paraphrase sample text, sample headlines, sample body copy, or sample supporting lines. "
            "Generate fresh content for the user's input using the approved slide copy."
            if style_reference_sample_active and AIOrchestratorService._request_uses_template_adaptance(request)
            else ""
        )
        sample_logo_exclusion_contract = (
            "SAMPLE LOGO EXCEPTION: when following the selected sample page, copy only the empty logo-safe zone geometry and surrounding whitespace. "
            "Do not copy the sample page's logo artwork, wordmark, brand name, symbol cluster, or any top-corner brand mark; that area must stay blank because the exact stored asset is overlaid after generation."
            if style_reference_sample_active
            else ""
        )
        sample_editorial_render_contract = ""
        if style_reference_sample_active:
            sample_editorial_parts = [
                f"editorial_role={AIOrchestratorService._normalize_metadata_text(slide_metadata.get('sample_page_editorial_role'), limit=40)}",
                f"copy_behavior={AIOrchestratorService._normalize_metadata_text(slide_metadata.get('sample_page_copy_behavior'), limit=48)}",
                f"copy_density={AIOrchestratorService._normalize_metadata_text(slide_metadata.get('sample_page_copy_density'), limit=24)}",
                f"closing_grammar={AIOrchestratorService._normalize_metadata_text(slide_metadata.get('sample_page_closing_grammar'), limit=40)}",
            ]
            sample_editorial_summary = "; ".join(part for part in sample_editorial_parts if not part.endswith("="))
            if sample_editorial_summary:
                sample_editorial_render_contract = (
                    f"SELECTED SAMPLE EDITORIAL CONTRACT: {sample_editorial_summary}. "
                    "This controls creative posture: hook pages need the sample's entry tension and hero/image weight; mechanism pages need the sample's density and module grammar; undercovered-angle pages need analytical reveal behavior; macro-takeaway closing pages must not become product/platform promotion unless the sample closing grammar is product_cta."
                )
        role_visual_language_guidance = (
            "Story-role visual language is secondary to the selected sample page: keep the sample page's visual-slot structure and module rhythm first, then adapt subject matter to this slide's story role."
            if style_reference_sample_active
            else "Role-based visual language: hook slides may use a broad conceptual anchor; structure slides should show mechanism/process; undercovered-angle slides should show evidence, documents, or hidden-layer metaphors; strategic-meaning slides should show network/map/outcome systems; closing slides should show an action/product/context surface."
        )
        broad_brand_layout_guidance_enabled = not style_reference_sample_active
        sections = [
            (
                f"Create the visual substrate for slide {slide_index} of {slide_count} in one cohesive premium branded carousel series."
                if use_backend_text_overlay
                else f"Create the finished slide {slide_index} of {slide_count} in one cohesive premium branded carousel series."
            ),
            sample_primary_render_contract,
            sample_module_execution_contract,
            sample_logo_exclusion_contract,
            "Brand visual constraints: Use the approved color palette and styling, but never write the brand name or place any standalone graphic or signature mark anywhere in the canvas.",
            "Never typeset the brand name in the corners.",
            "Do not typeset the brand name as a top-corner signature.",
            f"Keep the {reserved_logo_area} area completely empty and visually clean. Do not place any headline, body copy, supporting text, proof point, CTA, icon, or visual element inside or immediately adjacent to this corner.",
            *AIOrchestratorService._logo_non_generation_contract(
                brand_name=brand_name,
                reserved_logo_area=reserved_logo_area,
            ),
            disclaimer_overlay_guidance,
            f"Platform: {platform}.",
            f"Output type: {file_type}.",
            canvas_fit_guidance,
            layout_quality_guard,
            f"Creative mode: {creative_decision.layout_mode}.",
            (
                (
                    f"Reference-zone geometry contract JSON: {geometry_contract}. Treat these regions as the authoritative slide skeleton. Preserve region proportions, spacing rhythm, and negative space closely; only make tiny readability adjustments and never recompose the slide from scratch."
                    if has_reference_zone_map
                    else f"Scene-graph geometry contract JSON: {geometry_contract}. Preserve these normalized regions closely; only make tiny adjustments for readability and never recompose the slide from scratch."
                )
                if geometry_contract
                else ""
            ),
            reference_zone_guidance,
            layout_anchor_guidance,
            sample_page_visual_obedience,
            sample_aware_data_visual_rule,
            sample_surface_adaptation_guard,
            template_copy_authority_guard,
            sample_editorial_render_contract,
            sample_metadata_guard,
            topic_anchor_guidance,
            reference_anchor_guidance,
            (
                f"Template/layout DNA contract JSON: {layout_dna_contract}. Use this as the authoritative slide skeleton for region balance, hierarchy, and spacing."
                if layout_dna_contract
                else ""
            ),
            (
                f"STRICT SAMPLE LAYOUT CONTRACT JSON: {strict_sample_layout_contract}. This is mandatory: match the sample's zone counts, image count, text density, section partitions, spacing geometry, image style, color discipline, and closing grammar before adding creative interpretation."
                if strict_sample_layout_contract
                else ""
            ),
            (
                f"SAMPLE METADATA FALLBACK CONTRACT JSON: {thin_sample_conditioning_contract}. The sample analysis is thin, so this fallback contract is mandatory: preserve selected-sample layout authority, story-role partitioning, proof/module count, visual-focus specificity, and anti-generic image rules."
                if thin_sample_conditioning_contract
                else ""
            ),
            "Respect the reference/template layout strictly: preserve the contracted regions, spacing rhythm, text-safe negative space, and image-zone discipline before adding decorative detail.",
            f"Slide role: {slide.get('role')}.",
            f"Story role: {story_role}.",
            (f"Carousel archetype: {carousel_archetype}." if carousel_archetype else ""),
            AIOrchestratorService._carousel_story_role_guidance(story_role),
            "Carousel visual-diversity contract: do not reuse the same hero subject, gesture, icon, chart, document, flag, or stock business motif across slides. Make this slide's image metaphor unique to its story role while keeping the series visually consistent.",
            role_visual_language_guidance,
            f"Campaign theme: {message_theme}.",
            f"Emotional direction: {emotional_direction}.",
            *text_render_contract,
            (
                (
                    f"This slide needs {len(AIOrchestratorService._carousel_slide_visible_module_lines(prompt_slide, limit=proof_points_limit))} proof/callout module shell(s); reserve those modules as empty text-safe cards or callouts for backend overlay."
                    if use_backend_text_overlay
                    else f"This slide needs {len(AIOrchestratorService._carousel_slide_visible_module_lines(prompt_slide, limit=proof_points_limit))} proof/callout module(s); render the approved readable copy once inside those modules with clean hierarchy and no duplicate shadow text."
                )
                if AIOrchestratorService._carousel_slide_visible_module_lines(prompt_slide, limit=proof_points_limit)
                else "Do not repeat the factual bullet list on this slide unless the story role explicitly requires it."
            ),
            (
                (
                    "If claim-evidence anchors are present, express them through visual evidence and empty callout shells only; leave the approved words for backend overlay and do not fabricate extra stats or labels."
                    if use_backend_text_overlay
                    else "If claim-evidence anchors are present, render only the approved claim/evidence wording once in the designated modules and do not fabricate extra stats, labels, or duplicate text."
                )
                if AIOrchestratorService._normalize_claim_evidence_pairs(slide.get("claim_evidence_pairs"), limit=2)
                else ""
            ),
            (
                "Visual evidence contract: make the image system explain these approved specifics rather than falling back to generic treaty symbolism: "
                + "; ".join(f'"{item}"' for item in factual_visual_anchors)
                + "."
                if factual_visual_anchors
                else ""
            ),
            data_visualization_contract,
            broad_visual_context_section,
            structured_visual_metadata_section,
            dynamic_visual_art_direction,
            reference_adaptation_profile_section,
            generation_surface_contract_section,
            reference_creative_profile_section,
            pinned_template_adaptation_profile_section,
            (
                (
                    "If this slide includes a CTA, reserve only an empty premium CTA-safe shell in the CTA region; do not render the CTA words in the AI image."
                    if use_backend_text_overlay
                    else "If this slide includes a CTA, render only the approved CTA wording once inside the CTA region with clear contrast and no button duplication elsewhere."
                )
                if AIOrchestratorService._normalize_metadata_text(prompt_slide.get("cta"), limit=80)
                else (
                    "Do not add a CTA button on this slide; preserve only a thin quiet legal-footer-safe strip at the bottom."
                    if legal_footer_text
                    else "Do not add a CTA button or footer treatment on this slide."
                )
            ),
            research_quality_section,
            *consultant_contract,
            *multimodal_balance_contract,
            *reference_family_contract,
            *visual_style_policy_sections,
            *continuity_sections,
            *sequence_alignment_sections,
            *sample_alignment_sections,
            (
                f"Visual focus for this slide: {visual_focus_for_prompt}."
                if visual_focus_for_prompt
                else ""
            ),
            story_role_visual_execution,
            (
                f"Transition note from the sequence: {transition_note_for_prompt}."
                if transition_note_for_prompt
                else ""
            ),
            f"Brand palette to honor: {palette}.",
            f"Palette role guidance: {palette_guidance}.",
            strict_palette_contract,
            palette_execution_contract,
            f"Typography direction: {typography}.",
            (
                "Brand-system priority for this style-reference slide: use brand palette, typography, and corner-safe behavior as guardrails, but let the selected sample page control layout, image slots, module count, spacing, and visual subject category."
                if style_reference_sample_active
                else ""
            ),
            f"Brand design-system layout guidance: {design_system_guidance.get('layout')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("layout") else "",
            f"Preferred zone roles from the brand system: {design_system_guidance.get('zones')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("zones") else "",
            f"Background style guidance from the brand system: {design_system_guidance.get('background')}." if design_system_guidance.get("background") else "",
            f"Motif guidance from the brand system: {design_system_guidance.get('motifs')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("motifs") else "",
            f"Hierarchy guidance from the brand system: {design_system_guidance.get('hierarchy')}." if design_system_guidance.get("hierarchy") else "",
            f"Content-structure guidance from the brand system: {design_system_guidance.get('content_structure')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("content_structure") else "",
            f"Image-treatment guidance from the brand system: {design_system_guidance.get('image_treatment')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("image_treatment") else "",
            f"Visual-craft guidance from the brand system: {design_system_guidance.get('visual_craft')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("visual_craft") else "",
            f"Composition guidance from the brand system: {design_system_guidance.get('composition')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("composition") else "",
            f"Subject guidance from the brand system: {design_system_guidance.get('subjects')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("subjects") else "",
            f"Editorial rhythm guidance from the brand system: {design_system_guidance.get('editorial')}." if broad_brand_layout_guidance_enabled and design_system_guidance.get("editorial") else "",
            f"Brand-cue guidance from the brand system: {design_system_guidance.get('brand_cues')}." if design_system_guidance.get("brand_cues") else "",
            *grounding_sections,
            reference_image_guidance,
            "Current slide subject matter overrides reference subject matter: preserve only approved palette, spacing, layout rhythm, and visual craft from references; never import unrelated objects, industries, products, or scenes from a template.",
            (
                "Hard per-slide layout lock: use the reference slide zone-map as the authoritative geometry skeleton. Keep the headline, support copy, image region, CTA region, and legal strip in the same proportional neighborhoods instead of collapsing them into a single full-canvas content block."
                if geometry_contract
                else ""
            ),
            (
                f"Exact per-slide geometry manifest (treat as mandatory, not inspirational): {geometry_contract}."
                if geometry_contract
                else ""
            ),
            (
                f"Reference zone-map execution summary: {reference_zone_guidance} Reproduce this slide's layout archetype and spacing rhythm strictly; do not substitute a generic poster, centered stack, or unrelated carousel pattern."
                if reference_zone_guidance
                else ""
            ),
            "Keep this slide visually consistent with the rest of the carousel series in palette, mood, and hierarchy.",
            (
                "Use one clean composition with slide-safe spacing, strong focal emphasis, and clean future-copy integration."
                if use_backend_text_overlay
                else "Use one clean composition with slide-safe spacing, strong focal emphasis, and integrated readable copy that feels native to the design rather than overlaid afterward."
            ),
            "Do not crop or crowd any reserved text, CTA, corner-safe, or legal-safe region. If space is tight, simplify the visual substrate instead of pushing zones to the edge.",
            "All reserved slide text and CTA surfaces must remain fully inside the export frame; do not let bottom shells, cards, dividers, or image subjects touch or cross the crop boundary.",
            "For square or portrait carousel exports, do not let bottom buttons, bullets, or lower text touch or cross the crop boundary.",
            (
                "Make the slide visual communicate the exact approved idea from this slide's copy intent and user prompt, while leaving all approved words for backend overlay."
                if use_backend_text_overlay
                else "Make the slide visual communicate the exact approved idea from this slide's copy intent and user prompt while rendering the approved readable copy directly into the finished slide."
            ),
            visual_plan_guidance,
            "Prefer content-specific explanatory imagery, comparison cues, process metaphors, product-context objects, or restrained data/diagram anchors over a generic business person.",
            "Use people only when their action or setting directly explains the slide message; do not default to a standalone portrait for investor confidence or expertise themes.",
            (
                "Avoid defaulting to generic handshake, flag-only, treaty-signing, or stock business celebration visuals unless the approved slide copy explicitly centers bilateral symbolism or ceremony."
                if not style_reference_sample_active
                else "Do not add generic finance, treaty, or business motifs unless that motif family is visibly part of the selected sample page or explicitly required by the approved slide copy."
            ),
            (
                "If the brand design system implies airy hierarchy or generous whitespace, keep the slide cleaner and preserve negative space instead of overfilling every region."
                if "airy" in str(design_system_guidance.get("hierarchy") or "").casefold() or "generous" in str(design_system_guidance.get("hierarchy") or "").casefold()
                else ""
            ),
            (
                "If the brand design system implies comparison, steps, or data-story structure, let this slide use modular explainer composition rather than a plain poster block."
                if any(token in str(design_system_guidance.get("content_structure") or "").casefold() for token in ("comparison", "data", "benefit", "steps"))
                else ""
            ),
            (
                "Bias away from generic business-person imagery when the brand design system points toward diagram-led, icon-led, editorial, or abstract treatment."
                if any(token in str(design_system_guidance.get("image_treatment") or "").casefold() for token in ("diagram", "icon", "editorial", "abstract", "illustration"))
                else ""
            ),
            "Respect the reference/template layout for this slide strictly by keeping visual emphasis inside the intended image region, preserving text-safe negative space, and maintaining the sample's composition skeleton.",
            (
                "Do not copy the uploaded sample slide's literal text, logo, or exact original artwork. "
                "Do preserve its region proportions, section partitions, visual-slot count, object scale, spacing rhythm, balance, and sequencing cues as closely as possible."
                if template_surface_policy == "style_reference_only"
                else ""
            ),
            "Do not render fake UI chrome, logos, or unrelated charts unless they are directly implied by the supplied text.",
            (
                "Do not render any readable headline, body copy, proof text, CTA words, disclaimer text, pseudo-text, or decorative lettering in the AI image; the backend will overlay the approved copy after generation."
                if use_backend_text_overlay
                else "Render only the approved readable copy once. Do not add duplicate shadow headlines, floating labels, pseudo-text, decorative lettering, or any extra wording outside the approved text-safe regions."
            ),
            "CRITICAL TEXT WRAPPING RULE: You must aggressively wrap the headline and body text so they stay strictly inside their designated coordinate zones. Never let any letters or words stretch into the 'empty space' corner. Keep the 'empty space' 100% blank and free of text so the backend overlay does not clash with your lettering.",
            anti_hallucination_section,
            AIOrchestratorService._normalize_metadata_text(retry_note, limit=220),
        ]
        optional_prefixes = (
            "Brand design-system layout guidance:",
            "Preferred zone roles from the brand system:",
            "Background style guidance from the brand system:",
            "Motif guidance from the brand system:",
            "Hierarchy guidance from the brand system:",
            "Content-structure guidance from the brand system:",
            "Image-treatment guidance from the brand system:",
            "Visual-craft guidance from the brand system:",
            "Composition guidance from the brand system:",
            "Subject guidance from the brand system:",
            "Editorial rhythm guidance from the brand system:",
            "Brand-cue guidance from the brand system:",
            "If the brand design system implies airy hierarchy",
            "If the brand design system implies comparison",
            "Bias away from generic business-person imagery",
            "Reference family contract:",
            "Reference family layout lock:",
            "Reference family zone grammar:",
            "Reference family module grammar:",
            "Reference family density target:",
            "Image/text balance target:",
            "Reference family spacing rhythm:",
            "Reference family composition target:",
            "Reference family craft target:",
            "Reference family subject target:",
            "Carousel continuity contract:",
            "Carousel pacing target:",
            "Composition evolution target:",
            "Whitespace target:",
            "Adjacency contract:",
            "Anti-poster rule:",
            "Brand knowledge grounding mode:",
            "Brand knowledge grounding:",
            "Because retrieved brand knowledge is absent,",
            "If retrieved brand knowledge is present,",
            "Template-derived cues were suppressed",
            "If the active brand grounding is fallback-only,",
            "Layout approach:",
            "Dominant visual system:",
            "Supporting visual system:",
            "Reusable brand design assets available as stylistic cues:",
        )
        required_sections: list[str] = []
        optional_sections: list[str] = []
        for section in sections:
            cleaned = str(section or "").strip()
            if not cleaned or cleaned.endswith(": ."):
                continue
            target = optional_sections if cleaned.startswith(optional_prefixes) else required_sections
            target.append(cleaned)
        template_reference_prompt_active = (
            style_reference_sample_active
            or AIOrchestratorService._request_uses_template_adaptance(request)
            or (
                not AIOrchestratorService._request_uses_content_intelligence(request)
                and (
                    AIOrchestratorService._compiled_context_has_authoritative_layout(compiled_context)
                    or str((getattr(request, "layout_decision", None) or {}).get("mode") or "").strip().lower()
                    in {"adapted_template", "exact_template"}
                )
            )
        )
        prompt_limit = AIOrchestratorService.DEFAULT_CAROUSEL_IMAGE_PROMPT_MAX_LENGTH
        if template_reference_prompt_active:
            prompt_limit = AIOrchestratorService.CAROUSEL_IMAGE_PROMPT_MAX_LENGTH
        elif strict_sample_layout_contract and thin_sample_conditioning_contract:
            prompt_limit = AIOrchestratorService.CAROUSEL_STRICT_SAMPLE_IMAGE_PROMPT_MAX_LENGTH
        prompt = AIOrchestratorService._compose_prompt_sections(
            required_sections=required_sections,
            optional_sections=optional_sections,
            limit=prompt_limit,
        )
        prompt = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(prompt)
        # Returns one provider-ready prompt for a single carousel slide; the caller repeats this for each slide asset.
        # The prompt limit grows when a template/sample is authoritative because the slide needs extra layout contracts.
        return AIOrchestratorService._trim_prompt(prompt, prompt_limit)

    @staticmethod
    def build_logo_composite_prompt(
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        scene_graph: GenerationSceneGraph,
        selected_logo_variant: str | None = None,
    ) -> str:
        # Builds logo composite prompt from request state, copy payload, and scene graph for AIOrchestratorService.
        # It calls _normalize_metadata_text and _trim_prompt while assembling the payload or prompt text.
        platform = str(request.studio_panel.get("platform_preset") or "social")
        format_name = str(request.studio_panel.get("format") or "static")
        headline = AIOrchestratorService._normalize_metadata_text(text_payload.headline, limit=140)
        supporting_line = AIOrchestratorService._normalize_metadata_text(
            (text_payload.metadata or {}).get("supporting_line") or text_payload.body,
            limit=180,
        )
        variant_guidance = (
            f"Use the logo variant best matching this request: {selected_logo_variant}."
            if selected_logo_variant
            else ""
        )
        prompt = AIOrchestratorService._trim_prompt(
            " ".join(
                [
                    "Edit the first input image by compositing the exact brand mark from the second input image into the masked region only.",
                    f"Platform: {platform}.",
                    f"Format: {format_name}.",
                    f"Headline context: {headline}.",
                    f"Supporting copy context: {supporting_line}.",
                    variant_guidance,
                    "Use the second image exactly as the brand mark reference.",
                    "Preserve the mark wording, colors, shape, and aspect ratio with high fidelity.",
                    "Place the mark cleanly inside the masked area with professional spacing and no distortion.",
                    "Do not change any other part of the base creative outside the masked region.",
                    "Do not invent a new mark, do not stylize it, and do not add any extra text.",
                ]
            ),
            AIOrchestratorService.IMAGE_PROMPT_MAX_LENGTH,
        )
        prompt = re.sub(r"(?i)(?<![a-zA-Z])logo(s)?(?![a-zA-Z])", "brand mark", prompt)
        return re.sub(r"(?i)corner_safe_zone", "brand mark", prompt)

    @staticmethod
    def build_image_prompt(
        request: AIOrchestrationRequest,
        text_payload: StructuredTextPayload,
        creative_decision: CreativeDecisionPayload | None = None,
        message_strategy: MessageStrategyPayload | None = None,
        compiled_context: dict[str, Any] | None = None,
        visual_explanation_plan: dict[str, Any] | None = None,
    ) -> str:
        # Builds image prompt from request state, copy payload, and creative decision for AIOrchestratorService.
        # It calls _coerce_text_value and _llm_led_static_infographic_format while assembling the payload or prompt text.
        compiled_context = compiled_context or {}
        creative_decision = creative_decision or CreativeDecisionPayload()
        if not isinstance(text_payload, StructuredTextPayload):
            text_payload = StructuredTextPayload(
                headline=AIOrchestratorService._coerce_text_value(getattr(text_payload, "headline", "")),
                body=AIOrchestratorService._coerce_text_value(getattr(text_payload, "body", "")),
                cta=AIOrchestratorService._coerce_text_value(getattr(text_payload, "cta", "")),
                hashtags=list(getattr(text_payload, "hashtags", []) or []),
                metadata=(
                    dict(getattr(text_payload, "metadata", {}) or {})
                    if isinstance(getattr(text_payload, "metadata", {}), dict)
                    else {}
                ),
            )
        request_prompt = AIOrchestratorService._coerce_text_value(getattr(request, "prompt", ""))
        studio_panel = getattr(request, "studio_panel", {}) or {}
        resolved_brand_context = getattr(request, "resolved_brand_context", {}) or {}
        reference_asset_list = getattr(request, "reference_assets", []) or []
        asset_catalog = getattr(request, "asset_catalog", []) or []
        layout_decision_payload = getattr(request, "layout_decision", {}) or {}
        format_name = str(studio_panel.get("format") or "static").strip().lower()
        llm_led_static_infographic = AIOrchestratorService._llm_led_static_infographic_format(format_name)
        # Sanitize visual metadata first so stale model-generated imagery cannot leak into later prompt sections.
        metadata, sanitized_visual_fields = AIOrchestratorService._sanitize_visual_metadata_fields(
            text_payload.metadata or {},
            compiled_context=compiled_context,
        )
        metadata, infographic_sanitized_visual_fields = AIOrchestratorService._sanitize_infographic_surface_visual_metadata_fields(
            metadata,
            request_prompt=request_prompt,
            format_name=format_name,
        )
        sanitized_visual_fields = [
            *sanitized_visual_fields,
            *[
                field
                for field in infographic_sanitized_visual_fields
                if field not in sanitized_visual_fields
            ],
        ]
        visual_identity = resolved_brand_context.get("visual_identity", {}) if isinstance(resolved_brand_context, dict) else {}
        palette = AIOrchestratorService._compact_palette_summary(visual_identity)
        palette_guidance = AIOrchestratorService._palette_role_guidance(visual_identity)
        strict_palette_contract = AIOrchestratorService._strict_palette_contract(visual_identity)
        typography = AIOrchestratorService._compact_typography_summary(visual_identity)
        asset_strategy = (creative_decision.asset_strategy if creative_decision else {}) or {}
        template_surface_policy = AIOrchestratorService._effective_template_surface_policy(
            request,
            creative_decision=creative_decision,
            metadata=metadata,
        )
        # Asset strategy decides whether brand assets are literal inputs, broad style cues, or suppressed entirely.
        dominant_visual_system = AIOrchestratorService._normalized_dominant_visual_system(asset_strategy) or "generated_image"
        supporting_visual_system = AIOrchestratorService._normalized_supporting_visual_system(asset_strategy)
        explicit_data_visual_request = AIOrchestratorService._has_explicit_data_visual_request(
            request_prompt,
            text_payload=text_payload,
        )
        iconography_supported = (
            format_name in {"carousel", "infographic"}
            or supporting_visual_system == "icon_sequence"
            or bool(asset_strategy.get("icon_sequence"))
        )
        include_reference_system = (
            dominant_visual_system in {"reference_assets", "icon_sequence"}
            or supporting_visual_system in {"reference_assets", "icon_sequence"}
            or (iconography_supported and bool(asset_strategy.get("use_brand_reference_assets")))
        )
        reusable_assets = (
            AIOrchestratorService._compact_named_items(
                visual_identity.get("reusable_design_assets"),
                limit=4,
            )
            if include_reference_system
            else "Do not directly embed brand icon sets or decorative assets into the generated image."
        )
        proof_points = AIOrchestratorService._compact_named_items(metadata.get("proof_points"), limit=4)
        stat_highlights = AIOrchestratorService._compact_named_items(metadata.get("stat_highlights"), limit=3)
        reference_assets = (
            "Sequence-approved style guidance only; do not recreate, trace, or literalize uploaded sample slides."
            if template_surface_policy == "style_reference_only"
            else (
                AIOrchestratorService._compact_reference_assets(reference_asset_list)
                if include_reference_system
                else "Use these only as abstract style guidance, not as literal icon or logo content."
            )
        )
        layout_decision = AIOrchestratorService._compact_layout_decision(layout_decision_payload)
        if llm_led_static_infographic:
            # LLM-led static/infographic posts must not inherit old template geometry or reference-content authority.
            reference_assets = (
                "Reference/template assets have no content or layout authority for this static/infographic ad creative; "
                "use them only for broad brand style if they reinforce the current prompt."
            )
            layout_decision = (
                "AI-led paid-social composition; choose the most attractive topic-specific ad layout dynamically, "
                "reserve only the backend logo/footer safe areas, and do not preserve template geometry."
            )
        if template_surface_policy == "style_reference_only" and "template " in layout_decision.casefold():
            layout_decision = (
                "adapted sequence-guided composition using approved spacing, hierarchy, and zones only; "
                "do not reproduce the uploaded sample artwork"
            )
        design_style = AIOrchestratorService._normalize_metadata_text(metadata.get("design_style"), limit=80)
        visual_direction = AIOrchestratorService._normalize_metadata_text(metadata.get("visual_direction"), limit=180)
        preferred_scene = AIOrchestratorService._normalize_metadata_text(metadata.get("image_prompt"), limit=220)
        visual_knowledge_brief = ContextCompilerService.coerce_visual_knowledge_brief(
            compiled_context.get("visual_knowledge_brief"),
        )
        direct_brand_grounding = AIOrchestratorService._brand_knowledge_visual_grounding(
            visual_knowledge_brief,
        )
        grounding_mode = AIOrchestratorService._normalize_metadata_text(
            visual_knowledge_brief.get("grounding_mode"),
            limit=32,
        )
        grounding_strength = AIOrchestratorService._normalize_metadata_text(
            visual_knowledge_brief.get("grounding_strength"),
            limit=32,
        )
        grounding_abstention_reason = AIOrchestratorService._normalize_metadata_text(
            visual_knowledge_brief.get("abstention_reason"),
            limit=64,
        )
        template_suppressed = bool(visual_knowledge_brief.get("template_suppressed"))
        use_llm_fallback_visual_metadata = grounding_mode == "llm_fallback" or not direct_brand_grounding
        # Grounding mode tells the prompt whether to trust retrieved brand evidence or derive visuals from copy/context.
        secondary_visual_hints = " | ".join(
            part
            for part in [
                f"visual direction={visual_direction}" if visual_direction else "",
                f"design style={design_style}" if design_style else "",
                f"preferred scene={preferred_scene}" if preferred_scene else "",
            ]
            if part
        )
        strategy_payload = message_strategy.model_dump(mode="json") if isinstance(message_strategy, MessageStrategyPayload) else {}
        message_theme = AIOrchestratorService._normalize_metadata_text(strategy_payload.get("primary_campaign_theme"), limit=180)
        audience_message = AIOrchestratorService._normalize_metadata_text(strategy_payload.get("core_audience_message"), limit=220)
        emotional_direction = AIOrchestratorService._normalize_metadata_text(strategy_payload.get("emotional_messaging_direction"), limit=120)
        keywords = ", ".join(strategy_payload.get("important_keywords") or []) or "None"
        avoid_list = ", ".join(strategy_payload.get("what_must_be_avoided_in_messaging") or []) or "None"
        stripped_prompt_theme = AIOrchestratorService._normalize_metadata_text(
            re.sub(
                r"^(create|make|design|generate|write|craft)\s+(an?|the)?\s*(engaging|compelling|instagram|social|media|post|creative|visual)*\s*",
                "",
                request_prompt,
                flags=re.IGNORECASE,
            ).strip()
            or request_prompt,
            limit=220,
        )
        resolved_copy_theme = AIOrchestratorService._normalize_metadata_text(
            " ".join(
                part
                for part in [
                    text_payload.headline,
                    metadata.get("supporting_line") or "",
                    text_payload.body,
                ]
                if AIOrchestratorService._coerce_text_value(part).strip()
            ),
            limit=220,
        )
        semantic_visual_brief = AIOrchestratorService._normalize_metadata_text(
            " ".join(
                part
                for part in [
                    stripped_prompt_theme,
                    text_payload.headline,
                    metadata.get("supporting_line") or "",
                    text_payload.body,
                    proof_points if proof_points != "None" else "",
                    stat_highlights if stat_highlights != "None" else "",
                ]
                if AIOrchestratorService._coerce_text_value(part).strip()
            ),
            limit=340,
        )
        data_visualization_anchor_sources: list[tuple[str, Any]] = [
            # Collect every approved numeric/list source before deciding whether chart/table guidance is allowed.
            ("stat_highlights", metadata.get("stat_highlights")),
            ("claim_evidence_pairs", metadata.get("claim_evidence_pairs")),
            ("proof_points", metadata.get("proof_points")),
            ("body_points", metadata.get("body_points")),
            ("infographic_section_specs", metadata.get("infographic_section_specs")),
            ("static_panel_spec", metadata.get("static_panel_spec")),
            ("supporting_line", metadata.get("supporting_line")),
            ("body", text_payload.body),
        ]
        if AIOrchestratorService._prompt_has_explicit_data_anchor(request_prompt):
            data_visualization_anchor_sources.insert(0, ("user_prompt", request_prompt))
        data_visualization_anchor_lines = AIOrchestratorService._data_visualization_anchor_lines(
            *[value for _, value in data_visualization_anchor_sources],
            limit=10,
        )
        broad_visual_context_section = AIOrchestratorService._broad_visual_context_prompt_section(
            data_visualization_anchor_lines,
            limit=8,
        )
        structured_anchor_sources = AIOrchestratorService._structured_visual_data_anchor_sources(
            metadata,
            prompt=request_prompt,
            request=request,
        )
        visual_intent = " ".join(
            [
                visual_direction,
                design_style,
                preferred_scene,
                AIOrchestratorService._coerce_text_value(metadata.get("visual_focus")),
            ]
        )
        # Reference template structure is only active when the source and output surfaces are compatible.
        reference_template_blueprint = AIOrchestratorService._reference_template_sample_page_blueprint(
            compiled_context,
            format_name=format_name,
        )
        reference_profile = (
            compiled_context.get("reference_adaptation_profile")
            if isinstance(compiled_context.get("reference_adaptation_profile"), dict)
            else {}
        )
        reference_source_family = AIOrchestratorService._normalize_metadata_text(
            reference_profile.get("source_format_family"),
            limit=32,
        ).casefold()
        reference_output_family = (
            AIOrchestratorService._normalize_metadata_text(reference_profile.get("output_format_family"), limit=32).casefold()
            or format_name
        )
        runtime_sample_page_blueprint = (
            metadata.get("sample_page_blueprint")
            if isinstance(metadata.get("sample_page_blueprint"), dict)
            else {}
        )
        reference_template_structure_payload = (
            AIOrchestratorService._reference_template_structure_payload(
                compiled_context,
                reference_profile,
                source_family=reference_source_family,
                output_family=reference_output_family,
                sample_page_blueprint=runtime_sample_page_blueprint,
            )
            if (
                reference_profile
                and reference_source_family in {"static", "infographic"}
                and reference_output_family in {"static", "infographic"}
            )
            or (runtime_sample_page_blueprint and format_name in {"static", "infographic"})
            else {}
        )
        active_sample_page_blueprint = (
            runtime_sample_page_blueprint
            if runtime_sample_page_blueprint
            else reference_template_blueprint
        )
        reference_profile_same_surface = reference_source_family == reference_output_family == format_name
        # Same-surface reference structure may guide layout; cross-surface references stay broad style only.
        reference_template_structure_active = (
            format_name in {"static", "infographic"}
            and not llm_led_static_infographic
            and bool(
                active_sample_page_blueprint
                or (
                    reference_profile_same_surface
                    and (
                        reference_template_structure_payload.get("template_layout_grammar")
                        or reference_template_structure_payload.get("layout_dna")
                        or reference_template_structure_payload.get("sample_page_blueprint")
                    )
                )
            )
        )
        structured_visual_metadata = AIOrchestratorService._normalize_structured_visual_metadata(
            metadata=metadata,
            request=request,
            visual_intent=visual_intent,
            anchor_entries=AIOrchestratorService._structured_visual_anchor_entries(
                structured_anchor_sources,
                request=request,
                limit=8,
            ),
            format_name=format_name,
            sample_page_blueprint=active_sample_page_blueprint,
            visual_plan=visual_explanation_plan,
            visual_identity=visual_identity,
            reference_assets=reference_asset_list,
            asset_catalog=asset_catalog,
            compiled_context=compiled_context,
        )
        # Structured visual metadata consolidates anchors, permissions, and sample limits for downstream prompt sections.
        structured_visual_metadata_section = AIOrchestratorService._structured_visual_metadata_prompt_section(
            structured_visual_metadata
        )
        infographic_surface_enforcement = AIOrchestratorService._infographic_surface_enforcement_section(
            request=request,
            format_name=format_name,
            structured_visual_metadata=structured_visual_metadata,
            visual_plan=visual_explanation_plan,
        )
        strict_data_visualization_anchor_lines = [
            AIOrchestratorService._normalize_metadata_text(anchor.get("text"), limit=260)
            for anchor in structured_visual_metadata.get("data_anchors", [])
            if isinstance(anchor, dict)
            and AIOrchestratorService._normalize_metadata_text(anchor.get("text"), limit=260)
        ]
        data_visualization_contract = AIOrchestratorService._data_visualization_contract(
            request=request,
            visual_intent=visual_intent,
            anchors=strict_data_visualization_anchor_lines,
            format_name=format_name,
        )
        stripped_prompt_keywords = AIOrchestratorService._topic_anchor_keywords(stripped_prompt_theme)
        resolved_copy_keywords = AIOrchestratorService._topic_anchor_keywords(resolved_copy_theme)
        theme_anchor = (
            stripped_prompt_theme
            if llm_led_static_infographic
            else stripped_prompt_theme
            if stripped_prompt_keywords and resolved_copy_keywords and stripped_prompt_keywords & resolved_copy_keywords
            else (message_theme or resolved_copy_theme or stripped_prompt_theme)
        )
        # Theme anchor protects the current user topic from being replaced by older copy or template semantics.
        brand_name = AIOrchestratorService._normalize_metadata_text(
            request.resolved_brand_context.get("brand_name") or "",
            limit=80,
        )
        logo_position_hint = AIOrchestratorService._effective_logo_position_hint(
            request=request,
            creative_decision=creative_decision,
            text_payload=AIOrchestratorService._text_payload_prompt_dict(text_payload),
        )
        logo_safe_zone_guidance = AIOrchestratorService._logo_safe_zone_guidance(
            request,
            hint=logo_position_hint,
        )
        reserved_logo_area = AIOrchestratorService._logo_reserved_area_label(logo_position_hint)
        logo_surface_guidance = AIOrchestratorService._logo_surface_guidance(
            background_tone=AIOrchestratorService._resolve_logo_background_tone(
                metadata=metadata,
                creative_decision=creative_decision,
            )
        )
        logo_non_generation_contract = AIOrchestratorService._logo_non_generation_contract(
            brand_name=brand_name,
            reserved_logo_area=reserved_logo_area,
        )
        text_containment_contract = AIOrchestratorService._image_text_containment_contract(format_name=format_name)
        legal_footer_text = AIOrchestratorService._brand_legal_footer_text_for_format(request)
        disclaimer_overlay_guidance = AIOrchestratorService._disclaimer_overlay_guidance(request)
        bottom_footer_requested = AIOrchestratorService._bottom_footer_overlay_requested(
            request,
            legal_footer_text=legal_footer_text,
        )
        # Footer safe area is calculated before layout budgeting so image content leaves room for legal overlay.
        request_size = request.studio_panel.get("size") if isinstance(request.studio_panel.get("size"), dict) else {}
        footer_safe_area = (
            calculate_footer_safe_area(
                canvas_width=int(request_size.get("width") or 1080),
                canvas_height=int(request_size.get("height") or 1080),
                footer_text=legal_footer_text,
            )
            if bottom_footer_requested
            else {}
        )
        infographic_footer_layout_contract = AIOrchestratorService._infographic_footer_safe_layout_contract(
            format_name=format_name,
            footer_requested=bottom_footer_requested,
            footer_safe_area=footer_safe_area,
        )
        layout_content_budget = AIOrchestratorService._dynamic_layout_content_budget(
            request=request,
            format_name=format_name,
            text_payload=text_payload,
            metadata=metadata,
            sample_page_blueprint=active_sample_page_blueprint,
            scene_graph=None,
            legal_footer_text=legal_footer_text,
            disclaimer_guidance=disclaimer_overlay_guidance,
            logo_position_hint=logo_position_hint,
        )
        # The budget trims visible text before prompt building, preventing the model from overfilling the canvas.
        prompt_text_payload = AIOrchestratorService._prompt_visible_text_payload_from_layout_budget(
            text_payload,
            layout_content_budget,
        )
        prompt_metadata = prompt_text_payload.metadata or {}
        prompt_proof_points = AIOrchestratorService._compact_named_items(prompt_metadata.get("proof_points"), limit=4)
        prompt_stat_highlights = AIOrchestratorService._compact_named_items(prompt_metadata.get("stat_highlights"), limit=3)
        prompt_semantic_visual_brief = AIOrchestratorService._normalize_metadata_text(
            " ".join(
                part
                for part in [
                    stripped_prompt_theme,
                    prompt_text_payload.headline,
                    prompt_metadata.get("supporting_line") or "",
                    prompt_text_payload.body,
                    prompt_proof_points if prompt_proof_points != "None" else "",
                    prompt_stat_highlights if prompt_stat_highlights != "None" else "",
                ]
                if AIOrchestratorService._coerce_text_value(part).strip()
            ),
            limit=340,
        )
        visual_plan = visual_explanation_plan or AIOrchestratorService._visual_explanation_plan(
            request,
            prompt_text_payload,
            creative_decision,
            reference_asset_list,
            message_strategy,
        )
        research_quality_section = AIOrchestratorService._research_editorial_prompt_section(
            request,
            compiled_context,
        )
        anti_hallucination_section = AIOrchestratorService._image_generation_anti_hallucination_prompt_section(
            request=request,
            compiled_context=compiled_context,
        )
        consultant_contract = AIOrchestratorService._consultant_quality_contract(
            for_visual_only=True,
            for_carousel=format_name == "carousel",
            for_static_infographic_ad=llm_led_static_infographic,
        )
        ad_post_visual_quality_contract = AIOrchestratorService._ad_post_visual_quality_contract(format_name)
        multimodal_balance_contract = AIOrchestratorService._multimodal_balance_contract(
            format_name=format_name,
            supporting_line=AIOrchestratorService._normalize_metadata_text(prompt_metadata.get("supporting_line") or prompt_text_payload.body, limit=220),
            proof_points=AIOrchestratorService._normalize_metadata_list(prompt_metadata.get("proof_points"), limit=4),
            claim_evidence_pairs=AIOrchestratorService._normalize_claim_evidence_pairs(prompt_metadata.get("claim_evidence_pairs"), limit=3),
            for_visual_only=True,
        )
        reference_family_contract = AIOrchestratorService._reference_family_contract_sections(
            compiled_context,
            for_visual_only=True,
        )
        visual_plan_guidance = AIOrchestratorService._visual_explanation_guidance(visual_plan)
        sample_alignment_sections = AIOrchestratorService._sample_visual_alignment_sections(
            compiled_context,
            for_carousel=format_name == "carousel",
        )
        visual_style_policy_sections = AIOrchestratorService._visual_style_policy_prompt_sections(
            compiled_context,
            format_name=format_name,
            reference_assets=reference_asset_list,
        )
        sequence_alignment_sections = AIOrchestratorService._sequence_blueprint_alignment_sections(
            compiled_context,
            for_carousel=format_name == "carousel",
            for_visual_only=True,
        )
        dynamic_visual_art_direction = AIOrchestratorService._dynamic_visual_art_direction_section(
            request=request,
            format_name=format_name,
            semantic_visual_brief=prompt_semantic_visual_brief or semantic_visual_brief,
            visual_intent=visual_intent,
            structured_visual_metadata=structured_visual_metadata,
            visual_plan=visual_plan,
            visual_identity=visual_identity,
            data_visualization_anchor_lines=data_visualization_anchor_lines,
            sample_page_blueprint=active_sample_page_blueprint,
            slide_purpose=message_theme or audience_message or resolved_copy_theme,
            reference_assets=reference_asset_list,
            asset_catalog=asset_catalog,
            compiled_context=compiled_context,
        )
        # Dynamic art direction translates approved metadata into concrete visual treatment for this format.
        reference_creative_profile = AIOrchestratorService._resolve_reference_creative_profile(
            request=request,
            format_name=format_name,
            sample_page_blueprint=active_sample_page_blueprint,
            visual_identity=visual_identity,
            visual_plan=visual_plan,
            structured_visual_metadata=structured_visual_metadata,
            layout_budget=layout_content_budget,
            metadata=prompt_metadata,
            slide_purpose=message_theme or audience_message or resolved_copy_theme,
            reference_assets=reference_asset_list,
            asset_catalog=asset_catalog,
            compiled_context=compiled_context,
            logo_position_hint=logo_position_hint,
        )
        # Reference profile describes what can be borrowed from uploaded/reference assets without copying them literally.
        reference_creative_profile_section = AIOrchestratorService._reference_creative_profile_prompt_section(
            reference_creative_profile
        )
        reference_adaptation_profile_section = AIOrchestratorService._reference_adaptation_profile_prompt_section(
            compiled_context.get("reference_adaptation_profile")
        )
        generation_surface_contract_section = AIOrchestratorService._generation_surface_contract_prompt_section(
            compiled_context.get("generation_surface_contract")
        )
        reference_template_adaptation_sections = (
            []
            if llm_led_static_infographic
            else AIOrchestratorService._reference_template_adaptation_prompt_sections(
                compiled_context,
                format_name=format_name,
                layout_content_budget=layout_content_budget,
                sample_page_blueprint=active_sample_page_blueprint,
            )
        )
        layout_quality_guard = AIOrchestratorService._dynamic_layout_quality_guard_section(
            request=request,
            format_name=format_name,
            text_payload=prompt_text_payload,
            metadata=prompt_metadata,
            sample_page_blueprint=active_sample_page_blueprint,
            structured_visual_metadata=structured_visual_metadata,
            data_visualization_anchor_lines=data_visualization_anchor_lines,
            visual_intent=visual_intent,
            visual_plan=visual_plan,
            visual_identity=visual_identity,
            reference_assets=reference_asset_list,
            asset_catalog=asset_catalog,
            scene_graph=None,
            legal_footer_text=legal_footer_text,
            disclaimer_guidance=disclaimer_overlay_guidance,
            layout_budget=layout_content_budget,
        )
        llm_led_visual_authority_instruction = (
            "LLM-led static/infographic ad authority: act as the creative designer. Create an attractive paid-social post from the current prompt, including the main layout, readable ad copy, topic-specific imagery, modules, visual metaphor, and CTA. Do not preserve reference/template scaffolds, panel shells, scene-graph geometry, stale sample subjects, or extracted sample layout controls. Reserve only the logo-safe corner and footer/disclaimer area for backend compositing."
            if llm_led_static_infographic
            else ""
        )
        social_media_ad_contract = (
            AIOrchestratorService._social_media_ad_composition_contract(
                format_name=format_name,
                platform=studio_panel.get("platform_preset", "social"),
            )
            if llm_led_static_infographic
            else ""
        )
        content_visual_explanation_contract = (
            AIOrchestratorService._content_visual_explanation_contract(
                request=request,
                format_name=format_name,
                platform=studio_panel.get("platform_preset", "social"),
            )
            if llm_led_static_infographic
            else ""
        )
        quality_floor_contract = (
            AIOrchestratorService._final_render_static_infographic_quality_floor_contract(
                request=request,
                format_name=format_name,
            )
            if llm_led_static_infographic
            else ""
        )
        hallucination_control_contract = (
            AIOrchestratorService._image_hallucination_control_contract(
                format_name=format_name,
                use_backend_text_overlay=False,
            )
            if llm_led_static_infographic
            else ""
        )
        llm_led_topic_fit_contract = AIOrchestratorService._final_render_topic_fit_contract(
            request=request,
            format_name=format_name,
        )
        semantic_visual_brief_instruction = (
            f"Semantic creative brief: communicate this exact idea through finished readable content plus topic-specific visual hierarchy: {prompt_semantic_visual_brief or semantic_visual_brief}."
            if llm_led_static_infographic
            else f"Semantic visual brief: communicate this exact idea visually, without relying on text: {prompt_semantic_visual_brief or semantic_visual_brief}."
        )
        image_text_generation_instruction = (
            "Render finished readable main content for the static/infographic creative. Keep only the logo-safe corner and footer/disclaimer strip blank for backend compositing; do not generate logos, watermarks, placeholder brand marks, or legal/footer copy."
            if llm_led_static_infographic
            else (
                "Do not include readable text, typography, letters, numbers, sample copy, or brand marks in the image. Preserve blank template panel/card/CTA shells and callout/module surfaces as layout structure; never fill them with pseudo-text or fake marks."
                if reference_template_structure_active
                else (
                    "Do not include any text, typography, letters, numbers, logos, CTA buttons, UI panels, "
                    "poster layouts, or branded wordmarks in the image. "
                    "Generate only a clean visual, illustration, texture, or scene that the backend renderer can place into the layout."
                )
            )
        )
        static_infographic_backend_overlay_contract = (
            "Backend overlay contract for static/infographic: the image model must not draw, typeset, imitate, or place any standalone brand asset, brand-name signature, watermark, masthead, registration text, disclaimer, or footer text. Keep the brand-asset safe corner and bottom footer/disclaimer strip plain and empty; backend post-processing alone will place the exact stored brand asset and legal footer."
            if llm_led_static_infographic
            else ""
        )
        if llm_led_static_infographic:
            # LLM-led mode intentionally clears template/sample sections so only the current prompt controls composition.
            layout_quality_guard = ""
            infographic_surface_enforcement = ""
            data_visualization_contract = ""
            structured_visual_metadata_section = ""
            dynamic_visual_art_direction = ""
            reference_creative_profile_section = ""
            reference_adaptation_profile_section = ""
            generation_surface_contract_section = ""
            visual_plan_guidance = ""
            visual_style_policy_sections = []
            sample_alignment_sections = []
            sequence_alignment_sections = []
            reference_family_contract = []

        sections = [
            (
                "Create a finished premium static/infographic creative led by the current prompt."
                if llm_led_static_infographic
                else (
                "Create a template-aware visual substrate that preserves the selected static/infographic layout scaffold, blank text-safe modules, image zones, and safe-area posture."
                if reference_template_structure_active
                else "Create a clean supporting visual aligned to the brand system."
                )
            ),
            llm_led_visual_authority_instruction,
            static_infographic_backend_overlay_contract,
            llm_led_topic_fit_contract,
            social_media_ad_contract,
            content_visual_explanation_contract,
            hallucination_control_contract,
            quality_floor_contract,
            "Visual quality bar: premium, high-end, richly detailed composition with polished craft, clean hierarchy, and brand-safe restraint.",
            f"Keep the {reserved_logo_area} area completely empty and visually clean. Do not place any icon, illustration element, text, or visual detail inside or immediately adjacent to this corner.",
            logo_safe_zone_guidance,
            logo_surface_guidance,
            *logo_non_generation_contract,
            disclaimer_overlay_guidance,
            infographic_footer_layout_contract,
            *text_containment_contract,
            f"Theme: {theme_anchor}.",
            f"Message strategy theme: {message_theme}.",
            f"Core audience message: {audience_message}.",
            f"Emotional direction: {emotional_direction}.",
            f"Important keywords: {keywords}.",
            f"Avoid these messaging cues in the visual: {avoid_list}.",
            f"Platform: {studio_panel.get('platform_preset', 'social')}.",
            f"Format: {studio_panel.get('format', 'static')}.",
            layout_quality_guard,
            infographic_surface_enforcement,
            f"Headline intent: {AIOrchestratorService._normalize_metadata_text(prompt_text_payload.headline, limit=160)}.",
            f"Body summary: {AIOrchestratorService._normalize_metadata_text(prompt_text_payload.body, limit=260)}.",
            semantic_visual_brief_instruction,
            data_visualization_contract,
            broad_visual_context_section,
            structured_visual_metadata_section,
            dynamic_visual_art_direction,
            reference_creative_profile_section,
            reference_adaptation_profile_section,
            generation_surface_contract_section,
            *reference_template_adaptation_sections,
            (
                f"Brand knowledge grounding mode: {grounding_mode or 'brand_knowledge'} (strength: {grounding_strength or 'supported'})."
                if direct_brand_grounding
                else (
                    f"Brand knowledge grounding mode: llm_fallback (reason: {grounding_abstention_reason})."
                    if grounding_abstention_reason
                    else "Brand knowledge grounding mode: llm_fallback."
                )
            ),
            (
                f"Brand knowledge grounding: {direct_brand_grounding}. Use these retrieved brand-knowledge cues as the primary source of visual grounding."
                if direct_brand_grounding
                else "Brand knowledge grounding: no retrieved brand knowledge is available, so infer the visual grounding from the approved copy, message strategy, and brand visual system."
            ),
            (
                "If retrieved brand knowledge is present, do not override it with a generic LLM-invented scene. Use LLM reasoning only to interpret and combine the retrieved cues coherently."
                if direct_brand_grounding
                else "Because retrieved brand knowledge is absent, generate the visual direction through LLM reasoning from the approved copy, message strategy, and brand visual system."
            ),
            (
                "Template-derived cues were suppressed because stronger visual_identity or mood_board evidence exists."
                if direct_brand_grounding and template_suppressed
                else ""
            ),
            (
                "If the active brand grounding is fallback-only, use template or metadata cues only for structure, palette, spacing, and composition. Do not literalize template copy."
                if direct_brand_grounding and grounding_strength == "fallback_only"
                else ""
            ),
            (
                "When sequence guidance is style-reference-only, treat uploaded sample slides as strict composition and craft references. "
                "Preserve their region proportions, negative space, sequencing, balance, and composition rhythm closely, but rebuild the artwork, poster surface, text blocks, background illustration, and sample typography from scratch."
                if template_surface_policy == "style_reference_only" and not llm_led_static_infographic
                else ""
            ),
            (
                f"LLM fallback visual direction: {visual_direction}."
                if use_llm_fallback_visual_metadata and visual_direction
                else ""
            ),
            (
                f"LLM fallback design style: {design_style}."
                if use_llm_fallback_visual_metadata and design_style
                else ""
            ),
            (
                f"LLM fallback preferred scene: {preferred_scene}."
                if use_llm_fallback_visual_metadata and preferred_scene
                else ""
            ),
            (
                f"Secondary synthesis hints from approved metadata: {secondary_visual_hints}. "
                "Use these only if they reinforce the retrieved brand-grounded cues and never let them override or "
                "reintroduce suppressed template or rejected channel ideas."
                if not use_llm_fallback_visual_metadata and secondary_visual_hints
                else ""
            ),
            (
                f"Discarded incompatible model-generated visual metadata for: {', '.join(sanitized_visual_fields)}."
                if sanitized_visual_fields
                else ""
            ),
            f"Brand palette: {palette}.",
            f"Palette role guidance: {palette_guidance}.",
            strict_palette_contract,
            AIOrchestratorService._brand_color_text_overlay_preparation_contract(),
            f"Typography vibe: {typography}.",
            f"Layout approach: {layout_decision}.",
            f"Dominant visual system: {dominant_visual_system}.",
            f"Supporting visual system: {supporting_visual_system or 'none'}.",
            f"Key proof points: {proof_points}.",
            f"Stat highlights: {stat_highlights}.",
            f"Reusable decorative cues: {reusable_assets}.",
            f"Reference assets: {reference_assets}.",
            *visual_style_policy_sections,
            *sample_alignment_sections,
            *sequence_alignment_sections,
            research_quality_section,
            *consultant_contract,
            ad_post_visual_quality_contract,
            *multimodal_balance_contract,
            *reference_family_contract,
            AIOrchestratorService._visual_quality_bar_for_treatment(structured_visual_metadata),
            "Choose background tones from true background or surface palette evidence rather than overpowering the canvas with a strong secondary brand color.",
            "Maintain clear contrast between any light surfaces and darker emphasis colors so later text placement stays readable and never fades into a white or dull field.",
            (
                "Template-active fallback policy: preserve the observed module/card/panel/row/callout scaffold as the primary composition. Any fallback photo, illustration, object scene, or human-subject visual may only fill an approved visual slot and must not replace the whole layout."
                if reference_template_structure_active
                else (
                "Prefer one coherent content-led hero concept: a product or process metaphor, comparison setup, outcome-focused object composition, "
                "or structured explainer centerpiece tied directly to the semantic visual brief. Do not default to a standalone professional portrait or generic lifestyle scene."
                )
            ),
            visual_plan_guidance,
            (
                "Template-active explainer composition: keep observed template modules, blank card/panel shells, section rhythm, and callout surfaces visible; if an image slot exists, use topic-specific imagery only inside that slot."
                if reference_template_structure_active
                else (
                "If the requested format is carousel or infographic, build a richer explainer composition with a clear focal subject and only the minimum supporting structure truly required by the content. Do not default to rising bars, upward arrows, dashboard tiles, symbolic finance stickers, or a chart/graph hero unless the user explicitly asked for chart or diagram content."
                if iconography_supported
                else "Keep the composition focused on one premium content-specific scene rather than many small supporting motifs."
                )
            ),
            (
                "Preserve the template's text-safe regions, image slots, card/panel shells, callout modules, header reservation, and footer reservation; simplify imagery before changing the layout scaffold."
                if reference_template_structure_active
                else (
                "Compose a finished, creative ad post as a complete image-model design. Do not follow a reserved reference/template image slot; only leave the logo-safe corner and footer/disclaimer strip clear for backend overlay."
                if llm_led_static_infographic
                else (
                "Reserve calm negative space where headline and CTA can sit cleanly after rendering. "
                "Respect the selected reference/template layout by composing the focal visual for the reserved image area. Do not compose the image like a finished poster."
                )
                )
            ),
            (
                "Avoid clip-art, flat stock-style icon grids, low-detail collage aesthetics, childish illustration, "
                "or generic corporate poster art."
            ),
            (
                "If reusable icons or diagram cues are available, you may integrate a small number of them as premium, brand-consistent supporting elements rather than ignoring them completely."
                if include_reference_system and iconography_supported
                else "Do not directly embed brand icon sets or decorative assets into the generated image."
            ),
            image_text_generation_instruction,
            (
                "Keep any iconographic support inside the observed template modules; do not scatter unrelated symbols outside the approved card, row, panel, callout, or image-zone structure."
                if reference_template_structure_active
                else (
                "Do not paint random sticker-like icons, fake logos, faux UI panels, or cluttered symbol dumps. Any infographic or iconographic support must feel intentional, premium, and integrated into the overall composition."
                if iconography_supported
                else "Do not paint standalone icons, infographic symbols, tables, badges, checkmarks, or faux UI graphics onto the scene unless they exist as real-world objects inside the photographed environment."
                )
            ),
            (
                "If the prompt is about tips, strategies, comparisons, or education, express the idea through the observed template modules and approved visual slots rather than creating a single full-frame fallback scene."
                if reference_template_structure_active
                else (
                "If the prompt is about tips, strategies, comparisons, or financial education, keep the scene topic-led and avoid auto-inserting symbolic growth charts, rising bars, arrows, finance app mini-graphics, or a graph-like hero composition. Only add chart or diagram elements when the user explicitly requested them."
                if iconography_supported
                else "If the prompt is about tips, strategies, or comparisons, still create one premium unified hero scene rather than a collage of icon stickers."
                )
            ),
            (
                "Do not add bar-chart icons, rising-arrow symbols, dashboard tiles, comparison stickers, decorative finance mini-graphics, or a chart/graph/bar-and-arrow hero image unless the user explicitly asked for a chart, graph, table, timeline, diagram, or infographic data visualization."
                if not explicit_data_visual_request
                else "If a chart or diagram is explicitly requested, keep it literal, sparse, and directly tied to the supplied data instead of using generic symbolic finance icons."
            ),
        ]
        required_sections = [
            (
                "Create a finished premium static/infographic creative led by the current prompt."
                if llm_led_static_infographic
                else (
                "Create a template-aware visual substrate that preserves the selected static/infographic layout scaffold, blank text-safe modules, image zones, and safe-area posture."
                if reference_template_structure_active
                else "Create a clean supporting visual aligned to the brand system."
                )
            ),
            "Visual quality bar: premium, high-end, richly detailed composition with polished craft, clean hierarchy, and brand-safe restraint.",
            AIOrchestratorService._visual_quality_bar_for_treatment(structured_visual_metadata),
            f"Keep the {reserved_logo_area} area completely empty and visually clean. Do not place any icon, illustration element, text, or visual detail inside or immediately adjacent to this corner.",
            disclaimer_overlay_guidance,
            infographic_footer_layout_contract,
            *text_containment_contract,
            f"Theme: {theme_anchor}.",
            f"Platform: {studio_panel.get('platform_preset', 'social')}.",
            f"Format: {studio_panel.get('format', 'static')}.",
            llm_led_visual_authority_instruction,
            llm_led_topic_fit_contract,
            social_media_ad_contract,
            ad_post_visual_quality_contract,
            AIOrchestratorService._brand_color_text_overlay_preparation_contract(),
            quality_floor_contract,
            reference_adaptation_profile_section,
            generation_surface_contract_section,
            *reference_template_adaptation_sections,
            f"Headline intent: {AIOrchestratorService._normalize_metadata_text(prompt_text_payload.headline, limit=160)}.",
            f"Body summary: {AIOrchestratorService._normalize_metadata_text(prompt_text_payload.body, limit=260)}.",
            semantic_visual_brief_instruction,
            broad_visual_context_section,
            f"Layout approach: {layout_decision}.",
            f"Reference assets: {reference_assets}.",
            image_text_generation_instruction,
            (
                "Template-active fallback policy: preserve the observed module/card/panel/row/callout scaffold as the primary composition. Any fallback photo, illustration, object scene, or human-subject visual may only fill an approved visual slot and must not replace the whole layout."
                if reference_template_structure_active
                else (
                "Prefer one coherent content-led hero concept: a product or process metaphor, comparison setup, outcome-focused object composition, "
                "or structured explainer centerpiece tied directly to the semantic visual brief. Do not default to a standalone professional portrait or generic lifestyle scene."
                )
            ),
            research_quality_section,
            *consultant_contract,
            layout_quality_guard,
            infographic_surface_enforcement,
            data_visualization_contract,
            structured_visual_metadata_section,
            dynamic_visual_art_direction,
            (
                f"Brand knowledge grounding mode: {grounding_mode or 'brand_knowledge'} (strength: {grounding_strength or 'supported'})."
                if direct_brand_grounding
                else (
                    f"Brand knowledge grounding mode: llm_fallback (reason: {grounding_abstention_reason})."
                    if grounding_abstention_reason
                    else "Brand knowledge grounding mode: llm_fallback."
                )
            ),
            (
                f"Brand knowledge grounding: {direct_brand_grounding}. Use these retrieved brand-knowledge cues as the primary source of visual grounding."
                if direct_brand_grounding
                else "Brand knowledge grounding: no retrieved brand knowledge is available, so infer the visual grounding from the approved copy, message strategy, and brand visual system."
            ),
            (
                "If retrieved brand knowledge is present, do not override it with a generic LLM-invented scene. Use LLM reasoning only to interpret and combine the retrieved cues coherently."
                if direct_brand_grounding
                else "Because retrieved brand knowledge is absent, generate the visual direction through LLM reasoning from the approved copy, message strategy, and brand visual system."
            ),
            (
                "Template-derived cues were suppressed because stronger visual_identity or mood_board evidence exists."
                if direct_brand_grounding and template_suppressed
                else ""
            ),
            (
                f"Secondary synthesis hints from approved metadata: {secondary_visual_hints}. "
                "Use these only if they reinforce the retrieved brand-grounded cues and never let them override or "
                "reintroduce suppressed template or rejected channel ideas."
                if not use_llm_fallback_visual_metadata and secondary_visual_hints
                else ""
            ),
            (
                f"Discarded incompatible model-generated visual metadata for: {', '.join(sanitized_visual_fields)}."
                if sanitized_visual_fields
                else ""
            ),
            reference_creative_profile_section,
            *visual_style_policy_sections,
            *sample_alignment_sections,
            *sequence_alignment_sections,
            *reference_family_contract,
            f"Brand palette: {palette}.",
            f"Palette role guidance: {palette_guidance}.",
            strict_palette_contract,
            f"Typography vibe: {typography}.",
            f"Dominant visual system: {dominant_visual_system}.",
            f"Supporting visual system: {supporting_visual_system or 'none'}.",
            (
                "If reusable icons or diagram cues are available, you may integrate a small number of them as premium, brand-consistent supporting elements rather than ignoring them completely."
                if include_reference_system and iconography_supported
                else "Do not directly embed brand icon sets or decorative assets into the generated image."
            ),
            f"Key proof points: {proof_points}.",
            f"Stat highlights: {stat_highlights}.",
            *multimodal_balance_contract,
            "Choose background tones from true background or surface palette evidence rather than overpowering the canvas with a strong secondary brand color.",
            "Maintain clear contrast between any light surfaces and darker emphasis colors so later text placement stays readable and never fades into a white or dull field.",
            visual_plan_guidance,
            (
                "Template-active explainer composition: keep observed template modules, blank card/panel shells, section rhythm, and callout surfaces visible; if an image slot exists, use topic-specific imagery only inside that slot."
                if reference_template_structure_active
                else (
                "If the requested format is carousel or infographic, build a richer explainer composition with a clear focal subject and only the minimum supporting structure truly required by the content. Do not default to rising bars, upward arrows, dashboard tiles, symbolic finance stickers, or a chart/graph hero unless the user explicitly asked for chart or diagram content."
                if iconography_supported
                else "Keep the composition focused on one premium content-specific scene rather than many small supporting motifs."
                )
            ),
            (
                "Preserve the template's text-safe regions, image slots, card/panel shells, callout modules, header reservation, and footer reservation; simplify imagery before changing the layout scaffold."
                if reference_template_structure_active
                else (
                "Compose a finished, creative ad post as a complete image-model design. Do not follow a reserved reference/template image slot; only leave the logo-safe corner and footer/disclaimer strip clear for backend overlay."
                if llm_led_static_infographic
                else (
                "Reserve calm negative space where headline and CTA can sit cleanly after rendering. "
                "Respect the selected reference/template layout by composing the focal visual for the reserved image area. Do not compose the image like a finished poster."
                )
                )
            ),
            (
                "Do not add bar-chart icons, rising-arrow symbols, dashboard tiles, comparison stickers, decorative finance mini-graphics, or a chart/graph/bar-and-arrow hero image unless the user explicitly asked for a chart, graph, table, timeline, diagram, or infographic data visualization."
                if not explicit_data_visual_request
                else "If a chart or diagram is explicitly requested, keep it literal, sparse, and directly tied to the supplied data instead of using generic symbolic finance icons."
            ),
        ]
        prompt = AIOrchestratorService._compose_prompt_sections(
            required_sections=required_sections,
            optional_sections=sections,
            limit=AIOrchestratorService.IMAGE_PROMPT_MAX_LENGTH,
        )
        prompt = AIOrchestratorService._strip_blocked_compliance_phrases(prompt)
        prompt = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(prompt)
        # Returns a supporting-image prompt for backend-rendered layouts, not the final response object.
        # Static/infographic LLM-led final renders bypass this and use build_final_render_prompt instead.
        return AIOrchestratorService._trim_prompt(prompt, AIOrchestratorService.IMAGE_PROMPT_MAX_LENGTH)

    @staticmethod
    def _compact_named_items(value: Any, limit: int) -> str:
        # Compacts named items from input value and limit for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        items = AIOrchestratorService._normalize_metadata_list(value, limit=limit)
        return ", ".join(items) if items else "None"

    @staticmethod
    def _compact_palette_summary(visual_identity: Any) -> str:
        # Compacts palette summary from visual identity for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(visual_identity, dict):
            return "Use the validated primary and secondary brand colors."
        resolved_roles = derive_palette_roles(visual_identity)
        if resolved_roles:
            prioritized = [
                f"{role} {value}"
                for role in ("background", "primary", "secondary", "accent")
                if (value := resolved_roles.get(role))
            ]
            if prioritized:
                return ", ".join(prioritized[:5])
        palette_entries = visual_identity.get("brand_color_palette") or visual_identity.get("palette_entries") or []
        if isinstance(palette_entries, dict):
            colors = [
                f"{AIOrchestratorService._normalize_metadata_text(role, limit=18)} {AIOrchestratorService._normalize_metadata_text(value, limit=24)}".strip()
                for role, value in list(palette_entries.items())[:5]
                if AIOrchestratorService._normalize_metadata_text(value, limit=24)
            ]
            return ", ".join(colors) if colors else "Use the validated primary and secondary brand colors."
        if not isinstance(palette_entries, list):
            return "Use the validated primary and secondary brand colors."
        colors: list[str] = []
        for entry in palette_entries[:5]:
            if not isinstance(entry, dict):
                continue
            role = AIOrchestratorService._normalize_metadata_text(entry.get("role"), limit=18)
            code = AIOrchestratorService._normalize_metadata_text(
                entry.get("hex_code") or entry.get("hex") or entry.get("color_code") or entry.get("name"),
                limit=24,
            )
            label = " ".join(part for part in [role, code] if part)
            if label:
                colors.append(label)
        return ", ".join(colors) if colors else "Use the validated primary and secondary brand colors."

    @staticmethod
    def _palette_role_guidance(visual_identity: Any) -> str:
        # Derives palette role guidance from visual identity for AIOrchestratorService.
        # This turns source evidence into a stable planning hint.
        if not isinstance(visual_identity, dict):
            return (
                "Use the brand background or surface color for the main canvas, keep primary and secondary colors for emphasis, "
                "and maintain strong readable contrast between text and background."
            )
        roles = derive_palette_roles(visual_identity)
        if not roles:
            return (
                "Use the brand background or surface color for the main canvas, keep primary and secondary colors for emphasis, "
                "and maintain strong readable contrast between text and background."
            )
        guidance: list[str] = []
        if roles.get("background") or roles.get("surface"):
            guidance.append(
                f"Base canvas should lean on {roles.get('background') or roles.get('surface')} rather than overpowering accent fills."
            )
        if roles.get("primary"):
            guidance.append(f"Use {roles['primary']} as the main emphasis color.")
        if roles.get("secondary"):
            guidance.append(
                f"Use {roles['secondary']} as a supporting color instead of making it the dominant full-background field unless the layout truly needs it."
            )
        if roles.get("accent"):
            guidance.append(f"Reserve {roles['accent']} for highlights, chart moments, or CTA emphasis.")
        guidance.append("Protect readability with high contrast and avoid white-on-white, pale-on-pale, or dull low-contrast text treatments.")
        return " ".join(guidance)

    @staticmethod
    def _strict_palette_contract(visual_identity: Any) -> str:
        # Derives strict palette contract from visual identity for AIOrchestratorService.
        # The derived signal gives the LLM explicit evidence instead of asking it to infer the same thing later.
        if not isinstance(visual_identity, dict):
            return ""
        roles = derive_palette_roles(visual_identity)
        if not roles:
            return ""
        background = str(roles.get("background") or roles.get("surface") or "").strip()
        primary = str(roles.get("primary") or "").strip()
        secondary = str(roles.get("secondary") or "").strip()
        accent = str(roles.get("accent") or "").strip()
        contract: list[str] = [
            "Strict palette contract: stay inside the validated brand color system and do not introduce unrelated dominant hues."
        ]
        if background:
            contract.append(
                f"Let {background} act as the dominant background or surface tone for most of the canvas."
            )
        if primary:
            contract.append(
                f"Use {primary} as the principal brand emphasis color for major structure, strong contrast, and key framing."
            )
        if secondary:
            contract.append(
                f"Use {secondary} only as a controlled secondary accent or emphasis field, not as a full-canvas wash unless the composition truly demands it."
            )
        if accent:
            contract.append(
                f"Keep {accent} limited to small highlight moments, not broad background coverage."
            )
        contract.append(
            "Avoid drifting into generic blue-gray, random teal, muddy beige, purple, or off-brand pastel systems that are not validated by the brand palette."
        )
        return " ".join(contract)

    @staticmethod
    def _palette_role_execution_contract(visual_identity: Any, *, has_cta: bool, has_legal_footer: bool) -> str:
        # Derives palette role execution contract from visual identity, has cta, and has legal footer for AIOrchestratorService.
        # The derived signal gives the LLM explicit evidence instead of asking it to infer the same thing later.
        if not isinstance(visual_identity, dict):
            return ""
        roles = derive_palette_roles(visual_identity)
        if not roles:
            return ""
        instructions: list[str] = []
        background = str(roles.get("background") or roles.get("surface") or "").strip()
        primary = str(roles.get("primary") or "").strip()
        secondary = str(roles.get("secondary") or "").strip()
        accent = str(roles.get("accent") or "").strip()
        if background:
            instructions.append(f"Canvas and quiet negative space should predominantly use {background}.")
        if primary:
            instructions.append(
                f"Headlines, core body text, key dividers, and the main explanatory structure should primarily use {primary} when strong contrast is needed."
            )
        if secondary:
            instructions.append(
                f"Use {secondary} only for secondary modules, supporting shapes, or restrained depth accents rather than the dominant reading layer."
            )
        if accent:
            accent_targets = ["small stat moments", "highlight chips", "one hero object accent"]
            if has_cta:
                accent_targets.append("the CTA treatment")
            instructions.append(
                f"Use {accent} selectively for " + ", ".join(accent_targets) + " instead of spreading it across the whole slide."
            )
        if has_legal_footer and primary:
            instructions.append(f"Keep the bottom compliance strip calm, blank, and low-noise so exact post-processing text can sit cleanly in or near {primary}.")
        return " ".join(instructions)

    @staticmethod
    def _story_role_visual_execution_guidance(story_role: str, *, has_cta: bool = False) -> str:
        # Builds story role visual execution guidance from story role and has cta for AIOrchestratorService.
        # It calls _normalize_metadata_text while assembling the payload or prompt text.
        normalized = AIOrchestratorService._normalize_metadata_text(story_role, limit=48).casefold().replace(" ", "_")
        if normalized in {"hook", "cover", "opening", "title"}:
            return (
                "Execution guidance for this hook: use one bold focal metaphor with generous spread and clear negative space; let the image lead, not a stack of generic icon badges."
            )
        if normalized in {"structure", "deal_structure", "what_happened", "mechanics", "breakdown"}:
            return (
                "Execution guidance for this structure slide: use a mechanism-led composition such as segmented comparisons, process logic, layered explainer modules, or one strong analytical diagram anchored to the approved facts."
            )
        if normalized in {"undercovered_angle", "missed_angle", "angle"}:
            return (
                "Execution guidance for this undercovered-angle slide: use evidence-led objects, sector-specific symbols, hidden-layer reveals, document fragments, or magnified detail rather than a generic business scene."
            )
        if normalized in {"strategic_meaning", "implications", "implication", "analysis", "what_matters"}:
            return (
                "Execution guidance for this strategic-meaning slide: show systems, networks, balance, ripple effects, or outcome pathways so the visual explains second-order meaning rather than the headline event."
            )
        if normalized in {"takeaway", "close", "closing", "cta", "final"}:
            closing_tail = " and keep the CTA integrated but restrained." if has_cta else "."
            return (
                "Execution guidance for this closing slide: use an action-oriented product or decision-support surface, not a ceremonial or generic symbolic illustration"
                + closing_tail
            )
        return (
            "Execution guidance: choose a distinct explanatory composition that matches the approved slide role and advances the sequence instead of repeating the previous slide's visual grammar."
        )

    @staticmethod
    def _is_non_retryable_image_error(error: Exception) -> bool:
        # Checks non retryable image error from error for AIOrchestratorService.
        # This keeps the allowed/blocked rule in one place.
        text = str(error or "").strip().lower()
        return any(
            marker in text
            for marker in (
                "image_generation_user_error",
                "invalid_input_fidelity_model",
                "input_fidelity",
                "not supported for",
                "quality is not supported",
            )
        )

    @staticmethod
    def _compact_typography_summary(visual_identity: Any) -> str:
        # Compacts typography summary from visual identity for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(visual_identity, dict):
            return "Follow the validated brand typography."
        typography = visual_identity.get("typography") or visual_identity.get("typography_guide") or {}
        if isinstance(typography, dict):
            families = typography.get("font_families") or typography.get("font_family") or typography.get("families")
            family_text = AIOrchestratorService._compact_named_items(families, limit=3)
            if family_text != "None":
                return family_text
        return "Follow the validated brand typography."

    @staticmethod
    def _compact_layout_decision(layout_decision: Any) -> str:
        # Compacts layout decision from layout decision for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(layout_decision, dict):
            return "Synthesized layout"
        mode = AIOrchestratorService._normalize_metadata_text(layout_decision.get("mode"), limit=32) or "Synthesized layout"
        template_name = AIOrchestratorService._normalize_metadata_text(
            layout_decision.get("template_name") or layout_decision.get("selected_template_name"),
            limit=80,
        )
        reasons = AIOrchestratorService._compact_named_items(layout_decision.get("reasons"), limit=2)
        parts = [mode]
        if template_name:
            parts.append(f"template {template_name}")
        if reasons != "None":
            parts.append(f"because {reasons}")
        return ", ".join(parts)

    @staticmethod
    def _compact_reference_assets(reference_assets: Any) -> str:
        # Compacts reference assets from reference assets for AIOrchestratorService.
        # The helper owns a small rule that would distract from the surrounding flow.
        if not isinstance(reference_assets, list):
            return "None"
        labels: list[str] = []
        for asset in reference_assets[:4]:
            if isinstance(asset, dict):
                label = AIOrchestratorService._reference_asset_display_label(asset)
                mime_type = AIOrchestratorService._normalize_metadata_text(asset.get("mime_type"), limit=32)
                if mime_type and mime_type not in label:
                    label = f"{label} ({mime_type})"
            else:
                label = asset
            normalized = AIOrchestratorService._normalize_metadata_text(label, limit=60)
            if normalized:
                labels.append(normalized)
        return ", ".join(labels) if labels else "None"

    @staticmethod
    def _brand_knowledge_visual_grounding(knowledge_brief: Any) -> str:
        # Centralizes brand knowledge visual grounding from knowledge brief for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        brief = ContextCompilerService.coerce_visual_knowledge_brief(knowledge_brief)
        items = brief.get("items") if isinstance(brief.get("items"), list) else []
        grounded_items: list[str] = []
        for item in items[:4]:
            if not isinstance(item, dict):
                continue
            channel = AIOrchestratorService._normalize_metadata_text(item.get("channel"), limit=32).lower()
            content = AIOrchestratorService._normalize_metadata_text(item.get("content"), limit=180)
            if channel and content:
                # Prevent RAG snippets from injecting brand-asset trigger words into the image prompt.
                content_safe = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(content, replacement="reserved space")
                channel_safe = AIOrchestratorService._scrub_image_prompt_brand_mark_triggers(channel, replacement="reserved space")
                grounded_items.append(f"{channel_safe}: {content_safe}")
        return " | ".join(grounded_items)

    @staticmethod
    def _trim_prompt(value: str, limit: int) -> str:
        # Trims trim prompt from input value and limit for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        text = " ".join(value.split())
        if len(text) <= limit:
            return text
        trimmed = text[: limit - 3].rstrip(" ,.;:")
        return f"{trimmed}..."

    @staticmethod
    def estimate_token_usage(input_segments: list[str], output_segments: list[str]) -> dict:
        # Extracts estimate token usage from input segments and output segments for AIOrchestratorService.
        # It calls _estimate_tokens to turn raw evidence into the structured signal the caller needs.
        input_tokens = sum(AIOrchestratorService._estimate_tokens(segment) for segment in input_segments)
        output_tokens = sum(AIOrchestratorService._estimate_tokens(segment) for segment in output_segments)
        return {
            "input_tokens": input_tokens,
            "output_tokens": output_tokens,
            "total_tokens": input_tokens + output_tokens,
            "estimated": True,
        }

    @staticmethod
    def _estimate_tokens(value: str | None) -> int:
        # Centralizes estimate tokens from input value for AIOrchestratorService.
        # The main branch stays readable while this function handles the local edge case.
        if not value:
            return 0
        return max(1, (len(value.strip()) + 3) // 4)

    @staticmethod
    def _trace_text_token_measurement(label: str, value: Any) -> dict[str, Any]:
        # Extracts trace text token measurement from label and input value for AIOrchestratorService.
        # It calls _estimate_tokens to turn raw evidence into the structured signal the caller needs.
        text = str(value or "")
        return {
            "label": label,
            "chars": len(text),
            "lines": text.count("\n") + 1 if text else 0,
            "estimated_tokens": AIOrchestratorService._estimate_tokens(text),
            "estimated": True,
        }

    @classmethod
    def _trace_prompt_token_measurement(cls, segments: dict[str, Any]) -> dict[str, Any]:
        # Builds trace prompt token measurement from segments for AIOrchestratorService.
        # It calls _trace_text_token_measurement while assembling the payload or prompt text.
        measured_segments = [
            cls._trace_text_token_measurement(label, value)
            for label, value in segments.items()
            if str(value or "")
        ]
        return {
            "segments": measured_segments,
            "total_chars": sum(int(item["chars"]) for item in measured_segments),
            "estimated_tokens": sum(int(item["estimated_tokens"]) for item in measured_segments),
            "estimated": True,
        }

    @classmethod
    def _trace_mapping_token_measurement(
        cls,
        mapping: dict[str, Any] | None,
        *,
        limit: int = 20,
    ) -> dict[str, Any]:
        # Extracts trace mapping token measurement from mapping and limit for AIOrchestratorService.
        # It calls _estimate_tokens to turn raw evidence into the structured signal the caller needs.
        if not isinstance(mapping, dict) or not mapping:
            return {"sections": [], "total_chars": 0, "estimated_tokens": 0, "estimated": True}
        sections: list[dict[str, Any]] = []
        total_chars = 0
        total_tokens = 0
        for key, value in mapping.items():
            try:
                serialized = json.dumps(value, ensure_ascii=False, default=str, separators=(",", ":"))
            except TypeError:
                serialized = str(value)
            chars = len(serialized)
            tokens = cls._estimate_tokens(serialized)
            total_chars += chars
            total_tokens += tokens
            sections.append(
                {
                    "section": str(key),
                    "chars": chars,
                    "estimated_tokens": tokens,
                }
            )
        sections.sort(key=lambda item: int(item["chars"]), reverse=True)
        return {
            "sections": sections[: max(int(limit), 1)],
            "total_chars": total_chars,
            "estimated_tokens": total_tokens,
            "estimated": True,
        }

    @classmethod
    def _prompt_length_metrics(cls, prompt: str | None) -> dict[str, int]:
        # Builds prompt length metrics from prompt text for AIOrchestratorService.
        # It calls _estimate_tokens while assembling the payload or prompt text.
        text = str(prompt or "")
        char_count = len(text)
        return {
            "prompt_length": char_count,
            "prompt_length_chars": char_count,
            "estimated_prompt_tokens": cls._estimate_tokens(text),
            "prompt_word_count": len(text.split()) if text.strip() else 0,
        }
